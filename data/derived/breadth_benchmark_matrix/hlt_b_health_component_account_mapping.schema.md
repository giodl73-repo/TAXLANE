# HLT-B Health Component-to-Account Mapping Schema

This record closes `HLT-B-01` by assigning six bounded health components to
shared account roles. Evidence state, value presence, sign preservation, and
non-additivity remain separate dimensions. Every incomplete adapter names
`HLT-B-02` and its blocking gate. Completion does not create annual schedules
or solver inputs.
