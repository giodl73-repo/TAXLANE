# NET Level-4 endogenous-dependency audit schema

This record audits baseline, rate custody, upstream primary paths, maturity,
annual effective rates, borrowing timing, interest receipts, other financing,
and iterative feedback. Zero-input identity passage does not authorize a
nonzero result.

Every nonzero NET output remains blocked until all conjunctive dependencies are
ready and the upstream primary path is admitted.
