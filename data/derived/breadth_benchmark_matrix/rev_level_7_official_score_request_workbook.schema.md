# REV Level 7 official-score request workbook schema

The workbook contains exactly FY2026 through FY2035 and keeps every official
score field null until an external response is received. It separates the
Taxlane model proxy from official values; requests distribution, behavior,
macro, administration, compliance, and debt outputs; defines primary and total
deficit identities; and states that readiness is not submission.
