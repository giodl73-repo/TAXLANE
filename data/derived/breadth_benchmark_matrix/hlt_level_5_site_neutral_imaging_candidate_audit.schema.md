# HLT Level 5 site-neutral imaging candidate audit schema

This record replaces the rejected uniform Medicare Advantage benchmark envelope
with a narrower CBO-scored candidate. Selection for audit is not package
admission. The record must retain the official score vintage, enumerate every
candidate-specific floor, use zero for admitted savings until all required
gates pass, and leave blocked outputs null.
