# NET Level-3 zero-input endogenous-recomputation schema

This record applies the net-interest identity only to admitted upstream primary
paths. When none exists, the only eligible recomputation is a zero-input
identity fixture. A latest average rate may support a labeled mechanical
sensitivity but not a fiscal-year or multi-year savings estimate.

NET may never be entered as a direct cut or independent package candidate.
