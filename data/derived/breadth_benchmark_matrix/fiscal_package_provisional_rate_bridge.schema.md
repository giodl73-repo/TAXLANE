# Fiscal-package provisional-rate bridge schema

This bridge may publish a provisional modeled rate only with its model, data,
period, behavior assumption, range, and target alignment. It must keep the
assigned rate null until every fiscal-assignment gate passes.
