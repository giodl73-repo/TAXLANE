# REV Level-2 formal-rate gate-audit schema

This audit distinguishes a reproducible mechanical rate sensitivity from a
properly scored and assignable rate. Every required gate is conjunctive.

The scoring specification must preserve tax-year liability, fiscal-year cash,
behavior, distribution, administration, interactions, and macro outputs as
separate fields. Missing outputs remain null.
