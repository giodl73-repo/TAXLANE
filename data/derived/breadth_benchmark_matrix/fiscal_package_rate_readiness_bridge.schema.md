# Fiscal-package rate-readiness bridge schema

This bridge publishes which planning sensitivity matches the admitted package
and whether a proper rate is ready. Superseded, mechanical, formally scored,
and assigned schedules must remain separate.

An active mechanical sensitivity may define the next scoring target, but it may
not be presented as a tax proposal, assigned rate, or balanced-budget result.
