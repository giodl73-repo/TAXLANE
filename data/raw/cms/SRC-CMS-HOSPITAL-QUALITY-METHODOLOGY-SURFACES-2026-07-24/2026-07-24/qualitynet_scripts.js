!function() {
  "use strict";
  var e = function(e2) {
    if (null === e2) return "null";
    if (void 0 === e2) return "undefined";
    var t2 = typeof e2;
    return "object" === t2 && (Array.prototype.isPrototypeOf(e2) || e2.constructor && "Array" === e2.constructor.name) ? "array" : "object" === t2 && (String.prototype.isPrototypeOf(e2) || e2.constructor && "String" === e2.constructor.name) ? "string" : t2;
  }, t = function(e2) {
    return { eq: e2 };
  }, n = t(function(e2, t2) {
    return e2 === t2;
  }), o = function(e2) {
    return t(function(t2, n2) {
      if (t2.length !== n2.length) return false;
      for (var o2 = t2.length, r2 = 0; r2 < o2; r2++) if (!e2.eq(t2[r2], n2[r2])) return false;
      return true;
    });
  }, r = function(e2) {
    return t(function(r2, s2) {
      var a2 = Object.keys(r2), i3 = Object.keys(s2);
      if (!function(e3, n2) {
        return function(e4, n3) {
          return t(function(t2, o2) {
            return e4.eq(n3(t2), n3(o2));
          });
        }(o(e3), function(e4) {
          return function(e5, t2) {
            return Array.prototype.slice.call(e5).sort(t2);
          }(e4, n2);
        });
      }(n).eq(a2, i3)) return false;
      for (var l2 = a2.length, d2 = 0; d2 < l2; d2++) {
        var c2 = a2[d2];
        if (!e2.eq(r2[c2], s2[c2])) return false;
      }
      return true;
    });
  }, s = t(function(t2, n2) {
    if (t2 === n2) return true;
    var a2 = e(t2);
    return a2 === e(n2) && (function(e2) {
      return -1 !== ["undefined", "boolean", "number", "string", "function", "xml", "null"].indexOf(e2);
    }(a2) ? t2 === n2 : "array" === a2 ? o(s).eq(t2, n2) : "object" === a2 && r(s).eq(t2, n2));
  });
  const a = Object.getPrototypeOf, i2 = (e2, t2, n2) => {
    var o2;
    return !!n2(e2, t2.prototype) || (null === (o2 = e2.constructor) || void 0 === o2 ? void 0 : o2.name) === t2.name;
  }, l = (e2) => (t2) => ((e3) => {
    const t3 = typeof e3;
    return null === e3 ? "null" : "object" === t3 && Array.isArray(e3) ? "array" : "object" === t3 && i2(e3, String, (e4, t4) => t4.isPrototypeOf(e4)) ? "string" : t3;
  })(t2) === e2, d = (e2) => (t2) => typeof t2 === e2, c = (e2) => (t2) => e2 === t2, u = (e2, t2) => f(e2) && i2(e2, t2, (e3, t3) => a(e3) === t3), m = l("string"), f = l("object"), g = (e2) => u(e2, Object), p = l("array"), h = c(null), b = d("boolean"), v = c(void 0), y = (e2) => null == e2, C = (e2) => !y(e2), w = d("function"), x = d("number"), k = (e2, t2) => {
    if (p(e2)) {
      for (let n2 = 0, o2 = e2.length; n2 < o2; ++n2) if (!t2(e2[n2])) return false;
      return true;
    }
    return false;
  }, S = () => {
  }, _ = (e2, t2) => (...n2) => e2(t2.apply(null, n2)), E = (e2, t2) => (n2) => e2(t2(n2)), N = (e2) => () => e2, R = (e2) => e2, A = (e2, t2) => e2 === t2;
  function O(e2, ...t2) {
    return (...n2) => {
      const o2 = t2.concat(n2);
      return e2.apply(null, o2);
    };
  }
  const T = (e2) => (t2) => !e2(t2), B = (e2) => e2(), D = (e2) => {
    e2();
  }, P = N(false), L = N(true);
  class M {
    constructor(e2, t2) {
      this.tag = e2, this.value = t2;
    }
    static some(e2) {
      return new M(true, e2);
    }
    static none() {
      return M.singletonNone;
    }
    fold(e2, t2) {
      return this.tag ? t2(this.value) : e2();
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(e2) {
      return this.tag ? M.some(e2(this.value)) : M.none();
    }
    bind(e2) {
      return this.tag ? e2(this.value) : M.none();
    }
    exists(e2) {
      return this.tag && e2(this.value);
    }
    forall(e2) {
      return !this.tag || e2(this.value);
    }
    filter(e2) {
      return !this.tag || e2(this.value) ? this : M.none();
    }
    getOr(e2) {
      return this.tag ? this.value : e2;
    }
    or(e2) {
      return this.tag ? this : e2;
    }
    getOrThunk(e2) {
      return this.tag ? this.value : e2();
    }
    orThunk(e2) {
      return this.tag ? this : e2();
    }
    getOrDie(e2) {
      if (this.tag) return this.value;
      throw new Error(null != e2 ? e2 : "Called getOrDie on None");
    }
    static from(e2) {
      return C(e2) ? M.some(e2) : M.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(e2) {
      this.tag && e2(this.value);
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  M.singletonNone = new M(false);
  const I = Array.prototype.slice, F = Array.prototype.indexOf, U = Array.prototype.push, z = (e2, t2) => F.call(e2, t2), j = (e2, t2) => z(e2, t2) > -1, H = (e2, t2) => {
    for (let n2 = 0, o2 = e2.length; n2 < o2; n2++) if (t2(e2[n2], n2)) return true;
    return false;
  }, $2 = (e2, t2) => {
    const n2 = e2.length, o2 = new Array(n2);
    for (let r2 = 0; r2 < n2; r2++) {
      const n3 = e2[r2];
      o2[r2] = t2(n3, r2);
    }
    return o2;
  }, V = (e2, t2) => {
    for (let n2 = 0, o2 = e2.length; n2 < o2; n2++) t2(e2[n2], n2);
  }, q = (e2, t2) => {
    for (let n2 = e2.length - 1; n2 >= 0; n2--) t2(e2[n2], n2);
  }, W = (e2, t2) => {
    const n2 = [], o2 = [];
    for (let r2 = 0, s2 = e2.length; r2 < s2; r2++) {
      const s3 = e2[r2];
      (t2(s3, r2) ? n2 : o2).push(s3);
    }
    return { pass: n2, fail: o2 };
  }, K = (e2, t2) => {
    const n2 = [];
    for (let o2 = 0, r2 = e2.length; o2 < r2; o2++) {
      const r3 = e2[o2];
      t2(r3, o2) && n2.push(r3);
    }
    return n2;
  }, G = (e2, t2, n2) => (q(e2, (e3, o2) => {
    n2 = t2(n2, e3, o2);
  }), n2), Y = (e2, t2, n2) => (V(e2, (e3, o2) => {
    n2 = t2(n2, e3, o2);
  }), n2), X = (e2, t2, n2) => {
    for (let o2 = 0, r2 = e2.length; o2 < r2; o2++) {
      const r3 = e2[o2];
      if (t2(r3, o2)) return M.some(r3);
      if (n2(r3, o2)) break;
    }
    return M.none();
  }, Q = (e2, t2) => X(e2, t2, P), J = (e2, t2) => {
    for (let n2 = 0, o2 = e2.length; n2 < o2; n2++) if (t2(e2[n2], n2)) return M.some(n2);
    return M.none();
  }, Z = (e2) => {
    const t2 = [];
    for (let n2 = 0, o2 = e2.length; n2 < o2; ++n2) {
      if (!p(e2[n2])) throw new Error("Arr.flatten item " + n2 + " was not an array, input: " + e2);
      U.apply(t2, e2[n2]);
    }
    return t2;
  }, ee = (e2, t2) => Z($2(e2, t2)), te = (e2, t2) => {
    for (let n2 = 0, o2 = e2.length; n2 < o2; ++n2) if (true !== t2(e2[n2], n2)) return false;
    return true;
  }, ne = (e2) => {
    const t2 = I.call(e2, 0);
    return t2.reverse(), t2;
  }, oe = (e2, t2) => K(e2, (e3) => !j(t2, e3)), re = (e2, t2) => {
    const n2 = {};
    for (let o2 = 0, r2 = e2.length; o2 < r2; o2++) {
      const r3 = e2[o2];
      n2[String(r3)] = t2(r3, o2);
    }
    return n2;
  }, se = (e2, t2) => {
    const n2 = I.call(e2, 0);
    return n2.sort(t2), n2;
  }, ae = (e2, t2) => t2 >= 0 && t2 < e2.length ? M.some(e2[t2]) : M.none(), ie = (e2) => ae(e2, 0), le = (e2) => ae(e2, e2.length - 1), de = w(Array.from) ? Array.from : (e2) => I.call(e2), ce = (e2, t2) => {
    for (let n2 = 0; n2 < e2.length; n2++) {
      const o2 = t2(e2[n2], n2);
      if (o2.isSome()) return o2;
    }
    return M.none();
  }, ue = Object.keys, me = Object.hasOwnProperty, fe = (e2, t2) => {
    const n2 = ue(e2);
    for (let o2 = 0, r2 = n2.length; o2 < r2; o2++) {
      const r3 = n2[o2];
      t2(e2[r3], r3);
    }
  }, ge = (e2, t2) => pe(e2, (e3, n2) => ({ k: n2, v: t2(e3, n2) })), pe = (e2, t2) => {
    const n2 = {};
    return fe(e2, (e3, o2) => {
      const r2 = t2(e3, o2);
      n2[r2.k] = r2.v;
    }), n2;
  }, he = (e2) => (t2, n2) => {
    e2[n2] = t2;
  }, be = (e2, t2, n2, o2) => {
    fe(e2, (e3, r2) => {
      (t2(e3, r2) ? n2 : o2)(e3, r2);
    });
  }, ve = (e2, t2) => {
    const n2 = {};
    return be(e2, t2, he(n2), S), n2;
  }, ye = (e2, t2) => {
    const n2 = [];
    return fe(e2, (e3, o2) => {
      n2.push(t2(e3, o2));
    }), n2;
  }, Ce = (e2) => ye(e2, R), we = (e2, t2) => xe(e2, t2) ? M.from(e2[t2]) : M.none(), xe = (e2, t2) => me.call(e2, t2), ke = (e2, t2) => xe(e2, t2) && void 0 !== e2[t2] && null !== e2[t2], Se = (e2) => {
    const t2 = {};
    return V(e2, (e3) => {
      t2[e3] = {};
    }), ue(t2);
  }, _e = (e2) => void 0 !== e2.length, Ee = Array.isArray, Ne = (e2, t2, n2) => {
    if (!e2) return false;
    if (n2 = n2 || e2, _e(e2)) {
      for (let o2 = 0, r2 = e2.length; o2 < r2; o2++) if (false === t2.call(n2, e2[o2], o2, e2)) return false;
    } else for (const o2 in e2) if (xe(e2, o2) && false === t2.call(n2, e2[o2], o2, e2)) return false;
    return true;
  }, Re = (e2, t2) => {
    const n2 = [];
    return Ne(e2, (o2, r2) => {
      n2.push(t2(o2, r2, e2));
    }), n2;
  }, Ae = (e2, t2) => {
    const n2 = [];
    return Ne(e2, (o2, r2) => {
      t2 && !t2(o2, r2, e2) || n2.push(o2);
    }), n2;
  }, Oe = (e2, t2, n2, o2) => {
    let r2 = v(n2) ? e2[0] : n2;
    for (let n3 = 0; n3 < e2.length; n3++) r2 = t2.call(o2, r2, e2[n3], n3);
    return r2;
  }, Te = (e2, t2, n2) => {
    for (let o2 = 0, r2 = e2.length; o2 < r2; o2++) if (t2.call(n2, e2[o2], o2, e2)) return o2;
    return -1;
  }, Be = (e2) => e2[e2.length - 1], De = (e2) => {
    let t2, n2 = false;
    return (...o2) => (n2 || (n2 = true, t2 = e2.apply(null, o2)), t2);
  }, Pe = () => Le(0, 0), Le = (e2, t2) => ({ major: e2, minor: t2 }), Me = { nu: Le, detect: (e2, t2) => {
    const n2 = String(t2).toLowerCase();
    return 0 === e2.length ? Pe() : ((e3, t3) => {
      const n3 = ((e4, t4) => {
        for (let n4 = 0; n4 < e4.length; n4++) {
          const o3 = e4[n4];
          if (o3.test(t4)) return o3;
        }
      })(e3, t3);
      if (!n3) return { major: 0, minor: 0 };
      const o2 = (e4) => Number(t3.replace(n3, "$" + e4));
      return Le(o2(1), o2(2));
    })(e2, n2);
  }, unknown: Pe }, Ie = (e2, t2) => {
    const n2 = String(t2).toLowerCase();
    return Q(e2, (e3) => e3.search(n2));
  }, Fe = (e2, t2, n2) => "" === t2 || e2.length >= t2.length && e2.substr(n2, n2 + t2.length) === t2, Ue = (e2, t2, n2 = 0, o2) => {
    const r2 = e2.indexOf(t2, n2);
    return -1 !== r2 && (!!v(o2) || r2 + t2.length <= o2);
  }, ze = (e2, t2) => Fe(e2, t2, 0), je = (e2, t2) => Fe(e2, t2, e2.length - t2.length), He = (e2) => (t2) => t2.replace(e2, ""), $e = He(/^\s+|\s+$/g), Ve = He(/^\s+/g), qe = He(/\s+$/g), We = (e2) => e2.length > 0, Ke = (e2) => !We(e2), Ge = (e2, t2 = 10) => {
    const n2 = parseInt(e2, t2);
    return isNaN(n2) ? M.none() : M.some(n2);
  }, Ye = /.*?version\/\ ?([0-9]+)\.([0-9]+).*/, Xe = (e2) => (t2) => Ue(t2, e2), Qe = [{ name: "Edge", versionRegexes: [/.*?edge\/ ?([0-9]+)\.([0-9]+)$/], search: (e2) => Ue(e2, "edge/") && Ue(e2, "chrome") && Ue(e2, "safari") && Ue(e2, "applewebkit") }, { name: "Chromium", brand: "Chromium", versionRegexes: [/.*?chrome\/([0-9]+)\.([0-9]+).*/, Ye], search: (e2) => Ue(e2, "chrome") && !Ue(e2, "chromeframe") }, { name: "IE", versionRegexes: [/.*?msie\ ?([0-9]+)\.([0-9]+).*/, /.*?rv:([0-9]+)\.([0-9]+).*/], search: (e2) => Ue(e2, "msie") || Ue(e2, "trident") }, { name: "Opera", versionRegexes: [Ye, /.*?opera\/([0-9]+)\.([0-9]+).*/], search: Xe("opera") }, { name: "Firefox", versionRegexes: [/.*?firefox\/\ ?([0-9]+)\.([0-9]+).*/], search: Xe("firefox") }, { name: "Safari", versionRegexes: [Ye, /.*?cpu os ([0-9]+)_([0-9]+).*/], search: (e2) => (Ue(e2, "safari") || Ue(e2, "mobile/")) && Ue(e2, "applewebkit") }], Je = [{ name: "Windows", search: Xe("win"), versionRegexes: [/.*?windows\ nt\ ?([0-9]+)\.([0-9]+).*/] }, { name: "iOS", search: (e2) => Ue(e2, "iphone") || Ue(e2, "ipad"), versionRegexes: [/.*?version\/\ ?([0-9]+)\.([0-9]+).*/, /.*cpu os ([0-9]+)_([0-9]+).*/, /.*cpu iphone os ([0-9]+)_([0-9]+).*/] }, { name: "Android", search: Xe("android"), versionRegexes: [/.*?android\ ?([0-9]+)\.([0-9]+).*/] }, { name: "macOS", search: Xe("mac os x"), versionRegexes: [/.*?mac\ os\ x\ ?([0-9]+)_([0-9]+).*/] }, { name: "Linux", search: Xe("linux"), versionRegexes: [] }, { name: "Solaris", search: Xe("sunos"), versionRegexes: [] }, { name: "FreeBSD", search: Xe("freebsd"), versionRegexes: [] }, { name: "ChromeOS", search: Xe("cros"), versionRegexes: [/.*?chrome\/([0-9]+)\.([0-9]+).*/] }], Ze = { browsers: N(Qe), oses: N(Je) }, et = "Edge", tt = "Chromium", nt = "Opera", ot = "Firefox", rt = "Safari", st = (e2) => {
    const t2 = e2.current, n2 = e2.version, o2 = (e3) => () => t2 === e3;
    return { current: t2, version: n2, isEdge: o2(et), isChromium: o2(tt), isIE: o2("IE"), isOpera: o2(nt), isFirefox: o2(ot), isSafari: o2(rt) };
  }, at = () => st({ current: void 0, version: Me.unknown() }), it = st, lt = (N(et), N(tt), N("IE"), N(nt), N(ot), N(rt), "Windows"), dt = "Android", ct = "Linux", ut = "macOS", mt = "Solaris", ft = "FreeBSD", gt = "ChromeOS", pt = (e2) => {
    const t2 = e2.current, n2 = e2.version, o2 = (e3) => () => t2 === e3;
    return { current: t2, version: n2, isWindows: o2(lt), isiOS: o2("iOS"), isAndroid: o2(dt), isMacOS: o2(ut), isLinux: o2(ct), isSolaris: o2(mt), isFreeBSD: o2(ft), isChromeOS: o2(gt) };
  }, ht = () => pt({ current: void 0, version: Me.unknown() }), bt = pt, vt = (N(lt), N("iOS"), N(dt), N(ct), N(ut), N(mt), N(ft), N(gt), (e2) => window.matchMedia(e2).matches);
  let yt = De(() => ((e2, t2, n2) => {
    const o2 = Ze.browsers(), r2 = Ze.oses(), s2 = t2.bind((e3) => ((e4, t3) => ce(t3.brands, (t4) => {
      const n3 = t4.brand.toLowerCase();
      return Q(e4, (e5) => {
        var t5;
        return n3 === (null === (t5 = e5.brand) || void 0 === t5 ? void 0 : t5.toLowerCase());
      }).map((e5) => ({ current: e5.name, version: Me.nu(parseInt(t4.version, 10), 0) }));
    }))(o2, e3)).orThunk(() => ((e3, t3) => Ie(e3, t3).map((e4) => {
      const n3 = Me.detect(e4.versionRegexes, t3);
      return { current: e4.name, version: n3 };
    }))(o2, e2)).fold(at, it), a2 = ((e3, t3) => Ie(e3, t3).map((e4) => {
      const n3 = Me.detect(e4.versionRegexes, t3);
      return { current: e4.name, version: n3 };
    }))(r2, e2).fold(ht, bt), i3 = ((e3, t3, n3, o3) => {
      const r3 = e3.isiOS() && true === /ipad/i.test(n3), s3 = e3.isiOS() && !r3, a3 = e3.isiOS() || e3.isAndroid(), i4 = a3 || o3("(pointer:coarse)"), l2 = r3 || !s3 && a3 && o3("(min-device-width:768px)"), d2 = s3 || a3 && !l2, c2 = t3.isSafari() && e3.isiOS() && false === /safari/i.test(n3), u2 = !d2 && !l2 && !c2;
      return { isiPad: N(r3), isiPhone: N(s3), isTablet: N(l2), isPhone: N(d2), isTouch: N(i4), isAndroid: e3.isAndroid, isiOS: e3.isiOS, isWebView: N(c2), isDesktop: N(u2) };
    })(a2, s2, e2, n2);
    return { browser: s2, os: a2, deviceType: i3 };
  })(navigator.userAgent, M.from(navigator.userAgentData), vt));
  const Ct = () => yt(), wt = navigator.userAgent, xt = Ct(), kt = xt.browser, St = xt.os, _t = xt.deviceType, Et = -1 !== wt.indexOf("Windows Phone"), Nt = { transparentSrc: "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7", documentMode: kt.isIE() ? document.documentMode || 7 : 10, cacheSuffix: null, container: null, canHaveCSP: !kt.isIE(), windowsPhone: Et, browser: { current: kt.current, version: kt.version, isChromium: kt.isChromium, isEdge: kt.isEdge, isFirefox: kt.isFirefox, isIE: kt.isIE, isOpera: kt.isOpera, isSafari: kt.isSafari }, os: { current: St.current, version: St.version, isAndroid: St.isAndroid, isChromeOS: St.isChromeOS, isFreeBSD: St.isFreeBSD, isiOS: St.isiOS, isLinux: St.isLinux, isMacOS: St.isMacOS, isSolaris: St.isSolaris, isWindows: St.isWindows }, deviceType: { isDesktop: _t.isDesktop, isiPad: _t.isiPad, isiPhone: _t.isiPhone, isPhone: _t.isPhone, isTablet: _t.isTablet, isTouch: _t.isTouch, isWebView: _t.isWebView } }, Rt = /^\s*|\s*$/g, At = (e2) => y(e2) ? "" : ("" + e2).replace(Rt, ""), Ot = function(e2, t2, n2, o2) {
    o2 = o2 || this, e2 && (n2 && (e2 = e2[n2]), Ne(e2, (e3, r2) => false !== t2.call(o2, e3, r2, n2) && (Ot(e3, t2, n2, o2), true)));
  }, Tt = { trim: At, isArray: Ee, is: (e2, t2) => t2 ? !("array" !== t2 || !Ee(e2)) || typeof e2 === t2 : void 0 !== e2, toArray: (e2) => {
    if (Ee(e2)) return e2;
    {
      const t2 = [];
      for (let n2 = 0, o2 = e2.length; n2 < o2; n2++) t2[n2] = e2[n2];
      return t2;
    }
  }, makeMap: (e2, t2, n2 = {}) => {
    const o2 = m(e2) ? e2.split(t2 || ",") : e2 || [];
    let r2 = o2.length;
    for (; r2--; ) n2[o2[r2]] = {};
    return n2;
  }, each: Ne, map: Re, grep: Ae, inArray: (e2, t2) => {
    if (e2) {
      for (let n2 = 0, o2 = e2.length; n2 < o2; n2++) if (e2[n2] === t2) return n2;
    }
    return -1;
  }, hasOwn: xe, extend: (e2, ...t2) => {
    for (let n2 = 0; n2 < t2.length; n2++) {
      const o2 = t2[n2];
      for (const t3 in o2) if (xe(o2, t3)) {
        const n3 = o2[t3];
        void 0 !== n3 && (e2[t3] = n3);
      }
    }
    return e2;
  }, walk: Ot, resolve: (e2, t2 = window) => {
    const n2 = e2.split(".");
    for (let e3 = 0, o2 = n2.length; e3 < o2 && (t2 = t2[n2[e3]]); e3++) ;
    return t2;
  }, explode: (e2, t2) => p(e2) ? e2 : "" === e2 ? [] : Re(e2.split(t2 || ","), At), _addCacheSuffix: (e2) => {
    const t2 = Nt.cacheSuffix;
    return t2 && (e2 += (-1 === e2.indexOf("?") ? "?" : "&") + t2), e2;
  } }, Bt = (e2, t2, n2 = A) => e2.exists((e3) => n2(e3, t2)), Dt = (e2, t2, n2) => e2.isSome() && t2.isSome() ? M.some(n2(e2.getOrDie(), t2.getOrDie())) : M.none(), Pt = (e2, t2) => e2 ? M.some(t2) : M.none();
  "undefined" != typeof window ? window : Function("return this;")();
  const Lt = (e2) => e2.dom.nodeName.toLowerCase(), Mt = (e2) => e2.dom.nodeType, It = (e2) => (t2) => Mt(t2) === e2, Ft = It(1), Ut = It(3), zt = It(9), jt = It(11), Ht = (e2) => (t2) => Ft(t2) && Lt(t2) === e2, $t = (e2, t2, n2) => {
    if (!(m(n2) || b(n2) || x(n2))) throw console.error("Invalid call to Attribute.set. Key ", t2, ":: Value ", n2, ":: Element ", e2), new Error("Attribute value was not simple");
    e2.setAttribute(t2, n2 + "");
  }, Vt = (e2, t2, n2) => {
    $t(e2.dom, t2, n2);
  }, qt = (e2, t2) => {
    const n2 = e2.dom;
    fe(t2, (e3, t3) => {
      $t(n2, t3, e3);
    });
  }, Wt = (e2, t2) => {
    const n2 = e2.dom.getAttribute(t2);
    return null === n2 ? void 0 : n2;
  }, Kt = (e2, t2) => M.from(Wt(e2, t2)), Gt = (e2, t2) => {
    const n2 = e2.dom;
    return !(!n2 || !n2.hasAttribute) && n2.hasAttribute(t2);
  }, Yt = (e2, t2) => {
    e2.dom.removeAttribute(t2);
  }, Xt = (e2) => Y(e2.dom.attributes, (e3, t2) => (e3[t2.name] = t2.value, e3), {}), Qt = (e2, t2) => {
    const n2 = Wt(e2, t2);
    return void 0 === n2 || "" === n2 ? [] : n2.split(" ");
  }, Jt = (e2) => void 0 !== e2.dom.classList, Zt = (e2) => Qt(e2, "class"), en = (e2, t2) => ((e3, t3, n2) => {
    const o2 = Qt(e3, t3).concat([n2]);
    return Vt(e3, t3, o2.join(" ")), true;
  })(e2, "class", t2), tn = (e2, t2) => ((e3, t3, n2) => {
    const o2 = K(Qt(e3, t3), (e4) => e4 !== n2);
    return o2.length > 0 ? Vt(e3, t3, o2.join(" ")) : Yt(e3, t3), false;
  })(e2, "class", t2), nn = (e2, t2) => {
    Jt(e2) ? e2.dom.classList.add(t2) : en(e2, t2);
  }, on = (e2) => {
    0 === (Jt(e2) ? e2.dom.classList : Zt(e2)).length && Yt(e2, "class");
  }, rn = (e2, t2) => {
    Jt(e2) ? e2.dom.classList.remove(t2) : tn(e2, t2), on(e2);
  }, sn = (e2, t2) => Jt(e2) && e2.dom.classList.contains(t2), an = (e2) => void 0 !== e2.style && w(e2.style.getPropertyValue), ln = (e2) => {
    if (null == e2) throw new Error("Node cannot be null or undefined");
    return { dom: e2 };
  }, dn = (e2, t2) => {
    const n2 = (t2 || document).createElement("div");
    if (n2.innerHTML = e2, !n2.hasChildNodes() || n2.childNodes.length > 1) {
      const t3 = "HTML does not have a single root node";
      throw console.error(t3, e2), new Error(t3);
    }
    return ln(n2.childNodes[0]);
  }, cn = (e2, t2) => {
    const n2 = (t2 || document).createElement(e2);
    return ln(n2);
  }, un = (e2, t2) => {
    const n2 = (t2 || document).createTextNode(e2);
    return ln(n2);
  }, mn = ln, fn = (e2, t2, n2) => M.from(e2.dom.elementFromPoint(t2, n2)).map(ln), gn = (e2, t2) => {
    const n2 = [], o2 = (e3) => (n2.push(e3), t2(e3));
    let r2 = t2(e2);
    do {
      r2 = r2.bind(o2);
    } while (r2.isSome());
    return n2;
  }, pn = (e2, t2) => {
    const n2 = e2.dom;
    if (1 !== n2.nodeType) return false;
    {
      const e3 = n2;
      if (void 0 !== e3.matches) return e3.matches(t2);
      if (void 0 !== e3.msMatchesSelector) return e3.msMatchesSelector(t2);
      if (void 0 !== e3.webkitMatchesSelector) return e3.webkitMatchesSelector(t2);
      if (void 0 !== e3.mozMatchesSelector) return e3.mozMatchesSelector(t2);
      throw new Error("Browser lacks native selectors");
    }
  }, hn = (e2) => 1 !== e2.nodeType && 9 !== e2.nodeType && 11 !== e2.nodeType || 0 === e2.childElementCount, bn = (e2, t2) => e2.dom === t2.dom, vn = (e2, t2) => {
    const n2 = e2.dom, o2 = t2.dom;
    return n2 !== o2 && n2.contains(o2);
  }, yn = (e2) => mn(e2.dom.ownerDocument), Cn = (e2) => zt(e2) ? e2 : yn(e2), wn = (e2) => mn(Cn(e2).dom.defaultView), xn = (e2) => M.from(e2.dom.parentNode).map(mn), kn = (e2) => M.from(e2.dom.previousSibling).map(mn), Sn = (e2) => M.from(e2.dom.nextSibling).map(mn), _n = (e2) => ne(gn(e2, kn)), En = (e2) => gn(e2, Sn), Nn = (e2) => $2(e2.dom.childNodes, mn), Rn = (e2, t2) => {
    const n2 = e2.dom.childNodes;
    return M.from(n2[t2]).map(mn);
  }, An = (e2) => Rn(e2, 0), On = (e2) => Rn(e2, e2.dom.childNodes.length - 1), Tn = (e2) => e2.dom.childNodes.length, Bn = (e2) => jt(e2) && C(e2.dom.host), Dn = w(Element.prototype.attachShadow) && w(Node.prototype.getRootNode), Pn = N(Dn), Ln = Dn ? (e2) => mn(e2.dom.getRootNode()) : Cn, Mn = (e2) => Bn(e2) ? e2 : ((e3) => {
    const t2 = e3.dom.head;
    if (null == t2) throw new Error("Head is not available yet");
    return mn(t2);
  })(Cn(e2)), In = (e2) => mn(e2.dom.host), Fn = (e2) => {
    if (Pn() && C(e2.target)) {
      const t2 = mn(e2.target);
      if (Ft(t2) && Un(t2) && e2.composed && e2.composedPath) {
        const t3 = e2.composedPath();
        if (t3) return ie(t3);
      }
    }
    return M.from(e2.target);
  }, Un = (e2) => C(e2.dom.shadowRoot), zn = (e2) => {
    const t2 = Ut(e2) ? e2.dom.parentNode : e2.dom;
    if (null == t2 || null === t2.ownerDocument) return false;
    const n2 = t2.ownerDocument;
    return ((e3) => {
      const t3 = Ln(e3);
      return Bn(t3) ? M.some(t3) : M.none();
    })(mn(t2)).fold(() => n2.body.contains(t2), E(zn, In));
  }, jn = (e2, t2, n2) => {
    if (!m(n2)) throw console.error("Invalid call to CSS.set. Property ", t2, ":: Value ", n2, ":: Element ", e2), new Error("CSS value must be a string: " + n2);
    an(e2) && e2.style.setProperty(t2, n2);
  }, Hn = (e2, t2, n2) => {
    const o2 = e2.dom;
    jn(o2, t2, n2);
  }, $n = (e2, t2) => {
    const n2 = e2.dom;
    fe(t2, (e3, t3) => {
      jn(n2, t3, e3);
    });
  }, Vn = (e2, t2) => {
    const n2 = e2.dom, o2 = window.getComputedStyle(n2).getPropertyValue(t2);
    return "" !== o2 || zn(e2) ? o2 : qn(n2, t2);
  }, qn = (e2, t2) => an(e2) ? e2.style.getPropertyValue(t2) : "", Wn = (e2, t2) => {
    const n2 = e2.dom, o2 = qn(n2, t2);
    return M.from(o2).filter((e3) => e3.length > 0);
  }, Kn = (e2) => {
    const t2 = {}, n2 = e2.dom;
    if (an(n2)) for (let e3 = 0; e3 < n2.style.length; e3++) {
      const o2 = n2.style.item(e3);
      t2[o2] = n2.style[o2];
    }
    return t2;
  }, Gn = (e2, t2) => {
    ((e3, t3) => {
      an(e3) && e3.style.removeProperty(t3);
    })(e2.dom, t2), Bt(Kt(e2, "style").map($e), "") && Yt(e2, "style");
  }, Yn = (e2, t2) => {
    xn(e2).each((n2) => {
      n2.dom.insertBefore(t2.dom, e2.dom);
    });
  }, Xn = (e2, t2) => {
    Sn(e2).fold(() => {
      xn(e2).each((e3) => {
        Jn(e3, t2);
      });
    }, (e3) => {
      Yn(e3, t2);
    });
  }, Qn = (e2, t2) => {
    An(e2).fold(() => {
      Jn(e2, t2);
    }, (n2) => {
      e2.dom.insertBefore(t2.dom, n2.dom);
    });
  }, Jn = (e2, t2) => {
    e2.dom.appendChild(t2.dom);
  }, Zn = (e2, t2) => {
    V(t2, (t3) => {
      Jn(e2, t3);
    });
  }, eo = (e2) => {
    e2.dom.textContent = "", V(Nn(e2), (e3) => {
      to(e3);
    });
  }, to = (e2) => {
    const t2 = e2.dom;
    null !== t2.parentNode && t2.parentNode.removeChild(t2);
  }, no = (e2) => {
    const t2 = Nn(e2);
    var n2, o2;
    t2.length > 0 && (n2 = e2, V(o2 = t2, (e3, t3) => {
      const r2 = 0 === t3 ? n2 : o2[t3 - 1];
      Xn(r2, e3);
    })), to(e2);
  }, oo = (e2) => e2.dom.innerHTML, ro = (e2, t2) => {
    const n2 = yn(e2).dom, o2 = mn(n2.createDocumentFragment()), r2 = ((e3, t3) => {
      const n3 = (t3 || document).createElement("div");
      return n3.innerHTML = e3, Nn(mn(n3));
    })(t2, n2);
    Zn(o2, r2), eo(e2), Jn(e2, o2);
  }, so = (e2, t2, n2, o2) => ((e3, t3, n3, o3, r2) => {
    const s2 = /* @__PURE__ */ ((e4, t4) => (n4) => {
      e4(n4) && t4(((e5) => {
        const t5 = mn(Fn(e5).getOr(e5.target)), n5 = () => e5.stopPropagation(), o4 = () => e5.preventDefault(), r3 = _(o4, n5);
        return /* @__PURE__ */ ((e6, t6, n6, o5, r4, s3, a2) => ({ target: e6, x: t6, y: n6, stop: o5, prevent: r4, kill: s3, raw: a2 }))(t5, e5.clientX, e5.clientY, n5, o4, r3, e5);
      })(n4));
    })(n3, o3);
    return e3.dom.addEventListener(t3, s2, false), { unbind: O(ao, e3, t3, s2, false) };
  })(e2, t2, n2, o2), ao = (e2, t2, n2, o2) => {
    e2.dom.removeEventListener(t2, n2, o2);
  }, io = (e2, t2) => ({ left: e2, top: t2, translate: (n2, o2) => io(e2 + n2, t2 + o2) }), lo = io, co = (e2, t2) => void 0 !== e2 ? e2 : void 0 !== t2 ? t2 : 0, uo = (e2) => {
    const t2 = e2.dom, n2 = t2.ownerDocument.body;
    return n2 === t2 ? lo(n2.offsetLeft, n2.offsetTop) : zn(e2) ? ((e3) => {
      const t3 = e3.getBoundingClientRect();
      return lo(t3.left, t3.top);
    })(t2) : lo(0, 0);
  }, mo = (e2) => {
    const t2 = void 0 !== e2 ? e2.dom : document, n2 = t2.body.scrollLeft || t2.documentElement.scrollLeft, o2 = t2.body.scrollTop || t2.documentElement.scrollTop;
    return lo(n2, o2);
  }, fo = (e2, t2, n2) => {
    const o2 = (void 0 !== n2 ? n2.dom : document).defaultView;
    o2 && o2.scrollTo(e2, t2);
  }, go = (e2, t2) => {
    Ct().browser.isSafari() && w(e2.dom.scrollIntoViewIfNeeded) ? e2.dom.scrollIntoViewIfNeeded(false) : e2.dom.scrollIntoView(t2);
  }, po = (e2, t2, n2, o2) => ({ x: e2, y: t2, width: n2, height: o2, right: e2 + n2, bottom: t2 + o2 }), ho = (e2) => {
    const t2 = void 0 === e2 ? window : e2, n2 = t2.document, o2 = mo(mn(n2));
    return ((e3) => {
      const t3 = void 0 === e3 ? window : e3;
      return Ct().browser.isFirefox() ? M.none() : M.from(t3.visualViewport);
    })(t2).fold(() => {
      const e3 = t2.document.documentElement, n3 = e3.clientWidth, r2 = e3.clientHeight;
      return po(o2.left, o2.top, n3, r2);
    }, (e3) => po(Math.max(e3.pageLeft, o2.left), Math.max(e3.pageTop, o2.top), e3.width, e3.height));
  }, bo = (e2) => (t2) => !!t2 && t2.nodeType === e2, vo = (e2) => !!e2 && !Object.getPrototypeOf(e2), yo = bo(1), Co = (e2) => {
    const t2 = e2.toLowerCase();
    return (e3) => C(e3) && e3.nodeName.toLowerCase() === t2;
  }, wo = (e2) => {
    const t2 = e2.map((e3) => e3.toLowerCase());
    return (e3) => {
      if (e3 && e3.nodeName) {
        const n2 = e3.nodeName.toLowerCase();
        return j(t2, n2);
      }
      return false;
    };
  }, xo = (e2, t2) => {
    const n2 = t2.toLowerCase().split(" ");
    return (t3) => {
      if (yo(t3)) {
        const o2 = t3.ownerDocument.defaultView;
        if (o2) for (let r2 = 0; r2 < n2.length; r2++) {
          const s2 = o2.getComputedStyle(t3, null);
          if ((s2 ? s2.getPropertyValue(e2) : null) === n2[r2]) return true;
        }
      }
      return false;
    };
  }, ko = (e2) => (t2) => yo(t2) && t2.hasAttribute(e2), So = (e2) => yo(e2) && e2.hasAttribute("data-mce-bogus"), _o = (e2) => yo(e2) && "TABLE" === e2.tagName, Eo = (e2) => (t2) => {
    if (yo(t2)) {
      if (t2.contentEditable === e2) return true;
      if (t2.getAttribute("data-mce-contenteditable") === e2) return true;
    }
    return false;
  }, No = wo(["textarea", "input"]), Ro = bo(3), Ao = bo(4), Oo = bo(7), To = bo(8), Bo = bo(9), Do = bo(11), Po = Co("br"), Lo = Co("img"), Mo = Eo("true"), Io = Eo("false"), Fo = wo(["td", "th"]), Uo = wo(["td", "th", "caption"]), zo = wo(["video", "audio", "object", "embed"]), jo = Co("li"), Ho = Ct().browser, $o = (e2) => Q(e2, Ft), Vo = (e2, t2) => e2.children && j(e2.children, t2);
  var qo = (e2, t2, n2, o2, r2) => e2(n2, o2) ? M.some(n2) : w(r2) && r2(n2) ? M.none() : t2(n2, o2, r2);
  const Wo = (e2, t2, n2) => {
    let o2 = e2.dom;
    const r2 = w(n2) ? n2 : P;
    for (; o2.parentNode; ) {
      o2 = o2.parentNode;
      const e3 = mn(o2);
      if (t2(e3)) return M.some(e3);
      if (r2(e3)) break;
    }
    return M.none();
  }, Ko = (e2, t2, n2) => qo((e3, t3) => t3(e3), Wo, e2, t2, n2), Go = (e2, t2, n2) => Wo(e2, (e3) => pn(e3, t2), n2), Yo = (e2, t2) => ((e3, t3) => {
    const n2 = void 0 === t3 ? document : t3.dom;
    return hn(n2) ? M.none() : M.from(n2.querySelector(e3)).map(mn);
  })(t2, e2), Xo = (e2, t2, n2) => qo((e3, t3) => pn(e3, t3), Go, e2, t2, n2), Qo = (e2, t2 = {}) => {
    let n2 = 0;
    const o2 = {}, r2 = mn(e2), s2 = Cn(r2), a2 = t2.maxLoadTime || 5e3, i3 = (i4) => new Promise((l3, d2) => {
      let c2;
      const u2 = Tt._addCacheSuffix(i4), m2 = ((e3) => we(o2, e3).getOrThunk(() => ({ id: "mce-u" + n2++, passed: [], failed: [], count: 0 })))(u2);
      o2[u2] = m2, m2.count++;
      const f2 = (e3, t3) => {
        V(e3, D), m2.status = t3, m2.passed = [], m2.failed = [], c2 && (c2.onload = null, c2.onerror = null, c2 = null);
      }, g2 = () => f2(m2.passed, 2), p2 = () => f2(m2.failed, 3), h2 = () => {
        var t3;
        t3 = h2, (() => {
          const t4 = e2.styleSheets;
          let n3 = t4.length;
          for (; n3--; ) {
            const e3 = t4[n3].ownerNode;
            if (e3 && c2 && e3.id === c2.id) return g2(), true;
          }
          return false;
        })() || (Date.now() - v2 < a2 ? setTimeout(t3) : p2());
      };
      if (l3 && m2.passed.push(l3), d2 && m2.failed.push(d2), 1 === m2.status) return;
      if (2 === m2.status) return void g2();
      if (3 === m2.status) return void p2();
      m2.status = 1;
      const b2 = cn("link", s2.dom);
      qt(b2, { rel: "stylesheet", type: "text/css", id: m2.id });
      const v2 = Date.now();
      var y2;
      t2.contentCssCors && Vt(b2, "crossOrigin", "anonymous"), t2.referrerPolicy && Vt(b2, "referrerpolicy", t2.referrerPolicy), c2 = b2.dom, c2.onload = h2, c2.onerror = p2, y2 = b2, Jn(Mn(r2), y2), Vt(b2, "href", u2);
    }), l2 = (e3) => {
      const t3 = Tt._addCacheSuffix(e3);
      we(o2, t3).each((e4) => {
        0 == --e4.count && (delete o2[t3], ((e5) => {
          const t4 = Mn(r2);
          Yo(t4, "#" + e5).each(to);
        })(e4.id));
      });
    };
    return { load: i3, loadAll: (e3) => Promise.allSettled($2(e3, (e4) => i3(e4).then(N(e4)))).then((e4) => {
      const t3 = W(e4, (e5) => "fulfilled" === e5.status);
      return t3.fail.length > 0 ? Promise.reject($2(t3.fail, (e5) => e5.reason)) : $2(t3.pass, (e5) => e5.value);
    }), unload: l2, unloadAll: (e3) => {
      V(e3, (e4) => {
        l2(e4);
      });
    }, _setReferrerPolicy: (e3) => {
      t2.referrerPolicy = e3;
    } };
  }, Jo = /* @__PURE__ */ (() => {
    const e2 = /* @__PURE__ */ new WeakMap();
    return { forElement: (t2, n2) => {
      const o2 = Ln(t2).dom;
      return M.from(e2.get(o2)).getOrThunk(() => {
        const t3 = Qo(o2, n2);
        return e2.set(o2, t3), t3;
      });
    } };
  })();
  class Zo {
    constructor(e2, t2) {
      this.node = e2, this.rootNode = t2, this.current = this.current.bind(this), this.next = this.next.bind(this), this.prev = this.prev.bind(this), this.prev2 = this.prev2.bind(this);
    }
    current() {
      return this.node;
    }
    next(e2) {
      return this.node = this.findSibling(this.node, "firstChild", "nextSibling", e2), this.node;
    }
    prev(e2) {
      return this.node = this.findSibling(this.node, "lastChild", "previousSibling", e2), this.node;
    }
    prev2(e2) {
      return this.node = this.findPreviousNode(this.node, e2), this.node;
    }
    findSibling(e2, t2, n2, o2) {
      if (e2) {
        if (!o2 && e2[t2]) return e2[t2];
        if (e2 !== this.rootNode) {
          let t3 = e2[n2];
          if (t3) return t3;
          for (let o3 = e2.parentNode; o3 && o3 !== this.rootNode; o3 = o3.parentNode) if (t3 = o3[n2], t3) return t3;
        }
      }
    }
    findPreviousNode(e2, t2) {
      if (e2) {
        const n2 = e2.previousSibling;
        if (this.rootNode && n2 === this.rootNode) return;
        if (n2) {
          if (!t2) {
            for (let e3 = n2.lastChild; e3; e3 = e3.lastChild) if (!e3.lastChild) return e3;
          }
          return n2;
        }
        const o2 = e2.parentNode;
        if (o2 && o2 !== this.rootNode) return o2;
      }
    }
  }
  const er = ["pre"].concat(["h1", "h2", "h3", "h4", "h5", "h6"]), tr = (e2) => {
    let t2;
    return (n2) => (t2 = t2 || re(e2, L), xe(t2, Lt(n2)));
  }, nr = tr(["article", "aside", "details", "div", "dt", "figcaption", "footer", "form", "fieldset", "header", "hgroup", "html", "main", "nav", "section", "summary", "body", "p", "dl", "multicol", "dd", "figure", "address", "center", "blockquote", "h1", "h2", "h3", "h4", "h5", "h6", "listing", "xmp", "pre", "plaintext", "menu", "dir", "ul", "ol", "li", "hr", "table", "tbody", "thead", "tfoot", "th", "tr", "td", "caption"]), or = (e2) => Ft(e2) && !nr(e2), rr = (e2) => Ft(e2) && "br" === Lt(e2), sr = tr(["h1", "h2", "h3", "h4", "h5", "h6", "p", "div", "address", "pre", "form", "blockquote", "center", "dir", "fieldset", "header", "footer", "article", "section", "hgroup", "aside", "nav", "figure"]), ar = tr(["ul", "ol", "dl"]), ir = tr(["li", "dd", "dt"]), lr = tr(["thead", "tbody", "tfoot"]), dr = tr(["td", "th"]), cr = tr(["pre", "script", "textarea", "style"]), ur = tr(er), mr = (e2) => ur(e2) || or(e2), fr = (e2, t2, n2) => Go(e2, t2, n2).isSome(), gr = "\uFEFF", pr = "\xA0", hr = (e2) => e2 === gr, br = (e2, t2) => {
    let n2 = [];
    return V(Nn(e2), (e3) => {
      t2(e3) && (n2 = n2.concat([e3])), n2 = n2.concat(br(e3, t2));
    }), n2;
  }, vr = (e2, t2) => ((e3, t3) => {
    const n2 = void 0 === t3 ? document : t3.dom;
    return hn(n2) ? [] : $2(n2.querySelectorAll(e3), mn);
  })(t2, e2), yr = /* @__PURE__ */ ((e2, t2) => {
    const n2 = (t3) => e2(t3) ? M.from(t3.dom.nodeValue) : M.none();
    return { get: (t3) => {
      if (!e2(t3)) throw new Error("Can only get text value of a text node");
      return n2(t3).getOr("");
    }, getOption: n2, set: (t3, n3) => {
      if (!e2(t3)) throw new Error("Can only set raw text value of a text node");
      t3.dom.nodeValue = n3;
    } };
  })(Ut), Cr = (e2) => yr.get(e2), wr = (e2) => yr.getOption(e2), xr = () => {
    const e2 = cn("br");
    return Vt(e2, "data-mce-bogus", "1"), e2;
  }, kr = (e2) => {
    eo(e2), Jn(e2, xr());
  }, Sr = (e2) => {
    On(e2).each((t2) => {
      kn(t2).each((n2) => {
        nr(e2) && rr(t2) && nr(n2) && to(t2);
      });
    });
  }, _r = gr, Er = hr, Nr = (e2) => e2.replace(/\uFEFF/g, ""), Rr = yo, Ar = Ro, Or = (e2) => (Ar(e2) && (e2 = e2.parentNode), Rr(e2) && e2.hasAttribute("data-mce-caret")), Tr = (e2) => Ar(e2) && Er(e2.data), Br = (e2) => Or(e2) || Tr(e2), Dr = (e2) => e2.firstChild !== e2.lastChild || !Po(e2.firstChild), Pr = (e2) => {
    const t2 = e2.container();
    return !!Ro(t2) && (t2.data.charAt(e2.offset()) === _r || e2.isAtStart() && Tr(t2.previousSibling));
  }, Lr = (e2) => {
    const t2 = e2.container();
    return !!Ro(t2) && (t2.data.charAt(e2.offset() - 1) === _r || e2.isAtEnd() && Tr(t2.nextSibling));
  }, Mr = (e2) => Ar(e2) && e2.data[0] === _r, Ir = (e2) => Ar(e2) && e2.data[e2.data.length - 1] === _r, Fr = (e2) => e2 && e2.hasAttribute("data-mce-caret") ? (((e3) => {
    var t2;
    const n2 = e3.getElementsByTagName("br"), o2 = n2[n2.length - 1];
    So(o2) && (null === (t2 = o2.parentNode) || void 0 === t2 || t2.removeChild(o2));
  })(e2), e2.removeAttribute("data-mce-caret"), e2.removeAttribute("data-mce-bogus"), e2.removeAttribute("style"), e2.removeAttribute("data-mce-style"), e2.removeAttribute("_moz_abspos"), e2) : null, Ur = (e2) => Or(e2.startContainer), zr = Mo, jr = Io, Hr = Po, $r = Ro, Vr = wo(["script", "style", "textarea"]), qr = wo(["img", "input", "textarea", "hr", "iframe", "video", "audio", "object", "embed"]), Wr = wo(["table"]), Kr = Br, Gr = (e2) => !Kr(e2) && ($r(e2) ? !Vr(e2.parentNode) : qr(e2) || Hr(e2) || Wr(e2) || Yr(e2)), Yr = (e2) => !((e3) => yo(e3) && "true" === e3.getAttribute("unselectable"))(e2) && jr(e2), Xr = (e2, t2) => Gr(e2) && ((e3, t3) => {
    for (let n2 = e3.parentNode; n2 && n2 !== t3; n2 = n2.parentNode) {
      if (Yr(n2)) return false;
      if (zr(n2)) return true;
    }
    return true;
  })(e2, t2), Qr = /^[ \t\r\n]*$/, Jr = (e2) => Qr.test(e2), Zr = (e2) => "\n" === e2 || "\r" === e2, es = (e2, t2 = 4, n2 = true, o2 = true) => {
    const r2 = ((e3, t3) => t3 <= 0 ? "" : new Array(t3 + 1).join(" "))(0, t2), s2 = e2.replace(/\t/g, r2), a2 = Y(s2, (e3, t3) => ((e4) => -1 !== " \f	\v".indexOf(e4))(t3) || t3 === pr ? e3.pcIsSpace || "" === e3.str && n2 || e3.str.length === s2.length - 1 && o2 || ((e4, t4) => t4 < e4.length && t4 >= 0 && Zr(e4[t4]))(s2, e3.str.length + 1) ? { pcIsSpace: false, str: e3.str + pr } : { pcIsSpace: true, str: e3.str + " " } : { pcIsSpace: Zr(t3), str: e3.str + t3 }, { pcIsSpace: false, str: "" });
    return a2.str;
  }, ts = (e2, t2) => Gr(e2) && !((e3, t3) => Ro(e3) && Jr(e3.data) && !((e4, t4) => {
    const n2 = mn(t4), o2 = mn(e4);
    return fr(o2, "pre,code", O(bn, n2));
  })(e3, t3))(e2, t2) || ((e3) => yo(e3) && "A" === e3.nodeName && !e3.hasAttribute("href") && (e3.hasAttribute("name") || e3.hasAttribute("id")))(e2) || ns(e2), ns = ko("data-mce-bookmark"), os = ko("data-mce-bogus"), rs = ("data-mce-bogus", "all", (e2) => yo(e2) && "all" === e2.getAttribute("data-mce-bogus"));
  const ss = (e2, t2 = true) => ((e3, t3) => {
    let n2 = 0;
    if (ts(e3, e3)) return false;
    {
      let o2 = e3.firstChild;
      if (!o2) return true;
      const r2 = new Zo(o2, e3);
      do {
        if (t3) {
          if (rs(o2)) {
            o2 = r2.next(true);
            continue;
          }
          if (os(o2)) {
            o2 = r2.next();
            continue;
          }
        }
        if (Po(o2)) n2++, o2 = r2.next();
        else {
          if (ts(o2, e3)) return false;
          o2 = r2.next();
        }
      } while (o2);
      return n2 <= 1;
    }
  })(e2.dom, t2), as = (e2, t2) => C(e2) && (ts(e2, t2) || or(mn(e2))), is = (e2) => ((e3) => "span" === e3.nodeName.toLowerCase())(e2) && "bookmark" === e2.getAttribute("data-mce-type"), ls = (e2, t2, n2) => {
    var o2;
    const r2 = n2 || t2;
    if (yo(t2) && is(t2)) return t2;
    const s2 = t2.childNodes;
    for (let t3 = s2.length - 1; t3 >= 0; t3--) ls(e2, s2[t3], r2);
    if (yo(t2)) {
      const e3 = t2.childNodes;
      1 === e3.length && is(e3[0]) && (null === (o2 = t2.parentNode) || void 0 === o2 || o2.insertBefore(e3[0], t2));
    }
    return ((e3) => Do(e3) || Bo(e3))(t2) || ts(t2, r2) || ((e3) => !!yo(e3) && e3.childNodes.length > 0)(t2) || ((e3, t3) => Ro(e3) && e3.data.length > 0 && ((e4, t4) => {
      const n3 = new Zo(e4, t4).prev(false), o3 = new Zo(e4, t4).next(false), r3 = v(n3) || as(n3, t4), s3 = v(o3) || as(o3, t4);
      return r3 && s3;
    })(e3, t3))(t2, r2) || e2.remove(t2), t2;
  }, ds = Tt.makeMap, cs = /[&<>\"\u0060\u007E-\uD7FF\uE000-\uFFEF]|[\uD800-\uDBFF][\uDC00-\uDFFF]/g, us = /[<>&\u007E-\uD7FF\uE000-\uFFEF]|[\uD800-\uDBFF][\uDC00-\uDFFF]/g, ms = /[<>&\"\']/g, fs = /&#([a-z0-9]+);?|&([a-z0-9]+);/gi, gs = { 128: "\u20AC", 130: "\u201A", 131: "\u0192", 132: "\u201E", 133: "\u2026", 134: "\u2020", 135: "\u2021", 136: "\u02C6", 137: "\u2030", 138: "\u0160", 139: "\u2039", 140: "\u0152", 142: "\u017D", 145: "\u2018", 146: "\u2019", 147: "\u201C", 148: "\u201D", 149: "\u2022", 150: "\u2013", 151: "\u2014", 152: "\u02DC", 153: "\u2122", 154: "\u0161", 155: "\u203A", 156: "\u0153", 158: "\u017E", 159: "\u0178" }, ps = { '"': "&quot;", "'": "&#39;", "<": "&lt;", ">": "&gt;", "&": "&amp;", "`": "&#96;" }, hs = { "&lt;": "<", "&gt;": ">", "&amp;": "&", "&quot;": '"', "&apos;": "'" }, bs = (e2, t2) => {
    const n2 = {};
    if (e2) {
      const o2 = e2.split(",");
      t2 = t2 || 10;
      for (let e3 = 0; e3 < o2.length; e3 += 2) {
        const r2 = String.fromCharCode(parseInt(o2[e3], t2));
        if (!ps[r2]) {
          const t3 = "&" + o2[e3 + 1] + ";";
          n2[r2] = t3, n2[t3] = r2;
        }
      }
      return n2;
    }
  }, vs = bs("50,nbsp,51,iexcl,52,cent,53,pound,54,curren,55,yen,56,brvbar,57,sect,58,uml,59,copy,5a,ordf,5b,laquo,5c,not,5d,shy,5e,reg,5f,macr,5g,deg,5h,plusmn,5i,sup2,5j,sup3,5k,acute,5l,micro,5m,para,5n,middot,5o,cedil,5p,sup1,5q,ordm,5r,raquo,5s,frac14,5t,frac12,5u,frac34,5v,iquest,60,Agrave,61,Aacute,62,Acirc,63,Atilde,64,Auml,65,Aring,66,AElig,67,Ccedil,68,Egrave,69,Eacute,6a,Ecirc,6b,Euml,6c,Igrave,6d,Iacute,6e,Icirc,6f,Iuml,6g,ETH,6h,Ntilde,6i,Ograve,6j,Oacute,6k,Ocirc,6l,Otilde,6m,Ouml,6n,times,6o,Oslash,6p,Ugrave,6q,Uacute,6r,Ucirc,6s,Uuml,6t,Yacute,6u,THORN,6v,szlig,70,agrave,71,aacute,72,acirc,73,atilde,74,auml,75,aring,76,aelig,77,ccedil,78,egrave,79,eacute,7a,ecirc,7b,euml,7c,igrave,7d,iacute,7e,icirc,7f,iuml,7g,eth,7h,ntilde,7i,ograve,7j,oacute,7k,ocirc,7l,otilde,7m,ouml,7n,divide,7o,oslash,7p,ugrave,7q,uacute,7r,ucirc,7s,uuml,7t,yacute,7u,thorn,7v,yuml,ci,fnof,sh,Alpha,si,Beta,sj,Gamma,sk,Delta,sl,Epsilon,sm,Zeta,sn,Eta,so,Theta,sp,Iota,sq,Kappa,sr,Lambda,ss,Mu,st,Nu,su,Xi,sv,Omicron,t0,Pi,t1,Rho,t3,Sigma,t4,Tau,t5,Upsilon,t6,Phi,t7,Chi,t8,Psi,t9,Omega,th,alpha,ti,beta,tj,gamma,tk,delta,tl,epsilon,tm,zeta,tn,eta,to,theta,tp,iota,tq,kappa,tr,lambda,ts,mu,tt,nu,tu,xi,tv,omicron,u0,pi,u1,rho,u2,sigmaf,u3,sigma,u4,tau,u5,upsilon,u6,phi,u7,chi,u8,psi,u9,omega,uh,thetasym,ui,upsih,um,piv,812,bull,816,hellip,81i,prime,81j,Prime,81u,oline,824,frasl,88o,weierp,88h,image,88s,real,892,trade,89l,alefsym,8cg,larr,8ch,uarr,8ci,rarr,8cj,darr,8ck,harr,8dl,crarr,8eg,lArr,8eh,uArr,8ei,rArr,8ej,dArr,8ek,hArr,8g0,forall,8g2,part,8g3,exist,8g5,empty,8g7,nabla,8g8,isin,8g9,notin,8gb,ni,8gf,prod,8gh,sum,8gi,minus,8gn,lowast,8gq,radic,8gt,prop,8gu,infin,8h0,ang,8h7,and,8h8,or,8h9,cap,8ha,cup,8hb,int,8hk,there4,8hs,sim,8i5,cong,8i8,asymp,8j0,ne,8j1,equiv,8j4,le,8j5,ge,8k2,sub,8k3,sup,8k4,nsub,8k6,sube,8k7,supe,8kl,oplus,8kn,otimes,8l5,perp,8m5,sdot,8o8,lceil,8o9,rceil,8oa,lfloor,8ob,rfloor,8p9,lang,8pa,rang,9ea,loz,9j0,spades,9j3,clubs,9j5,hearts,9j6,diams,ai,OElig,aj,oelig,b0,Scaron,b1,scaron,bo,Yuml,m6,circ,ms,tilde,802,ensp,803,emsp,809,thinsp,80c,zwnj,80d,zwj,80e,lrm,80f,rlm,80j,ndash,80k,mdash,80o,lsquo,80p,rsquo,80q,sbquo,80s,ldquo,80t,rdquo,80u,bdquo,810,dagger,811,Dagger,81g,permil,81p,lsaquo,81q,rsaquo,85c,euro", 32), ys = (e2, t2) => e2.replace(t2 ? cs : us, (e3) => ps[e3] || e3), Cs = (e2, t2) => e2.replace(t2 ? cs : us, (e3) => e3.length > 1 ? "&#" + (1024 * (e3.charCodeAt(0) - 55296) + (e3.charCodeAt(1) - 56320) + 65536) + ";" : ps[e3] || "&#" + e3.charCodeAt(0) + ";"), ws = (e2, t2, n2) => {
    const o2 = n2 || vs;
    return e2.replace(t2 ? cs : us, (e3) => ps[e3] || o2[e3] || e3);
  }, xs = { encodeRaw: ys, encodeAllRaw: (e2) => ("" + e2).replace(ms, (e3) => ps[e3] || e3), encodeNumeric: Cs, encodeNamed: ws, getEncodeFunc: (e2, t2) => {
    const n2 = bs(t2) || vs, o2 = ds(e2.replace(/\+/g, ","));
    return o2.named && o2.numeric ? (e3, t3) => e3.replace(t3 ? cs : us, (e4) => void 0 !== ps[e4] ? ps[e4] : void 0 !== n2[e4] ? n2[e4] : e4.length > 1 ? "&#" + (1024 * (e4.charCodeAt(0) - 55296) + (e4.charCodeAt(1) - 56320) + 65536) + ";" : "&#" + e4.charCodeAt(0) + ";") : o2.named ? t2 ? (e3, t3) => ws(e3, t3, n2) : ws : o2.numeric ? Cs : ys;
  }, decode: (e2) => e2.replace(fs, (e3, t2) => t2 ? (t2 = "x" === t2.charAt(0).toLowerCase() ? parseInt(t2.substr(1), 16) : parseInt(t2, 10)) > 65535 ? (t2 -= 65536, String.fromCharCode(55296 + (t2 >> 10), 56320 + (1023 & t2))) : gs[t2] || String.fromCharCode(t2) : hs[e3] || vs[e3] || ((e4) => {
    const t3 = cn("div").dom;
    return t3.innerHTML = e4, t3.textContent || t3.innerText || e4;
  })(e3)) }, ks = {}, Ss = {}, _s = {}, Es = Tt.makeMap, Ns = Tt.each, Rs = Tt.extend, As = Tt.explode, Os = Tt.inArray, Ts = (e2, t2) => (e2 = Tt.trim(e2)) ? e2.split(t2 || " ") : [], Bs = (e2, t2 = {}) => {
    const n2 = Es(e2, " ", Es(e2.toUpperCase(), " "));
    return Rs(n2, t2);
  }, Ds = (e2) => Bs("td th li dt dd figcaption caption details summary", e2.getTextBlockElements()), Ps = (e2, t2) => {
    if (e2) {
      const n2 = {};
      return m(e2) && (e2 = { "*": e2 }), Ns(e2, (e3, o2) => {
        n2[o2] = n2[o2.toUpperCase()] = "map" === t2 ? Es(e3, /[, ]/) : As(e3, /[, ]/);
      }), n2;
    }
  }, Ls = (e2 = {}) => {
    var t2;
    const n2 = {}, o2 = {};
    let r2 = [];
    const s2 = {}, a2 = {}, i3 = (t3, n3, o3) => {
      const r3 = e2[t3];
      if (r3) return Es(r3, /[, ]/, Es(r3.toUpperCase(), /[, ]/));
      {
        let e3 = Ss[t3];
        return e3 || (e3 = Bs(n3, o3), Ss[t3] = e3), e3;
      }
    }, l2 = null !== (t2 = e2.schema) && void 0 !== t2 ? t2 : "html5", d2 = ((e3) => {
      const t3 = {};
      let n3, o3, r3, s3;
      const a3 = (e4, o4 = "", r4 = "") => {
        const s4 = Ts(r4), a4 = Ts(e4);
        let i5 = a4.length;
        for (; i5--; ) {
          const e5 = Ts([n3, o4].join(" "));
          t3[a4[i5]] = { attributes: re(e5, () => ({})), attributesOrder: e5, children: re(s4, N(_s)) };
        }
      }, i4 = (e4, n4) => {
        const o4 = Ts(e4), r4 = Ts(n4);
        let s4 = o4.length;
        for (; s4--; ) {
          const e5 = t3[o4[s4]];
          for (let t4 = 0, n5 = r4.length; t4 < n5; t4++) e5.attributes[r4[t4]] = {}, e5.attributesOrder.push(r4[t4]);
        }
      };
      if (ks[e3]) return ks[e3];
      if (n3 = "id accesskey class dir lang style tabindex title role", o3 = "address blockquote div dl fieldset form h1 h2 h3 h4 h5 h6 hr menu ol p pre table ul", r3 = "a abbr b bdo br button cite code del dfn em embed i iframe img input ins kbd label map noscript object q s samp script select small span strong sub sup textarea u var #text #comment", "html4" !== e3 && (n3 += " contenteditable contextmenu draggable dropzone hidden spellcheck translate", o3 += " article aside details dialog figure main header footer hgroup section nav", r3 += " audio canvas command datalist mark meter output picture progress time wbr video ruby bdi keygen"), "html5-strict" !== e3) {
        n3 += " xml:lang";
        const e4 = "acronym applet basefont big font strike tt";
        r3 = [r3, e4].join(" "), Ns(Ts(e4), (e5) => {
          a3(e5, "", r3);
        });
        const t4 = "center dir isindex noframes";
        o3 = [o3, t4].join(" "), s3 = [o3, r3].join(" "), Ns(Ts(t4), (e5) => {
          a3(e5, "", s3);
        });
      }
      return s3 = s3 || [o3, r3].join(" "), a3("html", "manifest", "head body"), a3("head", "", "base command link meta noscript script style title"), a3("title hr noscript br"), a3("base", "href target"), a3("link", "href rel media hreflang type sizes hreflang"), a3("meta", "name http-equiv content charset"), a3("style", "media type scoped"), a3("script", "src async defer type charset"), a3("body", "onafterprint onbeforeprint onbeforeunload onblur onerror onfocus onhashchange onload onmessage onoffline ononline onpagehide onpageshow onpopstate onresize onscroll onstorage onunload", s3), a3("address dt dd div caption", "", s3), a3("h1 h2 h3 h4 h5 h6 pre p abbr code var samp kbd sub sup i b u bdo span legend em strong small s cite dfn", "", r3), a3("blockquote", "cite", s3), a3("ol", "reversed start type", "li"), a3("ul", "", "li"), a3("li", "value", s3), a3("dl", "", "dt dd"), a3("a", "href target rel media hreflang type", r3), a3("q", "cite", r3), a3("ins del", "cite datetime", s3), a3("img", "src sizes srcset alt usemap ismap width height"), a3("iframe", "src name width height", s3), a3("embed", "src type width height"), a3("object", "data type typemustmatch name usemap form width height", [s3, "param"].join(" ")), a3("param", "name value"), a3("map", "name", [s3, "area"].join(" ")), a3("area", "alt coords shape href target rel media hreflang type"), a3("table", "border", "caption colgroup thead tfoot tbody tr" + ("html4" === e3 ? " col" : "")), a3("colgroup", "span", "col"), a3("col", "span"), a3("tbody thead tfoot", "", "tr"), a3("tr", "", "td th"), a3("td", "colspan rowspan headers", s3), a3("th", "colspan rowspan headers scope abbr", s3), a3("form", "accept-charset action autocomplete enctype method name novalidate target", s3), a3("fieldset", "disabled form name", [s3, "legend"].join(" ")), a3("label", "form for", r3), a3("input", "accept alt autocomplete checked dirname disabled form formaction formenctype formmethod formnovalidate formtarget height list max maxlength min multiple name pattern readonly required size src step type value width"), a3("button", "disabled form formaction formenctype formmethod formnovalidate formtarget name type value", "html4" === e3 ? s3 : r3), a3("select", "disabled form multiple name required size", "option optgroup"), a3("optgroup", "disabled label", "option"), a3("option", "disabled label selected value"), a3("textarea", "cols dirname disabled form maxlength name readonly required rows wrap"), a3("menu", "type label", [s3, "li"].join(" ")), a3("noscript", "", s3), "html4" !== e3 && (a3("wbr"), a3("ruby", "", [r3, "rt rp"].join(" ")), a3("figcaption", "", s3), a3("mark rt rp summary bdi", "", r3), a3("canvas", "width height", s3), a3("video", "src crossorigin poster preload autoplay mediagroup loop muted controls width height buffered", [s3, "track source"].join(" ")), a3("audio", "src crossorigin preload autoplay mediagroup loop muted controls buffered volume", [s3, "track source"].join(" ")), a3("picture", "", "img source"), a3("source", "src srcset type media sizes"), a3("track", "kind src srclang label default"), a3("datalist", "", [r3, "option"].join(" ")), a3("article section nav aside main header footer", "", s3), a3("hgroup", "", "h1 h2 h3 h4 h5 h6"), a3("figure", "", [s3, "figcaption"].join(" ")), a3("time", "datetime", r3), a3("dialog", "open", s3), a3("command", "type label icon disabled checked radiogroup command"), a3("output", "for form name", r3), a3("progress", "value max", r3), a3("meter", "value min max low high optimum", r3), a3("details", "open", [s3, "summary"].join(" ")), a3("keygen", "autofocus challenge disabled form keytype name")), "html5-strict" !== e3 && (i4("script", "language xml:space"), i4("style", "xml:space"), i4("object", "declare classid code codebase codetype archive standby align border hspace vspace"), i4("embed", "align name hspace vspace"), i4("param", "valuetype type"), i4("a", "charset name rev shape coords"), i4("br", "clear"), i4("applet", "codebase archive code object alt name width height align hspace vspace"), i4("img", "name longdesc align border hspace vspace"), i4("iframe", "longdesc frameborder marginwidth marginheight scrolling align"), i4("font basefont", "size color face"), i4("input", "usemap align"), i4("select"), i4("textarea"), i4("h1 h2 h3 h4 h5 h6 div p legend caption", "align"), i4("ul", "type compact"), i4("li", "type"), i4("ol dl menu dir", "compact"), i4("pre", "width xml:space"), i4("hr", "align noshade size width"), i4("isindex", "prompt"), i4("table", "summary width frame rules cellspacing cellpadding align bgcolor"), i4("col", "width align char charoff valign"), i4("colgroup", "width align char charoff valign"), i4("thead", "align char charoff valign"), i4("tr", "align char charoff valign bgcolor"), i4("th", "axis align char charoff valign nowrap bgcolor width height"), i4("form", "accept"), i4("td", "abbr axis scope align char charoff valign nowrap bgcolor width height"), i4("tfoot", "align char charoff valign"), i4("tbody", "align char charoff valign"), i4("area", "nohref"), i4("body", "background bgcolor text link vlink alink")), "html4" !== e3 && (i4("input button select textarea", "autofocus"), i4("input textarea", "placeholder"), i4("a", "download"), i4("link script img", "crossorigin"), i4("img", "loading"), i4("iframe", "sandbox seamless allow allowfullscreen loading")), "html4" !== e3 && V([t3.video, t3.audio], (e4) => {
        delete e4.children.audio, delete e4.children.video;
      }), Ns(Ts("a form meter progress dfn"), (e4) => {
        t3[e4] && delete t3[e4].children[e4];
      }), delete t3.caption.children.table, delete t3.script, ks[e3] = t3, t3;
    })(l2);
    false === e2.verify_html && (e2.valid_elements = "*[*]");
    const c2 = Ps(e2.valid_styles), u2 = Ps(e2.invalid_styles, "map"), m2 = Ps(e2.valid_classes, "map"), f2 = i3("whitespace_elements", "pre script noscript style textarea video audio iframe object code"), g2 = i3("self_closing_elements", "colgroup dd dt li option p td tfoot th thead tr"), p2 = i3("void_elements", "area base basefont br col frame hr img input isindex link meta param embed source wbr track"), h2 = i3("boolean_attributes", "checked compact declare defer disabled ismap multiple nohref noresize noshade nowrap readonly selected autoplay loop controls allowfullscreen"), b2 = "td th iframe video audio object script code", v2 = i3("non_empty_elements", b2 + " pre", p2), y2 = i3("move_caret_before_on_enter_elements", b2 + " table", p2), C2 = i3("text_block_elements", "h1 h2 h3 h4 h5 h6 p div address pre form blockquote center dir fieldset header footer article section hgroup aside main nav figure"), w2 = i3("block_elements", "hr table tbody thead tfoot th tr td li ol ul caption dl dt dd noscript menu isindex option datalist select optgroup figcaption details summary", C2), x2 = i3("text_inline_elements", "span strong b em i font s strike u var cite dfn code mark q sup sub samp");
    Ns("script noscript iframe noframes noembed title style textarea xmp plaintext".split(" "), (e3) => {
      a2[e3] = new RegExp("</" + e3 + "[^>]*>", "gi");
    });
    const k2 = (e3) => new RegExp("^" + e3.replace(/([?+*])/g, ".$1") + "$"), S2 = (e3) => {
      const t3 = /^([#+\-])?([^\[!\/]+)(?:\/([^\[!]+))?(?:(!?)\[([^\]]+)])?$/, o3 = /^([!\-])?(\w+[\\:]:\w+|[^=~<]+)?(?:([=~<])(.*))?$/, s3 = /[*?+]/;
      if (e3) {
        const a3 = Ts(e3, ",");
        let i4, l3;
        n2["@"] && (i4 = n2["@"].attributes, l3 = n2["@"].attributesOrder);
        for (let e4 = 0, d3 = a3.length; e4 < d3; e4++) {
          let d4 = t3.exec(a3[e4]);
          if (d4) {
            const e5 = d4[1], t4 = d4[2], a4 = d4[3], c3 = d4[5], u3 = {}, m3 = [], f3 = { attributes: u3, attributesOrder: m3 };
            if ("#" === e5 && (f3.paddEmpty = true), "-" === e5 && (f3.removeEmpty = true), "!" === d4[4] && (f3.removeEmptyAttrs = true), i4 && (fe(i4, (e6, t5) => {
              u3[t5] = e6;
            }), l3 && m3.push(...l3)), c3) {
              const e6 = Ts(c3, "|");
              for (let t5 = 0, n3 = e6.length; t5 < n3; t5++) if (d4 = o3.exec(e6[t5]), d4) {
                const e7 = {}, t6 = d4[1], n4 = d4[2].replace(/[\\:]:/g, ":"), o4 = d4[3], r3 = d4[4];
                if ("!" === t6 && (f3.attributesRequired = f3.attributesRequired || [], f3.attributesRequired.push(n4), e7.required = true), "-" === t6) {
                  delete u3[n4], m3.splice(Os(m3, n4), 1);
                  continue;
                }
                if (o4 && ("=" === o4 && (f3.attributesDefault = f3.attributesDefault || [], f3.attributesDefault.push({ name: n4, value: r3 }), e7.defaultValue = r3), "~" === o4 && (f3.attributesForced = f3.attributesForced || [], f3.attributesForced.push({ name: n4, value: r3 }), e7.forcedValue = r3), "<" === o4 && (e7.validValues = Es(r3, "?"))), s3.test(n4)) {
                  const t7 = e7;
                  f3.attributePatterns = f3.attributePatterns || [], t7.pattern = k2(n4), f3.attributePatterns.push(t7);
                } else u3[n4] || m3.push(n4), u3[n4] = e7;
              }
            }
            if (i4 || "@" !== t4 || (i4 = u3, l3 = m3), a4 && (f3.outputName = t4, n2[a4] = f3), s3.test(t4)) {
              const e6 = f3;
              e6.pattern = k2(t4), r2.push(e6);
            } else n2[t4] = f3;
          }
        }
      }
    }, _2 = (e3) => {
      r2 = [], V(ue(n2), (e4) => {
        delete n2[e4];
      }), S2(e3), Ns(d2, (e4, t3) => {
        o2[t3] = e4.children;
      });
    }, E2 = (e3) => {
      const t3 = /^(~)?(.+)$/;
      e3 && (delete Ss.text_block_elements, delete Ss.block_elements, Ns(Ts(e3, ","), (e4) => {
        const r3 = t3.exec(e4);
        if (r3) {
          const e5 = "~" === r3[1], t4 = e5 ? "span" : "div", a3 = r3[2];
          if (o2[a3] = o2[t4], s2[a3] = t4, v2[a3.toUpperCase()] = {}, v2[a3] = {}, e5 || (w2[a3.toUpperCase()] = {}, w2[a3] = {}), !n2[a3]) {
            let e6 = n2[t4];
            e6 = Rs({}, e6), delete e6.removeEmptyAttrs, delete e6.removeEmpty, n2[a3] = e6;
          }
          Ns(o2, (e6, n3) => {
            e6[t4] && (o2[n3] = e6 = Rs({}, o2[n3]), e6[a3] = e6[t4]);
          });
        }
      }));
    }, R2 = (e3) => {
      const t3 = /^([+\-]?)([A-Za-z0-9_\-.\u00b7\u00c0-\u00d6\u00d8-\u00f6\u00f8-\u037d\u037f-\u1fff\u200c-\u200d\u203f-\u2040\u2070-\u218f\u2c00-\u2fef\u3001-\ud7ff\uf900-\ufdcf\ufdf0-\ufffd]+)\[([^\]]+)]$/;
      delete ks[l2], e3 && Ns(Ts(e3, ","), (e4) => {
        const n3 = t3.exec(e4);
        if (n3) {
          const e5 = n3[1];
          let t4;
          t4 = e5 ? o2[n3[2]] : o2[n3[2]] = { "#comment": {} }, t4 = o2[n3[2]], Ns(Ts(n3[3], "|"), (n4) => {
            "-" === e5 ? delete t4[n4] : t4[n4] = {};
          });
        }
      });
    }, A2 = (e3) => {
      const t3 = n2[e3];
      if (t3) return t3;
      let o3 = r2.length;
      for (; o3--; ) {
        const t4 = r2[o3];
        if (t4.pattern.test(e3)) return t4;
      }
    };
    e2.valid_elements ? _2(e2.valid_elements) : (Ns(d2, (e3, t3) => {
      n2[t3] = { attributes: e3.attributes, attributesOrder: e3.attributesOrder }, o2[t3] = e3.children;
    }), Ns(Ts("strong/b em/i"), (e3) => {
      const t3 = Ts(e3, "/");
      n2[t3[1]].outputName = t3[0];
    }), Ns(x2, (t3, o3) => {
      n2[o3] && (e2.padd_empty_block_inline_children && (n2[o3].paddInEmptyBlock = true), n2[o3].removeEmpty = true);
    }), Ns(Ts("ol ul blockquote a table tbody"), (e3) => {
      n2[e3] && (n2[e3].removeEmpty = true);
    }), Ns(Ts("p h1 h2 h3 h4 h5 h6 th td pre div address caption li"), (e3) => {
      n2[e3].paddEmpty = true;
    }), Ns(Ts("span"), (e3) => {
      n2[e3].removeEmptyAttrs = true;
    })), E2(e2.custom_elements), R2(e2.valid_children), S2(e2.extended_valid_elements), R2("+ol[ul|ol],+ul[ul|ol]"), Ns({ dd: "dl", dt: "dl", li: "ul ol", td: "tr", th: "tr", tr: "tbody thead tfoot", tbody: "table", thead: "table", tfoot: "table", legend: "fieldset", area: "map", param: "video audio object" }, (e3, t3) => {
      n2[t3] && (n2[t3].parentsRequired = Ts(e3));
    }), e2.invalid_elements && Ns(As(e2.invalid_elements), (e3) => {
      n2[e3] && delete n2[e3];
    }), A2("span") || S2("span[!data-mce-type|*]");
    const O2 = N(c2), T2 = N(u2), B2 = N(m2), D2 = N(h2), P2 = N(w2), L2 = N(C2), M2 = N(x2), I2 = N(Object.seal(p2)), F2 = N(g2), U2 = N(v2), z2 = N(y2), j2 = N(f2), H2 = N(Object.seal(a2)), $3 = N(s2);
    return { type: l2, children: o2, elements: n2, getValidStyles: O2, getValidClasses: B2, getBlockElements: P2, getInvalidStyles: T2, getVoidElements: I2, getTextBlockElements: L2, getTextInlineElements: M2, getBoolAttrs: D2, getElementRule: A2, getSelfClosingElements: F2, getNonEmptyElements: U2, getMoveCaretBeforeOnEnterElements: z2, getWhitespaceElements: j2, getSpecialElements: H2, isValidChild: (e3, t3) => {
      const n3 = o2[e3.toLowerCase()];
      return !(!n3 || !n3[t3.toLowerCase()]);
    }, isValid: (e3, t3) => {
      const n3 = A2(e3);
      if (n3) {
        if (!t3) return true;
        {
          if (n3.attributes[t3]) return true;
          const e4 = n3.attributePatterns;
          if (e4) {
            let n4 = e4.length;
            for (; n4--; ) if (e4[n4].pattern.test(t3)) return true;
          }
        }
      }
      return false;
    }, getCustomElements: $3, addValidElements: S2, setValidElements: _2, addCustomElements: E2, addValidChildren: R2 };
  }, Ms = (e2 = {}, t2) => {
    const n2 = /(?:url(?:(?:\(\s*\"([^\"]+)\"\s*\))|(?:\(\s*\'([^\']+)\'\s*\))|(?:\(\s*([^)\s]+)\s*\))))|(?:\'([^\']+)\')|(?:\"([^\"]+)\")/gi, o2 = /\s*([^:]+):\s*([^;]+);?/g, r2 = /\s+$/, s2 = {};
    let a2, i3;
    t2 && (a2 = t2.getValidStyles(), i3 = t2.getInvalidStyles());
    const l2 = `\\" \\' \\; \\: ; : \uFEFF`.split(" ");
    for (let e3 = 0; e3 < l2.length; e3++) s2[l2[e3]] = "\uFEFF" + e3, s2["\uFEFF" + e3] = l2[e3];
    const d2 = { parse: (t3) => {
      const a3 = {};
      let i4 = false;
      const l3 = e2.url_converter, c2 = e2.url_converter_scope || d2, u2 = (e3, t4, n3) => {
        const o3 = a3[e3 + "-top" + t4];
        if (!o3) return;
        const r3 = a3[e3 + "-right" + t4];
        if (!r3) return;
        const s3 = a3[e3 + "-bottom" + t4];
        if (!s3) return;
        const i5 = a3[e3 + "-left" + t4];
        if (!i5) return;
        const l4 = [o3, r3, s3, i5];
        let d3 = l4.length - 1;
        for (; d3-- && l4[d3] === l4[d3 + 1]; ) ;
        d3 > -1 && n3 || (a3[e3 + t4] = -1 === d3 ? l4[0] : l4.join(" "), delete a3[e3 + "-top" + t4], delete a3[e3 + "-right" + t4], delete a3[e3 + "-bottom" + t4], delete a3[e3 + "-left" + t4]);
      }, m2 = (e3) => {
        const t4 = a3[e3];
        if (!t4) return;
        const n3 = t4.split(" ");
        let o3 = n3.length;
        for (; o3--; ) if (n3[o3] !== n3[0]) return false;
        return a3[e3] = n3[0], true;
      }, f2 = (e3) => (i4 = true, s2[e3]), g2 = (e3, t4) => (i4 && (e3 = e3.replace(/\uFEFF[0-9]/g, (e4) => s2[e4])), t4 || (e3 = e3.replace(/\\([\'\";:])/g, "$1")), e3), p2 = (e3) => String.fromCharCode(parseInt(e3.slice(1), 16)), h2 = (e3) => e3.replace(/\\[0-9a-f]+/gi, p2), b2 = (t4, n3, o3, r3, s3, a4) => {
        if (s3 = s3 || a4) return "'" + (s3 = g2(s3)).replace(/\'/g, "\\'") + "'";
        if (n3 = g2(n3 || o3 || r3 || ""), !e2.allow_script_urls) {
          const t5 = n3.replace(/[\s\r\n]+/g, "");
          if (/(java|vb)script:/i.test(t5)) return "";
          if (!e2.allow_svg_data_urls && /^data:image\/svg/i.test(t5)) return "";
        }
        return l3 && (n3 = l3.call(c2, n3, "style")), "url('" + n3.replace(/\'/g, "\\'") + "')";
      };
      if (t3) {
        let s3;
        for (t3 = (t3 = t3.replace(/[\u0000-\u001F]/g, "")).replace(/\\[\"\';:\uFEFF]/g, f2).replace(/\"[^\"]+\"|\'[^\']+\'/g, (e3) => e3.replace(/[;:]/g, f2)); s3 = o2.exec(t3); ) {
          o2.lastIndex = s3.index + s3[0].length;
          let t4 = s3[1].replace(r2, "").toLowerCase(), l4 = s3[2].replace(r2, "");
          if (t4 && l4) {
            if (t4 = h2(t4), l4 = h2(l4), -1 !== t4.indexOf("\uFEFF") || -1 !== t4.indexOf('"')) continue;
            if (!e2.allow_script_urls && ("behavior" === t4 || /expression\s*\(|\/\*|\*\//.test(l4))) continue;
            "font-weight" === t4 && "700" === l4 ? l4 = "bold" : "color" !== t4 && "background-color" !== t4 || (l4 = l4.toLowerCase()), l4 = l4.replace(n2, b2), a3[t4] = i4 ? g2(l4, true) : l4;
          }
        }
        u2("border", "", true), u2("border", "-width"), u2("border", "-color"), u2("border", "-style"), u2("padding", ""), u2("margin", ""), "border", y2 = "border-style", C2 = "border-color", m2(v2 = "border-width") && m2(y2) && m2(C2) && (a3.border = a3[v2] + " " + a3[y2] + " " + a3[C2], delete a3[v2], delete a3[y2], delete a3[C2]), "medium none" === a3.border && delete a3.border, "none" === a3["border-image"] && delete a3["border-image"];
      }
      var v2, y2, C2;
      return a3;
    }, serialize: (e3, t3) => {
      let n3 = "";
      const o3 = (t4, o4) => {
        const r3 = o4[t4];
        if (r3) for (let t5 = 0, o5 = r3.length; t5 < o5; t5++) {
          const o6 = r3[t5], s3 = e3[o6];
          s3 && (n3 += (n3.length > 0 ? " " : "") + o6 + ": " + s3 + ";");
        }
      };
      return t3 && a2 ? (o3("*", a2), o3(t3, a2)) : fe(e3, (e4, o4) => {
        e4 && ((e5, t4) => {
          if (!i3 || !t4) return true;
          let n4 = i3["*"];
          return !(n4 && n4[e5] || (n4 = i3[t4], n4 && n4[e5]));
        })(o4, t3) && (n3 += (n3.length > 0 ? " " : "") + o4 + ": " + e4 + ";");
      }), n3;
    } };
    return d2;
  }, Is = { keyLocation: true, layerX: true, layerY: true, returnValue: true, webkitMovementX: true, webkitMovementY: true, keyIdentifier: true, mozPressure: true }, Fs = (e2, t2) => {
    const n2 = null != t2 ? t2 : {};
    for (const t3 in e2) xe(Is, t3) || (n2[t3] = e2[t3]);
    return C(e2.composedPath) && (n2.composedPath = () => e2.composedPath()), n2;
  }, Us = (e2, t2, n2, o2) => {
    var r2;
    const s2 = Fs(t2, o2);
    return s2.type = e2, y(s2.target) && (s2.target = null !== (r2 = s2.srcElement) && void 0 !== r2 ? r2 : n2), ((e3) => y(e3.preventDefault) || ((e4) => e4 instanceof Event || w(e4.initEvent))(e3))(t2) && (s2.preventDefault = () => {
      s2.defaultPrevented = true, s2.isDefaultPrevented = L, w(t2.preventDefault) && t2.preventDefault();
    }, s2.stopPropagation = () => {
      s2.cancelBubble = true, s2.isPropagationStopped = L, w(t2.stopPropagation) && t2.stopPropagation();
    }, s2.stopImmediatePropagation = () => {
      s2.isImmediatePropagationStopped = L, s2.stopPropagation();
    }, ((e3) => e3.isDefaultPrevented === L || e3.isDefaultPrevented === P)(s2) || (s2.isDefaultPrevented = true === s2.defaultPrevented ? L : P, s2.isPropagationStopped = true === s2.cancelBubble ? L : P, s2.isImmediatePropagationStopped = P)), s2;
  }, zs = /^(?:mouse|contextmenu)|click/, js = (e2, t2, n2, o2) => {
    e2.addEventListener(t2, n2, o2 || false);
  }, Hs = (e2, t2, n2, o2) => {
    e2.removeEventListener(t2, n2, o2 || false);
  }, $s = (e2, t2) => {
    const n2 = Us(e2.type, e2, document, t2);
    if (((e3) => C(e3) && zs.test(e3.type))(e2) && v(e2.pageX) && !v(e2.clientX)) {
      const t3 = n2.target.ownerDocument || document, o2 = t3.documentElement, r2 = t3.body, s2 = n2;
      s2.pageX = e2.clientX + (o2 && o2.scrollLeft || r2 && r2.scrollLeft || 0) - (o2 && o2.clientLeft || r2 && r2.clientLeft || 0), s2.pageY = e2.clientY + (o2 && o2.scrollTop || r2 && r2.scrollTop || 0) - (o2 && o2.clientTop || r2 && r2.clientTop || 0);
    }
    return n2;
  }, Vs = (e2, t2, n2) => {
    const o2 = e2.document, r2 = { type: "ready" };
    if (n2.domLoaded) return void t2(r2);
    const s2 = () => {
      Hs(e2, "DOMContentLoaded", s2), Hs(e2, "load", s2), n2.domLoaded || (n2.domLoaded = true, t2(r2)), e2 = null;
    };
    "complete" === o2.readyState || "interactive" === o2.readyState && o2.body ? s2() : js(e2, "DOMContentLoaded", s2), n2.domLoaded || js(e2, "load", s2);
  };
  class qs {
    constructor() {
      this.domLoaded = false, this.events = {}, this.count = 1, this.expando = "mce-data-" + (+/* @__PURE__ */ new Date()).toString(32), this.hasFocusIn = "onfocusin" in document.documentElement, this.count = 1;
    }
    bind(e2, t2, n2, o2) {
      const r2 = this;
      let s2;
      const a2 = window, i3 = (e3) => {
        r2.executeHandlers($s(e3 || a2.event), l2);
      };
      if (!e2 || Ro(e2) || To(e2)) return n2;
      let l2;
      e2[r2.expando] ? l2 = e2[r2.expando] : (l2 = r2.count++, e2[r2.expando] = l2, r2.events[l2] = {}), o2 = o2 || e2;
      const d2 = t2.split(" ");
      let c2 = d2.length;
      for (; c2--; ) {
        let t3 = d2[c2], u2 = i3, m2 = false, f2 = false;
        "DOMContentLoaded" === t3 && (t3 = "ready"), r2.domLoaded && "ready" === t3 && "complete" === e2.readyState ? n2.call(o2, $s({ type: t3 })) : (r2.hasFocusIn || "focusin" !== t3 && "focusout" !== t3 || (m2 = true, f2 = "focusin" === t3 ? "focus" : "blur", u2 = (e3) => {
          const t4 = $s(e3 || a2.event);
          t4.type = "focus" === t4.type ? "focusin" : "focusout", r2.executeHandlers(t4, l2);
        }), s2 = r2.events[l2][t3], s2 ? "ready" === t3 && r2.domLoaded ? n2($s({ type: t3 })) : s2.push({ func: n2, scope: o2 }) : (r2.events[l2][t3] = s2 = [{ func: n2, scope: o2 }], s2.fakeName = f2, s2.capture = m2, s2.nativeHandler = u2, "ready" === t3 ? Vs(e2, u2, r2) : js(e2, f2 || t3, u2, m2)));
      }
      return e2 = s2 = null, n2;
    }
    unbind(e2, t2, n2) {
      if (!e2 || Ro(e2) || To(e2)) return this;
      const o2 = e2[this.expando];
      if (o2) {
        let r2 = this.events[o2];
        if (t2) {
          const o3 = t2.split(" ");
          let s2 = o3.length;
          for (; s2--; ) {
            const t3 = o3[s2], a2 = r2[t3];
            if (a2) {
              if (n2) {
                let e3 = a2.length;
                for (; e3--; ) if (a2[e3].func === n2) {
                  const n3 = a2.nativeHandler, o4 = a2.fakeName, s3 = a2.capture, i3 = a2.slice(0, e3).concat(a2.slice(e3 + 1));
                  i3.nativeHandler = n3, i3.fakeName = o4, i3.capture = s3, r2[t3] = i3;
                }
              }
              n2 && 0 !== a2.length || (delete r2[t3], Hs(e2, a2.fakeName || t3, a2.nativeHandler, a2.capture));
            }
          }
        } else fe(r2, (t3, n3) => {
          Hs(e2, t3.fakeName || n3, t3.nativeHandler, t3.capture);
        }), r2 = {};
        for (const e3 in r2) if (xe(r2, e3)) return this;
        delete this.events[o2];
        try {
          delete e2[this.expando];
        } catch (t3) {
          e2[this.expando] = null;
        }
      }
      return this;
    }
    fire(e2, t2, n2) {
      return this.dispatch(e2, t2, n2);
    }
    dispatch(e2, t2, n2) {
      if (!e2 || Ro(e2) || To(e2)) return this;
      const o2 = $s({ type: t2, target: e2 }, n2);
      do {
        const t3 = e2[this.expando];
        t3 && this.executeHandlers(o2, t3), e2 = e2.parentNode || e2.ownerDocument || e2.defaultView || e2.parentWindow;
      } while (e2 && !o2.isPropagationStopped());
      return this;
    }
    clean(e2) {
      if (!e2 || Ro(e2) || To(e2)) return this;
      if (e2[this.expando] && this.unbind(e2), e2.getElementsByTagName || (e2 = e2.document), e2 && e2.getElementsByTagName) {
        this.unbind(e2);
        const t2 = e2.getElementsByTagName("*");
        let n2 = t2.length;
        for (; n2--; ) e2 = t2[n2], e2[this.expando] && this.unbind(e2);
      }
      return this;
    }
    destroy() {
      this.events = {};
    }
    cancel(e2) {
      return e2 && (e2.preventDefault(), e2.stopImmediatePropagation()), false;
    }
    executeHandlers(e2, t2) {
      const n2 = this.events[t2], o2 = n2 && n2[e2.type];
      if (o2) for (let t3 = 0, n3 = o2.length; t3 < n3; t3++) {
        const n4 = o2[t3];
        if (n4 && false === n4.func.call(n4.scope, e2) && e2.preventDefault(), e2.isImmediatePropagationStopped()) return;
      }
    }
  }
  qs.Event = new qs();
  const Ws = Tt.each, Ks = Tt.grep, Gs = "data-mce-style", Ys = Tt.makeMap("fill-opacity font-weight line-height opacity orphans widows z-index zoom", " "), Xs = (e2, t2, n2) => {
    y(n2) || "" === n2 ? Yt(e2, t2) : Vt(e2, t2, n2);
  }, Qs = (e2) => e2.replace(/[A-Z]/g, (e3) => "-" + e3.toLowerCase()), Js = (e2, t2) => {
    let n2 = 0;
    if (e2) for (let o2 = e2.nodeType, r2 = e2.previousSibling; r2; r2 = r2.previousSibling) {
      const e3 = r2.nodeType;
      (!t2 || !Ro(r2) || e3 !== o2 && r2.data.length) && (n2++, o2 = e3);
    }
    return n2;
  }, Zs = (e2, t2) => {
    const n2 = Wt(t2, "style"), o2 = e2.serialize(e2.parse(n2), Lt(t2));
    Xs(t2, Gs, o2);
  }, ea = (e2, t2, n2) => {
    const o2 = Qs(t2);
    y(n2) || "" === n2 ? Gn(e2, o2) : Hn(e2, o2, ((e3, t3) => x(e3) ? xe(Ys, t3) ? e3 + "" : e3 + "px" : e3)(n2, o2));
  }, ta = (e2, t2 = {}) => {
    const n2 = {}, o2 = window, r2 = {};
    let s2 = 0;
    const a2 = Jo.forElement(mn(e2), { contentCssCors: t2.contentCssCors, referrerPolicy: t2.referrerPolicy }), i3 = [], l2 = t2.schema ? t2.schema : Ls({}), d2 = Ms({ url_converter: t2.url_converter, url_converter_scope: t2.url_converter_scope }, t2.schema), c2 = t2.ownEvents ? new qs() : qs.Event, u2 = l2.getBlockElements(), f2 = (t3) => t3 && e2 && m(t3) ? e2.getElementById(t3) : t3, g2 = (e3) => {
      const t3 = f2(e3);
      return C(t3) ? mn(t3) : null;
    }, h2 = (e3, t3, n3 = "") => {
      let o3;
      const r3 = g2(e3);
      if (C(r3) && Ft(r3)) {
        const e4 = Y2[t3];
        o3 = e4 && e4.get ? e4.get(r3.dom, t3) : Wt(r3, t3);
      }
      return C(o3) ? o3 : n3;
    }, b2 = (e3) => {
      const t3 = f2(e3);
      return y(t3) ? [] : t3.attributes;
    }, v2 = (e3, n3, o3) => {
      T2(e3, (e4) => {
        if (yo(e4)) {
          const r3 = mn(e4), s3 = "" === o3 ? null : o3, a3 = Wt(r3, n3), i4 = Y2[n3];
          i4 && i4.set ? i4.set(r3.dom, s3, n3) : Xs(r3, n3, s3), a3 !== s3 && t2.onSetAttrib && t2.onSetAttrib({ attrElm: r3.dom, attrName: n3, attrValue: s3 });
        }
      });
    }, x2 = () => t2.root_element || e2.body, k2 = (t3, n3) => ((e3, t4, n4) => {
      let o3 = 0, r3 = 0;
      const s3 = e3.ownerDocument;
      if (n4 = n4 || e3, t4) {
        if (n4 === e3 && t4.getBoundingClientRect && "static" === Vn(mn(e3), "position")) {
          const n5 = t4.getBoundingClientRect();
          return o3 = n5.left + (s3.documentElement.scrollLeft || e3.scrollLeft) - s3.documentElement.clientLeft, r3 = n5.top + (s3.documentElement.scrollTop || e3.scrollTop) - s3.documentElement.clientTop, { x: o3, y: r3 };
        }
        let a3 = t4;
        for (; a3 && a3 !== n4 && a3.nodeType && !Vo(a3, n4); ) {
          const e4 = a3;
          o3 += e4.offsetLeft || 0, r3 += e4.offsetTop || 0, a3 = e4.offsetParent;
        }
        for (a3 = t4.parentNode; a3 && a3 !== n4 && a3.nodeType && !Vo(a3, n4); ) o3 -= a3.scrollLeft || 0, r3 -= a3.scrollTop || 0, a3 = a3.parentNode;
        r3 += ((e4) => Ho.isFirefox() && "table" === Lt(e4) ? $o(Nn(e4)).filter((e5) => "caption" === Lt(e5)).bind((e5) => $o(En(e5)).map((t5) => {
          const n5 = t5.dom.offsetTop, o4 = e5.dom.offsetTop, r4 = e5.dom.offsetHeight;
          return n5 <= o4 ? -r4 : 0;
        })).getOr(0) : 0)(mn(t4));
      }
      return { x: o3, y: r3 };
    })(e2.body, f2(t3), n3), _2 = (e3, t3, n3) => {
      const o3 = f2(e3);
      if (!y(o3) && yo(o3)) return n3 ? Vn(mn(o3), Qs(t3)) : ("float" === (t3 = t3.replace(/-(\D)/g, (e4, t4) => t4.toUpperCase())) && (t3 = "cssFloat"), o3.style ? o3.style[t3] : void 0);
    }, E2 = (e3) => {
      const t3 = f2(e3);
      if (!t3) return { w: 0, h: 0 };
      let n3 = _2(t3, "width"), o3 = _2(t3, "height");
      return n3 && -1 !== n3.indexOf("px") || (n3 = "0"), o3 && -1 !== o3.indexOf("px") || (o3 = "0"), { w: parseInt(n3, 10) || t3.offsetWidth || t3.clientWidth, h: parseInt(o3, 10) || t3.offsetHeight || t3.clientHeight };
    }, R2 = (e3, t3) => {
      if (!e3) return false;
      const n3 = p(e3) ? e3 : [e3];
      return H(n3, (e4) => pn(mn(e4), t3));
    }, A2 = (e3, t3, n3, o3) => {
      const r3 = [];
      let s3 = f2(e3);
      o3 = void 0 === o3;
      const a3 = n3 || ("BODY" !== x2().nodeName ? x2().parentNode : null);
      if (m(t3)) if ("*" === t3) t3 = yo;
      else {
        const e4 = t3;
        t3 = (t4) => R2(t4, e4);
      }
      for (; s3 && !(s3 === a3 || y(s3.nodeType) || Bo(s3) || Do(s3)); ) {
        if (!t3 || t3(s3)) {
          if (!o3) return [s3];
          r3.push(s3);
        }
        s3 = s3.parentNode;
      }
      return o3 ? r3 : null;
    }, O2 = (e3, t3, n3) => {
      let o3 = t3;
      if (e3) {
        m(t3) && (o3 = (e4) => R2(e4, t3));
        for (let t4 = e3[n3]; t4; t4 = t4[n3]) if (w(o3) && o3(t4)) return t4;
      }
      return null;
    }, T2 = function(e3, t3, n3) {
      const o3 = null != n3 ? n3 : this;
      if (p(e3)) {
        const n4 = [];
        return Ws(e3, (e4, r3) => {
          const s3 = f2(e4);
          s3 && n4.push(t3.call(o3, s3, r3));
        }), n4;
      }
      {
        const n4 = f2(e3);
        return !!n4 && t3.call(o3, n4);
      }
    }, B2 = (e3, t3) => {
      T2(e3, (e4) => {
        fe(t3, (t4, n3) => {
          v2(e4, n3, t4);
        });
      });
    }, D2 = (e3, t3) => {
      T2(e3, (e4) => {
        const n3 = mn(e4);
        ro(n3, t3);
      });
    }, P2 = (t3, n3, o3, r3, s3) => T2(t3, (t4) => {
      const a3 = m(n3) ? e2.createElement(n3) : n3;
      return C(o3) && B2(a3, o3), r3 && (!m(r3) && r3.nodeType ? a3.appendChild(r3) : m(r3) && D2(a3, r3)), s3 ? a3 : t4.appendChild(a3);
    }), L2 = (t3, n3, o3) => P2(e2.createElement(t3), t3, n3, o3, true), M2 = xs.encodeAllRaw, I2 = (e3, t3) => T2(e3, (e4) => {
      const n3 = mn(e4);
      return t3 && V(Nn(n3), (e5) => {
        Ut(e5) && 0 === e5.dom.length ? to(e5) : Yn(n3, e5);
      }), to(n3), n3.dom;
    }), F2 = (e3, t3, n3) => {
      T2(e3, (e4) => {
        if (yo(e4)) {
          const o3 = mn(e4), r3 = t3.split(" ");
          V(r3, (e5) => {
            C(n3) ? (n3 ? nn : rn)(o3, e5) : ((e6, t4) => {
              const n4 = Jt(e6) ? e6.dom.classList.toggle(t4) : ((e7, t5) => j(Zt(e7), t5) ? tn(e7, t5) : en(e7, t5))(e6, t4);
              on(e6);
            })(o3, e5);
          });
        }
      });
    }, U2 = (e3, t3, n3) => T2(t3, (o3) => {
      var r3;
      const s3 = p(t3) ? e3.cloneNode(true) : e3;
      return n3 && Ws(Ks(o3.childNodes), (e4) => {
        s3.appendChild(e4);
      }), null === (r3 = o3.parentNode) || void 0 === r3 || r3.replaceChild(s3, o3), o3;
    }), z2 = (e3) => {
      if (yo(e3)) {
        const t3 = "a" === e3.nodeName.toLowerCase() && !h2(e3, "href") && h2(e3, "id");
        if (h2(e3, "name") || h2(e3, "data-mce-bookmark") || t3) return true;
      }
      return false;
    }, $3 = () => e2.createRange(), q2 = (n3, r3, s3, a3) => {
      if (p(n3)) {
        let e3 = n3.length;
        const t3 = [];
        for (; e3--; ) t3[e3] = q2(n3[e3], r3, s3, a3);
        return t3;
      }
      return !t2.collect || n3 !== e2 && n3 !== o2 || i3.push([n3, r3, s3, a3]), c2.bind(n3, r3, s3, a3 || G2);
    }, W2 = (t3, n3, r3) => {
      if (p(t3)) {
        let e3 = t3.length;
        const o3 = [];
        for (; e3--; ) o3[e3] = W2(t3[e3], n3, r3);
        return o3;
      }
      if (i3.length > 0 && (t3 === e2 || t3 === o2)) {
        let e3 = i3.length;
        for (; e3--; ) {
          const [o3, s3, a3] = i3[e3];
          t3 !== o3 || n3 && n3 !== s3 || r3 && r3 !== a3 || c2.unbind(o3, s3, a3);
        }
      }
      return c2.unbind(t3, n3, r3);
    }, K2 = (e3) => {
      if (e3 && yo(e3)) {
        const t3 = e3.getAttribute("data-mce-contenteditable");
        return t3 && "inherit" !== t3 ? t3 : "inherit" !== e3.contentEditable ? e3.contentEditable : null;
      }
      return null;
    }, G2 = { doc: e2, settings: t2, win: o2, files: r2, stdMode: true, boxModel: true, styleSheetLoader: a2, boundEvents: i3, styles: d2, schema: l2, events: c2, isBlock: (e3) => m(e3) ? xe(u2, e3) : yo(e3) && xe(u2, e3.nodeName), root: null, clone: (e3, t3) => e3.cloneNode(t3), getRoot: x2, getViewPort: (e3) => {
      const t3 = ho(e3);
      return { x: t3.x, y: t3.y, w: t3.width, h: t3.height };
    }, getRect: (e3) => {
      const t3 = f2(e3), n3 = k2(t3), o3 = E2(t3);
      return { x: n3.x, y: n3.y, w: o3.w, h: o3.h };
    }, getSize: E2, getParent: (e3, t3, n3) => {
      const o3 = A2(e3, t3, n3, false);
      return o3 && o3.length > 0 ? o3[0] : null;
    }, getParents: A2, get: f2, getNext: (e3, t3) => O2(e3, t3, "nextSibling"), getPrev: (e3, t3) => O2(e3, t3, "previousSibling"), select: (n3, o3) => {
      var r3, s3;
      const a3 = null !== (s3 = null !== (r3 = f2(o3)) && void 0 !== r3 ? r3 : t2.root_element) && void 0 !== s3 ? s3 : e2;
      return w(a3.querySelectorAll) ? de(a3.querySelectorAll(n3)) : [];
    }, is: R2, add: P2, create: L2, createHTML: (e3, t3, n3 = "") => {
      let o3 = "<" + e3;
      for (const e4 in t3) ke(t3, e4) && (o3 += " " + e4 + '="' + M2(t3[e4]) + '"');
      return Ke(n3) && xe(l2.getVoidElements(), e3) ? o3 + " />" : o3 + ">" + n3 + "</" + e3 + ">";
    }, createFragment: (t3) => {
      const n3 = e2.createElement("div"), o3 = e2.createDocumentFragment();
      let r3;
      for (o3.appendChild(n3), t3 && (n3.innerHTML = t3); r3 = n3.firstChild; ) o3.appendChild(r3);
      return o3.removeChild(n3), o3;
    }, remove: I2, setStyle: (e3, n3, o3) => {
      T2(e3, (e4) => {
        const r3 = mn(e4);
        ea(r3, n3, o3), t2.update_styles && Zs(d2, r3);
      });
    }, getStyle: _2, setStyles: (e3, n3) => {
      T2(e3, (e4) => {
        const o3 = mn(e4);
        fe(n3, (e5, t3) => {
          ea(o3, t3, e5);
        }), t2.update_styles && Zs(d2, o3);
      });
    }, removeAllAttribs: (e3) => T2(e3, (e4) => {
      const t3 = e4.attributes;
      for (let n3 = t3.length - 1; n3 >= 0; n3--) e4.removeAttributeNode(t3.item(n3));
    }), setAttrib: v2, setAttribs: B2, getAttrib: h2, getPos: k2, parseStyle: (e3) => d2.parse(e3), serializeStyle: (e3, t3) => d2.serialize(e3, t3), addStyle: (t3) => {
      if (G2 !== ta.DOM && e2 === document) {
        if (n2[t3]) return;
        n2[t3] = true;
      }
      let o3 = e2.getElementById("mceDefaultStyles");
      if (!o3) {
        o3 = e2.createElement("style"), o3.id = "mceDefaultStyles", o3.type = "text/css";
        const t4 = e2.head;
        t4.firstChild ? t4.insertBefore(o3, t4.firstChild) : t4.appendChild(o3);
      }
      o3.styleSheet ? o3.styleSheet.cssText += t3 : o3.appendChild(e2.createTextNode(t3));
    }, loadCSS: (e3) => {
      e3 || (e3 = ""), V(e3.split(","), (e4) => {
        r2[e4] = true, a2.load(e4).catch(S);
      });
    }, addClass: (e3, t3) => {
      F2(e3, t3, true);
    }, removeClass: (e3, t3) => {
      F2(e3, t3, false);
    }, hasClass: (e3, t3) => {
      const n3 = g2(e3), o3 = t3.split(" ");
      return C(n3) && te(o3, (e4) => sn(n3, e4));
    }, toggleClass: F2, show: (e3) => {
      T2(e3, (e4) => Gn(mn(e4), "display"));
    }, hide: (e3) => {
      T2(e3, (e4) => Hn(mn(e4), "display", "none"));
    }, isHidden: (e3) => {
      const t3 = g2(e3);
      return C(t3) && Bt(Wn(t3, "display"), "none");
    }, uniqueId: (e3) => (e3 || "mce_") + s2++, setHTML: D2, getOuterHTML: (e3) => {
      const t3 = g2(e3);
      return C(t3) ? yo(t3.dom) ? t3.dom.outerHTML : ((e4) => {
        const t4 = cn("div"), n3 = mn(e4.dom.cloneNode(true));
        return Jn(t4, n3), oo(t4);
      })(t3) : "";
    }, setOuterHTML: (e3, t3) => {
      T2(e3, (e4) => {
        yo(e4) && (e4.outerHTML = t3);
      });
    }, decode: xs.decode, encode: M2, insertAfter: (e3, t3) => {
      const n3 = f2(t3);
      return T2(e3, (e4) => {
        const t4 = null == n3 ? void 0 : n3.parentNode, o3 = null == n3 ? void 0 : n3.nextSibling;
        return t4 && (o3 ? t4.insertBefore(e4, o3) : t4.appendChild(e4)), e4;
      });
    }, replace: U2, rename: (e3, t3) => {
      if (e3.nodeName !== t3.toUpperCase()) {
        const n3 = L2(t3);
        return Ws(b2(e3), (t4) => {
          v2(n3, t4.nodeName, h2(e3, t4.nodeName));
        }), U2(n3, e3, true), n3;
      }
      return e3;
    }, findCommonAncestor: (e3, t3) => {
      let n3 = e3;
      for (; n3; ) {
        let e4 = t3;
        for (; e4 && n3 !== e4; ) e4 = e4.parentNode;
        if (n3 === e4) break;
        n3 = n3.parentNode;
      }
      return !n3 && e3.ownerDocument ? e3.ownerDocument.documentElement : n3;
    }, run: T2, getAttribs: b2, isEmpty: (e3, t3) => {
      let n3 = 0;
      if (z2(e3)) return false;
      const o3 = e3.firstChild;
      if (o3) {
        const r3 = new Zo(o3, e3), s3 = l2 ? l2.getWhitespaceElements() : {}, a3 = t3 || (l2 ? l2.getNonEmptyElements() : null);
        let i4 = o3;
        do {
          if (yo(i4)) {
            const e4 = i4.getAttribute("data-mce-bogus");
            if (e4) {
              i4 = r3.next("all" === e4);
              continue;
            }
            const t4 = i4.nodeName.toLowerCase();
            if (a3 && a3[t4]) {
              if ("br" === t4) {
                n3++, i4 = r3.next();
                continue;
              }
              return false;
            }
            if (z2(i4)) return false;
          }
          if (To(i4)) return false;
          if (Ro(i4) && !Jr(i4.data)) return false;
          if (Ro(i4) && i4.parentNode && s3[i4.parentNode.nodeName] && Jr(i4.data)) return false;
          i4 = r3.next();
        } while (i4);
      }
      return n3 <= 1;
    }, createRng: $3, nodeIndex: Js, split: (e3, t3, n3) => {
      let o3, r3, s3 = $3();
      if (e3 && t3 && e3.parentNode && t3.parentNode) {
        const a3 = e3.parentNode;
        return s3.setStart(a3, Js(e3)), s3.setEnd(t3.parentNode, Js(t3)), o3 = s3.extractContents(), s3 = $3(), s3.setStart(t3.parentNode, Js(t3) + 1), s3.setEnd(a3, Js(e3) + 1), r3 = s3.extractContents(), a3.insertBefore(ls(G2, o3), e3), n3 ? a3.insertBefore(n3, e3) : a3.insertBefore(t3, e3), a3.insertBefore(ls(G2, r3), e3), I2(e3), n3 || t3;
      }
    }, bind: q2, unbind: W2, fire: (e3, t3, n3) => c2.dispatch(e3, t3, n3), dispatch: (e3, t3, n3) => c2.dispatch(e3, t3, n3), getContentEditable: K2, getContentEditableParent: (e3) => {
      const t3 = x2();
      let n3 = null;
      for (let o3 = e3; o3 && o3 !== t3 && (n3 = K2(o3), null === n3); o3 = o3.parentNode) ;
      return n3;
    }, destroy: () => {
      if (i3.length > 0) {
        let e3 = i3.length;
        for (; e3--; ) {
          const [t3, n3, o3] = i3[e3];
          c2.unbind(t3, n3, o3);
        }
      }
      fe(r2, (e3, t3) => {
        a2.unload(t3), delete r2[t3];
      });
    }, isChildOf: (e3, t3) => e3 === t3 || t3.contains(e3), dumpRng: (e3) => "startContainer: " + e3.startContainer.nodeName + ", startOffset: " + e3.startOffset + ", endContainer: " + e3.endContainer.nodeName + ", endOffset: " + e3.endOffset }, Y2 = ((e3, t3, n3) => {
      const o3 = t3.keep_values, r3 = { set: (e4, o4, r4) => {
        const s4 = mn(e4);
        w(t3.url_converter) && C(o4) && (o4 = t3.url_converter.call(t3.url_converter_scope || n3(), String(o4), r4, e4)), Xs(s4, "data-mce-" + r4, o4), Xs(s4, r4, o4);
      }, get: (e4, t4) => {
        const n4 = mn(e4);
        return Wt(n4, "data-mce-" + t4) || Wt(n4, t4);
      } }, s3 = { style: { set: (t4, n4) => {
        const r4 = mn(t4);
        o3 && Xs(r4, Gs, n4), Yt(r4, "style"), m(n4) && $n(r4, e3.parse(n4));
      }, get: (t4) => {
        const n4 = mn(t4), o4 = Wt(n4, Gs) || Wt(n4, "style");
        return e3.serialize(e3.parse(o4), Lt(n4));
      } } };
      return o3 && (s3.href = s3.src = r3), s3;
    })(d2, t2, N(G2));
    return G2;
  };
  ta.DOM = ta(document), ta.nodeIndex = Js;
  const na = ta.DOM;
  class oa {
    constructor(e2 = {}) {
      this.states = {}, this.queue = [], this.scriptLoadedCallbacks = {}, this.queueLoadedCallbacks = [], this.loading = false, this.settings = e2;
    }
    _setReferrerPolicy(e2) {
      this.settings.referrerPolicy = e2;
    }
    loadScript(e2) {
      return new Promise((t2, n2) => {
        const o2 = na;
        let r2;
        const s2 = () => {
          o2.remove(a2), r2 && (r2.onerror = r2.onload = r2 = null);
        }, a2 = o2.uniqueId();
        r2 = document.createElement("script"), r2.id = a2, r2.type = "text/javascript", r2.src = Tt._addCacheSuffix(e2), this.settings.referrerPolicy && o2.setAttrib(r2, "referrerpolicy", this.settings.referrerPolicy), r2.onload = () => {
          s2(), t2();
        }, r2.onerror = () => {
          s2(), n2("Failed to load script: " + e2);
        }, (document.getElementsByTagName("head")[0] || document.body).appendChild(r2);
      });
    }
    isDone(e2) {
      return 2 === this.states[e2];
    }
    markDone(e2) {
      this.states[e2] = 2;
    }
    add(e2) {
      const t2 = this;
      return t2.queue.push(e2), void 0 === t2.states[e2] && (t2.states[e2] = 0), new Promise((n2, o2) => {
        t2.scriptLoadedCallbacks[e2] || (t2.scriptLoadedCallbacks[e2] = []), t2.scriptLoadedCallbacks[e2].push({ resolve: n2, reject: o2 });
      });
    }
    load(e2) {
      return this.add(e2);
    }
    remove(e2) {
      delete this.states[e2], delete this.scriptLoadedCallbacks[e2];
    }
    loadQueue() {
      const e2 = this.queue;
      return this.queue = [], this.loadScripts(e2);
    }
    loadScripts(e2) {
      const t2 = this, n2 = (e3, n3) => {
        we(t2.scriptLoadedCallbacks, n3).each((t3) => {
          V(t3, (t4) => t4[e3](n3));
        }), delete t2.scriptLoadedCallbacks[n3];
      }, o2 = (e3) => {
        const t3 = K(e3, (e4) => "rejected" === e4.status);
        return t3.length > 0 ? Promise.reject(ee(t3, ({ reason: e4 }) => p(e4) ? e4 : [e4])) : Promise.resolve();
      }, r2 = (e3) => Promise.allSettled($2(e3, (e4) => 2 === t2.states[e4] ? (n2("resolve", e4), Promise.resolve()) : 3 === t2.states[e4] ? (n2("reject", e4), Promise.reject(e4)) : (t2.states[e4] = 1, t2.loadScript(e4).then(() => {
        t2.states[e4] = 2, n2("resolve", e4);
        const s3 = t2.queue;
        return s3.length > 0 ? (t2.queue = [], r2(s3).then(o2)) : Promise.resolve();
      }, () => (t2.states[e4] = 3, n2("reject", e4), Promise.reject(e4)))))), s2 = (e3) => (t2.loading = true, r2(e3).then((e4) => {
        t2.loading = false;
        const n3 = t2.queueLoadedCallbacks.shift();
        return M.from(n3).each(D), o2(e4);
      })), a2 = Se(e2);
      return t2.loading ? new Promise((e3, n3) => {
        t2.queueLoadedCallbacks.push(() => s2(a2).then(e3, n3));
      }) : s2(a2);
    }
  }
  oa.ScriptLoader = new oa();
  const ra = (e2) => {
    let t2 = e2;
    return { get: () => t2, set: (e3) => {
      t2 = e3;
    } };
  }, sa = {}, aa = ra("en"), ia = () => we(sa, aa.get()), la = { getData: () => ge(sa, (e2) => ({ ...e2 })), setCode: (e2) => {
    e2 && aa.set(e2);
  }, getCode: () => aa.get(), add: (e2, t2) => {
    let n2 = sa[e2];
    n2 || (sa[e2] = n2 = {}), fe(t2, (e3, t3) => {
      n2[t3.toLowerCase()] = e3;
    });
  }, translate: (e2) => {
    const t2 = ia().getOr({}), n2 = (e3) => w(e3) ? Object.prototype.toString.call(e3) : o2(e3) ? "" : "" + e3, o2 = (e3) => "" === e3 || null == e3, r2 = (e3) => {
      const o3 = n2(e3);
      return we(t2, o3.toLowerCase()).map(n2).getOr(o3);
    }, s2 = (e3) => e3.replace(/{context:\w+}$/, "");
    if (o2(e2)) return "";
    if (f(a2 = e2) && xe(a2, "raw")) return n2(e2.raw);
    var a2;
    if (((e3) => p(e3) && e3.length > 1)(e2)) {
      const t3 = e2.slice(1);
      return s2(r2(e2[0]).replace(/\{([0-9]+)\}/g, (e3, o3) => xe(t3, o3) ? n2(t3[o3]) : e3));
    }
    return s2(r2(e2));
  }, isRtl: () => ia().bind((e2) => we(e2, "_dir")).exists((e2) => "rtl" === e2), hasCode: (e2) => xe(sa, e2) }, da = () => {
    const e2 = [], t2 = {}, n2 = {}, o2 = [], r2 = (e3, t3) => {
      const n3 = K(o2, (n4) => n4.name === e3 && n4.state === t3);
      V(n3, (e4) => e4.resolve());
    }, s2 = (e3) => xe(t2, e3), a2 = (e3, n3) => {
      const o3 = la.getCode();
      !o3 || n3 && -1 === ("," + (n3 || "") + ",").indexOf("," + o3 + ",") || oa.ScriptLoader.add(t2[e3] + "/langs/" + o3 + ".js");
    }, i3 = (e3, t3 = "added") => "added" === t3 && ((e4) => xe(n2, e4))(e3) || "loaded" === t3 && s2(e3) ? Promise.resolve() : new Promise((n3) => {
      o2.push({ name: e3, state: t3, resolve: n3 });
    });
    return { items: e2, urls: t2, lookup: n2, get: (e3) => {
      if (n2[e3]) return n2[e3].instance;
    }, requireLangPack: (e3, t3) => {
      false !== da.languageLoad && (s2(e3) ? a2(e3, t3) : i3(e3, "loaded").then(() => a2(e3, t3)));
    }, add: (t3, o3) => (e2.push(o3), n2[t3] = { instance: o3 }, r2(t3, "added"), o3), remove: (e3) => {
      delete t2[e3], delete n2[e3];
    }, createUrl: (e3, t3) => m(t3) ? m(e3) ? { prefix: "", resource: t3, suffix: "" } : { prefix: e3.prefix, resource: t3, suffix: e3.suffix } : t3, load: (e3, o3) => {
      if (t2[e3]) return Promise.resolve();
      let s3 = m(o3) ? o3 : o3.prefix + o3.resource + o3.suffix;
      0 !== s3.indexOf("/") && -1 === s3.indexOf("://") && (s3 = da.baseURL + "/" + s3), t2[e3] = s3.substring(0, s3.lastIndexOf("/"));
      const a3 = () => (r2(e3, "loaded"), Promise.resolve());
      return n2[e3] ? a3() : oa.ScriptLoader.add(s3).then(a3);
    }, waitFor: i3 };
  };
  da.languageLoad = true, da.baseURL = "", da.PluginManager = da(), da.ThemeManager = da(), da.ModelManager = da();
  const ca = (e2) => {
    const t2 = ra(M.none()), n2 = () => t2.get().each((e3) => clearInterval(e3));
    return { clear: () => {
      n2(), t2.set(M.none());
    }, isSet: () => t2.get().isSome(), get: () => t2.get(), set: (o2) => {
      n2(), t2.set(M.some(setInterval(o2, e2)));
    } };
  }, ua = () => {
    const e2 = ((e3) => {
      const t2 = ra(M.none()), n2 = () => t2.get().each(e3);
      return { clear: () => {
        n2(), t2.set(M.none());
      }, isSet: () => t2.get().isSome(), get: () => t2.get(), set: (e4) => {
        n2(), t2.set(M.some(e4));
      } };
    })(S);
    return { ...e2, on: (t2) => e2.get().each(t2) };
  }, ma = (e2, t2) => {
    let n2 = null;
    return { cancel: () => {
      h(n2) || (clearTimeout(n2), n2 = null);
    }, throttle: (...o2) => {
      h(n2) && (n2 = setTimeout(() => {
        n2 = null, e2.apply(null, o2);
      }, t2));
    } };
  }, fa = (e2, t2) => {
    let n2 = null;
    const o2 = () => {
      h(n2) || (clearTimeout(n2), n2 = null);
    };
    return { cancel: o2, throttle: (...r2) => {
      o2(), n2 = setTimeout(() => {
        n2 = null, e2.apply(null, r2);
      }, t2);
    } };
  }, ga = N("mce-annotation"), pa = N("data-mce-annotation"), ha = N("data-mce-annotation-uid"), ba = N("data-mce-annotation-active"), va = N("data-mce-annotation-classes"), ya = N("data-mce-annotation-attrs"), Ca = (e2) => (t2) => bn(t2, e2), wa = (e2, t2) => {
    const n2 = e2.selection.getRng(), o2 = mn(n2.startContainer), r2 = mn(e2.getBody()), s2 = t2.fold(() => "." + ga(), (e3) => `[${pa()}="${e3}"]`), a2 = Rn(o2, n2.startOffset).getOr(o2);
    return Xo(a2, s2, Ca(r2)).bind((t3) => Kt(t3, `${ha()}`).bind((n3) => Kt(t3, `${pa()}`).map((t4) => {
      const o3 = ka(e2, n3);
      return { uid: n3, name: t4, elements: o3 };
    })));
  }, xa = (e2, t2) => Gt(e2, "data-mce-bogus") || fr(e2, '[data-mce-bogus="all"]', Ca(t2)), ka = (e2, t2) => {
    const n2 = mn(e2.getBody()), o2 = vr(n2, `[${ha()}="${t2}"]`);
    return K(o2, (e3) => !xa(e3, n2));
  }, Sa = (e2, t2) => {
    const n2 = mn(e2.getBody()), o2 = vr(n2, `[${pa()}="${t2}"]`), r2 = {};
    return V(o2, (e3) => {
      if (!xa(e3, n2)) {
        const t3 = Wt(e3, ha()), n3 = we(r2, t3).getOr([]);
        r2[t3] = n3.concat([e3]);
      }
    }), r2;
  };
  let _a = 0;
  const Ea = (e2) => {
    const t2 = (/* @__PURE__ */ new Date()).getTime(), n2 = Math.floor(1e9 * Math.random());
    return _a++, e2 + "_" + n2 + _a + String(t2);
  }, Na = (e2, t2) => mn(e2.dom.cloneNode(t2)), Ra = (e2) => Na(e2, false), Aa = (e2) => Na(e2, true), Oa = (e2, t2, n2 = P) => {
    const o2 = new Zo(e2, t2), r2 = (e3) => {
      let t3;
      do {
        t3 = o2[e3]();
      } while (t3 && !Ro(t3) && !n2(t3));
      return M.from(t3).filter(Ro);
    };
    return { current: () => M.from(o2.current()).filter(Ro), next: () => r2("next"), prev: () => r2("prev"), prev2: () => r2("prev2") };
  }, Ta = (e2, t2) => {
    const n2 = t2 || ((t3) => e2.isBlock(t3) || Po(t3) || Io(t3)), o2 = (e3, t3, n3, r2) => {
      if (Ro(e3)) {
        const n4 = r2(e3, t3, e3.data);
        if (-1 !== n4) return M.some({ container: e3, offset: n4 });
      }
      return n3().bind((e4) => o2(e4.container, e4.offset, n3, r2));
    };
    return { backwards: (t3, r2, s2, a2) => {
      const i3 = Oa(t3, null != a2 ? a2 : e2.getRoot(), n2);
      return o2(t3, r2, () => i3.prev().map((e3) => ({ container: e3, offset: e3.length })), s2).getOrNull();
    }, forwards: (t3, r2, s2, a2) => {
      const i3 = Oa(t3, null != a2 ? a2 : e2.getRoot(), n2);
      return o2(t3, r2, () => i3.next().map((e3) => ({ container: e3, offset: 0 })), s2).getOrNull();
    } };
  }, Ba = Math.round, Da = (e2) => e2 ? { left: Ba(e2.left), top: Ba(e2.top), bottom: Ba(e2.bottom), right: Ba(e2.right), width: Ba(e2.width), height: Ba(e2.height) } : { left: 0, top: 0, bottom: 0, right: 0, width: 0, height: 0 }, Pa = (e2, t2) => (e2 = Da(e2), t2 || (e2.left = e2.left + e2.width), e2.right = e2.left, e2.width = 0, e2), La = (e2, t2, n2) => e2 >= 0 && e2 <= Math.min(t2.height, n2.height) / 2, Ma = (e2, t2) => {
    const n2 = Math.min(t2.height / 2, e2.height / 2);
    return e2.bottom - n2 < t2.top || !(e2.top > t2.bottom) && La(t2.top - e2.bottom, e2, t2);
  }, Ia = (e2, t2) => e2.top > t2.bottom || !(e2.bottom < t2.top) && La(t2.bottom - e2.top, e2, t2), Fa = (e2, t2, n2) => {
    const o2 = Math.max(Math.min(t2, e2.left + e2.width), e2.left), r2 = Math.max(Math.min(n2, e2.top + e2.height), e2.top);
    return Math.sqrt((t2 - o2) * (t2 - o2) + (n2 - r2) * (n2 - r2));
  }, Ua = (e2) => {
    const t2 = e2.startContainer, n2 = e2.startOffset;
    return t2 === e2.endContainer && t2.hasChildNodes() && e2.endOffset === n2 + 1 ? t2.childNodes[n2] : null;
  }, za = (e2, t2) => {
    if (yo(e2) && e2.hasChildNodes()) {
      const n2 = e2.childNodes, o2 = ((e3, t3, n3) => Math.min(Math.max(e3, 0), n3))(t2, 0, n2.length - 1);
      return n2[o2];
    }
    return e2;
  }, ja = new RegExp("[\u0300-\u036F\u0483-\u0487\u0488-\u0489\u0591-\u05BD\u05BF\u05C1-\u05C2\u05C4-\u05C5\u05C7\u0610-\u061A\u064B-\u065F\u0670\u06D6-\u06DC\u06DF-\u06E4\u06E7-\u06E8\u06EA-\u06ED\u0711\u0730-\u074A\u07A6-\u07B0\u07EB-\u07F3\u0816-\u0819\u081B-\u0823\u0825-\u0827\u0829-\u082D\u0859-\u085B\u08E3-\u0902\u093A\u093C\u0941-\u0948\u094D\u0951-\u0957\u0962-\u0963\u0981\u09BC\u09BE\u09C1-\u09C4\u09CD\u09D7\u09E2-\u09E3\u0A01-\u0A02\u0A3C\u0A41-\u0A42\u0A47-\u0A48\u0A4B-\u0A4D\u0A51\u0A70-\u0A71\u0A75\u0A81-\u0A82\u0ABC\u0AC1-\u0AC5\u0AC7-\u0AC8\u0ACD\u0AE2-\u0AE3\u0B01\u0B3C\u0B3E\u0B3F\u0B41-\u0B44\u0B4D\u0B56\u0B57\u0B62-\u0B63\u0B82\u0BBE\u0BC0\u0BCD\u0BD7\u0C00\u0C3E-\u0C40\u0C46-\u0C48\u0C4A-\u0C4D\u0C55-\u0C56\u0C62-\u0C63\u0C81\u0CBC\u0CBF\u0CC2\u0CC6\u0CCC-\u0CCD\u0CD5-\u0CD6\u0CE2-\u0CE3\u0D01\u0D3E\u0D41-\u0D44\u0D4D\u0D57\u0D62-\u0D63\u0DCA\u0DCF\u0DD2-\u0DD4\u0DD6\u0DDF\u0E31\u0E34-\u0E3A\u0E47-\u0E4E\u0EB1\u0EB4-\u0EB9\u0EBB-\u0EBC\u0EC8-\u0ECD\u0F18-\u0F19\u0F35\u0F37\u0F39\u0F71-\u0F7E\u0F80-\u0F84\u0F86-\u0F87\u0F8D-\u0F97\u0F99-\u0FBC\u0FC6\u102D-\u1030\u1032-\u1037\u1039-\u103A\u103D-\u103E\u1058-\u1059\u105E-\u1060\u1071-\u1074\u1082\u1085-\u1086\u108D\u109D\u135D-\u135F\u1712-\u1714\u1732-\u1734\u1752-\u1753\u1772-\u1773\u17B4-\u17B5\u17B7-\u17BD\u17C6\u17C9-\u17D3\u17DD\u180B-\u180D\u18A9\u1920-\u1922\u1927-\u1928\u1932\u1939-\u193B\u1A17-\u1A18\u1A1B\u1A56\u1A58-\u1A5E\u1A60\u1A62\u1A65-\u1A6C\u1A73-\u1A7C\u1A7F\u1AB0-\u1ABD\u1ABE\u1B00-\u1B03\u1B34\u1B36-\u1B3A\u1B3C\u1B42\u1B6B-\u1B73\u1B80-\u1B81\u1BA2-\u1BA5\u1BA8-\u1BA9\u1BAB-\u1BAD\u1BE6\u1BE8-\u1BE9\u1BED\u1BEF-\u1BF1\u1C2C-\u1C33\u1C36-\u1C37\u1CD0-\u1CD2\u1CD4-\u1CE0\u1CE2-\u1CE8\u1CED\u1CF4\u1CF8-\u1CF9\u1DC0-\u1DF5\u1DFC-\u1DFF\u200C-\u200D\u20D0-\u20DC\u20DD-\u20E0\u20E1\u20E2-\u20E4\u20E5-\u20F0\u2CEF-\u2CF1\u2D7F\u2DE0-\u2DFF\u302A-\u302D\u302E-\u302F\u3099-\u309A\uA66F\uA670-\uA672\uA674-\uA67D\uA69E-\uA69F\uA6F0-\uA6F1\uA802\uA806\uA80B\uA825-\uA826\uA8C4\uA8E0-\uA8F1\uA926-\uA92D\uA947-\uA951\uA980-\uA982\uA9B3\uA9B6-\uA9B9\uA9BC\uA9E5\uAA29-\uAA2E\uAA31-\uAA32\uAA35-\uAA36\uAA43\uAA4C\uAA7C\uAAB0\uAAB2-\uAAB4\uAAB7-\uAAB8\uAABE-\uAABF\uAAC1\uAAEC-\uAAED\uAAF6\uABE5\uABE8\uABED\uFB1E\uFE00-\uFE0F\uFE20-\uFE2F\uFF9E-\uFF9F]"), Ha = (e2) => m(e2) && e2.charCodeAt(0) >= 768 && ja.test(e2), $a = yo, Va = Gr, qa = xo("display", "block table"), Wa = xo("float", "left right"), Ka = /* @__PURE__ */ ((...e2) => (t2) => {
    for (let n2 = 0; n2 < e2.length; n2++) if (!e2[n2](t2)) return false;
    return true;
  })($a, Va, T(Wa)), Ga = T(xo("white-space", "pre pre-line pre-wrap")), Ya = Ro, Xa = Po, Qa = ta.nodeIndex, Ja = (e2, t2) => t2 < 0 && yo(e2) && e2.hasChildNodes() ? void 0 : za(e2, t2), Za = (e2) => e2 ? e2.createRange() : ta.DOM.createRng(), ei = (e2) => m(e2) && /[\r\n\t ]/.test(e2), ti = (e2) => !!e2.setStart && !!e2.setEnd, ni = (e2) => {
    const t2 = e2.startContainer, n2 = e2.startOffset;
    if (ei(e2.toString()) && Ga(t2.parentNode) && Ro(t2)) {
      const e3 = t2.data;
      if (ei(e3[n2 - 1]) || ei(e3[n2 + 1])) return true;
    }
    return false;
  }, oi = (e2) => 0 === e2.left && 0 === e2.right && 0 === e2.top && 0 === e2.bottom, ri = (e2) => {
    var t2;
    let n2;
    const o2 = e2.getClientRects();
    return n2 = o2.length > 0 ? Da(o2[0]) : Da(e2.getBoundingClientRect()), !ti(e2) && Xa(e2) && oi(n2) ? ((e3) => {
      const t3 = e3.ownerDocument, n3 = Za(t3), o3 = t3.createTextNode(pr), r2 = e3.parentNode;
      r2.insertBefore(o3, e3), n3.setStart(o3, 0), n3.setEnd(o3, 1);
      const s2 = Da(n3.getBoundingClientRect());
      return r2.removeChild(o3), s2;
    })(e2) : oi(n2) && ti(e2) && null !== (t2 = ((e3) => {
      const t3 = e3.startContainer, n3 = e3.endContainer, o3 = e3.startOffset, r2 = e3.endOffset;
      if (t3 === n3 && Ro(n3) && 0 === o3 && 1 === r2) {
        const t4 = e3.cloneRange();
        return t4.setEndAfter(n3), ri(t4);
      }
      return null;
    })(e2)) && void 0 !== t2 ? t2 : n2;
  }, si = (e2, t2) => {
    const n2 = Pa(e2, t2);
    return n2.width = 1, n2.right = n2.left + 1, n2;
  }, ai = (e2, t2, n2) => {
    const o2 = () => (n2 || (n2 = ((e3) => {
      const t3 = [], n3 = (e4) => {
        var n4, o4;
        0 !== e4.height && (t3.length > 0 && (n4 = e4, o4 = t3[t3.length - 1], n4.left === o4.left && n4.top === o4.top && n4.bottom === o4.bottom && n4.right === o4.right) || t3.push(e4));
      }, o3 = (e4, t4) => {
        const o4 = Za(e4.ownerDocument);
        if (t4 < e4.data.length) {
          if (Ha(e4.data[t4])) return;
          if (Ha(e4.data[t4 - 1]) && (o4.setStart(e4, t4), o4.setEnd(e4, t4 + 1), !ni(o4))) return void n3(si(ri(o4), false));
        }
        t4 > 0 && (o4.setStart(e4, t4 - 1), o4.setEnd(e4, t4), ni(o4) || n3(si(ri(o4), false))), t4 < e4.data.length && (o4.setStart(e4, t4), o4.setEnd(e4, t4 + 1), ni(o4) || n3(si(ri(o4), true)));
      }, r2 = e3.container(), s2 = e3.offset();
      if (Ya(r2)) return o3(r2, s2), t3;
      if ($a(r2)) if (e3.isAtEnd()) {
        const e4 = Ja(r2, s2);
        Ya(e4) && o3(e4, e4.data.length), Ka(e4) && !Xa(e4) && n3(si(ri(e4), false));
      } else {
        const a2 = Ja(r2, s2);
        if (Ya(a2) && o3(a2, 0), Ka(a2) && e3.isAtEnd()) return n3(si(ri(a2), false)), t3;
        const i3 = Ja(e3.container(), e3.offset() - 1);
        Ka(i3) && !Xa(i3) && (qa(i3) || qa(a2) || !Ka(a2)) && n3(si(ri(i3), false)), Ka(a2) && n3(si(ri(a2), true));
      }
      return t3;
    })(ai(e2, t2))), n2);
    return { container: N(e2), offset: N(t2), toRange: () => {
      const n3 = Za(e2.ownerDocument);
      return n3.setStart(e2, t2), n3.setEnd(e2, t2), n3;
    }, getClientRects: o2, isVisible: () => o2().length > 0, isAtStart: () => (Ya(e2), 0 === t2), isAtEnd: () => Ya(e2) ? t2 >= e2.data.length : t2 >= e2.childNodes.length, isEqual: (n3) => n3 && e2 === n3.container() && t2 === n3.offset(), getNode: (n3) => Ja(e2, n3 ? t2 - 1 : t2) };
  };
  ai.fromRangeStart = (e2) => ai(e2.startContainer, e2.startOffset), ai.fromRangeEnd = (e2) => ai(e2.endContainer, e2.endOffset), ai.after = (e2) => ai(e2.parentNode, Qa(e2) + 1), ai.before = (e2) => ai(e2.parentNode, Qa(e2)), ai.isAbove = (e2, t2) => Dt(ie(t2.getClientRects()), le(e2.getClientRects()), Ma).getOr(false), ai.isBelow = (e2, t2) => Dt(le(t2.getClientRects()), ie(e2.getClientRects()), Ia).getOr(false), ai.isAtStart = (e2) => !!e2 && e2.isAtStart(), ai.isAtEnd = (e2) => !!e2 && e2.isAtEnd(), ai.isTextPosition = (e2) => !!e2 && Ro(e2.container()), ai.isElementPosition = (e2) => !ai.isTextPosition(e2);
  const ii = (e2, t2) => {
    Ro(t2) && 0 === t2.data.length && e2.remove(t2);
  }, li = (e2, t2, n2) => {
    Do(n2) ? ((e3, t3, n3) => {
      const o2 = M.from(n3.firstChild), r2 = M.from(n3.lastChild);
      t3.insertNode(n3), o2.each((t4) => ii(e3, t4.previousSibling)), r2.each((t4) => ii(e3, t4.nextSibling));
    })(e2, t2, n2) : ((e3, t3, n3) => {
      t3.insertNode(n3), ii(e3, n3.previousSibling), ii(e3, n3.nextSibling);
    })(e2, t2, n2);
  }, di = Ro, ci = So, ui = ta.nodeIndex, mi = (e2) => {
    const t2 = e2.parentNode;
    return ci(t2) ? mi(t2) : t2;
  }, fi = (e2) => e2 ? Oe(e2.childNodes, (e3, t2) => (ci(t2) && "BR" !== t2.nodeName ? e3 = e3.concat(fi(t2)) : e3.push(t2), e3), []) : [], gi = (e2) => (t2) => e2 === t2, pi = (e2) => (di(e2) ? "text()" : e2.nodeName.toLowerCase()) + "[" + ((e3) => {
    let t2, n2;
    t2 = fi(mi(e3)), n2 = Te(t2, gi(e3), e3), t2 = t2.slice(0, n2 + 1);
    const o2 = Oe(t2, (e4, n3, o3) => (di(n3) && di(t2[o3 - 1]) && e4++, e4), 0);
    return t2 = Ae(t2, wo([e3.nodeName])), n2 = Te(t2, gi(e3), e3), n2 - o2;
  })(e2) + "]", hi = (e2, t2) => {
    let n2, o2 = [], r2 = t2.container(), s2 = t2.offset();
    if (di(r2)) n2 = ((e3, t3) => {
      let n3 = e3;
      for (; (n3 = n3.previousSibling) && di(n3); ) t3 += n3.data.length;
      return t3;
    })(r2, s2);
    else {
      const e3 = r2.childNodes;
      s2 >= e3.length ? (n2 = "after", s2 = e3.length - 1) : n2 = "before", r2 = e3[s2];
    }
    o2.push(pi(r2));
    let a2 = ((e3, t3, n3) => {
      const o3 = [];
      for (let n4 = t3.parentNode; n4 && n4 !== e3; n4 = n4.parentNode) o3.push(n4);
      return o3;
    })(e2, r2);
    return a2 = Ae(a2, T(So)), o2 = o2.concat(Re(a2, (e3) => pi(e3))), o2.reverse().join("/") + "," + n2;
  }, bi = (e2, t2) => {
    if (!t2) return null;
    const n2 = t2.split(","), o2 = n2[0].split("/"), r2 = n2.length > 1 ? n2[1] : "before", s2 = Oe(o2, (e3, t3) => {
      const n3 = /([\w\-\(\)]+)\[([0-9]+)\]/.exec(t3);
      return n3 ? ("text()" === n3[1] && (n3[1] = "#text"), ((e4, t4, n4) => {
        let o3 = fi(e4);
        return o3 = Ae(o3, (e5, t5) => !di(e5) || !di(o3[t5 - 1])), o3 = Ae(o3, wo([t4])), o3[n4];
      })(e3, n3[1], parseInt(n3[2], 10))) : null;
    }, e2);
    if (!s2) return null;
    if (!di(s2) && s2.parentNode) {
      let e3;
      return e3 = "after" === r2 ? ui(s2) + 1 : ui(s2), ai(s2.parentNode, e3);
    }
    return ((e3, t3) => {
      let n3 = e3, o3 = 0;
      for (; di(n3); ) {
        const r3 = n3.data.length;
        if (t3 >= o3 && t3 <= o3 + r3) {
          e3 = n3, t3 -= o3;
          break;
        }
        if (!di(n3.nextSibling)) {
          e3 = n3, t3 = r3;
          break;
        }
        o3 += r3, n3 = n3.nextSibling;
      }
      return di(e3) && t3 > e3.data.length && (t3 = e3.data.length), ai(e3, t3);
    })(s2, parseInt(r2, 10));
  }, vi = Io, yi = (e2, t2, n2, o2, r2) => {
    const s2 = r2 ? o2.startContainer : o2.endContainer;
    let a2 = r2 ? o2.startOffset : o2.endOffset;
    const i3 = [], l2 = e2.getRoot();
    if (Ro(s2)) i3.push(n2 ? ((e3, t3, n3) => {
      let o3 = e3(t3.data.slice(0, n3)).length;
      for (let n4 = t3.previousSibling; n4 && Ro(n4); n4 = n4.previousSibling) o3 += e3(n4.data).length;
      return o3;
    })(t2, s2, a2) : a2);
    else {
      let t3 = 0;
      const o3 = s2.childNodes;
      a2 >= o3.length && o3.length && (t3 = 1, a2 = Math.max(0, o3.length - 1)), i3.push(e2.nodeIndex(o3[a2], n2) + t3);
    }
    for (let t3 = s2; t3 && t3 !== l2; t3 = t3.parentNode) i3.push(e2.nodeIndex(t3, n2));
    return i3;
  }, Ci = (e2, t2, n2) => {
    let o2 = 0;
    return Tt.each(e2.select(t2), (e3) => "all" === e3.getAttribute("data-mce-bogus") ? void 0 : e3 !== n2 && void o2++), o2;
  }, wi = (e2, t2) => {
    let n2 = t2 ? e2.startContainer : e2.endContainer, o2 = t2 ? e2.startOffset : e2.endOffset;
    if (yo(n2) && "TR" === n2.nodeName) {
      const r2 = n2.childNodes;
      n2 = r2[Math.min(t2 ? o2 : o2 - 1, r2.length - 1)], n2 && (o2 = t2 ? 0 : n2.childNodes.length, t2 ? e2.setStart(n2, o2) : e2.setEnd(n2, o2));
    }
  }, xi = (e2) => (wi(e2, true), wi(e2, false), e2), ki = (e2, t2) => {
    if (yo(e2) && (e2 = za(e2, t2), vi(e2))) return e2;
    if (Br(e2)) {
      Ro(e2) && Or(e2) && (e2 = e2.parentNode);
      let t3 = e2.previousSibling;
      if (vi(t3)) return t3;
      if (t3 = e2.nextSibling, vi(t3)) return t3;
    }
  }, Si = (e2, t2, n2) => {
    const o2 = n2.getNode(), r2 = n2.getRng();
    if ("IMG" === o2.nodeName || vi(o2)) {
      const e3 = o2.nodeName;
      return { name: e3, index: Ci(n2.dom, e3, o2) };
    }
    const s2 = ((e3) => ki(e3.startContainer, e3.startOffset) || ki(e3.endContainer, e3.endOffset))(r2);
    if (s2) {
      const e3 = s2.tagName;
      return { name: e3, index: Ci(n2.dom, e3, s2) };
    }
    return ((e3, t3, n3, o3) => {
      const r3 = t3.dom, s3 = yi(r3, e3, n3, o3, true), a2 = t3.isForward(), i3 = Ur(o3) ? { isFakeCaret: true } : {};
      return t3.isCollapsed() ? { start: s3, forward: a2, ...i3 } : { start: s3, end: yi(r3, e3, n3, o3, false), forward: a2, ...i3 };
    })(e2, n2, t2, r2);
  }, _i = (e2, t2, n2) => {
    const o2 = { "data-mce-type": "bookmark", id: t2, style: "overflow:hidden;line-height:0px" };
    return n2 ? e2.create("span", o2, "&#xFEFF;") : e2.create("span", o2);
  }, Ei = (e2, t2) => {
    const n2 = e2.dom;
    let o2 = e2.getRng();
    const r2 = n2.uniqueId(), s2 = e2.isCollapsed(), a2 = e2.getNode(), i3 = a2.nodeName, l2 = e2.isForward();
    if ("IMG" === i3) return { name: i3, index: Ci(n2, i3, a2) };
    const d2 = xi(o2.cloneRange());
    if (!s2) {
      d2.collapse(false);
      const e3 = _i(n2, r2 + "_end", t2);
      li(n2, d2, e3);
    }
    o2 = xi(o2), o2.collapse(true);
    const c2 = _i(n2, r2 + "_start", t2);
    return li(n2, o2, c2), e2.moveToBookmark({ id: r2, keep: true, forward: l2 }), { id: r2, forward: l2 };
  }, Ni = O(Si, R, true), Ri = (e2) => {
    const t2 = (t3) => t3(e2), n2 = N(e2), o2 = () => r2, r2 = { tag: true, inner: e2, fold: (t3, n3) => n3(e2), isValue: L, isError: P, map: (t3) => Oi.value(t3(e2)), mapError: o2, bind: t2, exists: t2, forall: t2, getOr: n2, or: o2, getOrThunk: n2, orThunk: o2, getOrDie: n2, each: (t3) => {
      t3(e2);
    }, toOptional: () => M.some(e2) };
    return r2;
  }, Ai = (e2) => {
    const t2 = () => n2, n2 = { tag: false, inner: e2, fold: (t3, n3) => t3(e2), isValue: P, isError: L, map: t2, mapError: (t3) => Oi.error(t3(e2)), bind: t2, exists: P, forall: L, getOr: R, or: R, getOrThunk: B, orThunk: B, getOrDie: (o2 = String(e2), () => {
      throw new Error(o2);
    }), each: S, toOptional: M.none };
    var o2;
    return n2;
  }, Oi = { value: Ri, error: Ai, fromOption: (e2, t2) => e2.fold(() => Ai(t2), Ri) }, Ti = (e2) => {
    if (!p(e2)) throw new Error("cases must be an array");
    if (0 === e2.length) throw new Error("there must be at least one case");
    const t2 = [], n2 = {};
    return V(e2, (o2, r2) => {
      const s2 = ue(o2);
      if (1 !== s2.length) throw new Error("one and only one name per case");
      const a2 = s2[0], i3 = o2[a2];
      if (void 0 !== n2[a2]) throw new Error("duplicate key detected:" + a2);
      if ("cata" === a2) throw new Error("cannot have a case named cata (sorry)");
      if (!p(i3)) throw new Error("case arguments must be an array");
      t2.push(a2), n2[a2] = (...n3) => {
        const o3 = n3.length;
        if (o3 !== i3.length) throw new Error("Wrong number of arguments to case " + a2 + ". Expected " + i3.length + " (" + i3 + "), got " + o3);
        return { fold: (...t3) => {
          if (t3.length !== e2.length) throw new Error("Wrong number of arguments to fold. Expected " + e2.length + ", got " + t3.length);
          return t3[r2].apply(null, n3);
        }, match: (e3) => {
          const o4 = ue(e3);
          if (t2.length !== o4.length) throw new Error("Wrong number of arguments to match. Expected: " + t2.join(",") + "\nActual: " + o4.join(","));
          if (!te(t2, (e4) => j(o4, e4))) throw new Error("Not all branches were specified when using match. Specified: " + o4.join(", ") + "\nRequired: " + t2.join(", "));
          return e3[a2].apply(null, n3);
        }, log: (e3) => {
          console.log(e3, { constructors: t2, constructor: a2, params: n3 });
        } };
      };
    }), n2;
  };
  Ti([{ bothErrors: ["error1", "error2"] }, { firstError: ["error1", "value2"] }, { secondError: ["value1", "error2"] }, { bothValues: ["value1", "value2"] }]);
  const Bi = (e2) => "inline-command" === e2.type || "inline-format" === e2.type, Di = (e2) => "block-command" === e2.type || "block-format" === e2.type, Pi = (e2) => {
    const t2 = (t3) => Oi.error({ message: t3, pattern: e2 }), n2 = (n3, o2, r2) => {
      if (void 0 !== e2.format) {
        let r3;
        if (p(e2.format)) {
          if (!te(e2.format, m)) return t2(n3 + " pattern has non-string items in the `format` array");
          r3 = e2.format;
        } else {
          if (!m(e2.format)) return t2(n3 + " pattern has non-string `format` parameter");
          r3 = [e2.format];
        }
        return Oi.value(o2(r3));
      }
      return void 0 !== e2.cmd ? m(e2.cmd) ? Oi.value(r2(e2.cmd, e2.value)) : t2(n3 + " pattern has non-string `cmd` parameter") : t2(n3 + " pattern is missing both `format` and `cmd` parameters");
    };
    if (!f(e2)) return t2("Raw pattern is not an object");
    if (!m(e2.start)) return t2("Raw pattern is missing `start` parameter");
    if (void 0 !== e2.end) {
      if (!m(e2.end)) return t2("Inline pattern has non-string `end` parameter");
      if (0 === e2.start.length && 0 === e2.end.length) return t2("Inline pattern has empty `start` and `end` parameters");
      let o2 = e2.start, r2 = e2.end;
      return 0 === r2.length && (r2 = o2, o2 = ""), n2("Inline", (e3) => ({ type: "inline-format", start: o2, end: r2, format: e3 }), (e3, t3) => ({ type: "inline-command", start: o2, end: r2, cmd: e3, value: t3 }));
    }
    return void 0 !== e2.replacement ? m(e2.replacement) ? 0 === e2.start.length ? t2("Replacement pattern has empty `start` parameter") : Oi.value({ type: "inline-command", start: "", end: e2.start, cmd: "mceInsertContent", value: e2.replacement }) : t2("Replacement pattern has non-string `replacement` parameter") : 0 === e2.start.length ? t2("Block pattern has empty `start` parameter") : n2("Block", (t3) => ({ type: "block-format", start: e2.start, format: t3[0] }), (t3, n3) => ({ type: "block-command", start: e2.start, cmd: t3, value: n3 }));
  }, Li = (e2) => K(e2, Di), Mi = (e2) => K(e2, Bi), Ii = (e2) => {
    const t2 = ((e3) => {
      const t3 = [], n2 = [];
      return V(e3, (e4) => {
        e4.fold((e5) => {
          t3.push(e5);
        }, (e5) => {
          n2.push(e5);
        });
      }), { errors: t3, values: n2 };
    })($2(e2, Pi));
    return V(t2.errors, (e3) => console.error(e3.message, e3.pattern)), t2.values;
  }, Fi = Ct().deviceType, Ui = Fi.isTouch(), zi = ta.DOM, ji = (e2) => u(e2, RegExp), Hi = (e2) => (t2) => t2.options.get(e2), $i = (e2) => m(e2) || f(e2), Vi = (e2, t2 = "") => (n2) => {
    const o2 = m(n2);
    if (o2) {
      if (-1 !== n2.indexOf("=")) {
        const r2 = ((e3) => {
          const t3 = e3.indexOf("=") > 0 ? e3.split(/[;,](?![^=;,]*(?:[;,]|$))/) : e3.split(",");
          return Y(t3, (e4, t4) => {
            const n3 = t4.split("="), o3 = n3[0], r3 = n3.length > 1 ? n3[1] : o3;
            return e4[$e(o3)] = $e(r3), e4;
          }, {});
        })(n2);
        return { value: we(r2, e2.id).getOr(t2), valid: o2 };
      }
      return { value: n2, valid: o2 };
    }
    return { valid: false, message: "Must be a string." };
  }, qi = Hi("iframe_attrs"), Wi = Hi("doctype"), Ki = Hi("document_base_url"), Gi = Hi("body_id"), Yi = Hi("body_class"), Xi = Hi("content_security_policy"), Qi = Hi("br_in_pre"), Ji = Hi("forced_root_block"), Zi = Hi("forced_root_block_attrs"), el = Hi("newline_behavior"), tl = Hi("br_newline_selector"), nl = Hi("no_newline_selector"), ol = Hi("keep_styles"), rl = Hi("end_container_on_empty_block"), sl = Hi("automatic_uploads"), al = Hi("images_reuse_filename"), il = Hi("images_replace_blob_uris"), ll = Hi("icons"), dl = Hi("icons_url"), cl = Hi("images_upload_url"), ul = Hi("images_upload_base_path"), ml = Hi("images_upload_credentials"), fl = Hi("images_upload_handler"), gl = Hi("content_css_cors"), pl = Hi("referrer_policy"), hl = Hi("language"), bl = Hi("language_url"), vl = Hi("indent_use_margin"), yl = Hi("indentation"), Cl = Hi("content_css"), wl = Hi("content_style"), xl = Hi("font_css"), kl = Hi("directionality"), Sl = Hi("inline_boundaries_selector"), _l = Hi("object_resizing"), El = Hi("resize_img_proportional"), Nl = Hi("placeholder"), Rl = Hi("event_root"), Al = Hi("service_message"), Ol = Hi("theme"), Tl = Hi("theme_url"), Bl = Hi("model"), Dl = Hi("model_url"), Pl = Hi("inline_boundaries"), Ll = Hi("formats"), Ml = Hi("preview_styles"), Il = Hi("format_empty_lines"), Fl = Hi("format_noneditable_selector"), Ul = Hi("custom_ui_selector"), zl = Hi("inline"), jl = Hi("hidden_input"), Hl = Hi("submit_patch"), $l = Hi("add_form_submit_trigger"), Vl = Hi("add_unload_trigger"), ql = Hi("custom_undo_redo_levels"), Wl = Hi("disable_nodechange"), Kl = Hi("readonly"), Gl = Hi("content_css_cors"), Yl = Hi("plugins"), Xl = Hi("external_plugins"), Ql = Hi("block_unsupported_drop"), Jl = Hi("visual"), Zl = Hi("visual_table_class"), ed = Hi("visual_anchor_class"), td = Hi("iframe_aria_text"), nd = Hi("setup"), od = Hi("init_instance_callback"), rd = Hi("urlconverter_callback"), sd = Hi("auto_focus"), ad = Hi("browser_spellcheck"), id = Hi("protect"), ld = Hi("paste_block_drop"), dd = Hi("paste_data_images"), cd = Hi("paste_preprocess"), ud = Hi("paste_postprocess"), md = Hi("paste_webkit_styles"), fd = Hi("paste_remove_styles_if_webkit"), gd = Hi("paste_merge_formats"), pd = Hi("smart_paste"), hd = Hi("paste_as_text"), bd = Hi("paste_tab_spaces"), vd = Hi("allow_html_data_urls"), yd = Hi("text_patterns"), Cd = Hi("text_patterns_lookup"), wd = Hi("noneditable_class"), xd = Hi("editable_class"), kd = Hi("noneditable_regexp"), Sd = Hi("preserve_cdata"), _d = (e2) => Tt.explode(e2.options.get("images_file_types")), Ed = Hi("table_tab_navigation"), Nd = yo, Rd = Ro, Ad = (e2) => {
    const t2 = e2.parentNode;
    t2 && t2.removeChild(e2);
  }, Od = (e2) => {
    const t2 = Nr(e2);
    return { count: e2.length - t2.length, text: t2 };
  }, Td = (e2) => {
    let t2;
    for (; -1 !== (t2 = e2.data.lastIndexOf(_r)); ) e2.deleteData(t2, 1);
  }, Bd = (e2, t2) => (Pd(e2), t2), Dd = (e2, t2) => ai.isTextPosition(t2) ? ((e3, t3) => Rd(e3) && t3.container() === e3 ? ((e4, t4) => {
    const n2 = Od(e4.data.substr(0, t4.offset())), o2 = Od(e4.data.substr(t4.offset()));
    return (n2.text + o2.text).length > 0 ? (Td(e4), ai(e4, t4.offset() - n2.count)) : t4;
  })(e3, t3) : Bd(e3, t3))(e2, t2) : ((e3, t3) => t3.container() === e3.parentNode ? ((e4, t4) => {
    const n2 = t4.container(), o2 = ((e5, t5) => {
      const n3 = z(e5, t5);
      return -1 === n3 ? M.none() : M.some(n3);
    })(de(n2.childNodes), e4).map((e5) => e5 < t4.offset() ? ai(n2, t4.offset() - 1) : t4).getOr(t4);
    return Pd(e4), o2;
  })(e3, t3) : Bd(e3, t3))(e2, t2), Pd = (e2) => {
    Nd(e2) && Br(e2) && (Dr(e2) ? e2.removeAttribute("data-mce-caret") : Ad(e2)), Rd(e2) && (Td(e2), 0 === e2.data.length && Ad(e2));
  }, Ld = Io, Md = zo, Id = Fo, Fd = (e2, t2, n2) => {
    const o2 = Pa(t2.getBoundingClientRect(), n2);
    let r2, s2;
    if ("BODY" === e2.tagName) {
      const t3 = e2.ownerDocument.documentElement;
      r2 = e2.scrollLeft || t3.scrollLeft, s2 = e2.scrollTop || t3.scrollTop;
    } else {
      const t3 = e2.getBoundingClientRect();
      r2 = e2.scrollLeft - t3.left, s2 = e2.scrollTop - t3.top;
    }
    o2.left += r2, o2.right += r2, o2.top += s2, o2.bottom += s2, o2.width = 1;
    let a2 = t2.offsetWidth - t2.clientWidth;
    return a2 > 0 && (n2 && (a2 *= -1), o2.left += a2, o2.right += a2), o2;
  }, Ud = (e2, t2, n2, o2) => {
    const r2 = ua();
    let s2, a2;
    const i3 = Ji(e2), l2 = e2.dom, d2 = () => {
      ((e3) => {
        var t3, n3;
        const o3 = vr(mn(e3), "*[contentEditable=false],video,audio,embed,object");
        for (let e4 = 0; e4 < o3.length; e4++) {
          const r3 = o3[e4].dom;
          let s3 = r3.previousSibling;
          if (Ir(s3)) {
            const e5 = s3.data;
            1 === e5.length ? null === (t3 = s3.parentNode) || void 0 === t3 || t3.removeChild(s3) : s3.deleteData(e5.length - 1, 1);
          }
          s3 = r3.nextSibling, Mr(s3) && (1 === s3.data.length ? null === (n3 = s3.parentNode) || void 0 === n3 || n3.removeChild(s3) : s3.deleteData(0, 1));
        }
      })(t2), a2 && (Pd(a2), a2 = null), r2.on((e3) => {
        l2.remove(e3.caret), r2.clear();
      }), s2 && (clearInterval(s2), s2 = void 0);
    };
    return { show: (e3, c2) => {
      let u2;
      if (d2(), Id(c2)) return null;
      if (!n2(c2)) return a2 = ((e4, t3) => {
        var n3;
        const o3 = (null !== (n3 = e4.ownerDocument) && void 0 !== n3 ? n3 : document).createTextNode(_r), r3 = e4.parentNode;
        if (t3) {
          const t4 = e4.previousSibling;
          if (Ar(t4)) {
            if (Br(t4)) return t4;
            if (Ir(t4)) return t4.splitText(t4.data.length - 1);
          }
          null == r3 || r3.insertBefore(o3, e4);
        } else {
          const t4 = e4.nextSibling;
          if (Ar(t4)) {
            if (Br(t4)) return t4;
            if (Mr(t4)) return t4.splitText(1), t4;
          }
          e4.nextSibling ? null == r3 || r3.insertBefore(o3, e4.nextSibling) : null == r3 || r3.appendChild(o3);
        }
        return o3;
      })(c2, e3), u2 = c2.ownerDocument.createRange(), jd(a2.nextSibling) ? (u2.setStart(a2, 0), u2.setEnd(a2, 0)) : (u2.setStart(a2, 1), u2.setEnd(a2, 1)), u2;
      {
        const n3 = ((e4, t3, n4) => {
          var o3;
          const r3 = (null !== (o3 = t3.ownerDocument) && void 0 !== o3 ? o3 : document).createElement(e4);
          r3.setAttribute("data-mce-caret", n4 ? "before" : "after"), r3.setAttribute("data-mce-bogus", "all"), r3.appendChild(xr().dom);
          const s3 = t3.parentNode;
          return n4 ? null == s3 || s3.insertBefore(r3, t3) : t3.nextSibling ? null == s3 || s3.insertBefore(r3, t3.nextSibling) : null == s3 || s3.appendChild(r3), r3;
        })(i3, c2, e3), d3 = Fd(t2, c2, e3);
        l2.setStyle(n3, "top", d3.top), a2 = n3;
        const m2 = l2.create("div", { class: "mce-visual-caret", "data-mce-bogus": "all" });
        l2.setStyles(m2, { ...d3 }), l2.add(t2, m2), r2.set({ caret: m2, element: c2, before: e3 }), e3 && l2.addClass(m2, "mce-visual-caret-before"), s2 = setInterval(() => {
          r2.on((e4) => {
            o2() ? l2.toggleClass(e4.caret, "mce-visual-caret-hidden") : l2.addClass(e4.caret, "mce-visual-caret-hidden");
          });
        }, 500), u2 = c2.ownerDocument.createRange(), u2.setStart(n3, 0), u2.setEnd(n3, 0);
      }
      return u2;
    }, hide: d2, getCss: () => ".mce-visual-caret {position: absolute;background-color: black;background-color: currentcolor;}.mce-visual-caret-hidden {display: none;}*[data-mce-caret] {position: absolute;left: -1000px;right: auto;top: 0;margin: 0;padding: 0;}", reposition: () => {
      r2.on((e3) => {
        const n3 = Fd(t2, e3.element, e3.before);
        l2.setStyles(e3.caret, { ...n3 });
      });
    }, destroy: () => clearInterval(s2) };
  }, zd = () => Nt.browser.isFirefox(), jd = (e2) => Ld(e2) || Md(e2), Hd = (e2) => jd(e2) || _o(e2) && zd(), $d = Mo, Vd = Io, qd = zo, Wd = xo("display", "block table table-cell table-caption list-item"), Kd = Br, Gd = Or, Yd = yo, Xd = Ro, Qd = Gr, Jd = (e2) => e2 > 0, Zd = (e2) => e2 < 0, ec = (e2, t2) => {
    let n2;
    for (; n2 = e2(t2); ) if (!Gd(n2)) return n2;
    return null;
  }, tc = (e2, t2, n2, o2, r2) => {
    const s2 = new Zo(e2, o2), a2 = Vd(e2) || Gd(e2);
    let i3;
    if (Zd(t2)) {
      if (a2 && (i3 = ec(s2.prev.bind(s2), true), n2(i3))) return i3;
      for (; i3 = ec(s2.prev.bind(s2), r2); ) if (n2(i3)) return i3;
    }
    if (Jd(t2)) {
      if (a2 && (i3 = ec(s2.next.bind(s2), true), n2(i3))) return i3;
      for (; i3 = ec(s2.next.bind(s2), r2); ) if (n2(i3)) return i3;
    }
    return null;
  }, nc = (e2, t2) => {
    for (; e2 && e2 !== t2; ) {
      if (Wd(e2)) return e2;
      e2 = e2.parentNode;
    }
    return null;
  }, oc = (e2, t2, n2) => nc(e2.container(), n2) === nc(t2.container(), n2), rc = (e2, t2) => {
    if (!t2) return M.none();
    const n2 = t2.container(), o2 = t2.offset();
    return Yd(n2) ? M.from(n2.childNodes[o2 + e2]) : M.none();
  }, sc = (e2, t2) => {
    var n2;
    const o2 = (null !== (n2 = t2.ownerDocument) && void 0 !== n2 ? n2 : document).createRange();
    return e2 ? (o2.setStartBefore(t2), o2.setEndBefore(t2)) : (o2.setStartAfter(t2), o2.setEndAfter(t2)), o2;
  }, ac = (e2, t2, n2) => nc(t2, e2) === nc(n2, e2), ic = (e2, t2, n2) => {
    const o2 = e2 ? "previousSibling" : "nextSibling";
    let r2 = n2;
    for (; r2 && r2 !== t2; ) {
      let e3 = r2[o2];
      if (e3 && Kd(e3) && (e3 = e3[o2]), Vd(e3) || qd(e3)) {
        if (ac(t2, e3, r2)) return e3;
        break;
      }
      if (Qd(e3)) break;
      r2 = r2.parentNode;
    }
    return null;
  }, lc = O(sc, true), dc = O(sc, false), cc = (e2, t2, n2) => {
    let o2;
    const r2 = O(ic, true, t2), s2 = O(ic, false, t2), a2 = n2.startContainer, i3 = n2.startOffset;
    if (Or(a2)) {
      const e3 = Xd(a2) ? a2.parentNode : a2, t3 = e3.getAttribute("data-mce-caret");
      if ("before" === t3 && (o2 = e3.nextSibling, Hd(o2))) return lc(o2);
      if ("after" === t3 && (o2 = e3.previousSibling, Hd(o2))) return dc(o2);
    }
    if (!n2.collapsed) return n2;
    if (Ro(a2)) {
      if (Kd(a2)) {
        if (1 === e2) {
          if (o2 = s2(a2), o2) return lc(o2);
          if (o2 = r2(a2), o2) return dc(o2);
        }
        if (-1 === e2) {
          if (o2 = r2(a2), o2) return dc(o2);
          if (o2 = s2(a2), o2) return lc(o2);
        }
        return n2;
      }
      if (Ir(a2) && i3 >= a2.data.length - 1) return 1 === e2 && (o2 = s2(a2), o2) ? lc(o2) : n2;
      if (Mr(a2) && i3 <= 1) return -1 === e2 && (o2 = r2(a2), o2) ? dc(o2) : n2;
      if (i3 === a2.data.length) return o2 = s2(a2), o2 ? lc(o2) : n2;
      if (0 === i3) return o2 = r2(a2), o2 ? dc(o2) : n2;
    }
    return n2;
  }, uc = (e2, t2) => rc(e2 ? 0 : -1, t2).filter(Vd), mc = (e2, t2, n2) => {
    const o2 = cc(e2, t2, n2);
    return -1 === e2 ? ai.fromRangeStart(o2) : ai.fromRangeEnd(o2);
  }, fc = (e2) => M.from(e2.getNode()).map(mn), gc = (e2, t2) => {
    let n2 = t2;
    for (; n2 = e2(n2); ) if (n2.isVisible()) return n2;
    return n2;
  }, pc = (e2, t2) => {
    const n2 = oc(e2, t2);
    return !(n2 || !Po(e2.getNode())) || n2;
  };
  var hc;
  !function(e2) {
    e2[e2.Backwards = -1] = "Backwards", e2[e2.Forwards = 1] = "Forwards";
  }(hc || (hc = {}));
  const bc = Io, vc = Ro, yc = yo, Cc = Po, wc = Gr, xc = (e2) => qr(e2) || ((e3) => !!Yr(e3) && !Y(de(e3.getElementsByTagName("*")), (e4, t2) => e4 || zr(t2), false))(e2), kc = Xr, Sc = (e2, t2) => e2.hasChildNodes() && t2 < e2.childNodes.length ? e2.childNodes[t2] : null, _c = (e2, t2) => {
    if (Jd(e2)) {
      if (wc(t2.previousSibling) && !vc(t2.previousSibling)) return ai.before(t2);
      if (vc(t2)) return ai(t2, 0);
    }
    if (Zd(e2)) {
      if (wc(t2.nextSibling) && !vc(t2.nextSibling)) return ai.after(t2);
      if (vc(t2)) return ai(t2, t2.data.length);
    }
    return Zd(e2) ? Cc(t2) ? ai.before(t2) : ai.after(t2) : ai.before(t2);
  }, Ec = (e2, t2, n2) => {
    let o2, r2, s2, a2;
    if (!yc(n2) || !t2) return null;
    if (t2.isEqual(ai.after(n2)) && n2.lastChild) {
      if (a2 = ai.after(n2.lastChild), Zd(e2) && wc(n2.lastChild) && yc(n2.lastChild)) return Cc(n2.lastChild) ? ai.before(n2.lastChild) : a2;
    } else a2 = t2;
    const i3 = a2.container();
    let l2 = a2.offset();
    if (vc(i3)) {
      if (Zd(e2) && l2 > 0) return ai(i3, --l2);
      if (Jd(e2) && l2 < i3.length) return ai(i3, ++l2);
      o2 = i3;
    } else {
      if (Zd(e2) && l2 > 0 && (r2 = Sc(i3, l2 - 1), wc(r2))) return !xc(r2) && (s2 = tc(r2, e2, kc, r2), s2) ? vc(s2) ? ai(s2, s2.data.length) : ai.after(s2) : vc(r2) ? ai(r2, r2.data.length) : ai.before(r2);
      if (Jd(e2) && l2 < i3.childNodes.length && (r2 = Sc(i3, l2), wc(r2))) return Cc(r2) ? ((e3, t3) => {
        const n3 = t3.nextSibling;
        return n3 && wc(n3) ? vc(n3) ? ai(n3, 0) : ai.before(n3) : Ec(hc.Forwards, ai.after(t3), e3);
      })(n2, r2) : !xc(r2) && (s2 = tc(r2, e2, kc, r2), s2) ? vc(s2) ? ai(s2, 0) : ai.before(s2) : vc(r2) ? ai(r2, 0) : ai.after(r2);
      o2 = r2 || a2.getNode();
    }
    if (o2 && (Jd(e2) && a2.isAtEnd() || Zd(e2) && a2.isAtStart()) && (o2 = tc(o2, e2, L, n2, true), kc(o2, n2))) return _c(e2, o2);
    r2 = o2 ? tc(o2, e2, kc, n2) : o2;
    const d2 = Be(K(((e3, t3) => {
      const n3 = [];
      let o3 = e3;
      for (; o3 && o3 !== t3; ) n3.push(o3), o3 = o3.parentNode;
      return n3;
    })(i3, n2), bc));
    return !d2 || r2 && d2.contains(r2) ? r2 ? _c(e2, r2) : null : (a2 = Jd(e2) ? ai.after(d2) : ai.before(d2), a2);
  }, Nc = (e2) => ({ next: (t2) => Ec(hc.Forwards, t2, e2), prev: (t2) => Ec(hc.Backwards, t2, e2) }), Rc = (e2) => ai.isTextPosition(e2) ? 0 === e2.offset() : Gr(e2.getNode()), Ac = (e2) => {
    if (ai.isTextPosition(e2)) {
      const t2 = e2.container();
      return e2.offset() === t2.data.length;
    }
    return Gr(e2.getNode(true));
  }, Oc = (e2, t2) => !ai.isTextPosition(e2) && !ai.isTextPosition(t2) && e2.getNode() === t2.getNode(true), Tc = (e2, t2, n2) => {
    const o2 = Nc(t2);
    return M.from(e2 ? o2.next(n2) : o2.prev(n2));
  }, Bc = (e2, t2, n2) => Tc(e2, t2, n2).bind((o2) => oc(n2, o2, t2) && ((e3, t3, n3) => {
    return e3 ? !Oc(t3, n3) && (o3 = t3, !(!ai.isTextPosition(o3) && Po(o3.getNode()))) && Ac(t3) && Rc(n3) : !Oc(n3, t3) && Rc(t3) && Ac(n3);
    var o3;
  })(e2, n2, o2) ? Tc(e2, t2, o2) : M.some(o2)), Dc = (e2, t2, n2, o2) => Bc(e2, t2, n2).bind((n3) => o2(n3) ? Dc(e2, t2, n3, o2) : M.some(n3)), Pc = (e2, t2) => {
    const n2 = e2 ? t2.firstChild : t2.lastChild;
    return Ro(n2) ? M.some(ai(n2, e2 ? 0 : n2.data.length)) : n2 ? Gr(n2) ? M.some(e2 ? ai.before(n2) : Po(o2 = n2) ? ai.before(o2) : ai.after(o2)) : ((e3, t3, n3) => {
      const o3 = e3 ? ai.before(n3) : ai.after(n3);
      return Tc(e3, t3, o3);
    })(e2, t2, n2) : M.none();
    var o2;
  }, Lc = O(Tc, true), Mc = O(Tc, false), Ic = O(Pc, true), Fc = O(Pc, false), Uc = "_mce_caret", zc = (e2) => yo(e2) && e2.id === Uc, jc = (e2, t2) => {
    let n2 = t2;
    for (; n2 && n2 !== e2; ) {
      if (zc(n2)) return n2;
      n2 = n2.parentNode;
    }
    return null;
  }, Hc = (e2) => xe(e2, "name"), $c = (e2) => Tt.isArray(e2.start), Vc = (e2) => !(!Hc(e2) && b(e2.forward)) || e2.forward, qc = (e2, t2) => (yo(t2) && e2.isBlock(t2) && !t2.innerHTML && (t2.innerHTML = '<br data-mce-bogus="1" />'), t2), Wc = (e2, t2) => Fc(e2).fold(P, (e3) => (t2.setStart(e3.container(), e3.offset()), t2.setEnd(e3.container(), e3.offset()), true)), Kc = (e2, t2, n2) => !(!((e3) => !e3.hasChildNodes())(t2) || !jc(e2, t2) || (((e3, t3) => {
    var n3;
    const o2 = (null !== (n3 = e3.ownerDocument) && void 0 !== n3 ? n3 : document).createTextNode(_r);
    e3.appendChild(o2), t3.setStart(o2, 0), t3.setEnd(o2, 0);
  })(t2, n2), 0)), Gc = (e2, t2, n2, o2) => {
    const r2 = n2[t2 ? "start" : "end"], s2 = e2.getRoot();
    if (r2) {
      let e3 = s2, n3 = r2[0];
      for (let t3 = r2.length - 1; e3 && t3 >= 1; t3--) {
        const n4 = e3.childNodes;
        if (Kc(s2, e3, o2)) return true;
        if (r2[t3] > n4.length - 1) return !!Kc(s2, e3, o2) || Wc(e3, o2);
        e3 = n4[r2[t3]];
      }
      Ro(e3) && (n3 = Math.min(r2[0], e3.data.length)), yo(e3) && (n3 = Math.min(r2[0], e3.childNodes.length)), t2 ? o2.setStart(e3, n3) : o2.setEnd(e3, n3);
    }
    return true;
  }, Yc = (e2) => Ro(e2) && e2.data.length > 0, Xc = (e2, t2, n2) => {
    const o2 = e2.get(n2.id + "_" + t2), r2 = null == o2 ? void 0 : o2.parentNode, s2 = n2.keep;
    if (o2 && r2) {
      let a2, i3;
      if ("start" === t2 ? s2 ? o2.hasChildNodes() ? (a2 = o2.firstChild, i3 = 1) : Yc(o2.nextSibling) ? (a2 = o2.nextSibling, i3 = 0) : Yc(o2.previousSibling) ? (a2 = o2.previousSibling, i3 = o2.previousSibling.data.length) : (a2 = r2, i3 = e2.nodeIndex(o2) + 1) : (a2 = r2, i3 = e2.nodeIndex(o2)) : s2 ? o2.hasChildNodes() ? (a2 = o2.firstChild, i3 = 1) : Yc(o2.previousSibling) ? (a2 = o2.previousSibling, i3 = o2.previousSibling.data.length) : (a2 = r2, i3 = e2.nodeIndex(o2)) : (a2 = r2, i3 = e2.nodeIndex(o2)), !s2) {
        const r3 = o2.previousSibling, s3 = o2.nextSibling;
        let l2;
        for (Tt.each(Tt.grep(o2.childNodes), (e3) => {
          Ro(e3) && (e3.data = e3.data.replace(/\uFEFF/g, ""));
        }); l2 = e2.get(n2.id + "_" + t2); ) e2.remove(l2, true);
        if (Ro(s3) && Ro(r3) && !Nt.browser.isOpera()) {
          const t3 = r3.data.length;
          r3.appendData(s3.data), e2.remove(s3), a2 = r3, i3 = t3;
        }
      }
      return M.some(ai(a2, i3));
    }
    return M.none();
  }, Qc = (e2, t2, n2) => ((e3, t3, n3 = false) => 2 === t3 ? Si(Nr, n3, e3) : 3 === t3 ? ((e4) => {
    const t4 = e4.getRng();
    return { start: hi(e4.dom.getRoot(), ai.fromRangeStart(t4)), end: hi(e4.dom.getRoot(), ai.fromRangeEnd(t4)), forward: e4.isForward() };
  })(e3) : t3 ? ((e4) => ({ rng: e4.getRng(), forward: e4.isForward() }))(e3) : Ei(e3, false))(e2, t2, n2), Jc = (e2, t2) => {
    ((e3, t3) => {
      const n2 = e3.dom;
      if (t3) {
        if ($c(t3)) return ((e4, t4) => {
          const n3 = e4.createRng();
          return Gc(e4, true, t4, n3) && Gc(e4, false, t4, n3) ? M.some({ range: n3, forward: Vc(t4) }) : M.none();
        })(n2, t3);
        if (((e4) => m(e4.start))(t3)) return ((e4, t4) => {
          const n3 = M.from(bi(e4.getRoot(), t4.start)), o2 = M.from(bi(e4.getRoot(), t4.end));
          return Dt(n3, o2, (n4, o3) => {
            const r2 = e4.createRng();
            return r2.setStart(n4.container(), n4.offset()), r2.setEnd(o3.container(), o3.offset()), { range: r2, forward: Vc(t4) };
          });
        })(n2, t3);
        if (((e4) => xe(e4, "id"))(t3)) return ((e4, t4) => {
          const n3 = Xc(e4, "start", t4), o2 = Xc(e4, "end", t4);
          return Dt(n3, o2.or(n3), (n4, o3) => {
            const r2 = e4.createRng();
            return r2.setStart(qc(e4, n4.container()), n4.offset()), r2.setEnd(qc(e4, o3.container()), o3.offset()), { range: r2, forward: Vc(t4) };
          });
        })(n2, t3);
        if (Hc(t3)) return ((e4, t4) => M.from(e4.select(t4.name)[t4.index]).map((t5) => {
          const n3 = e4.createRng();
          return n3.selectNode(t5), { range: n3, forward: true };
        }))(n2, t3);
        if (((e4) => xe(e4, "rng"))(t3)) return M.some({ range: t3.rng, forward: Vc(t3) });
      }
      return M.none();
    })(e2, t2).each(({ range: t3, forward: n2 }) => {
      e2.setRng(t3, n2);
    });
  }, Zc = (e2) => yo(e2) && "SPAN" === e2.tagName && "bookmark" === e2.getAttribute("data-mce-type"), eu = (pr, (e2) => "\xA0" === e2);
  const tu = (e2) => "" !== e2 && -1 !== " \f\n\r	\v".indexOf(e2), nu = (e2) => !tu(e2) && !eu(e2) && !hr(e2), ou = (e2) => {
    const t2 = e2.toString(16);
    return (1 === t2.length ? "0" + t2 : t2).toUpperCase();
  }, ru = (e2) => /* @__PURE__ */ ((e3) => ({ value: e3 }))(ou(e2.red) + ou(e2.green) + ou(e2.blue)), su = /^\s*rgb\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*\)\s*$/i, au = /^\s*rgba\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d?(?:\.\d+)?)\s*\)\s*$/i, iu = (e2, t2, n2, o2) => ({ red: e2, green: t2, blue: n2, alpha: o2 }), lu = (e2, t2, n2, o2) => {
    const r2 = parseInt(e2, 10), s2 = parseInt(t2, 10), a2 = parseInt(n2, 10), i3 = parseFloat(o2);
    return iu(r2, s2, a2, i3);
  }, du = (e2) => ((e3) => {
    if ("transparent" === e3) return M.some(iu(0, 0, 0, 0));
    const t2 = su.exec(e3);
    if (null !== t2) return M.some(lu(t2[1], t2[2], t2[3], "1"));
    const n2 = au.exec(e3);
    return null !== n2 ? M.some(lu(n2[1], n2[2], n2[3], n2[4])) : M.none();
  })(e2).map(ru).map((e3) => "#" + e3.value).getOr(e2), cu = (e2) => {
    const t2 = [];
    if (e2) for (let n2 = 0; n2 < e2.rangeCount; n2++) t2.push(e2.getRangeAt(n2));
    return t2;
  }, uu = (e2, t2) => {
    const n2 = vr(t2, "td[data-mce-selected],th[data-mce-selected]");
    return n2.length > 0 ? n2 : ((e3) => K(((e4) => ee(e4, (e5) => {
      const t3 = Ua(e5);
      return t3 ? [mn(t3)] : [];
    }))(e3), dr))(e2);
  }, mu = (e2) => uu(cu(e2.selection.getSel()), mn(e2.getBody())), fu = (e2, t2) => Go(e2, "table", t2), gu = (e2) => An(e2).fold(N([e2]), (t2) => [e2].concat(gu(t2))), pu = (e2) => On(e2).fold(N([e2]), (t2) => "br" === Lt(t2) ? kn(t2).map((t3) => [e2].concat(pu(t3))).getOr([]) : [e2].concat(pu(t2))), hu = (e2, t2) => Dt(((e3) => {
    const t3 = e3.startContainer, n2 = e3.startOffset;
    return Ro(t3) ? 0 === n2 ? M.some(mn(t3)) : M.none() : M.from(t3.childNodes[n2]).map(mn);
  })(t2), ((e3) => {
    const t3 = e3.endContainer, n2 = e3.endOffset;
    return Ro(t3) ? n2 === t3.data.length ? M.some(mn(t3)) : M.none() : M.from(t3.childNodes[n2 - 1]).map(mn);
  })(t2), (t3, n2) => {
    const o2 = Q(gu(e2), O(bn, t3)), r2 = Q(pu(e2), O(bn, n2));
    return o2.isSome() && r2.isSome();
  }).getOr(false), bu = (e2, t2, n2, o2) => {
    const r2 = n2, s2 = new Zo(n2, r2), a2 = ve(e2.schema.getMoveCaretBeforeOnEnterElements(), (e3, t3) => !j(["td", "th", "table"], t3.toLowerCase()));
    let i3 = n2;
    do {
      if (Ro(i3) && 0 !== Tt.trim(i3.data).length) return void (o2 ? t2.setStart(i3, 0) : t2.setEnd(i3, i3.data.length));
      if (a2[i3.nodeName]) return void (o2 ? t2.setStartBefore(i3) : "BR" === i3.nodeName ? t2.setEndBefore(i3) : t2.setEndAfter(i3));
    } while (i3 = o2 ? s2.next() : s2.prev());
    "BODY" === r2.nodeName && (o2 ? t2.setStart(r2, 0) : t2.setEnd(r2, r2.childNodes.length));
  }, vu = (e2) => {
    const t2 = e2.selection.getSel();
    return C(t2) && t2.rangeCount > 0;
  }, yu = (e2, t2) => {
    const n2 = mu(e2);
    n2.length > 0 ? V(n2, (n3) => {
      const o2 = n3.dom, r2 = e2.dom.createRng();
      r2.setStartBefore(o2), r2.setEndAfter(o2), t2(r2, true);
    }) : t2(e2.selection.getRng(), false);
  }, Cu = (e2, t2, n2) => {
    const o2 = Ei(e2, t2);
    n2(o2), e2.moveToBookmark(o2);
  }, wu = (e2) => x(null == e2 ? void 0 : e2.nodeType), xu = (e2) => yo(e2) && !Zc(e2) && !zc(e2) && !So(e2), ku = (e2) => true === e2.isContentEditable, Su = (e2, t2, n2) => {
    const { selection: o2, dom: r2 } = e2, s2 = o2.getNode(), a2 = Io(s2);
    Cu(o2, true, () => {
      t2();
    }), a2 && Io(s2) && r2.isChildOf(s2, e2.getBody()) ? e2.selection.select(s2) : n2(o2.getStart()) && _u(r2, o2);
  }, _u = (e2, t2) => {
    var n2, o2;
    const r2 = t2.getRng(), { startContainer: s2, startOffset: a2 } = r2;
    if (!((e3, t3) => {
      if (xu(t3) && !/^(TD|TH)$/.test(t3.nodeName)) {
        const n3 = e3.getAttrib(t3, "data-mce-selected"), o3 = parseInt(n3, 10);
        return !isNaN(o3) && o3 > 0;
      }
      return false;
    })(e2, t2.getNode()) && yo(s2)) {
      const i3 = s2.childNodes, l2 = e2.getRoot();
      let d2;
      if (a2 < i3.length) {
        const t3 = i3[a2];
        d2 = new Zo(t3, null !== (n2 = e2.getParent(t3, e2.isBlock)) && void 0 !== n2 ? n2 : l2);
      } else {
        const t3 = i3[i3.length - 1];
        d2 = new Zo(t3, null !== (o2 = e2.getParent(t3, e2.isBlock)) && void 0 !== o2 ? o2 : l2), d2.next(true);
      }
      for (let n3 = d2.current(); n3; n3 = d2.next()) {
        if ("false" === e2.getContentEditable(n3)) return;
        if (Ro(n3) && !Au(n3)) return r2.setStart(n3, 0), void t2.setRng(r2);
      }
    }
  }, Eu = (e2, t2, n2) => {
    if (e2) {
      const o2 = t2 ? "nextSibling" : "previousSibling";
      for (e2 = n2 ? e2 : e2[o2]; e2; e2 = e2[o2]) if (yo(e2) || !Au(e2)) return e2;
    }
  }, Nu = (e2, t2) => (wu(t2) && (t2 = t2.nodeName), !!e2.schema.getTextBlockElements()[t2.toLowerCase()]), Ru = (e2, t2, n2) => e2.schema.isValidChild(t2, n2), Au = (e2, t2 = false) => {
    if (C(e2) && Ro(e2)) {
      const n2 = t2 ? e2.data.replace(/ /g, "\xA0") : e2.data;
      return Jr(n2);
    }
    return false;
  }, Ou = (e2, t2) => {
    const n2 = e2.dom;
    return xu(t2) && "false" === n2.getContentEditable(t2) && ((e3, t3) => {
      const n3 = "[data-mce-cef-wrappable]", o2 = Fl(e3), r2 = Ke(o2) ? n3 : `${n3},${o2}`;
      return pn(mn(t3), r2);
    })(e2, t2) && 0 === n2.select('[contenteditable="true"]', t2).length;
  }, Tu = (e2, t2) => w(e2) ? e2(t2) : (C(t2) && (e2 = e2.replace(/%(\w+)/g, (e3, n2) => t2[n2] || e3)), e2), Bu = (e2, t2) => (t2 = t2 || "", e2 = "" + ((e2 = e2 || "").nodeName || e2), t2 = "" + (t2.nodeName || t2), e2.toLowerCase() === t2.toLowerCase()), Du = (e2, t2) => {
    if (y(e2)) return null;
    {
      let n2 = String(e2);
      return "color" !== t2 && "backgroundColor" !== t2 || (n2 = du(n2)), "fontWeight" === t2 && 700 === e2 && (n2 = "bold"), "fontFamily" === t2 && (n2 = n2.replace(/[\'\"]/g, "").replace(/,\s+/g, ",")), n2;
    }
  }, Pu = (e2, t2, n2) => {
    const o2 = e2.getStyle(t2, n2);
    return Du(o2, n2);
  }, Lu = (e2, t2) => {
    let n2;
    return e2.getParent(t2, (t3) => !!yo(t3) && (n2 = e2.getStyle(t3, "text-decoration"), !!n2 && "none" !== n2)), n2;
  }, Mu = (e2, t2, n2) => e2.getParents(t2, n2, e2.getRoot()), Iu = (e2, t2, n2) => {
    const o2 = e2.formatter.get(t2);
    return C(o2) && H(o2, n2);
  }, Fu = (e2) => ke(e2, "block"), Uu = (e2) => ke(e2, "selector"), zu = (e2) => ke(e2, "inline"), ju = (e2) => Uu(e2) && false !== e2.expand && !zu(e2), Hu = Zc, $u = Mu, Vu = Au, qu = Nu, Wu = (e2, t2) => {
    let n2 = t2;
    for (; n2; ) {
      if (yo(n2) && e2.getContentEditable(n2)) return "false" === e2.getContentEditable(n2) ? n2 : t2;
      n2 = n2.parentNode;
    }
    return t2;
  }, Ku = (e2, t2, n2, o2) => {
    const r2 = t2.data;
    for (let t3 = n2; e2 ? t3 >= 0 : t3 < r2.length; e2 ? t3-- : t3++) if (o2(r2.charAt(t3))) return e2 ? t3 + 1 : t3;
    return -1;
  }, Gu = (e2, t2, n2) => Ku(e2, t2, n2, (e3) => eu(e3) || tu(e3)), Yu = (e2, t2, n2) => Ku(e2, t2, n2, nu), Xu = (e2, t2, n2, o2, r2, s2) => {
    let a2;
    const i3 = e2.getParent(n2, e2.isBlock) || t2, l2 = (t3, n3, o3) => {
      const s3 = Ta(e2), l3 = r2 ? s3.backwards : s3.forwards;
      return M.from(l3(t3, n3, (e3, t4) => Hu(e3.parentNode) ? -1 : (a2 = e3, o3(r2, e3, t4)), i3));
    };
    return l2(n2, o2, Gu).bind((e3) => s2 ? l2(e3.container, e3.offset + (r2 ? -1 : 0), Yu) : M.some(e3)).orThunk(() => a2 ? M.some({ container: a2, offset: r2 ? 0 : a2.length }) : M.none());
  }, Qu = (e2, t2, n2, o2, r2) => {
    const s2 = o2[r2];
    Ro(o2) && Ke(o2.data) && s2 && (o2 = s2);
    const a2 = $u(e2, o2);
    for (let o3 = 0; o3 < a2.length; o3++) for (let r3 = 0; r3 < t2.length; r3++) {
      const s3 = t2[r3];
      if ((!C(s3.collapsed) || s3.collapsed === n2.collapsed) && Uu(s3) && e2.is(a2[o3], s3.selector)) return a2[o3];
    }
    return o2;
  }, Ju = (e2, t2, n2, o2) => {
    var r2;
    let s2 = n2;
    const a2 = e2.dom, i3 = a2.getRoot(), l2 = t2[0];
    if (Fu(l2) && (s2 = l2.wrapper ? null : a2.getParent(n2, l2.block, i3)), !s2) {
      const t3 = null !== (r2 = a2.getParent(n2, "LI,TD,TH")) && void 0 !== r2 ? r2 : i3;
      s2 = a2.getParent(Ro(n2) ? n2.parentNode : n2, (t4) => t4 !== i3 && qu(e2, t4), t3);
    }
    if (s2 && Fu(l2) && l2.wrapper && (s2 = $u(a2, s2, "ul,ol").reverse()[0] || s2), !s2) for (s2 = n2; s2 && s2[o2] && !a2.isBlock(s2[o2]) && (s2 = s2[o2], !Bu(s2, "br")); ) ;
    return s2 || n2;
  }, Zu = (e2, t2, n2, o2) => {
    const r2 = n2.parentNode;
    return !C(n2[o2]) && (!(r2 !== t2 && !y(r2) && !e2.isBlock(r2)) || Zu(e2, t2, r2, o2));
  }, em = (e2, t2, n2, o2, r2) => {
    let s2 = n2;
    const a2 = r2 ? "previousSibling" : "nextSibling", i3 = e2.getRoot();
    if (Ro(n2) && !Vu(n2) && (r2 ? o2 > 0 : o2 < n2.data.length)) return n2;
    for (; s2; ) {
      if (!t2[0].block_expand && e2.isBlock(s2)) return s2;
      for (let t3 = s2[a2]; t3; t3 = t3[a2]) {
        const n3 = Ro(t3) && !Zu(e2, i3, t3, a2);
        if (!Hu(t3) && (!Po(l2 = t3) || !l2.getAttribute("data-mce-bogus") || l2.nextSibling) && !Vu(t3, n3)) return s2;
      }
      if (s2 === i3 || s2.parentNode === i3) {
        n2 = s2;
        break;
      }
      s2 = s2.parentNode;
    }
    var l2;
    return n2;
  }, tm = (e2) => Hu(e2.parentNode) || Hu(e2), nm = (e2, t2, n2, o2 = false) => {
    let { startContainer: r2, startOffset: s2, endContainer: a2, endOffset: i3 } = t2;
    const l2 = e2.dom, d2 = n2[0];
    return yo(r2) && r2.hasChildNodes() && (r2 = za(r2, s2), Ro(r2) && (s2 = 0)), yo(a2) && a2.hasChildNodes() && (a2 = za(a2, t2.collapsed ? i3 : i3 - 1), Ro(a2) && (i3 = a2.data.length)), r2 = Wu(l2, r2), a2 = Wu(l2, a2), tm(r2) && (r2 = Hu(r2) ? r2 : r2.parentNode, r2 = t2.collapsed ? r2.previousSibling || r2 : r2.nextSibling || r2, Ro(r2) && (s2 = t2.collapsed ? r2.length : 0)), tm(a2) && (a2 = Hu(a2) ? a2 : a2.parentNode, a2 = t2.collapsed ? a2.nextSibling || a2 : a2.previousSibling || a2, Ro(a2) && (i3 = t2.collapsed ? 0 : a2.length)), t2.collapsed && (Xu(l2, e2.getBody(), r2, s2, true, o2).each(({ container: e3, offset: t3 }) => {
      r2 = e3, s2 = t3;
    }), Xu(l2, e2.getBody(), a2, i3, false, o2).each(({ container: e3, offset: t3 }) => {
      a2 = e3, i3 = t3;
    })), (zu(d2) || d2.block_expand) && (zu(d2) && Ro(r2) && 0 !== s2 || (r2 = em(l2, n2, r2, s2, true)), zu(d2) && Ro(a2) && i3 !== a2.data.length || (a2 = em(l2, n2, a2, i3, false))), ju(d2) && (r2 = Qu(l2, n2, t2, r2, "previousSibling"), a2 = Qu(l2, n2, t2, a2, "nextSibling")), (Fu(d2) || Uu(d2)) && (r2 = Ju(e2, n2, r2, "previousSibling"), a2 = Ju(e2, n2, a2, "nextSibling"), Fu(d2) && (l2.isBlock(r2) || (r2 = em(l2, n2, r2, s2, true)), l2.isBlock(a2) || (a2 = em(l2, n2, a2, i3, false)))), yo(r2) && r2.parentNode && (s2 = l2.nodeIndex(r2), r2 = r2.parentNode), yo(a2) && a2.parentNode && (i3 = l2.nodeIndex(a2) + 1, a2 = a2.parentNode), { startContainer: r2, startOffset: s2, endContainer: a2, endOffset: i3 };
  }, om = (e2, t2, n2) => {
    var o2;
    const r2 = t2.startOffset, s2 = za(t2.startContainer, r2), a2 = t2.endOffset, i3 = za(t2.endContainer, a2 - 1), l2 = (e3) => {
      const t3 = e3[0];
      Ro(t3) && t3 === s2 && r2 >= t3.data.length && e3.splice(0, 1);
      const n3 = e3[e3.length - 1];
      return 0 === a2 && e3.length > 0 && n3 === i3 && Ro(n3) && e3.splice(e3.length - 1, 1), e3;
    }, d2 = (e3, t3, n3) => {
      const o3 = [];
      for (; e3 && e3 !== n3; e3 = e3[t3]) o3.push(e3);
      return o3;
    }, c2 = (t3, n3) => e2.getParent(t3, (e3) => e3.parentNode === n3, n3), u2 = (e3, t3, o3) => {
      const r3 = o3 ? "nextSibling" : "previousSibling";
      for (let s3 = e3, a3 = s3.parentNode; s3 && s3 !== t3; s3 = a3) {
        a3 = s3.parentNode;
        const t4 = d2(s3 === e3 ? s3 : s3[r3], r3);
        t4.length && (o3 || t4.reverse(), n2(l2(t4)));
      }
    };
    if (s2 === i3) return n2(l2([s2]));
    const m2 = null !== (o2 = e2.findCommonAncestor(s2, i3)) && void 0 !== o2 ? o2 : e2.getRoot();
    if (e2.isChildOf(s2, i3)) return u2(s2, m2, true);
    if (e2.isChildOf(i3, s2)) return u2(i3, m2);
    const f2 = c2(s2, m2) || s2, g2 = c2(i3, m2) || i3;
    u2(s2, f2, true);
    const p2 = d2(f2 === s2 ? f2 : f2.nextSibling, "nextSibling", g2 === i3 ? g2.nextSibling : g2);
    p2.length && n2(l2(p2)), u2(i3, g2);
  }, rm = ['pre[class*=language-][contenteditable="false"]', "figure.image", "div[data-ephox-embed-iri]", "div.tiny-pageembed", "div.mce-toc", "div[data-mce-toc]"], sm = (e2, t2, n2, o2, r2, s2) => {
    const { uid: a2 = t2, ...i3 } = n2;
    nn(e2, ga()), Vt(e2, `${ha()}`, a2), Vt(e2, `${pa()}`, o2);
    const { attributes: l2 = {}, classes: d2 = [] } = r2(a2, i3);
    if (qt(e2, l2), ((e3, t3) => {
      V(t3, (t4) => {
        nn(e3, t4);
      });
    })(e2, d2), s2) {
      d2.length > 0 && Vt(e2, `${va()}`, d2.join(","));
      const t3 = ue(l2);
      t3.length > 0 && Vt(e2, `${ya()}`, t3.join(","));
    }
  }, am = (e2, t2, n2, o2, r2) => {
    const s2 = cn("span", e2);
    return sm(s2, t2, n2, o2, r2, false), s2;
  }, im = (e2, t2, n2, o2, r2, s2) => {
    const a2 = [], i3 = am(e2.getDoc(), n2, s2, o2, r2), l2 = ua(), d2 = () => {
      l2.clear();
    }, c2 = (e3) => {
      V(e3, u2);
    }, u2 = (t3) => {
      switch (((e3, t4, n3, o3) => xn(t4).fold(() => "skipping", (r3) => "br" === o3 || ((e4) => Ut(e4) && Cr(e4) === _r)(t4) ? "valid" : ((e4) => Ft(e4) && sn(e4, ga()))(t4) ? "existing" : zc(t4.dom) ? "caret" : H(rm, (e4) => pn(t4, e4)) ? "valid-block" : Ru(e3, n3, o3) && Ru(e3, Lt(r3), n3) ? "valid" : "invalid-child"))(e2, t3, "span", Lt(t3))) {
        case "invalid-child": {
          d2();
          const e3 = Nn(t3);
          c2(e3), d2();
          break;
        }
        case "valid-block":
          d2(), sm(t3, n2, s2, o2, r2, true);
          break;
        case "valid": {
          const e3 = l2.get().getOrThunk(() => {
            const e4 = Ra(i3);
            return a2.push(e4), l2.set(e4), e4;
          });
          ((e4, t4) => {
            Yn(e4, t4), Jn(t4, e4);
          })(t3, e3);
          break;
        }
      }
    };
    return om(e2.dom, t2, (e3) => {
      d2(), ((e4) => {
        const t3 = $2(e4, mn);
        c2(t3);
      })(e3);
    }), a2;
  }, lm = (e2) => {
    const t2 = /* @__PURE__ */ (() => {
      const e3 = {};
      return { register: (t3, n3) => {
        e3[t3] = { name: t3, settings: n3 };
      }, lookup: (t3) => we(e3, t3).map((e4) => e4.settings), getNames: () => ue(e3) };
    })();
    ((e3, t3) => {
      const n3 = pa(), o3 = (e4) => M.from(e4.attr(n3)).bind(t3.lookup), r3 = (e4) => {
        var t4, n4;
        e4.attr(ha(), null), e4.attr(pa(), null), e4.attr(ba(), null);
        const o4 = M.from(e4.attr(ya())).map((e5) => e5.split(",")).getOr([]), r4 = M.from(e4.attr(va())).map((e5) => e5.split(",")).getOr([]);
        V(o4, (t5) => e4.attr(t5, null));
        const s2 = null !== (n4 = null === (t4 = e4.attr("class")) || void 0 === t4 ? void 0 : t4.split(" ")) && void 0 !== n4 ? n4 : [], a2 = oe(s2, [ga()].concat(r4));
        e4.attr("class", a2.length > 0 ? a2.join(" ") : null), e4.attr(va(), null), e4.attr(ya(), null);
      };
      e3.serializer.addTempAttr(ba()), e3.serializer.addAttributeFilter(n3, (e4) => {
        for (const t4 of e4) o3(t4).each((e5) => {
          false === e5.persistent && ("span" === t4.name ? t4.unwrap() : r3(t4));
        });
      });
    })(e2, t2);
    const n2 = ((e3, t3) => {
      const n3 = ra({}), o3 = () => ({ listeners: [], previous: ua() }), r3 = (e4, t4) => {
        s2(e4, (e5) => (t4(e5), e5));
      }, s2 = (e4, t4) => {
        const r4 = n3.get(), s3 = t4(we(r4, e4).getOrThunk(o3));
        r4[e4] = s3, n3.set(r4);
      }, a2 = (t4, n4) => {
        V(ka(e3, t4), (e4) => {
          n4 ? Vt(e4, ba(), "true") : Yt(e4, ba());
        });
      }, i3 = fa(() => {
        const n4 = se(t3.getNames());
        V(n4, (t4) => {
          s2(t4, (n5) => {
            const o4 = n5.previous.get();
            return wa(e3, M.some(t4)).fold(() => {
              o4.each((e4) => {
                ((e5) => {
                  r3(e5, (t5) => {
                    V(t5.listeners, (t6) => t6(false, e5));
                  });
                })(t4), n5.previous.clear(), a2(e4, false);
              });
            }, ({ uid: e4, name: t5, elements: s3 }) => {
              Bt(o4, e4) || (o4.each((e5) => a2(e5, false)), ((e5, t6, n6) => {
                r3(e5, (o5) => {
                  V(o5.listeners, (o6) => o6(true, e5, { uid: t6, nodes: $2(n6, (e6) => e6.dom) }));
                });
              })(t5, e4, s3), n5.previous.set(e4), a2(e4, true));
            }), { previous: n5.previous, listeners: n5.listeners };
          });
        });
      }, 30);
      return e3.on("remove", () => {
        i3.cancel();
      }), e3.on("NodeChange", () => {
        i3.throttle();
      }), { addListener: (e4, t4) => {
        s2(e4, (e5) => ({ previous: e5.previous, listeners: e5.listeners.concat([t4]) }));
      } };
    })(e2, t2), o2 = Ht("span"), r2 = (e3) => {
      V(e3, (e4) => {
        o2(e4) ? no(e4) : ((e5) => {
          rn(e5, ga()), Yt(e5, `${ha()}`), Yt(e5, `${pa()}`), Yt(e5, `${ba()}`);
          const t3 = Kt(e5, `${ya()}`).map((e6) => e6.split(",")).getOr([]), n3 = Kt(e5, `${va()}`).map((e6) => e6.split(",")).getOr([]);
          var o3;
          V(t3, (t4) => Yt(e5, t4)), o3 = e5, V(n3, (e6) => {
            rn(o3, e6);
          }), Yt(e5, `${va()}`), Yt(e5, `${ya()}`);
        })(e4);
      });
    };
    return { register: (e3, n3) => {
      t2.register(e3, n3);
    }, annotate: (n3, o3) => {
      t2.lookup(n3).each((t3) => {
        ((e3, t4, n4, o4) => {
          e3.undoManager.transact(() => {
            const r3 = e3.selection, s2 = r3.getRng(), a2 = mu(e3).length > 0, i3 = Ea("mce-annotation");
            if (s2.collapsed && !a2 && ((e4, t5) => {
              const n5 = nm(e4, t5, [{ inline: "span" }]);
              t5.setStart(n5.startContainer, n5.startOffset), t5.setEnd(n5.endContainer, n5.endOffset), e4.selection.setRng(t5);
            })(e3, s2), r3.getRng().collapsed && !a2) {
              const s3 = am(e3.getDoc(), i3, o4, t4, n4.decorate);
              ro(s3, pr), r3.getRng().insertNode(s3.dom), r3.select(s3.dom);
            } else Cu(r3, false, () => {
              yu(e3, (r4) => {
                im(e3, r4, i3, t4, n4.decorate, o4);
              });
            });
          });
        })(e2, n3, t3, o3);
      });
    }, annotationChanged: (e3, t3) => {
      n2.addListener(e3, t3);
    }, remove: (t3) => {
      const n3 = e2.selection.getBookmark();
      wa(e2, M.some(t3)).each(({ elements: e3 }) => {
        r2(e3);
      }), e2.selection.moveToBookmark(n3);
    }, removeAll: (t3) => {
      const n3 = e2.selection.getBookmark();
      fe(Sa(e2, t3), (e3, t4) => {
        r2(e3);
      }), e2.selection.moveToBookmark(n3);
    }, getAll: (t3) => {
      const n3 = Sa(e2, t3);
      return ge(n3, (e3) => $2(e3, (e4) => e4.dom));
    } };
  }, dm = (e2) => ({ getBookmark: O(Qc, e2), moveToBookmark: O(Jc, e2) });
  dm.isBookmarkNode = Zc;
  const cm = (e2, t2, n2) => !n2.collapsed && H(n2.getClientRects(), (n3) => ((e3, t3, n4) => t3 >= e3.left && t3 <= e3.right && n4 >= e3.top && n4 <= e3.bottom)(n3, e2, t2)), um = (e2, t2, n2) => {
    e2.dispatch(t2, n2);
  }, mm = (e2, t2, n2, o2) => {
    e2.dispatch("FormatApply", { format: t2, node: n2, vars: o2 });
  }, fm = (e2, t2, n2, o2) => {
    e2.dispatch("FormatRemove", { format: t2, node: n2, vars: o2 });
  }, gm = (e2, t2) => e2.dispatch("SetContent", t2), pm = (e2, t2) => e2.dispatch("GetContent", t2), hm = (e2, t2) => e2.dispatch("PastePlainTextToggle", { state: t2 }), bm = { BACKSPACE: 8, DELETE: 46, DOWN: 40, ENTER: 13, ESC: 27, LEFT: 37, RIGHT: 39, SPACEBAR: 32, TAB: 9, UP: 38, PAGE_UP: 33, PAGE_DOWN: 34, END: 35, HOME: 36, modifierPressed: (e2) => e2.shiftKey || e2.ctrlKey || e2.altKey || bm.metaKeyPressed(e2), metaKeyPressed: (e2) => Nt.os.isMacOS() || Nt.os.isiOS() ? e2.metaKey : e2.ctrlKey && !e2.altKey }, vm = "data-mce-selected", ym = Math.abs, Cm = Math.round, wm = { nw: [0, 0, -1, -1], ne: [1, 0, 1, -1], se: [1, 1, 1, 1], sw: [0, 1, -1, 1] }, xm = (e2, t2) => {
    const n2 = t2.dom, o2 = t2.getDoc(), r2 = document, s2 = t2.getBody();
    let a2, i3, l2, d2, c2, u2, m2, f2, g2, p2, h2, b2, v2, y2, w2;
    const x2 = (e3) => C(e3) && (Lo(e3) || n2.is(e3, "figure.image")), k2 = (e3) => zo(e3) || n2.hasClass(e3, "mce-preview-object"), S2 = (e3) => {
      const n3 = e3.target;
      ((e4, t3) => {
        if (((e5) => "longpress" === e5.type || 0 === e5.type.indexOf("touch"))(e4)) {
          const n4 = e4.touches[0];
          return x2(e4.target) && !cm(n4.clientX, n4.clientY, t3);
        }
        return x2(e4.target) && !cm(e4.clientX, e4.clientY, t3);
      })(e3, t2.selection.getRng()) && !e3.isDefaultPrevented() && t2.selection.select(n3);
    }, _2 = (e3) => n2.hasClass(e3, "mce-preview-object") && C(e3.firstElementChild) ? [e3, e3.firstElementChild] : n2.is(e3, "figure.image") ? [e3.querySelector("img")] : [e3], E2 = (e3) => {
      const o3 = _l(t2);
      return !!o3 && "false" !== e3.getAttribute("data-mce-resize") && e3 !== t2.getBody() && (n2.hasClass(e3, "mce-preview-object") && C(e3.firstElementChild) ? pn(mn(e3.firstElementChild), o3) : pn(mn(e3), o3));
    }, N2 = (e3, o3, r3) => {
      if (C(r3)) {
        const s3 = _2(e3);
        V(s3, (e4) => {
          e4.style[o3] || !t2.schema.isValid(e4.nodeName.toLowerCase(), o3) ? n2.setStyle(e4, o3, r3) : n2.setAttrib(e4, o3, "" + r3);
        });
      }
    }, R2 = (e3, t3, n3) => {
      N2(e3, "width", t3), N2(e3, "height", n3);
    }, A2 = (e3) => {
      let o3, r3, c3, C2, S3;
      o3 = e3.screenX - u2, r3 = e3.screenY - m2, b2 = o3 * d2[2] + f2, v2 = r3 * d2[3] + g2, b2 = b2 < 5 ? 5 : b2, v2 = v2 < 5 ? 5 : v2, c3 = (x2(a2) || k2(a2)) && false !== El(t2) ? !bm.modifierPressed(e3) : bm.modifierPressed(e3), c3 && (ym(o3) > ym(r3) ? (v2 = Cm(b2 * p2), b2 = Cm(v2 / p2)) : (b2 = Cm(v2 / p2), v2 = Cm(b2 * p2))), R2(i3, b2, v2), C2 = d2.startPos.x + o3, S3 = d2.startPos.y + r3, C2 = C2 > 0 ? C2 : 0, S3 = S3 > 0 ? S3 : 0, n2.setStyles(l2, { left: C2, top: S3, display: "block" }), l2.innerHTML = b2 + " &times; " + v2, d2[2] < 0 && i3.clientWidth <= b2 && n2.setStyle(i3, "left", void 0 + (f2 - b2)), d2[3] < 0 && i3.clientHeight <= v2 && n2.setStyle(i3, "top", void 0 + (g2 - v2)), o3 = s2.scrollWidth - y2, r3 = s2.scrollHeight - w2, o3 + r3 !== 0 && n2.setStyles(l2, { left: C2 - o3, top: S3 - r3 }), h2 || (((e4, t3, n3, o4, r4) => {
        e4.dispatch("ObjectResizeStart", { target: t3, width: n3, height: o4, origin: r4 });
      })(t2, a2, f2, g2, "corner-" + d2.name), h2 = true);
    }, O2 = () => {
      const e3 = h2;
      h2 = false, e3 && (N2(a2, "width", b2), N2(a2, "height", v2)), n2.unbind(o2, "mousemove", A2), n2.unbind(o2, "mouseup", O2), r2 !== o2 && (n2.unbind(r2, "mousemove", A2), n2.unbind(r2, "mouseup", O2)), n2.remove(i3), n2.remove(l2), n2.remove(c2), T2(a2), e3 && (((e4, t3, n3, o3, r3) => {
        e4.dispatch("ObjectResized", { target: t3, width: n3, height: o3, origin: r3 });
      })(t2, a2, b2, v2, "corner-" + d2.name), n2.setAttrib(a2, "style", n2.getAttrib(a2, "style"))), t2.nodeChanged();
    }, T2 = (e3) => {
      M2();
      const h3 = n2.getPos(e3, s2), C2 = h3.x, x3 = h3.y, S3 = e3.getBoundingClientRect(), N3 = S3.width || S3.right - S3.left, T3 = S3.height || S3.bottom - S3.top;
      a2 !== e3 && (D2(), a2 = e3, b2 = v2 = 0);
      const B3 = t2.dispatch("ObjectSelected", { target: e3 });
      E2(e3) && !B3.isDefaultPrevented() ? fe(wm, (e4, t3) => {
        let h4 = n2.get("mceResizeHandle" + t3);
        h4 && n2.remove(h4), h4 = n2.add(s2, "div", { id: "mceResizeHandle" + t3, "data-mce-bogus": "all", class: "mce-resizehandle", unselectable: true, style: "cursor:" + t3 + "-resize; margin:0; padding:0" }), n2.bind(h4, "mousedown", (h5) => {
          h5.stopImmediatePropagation(), h5.preventDefault(), ((h6) => {
            const b3 = _2(a2)[0];
            var v3;
            u2 = h6.screenX, m2 = h6.screenY, f2 = b3.clientWidth, g2 = b3.clientHeight, p2 = g2 / f2, d2 = e4, d2.name = t3, d2.startPos = { x: N3 * e4[0] + C2, y: T3 * e4[1] + x3 }, y2 = s2.scrollWidth, w2 = s2.scrollHeight, c2 = n2.add(s2, "div", { class: "mce-resize-backdrop", "data-mce-bogus": "all" }), n2.setStyles(c2, { position: "fixed", left: "0", top: "0", width: "100%", height: "100%" }), i3 = k2(v3 = a2) ? n2.create("img", { src: Nt.transparentSrc }) : v3.cloneNode(true), n2.addClass(i3, "mce-clonedresizable"), n2.setAttrib(i3, "data-mce-bogus", "all"), i3.contentEditable = "false", n2.setStyles(i3, { left: C2, top: x3, margin: 0 }), R2(i3, N3, T3), i3.removeAttribute(vm), s2.appendChild(i3), n2.bind(o2, "mousemove", A2), n2.bind(o2, "mouseup", O2), r2 !== o2 && (n2.bind(r2, "mousemove", A2), n2.bind(r2, "mouseup", O2)), l2 = n2.add(s2, "div", { class: "mce-resize-helper", "data-mce-bogus": "all" }, f2 + " &times; " + g2);
          })(h5);
        }), e4.elm = h4, n2.setStyles(h4, { left: N3 * e4[0] + C2 - h4.offsetWidth / 2, top: T3 * e4[1] + x3 - h4.offsetHeight / 2 });
      }) : D2(false);
    }, B2 = ma(T2, 0), D2 = (e3 = true) => {
      B2.cancel(), M2(), a2 && e3 && a2.removeAttribute(vm), fe(wm, (e4, t3) => {
        const o3 = n2.get("mceResizeHandle" + t3);
        o3 && (n2.unbind(o3), n2.remove(o3));
      });
    }, P2 = (e3, t3) => n2.isChildOf(e3, t3), L2 = (o3) => {
      if (h2 || t2.removed || t2.composing) return;
      const r3 = "mousedown" === o3.type ? o3.target : e2.getNode(), a3 = Xo(mn(r3), "table,img,figure.image,hr,video,span.mce-preview-object").map((e3) => e3.dom).getOrUndefined(), i4 = C(a3) ? n2.getAttrib(a3, vm, "1") : "1";
      if (V(n2.select("img[data-mce-selected],hr[data-mce-selected]"), (e3) => {
        e3.removeAttribute(vm);
      }), C(a3) && P2(a3, s2)) {
        I2();
        const t3 = e2.getStart(true);
        if (P2(t3, a3) && P2(e2.getEnd(true), a3)) return n2.setAttrib(a3, vm, i4), void B2.throttle(a3);
      }
      D2();
    }, M2 = () => {
      fe(wm, (e3) => {
        e3.elm && (n2.unbind(e3.elm), delete e3.elm);
      });
    }, I2 = () => {
      try {
        t2.getDoc().execCommand("enableObjectResizing", false, "false");
      } catch (e3) {
      }
    };
    return t2.on("init", () => {
      I2(), t2.on("NodeChange ResizeEditor ResizeWindow ResizeContent drop", L2), t2.on("keyup compositionend", (e3) => {
        a2 && "TABLE" === a2.nodeName && L2(e3);
      }), t2.on("hide blur", D2), t2.on("contextmenu longpress", S2, true);
    }), t2.on("remove", M2), { isResizable: E2, showResizeRect: T2, hideResizeRect: D2, updateResizeRect: L2, destroy: () => {
      B2.cancel(), a2 = i3 = c2 = null;
    } };
  }, km = (e2, t2, n2) => {
    const o2 = e2.document.createRange();
    var r2;
    return r2 = o2, t2.fold((e3) => {
      r2.setStartBefore(e3.dom);
    }, (e3, t3) => {
      r2.setStart(e3.dom, t3);
    }, (e3) => {
      r2.setStartAfter(e3.dom);
    }), ((e3, t3) => {
      t3.fold((t4) => {
        e3.setEndBefore(t4.dom);
      }, (t4, n3) => {
        e3.setEnd(t4.dom, n3);
      }, (t4) => {
        e3.setEndAfter(t4.dom);
      });
    })(o2, n2), o2;
  }, Sm = (e2, t2, n2, o2, r2) => {
    const s2 = e2.document.createRange();
    return s2.setStart(t2.dom, n2), s2.setEnd(o2.dom, r2), s2;
  }, _m = Ti([{ ltr: ["start", "soffset", "finish", "foffset"] }, { rtl: ["start", "soffset", "finish", "foffset"] }]), Em = (e2, t2, n2) => t2(mn(n2.startContainer), n2.startOffset, mn(n2.endContainer), n2.endOffset);
  _m.ltr, _m.rtl;
  const Nm = (e2, t2, n2, o2) => ({ start: e2, soffset: t2, finish: n2, foffset: o2 }), Rm = document.caretPositionFromPoint ? (e2, t2, n2) => {
    var o2, r2;
    return M.from(null === (r2 = (o2 = e2.dom).caretPositionFromPoint) || void 0 === r2 ? void 0 : r2.call(o2, t2, n2)).bind((t3) => {
      if (null === t3.offsetNode) return M.none();
      const n3 = e2.dom.createRange();
      return n3.setStart(t3.offsetNode, t3.offset), n3.collapse(), M.some(n3);
    });
  } : document.caretRangeFromPoint ? (e2, t2, n2) => {
    var o2, r2;
    return M.from(null === (r2 = (o2 = e2.dom).caretRangeFromPoint) || void 0 === r2 ? void 0 : r2.call(o2, t2, n2));
  } : M.none, Am = Ti([{ before: ["element"] }, { on: ["element", "offset"] }, { after: ["element"] }]), Om = { before: Am.before, on: Am.on, after: Am.after, cata: (e2, t2, n2, o2) => e2.fold(t2, n2, o2), getStart: (e2) => e2.fold(R, R, R) }, Tm = Ti([{ domRange: ["rng"] }, { relative: ["startSitu", "finishSitu"] }, { exact: ["start", "soffset", "finish", "foffset"] }]), Bm = { domRange: Tm.domRange, relative: Tm.relative, exact: Tm.exact, exactFromRange: (e2) => Tm.exact(e2.start, e2.soffset, e2.finish, e2.foffset), getWin: (e2) => {
    const t2 = ((e3) => e3.match({ domRange: (e4) => mn(e4.startContainer), relative: (e4, t3) => Om.getStart(e4), exact: (e4, t3, n2, o2) => e4 }))(e2);
    return wn(t2);
  }, range: Nm }, Dm = (e2, t2) => {
    const n2 = Lt(e2);
    return "input" === n2 ? Om.after(e2) : j(["br", "img"], n2) ? 0 === t2 ? Om.before(e2) : Om.after(e2) : Om.on(e2, t2);
  }, Pm = (e2, t2) => {
    const n2 = e2.fold(Om.before, Dm, Om.after), o2 = t2.fold(Om.before, Dm, Om.after);
    return Bm.relative(n2, o2);
  }, Lm = (e2, t2, n2, o2) => {
    const r2 = Dm(e2, t2), s2 = Dm(n2, o2);
    return Bm.relative(r2, s2);
  }, Mm = (e2, t2) => {
    const n2 = (t2 || document).createDocumentFragment();
    return V(e2, (e3) => {
      n2.appendChild(e3.dom);
    }), mn(n2);
  }, Im = (e2) => {
    const t2 = Bm.getWin(e2).dom, n2 = (e3, n3, o3, r2) => Sm(t2, e3, n3, o3, r2), o2 = ((e3) => e3.match({ domRange: (e4) => {
      const t3 = mn(e4.startContainer), n3 = mn(e4.endContainer);
      return Lm(t3, e4.startOffset, n3, e4.endOffset);
    }, relative: Pm, exact: Lm }))(e2);
    return ((e3, t3) => {
      const n3 = ((e4, t4) => t4.match({ domRange: (e5) => ({ ltr: N(e5), rtl: M.none }), relative: (t5, n4) => ({ ltr: De(() => km(e4, t5, n4)), rtl: De(() => M.some(km(e4, n4, t5))) }), exact: (t5, n4, o3, r2) => ({ ltr: De(() => Sm(e4, t5, n4, o3, r2)), rtl: De(() => M.some(Sm(e4, o3, r2, t5, n4))) }) }))(e3, t3);
      return ((e4, t4) => {
        const n4 = t4.ltr();
        return n4.collapsed ? t4.rtl().filter((e5) => false === e5.collapsed).map((e5) => _m.rtl(mn(e5.endContainer), e5.endOffset, mn(e5.startContainer), e5.startOffset)).getOrThunk(() => Em(0, _m.ltr, n4)) : Em(0, _m.ltr, n4);
      })(0, n3);
    })(t2, o2).match({ ltr: n2, rtl: n2 });
  }, Fm = (e2, t2, n2) => ((e3, t3, n3) => ((e4, t4, n4) => {
    const o2 = mn(e4.document);
    return Rm(o2, t4, n4).map((e5) => Nm(mn(e5.startContainer), e5.startOffset, mn(e5.endContainer), e5.endOffset));
  })(e3, t3, n3))(wn(mn(n2)).dom, e2, t2).map((e3) => {
    const t3 = n2.createRange();
    return t3.setStart(e3.start.dom, e3.soffset), t3.setEnd(e3.finish.dom, e3.foffset), t3;
  }).getOrUndefined(), Um = (e2, t2) => C(e2) && C(t2) && e2.startContainer === t2.startContainer && e2.startOffset === t2.startOffset && e2.endContainer === t2.endContainer && e2.endOffset === t2.endOffset, zm = (e2, t2, n2) => null !== ((e3, t3, n3) => {
    let o2 = e3;
    for (; o2 && o2 !== t3; ) {
      if (n3(o2)) return o2;
      o2 = o2.parentNode;
    }
    return null;
  })(e2, t2, n2), jm = (e2, t2, n2) => zm(e2, t2, (e3) => e3.nodeName === n2), Hm = (e2, t2) => Br(e2) && !zm(e2, t2, zc), $m = (e2, t2, n2) => {
    const o2 = t2.parentNode;
    if (o2) {
      const r2 = new Zo(t2, e2.getParent(o2, e2.isBlock) || e2.getRoot());
      let s2;
      for (; s2 = r2[n2 ? "prev" : "next"](); ) if (Po(s2)) return true;
    }
    return false;
  }, Vm = (e2, t2, n2, o2, r2) => {
    const s2 = e2.getRoot(), a2 = e2.schema.getNonEmptyElements(), i3 = r2.parentNode;
    let l2, d2;
    if (!i3) return M.none();
    const c2 = e2.getParent(i3, e2.isBlock) || s2;
    if (o2 && Po(r2) && t2 && e2.isEmpty(c2)) return M.some(ai(i3, e2.nodeIndex(r2)));
    const u2 = new Zo(r2, c2);
    for (; d2 = u2[o2 ? "prev" : "next"](); ) {
      if ("false" === e2.getContentEditableParent(d2) || Hm(d2, s2)) return M.none();
      if (Ro(d2) && d2.data.length > 0) return jm(d2, s2, "A") ? M.none() : M.some(ai(d2, o2 ? d2.data.length : 0));
      if (e2.isBlock(d2) || a2[d2.nodeName.toLowerCase()]) return M.none();
      l2 = d2;
    }
    return To(l2) ? M.none() : n2 && l2 ? M.some(ai(l2, 0)) : M.none();
  }, qm = (e2, t2, n2, o2) => {
    const r2 = e2.getRoot();
    let s2, a2 = false, i3 = n2 ? o2.startContainer : o2.endContainer, l2 = n2 ? o2.startOffset : o2.endOffset;
    const d2 = yo(i3) && l2 === i3.childNodes.length, c2 = e2.schema.getNonEmptyElements();
    let u2 = n2;
    if (Br(i3)) return M.none();
    if (yo(i3) && l2 > i3.childNodes.length - 1 && (u2 = false), Bo(i3) && (i3 = r2, l2 = 0), i3 === r2) {
      if (u2 && (s2 = i3.childNodes[l2 > 0 ? l2 - 1 : 0], s2)) {
        if (Br(s2)) return M.none();
        if (c2[s2.nodeName] || _o(s2)) return M.none();
      }
      if (i3.hasChildNodes()) {
        if (l2 = Math.min(!u2 && l2 > 0 ? l2 - 1 : l2, i3.childNodes.length - 1), i3 = i3.childNodes[l2], l2 = Ro(i3) && d2 ? i3.data.length : 0, !t2 && i3 === r2.lastChild && _o(i3)) return M.none();
        if (((e3, t3) => {
          let n3 = t3;
          for (; n3 && n3 !== e3; ) {
            if (Io(n3)) return true;
            n3 = n3.parentNode;
          }
          return false;
        })(r2, i3) || Br(i3)) return M.none();
        if (i3.hasChildNodes() && !_o(i3)) {
          s2 = i3;
          const t3 = new Zo(i3, r2);
          do {
            if (Io(s2) || Br(s2)) {
              a2 = false;
              break;
            }
            if (Ro(s2) && s2.data.length > 0) {
              l2 = u2 ? 0 : s2.data.length, i3 = s2, a2 = true;
              break;
            }
            if (c2[s2.nodeName.toLowerCase()] && !Uo(s2)) {
              l2 = e2.nodeIndex(s2), i3 = s2.parentNode, u2 || l2++, a2 = true;
              break;
            }
          } while (s2 = u2 ? t3.next() : t3.prev());
        }
      }
    }
    return t2 && (Ro(i3) && 0 === l2 && Vm(e2, d2, t2, true, i3).each((e3) => {
      i3 = e3.container(), l2 = e3.offset(), a2 = true;
    }), yo(i3) && (s2 = i3.childNodes[l2], s2 || (s2 = i3.childNodes[l2 - 1]), !s2 || !Po(s2) || ((e3, t3) => {
      var n3;
      return "A" === (null === (n3 = e3.previousSibling) || void 0 === n3 ? void 0 : n3.nodeName);
    })(s2) || $m(e2, s2, false) || $m(e2, s2, true) || Vm(e2, d2, t2, true, s2).each((e3) => {
      i3 = e3.container(), l2 = e3.offset(), a2 = true;
    }))), u2 && !t2 && Ro(i3) && l2 === i3.data.length && Vm(e2, d2, t2, false, i3).each((e3) => {
      i3 = e3.container(), l2 = e3.offset(), a2 = true;
    }), a2 && i3 ? M.some(ai(i3, l2)) : M.none();
  }, Wm = (e2, t2) => {
    const n2 = t2.collapsed, o2 = t2.cloneRange(), r2 = ai.fromRangeStart(t2);
    return qm(e2, n2, true, o2).each((e3) => {
      n2 && ai.isAbove(r2, e3) || o2.setStart(e3.container(), e3.offset());
    }), n2 || qm(e2, n2, false, o2).each((e3) => {
      o2.setEnd(e3.container(), e3.offset());
    }), n2 && o2.collapse(true), Um(t2, o2) ? M.none() : M.some(o2);
  }, Km = (e2, t2) => e2.splitText(t2), Gm = (e2) => {
    let t2 = e2.startContainer, n2 = e2.startOffset, o2 = e2.endContainer, r2 = e2.endOffset;
    if (t2 === o2 && Ro(t2)) {
      if (n2 > 0 && n2 < t2.data.length) if (o2 = Km(t2, n2), t2 = o2.previousSibling, r2 > n2) {
        r2 -= n2;
        const e3 = Km(o2, r2).previousSibling;
        t2 = o2 = e3, r2 = e3.data.length, n2 = 0;
      } else r2 = 0;
    } else if (Ro(t2) && n2 > 0 && n2 < t2.data.length && (t2 = Km(t2, n2), n2 = 0), Ro(o2) && r2 > 0 && r2 < o2.data.length) {
      const e3 = Km(o2, r2).previousSibling;
      o2 = e3, r2 = e3.data.length;
    }
    return { startContainer: t2, startOffset: n2, endContainer: o2, endOffset: r2 };
  }, Ym = (e2) => ({ walk: (t2, n2) => om(e2, t2, n2), split: Gm, normalize: (t2) => Wm(e2, t2).fold(P, (e3) => (t2.setStart(e3.startContainer, e3.startOffset), t2.setEnd(e3.endContainer, e3.endOffset), true)) });
  Ym.compareRanges = Um, Ym.getCaretRangeFromPoint = Fm, Ym.getSelectedNode = Ua, Ym.getNode = za;
  const Xm = /* @__PURE__ */ ((e2, t2) => {
    const n2 = (t3) => {
      const n3 = ((e3) => {
        const t4 = e3.dom;
        return zn(e3) ? t4.getBoundingClientRect().height : t4.offsetHeight;
      })(t3);
      if (n3 <= 0 || null === n3) {
        const n4 = Vn(t3, e2);
        return parseFloat(n4) || 0;
      }
      return n3;
    }, o2 = (e3, t3) => Y(t3, (t4, n3) => {
      const o3 = Vn(e3, n3), r2 = void 0 === o3 ? 0 : parseInt(o3, 10);
      return isNaN(r2) ? t4 : t4 + r2;
    }, 0);
    return { set: (t3, n3) => {
      if (!x(n3) && !n3.match(/^[0-9]+$/)) throw new Error(e2 + ".set accepts only positive integer values. Value was " + n3);
      const o3 = t3.dom;
      an(o3) && (o3.style[e2] = n3 + "px");
    }, get: n2, getOuter: n2, aggregate: o2, max: (e3, t3, n3) => {
      const r2 = o2(e3, n3);
      return t3 > r2 ? t3 - r2 : 0;
    } };
  })("height"), Qm = () => mn(document), Jm = (e2, t2) => e2.view(t2).fold(N([]), (t3) => {
    const n2 = e2.owner(t3), o2 = Jm(e2, n2);
    return [t3].concat(o2);
  });
  var Zm = Object.freeze({ __proto__: null, view: (e2) => {
    var t2;
    return (e2.dom === document ? M.none() : M.from(null === (t2 = e2.dom.defaultView) || void 0 === t2 ? void 0 : t2.frameElement)).map(mn);
  }, owner: (e2) => Cn(e2) });
  const ef = (e2) => "textarea" === Lt(e2), tf = (e2, t2) => {
    const n2 = ((e3) => {
      const t3 = e3.dom.ownerDocument, n3 = t3.body, o3 = t3.defaultView, r2 = t3.documentElement;
      if (n3 === e3.dom) return lo(n3.offsetLeft, n3.offsetTop);
      const s2 = co(null == o3 ? void 0 : o3.pageYOffset, r2.scrollTop), a2 = co(null == o3 ? void 0 : o3.pageXOffset, r2.scrollLeft), i3 = co(r2.clientTop, n3.clientTop), l2 = co(r2.clientLeft, n3.clientLeft);
      return uo(e3).translate(a2 - l2, s2 - i3);
    })(e2), o2 = ((e3) => Xm.get(e3))(e2);
    return { element: e2, bottom: n2.top + o2, height: o2, pos: n2, cleanup: t2 };
  }, nf = (e2, t2, n2, o2) => {
    af(e2, (r2, s2) => rf(e2, t2, n2, o2), n2);
  }, of = (e2, t2, n2, o2, r2) => {
    const s2 = { elm: o2.element.dom, alignToTop: r2 };
    ((e3, t3) => e3.dispatch("ScrollIntoView", t3).isDefaultPrevented())(e2, s2) || (n2(t2, mo(t2).top, o2, r2), ((e3, t3) => {
      e3.dispatch("AfterScrollIntoView", t3);
    })(e2, s2));
  }, rf = (e2, t2, n2, o2) => {
    const r2 = mn(e2.getBody()), s2 = mn(e2.getDoc());
    r2.dom.offsetWidth;
    const a2 = ((e3, t3) => {
      const n3 = ((e4, t4) => {
        const n4 = Nn(e4);
        if (0 === n4.length || ef(e4)) return { element: e4, offset: t4 };
        if (t4 < n4.length && !ef(n4[t4])) return { element: n4[t4], offset: 0 };
        {
          const o4 = n4[n4.length - 1];
          return ef(o4) ? { element: e4, offset: t4 } : "img" === Lt(o4) ? { element: o4, offset: 1 } : Ut(o4) ? { element: o4, offset: Cr(o4).length } : { element: o4, offset: Nn(o4).length };
        }
      })(e3, t3), o3 = dn('<span data-mce-bogus="all" style="display: inline-block;">\uFEFF</span>');
      return Yn(n3.element, o3), tf(o3, () => to(o3));
    })(mn(n2.startContainer), n2.startOffset);
    of(e2, s2, t2, a2, o2), a2.cleanup();
  }, sf = (e2, t2, n2, o2) => {
    const r2 = mn(e2.getDoc());
    of(e2, r2, n2, ((e3) => tf(mn(e3), S))(t2), o2);
  }, af = (e2, t2, n2) => {
    const o2 = n2.startContainer, r2 = n2.startOffset, s2 = n2.endContainer, a2 = n2.endOffset;
    t2(mn(o2), mn(s2));
    const i3 = e2.dom.createRng();
    i3.setStart(o2, r2), i3.setEnd(s2, a2), e2.selection.setRng(n2);
  }, lf = (e2, t2, n2, o2) => {
    const r2 = e2.pos;
    if (n2) fo(r2.left, r2.top, o2);
    else {
      const n3 = r2.top - t2 + e2.height;
      fo(r2.left, n3, o2);
    }
  }, df = (e2, t2, n2, o2, r2) => {
    const s2 = n2 + t2, a2 = o2.pos.top, i3 = o2.bottom, l2 = i3 - a2 >= n2;
    a2 < t2 ? lf(o2, n2, false !== r2, e2) : a2 > s2 ? lf(o2, n2, l2 ? false !== r2 : true === r2, e2) : i3 > s2 && !l2 && lf(o2, n2, true === r2, e2);
  }, cf = (e2, t2, n2, o2) => {
    const r2 = wn(e2).dom.innerHeight;
    df(e2, t2, r2, n2, o2);
  }, uf = (e2, t2, n2, o2) => {
    const r2 = wn(e2).dom.innerHeight;
    df(e2, t2, r2, n2, o2);
    const s2 = ((e3) => {
      const t3 = Qm(), n3 = mo(t3), o3 = ((e4, t4) => {
        const n4 = t4.owner(e4);
        return Jm(t4, n4);
      })(e3, Zm), r3 = uo(e3), s3 = G(o3, (e4, t4) => {
        const n4 = uo(t4);
        return { left: e4.left + n4.left, top: e4.top + n4.top };
      }, { left: 0, top: 0 });
      return lo(s3.left + r3.left + n3.left, s3.top + r3.top + n3.top);
    })(n2.element), a2 = ho(window);
    s2.top < a2.y ? go(n2.element, false !== o2) : s2.top > a2.bottom && go(n2.element, true === o2);
  }, mf = (e2, t2, n2) => nf(e2, cf, t2, n2), ff = (e2, t2, n2) => sf(e2, t2, cf, n2), gf = (e2, t2, n2) => nf(e2, uf, t2, n2), pf = (e2, t2, n2) => sf(e2, t2, uf, n2), hf = (e2, t2, n2) => {
    (e2.inline ? mf : gf)(e2, t2, n2);
  }, bf = (e2) => e2.dom.focus(), vf = (e2) => {
    const t2 = Ln(e2).dom;
    return e2.dom === t2.activeElement;
  }, yf = (e2 = Qm()) => M.from(e2.dom.activeElement).map(mn), Cf = (e2, t2) => {
    const n2 = Ut(t2) ? Cr(t2).length : Nn(t2).length + 1;
    return e2 > n2 ? n2 : e2 < 0 ? 0 : e2;
  }, wf = (e2) => Bm.range(e2.start, Cf(e2.soffset, e2.start), e2.finish, Cf(e2.foffset, e2.finish)), xf = (e2, t2) => !vo(t2.dom) && (vn(e2, t2) || bn(e2, t2)), kf = (e2) => (t2) => xf(e2, t2.start) && xf(e2, t2.finish), Sf = (e2) => Bm.range(mn(e2.startContainer), e2.startOffset, mn(e2.endContainer), e2.endOffset), _f = (e2) => {
    const t2 = document.createRange();
    try {
      return t2.setStart(e2.start.dom, e2.soffset), t2.setEnd(e2.finish.dom, e2.foffset), M.some(t2);
    } catch (e3) {
      return M.none();
    }
  }, Ef = (e2) => {
    const t2 = ((e3) => e3.inline)(e2) ? (n2 = mn(e2.getBody()), ((e3) => {
      const t3 = e3.getSelection();
      return (t3 && 0 !== t3.rangeCount ? M.from(t3.getRangeAt(0)) : M.none()).map(Sf);
    })(wn(n2).dom).filter(kf(n2))) : M.none();
    var n2;
    e2.bookmark = t2.isSome() ? t2 : e2.bookmark;
  }, Nf = (e2) => (e2.bookmark ? e2.bookmark : M.none()).bind((t2) => {
    return n2 = mn(e2.getBody()), o2 = t2, M.from(o2).filter(kf(n2)).map(wf);
    var n2, o2;
  }).bind(_f), Rf = { isEditorUIElement: (e2) => {
    const t2 = e2.className.toString();
    return -1 !== t2.indexOf("tox-") || -1 !== t2.indexOf("mce-");
  } }, Af = { setEditorTimeout: (e2, t2, n2) => ((e3, t3) => (x(t3) || (t3 = 0), setTimeout(e3, t3)))(() => {
    e2.removed || t2();
  }, n2), setEditorInterval: (e2, t2, n2) => {
    const o2 = ((e3, t3) => (x(t3) || (t3 = 0), setInterval(e3, t3)))(() => {
      e2.removed ? clearInterval(o2) : t2();
    }, n2);
    return o2;
  } };
  let Of;
  const Tf = ta.DOM, Bf = (e2, t2) => {
    const n2 = Ul(e2), o2 = Tf.getParent(t2, (t3) => ((e3) => yo(e3) && Rf.isEditorUIElement(e3))(t3) || !!n2 && e2.dom.is(t3, n2));
    return null !== o2;
  }, Df = (e2, t2) => {
    const n2 = t2.editor;
    ((e3) => {
      const t3 = ma(() => {
        Ef(e3);
      }, 0);
      e3.on("init", () => {
        e3.inline && ((e4, t4) => {
          const n3 = () => {
            t4.throttle();
          };
          ta.DOM.bind(document, "mouseup", n3), e4.on("remove", () => {
            ta.DOM.unbind(document, "mouseup", n3);
          });
        })(e3, t3), ((e4, t4) => {
          ((e5, t5) => {
            e5.on("mouseup touchend", (e6) => {
              t5.throttle();
            });
          })(e4, t4), e4.on("keyup NodeChange AfterSetSelectionRange", (t5) => {
            ((e5) => "nodechange" === e5.type && e5.selectionChange)(t5) || Ef(e4);
          });
        })(e3, t3);
      }), e3.on("remove", () => {
        t3.cancel();
      });
    })(n2), n2.on("focusin", () => {
      const t3 = e2.focusedEditor;
      t3 !== n2 && (t3 && t3.dispatch("blur", { focusedEditor: n2 }), e2.setActive(n2), e2.focusedEditor = n2, n2.dispatch("focus", { blurredEditor: t3 }), n2.focus(true));
    }), n2.on("focusout", () => {
      Af.setEditorTimeout(n2, () => {
        const t3 = e2.focusedEditor;
        Bf(n2, ((e3) => {
          try {
            const t4 = Ln(mn(e3.getElement()));
            return yf(t4).fold(() => document.body, (e4) => e4.dom);
          } catch (e4) {
            return document.body;
          }
        })(n2)) || t3 !== n2 || (n2.dispatch("blur", { focusedEditor: null }), e2.focusedEditor = null);
      });
    }), Of || (Of = (t3) => {
      const n3 = e2.activeEditor;
      n3 && Fn(t3).each((t4) => {
        const o2 = t4;
        o2.ownerDocument === document && (o2 === document.body || Bf(n3, o2) || e2.focusedEditor !== n3 || (n3.dispatch("blur", { focusedEditor: null }), e2.focusedEditor = null));
      });
    }, Tf.bind(document, "focusin", Of));
  }, Pf = (e2, t2) => {
    e2.focusedEditor === t2.editor && (e2.focusedEditor = null), !e2.activeEditor && Of && (Tf.unbind(document, "focusin", Of), Of = null);
  }, Lf = (e2, t2) => {
    ((e3, t3) => ((e4) => e4.collapsed ? M.from(za(e4.startContainer, e4.startOffset)).map(mn) : M.none())(t3).bind((t4) => lr(t4) ? M.some(t4) : vn(e3, t4) ? M.none() : M.some(e3)))(mn(e2.getBody()), t2).bind((e3) => Ic(e3.dom)).fold(() => {
      e2.selection.normalize();
    }, (t3) => e2.selection.setRng(t3.toRange()));
  }, Mf = (e2) => {
    if (e2.setActive) try {
      e2.setActive();
    } catch (t2) {
      e2.focus();
    }
    else e2.focus();
  }, If = (e2) => e2.inline ? ((e3) => {
    const t2 = e3.getBody();
    return t2 && (n2 = mn(t2), vf(n2) || (o2 = n2, yf(Ln(o2)).filter((e4) => o2.dom.contains(e4.dom))).isSome());
    var n2, o2;
  })(e2) : ((e3) => C(e3.iframeElement) && vf(mn(e3.iframeElement)))(e2), Ff = (e2) => e2.editorManager.setActive(e2), Uf = (e2, t2, n2, o2, r2) => {
    const s2 = n2 ? t2.startContainer : t2.endContainer, a2 = n2 ? t2.startOffset : t2.endOffset;
    return M.from(s2).map(mn).map((e3) => o2 && t2.collapsed ? e3 : Rn(e3, r2(e3, a2)).getOr(e3)).bind((e3) => Ft(e3) ? M.some(e3) : xn(e3).filter(Ft)).map((e3) => e3.dom).getOr(e2);
  }, zf = (e2, t2, n2 = false) => Uf(e2, t2, true, n2, (e3, t3) => Math.min(Tn(e3), t3)), jf = (e2, t2, n2 = false) => Uf(e2, t2, false, n2, (e3, t3) => t3 > 0 ? t3 - 1 : t3), Hf = (e2, t2) => {
    const n2 = e2;
    for (; e2 && Ro(e2) && 0 === e2.length; ) e2 = t2 ? e2.nextSibling : e2.previousSibling;
    return e2 || n2;
  }, $f = (e2, t2) => $2(t2, (t3) => {
    const n2 = e2.dispatch("GetSelectionRange", { range: t3 });
    return n2.range !== t3 ? n2.range : t3;
  }), Vf = ["img", "br"], qf = (e2) => {
    const t2 = wr(e2).filter((e3) => 0 !== e3.trim().length || e3.indexOf(pr) > -1).isSome();
    return t2 || j(Vf, Lt(e2));
  }, Wf = "[data-mce-autocompleter]", Kf = (e2, t2) => {
    if (Gf(mn(e2.getBody())).isNone()) {
      const o2 = dn('<span data-mce-autocompleter="1" data-mce-bogus="1"></span>', e2.getDoc());
      Jn(o2, mn(t2.extractContents())), t2.insertNode(o2.dom), xn(o2).each((e3) => e3.dom.normalize()), (n2 = o2, ((e3, t3) => {
        const n3 = (e4) => {
          const o3 = Nn(e4);
          for (let e5 = o3.length - 1; e5 >= 0; e5--) {
            const r2 = o3[e5];
            if (t3(r2)) return M.some(r2);
            const s2 = n3(r2);
            if (s2.isSome()) return s2;
          }
          return M.none();
        };
        return n3(e3);
      })(n2, qf)).map((t3) => {
        e2.selection.setCursorLocation(t3.dom, ((e3) => "img" === Lt(e3) ? 1 : wr(e3).fold(() => Nn(e3).length, (e4) => e4.length))(t3));
      });
    }
    var n2;
  }, Gf = (e2) => Yo(e2, Wf), Yf = { "#text": 3, "#comment": 8, "#cdata": 4, "#pi": 7, "#doctype": 10, "#document-fragment": 11 }, Xf = (e2, t2, n2) => {
    const o2 = n2 ? "lastChild" : "firstChild", r2 = n2 ? "prev" : "next";
    if (e2[o2]) return e2[o2];
    if (e2 !== t2) {
      let n3 = e2[r2];
      if (n3) return n3;
      for (let o3 = e2.parent; o3 && o3 !== t2; o3 = o3.parent) if (n3 = o3[r2], n3) return n3;
    }
  }, Qf = (e2) => {
    var t2;
    const n2 = null !== (t2 = e2.value) && void 0 !== t2 ? t2 : "";
    if (!Jr(n2)) return false;
    const o2 = e2.parent;
    return !o2 || "span" === o2.name && !o2.attr("style") || !/^[ ]+$/.test(n2);
  }, Jf = (e2) => {
    const t2 = "a" === e2.name && !e2.attr("href") && e2.attr("id");
    return e2.attr("name") || e2.attr("id") && !e2.firstChild || e2.attr("data-mce-bookmark") || t2;
  };
  class Zf {
    constructor(e2, t2) {
      this.name = e2, this.type = t2, 1 === t2 && (this.attributes = [], this.attributes.map = {});
    }
    static create(e2, t2) {
      const n2 = new Zf(e2, Yf[e2] || 1);
      return t2 && fe(t2, (e3, t3) => {
        n2.attr(t3, e3);
      }), n2;
    }
    replace(e2) {
      const t2 = this;
      return e2.parent && e2.remove(), t2.insert(e2, t2), t2.remove(), t2;
    }
    attr(e2, t2) {
      const n2 = this;
      if (!m(e2)) return C(e2) && fe(e2, (e3, t3) => {
        n2.attr(t3, e3);
      }), n2;
      const o2 = n2.attributes;
      if (o2) {
        if (void 0 !== t2) {
          if (null === t2) {
            if (e2 in o2.map) {
              delete o2.map[e2];
              let t3 = o2.length;
              for (; t3--; ) if (o2[t3].name === e2) return o2.splice(t3, 1), n2;
            }
            return n2;
          }
          if (e2 in o2.map) {
            let n3 = o2.length;
            for (; n3--; ) if (o2[n3].name === e2) {
              o2[n3].value = t2;
              break;
            }
          } else o2.push({ name: e2, value: t2 });
          return o2.map[e2] = t2, n2;
        }
        return o2.map[e2];
      }
    }
    clone() {
      const e2 = this, t2 = new Zf(e2.name, e2.type), n2 = e2.attributes;
      if (n2) {
        const e3 = [];
        e3.map = {};
        for (let t3 = 0, o2 = n2.length; t3 < o2; t3++) {
          const o3 = n2[t3];
          "id" !== o3.name && (e3[e3.length] = { name: o3.name, value: o3.value }, e3.map[o3.name] = o3.value);
        }
        t2.attributes = e3;
      }
      return t2.value = e2.value, t2;
    }
    wrap(e2) {
      const t2 = this;
      return t2.parent && (t2.parent.insert(e2, t2), e2.append(t2)), t2;
    }
    unwrap() {
      const e2 = this;
      for (let t2 = e2.firstChild; t2; ) {
        const n2 = t2.next;
        e2.insert(t2, e2, true), t2 = n2;
      }
      e2.remove();
    }
    remove() {
      const e2 = this, t2 = e2.parent, n2 = e2.next, o2 = e2.prev;
      return t2 && (t2.firstChild === e2 ? (t2.firstChild = n2, n2 && (n2.prev = null)) : o2 && (o2.next = n2), t2.lastChild === e2 ? (t2.lastChild = o2, o2 && (o2.next = null)) : n2 && (n2.prev = o2), e2.parent = e2.next = e2.prev = null), e2;
    }
    append(e2) {
      const t2 = this;
      e2.parent && e2.remove();
      const n2 = t2.lastChild;
      return n2 ? (n2.next = e2, e2.prev = n2, t2.lastChild = e2) : t2.lastChild = t2.firstChild = e2, e2.parent = t2, e2;
    }
    insert(e2, t2, n2) {
      e2.parent && e2.remove();
      const o2 = t2.parent || this;
      return n2 ? (t2 === o2.firstChild ? o2.firstChild = e2 : t2.prev && (t2.prev.next = e2), e2.prev = t2.prev, e2.next = t2, t2.prev = e2) : (t2 === o2.lastChild ? o2.lastChild = e2 : t2.next && (t2.next.prev = e2), e2.next = t2.next, e2.prev = t2, t2.next = e2), e2.parent = o2, e2;
    }
    getAll(e2) {
      const t2 = this, n2 = [];
      for (let o2 = t2.firstChild; o2; o2 = Xf(o2, t2)) o2.name === e2 && n2.push(o2);
      return n2;
    }
    children() {
      const e2 = [];
      for (let t2 = this.firstChild; t2; t2 = t2.next) e2.push(t2);
      return e2;
    }
    empty() {
      const e2 = this;
      if (e2.firstChild) {
        const t2 = [];
        for (let n3 = e2.firstChild; n3; n3 = Xf(n3, e2)) t2.push(n3);
        let n2 = t2.length;
        for (; n2--; ) {
          const e3 = t2[n2];
          e3.parent = e3.firstChild = e3.lastChild = e3.next = e3.prev = null;
        }
      }
      return e2.firstChild = e2.lastChild = null, e2;
    }
    isEmpty(e2, t2 = {}, n2) {
      var o2;
      const r2 = this;
      let s2 = r2.firstChild;
      if (Jf(r2)) return false;
      if (s2) do {
        if (1 === s2.type) {
          if (s2.attr("data-mce-bogus")) continue;
          if (e2[s2.name]) return false;
          if (Jf(s2)) return false;
        }
        if (8 === s2.type) return false;
        if (3 === s2.type && !Qf(s2)) return false;
        if (3 === s2.type && s2.parent && t2[s2.parent.name] && Jr(null !== (o2 = s2.value) && void 0 !== o2 ? o2 : "")) return false;
        if (n2 && n2(s2)) return false;
      } while (s2 = Xf(s2, r2));
      return true;
    }
    walk(e2) {
      return Xf(this, null, e2);
    }
  }
  const eg = (e2, t2, n2 = 0) => {
    const o2 = e2.toLowerCase();
    if (-1 !== o2.indexOf("[if ", n2) && ((e3, t3) => /^\s*\[if [\w\W]+\]>.*<!\[endif\](--!?)?>/.test(e3.substr(t3)))(o2, n2)) {
      const e3 = o2.indexOf("[endif]", n2);
      return o2.indexOf(">", e3);
    }
    if (t2) {
      const e3 = o2.indexOf(">", n2);
      return -1 !== e3 ? e3 : o2.length;
    }
    {
      const t3 = /--!?>/g;
      t3.lastIndex = n2;
      const r2 = t3.exec(e2);
      return r2 ? r2.index + r2[0].length : o2.length;
    }
  }, tg = (e2, t2, n2) => {
    const o2 = /<([!?\/])?([A-Za-z0-9\-_:.]+)/g, r2 = /(?:\s(?:[^'">]+(?:"[^"]*"|'[^']*'))*[^"'>]*(?:"[^">]*|'[^'>]*)?|\s*|\/)>/g, s2 = e2.getVoidElements();
    let a2 = 1, i3 = n2;
    for (; 0 !== a2; ) for (o2.lastIndex = i3; ; ) {
      const e3 = o2.exec(t2);
      if (null === e3) return i3;
      if ("!" === e3[1]) {
        i3 = ze(e3[2], "--") ? eg(t2, false, e3.index + "!--".length) : eg(t2, true, e3.index + 1);
        break;
      }
      {
        r2.lastIndex = o2.lastIndex;
        const n3 = r2.exec(t2);
        if (h(n3) || n3.index !== o2.lastIndex) continue;
        "/" === e3[1] ? a2 -= 1 : xe(s2, e3[2]) || (a2 += 1), i3 = o2.lastIndex + n3[0].length;
        break;
      }
    }
    return i3;
  }, ng = (e2, t2) => {
    const n2 = /<(\w+) [^>]*data-mce-bogus="all"[^>]*>/g, o2 = e2.schema;
    let r2 = ((e3, t3) => {
      const n3 = new RegExp(["\\s?(" + e3.join("|") + ')="[^"]+"'].join("|"), "gi");
      return t3.replace(n3, "");
    })(e2.getTempAttrs(), t2);
    const s2 = o2.getVoidElements();
    let a2;
    for (; a2 = n2.exec(r2); ) {
      const e3 = n2.lastIndex, t3 = a2[0].length;
      let i3;
      i3 = s2[a2[1]] ? e3 : tg(o2, r2, e3), r2 = r2.substring(0, e3 - t3) + r2.substring(i3), n2.lastIndex = e3 - t3;
    }
    return Nr(r2);
  }, og = ng, rg = (e2, t2, n2) => {
    let o2;
    return o2 = "raw" === t2.format ? Tt.trim(og(e2.serializer, n2.innerHTML)) : "text" === t2.format ? ((e3, t3) => {
      const n3 = e3.getDoc(), o3 = Ln(mn(e3.getBody())), r2 = cn("div", n3);
      Vt(r2, "data-mce-bogus", "all"), $n(r2, { position: "fixed", left: "-9999999px", top: "0" }), ro(r2, t3.innerHTML);
      const s2 = vr(r2, "[data-mce-bogus]");
      V(s2, (e4) => {
        "all" === Wt(e4, "data-mce-bogus") ? to(e4) : rr(e4) ? (Yn(e4, un(gr)), to(e4)) : no(e4);
      });
      const a2 = ((e4) => Bn(e4) ? e4 : mn(Cn(e4).dom.body))(o3);
      Jn(a2, r2);
      const i3 = Nr(r2.dom.innerText);
      return to(r2), i3;
    })(e2, n2) : "tree" === t2.format ? e2.serializer.serialize(n2, t2) : ((e3, t3) => {
      const n3 = Ji(e3), o3 = new RegExp(`^(<${n3}[^>]*>(&nbsp;|&#160;|\\s|\xA0|<br \\/>|)<\\/${n3}>[\r
]*|<br \\/>[\r
]*)$`);
      return t3.replace(o3, "");
    })(e2, e2.serializer.serialize(n2, t2)), "text" !== t2.format && !cr(mn(n2)) && m(o2) ? Tt.trim(o2) : o2;
  }, sg = Tt.makeMap, ag = (e2) => {
    const t2 = [], n2 = (e2 = e2 || {}).indent, o2 = sg(e2.indent_before || ""), r2 = sg(e2.indent_after || ""), s2 = xs.getEncodeFunc(e2.entity_encoding || "raw", e2.entities), a2 = "xhtml" !== e2.element_format;
    return { start: (e3, i3, l2) => {
      if (n2 && o2[e3] && t2.length > 0) {
        const e4 = t2[t2.length - 1];
        e4.length > 0 && "\n" !== e4 && t2.push("\n");
      }
      if (t2.push("<", e3), i3) for (let e4 = 0, n3 = i3.length; e4 < n3; e4++) {
        const n4 = i3[e4];
        t2.push(" ", n4.name, '="', s2(n4.value, true), '"');
      }
      if (t2[t2.length] = !l2 || a2 ? ">" : " />", l2 && n2 && r2[e3] && t2.length > 0) {
        const e4 = t2[t2.length - 1];
        e4.length > 0 && "\n" !== e4 && t2.push("\n");
      }
    }, end: (e3) => {
      let o3;
      t2.push("</", e3, ">"), n2 && r2[e3] && t2.length > 0 && (o3 = t2[t2.length - 1], o3.length > 0 && "\n" !== o3 && t2.push("\n"));
    }, text: (e3, n3) => {
      e3.length > 0 && (t2[t2.length] = n3 ? e3 : s2(e3));
    }, cdata: (e3) => {
      t2.push("<![CDATA[", e3, "]]>");
    }, comment: (e3) => {
      t2.push("<!--", e3, "-->");
    }, pi: (e3, o3) => {
      o3 ? t2.push("<?", e3, " ", s2(o3), "?>") : t2.push("<?", e3, "?>"), n2 && t2.push("\n");
    }, doctype: (e3) => {
      t2.push("<!DOCTYPE", e3, ">", n2 ? "\n" : "");
    }, reset: () => {
      t2.length = 0;
    }, getContent: () => t2.join("").replace(/\n$/, "") };
  }, ig = (e2 = {}, t2 = Ls()) => {
    const n2 = ag(e2);
    return e2.validate = !("validate" in e2) || e2.validate, { serialize: (o2) => {
      const r2 = e2.validate, s2 = { 3: (e3) => {
        var t3;
        n2.text(null !== (t3 = e3.value) && void 0 !== t3 ? t3 : "", e3.raw);
      }, 8: (e3) => {
        var t3;
        n2.comment(null !== (t3 = e3.value) && void 0 !== t3 ? t3 : "");
      }, 7: (e3) => {
        n2.pi(e3.name, e3.value);
      }, 10: (e3) => {
        var t3;
        n2.doctype(null !== (t3 = e3.value) && void 0 !== t3 ? t3 : "");
      }, 4: (e3) => {
        var t3;
        n2.cdata(null !== (t3 = e3.value) && void 0 !== t3 ? t3 : "");
      }, 11: (e3) => {
        let t3 = e3;
        if (t3 = t3.firstChild) do {
          a2(t3);
        } while (t3 = t3.next);
      } };
      n2.reset();
      const a2 = (e3) => {
        var o3;
        const i3 = s2[e3.type];
        if (i3) i3(e3);
        else {
          const s3 = e3.name, i4 = s3 in t2.getVoidElements();
          let l2 = e3.attributes;
          if (r2 && l2 && l2.length > 1) {
            const n3 = [];
            n3.map = {};
            const o4 = t2.getElementRule(e3.name);
            if (o4) {
              for (let e4 = 0, t3 = o4.attributesOrder.length; e4 < t3; e4++) {
                const t4 = o4.attributesOrder[e4];
                if (t4 in l2.map) {
                  const e5 = l2.map[t4];
                  n3.map[t4] = e5, n3.push({ name: t4, value: e5 });
                }
              }
              for (let e4 = 0, t3 = l2.length; e4 < t3; e4++) {
                const t4 = l2[e4].name;
                if (!(t4 in n3.map)) {
                  const e5 = l2.map[t4];
                  n3.map[t4] = e5, n3.push({ name: t4, value: e5 });
                }
              }
              l2 = n3;
            }
          }
          if (n2.start(s3, l2, i4), !i4) {
            let t3 = e3.firstChild;
            if (t3) {
              "pre" !== s3 && "textarea" !== s3 || 3 !== t3.type || "\n" !== (null === (o3 = t3.value) || void 0 === o3 ? void 0 : o3[0]) || n2.text("\n", true);
              do {
                a2(t3);
              } while (t3 = t3.next);
            }
            n2.end(s3);
          }
        }
      };
      return 1 !== o2.type || e2.inner ? 3 === o2.type ? s2[3](o2) : s2[11](o2) : a2(o2), n2.getContent();
    } };
  }, lg = /* @__PURE__ */ new Set();
  V(["margin", "margin-left", "margin-right", "margin-top", "margin-bottom", "padding", "padding-left", "padding-right", "padding-top", "padding-bottom", "border", "border-width", "border-style", "border-color", "background", "background-attachment", "background-clip", "background-color", "background-image", "background-origin", "background-position", "background-repeat", "background-size", "float", "position", "left", "right", "top", "bottom", "z-index", "display", "transform", "width", "max-width", "min-width", "height", "max-height", "min-height", "overflow", "overflow-x", "overflow-y", "text-overflow", "vertical-align", "transition", "transition-delay", "transition-duration", "transition-property", "transition-timing-function"], (e2) => {
    lg.add(e2);
  });
  const dg = ["font", "text-decoration", "text-emphasis"], cg = (e2, t2) => ue(e2.parseStyle(e2.getAttrib(t2, "style"))), ug = (e2, t2, n2) => {
    const o2 = cg(e2, t2), r2 = cg(e2, n2), s2 = (o3) => {
      var r3, s3;
      const a2 = null !== (r3 = e2.getStyle(t2, o3)) && void 0 !== r3 ? r3 : "", i3 = null !== (s3 = e2.getStyle(n2, o3)) && void 0 !== s3 ? s3 : "";
      return We(a2) && We(i3) && a2 !== i3;
    };
    return H(o2, (e3) => {
      const t3 = (t4) => H(t4, (t5) => t5 === e3);
      if (!t3(r2) && t3(dg)) {
        const e4 = K(r2, (e5) => H(dg, (t4) => ze(e5, t4)));
        return H(e4, s2);
      }
      return s2(e3);
    });
  }, mg = (e2, t2, n2) => M.from(n2.container()).filter(Ro).exists((o2) => {
    const r2 = e2 ? 0 : -1;
    return t2(o2.data.charAt(n2.offset() + r2));
  }), fg = O(mg, true, tu), gg = O(mg, false, tu), pg = (e2) => {
    const t2 = e2.container();
    return Ro(t2) && (0 === t2.data.length || Er(t2.data) && dm.isBookmarkNode(t2.parentNode));
  }, hg = (e2, t2) => (n2) => rc(e2 ? 0 : -1, n2).filter(t2).isSome(), bg = (e2) => Lo(e2) && "block" === Vn(mn(e2), "display"), vg = (e2) => Io(e2) && !((e3) => yo(e3) && "all" === e3.getAttribute("data-mce-bogus"))(e2), yg = hg(true, bg), Cg = hg(false, bg), wg = hg(true, zo), xg = hg(false, zo), kg = hg(true, _o), Sg = hg(false, _o), _g = hg(true, vg), Eg = hg(false, vg), Ng = (e2, t2) => ((e3, t3, n2) => {
    return vn(t3, e3) ? (o2 = ((e4, t4) => {
      const n3 = w(t4) ? t4 : P;
      let o3 = e4.dom;
      const r2 = [];
      for (; null !== o3.parentNode && void 0 !== o3.parentNode; ) {
        const e5 = o3.parentNode, t5 = mn(e5);
        if (r2.push(t5), true === n3(t5)) break;
        o3 = e5;
      }
      return r2;
    })(e3, (e4) => n2(e4) || bn(e4, t3)), o2.slice(0, -1)) : [];
    var o2;
  })(e2, t2, P), Rg = (e2, t2) => [e2].concat(Ng(e2, t2)), Ag = (e2, t2, n2) => Dc(e2, t2, n2, pg), Og = (e2, t2) => Q(Rg(mn(t2.container()), e2), nr), Tg = (e2, t2, n2) => Ag(e2, t2.dom, n2).forall((e3) => Og(t2, n2).fold(() => !oc(e3, n2, t2.dom), (o2) => !oc(e3, n2, t2.dom) && vn(o2, mn(e3.container())))), Bg = (e2, t2, n2) => Og(t2, n2).fold(() => Ag(e2, t2.dom, n2).forall((e3) => !oc(e3, n2, t2.dom)), (t3) => Ag(e2, t3.dom, n2).isNone()), Dg = O(Bg, false), Pg = O(Bg, true), Lg = O(Tg, false), Mg = O(Tg, true), Ig = (e2) => fc(e2).exists(rr), Fg = (e2, t2, n2) => {
    const o2 = K(Rg(mn(n2.container()), t2), nr), r2 = ie(o2).getOr(t2);
    return Tc(e2, r2.dom, n2).filter(Ig);
  }, Ug = (e2, t2) => fc(t2).exists(rr) || Fg(true, e2, t2).isSome(), zg = (e2, t2) => ((e3) => M.from(e3.getNode(true)).map(mn))(t2).exists(rr) || Fg(false, e2, t2).isSome(), jg = O(Fg, false), Hg = O(Fg, true), $g = (e2) => ai.isTextPosition(e2) && !e2.isAtStart() && !e2.isAtEnd(), Vg = (e2, t2) => {
    const n2 = K(Rg(mn(t2.container()), e2), nr);
    return ie(n2).getOr(e2);
  }, qg = (e2, t2) => $g(t2) ? gg(t2) : gg(t2) || Mc(Vg(e2, t2).dom, t2).exists(gg), Wg = (e2, t2) => $g(t2) ? fg(t2) : fg(t2) || Lc(Vg(e2, t2).dom, t2).exists(fg), Kg = (e2) => fc(e2).bind((e3) => Ko(e3, Ft)).exists((e3) => ((e4) => j(["pre", "pre-wrap"], e4))(Vn(e3, "white-space"))), Gg = (e2, t2) => (n2) => {
    return o2 = new Zo(n2, e2)[t2](), C(o2) && Io(o2) && Wd(o2);
    var o2;
  }, Yg = (e2, t2) => !Kg(t2) && (Dg(e2, t2) || Lg(e2, t2) || zg(e2, t2) || qg(e2, t2) || ((e3, t3) => {
    const n2 = Mc(e3.dom, t3).getOr(t3), o2 = Gg(e3.dom, "prev");
    return t3.isAtStart() && (o2(t3.container()) || o2(n2.container()));
  })(e2, t2)), Xg = (e2, t2) => !Kg(t2) && (Pg(e2, t2) || Mg(e2, t2) || Ug(e2, t2) || Wg(e2, t2) || ((e3, t3) => {
    const n2 = Lc(e3.dom, t3).getOr(t3), o2 = Gg(e3.dom, "next");
    return t3.isAtEnd() && (o2(t3.container()) || o2(n2.container()));
  })(e2, t2)), Qg = (e2, t2) => Yg(e2, t2) || Xg(e2, ((e3) => {
    const t3 = e3.container(), n2 = e3.offset();
    return Ro(t3) && n2 < t3.data.length ? ai(t3, n2 + 1) : e3;
  })(t2)), Jg = (e2, t2) => eu(e2.charAt(t2)), Zg = (e2, t2) => tu(e2.charAt(t2)), ep = (e2, t2, n2) => {
    const o2 = t2.data, r2 = ai(t2, 0);
    return n2 || !Jg(o2, 0) || Qg(e2, r2) ? !!(n2 && Zg(o2, 0) && Yg(e2, r2)) && (t2.data = pr + o2.slice(1), true) : (t2.data = " " + o2.slice(1), true);
  }, tp = (e2, t2, n2) => {
    const o2 = t2.data, r2 = ai(t2, o2.length - 1);
    return n2 || !Jg(o2, o2.length - 1) || Qg(e2, r2) ? !!(n2 && Zg(o2, o2.length - 1) && Xg(e2, r2)) && (t2.data = o2.slice(0, -1) + pr, true) : (t2.data = o2.slice(0, -1) + " ", true);
  }, np = (e2, t2) => {
    const n2 = t2.container();
    if (!Ro(n2)) return M.none();
    if (((e3) => {
      const t3 = e3.container();
      return Ro(t3) && Ue(t3.data, pr);
    })(t2)) {
      const o2 = ep(e2, n2, false) || ((e3) => {
        const t3 = e3.data, n3 = ((e4) => {
          const t4 = e4.split("");
          return $2(t4, (e5, n4) => eu(e5) && n4 > 0 && n4 < t4.length - 1 && nu(t4[n4 - 1]) && nu(t4[n4 + 1]) ? " " : e5).join("");
        })(t3);
        return n3 !== t3 && (e3.data = n3, true);
      })(n2) || tp(e2, n2, false);
      return Pt(o2, t2);
    }
    if (Qg(e2, t2)) {
      const o2 = ep(e2, n2, true) || tp(e2, n2, true);
      return Pt(o2, t2);
    }
    return M.none();
  }, op = (e2, t2, n2) => {
    if (0 === n2) return;
    const o2 = mn(e2), r2 = Wo(o2, nr).getOr(o2), s2 = e2.data.slice(t2, t2 + n2), a2 = t2 + n2 >= e2.data.length && Xg(r2, ai(e2, e2.data.length)), i3 = 0 === t2 && Yg(r2, ai(e2, 0));
    e2.replaceData(t2, n2, es(s2, 4, i3, a2));
  }, rp = (e2, t2) => {
    const n2 = e2.data.slice(t2), o2 = n2.length - Ve(n2).length;
    op(e2, t2, o2);
  }, sp = (e2, t2) => {
    const n2 = e2.data.slice(0, t2), o2 = n2.length - qe(n2).length;
    op(e2, t2 - o2, o2);
  }, ap = (e2, t2, n2, o2 = true) => {
    const r2 = qe(e2.data).length, s2 = o2 ? e2 : t2, a2 = o2 ? t2 : e2;
    return o2 ? s2.appendData(a2.data) : s2.insertData(0, a2.data), to(mn(a2)), n2 && rp(s2, r2), s2;
  }, ip = (e2, t2) => ((e3, t3) => {
    const n2 = e3.container(), o2 = e3.offset();
    return !ai.isTextPosition(e3) && n2 === t3.parentNode && o2 > ai.before(t3).offset();
  })(t2, e2) ? ai(t2.container(), t2.offset() - 1) : t2, lp = (e2) => {
    return Gr(e2.previousSibling) ? M.some((t2 = e2.previousSibling, Ro(t2) ? ai(t2, t2.data.length) : ai.after(t2))) : e2.previousSibling ? Fc(e2.previousSibling) : M.none();
    var t2;
  }, dp = (e2) => {
    return Gr(e2.nextSibling) ? M.some((t2 = e2.nextSibling, Ro(t2) ? ai(t2, 0) : ai.before(t2))) : e2.nextSibling ? Ic(e2.nextSibling) : M.none();
    var t2;
  }, cp = (e2, t2, n2) => ((e3, t3, n3) => e3 ? ((e4, t4) => dp(t4).orThunk(() => lp(t4)).orThunk(() => ((e5, t5) => Lc(e5, ai.after(t5)).orThunk(() => Mc(e5, ai.before(t5))))(e4, t4)))(t3, n3) : ((e4, t4) => lp(t4).orThunk(() => dp(t4)).orThunk(() => ((e5, t5) => M.from(t5.previousSibling ? t5.previousSibling : t5.parentNode).bind((t6) => Mc(e5, ai.before(t6))).orThunk(() => Lc(e5, ai.after(t5))))(e4, t4)))(t3, n3))(e2, t2, n2).map(O(ip, n2)), up = (e2, t2, n2) => {
    n2.fold(() => {
      e2.focus();
    }, (n3) => {
      e2.selection.setRng(n3.toRange(), t2);
    });
  }, mp = (e2, t2) => t2 && xe(e2.schema.getBlockElements(), Lt(t2)), fp = (e2) => {
    if (ss(e2)) {
      const t2 = dn('<br data-mce-bogus="1">');
      return eo(e2), Jn(e2, t2), M.some(ai.before(t2.dom));
    }
    return M.none();
  }, gp = (e2, t2, n2, o2 = true) => {
    const r2 = cp(t2, e2.getBody(), n2.dom), s2 = Wo(n2, O(mp, e2), (a2 = e2.getBody(), (e3) => e3.dom === a2));
    var a2;
    const i3 = ((e3, t3, n3) => {
      const o3 = kn(e3).filter(Ut), r3 = Sn(e3).filter(Ut);
      return to(e3), (s3 = o3, a3 = r3, i4 = t3, l2 = (e4, t4, o4) => {
        const r4 = e4.dom, s4 = t4.dom, a4 = r4.data.length;
        return ap(r4, s4, n3), o4.container() === s4 ? ai(r4, a4) : o4;
      }, s3.isSome() && a3.isSome() && i4.isSome() ? M.some(l2(s3.getOrDie(), a3.getOrDie(), i4.getOrDie())) : M.none()).orThunk(() => (n3 && (o3.each((e4) => sp(e4.dom, e4.dom.length)), r3.each((e4) => rp(e4.dom, 0))), t3));
      var s3, a3, i4, l2;
    })(n2, r2, ((e3, t3) => xe(e3.schema.getTextInlineElements(), Lt(t3)))(e2, n2));
    e2.dom.isEmpty(e2.getBody()) ? (e2.setContent(""), e2.selection.setCursorLocation()) : s2.bind(fp).fold(() => {
      o2 && up(e2, t2, i3);
    }, (n3) => {
      o2 && up(e2, t2, M.some(n3));
    });
  }, pp = /[\u0591-\u07FF\uFB1D-\uFDFF\uFE70-\uFEFC]/, hp = (e2, t2) => pn(mn(t2), Sl(e2)), bp = (e2, t2, n2) => {
    const o2 = ((e3, t3, n3) => K(ta.DOM.getParents(n3.container(), "*", t3), e3))(e2, t2, n2);
    return M.from(o2[o2.length - 1]);
  }, vp = (e2, t2) => {
    const n2 = t2.container(), o2 = t2.offset();
    return e2 ? Tr(n2) ? Ro(n2.nextSibling) ? ai(n2.nextSibling, 0) : ai.after(n2) : Pr(t2) ? ai(n2, o2 + 1) : t2 : Tr(n2) ? Ro(n2.previousSibling) ? ai(n2.previousSibling, n2.previousSibling.data.length) : ai.before(n2) : Lr(t2) ? ai(n2, o2 - 1) : t2;
  }, yp = O(vp, true), Cp = O(vp, false), wp = (e2, t2) => {
    const n2 = (e3) => e3.stopImmediatePropagation();
    e2.on("beforeinput input", n2, true), e2.getDoc().execCommand(t2), e2.off("beforeinput input", n2);
  }, xp = (e2) => wp(e2, "Delete"), kp = (e2) => sr(e2) || ir(e2), Sp = (e2, t2) => vn(e2, t2) ? Ko(t2, kp, /* @__PURE__ */ ((e3) => (t3) => Bt(xn(t3), e3, bn))(e2)) : M.none(), _p = (e2, t2 = true) => {
    e2.dom.isEmpty(e2.getBody()) && e2.setContent("", { no_selection: !t2 });
  }, Ep = (e2) => {
    var t2;
    return (8 === Mt(t2 = e2) || "#comment" === Lt(t2) ? kn(e2) : On(e2)).bind(Ep).orThunk(() => M.some(e2));
  }, Np = (e2, t2, n2, o2 = true) => {
    var r2;
    t2.deleteContents();
    const s2 = Ep(n2).getOr(n2), a2 = mn(null !== (r2 = e2.dom.getParent(s2.dom, e2.dom.isBlock)) && void 0 !== r2 ? r2 : n2.dom);
    if (a2.dom === e2.getBody() ? _p(e2, o2) : ss(a2) && (kr(a2), o2 && e2.selection.setCursorLocation(a2.dom, 0)), !bn(n2, a2)) {
      const e3 = Bt(xn(a2), n2) ? [] : xn(i3 = a2).map(Nn).map((e4) => K(e4, (e5) => !bn(i3, e5))).getOr([]);
      V(e3.concat(Nn(n2)), (e4) => {
        bn(e4, a2) || vn(e4, a2) || !ss(e4) || to(e4);
      });
    }
    var i3;
  }, Rp = (e2) => vr(e2, "td,th"), Ap = (e2, t2) => ({ start: e2, end: t2 }), Op = Ti([{ singleCellTable: ["rng", "cell"] }, { fullTable: ["table"] }, { partialTable: ["cells", "outsideDetails"] }, { multiTable: ["startTableCells", "endTableCells", "betweenRng"] }]), Tp = (e2, t2) => Xo(mn(e2), "td,th", t2), Bp = (e2) => !bn(e2.start, e2.end), Dp = (e2, t2) => fu(e2.start, t2).bind((n2) => fu(e2.end, t2).bind((e3) => Pt(bn(n2, e3), n2))), Pp = (e2) => (t2) => Dp(t2, e2).map((e3) => /* @__PURE__ */ ((e4, t3, n2) => ({ rng: e4, table: t3, cells: n2 }))(t2, e3, Rp(e3))), Lp = (e2, t2, n2, o2) => {
    if (n2.collapsed || !e2.forall(Bp)) return M.none();
    if (t2.isSameTable) {
      const t3 = e2.bind(Pp(o2));
      return M.some({ start: t3, end: t3 });
    }
    {
      const e3 = Tp(n2.startContainer, o2), t3 = Tp(n2.endContainer, o2), r2 = e3.bind(/* @__PURE__ */ ((e4) => (t4) => fu(t4, e4).bind((e5) => le(Rp(e5)).map((e6) => Ap(t4, e6))))(o2)).bind(Pp(o2)), s2 = t3.bind(/* @__PURE__ */ ((e4) => (t4) => fu(t4, e4).bind((e5) => ie(Rp(e5)).map((e6) => Ap(e6, t4))))(o2)).bind(Pp(o2));
      return M.some({ start: r2, end: s2 });
    }
  }, Mp = (e2, t2) => J(e2, (e3) => bn(e3, t2)), Ip = (e2) => Dt(Mp(e2.cells, e2.rng.start), Mp(e2.cells, e2.rng.end), (t2, n2) => e2.cells.slice(t2, n2 + 1)), Fp = (e2, t2) => {
    const { startTable: n2, endTable: o2 } = t2, r2 = e2.cloneRange();
    return n2.each((e3) => r2.setStartAfter(e3.dom)), o2.each((e3) => r2.setEndBefore(e3.dom)), r2;
  }, Up = (e2, t2) => {
    const n2 = /* @__PURE__ */ ((e3) => (t3) => bn(e3, t3))(e2), o2 = ((e3, t3) => {
      const n3 = Tp(e3.startContainer, t3), o3 = Tp(e3.endContainer, t3);
      return Dt(n3, o3, Ap);
    })(t2, n2), r2 = ((e3, t3) => {
      const n3 = (e4) => fu(mn(e4), t3), o3 = n3(e3.startContainer), r3 = n3(e3.endContainer), s2 = o3.isSome(), a2 = r3.isSome(), i3 = Dt(o3, r3, bn).getOr(false);
      return { startTable: o3, endTable: r3, isStartInTable: s2, isEndInTable: a2, isSameTable: i3, isMultiTable: !i3 && s2 && a2 };
    })(t2, n2);
    return ((e3, t3, n3) => e3.exists((e4) => ((e5, t4) => !Bp(e5) && Dp(e5, t4).exists((e6) => {
      const t5 = e6.dom.rows;
      return 1 === t5.length && 1 === t5[0].cells.length;
    }))(e4, n3) && hu(e4.start, t3)))(o2, t2, n2) ? o2.map((e3) => Op.singleCellTable(t2, e3.start)) : r2.isMultiTable ? ((e3, t3, n3, o3) => Lp(e3, t3, n3, o3).bind(({ start: e4, end: o4 }) => {
      const r3 = e4.bind(Ip).getOr([]), s2 = o4.bind(Ip).getOr([]);
      if (r3.length > 0 && s2.length > 0) {
        const e5 = Fp(n3, t3);
        return M.some(Op.multiTable(r3, s2, e5));
      }
      return M.none();
    }))(o2, r2, t2, n2) : ((e3, t3, n3, o3) => Lp(e3, t3, n3, o3).bind(({ start: e4, end: t4 }) => e4.or(t4)).bind((e4) => {
      const { isSameTable: o4 } = t3, r3 = Ip(e4).getOr([]);
      if (o4 && e4.cells.length === r3.length) return M.some(Op.fullTable(e4.table));
      if (r3.length > 0) {
        if (o4) return M.some(Op.partialTable(r3, M.none()));
        {
          const e5 = Fp(n3, t3);
          return M.some(Op.partialTable(r3, M.some({ ...t3, rng: e5 })));
        }
      }
      return M.none();
    }))(o2, r2, t2, n2);
  }, zp = (e2) => V(e2, (e3) => {
    Yt(e3, "contenteditable"), kr(e3);
  }), jp = (e2, t2, n2, o2) => {
    const r2 = n2.cloneRange();
    o2 ? (r2.setStart(n2.startContainer, n2.startOffset), r2.setEndAfter(t2.dom.lastChild)) : (r2.setStartBefore(t2.dom.firstChild), r2.setEnd(n2.endContainer, n2.endOffset)), qp(e2, r2, t2, false).each((e3) => e3());
  }, Hp = (e2) => {
    const t2 = mu(e2), n2 = mn(e2.selection.getNode());
    Fo(n2.dom) && ss(n2) ? e2.selection.setCursorLocation(n2.dom, 0) : e2.selection.collapse(true), t2.length > 1 && H(t2, (e3) => bn(e3, n2)) && Vt(n2, "data-mce-selected", "1");
  }, $p = (e2, t2, n2) => M.some(() => {
    const o2 = e2.selection.getRng(), r2 = n2.bind(({ rng: n3, isStartInTable: r3 }) => {
      const s2 = ((e3, t3) => M.from(e3.dom.getParent(t3, e3.dom.isBlock)).map(mn))(e2, r3 ? n3.endContainer : n3.startContainer);
      n3.deleteContents(), ((e3, t3, n4) => {
        n4.each((n5) => {
          t3 ? to(n5) : (kr(n5), e3.selection.setCursorLocation(n5.dom, 0));
        });
      })(e2, r3, s2.filter(ss));
      const a2 = r3 ? t2[0] : t2[t2.length - 1];
      return jp(e2, a2, o2, r3), ss(a2) ? M.none() : M.some(r3 ? t2.slice(1) : t2.slice(0, -1));
    }).getOr(t2);
    zp(r2), Hp(e2);
  }), Vp = (e2, t2, n2, o2) => M.some(() => {
    const r2 = e2.selection.getRng(), s2 = t2[0], a2 = n2[n2.length - 1];
    jp(e2, s2, r2, true), jp(e2, a2, r2, false);
    const i3 = ss(s2) ? t2 : t2.slice(1), l2 = ss(a2) ? n2 : n2.slice(0, -1);
    zp(i3.concat(l2)), o2.deleteContents(), Hp(e2);
  }), qp = (e2, t2, n2, o2 = true) => M.some(() => {
    Np(e2, t2, n2, o2);
  }), Wp = (e2, t2) => M.some(() => gp(e2, false, t2)), Kp = (e2, t2) => Q(Rg(t2, e2), dr), Gp = (e2, t2) => Q(Rg(t2, e2), Ht("caption")), Yp = (e2, t2) => M.some(() => {
    kr(t2), e2.selection.setCursorLocation(t2.dom, 0);
  }), Xp = (e2, t2) => e2 ? kg(t2) : Sg(t2), Qp = (e2, t2, n2) => {
    const o2 = mn(e2.getBody());
    return Gp(o2, n2).fold(() => ((e3, t3, n3, o3) => {
      const r2 = ai.fromRangeStart(e3.selection.getRng());
      return Kp(n3, o3).bind((o4) => ss(o4) ? Yp(e3, o4) : ((e4, t4, n4, o5, r3) => Bc(n4, e4.getBody(), r3).bind((e5) => Kp(t4, mn(e5.getNode())).bind((e6) => bn(e6, o5) ? M.none() : M.some(S))))(e3, n3, t3, o4, r2));
    })(e2, t2, o2, n2).orThunk(() => Pt(((e3, t3) => {
      const n3 = ai.fromRangeStart(e3.selection.getRng());
      return Xp(t3, n3) || Tc(t3, e3.getBody(), n3).exists((e4) => Xp(t3, e4));
    })(e2, t2), S)), (n3) => ((e3, t3, n4, o3) => {
      const r2 = ai.fromRangeStart(e3.selection.getRng());
      return ss(o3) ? Yp(e3, o3) : ((e4, t4, n5, o4, r3) => Bc(n5, e4.getBody(), r3).fold(() => M.some(S), (s2) => ((e5, t5, n6, o5) => Ic(e5.dom).bind((r4) => Fc(e5.dom).map((e6) => t5 ? n6.isEqual(r4) && o5.isEqual(e6) : n6.isEqual(e6) && o5.isEqual(r4))).getOr(true))(o4, n5, r3, s2) ? ((e5, t5) => Yp(e5, t5))(e4, o4) : ((e5, t5, n6) => Gp(e5, mn(n6.getNode())).fold(() => M.some(S), (e6) => Pt(!bn(e6, t5), S)))(t4, o4, s2)))(e3, n4, t3, o3, r2);
    })(e2, t2, o2, n3));
  }, Jp = (e2, t2) => {
    const n2 = mn(e2.selection.getStart(true)), o2 = mu(e2);
    return e2.selection.isCollapsed() && 0 === o2.length ? Qp(e2, t2, n2) : ((e3, t3, n3) => {
      const o3 = mn(e3.getBody()), r2 = e3.selection.getRng();
      return 0 !== n3.length ? $p(e3, n3, M.none()) : ((e4, t4, n4, o4) => Gp(t4, o4).fold(() => ((e5, t5, n5) => Up(t5, n5).bind((t6) => t6.fold(O(qp, e5), O(Wp, e5), O($p, e5), O(Vp, e5))))(e4, t4, n4), (t5) => ((e5, t6) => Yp(e5, t6))(e4, t5)))(e3, o3, r2, t3);
    })(e2, n2, o2);
  }, Zp = (e2, t2) => {
    let n2 = t2;
    for (; n2 && n2 !== e2; ) {
      if (Mo(n2) || Io(n2)) return n2;
      n2 = n2.parentNode;
    }
    return null;
  }, eh = ["data-ephox-", "data-mce-", "data-alloy-", "data-snooker-", "_"], th = Tt.each, nh = (e2) => {
    const t2 = e2.dom, n2 = new Set(e2.serializer.getTempAttrs()), o2 = (e3) => H(eh, (t3) => ze(e3, t3)) || n2.has(e3);
    return { compare: (e3, n3) => {
      if (e3.nodeName !== n3.nodeName || e3.nodeType !== n3.nodeType) return false;
      const r2 = (e4) => {
        const n4 = {};
        return th(t2.getAttribs(e4), (r3) => {
          const s3 = r3.nodeName.toLowerCase();
          "style" === s3 || o2(s3) || (n4[s3] = t2.getAttrib(e4, s3));
        }), n4;
      }, s2 = (e4, t3) => {
        for (const n4 in e4) if (xe(e4, n4)) {
          const o3 = t3[n4];
          if (v(o3)) return false;
          if (e4[n4] !== o3) return false;
          delete t3[n4];
        }
        for (const e5 in t3) if (xe(t3, e5)) return false;
        return true;
      };
      if (yo(e3) && yo(n3)) {
        if (!s2(r2(e3), r2(n3))) return false;
        if (!s2(t2.parseStyle(t2.getAttrib(e3, "style")), t2.parseStyle(t2.getAttrib(n3, "style")))) return false;
      }
      return !Zc(e3) && !Zc(n3);
    }, isAttributeInternal: o2 };
  }, oh = (e2, t2, n2, o2) => {
    const r2 = n2.name;
    for (let t3 = 0, s2 = e2.length; t3 < s2; t3++) {
      const s3 = e2[t3];
      if (s3.name === r2) {
        const e3 = o2.nodes[r2];
        e3 ? e3.nodes.push(n2) : o2.nodes[r2] = { filter: s3, nodes: [n2] };
      }
    }
    if (n2.attributes) for (let e3 = 0, r3 = t2.length; e3 < r3; e3++) {
      const r4 = t2[e3], s2 = r4.name;
      if (s2 in n2.attributes.map) {
        const e4 = o2.attributes[s2];
        e4 ? e4.nodes.push(n2) : o2.attributes[s2] = { filter: r4, nodes: [n2] };
      }
    }
  }, rh = (e2, t2) => {
    const n2 = (e3, n3) => {
      fe(e3, (e4) => {
        const o2 = de(e4.nodes);
        V(e4.filter.callbacks, (r2) => {
          for (let t3 = o2.length - 1; t3 >= 0; t3--) {
            const r3 = o2[t3];
            (n3 ? void 0 !== r3.attr(e4.filter.name) : r3.name === e4.filter.name) && !y(r3.parent) || o2.splice(t3, 1);
          }
          o2.length > 0 && r2(o2, e4.filter.name, t2);
        });
      });
    };
    n2(e2.nodes, false), n2(e2.attributes, true);
  }, sh = (e2, t2, n2, o2 = {}) => {
    const r2 = ((e3, t3, n3) => {
      const o3 = { nodes: {}, attributes: {} };
      return n3.firstChild && ((n4, r3) => {
        let s2 = n4;
        for (; s2 = s2.walk(); ) oh(e3, t3, s2, o3);
      })(n3), o3;
    })(e2, t2, n2);
    rh(r2, o2);
  }, ah = (e2, t2, n2, o2) => {
    if (t2.insert && n2[o2.name]) {
      const e3 = new Zf("br", 1);
      e3.attr("data-mce-bogus", "1"), o2.empty().append(e3);
    } else o2.empty().append(new Zf("#text", 3)).value = pr;
  }, ih = (e2, t2) => {
    const n2 = null == e2 ? void 0 : e2.firstChild;
    return C(n2) && n2 === e2.lastChild && n2.name === t2;
  }, lh = (e2, t2, n2, o2) => o2.isEmpty(t2, n2, (t3) => ((e3, t4) => {
    const n3 = e3.getElementRule(t4.name);
    return true === (null == n3 ? void 0 : n3.paddEmpty);
  })(e2, t3)), dh = (e2, t2, n2 = e2.parent) => {
    if (t2.getSpecialElements()[e2.name]) e2.empty().remove();
    else {
      const o2 = e2.children();
      for (const e3 of o2) n2 && !t2.isValidChild(n2.name, e3.name) && dh(e3, t2, n2);
      e2.unwrap();
    }
  }, ch = (e2, t2, n2 = S) => {
    const o2 = t2.getTextBlockElements(), r2 = t2.getNonEmptyElements(), s2 = t2.getWhitespaceElements(), a2 = Tt.makeMap("tr,td,th,tbody,thead,tfoot,table"), i3 = /* @__PURE__ */ new Set();
    for (let l2 = 0; l2 < e2.length; l2++) {
      const d2 = e2[l2];
      let c2, u2, m2;
      if (!d2.parent || i3.has(d2)) continue;
      if (o2[d2.name] && "li" === d2.parent.name) {
        let e3 = d2.next;
        for (; e3 && o2[e3.name]; ) e3.name = "li", i3.add(e3), d2.parent.insert(e3, d2.parent), e3 = e3.next;
        d2.unwrap();
        continue;
      }
      const f2 = [d2];
      for (c2 = d2.parent; c2 && !t2.isValidChild(c2.name, d2.name) && !a2[c2.name]; c2 = c2.parent) f2.push(c2);
      if (c2 && f2.length > 1) if (t2.isValidChild(c2.name, d2.name)) {
        f2.reverse(), u2 = f2[0].clone(), n2(u2);
        let e3 = u2;
        for (let o3 = 0; o3 < f2.length - 1; o3++) {
          t2.isValidChild(e3.name, f2[o3].name) ? (m2 = f2[o3].clone(), n2(m2), e3.append(m2)) : m2 = e3;
          for (let e4 = f2[o3].firstChild; e4 && e4 !== f2[o3 + 1]; ) {
            const t3 = e4.next;
            m2.append(e4), e4 = t3;
          }
          e3 = m2;
        }
        lh(t2, r2, s2, u2) ? c2.insert(d2, f2[0], true) : (c2.insert(u2, f2[0], true), c2.insert(d2, u2)), c2 = f2[0], (lh(t2, r2, s2, c2) || ih(c2, "br")) && c2.empty().remove();
      } else dh(d2, t2);
      else if (d2.parent) {
        if ("li" === d2.name) {
          let e3 = d2.prev;
          if (e3 && ("ul" === e3.name || "ol" === e3.name)) {
            e3.append(d2);
            continue;
          }
          if (e3 = d2.next, e3 && ("ul" === e3.name || "ol" === e3.name) && e3.firstChild) {
            e3.insert(d2, e3.firstChild, true);
            continue;
          }
          const t3 = new Zf("ul", 1);
          n2(t3), d2.wrap(t3);
          continue;
        }
        if (t2.isValidChild(d2.parent.name, "div") && t2.isValidChild("div", d2.name)) {
          const e3 = new Zf("div", 1);
          n2(e3), d2.wrap(e3);
        } else dh(d2, t2);
      }
    }
  }, uh = (e2) => e2.collapsed ? e2 : ((e3) => {
    const t2 = ai.fromRangeStart(e3), n2 = ai.fromRangeEnd(e3), o2 = e3.commonAncestorContainer;
    return Tc(false, o2, n2).map((r2) => !oc(t2, n2, o2) && oc(t2, r2, o2) ? ((e4, t3, n3, o3) => {
      const r3 = document.createRange();
      return r3.setStart(e4, t3), r3.setEnd(n3, o3), r3;
    })(t2.container(), t2.offset(), r2.container(), r2.offset()) : e3).getOr(e3);
  })(e2), mh = (e2, t2) => {
    let n2 = t2.firstChild, o2 = t2.lastChild;
    return n2 && "meta" === n2.name && (n2 = n2.next), o2 && "mce_marker" === o2.attr("id") && (o2 = o2.prev), ((e3, t3) => {
      const n3 = e3.getNonEmptyElements();
      return C(t3) && (t3.isEmpty(n3) || ((e4, t4) => e4.getBlockElements()[t4.name] && ((e5) => C(e5.firstChild) && e5.firstChild === e5.lastChild)(t4) && ((e5) => "br" === e5.name || e5.value === pr)(t4.firstChild))(e3, t3));
    })(e2, o2) && (o2 = null == o2 ? void 0 : o2.prev), !(!n2 || n2 !== o2 || "ul" !== n2.name && "ol" !== n2.name);
  }, fh = (e2) => {
    return e2.length > 0 && (!(n2 = e2[e2.length - 1]).firstChild || C(null == (t2 = n2) ? void 0 : t2.firstChild) && t2.firstChild === t2.lastChild && ((e3) => e3.data === pr || Po(e3))(t2.firstChild)) ? e2.slice(0, -1) : e2;
    var t2, n2;
  }, gh = (e2, t2) => {
    const n2 = e2.getParent(t2, e2.isBlock);
    return n2 && "LI" === n2.nodeName ? n2 : null;
  }, ph = (e2, t2) => {
    const n2 = ai.after(e2), o2 = Nc(t2).prev(n2);
    return o2 ? o2.toRange() : null;
  }, hh = (e2, t2, n2, o2) => {
    const r2 = ((e3, t3, n3) => {
      const o3 = t3.serialize(n3);
      return ((e4) => {
        var t4, n4;
        const o4 = e4.firstChild, r3 = e4.lastChild;
        return o4 && "META" === o4.nodeName && (null === (t4 = o4.parentNode) || void 0 === t4 || t4.removeChild(o4)), r3 && "mce_marker" === r3.id && (null === (n4 = r3.parentNode) || void 0 === n4 || n4.removeChild(r3)), e4;
      })(e3.createFragment(o3));
    })(t2, e2, o2), s2 = gh(t2, n2.startContainer), a2 = fh((i3 = r2.firstChild, K(null !== (l2 = null == i3 ? void 0 : i3.childNodes) && void 0 !== l2 ? l2 : [], (e3) => "LI" === e3.nodeName)));
    var i3, l2;
    const d2 = t2.getRoot(), c2 = (e3) => {
      const o3 = ai.fromRangeStart(n2), r3 = Nc(t2.getRoot()), a3 = 1 === e3 ? r3.prev(o3) : r3.next(o3), i4 = null == a3 ? void 0 : a3.getNode();
      return !i4 || gh(t2, i4) !== s2;
    };
    return s2 ? c2(1) ? ((e3, t3, n3) => {
      const o3 = e3.parentNode;
      return o3 && Tt.each(t3, (t4) => {
        o3.insertBefore(t4, e3);
      }), ((e4, t4) => {
        const n4 = ai.before(e4), o4 = Nc(t4).next(n4);
        return o4 ? o4.toRange() : null;
      })(e3, n3);
    })(s2, a2, d2) : c2(2) ? ((e3, t3, n3, o3) => (o3.insertAfter(t3.reverse(), e3), ph(t3[0], n3)))(s2, a2, d2, t2) : ((e3, t3, n3, o3) => {
      const r3 = ((e4, t4) => {
        const n4 = t4.cloneRange(), o4 = t4.cloneRange();
        return n4.setStartBefore(e4), o4.setEndAfter(e4), [n4.cloneContents(), o4.cloneContents()];
      })(e3, o3), s3 = e3.parentNode;
      return s3 && (s3.insertBefore(r3[0], e3), Tt.each(t3, (t4) => {
        s3.insertBefore(t4, e3);
      }), s3.insertBefore(r3[1], e3), s3.removeChild(e3)), ph(t3[t3.length - 1], n3);
    })(s2, a2, d2, n2) : null;
  }, bh = ["pre"], vh = Fo, yh = (e2, t2, n2) => {
    var o2, r2, s2;
    const a2 = e2.selection, i3 = e2.dom, l2 = e2.parser, d2 = n2.merge, c2 = ig({ validate: true }, e2.schema), u2 = '<span id="mce_marker" data-mce-type="bookmark">&#xFEFF;</span>';
    -1 === t2.indexOf("{$caret}") && (t2 += "{$caret}"), t2 = t2.replace(/\{\$caret\}/, u2);
    let m2 = a2.getRng();
    const f2 = m2.startContainer, g2 = e2.getBody();
    f2 === g2 && a2.isCollapsed() && i3.isBlock(g2.firstChild) && ((e3, t3) => C(t3) && !e3.schema.getVoidElements()[t3.nodeName])(e2, g2.firstChild) && i3.isEmpty(g2.firstChild) && (m2 = i3.createRng(), m2.setStart(g2.firstChild, 0), m2.setEnd(g2.firstChild, 0), a2.setRng(m2)), a2.isCollapsed() || ((e3) => {
      const t3 = e3.dom, n3 = uh(e3.selection.getRng());
      e3.selection.setRng(n3);
      const o3 = t3.getParent(n3.startContainer, vh);
      ((e4, t4, n4) => !!C(n4) && n4 === e4.getParent(t4.endContainer, vh) && hu(mn(n4), t4))(t3, n3, o3) ? qp(e3, n3, mn(o3)) : e3.getDoc().execCommand("Delete", false);
    })(e2);
    const p2 = a2.getNode(), h2 = { context: p2.nodeName.toLowerCase(), data: n2.data, insert: true }, b2 = l2.parse(t2, h2);
    if (true === n2.paste && mh(e2.schema, b2) && ((e3, t3) => !!gh(e3, t3))(i3, p2)) return m2 = hh(c2, i3, a2.getRng(), b2), m2 && a2.setRng(m2), t2;
    true === n2.paste && ((e3, t3, n3, o3) => {
      var r3;
      const s3 = t3.firstChild, a3 = t3.lastChild, i4 = s3 === ("bookmark" === a3.attr("data-mce-type") ? a3.prev : a3), l3 = j(bh, s3.name);
      if (i4 && l3) {
        const t4 = "false" !== s3.attr("contenteditable"), a4 = (null === (r3 = e3.getParent(n3, e3.isBlock)) || void 0 === r3 ? void 0 : r3.nodeName.toLowerCase()) === s3.name, i5 = M.from(Zp(o3, n3)).forall(Mo);
        return t4 && a4 && i5;
      }
      return false;
    })(i3, b2, p2, e2.getBody()) && (null === (o2 = b2.firstChild) || void 0 === o2 || o2.unwrap()), ((e3) => {
      let t3 = e3;
      for (; t3 = t3.walk(); ) 1 === t3.type && t3.attr("data-mce-fragment", "1");
    })(b2);
    let v2 = b2.lastChild;
    if (v2 && "mce_marker" === v2.attr("id")) {
      const t3 = v2;
      for (v2 = v2.prev; v2; v2 = v2.walk(true)) if (3 === v2.type || !i3.isBlock(v2.name)) {
        v2.parent && e2.schema.isValidChild(v2.parent.name, "span") && v2.parent.insert(t3, v2, "br" === v2.name);
        break;
      }
    }
    if (e2._selectionOverrides.showBlockCaretContainer(p2), h2.invalid) {
      e2.selection.setContent(u2);
      let n3, o3 = a2.getNode();
      const d3 = e2.getBody();
      for (Bo(o3) ? o3 = n3 = d3 : n3 = o3; n3 && n3 !== d3; ) o3 = n3, n3 = n3.parentNode;
      t2 = o3 === d3 ? d3.innerHTML : i3.getOuterHTML(o3);
      const m3 = l2.parse(t2);
      for (let e3 = m3; e3; e3 = e3.walk()) if ("mce_marker" === e3.attr("id")) {
        e3.replace(b2);
        break;
      }
      const f3 = b2.children(), g3 = null !== (s2 = null === (r2 = b2.parent) || void 0 === r2 ? void 0 : r2.name) && void 0 !== s2 ? s2 : m3.name;
      b2.unwrap();
      const p3 = K(f3, (t3) => !e2.schema.isValidChild(g3, t3.name));
      ch(p3, e2.schema), sh(l2.getNodeFilters(), l2.getAttributeFilters(), m3), t2 = c2.serialize(m3), o3 === d3 ? i3.setHTML(d3, t2) : i3.setOuterHTML(o3, t2);
    } else t2 = c2.serialize(b2), ((e3, t3, n3) => {
      var o3;
      if ("all" === n3.getAttribute("data-mce-bogus")) null === (o3 = n3.parentNode) || void 0 === o3 || o3.insertBefore(e3.dom.createFragment(t3), n3);
      else {
        const o4 = n3.firstChild, r3 = n3.lastChild;
        !o4 || o4 === r3 && "BR" === o4.nodeName ? e3.dom.setHTML(n3, t3) : e3.selection.setContent(t3, { no_events: true });
      }
    })(e2, t2, p2);
    var y2;
    return ((e3, t3) => {
      const n3 = e3.schema.getTextInlineElements(), o3 = e3.dom;
      if (t3) {
        const t4 = e3.getBody(), r3 = nh(e3);
        Tt.each(o3.select("*[data-mce-fragment]"), (e4) => {
          if (C(n3[e4.nodeName.toLowerCase()]) && ((e5, t5) => te(cg(e5, t5), (e6) => !((e7) => lg.has(e7))(e6)))(o3, e4)) {
            for (let n4 = e4.parentElement; C(n4) && n4 !== t4 && !ug(o3, e4, n4); n4 = n4.parentElement) if (r3.compare(n4, e4)) {
              o3.remove(e4, true);
              break;
            }
          }
        });
      }
    })(e2, d2), ((e3, t3) => {
      var n3, o3, r3;
      let s3;
      const a3 = e3.dom, i4 = e3.selection;
      if (!t3) return;
      i4.scrollIntoView(t3);
      const l3 = Zp(e3.getBody(), t3);
      if (l3 && "false" === a3.getContentEditable(l3)) return a3.remove(t3), void i4.select(l3);
      let d3 = a3.createRng();
      const c3 = t3.previousSibling;
      if (Ro(c3)) {
        d3.setStart(c3, null !== (o3 = null === (n3 = c3.nodeValue) || void 0 === n3 ? void 0 : n3.length) && void 0 !== o3 ? o3 : 0);
        const e4 = t3.nextSibling;
        Ro(e4) && (c3.appendData(e4.data), null === (r3 = e4.parentNode) || void 0 === r3 || r3.removeChild(e4));
      } else d3.setStartBefore(t3), d3.setEndBefore(t3);
      const u3 = a3.getParent(t3, a3.isBlock);
      a3.remove(t3), u3 && a3.isEmpty(u3) && (eo(mn(u3)), d3.setStart(u3, 0), d3.setEnd(u3, 0), vh(u3) || ((e4) => !!e4.getAttribute("data-mce-fragment"))(u3) || !(s3 = ((t4) => {
        let n4 = ai.fromRangeStart(t4);
        return n4 = Nc(e3.getBody()).next(n4), null == n4 ? void 0 : n4.toRange();
      })(d3)) ? a3.add(u3, a3.create("br", { "data-mce-bogus": "1" })) : (d3 = s3, a3.remove(u3))), i4.setRng(d3);
    })(e2, i3.get("mce_marker")), y2 = e2.getBody(), Tt.each(y2.getElementsByTagName("*"), (e3) => {
      e3.removeAttribute("data-mce-fragment");
    }), ((e3, t3) => {
      M.from(e3.getParent(t3, "td,th")).map(mn).each(Sr);
    })(i3, a2.getStart()), t2;
  }, Ch = (e2) => e2 instanceof Zf, wh = (e2, t2, n2) => {
    e2.dom.setHTML(e2.getBody(), t2), true !== n2 && ((e3) => {
      If(e3) && Ic(e3.getBody()).each((t3) => {
        const n3 = t3.getNode(), o2 = _o(n3) ? Ic(n3).getOr(t3) : t3;
        e3.selection.setRng(o2.toRange());
      });
    })(e2);
  }, xh = (e2, t2) => ((e3, t3) => {
    const n2 = e3.dom;
    return n2.parentNode ? ((e4, t4) => Q(e4.dom.childNodes, (e5) => t4(mn(e5))).map(mn))(mn(n2.parentNode), (n3) => !bn(e3, n3) && t3(n3)) : M.none();
  })(e2, t2).isSome(), kh = (e2) => w(e2) ? e2 : P, Sh = (e2, t2, n2) => {
    const o2 = t2(e2), r2 = kh(n2);
    return o2.orThunk(() => r2(e2) ? M.none() : ((e3, t3, n3) => {
      let o3 = e3.dom;
      const r3 = kh(n3);
      for (; o3.parentNode; ) {
        o3 = o3.parentNode;
        const e4 = mn(o3), n4 = t3(e4);
        if (n4.isSome()) return n4;
        if (r3(e4)) break;
      }
      return M.none();
    })(e2, t2, r2));
  }, _h = Bu, Eh = (e2, t2, n2) => {
    const o2 = e2.formatter.get(n2);
    if (o2) for (let n3 = 0; n3 < o2.length; n3++) {
      const r2 = o2[n3];
      if (Uu(r2) && false === r2.inherit && e2.dom.is(t2, r2.selector)) return true;
    }
    return false;
  }, Nh = (e2, t2, n2, o2, r2) => {
    const s2 = e2.dom.getRoot();
    if (t2 === s2) return false;
    const a2 = e2.dom.getParent(t2, (t3) => !!Eh(e2, t3, n2) || t3.parentNode === s2 || !!Oh(e2, t3, n2, o2, true));
    return !!Oh(e2, a2, n2, o2, r2);
  }, Rh = (e2, t2, n2) => !(!zu(n2) || !_h(t2, n2.inline)) || !(!Fu(n2) || !_h(t2, n2.block)) || !!Uu(n2) && yo(t2) && e2.is(t2, n2.selector), Ah = (e2, t2, n2, o2, r2, s2) => {
    const a2 = n2[o2], i3 = "attributes" === o2;
    if (w(n2.onmatch)) return n2.onmatch(t2, n2, o2);
    if (a2) {
      if (_e(a2)) {
        for (let n3 = 0; n3 < a2.length; n3++) if (i3 ? e2.getAttrib(t2, a2[n3]) : Pu(e2, t2, a2[n3])) return true;
      } else for (const o3 in a2) if (xe(a2, o3)) {
        const l2 = i3 ? e2.getAttrib(t2, o3) : Pu(e2, t2, o3), d2 = Tu(a2[o3], s2), c2 = y(l2) || Ke(l2);
        if (c2 && y(d2)) continue;
        if (r2 && c2 && !n2.exact) return false;
        if ((!r2 || n2.exact) && !_h(l2, Du(d2, o3))) return false;
      }
    }
    return true;
  }, Oh = (e2, t2, n2, o2, r2) => {
    const s2 = e2.formatter.get(n2), a2 = e2.dom;
    if (s2 && yo(t2)) for (let n3 = 0; n3 < s2.length; n3++) {
      const i3 = s2[n3];
      if (Rh(e2.dom, t2, i3) && Ah(a2, t2, i3, "attributes", r2, o2) && Ah(a2, t2, i3, "styles", r2, o2)) {
        const n4 = i3.classes;
        if (n4) {
          for (let r3 = 0; r3 < n4.length; r3++) if (!e2.dom.hasClass(t2, Tu(n4[r3], o2))) return;
        }
        return i3;
      }
    }
  }, Th = (e2, t2, n2, o2, r2) => {
    if (o2) return Nh(e2, o2, t2, n2, r2);
    if (o2 = e2.selection.getNode(), Nh(e2, o2, t2, n2, r2)) return true;
    const s2 = e2.selection.getStart();
    return !(s2 === o2 || !Nh(e2, s2, t2, n2, r2));
  }, Bh = _r, Dh = (e2) => ((e3) => {
    const t2 = [];
    let n2 = e3;
    for (; n2; ) {
      if (Ro(n2) && n2.data !== Bh || n2.childNodes.length > 1) return [];
      yo(n2) && t2.push(n2), n2 = n2.firstChild;
    }
    return t2;
  })(e2).length > 0, Ph = (e2) => {
    if (e2) {
      const t2 = new Zo(e2, e2);
      for (let e3 = t2.current(); e3; e3 = t2.next()) if (Ro(e3)) return e3;
    }
    return null;
  }, Lh = (e2) => {
    const t2 = cn("span");
    return qt(t2, { id: Uc, "data-mce-bogus": "1", "data-mce-type": "format-caret" }), e2 && Jn(t2, un(Bh)), t2;
  }, Mh = (e2, t2, n2 = true) => {
    const o2 = e2.dom, r2 = e2.selection;
    if (Dh(t2)) gp(e2, false, mn(t2), n2);
    else {
      const e3 = r2.getRng(), n3 = o2.getParent(t2, o2.isBlock), s2 = e3.startContainer, a2 = e3.startOffset, i3 = e3.endContainer, l2 = e3.endOffset, d2 = ((e4) => {
        const t3 = Ph(e4);
        return t3 && t3.data.charAt(0) === Bh && t3.deleteData(0, 1), t3;
      })(t2);
      o2.remove(t2, true), s2 === d2 && a2 > 0 && e3.setStart(d2, a2 - 1), i3 === d2 && l2 > 0 && e3.setEnd(d2, l2 - 1), n3 && o2.isEmpty(n3) && kr(mn(n3)), r2.setRng(e3);
    }
  }, Ih = (e2, t2, n2 = true) => {
    const o2 = e2.dom, r2 = e2.selection;
    if (t2) Mh(e2, t2, n2);
    else if (!(t2 = jc(e2.getBody(), r2.getStart()))) for (; t2 = o2.get(Uc); ) Mh(e2, t2, false);
  }, Fh = (e2, t2) => (e2.appendChild(t2), t2), Uh = (e2, t2) => {
    var n2;
    const o2 = G(e2, (e3, t3) => Fh(e3, t3.cloneNode(false)), t2), r2 = null !== (n2 = o2.ownerDocument) && void 0 !== n2 ? n2 : document;
    return Fh(o2, r2.createTextNode(Bh));
  }, zh = (e2, t2, n2, o2) => {
    const a2 = e2.dom, i3 = e2.selection;
    let l2 = false;
    const d2 = e2.formatter.get(t2);
    if (!d2) return;
    const c2 = i3.getRng(), u2 = c2.startContainer, m2 = c2.startOffset;
    let f2 = u2;
    Ro(u2) && (m2 !== u2.data.length && (l2 = true), f2 = f2.parentNode);
    const g2 = [];
    let h2;
    for (; f2; ) {
      if (Oh(e2, f2, t2, n2, o2)) {
        h2 = f2;
        break;
      }
      f2.nextSibling && (l2 = true), g2.push(f2), f2 = f2.parentNode;
    }
    if (h2) if (l2) {
      const r2 = i3.getBookmark();
      c2.collapse(true);
      let s2 = nm(e2, c2, d2, true);
      s2 = Gm(s2), e2.formatter.remove(t2, n2, s2, o2), i3.moveToBookmark(r2);
    } else {
      const l3 = jc(e2.getBody(), h2), d3 = Lh(false).dom;
      ((e3, t3, n3) => {
        var o3, r2;
        const s2 = e3.dom, a3 = s2.getParent(n3, O(Nu, e3));
        a3 && s2.isEmpty(a3) ? null === (o3 = n3.parentNode) || void 0 === o3 || o3.replaceChild(t3, n3) : (((e4) => {
          const t4 = vr(e4, "br"), n4 = K(((e5) => {
            const t5 = [];
            let n5 = e5.dom;
            for (; n5; ) t5.push(mn(n5)), n5 = n5.lastChild;
            return t5;
          })(e4).slice(-1), rr);
          t4.length === n4.length && V(n4, to);
        })(mn(n3)), s2.isEmpty(n3) ? null === (r2 = n3.parentNode) || void 0 === r2 || r2.replaceChild(t3, n3) : s2.insertAfter(t3, n3));
      })(e2, d3, null != l3 ? l3 : h2);
      const c3 = ((e3, t3, n3, o3, a3, i4) => {
        const l4 = e3.formatter, d4 = e3.dom, c4 = K(ue(l4.get()), (e4) => e4 !== o3 && !Ue(e4, "removeformat")), u4 = ((e4, t4, n4) => Y(n4, (n5, o4) => {
          const r2 = ((e5, t5) => Iu(e5, t5, (e6) => {
            const t6 = (e7) => w(e7) || e7.length > 1 && "%" === e7.charAt(0);
            return H(["styles", "attributes"], (n6) => we(e6, n6).exists((e7) => {
              const n7 = p(e7) ? e7 : Ce(e7);
              return H(n7, t6);
            }));
          }))(e4, o4);
          return e4.formatter.matchNode(t4, o4, {}, r2) ? n5.concat([o4]) : n5;
        }, []))(e3, n3, c4);
        if (K(u4, (t4) => !((e4, t5, n4) => {
          const o4 = ["inline", "block", "selector", "attributes", "styles", "classes"], a4 = (e5) => ve(e5, (e6, t6) => H(o4, (e7) => e7 === t6));
          return Iu(e4, t5, (t6) => {
            const o5 = a4(t6);
            return Iu(e4, n4, (e5) => {
              const t7 = a4(e5);
              return ((e6, t8, n5 = s) => r(n5).eq(e6, t8))(o5, t7);
            });
          });
        })(e3, t4, o3)).length > 0) {
          const e4 = n3.cloneNode(false);
          return d4.add(t3, e4), l4.remove(o3, a3, e4, i4), d4.remove(e4), M.some(e4);
        }
        return M.none();
      })(e2, d3, h2, t2, n2, o2), u3 = Uh(g2.concat(c3.toArray()), d3);
      l3 && Mh(e2, l3, false), i3.setCursorLocation(u3, 1), a2.isEmpty(h2) && a2.remove(h2);
    }
  }, jh = (e2, t2) => {
    const n2 = e2.schema.getTextInlineElements();
    return xe(n2, Lt(t2)) && !zc(t2.dom) && !So(t2.dom);
  }, Hh = {}, $h = wo(["pre"]);
  Hh.pre || (Hh.pre = []), Hh.pre.push((e2) => {
    if (!e2.selection.getRng().collapsed) {
      const t2 = e2.selection.getSelectedBlocks(), n2 = K(K(t2, $h), /* @__PURE__ */ ((e3) => (t3) => {
        const n3 = t3.previousSibling;
        return $h(n3) && j(e3, n3);
      })(t2));
      V(n2, (e3) => {
        ((e4, t3) => {
          const n3 = mn(t3), o2 = Cn(n3).dom;
          to(n3), Zn(mn(e4), [cn("br", o2), cn("br", o2), ...Nn(n3)]);
        })(e3.previousSibling, e3);
      });
    }
  });
  const Vh = ["fontWeight", "fontStyle", "color", "fontSize", "fontFamily"], qh = (e2, t2) => {
    const n2 = e2.get(t2);
    return p(n2) ? Q(n2, (e3) => zu(e3) && "span" === e3.inline && ((e4) => f(e4.styles) && H(ue(e4.styles), (e5) => j(Vh, e5)))(e3)) : M.none();
  }, Wh = (e2, t2) => Mc(t2, ai.fromRangeStart(e2)).isNone(), Kh = (e2, t2) => false === Lc(t2, ai.fromRangeEnd(e2)).exists((e3) => !Po(e3.getNode()) || Lc(t2, e3).isSome()), Gh = (e2) => (t2) => jo(t2) && "false" !== e2.getContentEditableParent(t2), Yh = (e2) => K(e2.getSelectedBlocks(), Gh(e2.dom)), Xh = Tt.each, Qh = (e2) => yo(e2) && !Zc(e2) && !zc(e2) && !So(e2), Jh = (e2, t2) => {
    for (let n2 = e2; n2; n2 = n2[t2]) {
      if (Ro(n2) && We(n2.data)) return e2;
      if (yo(n2) && !Zc(n2)) return n2;
    }
    return e2;
  }, Zh = (e2, t2, n2) => {
    const o2 = nh(e2), r2 = yo(t2) && ku(t2), s2 = yo(n2) && ku(n2);
    if (r2 && s2) {
      const r3 = Jh(t2, "previousSibling"), s3 = Jh(n2, "nextSibling");
      if (o2.compare(r3, s3)) {
        for (let e3 = r3.nextSibling; e3 && e3 !== s3; ) {
          const t3 = e3;
          e3 = e3.nextSibling, r3.appendChild(t3);
        }
        return e2.dom.remove(s3), Tt.each(Tt.grep(s3.childNodes), (e3) => {
          r3.appendChild(e3);
        }), r3;
      }
    }
    return n2;
  }, eb = (e2, t2, n2, o2) => {
    var r2;
    if (o2 && false !== t2.merge_siblings) {
      const t3 = null !== (r2 = Zh(e2, Eu(o2), o2)) && void 0 !== r2 ? r2 : o2;
      Zh(e2, t3, Eu(t3, true));
    }
  }, tb = (e2, t2, n2) => {
    Xh(e2.childNodes, (e3) => {
      Qh(e3) && (t2(e3) && n2(e3), e3.hasChildNodes() && tb(e3, t2, n2));
    });
  }, nb = (e2, t2) => (n2) => !(!n2 || !Pu(e2, n2, t2)), ob = (e2, t2, n2) => (o2) => {
    e2.setStyle(o2, t2, n2), "" === o2.getAttribute("style") && o2.removeAttribute("style"), ((e3, t3) => {
      "SPAN" === t3.nodeName && 0 === e3.getAttribs(t3).length && e3.remove(t3, true);
    })(e2, o2);
  }, rb = Ti([{ keep: [] }, { rename: ["name"] }, { removed: [] }]), sb = /^(src|href|style)$/, ab = Tt.each, ib = Bu, lb = (e2, t2, n2) => e2.isChildOf(t2, n2) && t2 !== n2 && !e2.isBlock(n2), db = (e2, t2, n2) => {
    let o2 = t2[n2 ? "startContainer" : "endContainer"], r2 = t2[n2 ? "startOffset" : "endOffset"];
    if (yo(o2)) {
      const e3 = o2.childNodes.length - 1;
      !n2 && r2 && r2--, o2 = o2.childNodes[r2 > e3 ? e3 : r2];
    }
    return Ro(o2) && n2 && r2 >= o2.data.length && (o2 = new Zo(o2, e2.getBody()).next() || o2), Ro(o2) && !n2 && 0 === r2 && (o2 = new Zo(o2, e2.getBody()).prev() || o2), o2;
  }, cb = (e2, t2) => {
    const n2 = t2 ? "firstChild" : "lastChild", o2 = e2[n2];
    return ((e3) => /^(TR|TH|TD)$/.test(e3.nodeName))(e2) && o2 ? "TR" === e2.nodeName && o2[n2] || o2 : e2;
  }, ub = (e2, t2, n2, o2) => {
    var r2;
    const s2 = e2.create(n2, o2);
    return null === (r2 = t2.parentNode) || void 0 === r2 || r2.insertBefore(s2, t2), s2.appendChild(t2), s2;
  }, mb = (e2, t2, n2, o2, r2) => {
    const s2 = mn(t2), a2 = mn(e2.create(o2, r2)), i3 = n2 ? En(s2) : _n(s2);
    return Zn(a2, i3), n2 ? (Yn(s2, a2), Qn(a2, s2)) : (Xn(s2, a2), Jn(a2, s2)), a2.dom;
  }, fb = (e2, t2, n2) => x(e2) ? { name: t2, value: null } : { name: e2, value: Tu(t2, n2) }, gb = (e2, t2) => {
    "" === e2.getAttrib(t2, "style") && (t2.removeAttribute("style"), t2.removeAttribute("data-mce-style"));
  }, pb = (e2, t2, n2, o2, r2) => {
    let s2 = false;
    ab(n2.styles, (a2, i3) => {
      const { name: l2, value: d2 } = fb(i3, a2, o2), c2 = Du(d2, l2);
      (n2.remove_similar || h(d2) || !yo(r2) || ib(Pu(e2, r2, l2), c2)) && e2.setStyle(t2, l2, ""), s2 = true;
    }), s2 && gb(e2, t2);
  }, hb = (e2, t2, n2, o2, r2) => {
    const s2 = e2.dom, a2 = nh(e2);
    if (!t2.ceFalseOverride && o2 && "false" === s2.getContentEditableParent(o2)) return rb.keep();
    if (o2 && !Rh(s2, o2, t2) && !((e3, t3) => t3.links && "A" === e3.nodeName)(o2, t2)) return rb.keep();
    const i3 = o2, l2 = t2.preserve_attributes;
    if (zu(t2) && "all" === t2.remove && p(l2)) {
      const e3 = K(s2.getAttribs(i3), (e4) => j(l2, e4.name.toLowerCase()));
      if (s2.removeAllAttribs(i3), V(e3, (e4) => s2.setAttrib(i3, e4.name, e4.value)), e3.length > 0) return rb.rename("span");
    }
    if ("all" !== t2.remove) {
      pb(s2, i3, t2, n2, r2), ab(t2.attributes, (e4, o3) => {
        const { name: a3, value: l3 } = fb(o3, e4, n2);
        if (t2.remove_similar || h(l3) || !yo(r2) || ib(s2.getAttrib(r2, a3), l3)) {
          if ("class" === a3) {
            const e5 = s2.getAttrib(i3, a3);
            if (e5) {
              let t3 = "";
              if (V(e5.split(/\s+/), (e6) => {
                /mce\-\w+/.test(e6) && (t3 += (t3 ? " " : "") + e6);
              }), t3) return void s2.setAttrib(i3, a3, t3);
            }
          }
          if (sb.test(a3) && i3.removeAttribute("data-mce-" + a3), "style" === a3 && wo(["li"])(i3) && "none" === s2.getStyle(i3, "list-style-type")) return i3.removeAttribute(a3), void s2.setStyle(i3, "list-style-type", "none");
          "class" === a3 && i3.removeAttribute("className"), i3.removeAttribute(a3);
        }
      }), ab(t2.classes, (e4) => {
        e4 = Tu(e4, n2), yo(r2) && !s2.hasClass(r2, e4) || s2.removeClass(i3, e4);
      });
      const e3 = s2.getAttribs(i3);
      for (let t3 = 0; t3 < e3.length; t3++) {
        const n3 = e3[t3].nodeName;
        if (!a2.isAttributeInternal(n3)) return rb.keep();
      }
    }
    return "none" !== t2.remove ? (((e3, t3, n3) => {
      const o3 = t3.parentNode;
      let r3;
      const s3 = e3.dom, a3 = Ji(e3);
      Fu(n3) && o3 === s3.getRoot() && (n3.list_block && ib(t3, n3.list_block) || V(de(t3.childNodes), (t4) => {
        Ru(e3, a3, t4.nodeName.toLowerCase()) ? r3 ? r3.appendChild(t4) : (r3 = ub(s3, t4, a3), s3.setAttribs(r3, Zi(e3))) : r3 = null;
      })), ((e4) => Uu(e4) && zu(e4) && Bt(we(e4, "mixed"), true))(n3) && !ib(n3.inline, t3) || s3.remove(t3, true);
    })(e2, i3, t2), rb.removed()) : rb.keep();
  }, bb = (e2, t2, n2, o2, r2) => hb(e2, t2, n2, o2, r2).fold(P, (t3) => (e2.dom.rename(o2, t3), true), L), vb = (e2, t2, n2, o2) => hb(e2, t2, n2, o2, o2).fold(N(o2), (t3) => (e2.dom.createFragment().appendChild(o2), e2.dom.rename(o2, t3)), N(null)), yb = (e2, t2, n2, o2, r2) => {
    const s2 = e2.formatter.get(t2), a2 = s2[0], i3 = e2.dom, l2 = e2.selection, d2 = (o3) => {
      const i4 = ((e3, t3, n3, o4, r3) => {
        let s3;
        return t3.parentNode && V(Mu(e3.dom, t3.parentNode).reverse(), (t4) => {
          if (!s3 && yo(t4) && "_start" !== t4.id && "_end" !== t4.id) {
            const a3 = Oh(e3, t4, n3, o4, r3);
            a3 && false !== a3.split && (s3 = t4);
          }
        }), s3;
      })(e2, o3, t2, n2, r2);
      return ((e3, t3, n3, o4, r3, s3, a3, i5) => {
        var l3, d3;
        let c3, u3;
        const m3 = e3.dom;
        if (n3) {
          const s4 = n3.parentNode;
          for (let n4 = o4.parentNode; n4 && n4 !== s4; n4 = n4.parentNode) {
            let o5 = m3.clone(n4, false);
            for (let n5 = 0; n5 < t3.length && (o5 = vb(e3, t3[n5], i5, o5), null !== o5); n5++) ;
            o5 && (c3 && o5.appendChild(c3), u3 || (u3 = o5), c3 = o5);
          }
          a3.mixed && m3.isBlock(n3) || (o4 = null !== (l3 = m3.split(n3, o4)) && void 0 !== l3 ? l3 : o4), c3 && u3 && (null === (d3 = r3.parentNode) || void 0 === d3 || d3.insertBefore(c3, r3), u3.appendChild(r3), zu(a3) && eb(e3, a3, 0, c3));
        }
        return o4;
      })(e2, s2, i4, o3, o3, 0, a2, n2);
    }, c2 = (t3) => H(s2, (o3) => bb(e2, o3, n2, t3, t3)), u2 = (t3) => {
      const n3 = de(t3.childNodes), o3 = c2(t3) || H(s2, (e3) => Rh(i3, t3, e3)), r3 = t3.parentNode;
      if (!o3 && C(r3) && ju(a2) && c2(r3), a2.deep && n3.length) for (let e3 = 0; e3 < n3.length; e3++) u2(n3[e3]);
      V(["underline", "line-through", "overline"], (n4) => {
        yo(t3) && e2.dom.getStyle(t3, "text-decoration") === n4 && t3.parentNode && Lu(i3, t3.parentNode) === n4 && bb(e2, { deep: false, exact: true, inline: "span", styles: { textDecoration: n4 } }, void 0, t3);
      });
    }, m2 = (e3) => {
      const t3 = i3.get(e3 ? "_start" : "_end");
      if (t3) {
        let n3 = t3[e3 ? "firstChild" : "lastChild"];
        return ((e4) => Zc(e4) && yo(e4) && ("_start" === e4.id || "_end" === e4.id))(n3) && (n3 = n3[e3 ? "firstChild" : "lastChild"]), Ro(n3) && 0 === n3.data.length && (n3 = e3 ? t3.previousSibling || t3.nextSibling : t3.nextSibling || t3.previousSibling), i3.remove(t3, true), n3;
      }
      return null;
    }, f2 = (t3) => {
      let n3, o3, r3 = nm(e2, t3, s2, t3.collapsed);
      if (a2.split) {
        if (r3 = Gm(r3), n3 = db(e2, r3, true), o3 = db(e2, r3), n3 !== o3) {
          if (n3 = cb(n3, true), o3 = cb(o3, false), lb(i3, n3, o3)) {
            const e4 = M.from(n3.firstChild).getOr(n3);
            return d2(mb(i3, e4, true, "span", { id: "_start", "data-mce-type": "bookmark" })), void m2(true);
          }
          if (lb(i3, o3, n3)) {
            const e4 = M.from(o3.lastChild).getOr(o3);
            return d2(mb(i3, e4, false, "span", { id: "_end", "data-mce-type": "bookmark" })), void m2(false);
          }
          n3 = ub(i3, n3, "span", { id: "_start", "data-mce-type": "bookmark" }), o3 = ub(i3, o3, "span", { id: "_end", "data-mce-type": "bookmark" });
          const e3 = i3.createRng();
          e3.setStartAfter(n3), e3.setEndBefore(o3), om(i3, e3, (e4) => {
            V(e4, (e5) => {
              Zc(e5) || Zc(e5.parentNode) || d2(e5);
            });
          }), d2(n3), d2(o3), n3 = m2(true), o3 = m2();
        } else n3 = o3 = d2(n3);
        r3.startContainer = n3.parentNode ? n3.parentNode : n3, r3.startOffset = i3.nodeIndex(n3), r3.endContainer = o3.parentNode ? o3.parentNode : o3, r3.endOffset = i3.nodeIndex(o3) + 1;
      }
      om(i3, r3, (e3) => {
        V(e3, u2);
      });
    };
    if (o2) {
      if (wu(o2)) {
        const e3 = i3.createRng();
        e3.setStartBefore(o2), e3.setEndAfter(o2), f2(e3);
      } else f2(o2);
      fm(e2, t2, o2, n2);
    } else l2.isCollapsed() && zu(a2) && !mu(e2).length ? zh(e2, t2, n2, r2) : (Su(e2, () => yu(e2, f2), (o3) => zu(a2) && Th(e2, t2, n2, o3)), e2.nodeChanged()), ((e3, t3, n3) => {
      "removeformat" === t3 ? V(Yh(e3.selection), (t4) => {
        V(Vh, (n4) => e3.dom.setStyle(t4, n4, "")), gb(e3.dom, t4);
      }) : qh(e3.formatter, t3).each((t4) => {
        V(Yh(e3.selection), (o3) => pb(e3.dom, o3, t4, n3, null));
      });
    })(e2, t2, n2), fm(e2, t2, o2, n2);
  }, Cb = Tt.each, wb = Tt.each, xb = (e2, t2, n2, o2) => {
    if (wb(n2.styles, (n3, r2) => {
      e2.setStyle(t2, r2, Tu(n3, o2));
    }), n2.styles) {
      const n3 = e2.getAttrib(t2, "style");
      n3 && e2.setAttrib(t2, "data-mce-style", n3);
    }
  }, kb = (e2, t2, n2, o2) => {
    const r2 = e2.formatter.get(t2), s2 = r2[0], a2 = !o2 && e2.selection.isCollapsed(), i3 = e2.dom, l2 = e2.selection, d2 = (e3, t3 = s2) => {
      w(t3.onformat) && t3.onformat(e3, t3, n2, o2), xb(i3, e3, t3, n2), wb(t3.attributes, (t4, o3) => {
        i3.setAttrib(e3, o3, Tu(t4, n2));
      }), wb(t3.classes, (t4) => {
        const o3 = Tu(t4, n2);
        i3.hasClass(e3, o3) || i3.addClass(e3, o3);
      });
    }, c2 = (e3, t3) => {
      let n3 = false;
      return wb(e3, (e4) => !(!Uu(e4) || ("false" !== i3.getContentEditable(t3) || e4.ceFalseOverride) && (!C(e4.collapsed) || e4.collapsed === a2) && i3.is(t3, e4.selector) && !zc(t3) && (d2(t3, e4), n3 = true, 1))), n3;
    }, u2 = (e3) => {
      if (m(e3)) {
        const t3 = i3.create(e3);
        return d2(t3), t3;
      }
      return null;
    }, f2 = (o3, a3, i4) => {
      const l3 = [];
      let m2 = true;
      const f3 = s2.inline || s2.block, g3 = u2(f3);
      om(o3, a3, (a4) => {
        let u3;
        const p2 = (a5) => {
          let h2 = false, b2 = m2, v2 = false;
          const y2 = a5.nodeName.toLowerCase(), w2 = a5.parentNode, x2 = w2.nodeName.toLowerCase(), k2 = o3.getContentEditable(a5);
          C(k2) && (b2 = m2, m2 = "true" === k2, h2 = true, v2 = Ou(e2, a5));
          const S2 = m2 && !h2;
          if (Po(a5) && !((e3, t3, n3, o4) => {
            if (Il(e3) && zu(t3) && n3.parentNode) {
              const t4 = Ds(e3.schema), r3 = xh(mn(n3), (e4) => zc(e4.dom));
              return ke(t4, o4) && ss(mn(n3.parentNode), false) && !r3;
            }
            return false;
          })(e2, s2, a5, x2)) return u3 = null, void (Fu(s2) && o3.remove(a5));
          if (((o4) => ((e3) => Fu(e3) && true === e3.wrapper)(s2) && Oh(e2, o4, t2, n2))(a5)) u3 = null;
          else {
            if (((t3, n3, o4) => {
              const r3 = ((e3) => Fu(e3) && true !== e3.wrapper)(s2) && Nu(e2, t3) && Ru(e2, n3, f3);
              return o4 && r3;
            })(y2, x2, S2)) {
              const e3 = o3.rename(a5, f3);
              return d2(e3), l3.push(e3), void (u3 = null);
            }
            if (Uu(s2)) {
              let e3 = c2(r2, a5);
              if (!e3 && C(w2) && ju(s2) && (e3 = c2(r2, w2)), !zu(s2) || e3) return void (u3 = null);
            }
            C(g3) && ((t3, n3, r3, a6) => {
              const l4 = t3.nodeName.toLowerCase(), d3 = Ru(e2, f3, l4) && Ru(e2, n3, f3), c3 = !i4 && Ro(t3) && Er(t3.data), u4 = zc(t3), m3 = !zu(s2) || !o3.isBlock(t3);
              return (r3 || a6) && d3 && !c3 && !u4 && m3;
            })(a5, x2, S2, v2) ? (u3 || (u3 = o3.clone(g3, false), w2.insertBefore(u3, a5), l3.push(u3)), v2 && h2 && (m2 = b2), u3.appendChild(a5)) : (u3 = null, V(de(a5.childNodes), p2), h2 && (m2 = b2), u3 = null);
          }
        };
        V(a4, p2);
      }), true === s2.links && V(l3, (e3) => {
        const t3 = (e4) => {
          "A" === e4.nodeName && d2(e4, s2), V(de(e4.childNodes), t3);
        };
        t3(e3);
      }), V(l3, (a4) => {
        const i5 = ((e3) => {
          let t3 = 0;
          return V(e3.childNodes, (e4) => {
            ((e5) => C(e5) && Ro(e5) && 0 === e5.length)(e4) || Zc(e4) || t3++;
          }), t3;
        })(a4);
        !(l3.length > 1) && o3.isBlock(a4) || 0 !== i5 ? (zu(s2) || Fu(s2) && s2.wrapper) && (s2.exact || 1 !== i5 || (a4 = ((e3) => {
          const t3 = Q(e3.childNodes, xu).filter((e4) => "false" !== o3.getContentEditable(e4) && Rh(o3, e4, s2));
          return t3.map((t4) => {
            const n3 = o3.clone(t4, false);
            return d2(n3), o3.replace(n3, e3, true), o3.remove(t4, true), n3;
          }).getOr(e3);
        })(a4)), ((e3, t3, n3, o4) => {
          Cb(t3, (t4) => {
            zu(t4) && Cb(e3.dom.select(t4.inline, o4), (o5) => {
              Qh(o5) && bb(e3, t4, n3, o5, t4.exact ? o5 : null);
            }), ((e4, t5, n4) => {
              if (t5.clear_child_styles) {
                const o5 = t5.links ? "*:not(a)" : "*";
                Xh(e4.select(o5, n4), (n5) => {
                  Qh(n5) && ku(n5) && Xh(t5.styles, (t6, o6) => {
                    e4.setStyle(n5, o6, "");
                  });
                });
              }
            })(e3.dom, t4, o4);
          });
        })(e2, r2, n2, a4), ((e3, t3, n3, o4, r3) => {
          const s3 = r3.parentNode;
          Oh(e3, s3, n3, o4) && bb(e3, t3, o4, r3) || t3.merge_with_parents && s3 && e3.dom.getParent(s3, (s4) => !!Oh(e3, s4, n3, o4) && (bb(e3, t3, o4, r3), true));
        })(e2, s2, t2, n2, a4), ((e3, t3, n3, o4) => {
          if (t3.styles && t3.styles.backgroundColor) {
            const r3 = nb(e3, "fontSize");
            tb(o4, (e4) => r3(e4) && ku(e4), ob(e3, "backgroundColor", Tu(t3.styles.backgroundColor, n3)));
          }
        })(o3, s2, n2, a4), ((e3, t3, n3, o4) => {
          const r3 = (t4) => {
            if (yo(t4) && yo(t4.parentNode) && ku(t4)) {
              const n4 = Lu(e3, t4.parentNode);
              e3.getStyle(t4, "color") && n4 ? e3.setStyle(t4, "text-decoration", n4) : e3.getStyle(t4, "text-decoration") === n4 && e3.setStyle(t4, "text-decoration", null);
            }
          };
          t3.styles && (t3.styles.color || t3.styles.textDecoration) && (Tt.walk(o4, r3, "childNodes"), r3(o4));
        })(o3, s2, 0, a4), ((e3, t3, n3, o4) => {
          if (zu(t3) && ("sub" === t3.inline || "sup" === t3.inline)) {
            const n4 = nb(e3, "fontSize");
            tb(o4, (e4) => n4(e4) && ku(e4), ob(e3, "fontSize", ""));
            const r3 = K(e3.select("sup" === t3.inline ? "sub" : "sup", o4), ku);
            e3.remove(r3, true);
          }
        })(o3, s2, 0, a4), eb(e2, s2, 0, a4)) : o3.remove(a4, true);
      });
    }, g2 = wu(o2) ? o2 : l2.getNode();
    if ("false" === i3.getContentEditable(g2) && !Ou(e2, g2)) return c2(r2, o2 = g2), void mm(e2, t2, o2, n2);
    if (s2) {
      if (o2) if (wu(o2)) {
        if (!c2(r2, o2)) {
          const t3 = i3.createRng();
          t3.setStartBefore(o2), t3.setEndAfter(o2), f2(i3, nm(e2, t3, r2), true);
        }
      } else f2(i3, o2, true);
      else a2 && zu(s2) && !mu(e2).length ? ((e3, t3, n3) => {
        let o3;
        const r3 = e3.selection, s3 = e3.formatter.get(t3);
        if (!s3) return;
        const a3 = r3.getRng();
        let i4 = a3.startOffset;
        const l3 = a3.startContainer.nodeValue;
        o3 = jc(e3.getBody(), r3.getStart());
        const d3 = /[^\s\u00a0\u00ad\u200b\ufeff]/;
        if (l3 && i4 > 0 && i4 < l3.length && d3.test(l3.charAt(i4)) && d3.test(l3.charAt(i4 - 1))) {
          const o4 = r3.getBookmark();
          a3.collapse(true);
          let i5 = nm(e3, a3, s3);
          i5 = Gm(i5), e3.formatter.apply(t3, n3, i5), r3.moveToBookmark(o4);
        } else {
          let s4 = o3 ? Ph(o3) : null;
          o3 && (null == s4 ? void 0 : s4.data) === Bh || (c3 = e3.getDoc(), u3 = Lh(true).dom, o3 = c3.importNode(u3, true), s4 = o3.firstChild, a3.insertNode(o3), i4 = 1), e3.formatter.apply(t3, n3, o3), r3.setCursorLocation(s4, i4);
        }
        var c3, u3;
      })(e2, t2, n2) : (l2.setRng(uh(l2.getRng())), Su(e2, () => {
        yu(e2, (t3, n3) => {
          const o3 = n3 ? t3 : nm(e2, t3, r2);
          f2(i3, o3, false);
        });
      }, L), e2.nodeChanged()), qh(e2.formatter, t2).each((t3) => {
        V(((e3) => K(((e4) => {
          const t4 = e4.getSelectedBlocks(), n3 = e4.getRng();
          if (e4.isCollapsed()) return [];
          if (1 === t4.length) return Wh(n3, t4[0]) && Kh(n3, t4[0]) ? t4 : [];
          {
            const e5 = ie(t4).filter((e6) => Wh(n3, e6)).toArray(), o3 = le(t4).filter((e6) => Kh(n3, e6)).toArray(), r3 = t4.slice(1, -1);
            return e5.concat(r3).concat(o3);
          }
        })(e3), Gh(e3.dom)))(e2.selection), (e3) => xb(i3, e3, t3, n2));
      });
      ((e3, t3) => {
        xe(Hh, e3) && V(Hh[e3], (e4) => {
          e4(t3);
        });
      })(t2, e2);
    }
    mm(e2, t2, o2, n2);
  }, Sb = (e2) => xe(e2, "vars"), _b = (e2) => e2.selection.getStart(), Eb = (e2, t2, n2, o2, r2) => X(t2, (t3) => {
    const s2 = e2.formatter.matchNode(t3, n2, null != r2 ? r2 : {}, o2);
    return !v(s2);
  }, (t3) => !!Eh(e2, t3, n2) || !o2 && C(e2.formatter.matchNode(t3, n2, r2, true))), Nb = (e2, t2) => {
    const n2 = null != t2 ? t2 : _b(e2);
    return K(Mu(e2.dom, n2), (e3) => yo(e3) && !So(e3));
  }, Rb = (e2, t2, n2) => {
    const o2 = Nb(e2, t2);
    fe(n2, (n3, r2) => {
      const s2 = (n4) => {
        const s3 = Eb(e2, o2, r2, n4.similar, Sb(n4) ? n4.vars : void 0), a2 = s3.isSome();
        if (n4.state.get() !== a2) {
          n4.state.set(a2);
          const e3 = s3.getOr(t2);
          Sb(n4) ? n4.callback(a2, { node: e3, format: r2, parents: o2 }) : V(n4.callbacks, (t3) => t3(a2, { node: e3, format: r2, parents: o2 }));
        }
      };
      V([n3.withSimilar, n3.withoutSimilar], s2), V(n3.withVars, s2);
    });
  };
  function Ab(e2) {
    return Ab = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(e3) {
      return typeof e3;
    } : function(e3) {
      return e3 && "function" == typeof Symbol && e3.constructor === Symbol && e3 !== Symbol.prototype ? "symbol" : typeof e3;
    }, Ab(e2);
  }
  function Ob(e2, t2) {
    return Ob = Object.setPrototypeOf || function(e3, t3) {
      return e3.__proto__ = t3, e3;
    }, Ob(e2, t2);
  }
  function Tb() {
    if ("undefined" == typeof Reflect || !Reflect.construct) return false;
    if (Reflect.construct.sham) return false;
    if ("function" == typeof Proxy) return true;
    try {
      return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
      })), true;
    } catch (e2) {
      return false;
    }
  }
  function Bb(e2, t2, n2) {
    return Bb = Tb() ? Reflect.construct : function(e3, t3, n3) {
      var o2 = [null];
      o2.push.apply(o2, t3);
      var r2 = new (Function.bind.apply(e3, o2))();
      return n3 && Ob(r2, n3.prototype), r2;
    }, Bb.apply(null, arguments);
  }
  function Db(e2) {
    return function(e3) {
      if (Array.isArray(e3)) return Pb(e3);
    }(e2) || function(e3) {
      if ("undefined" != typeof Symbol && null != e3[Symbol.iterator] || null != e3["@@iterator"]) return Array.from(e3);
    }(e2) || function(e3, t2) {
      if (e3) {
        if ("string" == typeof e3) return Pb(e3, t2);
        var n2 = Object.prototype.toString.call(e3).slice(8, -1);
        return "Object" === n2 && e3.constructor && (n2 = e3.constructor.name), "Map" === n2 || "Set" === n2 ? Array.from(e3) : "Arguments" === n2 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n2) ? Pb(e3, t2) : void 0;
      }
    }(e2) || function() {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }();
  }
  function Pb(e2, t2) {
    (null == t2 || t2 > e2.length) && (t2 = e2.length);
    for (var n2 = 0, o2 = new Array(t2); n2 < t2; n2++) o2[n2] = e2[n2];
    return o2;
  }
  var Lb = Object.hasOwnProperty, Mb = Object.setPrototypeOf, Ib = Object.isFrozen, Fb = Object.getPrototypeOf, Ub = Object.getOwnPropertyDescriptor, zb = Object.freeze, jb = Object.seal, Hb = Object.create, $b = "undefined" != typeof Reflect && Reflect, Vb = $b.apply, qb = $b.construct;
  Vb || (Vb = function(e2, t2, n2) {
    return e2.apply(t2, n2);
  }), zb || (zb = function(e2) {
    return e2;
  }), jb || (jb = function(e2) {
    return e2;
  }), qb || (qb = function(e2, t2) {
    return Bb(e2, Db(t2));
  });
  var Wb, Kb = ov(Array.prototype.forEach), Gb = ov(Array.prototype.pop), Yb = ov(Array.prototype.push), Xb = ov(String.prototype.toLowerCase), Qb = ov(String.prototype.match), Jb = ov(String.prototype.replace), Zb = ov(String.prototype.indexOf), ev = ov(String.prototype.trim), tv = ov(RegExp.prototype.test), nv = (Wb = TypeError, function() {
    for (var e2 = arguments.length, t2 = new Array(e2), n2 = 0; n2 < e2; n2++) t2[n2] = arguments[n2];
    return qb(Wb, t2);
  });
  function ov(e2) {
    return function(t2) {
      for (var n2 = arguments.length, o2 = new Array(n2 > 1 ? n2 - 1 : 0), r2 = 1; r2 < n2; r2++) o2[r2 - 1] = arguments[r2];
      return Vb(e2, t2, o2);
    };
  }
  function rv(e2, t2) {
    Mb && Mb(e2, null);
    for (var n2 = t2.length; n2--; ) {
      var o2 = t2[n2];
      if ("string" == typeof o2) {
        var r2 = Xb(o2);
        r2 !== o2 && (Ib(t2) || (t2[n2] = r2), o2 = r2);
      }
      e2[o2] = true;
    }
    return e2;
  }
  function sv(e2) {
    var t2, n2 = Hb(null);
    for (t2 in e2) Vb(Lb, e2, [t2]) && (n2[t2] = e2[t2]);
    return n2;
  }
  function av(e2, t2) {
    for (; null !== e2; ) {
      var n2 = Ub(e2, t2);
      if (n2) {
        if (n2.get) return ov(n2.get);
        if ("function" == typeof n2.value) return ov(n2.value);
      }
      e2 = Fb(e2);
    }
    return function(e3) {
      return console.warn("fallback value for", e3), null;
    };
  }
  var iv = zb(["a", "abbr", "acronym", "address", "area", "article", "aside", "audio", "b", "bdi", "bdo", "big", "blink", "blockquote", "body", "br", "button", "canvas", "caption", "center", "cite", "code", "col", "colgroup", "content", "data", "datalist", "dd", "decorator", "del", "details", "dfn", "dialog", "dir", "div", "dl", "dt", "element", "em", "fieldset", "figcaption", "figure", "font", "footer", "form", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hgroup", "hr", "html", "i", "img", "input", "ins", "kbd", "label", "legend", "li", "main", "map", "mark", "marquee", "menu", "menuitem", "meter", "nav", "nobr", "ol", "optgroup", "option", "output", "p", "picture", "pre", "progress", "q", "rp", "rt", "ruby", "s", "samp", "section", "select", "shadow", "small", "source", "spacer", "span", "strike", "strong", "style", "sub", "summary", "sup", "table", "tbody", "td", "template", "textarea", "tfoot", "th", "thead", "time", "tr", "track", "tt", "u", "ul", "var", "video", "wbr"]), lv = zb(["svg", "a", "altglyph", "altglyphdef", "altglyphitem", "animatecolor", "animatemotion", "animatetransform", "circle", "clippath", "defs", "desc", "ellipse", "filter", "font", "g", "glyph", "glyphref", "hkern", "image", "line", "lineargradient", "marker", "mask", "metadata", "mpath", "path", "pattern", "polygon", "polyline", "radialgradient", "rect", "stop", "style", "switch", "symbol", "text", "textpath", "title", "tref", "tspan", "view", "vkern"]), dv = zb(["feBlend", "feColorMatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence"]), cv = zb(["animate", "color-profile", "cursor", "discard", "fedropshadow", "font-face", "font-face-format", "font-face-name", "font-face-src", "font-face-uri", "foreignobject", "hatch", "hatchpath", "mesh", "meshgradient", "meshpatch", "meshrow", "missing-glyph", "script", "set", "solidcolor", "unknown", "use"]), uv = zb(["math", "menclose", "merror", "mfenced", "mfrac", "mglyph", "mi", "mlabeledtr", "mmultiscripts", "mn", "mo", "mover", "mpadded", "mphantom", "mroot", "mrow", "ms", "mspace", "msqrt", "mstyle", "msub", "msup", "msubsup", "mtable", "mtd", "mtext", "mtr", "munder", "munderover"]), mv = zb(["maction", "maligngroup", "malignmark", "mlongdiv", "mscarries", "mscarry", "msgroup", "mstack", "msline", "msrow", "semantics", "annotation", "annotation-xml", "mprescripts", "none"]), fv = zb(["#text"]), gv = zb(["accept", "action", "align", "alt", "autocapitalize", "autocomplete", "autopictureinpicture", "autoplay", "background", "bgcolor", "border", "capture", "cellpadding", "cellspacing", "checked", "cite", "class", "clear", "color", "cols", "colspan", "controls", "controlslist", "coords", "crossorigin", "datetime", "decoding", "default", "dir", "disabled", "disablepictureinpicture", "disableremoteplayback", "download", "draggable", "enctype", "enterkeyhint", "face", "for", "headers", "height", "hidden", "high", "href", "hreflang", "id", "inputmode", "integrity", "ismap", "kind", "label", "lang", "list", "loading", "loop", "low", "max", "maxlength", "media", "method", "min", "minlength", "multiple", "muted", "name", "nonce", "noshade", "novalidate", "nowrap", "open", "optimum", "pattern", "placeholder", "playsinline", "poster", "preload", "pubdate", "radiogroup", "readonly", "rel", "required", "rev", "reversed", "role", "rows", "rowspan", "spellcheck", "scope", "selected", "shape", "size", "sizes", "span", "srclang", "start", "src", "srcset", "step", "style", "summary", "tabindex", "title", "translate", "type", "usemap", "valign", "value", "width", "xmlns", "slot"]), pv = zb(["accent-height", "accumulate", "additive", "alignment-baseline", "ascent", "attributename", "attributetype", "azimuth", "basefrequency", "baseline-shift", "begin", "bias", "by", "class", "clip", "clippathunits", "clip-path", "clip-rule", "color", "color-interpolation", "color-interpolation-filters", "color-profile", "color-rendering", "cx", "cy", "d", "dx", "dy", "diffuseconstant", "direction", "display", "divisor", "dur", "edgemode", "elevation", "end", "fill", "fill-opacity", "fill-rule", "filter", "filterunits", "flood-color", "flood-opacity", "font-family", "font-size", "font-size-adjust", "font-stretch", "font-style", "font-variant", "font-weight", "fx", "fy", "g1", "g2", "glyph-name", "glyphref", "gradientunits", "gradienttransform", "height", "href", "id", "image-rendering", "in", "in2", "k", "k1", "k2", "k3", "k4", "kerning", "keypoints", "keysplines", "keytimes", "lang", "lengthadjust", "letter-spacing", "kernelmatrix", "kernelunitlength", "lighting-color", "local", "marker-end", "marker-mid", "marker-start", "markerheight", "markerunits", "markerwidth", "maskcontentunits", "maskunits", "max", "mask", "media", "method", "mode", "min", "name", "numoctaves", "offset", "operator", "opacity", "order", "orient", "orientation", "origin", "overflow", "paint-order", "path", "pathlength", "patterncontentunits", "patterntransform", "patternunits", "points", "preservealpha", "preserveaspectratio", "primitiveunits", "r", "rx", "ry", "radius", "refx", "refy", "repeatcount", "repeatdur", "restart", "result", "rotate", "scale", "seed", "shape-rendering", "specularconstant", "specularexponent", "spreadmethod", "startoffset", "stddeviation", "stitchtiles", "stop-color", "stop-opacity", "stroke-dasharray", "stroke-dashoffset", "stroke-linecap", "stroke-linejoin", "stroke-miterlimit", "stroke-opacity", "stroke", "stroke-width", "style", "surfacescale", "systemlanguage", "tabindex", "targetx", "targety", "transform", "transform-origin", "text-anchor", "text-decoration", "text-rendering", "textlength", "type", "u1", "u2", "unicode", "values", "viewbox", "visibility", "version", "vert-adv-y", "vert-origin-x", "vert-origin-y", "width", "word-spacing", "wrap", "writing-mode", "xchannelselector", "ychannelselector", "x", "x1", "x2", "xmlns", "y", "y1", "y2", "z", "zoomandpan"]), hv = zb(["accent", "accentunder", "align", "bevelled", "close", "columnsalign", "columnlines", "columnspan", "denomalign", "depth", "dir", "display", "displaystyle", "encoding", "fence", "frame", "height", "href", "id", "largeop", "length", "linethickness", "lspace", "lquote", "mathbackground", "mathcolor", "mathsize", "mathvariant", "maxsize", "minsize", "movablelimits", "notation", "numalign", "open", "rowalign", "rowlines", "rowspacing", "rowspan", "rspace", "rquote", "scriptlevel", "scriptminsize", "scriptsizemultiplier", "selection", "separator", "separators", "stretchy", "subscriptshift", "supscriptshift", "symmetric", "voffset", "width", "xmlns"]), bv = zb(["xlink:href", "xml:id", "xlink:title", "xml:space", "xmlns:xlink"]), vv = jb(/\{\{[\w\W]*|[\w\W]*\}\}/gm), yv = jb(/<%[\w\W]*|[\w\W]*%>/gm), Cv = jb(/^data-[\-\w.\u00B7-\uFFFF]/), wv = jb(/^aria-[\-\w]+$/), xv = jb(/^(?:(?:(?:f|ht)tps?|mailto|tel|callto|cid|xmpp):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i), kv = jb(/^(?:\w+script|data):/i), Sv = jb(/[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g), _v = jb(/^html$/i), Ev = function() {
    return "undefined" == typeof window ? null : window;
  }, Nv = function(e2, t2) {
    if ("object" !== Ab(e2) || "function" != typeof e2.createPolicy) return null;
    var n2 = null, o2 = "data-tt-policy-suffix";
    t2.currentScript && t2.currentScript.hasAttribute(o2) && (n2 = t2.currentScript.getAttribute(o2));
    var r2 = "dompurify" + (n2 ? "#" + n2 : "");
    try {
      return e2.createPolicy(r2, { createHTML: function(e3) {
        return e3;
      } });
    } catch (e3) {
      return console.warn("TrustedTypes policy " + r2 + " could not be created."), null;
    }
  }, Rv = function e2() {
    var t2 = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : Ev(), n2 = function(t3) {
      return e2(t3);
    };
    if (n2.version = "2.3.8", n2.removed = [], !t2 || !t2.document || 9 !== t2.document.nodeType) return n2.isSupported = false, n2;
    var o2 = t2.document, r2 = t2.document, s2 = t2.DocumentFragment, a2 = t2.HTMLTemplateElement, i3 = t2.Node, l2 = t2.Element, d2 = t2.NodeFilter, c2 = t2.NamedNodeMap, u2 = void 0 === c2 ? t2.NamedNodeMap || t2.MozNamedAttrMap : c2, m2 = t2.HTMLFormElement, f2 = t2.DOMParser, g2 = t2.trustedTypes, p2 = l2.prototype, h2 = av(p2, "cloneNode"), b2 = av(p2, "nextSibling"), v2 = av(p2, "childNodes"), y2 = av(p2, "parentNode");
    if ("function" == typeof a2) {
      var C2 = r2.createElement("template");
      C2.content && C2.content.ownerDocument && (r2 = C2.content.ownerDocument);
    }
    var w2 = Nv(g2, o2), x2 = w2 ? w2.createHTML("") : "", k2 = r2, S2 = k2.implementation, _2 = k2.createNodeIterator, E2 = k2.createDocumentFragment, N2 = k2.getElementsByTagName, R2 = o2.importNode, A2 = {};
    try {
      A2 = sv(r2).documentMode ? r2.documentMode : {};
    } catch (e3) {
    }
    var O2 = {};
    n2.isSupported = "function" == typeof y2 && S2 && void 0 !== S2.createHTMLDocument && 9 !== A2;
    var T2, B2, D2 = vv, P2 = yv, L2 = Cv, M2 = wv, I2 = kv, F2 = Sv, U2 = xv, z2 = null, j2 = rv({}, [].concat(Db(iv), Db(lv), Db(dv), Db(uv), Db(fv))), H2 = null, $3 = rv({}, [].concat(Db(gv), Db(pv), Db(hv), Db(bv))), V2 = Object.seal(Object.create(null, { tagNameCheck: { writable: true, configurable: false, enumerable: true, value: null }, attributeNameCheck: { writable: true, configurable: false, enumerable: true, value: null }, allowCustomizedBuiltInElements: { writable: true, configurable: false, enumerable: true, value: false } })), q2 = null, W2 = null, K2 = true, G2 = true, Y2 = false, X2 = false, Q2 = false, J2 = false, Z2 = false, ee2 = false, te2 = false, ne2 = false, oe2 = true, re2 = true, se2 = false, ae2 = {}, ie2 = null, le2 = rv({}, ["annotation-xml", "audio", "colgroup", "desc", "foreignobject", "head", "iframe", "math", "mi", "mn", "mo", "ms", "mtext", "noembed", "noframes", "noscript", "plaintext", "script", "style", "svg", "template", "thead", "title", "video", "xmp"]), de2 = null, ce2 = rv({}, ["audio", "video", "img", "source", "image", "track"]), ue2 = null, me2 = rv({}, ["alt", "class", "for", "id", "label", "name", "pattern", "placeholder", "role", "summary", "title", "value", "style", "xmlns"]), fe2 = "http://www.w3.org/1998/Math/MathML", ge2 = "http://www.w3.org/2000/svg", pe2 = "http://www.w3.org/1999/xhtml", he2 = pe2, be2 = false, ve2 = ["application/xhtml+xml", "text/html"], ye2 = "text/html", Ce2 = null, we2 = r2.createElement("form"), xe2 = function(e3) {
      return e3 instanceof RegExp || e3 instanceof Function;
    }, ke2 = function(e3) {
      Ce2 && Ce2 === e3 || (e3 && "object" === Ab(e3) || (e3 = {}), e3 = sv(e3), z2 = "ALLOWED_TAGS" in e3 ? rv({}, e3.ALLOWED_TAGS) : j2, H2 = "ALLOWED_ATTR" in e3 ? rv({}, e3.ALLOWED_ATTR) : $3, ue2 = "ADD_URI_SAFE_ATTR" in e3 ? rv(sv(me2), e3.ADD_URI_SAFE_ATTR) : me2, de2 = "ADD_DATA_URI_TAGS" in e3 ? rv(sv(ce2), e3.ADD_DATA_URI_TAGS) : ce2, ie2 = "FORBID_CONTENTS" in e3 ? rv({}, e3.FORBID_CONTENTS) : le2, q2 = "FORBID_TAGS" in e3 ? rv({}, e3.FORBID_TAGS) : {}, W2 = "FORBID_ATTR" in e3 ? rv({}, e3.FORBID_ATTR) : {}, ae2 = "USE_PROFILES" in e3 && e3.USE_PROFILES, K2 = false !== e3.ALLOW_ARIA_ATTR, G2 = false !== e3.ALLOW_DATA_ATTR, Y2 = e3.ALLOW_UNKNOWN_PROTOCOLS || false, X2 = e3.SAFE_FOR_TEMPLATES || false, Q2 = e3.WHOLE_DOCUMENT || false, ee2 = e3.RETURN_DOM || false, te2 = e3.RETURN_DOM_FRAGMENT || false, ne2 = e3.RETURN_TRUSTED_TYPE || false, Z2 = e3.FORCE_BODY || false, oe2 = false !== e3.SANITIZE_DOM, re2 = false !== e3.KEEP_CONTENT, se2 = e3.IN_PLACE || false, U2 = e3.ALLOWED_URI_REGEXP || U2, he2 = e3.NAMESPACE || pe2, e3.CUSTOM_ELEMENT_HANDLING && xe2(e3.CUSTOM_ELEMENT_HANDLING.tagNameCheck) && (V2.tagNameCheck = e3.CUSTOM_ELEMENT_HANDLING.tagNameCheck), e3.CUSTOM_ELEMENT_HANDLING && xe2(e3.CUSTOM_ELEMENT_HANDLING.attributeNameCheck) && (V2.attributeNameCheck = e3.CUSTOM_ELEMENT_HANDLING.attributeNameCheck), e3.CUSTOM_ELEMENT_HANDLING && "boolean" == typeof e3.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements && (V2.allowCustomizedBuiltInElements = e3.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements), T2 = T2 = -1 === ve2.indexOf(e3.PARSER_MEDIA_TYPE) ? ye2 : e3.PARSER_MEDIA_TYPE, B2 = "application/xhtml+xml" === T2 ? function(e4) {
        return e4;
      } : Xb, X2 && (G2 = false), te2 && (ee2 = true), ae2 && (z2 = rv({}, Db(fv)), H2 = [], true === ae2.html && (rv(z2, iv), rv(H2, gv)), true === ae2.svg && (rv(z2, lv), rv(H2, pv), rv(H2, bv)), true === ae2.svgFilters && (rv(z2, dv), rv(H2, pv), rv(H2, bv)), true === ae2.mathMl && (rv(z2, uv), rv(H2, hv), rv(H2, bv))), e3.ADD_TAGS && (z2 === j2 && (z2 = sv(z2)), rv(z2, e3.ADD_TAGS)), e3.ADD_ATTR && (H2 === $3 && (H2 = sv(H2)), rv(H2, e3.ADD_ATTR)), e3.ADD_URI_SAFE_ATTR && rv(ue2, e3.ADD_URI_SAFE_ATTR), e3.FORBID_CONTENTS && (ie2 === le2 && (ie2 = sv(ie2)), rv(ie2, e3.FORBID_CONTENTS)), re2 && (z2["#text"] = true), Q2 && rv(z2, ["html", "head", "body"]), z2.table && (rv(z2, ["tbody"]), delete q2.tbody), zb && zb(e3), Ce2 = e3);
    }, Se2 = rv({}, ["mi", "mo", "mn", "ms", "mtext"]), _e2 = rv({}, ["foreignobject", "desc", "title", "annotation-xml"]), Ee2 = rv({}, ["title", "style", "font", "a", "script"]), Ne2 = rv({}, lv);
    rv(Ne2, dv), rv(Ne2, cv);
    var Re2 = rv({}, uv);
    rv(Re2, mv);
    var Ae2 = function(e3) {
      var t3 = y2(e3);
      t3 && t3.tagName || (t3 = { namespaceURI: pe2, tagName: "template" });
      var n3 = Xb(e3.tagName), o3 = Xb(t3.tagName);
      return e3.namespaceURI === ge2 ? t3.namespaceURI === pe2 ? "svg" === n3 : t3.namespaceURI === fe2 ? "svg" === n3 && ("annotation-xml" === o3 || Se2[o3]) : Boolean(Ne2[n3]) : e3.namespaceURI === fe2 ? t3.namespaceURI === pe2 ? "math" === n3 : t3.namespaceURI === ge2 ? "math" === n3 && _e2[o3] : Boolean(Re2[n3]) : e3.namespaceURI === pe2 && !(t3.namespaceURI === ge2 && !_e2[o3]) && !(t3.namespaceURI === fe2 && !Se2[o3]) && !Re2[n3] && (Ee2[n3] || !Ne2[n3]);
    }, Oe2 = function(e3) {
      Yb(n2.removed, { element: e3 });
      try {
        e3.parentNode.removeChild(e3);
      } catch (t3) {
        try {
          e3.outerHTML = x2;
        } catch (t4) {
          e3.remove();
        }
      }
    }, Te2 = function(e3, t3) {
      try {
        Yb(n2.removed, { attribute: t3.getAttributeNode(e3), from: t3 });
      } catch (e4) {
        Yb(n2.removed, { attribute: null, from: t3 });
      }
      if (t3.removeAttribute(e3), "is" === e3 && !H2[e3]) if (ee2 || te2) try {
        Oe2(t3);
      } catch (e4) {
      }
      else try {
        t3.setAttribute(e3, "");
      } catch (e4) {
      }
    }, Be2 = function(e3) {
      var t3, n3;
      if (Z2) e3 = "<remove></remove>" + e3;
      else {
        var o3 = Qb(e3, /^[\r\n\t ]+/);
        n3 = o3 && o3[0];
      }
      "application/xhtml+xml" === T2 && (e3 = '<html xmlns="http://www.w3.org/1999/xhtml"><head></head><body>' + e3 + "</body></html>");
      var s3 = w2 ? w2.createHTML(e3) : e3;
      if (he2 === pe2) try {
        t3 = new f2().parseFromString(s3, T2);
      } catch (e4) {
      }
      if (!t3 || !t3.documentElement) {
        t3 = S2.createDocument(he2, "template", null);
        try {
          t3.documentElement.innerHTML = be2 ? "" : s3;
        } catch (e4) {
        }
      }
      var a3 = t3.body || t3.documentElement;
      return e3 && n3 && a3.insertBefore(r2.createTextNode(n3), a3.childNodes[0] || null), he2 === pe2 ? N2.call(t3, Q2 ? "html" : "body")[0] : Q2 ? t3.documentElement : a3;
    }, De2 = function(e3) {
      return _2.call(e3.ownerDocument || e3, e3, d2.SHOW_ELEMENT | d2.SHOW_COMMENT | d2.SHOW_TEXT, null, false);
    }, Pe2 = function(e3) {
      return e3 instanceof m2 && ("string" != typeof e3.nodeName || "string" != typeof e3.textContent || "function" != typeof e3.removeChild || !(e3.attributes instanceof u2) || "function" != typeof e3.removeAttribute || "function" != typeof e3.setAttribute || "string" != typeof e3.namespaceURI || "function" != typeof e3.insertBefore);
    }, Le2 = function(e3) {
      return "object" === Ab(i3) ? e3 instanceof i3 : e3 && "object" === Ab(e3) && "number" == typeof e3.nodeType && "string" == typeof e3.nodeName;
    }, Me2 = function(e3, t3, o3) {
      O2[e3] && Kb(O2[e3], function(e4) {
        e4.call(n2, t3, o3, Ce2);
      });
    }, Ie2 = function(e3) {
      var t3;
      if (Me2("beforeSanitizeElements", e3, null), Pe2(e3)) return Oe2(e3), true;
      if (tv(/[\u0080-\uFFFF]/, e3.nodeName)) return Oe2(e3), true;
      var o3 = B2(e3.nodeName);
      if (Me2("uponSanitizeElement", e3, { tagName: o3, allowedTags: z2 }), e3.hasChildNodes() && !Le2(e3.firstElementChild) && (!Le2(e3.content) || !Le2(e3.content.firstElementChild)) && tv(/<[/\w]/g, e3.innerHTML) && tv(/<[/\w]/g, e3.textContent)) return Oe2(e3), true;
      if ("select" === o3 && tv(/<template/i, e3.innerHTML)) return Oe2(e3), true;
      if (!z2[o3] || q2[o3]) {
        if (!q2[o3] && Ue2(o3)) {
          if (V2.tagNameCheck instanceof RegExp && tv(V2.tagNameCheck, o3)) return false;
          if (V2.tagNameCheck instanceof Function && V2.tagNameCheck(o3)) return false;
        }
        if (re2 && !ie2[o3]) {
          var r3 = y2(e3) || e3.parentNode, s3 = v2(e3) || e3.childNodes;
          if (s3 && r3) for (var a3 = s3.length - 1; a3 >= 0; --a3) r3.insertBefore(h2(s3[a3], true), b2(e3));
        }
        return Oe2(e3), true;
      }
      return e3 instanceof l2 && !Ae2(e3) ? (Oe2(e3), true) : "noscript" !== o3 && "noembed" !== o3 || !tv(/<\/no(script|embed)/i, e3.innerHTML) ? (X2 && 3 === e3.nodeType && (t3 = e3.textContent, t3 = Jb(t3, D2, " "), t3 = Jb(t3, P2, " "), e3.textContent !== t3 && (Yb(n2.removed, { element: e3.cloneNode() }), e3.textContent = t3)), Me2("afterSanitizeElements", e3, null), false) : (Oe2(e3), true);
    }, Fe2 = function(e3, t3, n3) {
      if (oe2 && ("id" === t3 || "name" === t3) && (n3 in r2 || n3 in we2)) return false;
      if (G2 && !W2[t3] && tv(L2, t3)) ;
      else if (K2 && tv(M2, t3)) ;
      else if (!H2[t3] || W2[t3]) {
        if (!(Ue2(e3) && (V2.tagNameCheck instanceof RegExp && tv(V2.tagNameCheck, e3) || V2.tagNameCheck instanceof Function && V2.tagNameCheck(e3)) && (V2.attributeNameCheck instanceof RegExp && tv(V2.attributeNameCheck, t3) || V2.attributeNameCheck instanceof Function && V2.attributeNameCheck(t3)) || "is" === t3 && V2.allowCustomizedBuiltInElements && (V2.tagNameCheck instanceof RegExp && tv(V2.tagNameCheck, n3) || V2.tagNameCheck instanceof Function && V2.tagNameCheck(n3)))) return false;
      } else if (ue2[t3]) ;
      else if (tv(U2, Jb(n3, F2, ""))) ;
      else if ("src" !== t3 && "xlink:href" !== t3 && "href" !== t3 || "script" === e3 || 0 !== Zb(n3, "data:") || !de2[e3]) {
        if (Y2 && !tv(I2, Jb(n3, F2, ""))) ;
        else if (n3) return false;
      }
      return true;
    }, Ue2 = function(e3) {
      return e3.indexOf("-") > 0;
    }, ze2 = function(e3) {
      var t3, n3, o3, r3;
      Me2("beforeSanitizeAttributes", e3, null);
      var s3 = e3.attributes;
      if (s3) {
        var a3 = { attrName: "", attrValue: "", keepAttr: true, allowedAttributes: H2 };
        for (r3 = s3.length; r3--; ) {
          var i4 = t3 = s3[r3], l3 = i4.name, d3 = i4.namespaceURI;
          n3 = "value" === l3 ? t3.value : ev(t3.value), o3 = B2(l3);
          var c3 = n3;
          if (a3.attrName = o3, a3.attrValue = n3, a3.keepAttr = true, a3.forceKeepAttr = void 0, Me2("uponSanitizeAttribute", e3, a3), n3 = a3.attrValue, !a3.forceKeepAttr) if (a3.keepAttr) if (tv(/\/>/i, n3)) Te2(l3, e3);
          else {
            X2 && (n3 = Jb(n3, D2, " "), n3 = Jb(n3, P2, " "));
            var u3 = B2(e3.nodeName);
            if (Fe2(u3, o3, n3)) {
              if (n3 !== c3) try {
                d3 ? e3.setAttributeNS(d3, l3, n3) : e3.setAttribute(l3, n3);
              } catch (t4) {
                Te2(l3, e3);
              }
            } else Te2(l3, e3);
          }
          else Te2(l3, e3);
        }
        Me2("afterSanitizeAttributes", e3, null);
      }
    }, je2 = function e3(t3) {
      var n3, o3 = De2(t3);
      for (Me2("beforeSanitizeShadowDOM", t3, null); n3 = o3.nextNode(); ) Me2("uponSanitizeShadowNode", n3, null), Ie2(n3) || (n3.content instanceof s2 && e3(n3.content), ze2(n3));
      Me2("afterSanitizeShadowDOM", t3, null);
    };
    return n2.sanitize = function(e3, r3) {
      var a3, l3, d3, c3, u3;
      if ((be2 = !e3) && (e3 = "<!-->"), "string" != typeof e3 && !Le2(e3)) {
        if ("function" != typeof e3.toString) throw nv("toString is not a function");
        if ("string" != typeof (e3 = e3.toString())) throw nv("dirty is not a string, aborting");
      }
      if (!n2.isSupported) {
        if ("object" === Ab(t2.toStaticHTML) || "function" == typeof t2.toStaticHTML) {
          if ("string" == typeof e3) return t2.toStaticHTML(e3);
          if (Le2(e3)) return t2.toStaticHTML(e3.outerHTML);
        }
        return e3;
      }
      if (J2 || ke2(r3), n2.removed = [], "string" == typeof e3 && (se2 = false), se2) {
        if (e3.nodeName) {
          var m3 = B2(e3.nodeName);
          if (!z2[m3] || q2[m3]) throw nv("root node is forbidden and cannot be sanitized in-place");
        }
      } else if (e3 instanceof i3) 1 === (l3 = (a3 = Be2("<!---->")).ownerDocument.importNode(e3, true)).nodeType && "BODY" === l3.nodeName || "HTML" === l3.nodeName ? a3 = l3 : a3.appendChild(l3);
      else {
        if (!ee2 && !X2 && !Q2 && -1 === e3.indexOf("<")) return w2 && ne2 ? w2.createHTML(e3) : e3;
        if (!(a3 = Be2(e3))) return ee2 ? null : ne2 ? x2 : "";
      }
      a3 && Z2 && Oe2(a3.firstChild);
      for (var f3 = De2(se2 ? e3 : a3); d3 = f3.nextNode(); ) 3 === d3.nodeType && d3 === c3 || Ie2(d3) || (d3.content instanceof s2 && je2(d3.content), ze2(d3), c3 = d3);
      if (c3 = null, se2) return e3;
      if (ee2) {
        if (te2) for (u3 = E2.call(a3.ownerDocument); a3.firstChild; ) u3.appendChild(a3.firstChild);
        else u3 = a3;
        return H2.shadowroot && (u3 = R2.call(o2, u3, true)), u3;
      }
      var g3 = Q2 ? a3.outerHTML : a3.innerHTML;
      return Q2 && z2["!doctype"] && a3.ownerDocument && a3.ownerDocument.doctype && a3.ownerDocument.doctype.name && tv(_v, a3.ownerDocument.doctype.name) && (g3 = "<!DOCTYPE " + a3.ownerDocument.doctype.name + ">\n" + g3), X2 && (g3 = Jb(g3, D2, " "), g3 = Jb(g3, P2, " ")), w2 && ne2 ? w2.createHTML(g3) : g3;
    }, n2.setConfig = function(e3) {
      ke2(e3), J2 = true;
    }, n2.clearConfig = function() {
      Ce2 = null, J2 = false;
    }, n2.isValidAttribute = function(e3, t3, n3) {
      Ce2 || ke2({});
      var o3 = B2(e3), r3 = B2(t3);
      return Fe2(o3, r3, n3);
    }, n2.addHook = function(e3, t3) {
      "function" == typeof t3 && (O2[e3] = O2[e3] || [], Yb(O2[e3], t3));
    }, n2.removeHook = function(e3) {
      if (O2[e3]) return Gb(O2[e3]);
    }, n2.removeHooks = function(e3) {
      O2[e3] && (O2[e3] = []);
    }, n2.removeAllHooks = function() {
      O2 = {};
    }, n2;
  }();
  const Av = Tt.explode, Ov = () => {
    const e2 = {};
    return { addFilter: (t2, n2) => {
      V(Av(t2), (t3) => {
        xe(e2, t3) || (e2[t3] = { name: t3, callbacks: [] }), e2[t3].callbacks.push(n2);
      });
    }, getFilters: () => Ce(e2), removeFilter: (t2, n2) => {
      V(Av(t2), (t3) => {
        if (xe(e2, t3)) if (C(n2)) {
          const o2 = e2[t3], r2 = K(o2.callbacks, (e3) => e3 !== n2);
          r2.length > 0 ? o2.callbacks = r2 : delete e2[t3];
        } else delete e2[t3];
      });
    } };
  }, Tv = (e2, t2, n2) => {
    var o2;
    const r2 = Ms();
    t2.convert_fonts_to_spans && ((e3, t3, n3) => {
      e3.addNodeFilter("font", (e4) => {
        V(e4, (e5) => {
          const o3 = t3.parse(e5.attr("style")), r3 = e5.attr("color"), s2 = e5.attr("face"), a2 = e5.attr("size");
          r3 && (o3.color = r3), s2 && (o3["font-family"] = s2), a2 && Ge(a2).each((e6) => {
            o3["font-size"] = n3[e6 - 1];
          }), e5.name = "span", e5.attr("style", t3.serialize(o3)), ((e6, t4) => {
            V(["color", "face", "size"], (t5) => {
              e6.attr(t5, null);
            });
          })(e5);
        });
      });
    })(e2, r2, Tt.explode(null !== (o2 = t2.font_size_legacy_values) && void 0 !== o2 ? o2 : "")), ((e3, t3, n3) => {
      e3.addNodeFilter("strike", (e4) => {
        const o3 = "html4" !== t3.type;
        V(e4, (e5) => {
          if (o3) e5.name = "s";
          else {
            const t4 = n3.parse(e5.attr("style"));
            t4["text-decoration"] = "line-through", e5.name = "span", e5.attr("style", n3.serialize(t4));
          }
        });
      });
    })(e2, n2, r2);
  }, Bv = (e2) => {
    const [t2, ...n2] = e2.split(","), o2 = n2.join(","), r2 = /data:([^/]+\/[^;]+)(;.+)?/.exec(t2);
    if (r2) {
      const e3 = ";base64" === r2[2], t3 = e3 ? ((e4) => {
        const t4 = /([a-z0-9+\/=\s]+)/i.exec(e4);
        return t4 ? t4[1] : "";
      })(o2) : decodeURIComponent(o2);
      return M.some({ type: r2[1], data: t3, base64Encoded: e3 });
    }
    return M.none();
  }, Dv = (e2, t2, n2 = true) => {
    let o2 = t2;
    if (n2) try {
      o2 = atob(t2);
    } catch (e3) {
      return M.none();
    }
    const r2 = new Uint8Array(o2.length);
    for (let e3 = 0; e3 < r2.length; e3++) r2[e3] = o2.charCodeAt(e3);
    return M.some(new Blob([r2], { type: e2 }));
  }, Pv = (e2) => new Promise((t2, n2) => {
    const o2 = new FileReader();
    o2.onloadend = () => {
      t2(o2.result);
    }, o2.onerror = () => {
      var e3;
      n2(null === (e3 = o2.error) || void 0 === e3 ? void 0 : e3.message);
    }, o2.readAsDataURL(e2);
  });
  let Lv = 0;
  const Mv = (e2, t2, n2) => Bv(e2).bind(({ data: e3, type: o2, base64Encoded: r2 }) => {
    if (t2 && !r2) return M.none();
    {
      const t3 = r2 ? e3 : btoa(e3);
      return n2(t3, o2);
    }
  }), Iv = (e2, t2, n2) => {
    const o2 = e2.create("blobid" + Lv++, t2, n2);
    return e2.add(o2), o2;
  }, Fv = (e2, t2, n2 = false) => Mv(t2, n2, (t3, n3) => M.from(e2.getByData(t3, n3)).orThunk(() => Dv(n3, t3).map((n4) => Iv(e2, n4, t3)))), Uv = Tt.each, zv = Tt.trim, jv = ["source", "protocol", "authority", "userInfo", "user", "password", "host", "port", "relative", "path", "directory", "file", "query", "anchor"], Hv = { ftp: 21, http: 80, https: 443, mailto: 25 }, $v = ["img", "video"], Vv = (e2, t2, n2) => {
    const o2 = ((e3) => {
      try {
        return decodeURIComponent(e3);
      } catch (t3) {
        return unescape(e3);
      }
    })(t2);
    return !e2.allow_script_urls && (!!/((java|vb)script|mhtml):/i.test(o2) || !e2.allow_html_data_urls && (/^data:image\//i.test(o2) ? ((e3, t3) => C(e3) ? !e3 : !C(t3) || !j($v, t3))(e2.allow_svg_data_urls, n2) && /^data:image\/svg\+xml/i.test(o2) : /^data:/i.test(o2)));
  };
  class qv {
    constructor(e2, t2 = {}) {
      this.path = "", this.directory = "", e2 = zv(e2), this.settings = t2;
      const n2 = t2.base_uri, o2 = this;
      if (/^([\w\-]+):([^\/]{2})/i.test(e2) || /^\s*#/.test(e2)) return void (o2.source = e2);
      const r2 = 0 === e2.indexOf("//");
      if (0 !== e2.indexOf("/") || r2 || (e2 = (n2 && n2.protocol || "http") + "://mce_host" + e2), !/^[\w\-]*:?\/\//.test(e2)) {
        const t3 = n2 ? n2.path : new qv(document.location.href).directory;
        if ("" === (null == n2 ? void 0 : n2.protocol)) e2 = "//mce_host" + o2.toAbsPath(t3, e2);
        else {
          const r3 = /([^#?]*)([#?]?.*)/.exec(e2);
          r3 && (e2 = (n2 && n2.protocol || "http") + "://mce_host" + o2.toAbsPath(t3, r3[1]) + r3[2]);
        }
      }
      e2 = e2.replace(/@@/g, "(mce_at)");
      const s2 = /^(?:(?![^:@]+:[^:@\/]*@)([^:\/?#.]+):)?(?:\/\/)?((?:(([^:@\/]*):?([^:@\/]*))?@)?(\[[a-zA-Z0-9:.%]+\]|[^:\/?#]*)(?::(\d*))?)(((\/(?:[^?#](?![^?#\/]*\.[^?#\/.]+(?:[?#]|$)))*\/?)?([^?#\/]*))(?:\?([^#]*))?(?:#(.*))?)/.exec(e2);
      s2 && Uv(jv, (e3, t3) => {
        let n3 = s2[t3];
        n3 && (n3 = n3.replace(/\(mce_at\)/g, "@@")), o2[e3] = n3;
      }), n2 && (o2.protocol || (o2.protocol = n2.protocol), o2.userInfo || (o2.userInfo = n2.userInfo), o2.port || "mce_host" !== o2.host || (o2.port = n2.port), o2.host && "mce_host" !== o2.host || (o2.host = n2.host), o2.source = ""), r2 && (o2.protocol = "");
    }
    static parseDataUri(e2) {
      let t2;
      const n2 = decodeURIComponent(e2).split(","), o2 = /data:([^;]+)/.exec(n2[0]);
      return o2 && (t2 = o2[1]), { type: t2, data: n2[1] };
    }
    static isDomSafe(e2, t2, n2 = {}) {
      if (n2.allow_script_urls) return true;
      {
        const o2 = xs.decode(e2).replace(/[\s\u0000-\u001F]+/g, "");
        return !Vv(n2, o2, t2);
      }
    }
    static getDocumentBaseUrl(e2) {
      var t2;
      let n2;
      return n2 = 0 !== e2.protocol.indexOf("http") && "file:" !== e2.protocol ? null !== (t2 = e2.href) && void 0 !== t2 ? t2 : "" : e2.protocol + "//" + e2.host + e2.pathname, /^[^:]+:\/\/\/?[^\/]+\//.test(n2) && (n2 = n2.replace(/[\?#].*$/, "").replace(/[\/\\][^\/]+$/, ""), /[\/\\]$/.test(n2) || (n2 += "/")), n2;
    }
    setPath(e2) {
      const t2 = /^(.*?)\/?(\w+)?$/.exec(e2);
      t2 && (this.path = t2[0], this.directory = t2[1], this.file = t2[2]), this.source = "", this.getURI();
    }
    toRelative(e2) {
      if ("./" === e2) return e2;
      const t2 = new qv(e2, { base_uri: this });
      if ("mce_host" !== t2.host && this.host !== t2.host && t2.host || this.port !== t2.port || this.protocol !== t2.protocol && "" !== t2.protocol) return t2.getURI();
      const n2 = this.getURI(), o2 = t2.getURI();
      if (n2 === o2 || "/" === n2.charAt(n2.length - 1) && n2.substr(0, n2.length - 1) === o2) return n2;
      let r2 = this.toRelPath(this.path, t2.path);
      return t2.query && (r2 += "?" + t2.query), t2.anchor && (r2 += "#" + t2.anchor), r2;
    }
    toAbsolute(e2, t2) {
      const n2 = new qv(e2, { base_uri: this });
      return n2.getURI(t2 && this.isSameOrigin(n2));
    }
    isSameOrigin(e2) {
      if (this.host == e2.host && this.protocol == e2.protocol) {
        if (this.port == e2.port) return true;
        const t2 = this.protocol ? Hv[this.protocol] : null;
        if (t2 && (this.port || t2) == (e2.port || t2)) return true;
      }
      return false;
    }
    toRelPath(e2, t2) {
      let n2, o2, r2 = 0, s2 = "";
      const a2 = e2.substring(0, e2.lastIndexOf("/")).split("/"), i3 = t2.split("/");
      if (a2.length >= i3.length) {
        for (n2 = 0, o2 = a2.length; n2 < o2; n2++) if (n2 >= i3.length || a2[n2] !== i3[n2]) {
          r2 = n2 + 1;
          break;
        }
      }
      if (a2.length < i3.length) {
        for (n2 = 0, o2 = i3.length; n2 < o2; n2++) if (n2 >= a2.length || a2[n2] !== i3[n2]) {
          r2 = n2 + 1;
          break;
        }
      }
      if (1 === r2) return t2;
      for (n2 = 0, o2 = a2.length - (r2 - 1); n2 < o2; n2++) s2 += "../";
      for (n2 = r2 - 1, o2 = i3.length; n2 < o2; n2++) s2 += n2 !== r2 - 1 ? "/" + i3[n2] : i3[n2];
      return s2;
    }
    toAbsPath(e2, t2) {
      let n2 = 0;
      const o2 = /\/$/.test(t2) ? "/" : "", r2 = e2.split("/"), s2 = t2.split("/"), a2 = [];
      Uv(r2, (e3) => {
        e3 && a2.push(e3);
      });
      const i3 = [];
      for (let e3 = s2.length - 1; e3 >= 0; e3--) 0 !== s2[e3].length && "." !== s2[e3] && (".." !== s2[e3] ? n2 > 0 ? n2-- : i3.push(s2[e3]) : n2++);
      const l2 = a2.length - n2;
      let d2;
      return d2 = l2 <= 0 ? ne(i3).join("/") : a2.slice(0, l2).join("/") + "/" + ne(i3).join("/"), 0 !== d2.indexOf("/") && (d2 = "/" + d2), o2 && d2.lastIndexOf("/") !== d2.length - 1 && (d2 += o2), d2;
    }
    getURI(e2 = false) {
      let t2;
      return this.source && !e2 || (t2 = "", e2 || (this.protocol ? t2 += this.protocol + "://" : t2 += "//", this.userInfo && (t2 += this.userInfo + "@"), this.host && (t2 += this.host), this.port && (t2 += ":" + this.port)), this.path && (t2 += this.path), this.query && (t2 += "?" + this.query), this.anchor && (t2 += "#" + this.anchor), this.source = t2), this.source;
    }
  }
  const Wv = Tt.makeMap, Kv = Tt.extend, Gv = { IN_PLACE: true, ALLOW_UNKNOWN_PROTOCOLS: true, ALLOWED_TAGS: ["#comment", "#cdata-section", "body"], ALLOWED_ATTR: [] }, Yv = Tt.makeMap("src,href,data,background,action,formaction,poster,xlink:href"), Xv = "data-mce-type", Qv = (e2, t2) => {
    const n2 = Rv(), o2 = t2.getSpecialElements(), r2 = e2.validate;
    let s2 = 0;
    return n2.addHook("uponSanitizeElement", (n3, a2) => {
      var i3, l2, d2;
      8 === n3.nodeType && !e2.allow_conditional_comments && /^\[if/i.test(null !== (i3 = n3.nodeValue) && void 0 !== i3 ? i3 : "") && (n3.nodeValue = " " + n3.nodeValue);
      const c2 = a2.tagName;
      if (1 !== n3.nodeType || "body" === c2) return;
      const u2 = mn(n3), f2 = c2.toLowerCase(), g2 = Gt(u2, Xv), p2 = Wt(u2, "data-mce-bogus");
      if (!g2 && m(p2)) return void ("all" === p2 ? to(u2) : no(u2));
      const h2 = t2.getElementRule(f2);
      if (!r2 || h2) {
        if (a2.allowedTags[c2] = true, r2 && h2 && !g2) {
          if (V(null !== (l2 = h2.attributesForced) && void 0 !== l2 ? l2 : [], (e3) => {
            Vt(u2, e3.name, "{$uid}" === e3.value ? "mce_" + s2++ : e3.value);
          }), V(null !== (d2 = h2.attributesDefault) && void 0 !== d2 ? d2 : [], (e3) => {
            Gt(u2, e3.name) || Vt(u2, e3.name, "{$uid}" === e3.value ? "mce_" + s2++ : e3.value);
          }), h2.attributesRequired && !H(h2.attributesRequired, (e3) => Gt(u2, e3))) return void no(u2);
          if (h2.removeEmptyAttrs && ((e3) => {
            const t3 = e3.dom.attributes;
            return null == t3 || 0 === t3.length;
          })(u2)) return void no(u2);
          h2.outputName && h2.outputName !== f2 && ((e3, t3) => {
            const n4 = ((e4, t4) => {
              const n5 = cn(t4), o4 = Xt(e4);
              return qt(n5, o4), n5;
            })(e3, t3);
            Xn(e3, n4);
            const o3 = Nn(e3);
            Zn(n4, o3), to(e3);
          })(u2, h2.outputName);
        }
      } else xe(o2, f2) ? to(u2) : no(u2);
    }), n2.addHook("uponSanitizeAttribute", (n3, o3) => {
      const s3 = n3.tagName.toLowerCase(), { attrName: a2, attrValue: i3 } = o3;
      o3.keepAttr = !r2 || t2.isValid(s3, a2) || ze(a2, "data-") || ze(a2, "aria-"), a2 in Yv && Vv(e2, i3, s3) && (o3.keepAttr = false), o3.keepAttr ? (o3.allowedAttributes[a2] = true, a2 in t2.getBoolAttrs() && (o3.attrValue = a2), e2.allow_svg_data_urls && ze(i3, "data:image/svg+xml") && (o3.forceKeepAttr = true)) : !n3.hasAttribute(Xv) || "id" !== a2 && "class" !== a2 && "style" !== a2 || (o3.forceKeepAttr = true);
    }), n2;
  }, Jv = (e2, t2, n2) => {
    const o2 = e2.name, r2 = o2 in n2 && "title" !== o2 && "textarea" !== o2, s2 = t2.childNodes;
    for (let t3 = 0, o3 = s2.length; t3 < o3; t3++) {
      const o4 = s2[t3], a2 = new Zf(o4.nodeName.toLowerCase(), o4.nodeType);
      if (yo(o4)) {
        const e3 = o4.attributes;
        for (let t4 = 0, n3 = e3.length; t4 < n3; t4++) {
          const n4 = e3[t4];
          a2.attr(n4.name, n4.value);
        }
      } else Ro(o4) ? (a2.value = o4.data, r2 && (a2.raw = true)) : (To(o4) || Ao(o4) || Oo(o4)) && (a2.value = o4.data);
      Jv(a2, o4, n2), e2.append(a2);
    }
  }, Zv = (e2 = {}, t2 = Ls()) => {
    const n2 = Ov(), o2 = Ov(), r2 = { validate: true, root_name: "body", ...e2 }, s2 = new DOMParser(), a2 = Qv(r2, t2), i3 = n2.addFilter, l2 = n2.getFilters, d2 = n2.removeFilter, c2 = o2.addFilter, u2 = o2.getFilters, m2 = o2.removeFilter, f2 = { schema: t2, addAttributeFilter: c2, getAttributeFilters: u2, removeAttributeFilter: m2, addNodeFilter: i3, getNodeFilters: l2, removeNodeFilter: d2, parse: (e3, n3 = {}) => {
      var o3;
      const i4 = r2.validate, d3 = null !== (o3 = n3.context) && void 0 !== o3 ? o3 : r2.root_name, c3 = ((e4, n4, o4 = "html") => {
        const i5 = "xhtml" === o4 ? "application/xhtml+xml" : "text/html", l3 = xe(t2.getSpecialElements(), n4.toLowerCase()), d4 = l3 ? `<${n4}>${e4}</${n4}>` : e4, c4 = "xhtml" === o4 ? `<html xmlns="http://www.w3.org/1999/xhtml"><head></head><body>${d4}</body></html>` : `<body>${d4}</body>`, u3 = s2.parseFromString(c4, i5).body;
        return a2.sanitize(u3, ((e5, t3) => {
          const n5 = { ...Gv };
          return n5.PARSER_MEDIA_TYPE = t3, e5.allow_script_urls ? n5.ALLOWED_URI_REGEXP = /.*/ : e5.allow_html_data_urls && (n5.ALLOWED_URI_REGEXP = /^(?!(\w+script|mhtml):)/i), n5;
        })(r2, i5)), a2.removed = [], l3 ? u3.firstChild : u3;
      })(e3, d3, n3.format), m3 = new Zf(d3, 11);
      Jv(m3, c3, t2.getSpecialElements());
      const [f3, g2] = ((e4, t3, n4, o4) => {
        const r3 = n4.validate, s3 = t3.getNonEmptyElements(), a3 = t3.getWhitespaceElements(), i5 = Kv(Wv("script,style,head,html,body,title,meta,param"), t3.getBlockElements()), l3 = Ds(t3), d4 = /[ \t\r\n]+/g, c4 = /^[ \t\r\n]+/, u3 = /[ \t\r\n]+$/, m4 = (e5) => {
          let t4 = e5.parent;
          for (; C(t4); ) {
            if (t4.name in a3) return true;
            t4 = t4.parent;
          }
          return false;
        }, f4 = (t4, n5) => {
          const r4 = n5 ? t4.prev : t4.next;
          return !C(r4) && !y(t4.parent) && t4.parent.name in i5 && (t4.parent !== e4 || true === o4.isRootContent);
        };
        return [(e5) => {
          var t4;
          if (3 === e5.type && !m4(e5)) {
            let n5 = null !== (t4 = e5.value) && void 0 !== t4 ? t4 : "";
            n5 = n5.replace(d4, " "), (((e6, t5) => C(e6) && (e6.name in t5 || "br" === e6.name))(e5.prev, i5) || f4(e5, true)) && (n5 = n5.replace(c4, "")), 0 === n5.length ? e5.remove() : e5.value = n5;
          }
        }, (e5) => {
          var n5;
          if (1 === e5.type) {
            const n6 = t3.getElementRule(e5.name);
            if (r3 && n6) {
              const r4 = lh(t3, s3, a3, e5);
              n6.paddInEmptyBlock && r4 && ((e6) => {
                let n7 = e6;
                for (; C(n7); ) {
                  if (n7.name in l3) return lh(t3, s3, a3, n7);
                  n7 = n7.parent;
                }
                return false;
              })(e5) ? ah(0, o4, i5, e5) : n6.removeEmpty && r4 ? i5[e5.name] ? e5.remove() : e5.unwrap() : n6.paddEmpty && (r4 || ((e6) => {
                var t4;
                return ih(e6, "#text") && (null === (t4 = null == e6 ? void 0 : e6.firstChild) || void 0 === t4 ? void 0 : t4.value) === pr;
              })(e5)) && ah(0, o4, i5, e5);
            }
          } else if (3 === e5.type && !m4(e5)) {
            let t4 = null !== (n5 = e5.value) && void 0 !== n5 ? n5 : "";
            (e5.next && i5[e5.next.name] || f4(e5, false)) && (t4 = t4.replace(u3, "")), 0 === t4.length ? e5.remove() : e5.value = t4;
          }
        }];
      })(m3, t2, r2, n3), p2 = [], h2 = i4 ? (e4) => ((e5, n4) => {
        const o4 = e5.parent;
        o4 && t2.children[e5.name] && !t2.isValidChild(o4.name, e5.name) && n4.push(e5);
      })(e4, p2) : S, b2 = { nodes: {}, attributes: {} }, v2 = (e4) => oh(l2(), u2(), e4, b2);
      if (((e4, t3, n4) => {
        const o4 = [];
        for (let n5 = e4, r3 = n5; n5; r3 = n5, n5 = n5.walk()) {
          const s3 = n5;
          V(t3, (e5) => e5(s3)), y(s3.parent) && s3 !== e4 ? n5 = r3 : o4.push(s3);
        }
        for (let e5 = o4.length - 1; e5 >= 0; e5--) {
          const t4 = o4[e5];
          V(n4, (e6) => e6(t4));
        }
      })(m3, [f3, v2], [g2, h2]), p2.reverse(), i4 && p2.length > 0) if (n3.context) {
        const { pass: e4, fail: o4 } = W(p2, (e5) => e5.parent === m3);
        ch(o4, t2, v2), n3.invalid = e4.length > 0;
      } else ch(p2, t2, v2);
      const w2 = ((e4, t3) => {
        var n4;
        const o4 = null !== (n4 = t3.forced_root_block) && void 0 !== n4 ? n4 : e4.forced_root_block;
        return false === o4 ? "" : true === o4 ? "p" : o4;
      })(r2, n3);
      return w2 && ("body" === m3.name || n3.isRootContent) && ((e4, n4) => {
        const o4 = Kv(Wv("script,style,head,html,body,title,meta,param"), t2.getBlockElements()), s3 = /^[ \t\r\n]+/, a3 = /[ \t\r\n]+$/;
        let i5 = e4.firstChild, l3 = null;
        const d4 = (e5) => {
          var t3, n5;
          e5 && (i5 = e5.firstChild, i5 && 3 === i5.type && (i5.value = null === (t3 = i5.value) || void 0 === t3 ? void 0 : t3.replace(s3, "")), i5 = e5.lastChild, i5 && 3 === i5.type && (i5.value = null === (n5 = i5.value) || void 0 === n5 ? void 0 : n5.replace(a3, "")));
        };
        if (t2.isValidChild(e4.name, n4.toLowerCase())) {
          for (; i5; ) {
            const t3 = i5.next;
            3 === i5.type || 1 === i5.type && "p" !== i5.name && !o4[i5.name] && !i5.attr(Xv) ? (l3 || (l3 = new Zf(n4, 1), l3.attr(r2.forced_root_block_attrs), e4.insert(l3, i5)), l3.append(i5)) : (d4(l3), l3 = null), i5 = t3;
          }
          d4(l3);
        }
      })(m3, w2), n3.invalid || rh(b2, n3), m3;
    } };
    return ((e3, t3) => {
      const n3 = e3.schema;
      t3.remove_trailing_brs && e3.addNodeFilter("br", (e4, t4, o4) => {
        const r3 = Tt.extend({}, n3.getBlockElements()), s3 = n3.getNonEmptyElements(), a3 = n3.getWhitespaceElements();
        r3.body = 1;
        for (let t5 = 0, i4 = e4.length; t5 < i4; t5++) {
          let i5 = e4[t5], l3 = i5.parent;
          if (l3 && r3[l3.name] && i5 === l3.lastChild) {
            let e5 = i5.prev;
            for (; e5; ) {
              const t6 = e5.name;
              if ("span" !== t6 || "bookmark" !== e5.attr("data-mce-type")) {
                "br" === t6 && (i5 = null);
                break;
              }
              e5 = e5.prev;
            }
            if (i5 && (i5.remove(), lh(n3, s3, a3, l3))) {
              const e6 = n3.getElementRule(l3.name);
              e6 && (e6.removeEmpty ? l3.remove() : e6.paddEmpty && ah(0, o4, r3, l3));
            }
          } else {
            let e5 = i5;
            for (; l3 && l3.firstChild === e5 && l3.lastChild === e5 && (e5 = l3, !r3[l3.name]); ) l3 = l3.parent;
            if (e5 === l3) {
              const e6 = new Zf("#text", 3);
              e6.value = pr, i5.replace(e6);
            }
          }
        }
      }), e3.addAttributeFilter("href", (e4) => {
        let n4 = e4.length;
        const o4 = (e5) => {
          const t4 = e5 ? Tt.trim(e5) : "";
          return /\b(noopener)\b/g.test(t4) ? t4 : ((e6) => e6.split(" ").filter((e7) => e7.length > 0).concat(["noopener"]).sort().join(" "))(t4);
        };
        if (!t3.allow_unsafe_link_target) for (; n4--; ) {
          const t4 = e4[n4];
          "a" === t4.name && "_blank" === t4.attr("target") && t4.attr("rel", o4(t4.attr("rel")));
        }
      }), t3.allow_html_in_named_anchor || e3.addAttributeFilter("id,name", (e4) => {
        let t4, n4, o4, r3, s3 = e4.length;
        for (; s3--; ) if (r3 = e4[s3], "a" === r3.name && r3.firstChild && !r3.attr("href")) for (o4 = r3.parent, t4 = r3.lastChild; t4 && o4; ) n4 = t4.prev, o4.insert(t4, r3), t4 = n4;
      }), t3.fix_list_elements && e3.addNodeFilter("ul,ol", (e4) => {
        let t4, n4, o4 = e4.length;
        for (; o4--; ) if (t4 = e4[o4], n4 = t4.parent, n4 && ("ul" === n4.name || "ol" === n4.name)) if (t4.prev && "li" === t4.prev.name) t4.prev.append(t4);
        else {
          const e5 = new Zf("li", 1);
          e5.attr("style", "list-style-type: none"), t4.wrap(e5);
        }
      });
      const o3 = n3.getValidClasses();
      t3.validate && o3 && e3.addAttributeFilter("class", (e4) => {
        var t4;
        let n4 = e4.length;
        for (; n4--; ) {
          const r3 = e4[n4], s3 = null !== (t4 = r3.attr("class")) && void 0 !== t4 ? t4 : "", a3 = Tt.explode(s3, " ");
          let i4 = "";
          for (let e5 = 0; e5 < a3.length; e5++) {
            const t5 = a3[e5];
            let n5 = false, s4 = o3["*"];
            s4 && s4[t5] && (n5 = true), s4 = o3[r3.name], !n5 && s4 && s4[t5] && (n5 = true), n5 && (i4 && (i4 += " "), i4 += t5);
          }
          i4.length || (i4 = null), r3.attr("class", i4);
        }
      }), ((e4, t4) => {
        const { blob_cache: n4 } = t4;
        if (n4) {
          const t5 = (e5) => {
            const t6 = e5.attr("src");
            ((e6) => e6.attr("src") === Nt.transparentSrc || C(e6.attr("data-mce-placeholder")))(e5) || ((e6) => C(e6.attr("data-mce-bogus")))(e5) || y(t6) || Fv(n4, t6, true).each((t7) => {
              e5.attr("src", t7.blobUri());
            });
          };
          e4.addAttributeFilter("src", (e5) => V(e5, t5));
        }
      })(e3, t3);
    })(f2, r2), ((e3, t3, n3) => {
      t3.inline_styles && Tv(e3, t3, n3);
    })(f2, r2, t2), f2;
  }, ey = (e2, t2) => {
    const n2 = ((e3) => Ch(e3) ? ig({ validate: false }).serialize(e3) : e3)(e2), o2 = t2(n2);
    if (o2.isDefaultPrevented()) return o2;
    if (Ch(e2)) {
      if (o2.content !== n2) {
        const t3 = Zv({ validate: false, forced_root_block: false }).parse(o2.content, { context: e2.name });
        return { ...o2, content: t3 };
      }
      return { ...o2, content: e2 };
    }
    return o2;
  }, ty = (e2, t2) => {
    if (t2.no_events) return Oi.value(t2);
    {
      const n2 = ((e3, t3) => e3.dispatch("BeforeGetContent", t3))(e2, t2);
      return n2.isDefaultPrevented() ? Oi.error(pm(e2, { content: "", ...n2 }).content) : Oi.value(n2);
    }
  }, ny = (e2, t2, n2) => n2.no_events ? t2 : ey(t2, (t3) => pm(e2, { ...n2, content: t3 })).content, oy = (e2, t2) => {
    if (t2.no_events) return Oi.value(t2);
    {
      const n2 = ey(t2.content, (n3) => ((e3, t3) => e3.dispatch("BeforeSetContent", t3))(e2, { ...t2, content: n3 }));
      return n2.isDefaultPrevented() ? (gm(e2, n2), Oi.error(void 0)) : Oi.value(n2);
    }
  }, ry = (e2, t2, n2) => {
    n2.no_events || gm(e2, { ...n2, content: t2 });
  }, sy = (e2, t2, n2) => ({ element: e2, width: t2, rows: n2 }), ay = (e2, t2) => ({ element: e2, cells: t2 }), iy = (e2, t2) => ({ x: e2, y: t2 }), ly = (e2, t2) => Kt(e2, t2).bind(Ge).getOr(1), dy = (e2, t2, n2) => {
    const o2 = e2.rows;
    return !!(o2[n2] ? o2[n2].cells : [])[t2];
  }, cy = (e2) => Y(e2, (e3, t2) => t2.cells.length > e3 ? t2.cells.length : e3, 0), uy = (e2, t2) => {
    const n2 = e2.rows;
    for (let e3 = 0; e3 < n2.length; e3++) {
      const o2 = n2[e3].cells;
      for (let n3 = 0; n3 < o2.length; n3++) if (bn(o2[n3], t2)) return M.some(iy(n3, e3));
    }
    return M.none();
  }, my = (e2, t2, n2, o2, r2) => {
    const s2 = [], a2 = e2.rows;
    for (let e3 = n2; e3 <= r2; e3++) {
      const n3 = a2[e3].cells, r3 = t2 < o2 ? n3.slice(t2, o2 + 1) : n3.slice(o2, t2 + 1);
      s2.push(ay(a2[e3].element, r3));
    }
    return s2;
  }, fy = (e2) => ((e3, t2) => {
    const n2 = Ra(e3.element), o2 = cn("tbody");
    return Zn(o2, t2), Jn(n2, o2), n2;
  })(e2, ((e3) => $2(e3.rows, (e4) => {
    const t2 = $2(e4.cells, (e5) => {
      const t3 = Aa(e5);
      return Yt(t3, "colspan"), Yt(t3, "rowspan"), t3;
    }), n2 = Ra(e4.element);
    return Zn(n2, t2), n2;
  }))(e2)), gy = (e2, t2) => {
    const n2 = mn(t2.commonAncestorContainer), o2 = Rg(n2, e2), r2 = K(o2, mr), s2 = ((e3, t3) => Q(e3, (e4) => "li" === Lt(e4) && hu(e4, t3)).fold(N([]), (t4) => ((e4) => Q(e4, (e5) => "ul" === Lt(e5) || "ol" === Lt(e5)))(e3).map((e4) => {
      const t5 = cn(Lt(e4)), n3 = ve(Kn(e4), (e5, t6) => ze(t6, "list-style"));
      return $n(t5, n3), [cn("li"), t5];
    }).getOr([])))(o2, t2), a2 = r2.concat(s2.length ? s2 : ((e3) => ir(e3) ? xn(e3).filter(ar).fold(N([]), (t3) => [e3, t3]) : ar(e3) ? [e3] : [])(n2));
    return $2(a2, Ra);
  }, py = () => Mm([]), hy = (e2, t2) => ((e3, t3) => Go(t3, "table", O(bn, e3)))(e2, t2[0]).bind((e3) => {
    const n2 = t2[0], o2 = t2[t2.length - 1], r2 = ((e4) => {
      const t3 = sy(Ra(e4), 0, []);
      return V(vr(e4, "tr"), (e5, n3) => {
        V(vr(e5, "td,th"), (o3, r3) => {
          ((e6, t4, n4, o4, r4) => {
            const s2 = ly(r4, "rowspan"), a2 = ly(r4, "colspan"), i3 = e6.rows;
            for (let e7 = n4; e7 < n4 + s2; e7++) {
              i3[e7] || (i3[e7] = ay(Aa(o4), []));
              for (let o5 = t4; o5 < t4 + a2; o5++) i3[e7].cells[o5] = e7 === n4 && o5 === t4 ? r4 : Ra(r4);
            }
          })(t3, ((e6, t4, n4) => {
            for (; dy(e6, t4, n4); ) t4++;
            return t4;
          })(t3, r3, n3), n3, e5, o3);
        });
      }), sy(t3.element, cy(t3.rows), t3.rows);
    })(e3);
    return ((e4, t3, n3) => uy(e4, t3).bind((t4) => uy(e4, n3).map((n4) => ((e5, t5, n5) => {
      const o3 = t5.x, r3 = t5.y, s2 = n5.x, a2 = n5.y, i3 = r3 < a2 ? my(e5, o3, r3, s2, a2) : my(e5, o3, a2, s2, r3);
      return sy(e5.element, cy(i3), i3);
    })(e4, t4, n4))))(r2, n2, o2).map((e4) => Mm([fy(e4)]));
  }).getOrThunk(py), by = (e2, t2) => {
    const n2 = uu(t2, e2);
    return n2.length > 0 ? hy(e2, n2) : ((e3, t3) => t3.length > 0 && t3[0].collapsed ? py() : ((e4, t4) => ((e5, t5) => {
      const n3 = Y(t5, (e6, t6) => (Jn(t6, e6), t6), e5);
      return t5.length > 0 ? Mm([n3]) : n3;
    })(mn(t4.cloneContents()), gy(e4, t4)))(e3, t3[0]))(e2, t2);
  }, vy = (e2, t2) => t2 >= 0 && t2 < e2.length && tu(e2.charAt(t2)), yy = (e2) => Nr(e2.innerText), Cy = (e2) => yo(e2) ? e2.outerHTML : Ro(e2) ? xs.encodeRaw(e2.data, false) : To(e2) ? "<!--" + e2.data + "-->" : "", wy = (e2, t2) => (((e3, t3) => {
    let n2 = 0;
    V(e3, (e4) => {
      0 === e4[0] ? n2++ : 1 === e4[0] ? (((e5, t4, n3) => {
        const o2 = ((e6) => {
          let t5;
          const n4 = document.createElement("div"), o3 = document.createDocumentFragment();
          for (e6 && (n4.innerHTML = e6); t5 = n4.firstChild; ) o3.appendChild(t5);
          return o3;
        })(t4);
        if (e5.hasChildNodes() && n3 < e5.childNodes.length) {
          const t5 = e5.childNodes[n3];
          e5.insertBefore(o2, t5);
        } else e5.appendChild(o2);
      })(t3, e4[1], n2), n2++) : 2 === e4[0] && ((e5, t4) => {
        if (e5.hasChildNodes() && t4 < e5.childNodes.length) {
          const n3 = e5.childNodes[t4];
          e5.removeChild(n3);
        }
      })(t3, n2);
    });
  })(((e3, t3) => {
    const n2 = e3.length + t3.length + 2, o2 = new Array(n2), r2 = new Array(n2), s2 = (n3, o3, r3, a3, l3) => {
      const d2 = i3(n3, o3, r3, a3);
      if (null === d2 || d2.start === o3 && d2.diag === o3 - a3 || d2.end === n3 && d2.diag === n3 - r3) {
        let s3 = n3, i4 = r3;
        for (; s3 < o3 || i4 < a3; ) s3 < o3 && i4 < a3 && e3[s3] === t3[i4] ? (l3.push([0, e3[s3]]), ++s3, ++i4) : o3 - n3 > a3 - r3 ? (l3.push([2, e3[s3]]), ++s3) : (l3.push([1, t3[i4]]), ++i4);
      } else {
        s2(n3, d2.start, r3, d2.start - d2.diag, l3);
        for (let t4 = d2.start; t4 < d2.end; ++t4) l3.push([0, e3[t4]]);
        s2(d2.end, o3, d2.end - d2.diag, a3, l3);
      }
    }, a2 = (n3, o3, r3, s3) => {
      let a3 = n3;
      for (; a3 - o3 < s3 && a3 < r3 && e3[a3] === t3[a3 - o3]; ) ++a3;
      return /* @__PURE__ */ ((e4, t4, n4) => ({ start: e4, end: t4, diag: n4 }))(n3, a3, o3);
    }, i3 = (n3, s3, i4, l3) => {
      const d2 = s3 - n3, c2 = l3 - i4;
      if (0 === d2 || 0 === c2) return null;
      const u2 = d2 - c2, m2 = c2 + d2, f2 = (m2 % 2 == 0 ? m2 : m2 + 1) / 2;
      let g2, p2, h2, b2, v2;
      for (o2[1 + f2] = n3, r2[1 + f2] = s3 + 1, g2 = 0; g2 <= f2; ++g2) {
        for (p2 = -g2; p2 <= g2; p2 += 2) {
          for (h2 = p2 + f2, p2 === -g2 || p2 !== g2 && o2[h2 - 1] < o2[h2 + 1] ? o2[h2] = o2[h2 + 1] : o2[h2] = o2[h2 - 1] + 1, b2 = o2[h2], v2 = b2 - n3 + i4 - p2; b2 < s3 && v2 < l3 && e3[b2] === t3[v2]; ) o2[h2] = ++b2, ++v2;
          if (u2 % 2 != 0 && u2 - g2 <= p2 && p2 <= u2 + g2 && r2[h2 - u2] <= o2[h2]) return a2(r2[h2 - u2], p2 + n3 - i4, s3, l3);
        }
        for (p2 = u2 - g2; p2 <= u2 + g2; p2 += 2) {
          for (h2 = p2 + f2 - u2, p2 === u2 - g2 || p2 !== u2 + g2 && r2[h2 + 1] <= r2[h2 - 1] ? r2[h2] = r2[h2 + 1] - 1 : r2[h2] = r2[h2 - 1], b2 = r2[h2] - 1, v2 = b2 - n3 + i4 - p2; b2 >= n3 && v2 >= i4 && e3[b2] === t3[v2]; ) r2[h2] = b2--, v2--;
          if (u2 % 2 == 0 && -g2 <= p2 && p2 <= g2 && r2[h2] <= o2[h2 + u2]) return a2(r2[h2], p2 + n3 - i4, s3, l3);
        }
      }
      return null;
    }, l2 = [];
    return s2(0, e3.length, 0, t3.length, l2), l2;
  })($2(de(t2.childNodes), Cy), e2), t2), t2), xy = De(() => document.implementation.createHTMLDocument("undo")), ky = (e2) => {
    const t2 = (n2 = e2.getBody(), K($2(de(n2.childNodes), Cy), (e3) => e3.length > 0));
    var n2;
    const o2 = ee(t2, (t3) => {
      const n3 = ng(e2.serializer, t3);
      return n3.length > 0 ? [n3] : [];
    }), r2 = o2.join("");
    return ((e3) => -1 !== e3.indexOf("</iframe>"))(r2) ? /* @__PURE__ */ ((e3) => ({ type: "fragmented", fragments: e3, content: "", bookmark: null, beforeBookmark: null }))(o2) : /* @__PURE__ */ ((e3) => ({ type: "complete", fragments: null, content: e3, bookmark: null, beforeBookmark: null }))(r2);
  }, Sy = (e2, t2, n2) => {
    const o2 = n2 ? t2.beforeBookmark : t2.bookmark;
    "fragmented" === t2.type ? wy(t2.fragments, e2.getBody()) : e2.setContent(t2.content, { format: "raw", no_selection: !C(o2) || !$c(o2) || !o2.isFakeCaret }), o2 && e2.selection.moveToBookmark(o2);
  }, _y = (e2) => "fragmented" === e2.type ? e2.fragments.join("") : e2.content, Ey = (e2) => {
    const t2 = cn("body", xy());
    return ro(t2, _y(e2)), V(vr(t2, "*[data-mce-bogus]"), no), oo(t2);
  }, Ny = (e2, t2) => !(!e2 || !t2) && (!!((e3, t3) => _y(e3) === _y(t3))(e2, t2) || ((e3, t3) => Ey(e3) === Ey(t3))(e2, t2)), Ry = (e2) => 0 === e2.get(), Ay = (e2, t2, n2) => {
    Ry(n2) && (e2.typing = t2);
  }, Oy = (e2, t2) => {
    e2.typing && (Ay(e2, false, t2), e2.add());
  }, Ty = (e2) => ({ init: { bindEvents: S }, undoManager: { beforeChange: (t2, n2) => ((e3, t3, n3) => {
    Ry(t3) && n3.set(Ni(e3.selection));
  })(e2, t2, n2), add: (t2, n2, o2, r2, s2, a2) => ((e3, t3, n3, o3, r3, s3, a3) => {
    const i3 = ky(e3), l2 = Tt.extend(s3 || {}, i3);
    if (!Ry(o3) || e3.removed) return null;
    const d2 = t3.data[n3.get()];
    if (e3.dispatch("BeforeAddUndo", { level: l2, lastLevel: d2, originalEvent: a3 }).isDefaultPrevented()) return null;
    if (d2 && Ny(d2, l2)) return null;
    t3.data[n3.get()] && r3.get().each((e4) => {
      t3.data[n3.get()].beforeBookmark = e4;
    });
    const c2 = ql(e3);
    if (c2 && t3.data.length > c2) {
      for (let e4 = 0; e4 < t3.data.length - 1; e4++) t3.data[e4] = t3.data[e4 + 1];
      t3.data.length--, n3.set(t3.data.length);
    }
    l2.bookmark = Ni(e3.selection), n3.get() < t3.data.length - 1 && (t3.data.length = n3.get() + 1), t3.data.push(l2), n3.set(t3.data.length - 1);
    const u2 = { level: l2, lastLevel: d2, originalEvent: a3 };
    return n3.get() > 0 ? (e3.setDirty(true), e3.dispatch("AddUndo", u2), e3.dispatch("change", u2)) : e3.dispatch("AddUndo", u2), l2;
  })(e2, t2, n2, o2, r2, s2, a2), undo: (t2, n2, o2) => ((e3, t3, n3, o3) => {
    let r2;
    return t3.typing && (t3.add(), t3.typing = false, Ay(t3, false, n3)), o3.get() > 0 && (o3.set(o3.get() - 1), r2 = t3.data[o3.get()], Sy(e3, r2, true), e3.setDirty(true), e3.dispatch("Undo", { level: r2 })), r2;
  })(e2, t2, n2, o2), redo: (t2, n2) => ((e3, t3, n3) => {
    let o2;
    return t3.get() < n3.length - 1 && (t3.set(t3.get() + 1), o2 = n3[t3.get()], Sy(e3, o2, false), e3.setDirty(true), e3.dispatch("Redo", { level: o2 })), o2;
  })(e2, t2, n2), clear: (t2, n2) => ((e3, t3, n3) => {
    t3.data = [], n3.set(0), t3.typing = false, e3.dispatch("ClearUndos");
  })(e2, t2, n2), reset: (e3) => ((e4) => {
    e4.clear(), e4.add();
  })(e3), hasUndo: (t2, n2) => ((e3, t3, n3) => n3.get() > 0 || t3.typing && t3.data[0] && !Ny(ky(e3), t3.data[0]))(e2, t2, n2), hasRedo: (e3, t2) => ((e4, t3) => t3.get() < e4.data.length - 1 && !e4.typing)(e3, t2), transact: (e3, t2, n2) => ((e4, t3, n3) => (Oy(e4, t3), e4.beforeChange(), e4.ignore(n3), e4.add()))(e3, t2, n2), ignore: (e3, t2) => ((e4, t3) => {
    try {
      e4.set(e4.get() + 1), t3();
    } finally {
      e4.set(e4.get() - 1);
    }
  })(e3, t2), extra: (t2, n2, o2, r2) => ((e3, t3, n3, o3, r3) => {
    if (t3.transact(o3)) {
      const o4 = t3.data[n3.get()].bookmark, s2 = t3.data[n3.get() - 1];
      Sy(e3, s2, true), t3.transact(r3) && (t3.data[n3.get() - 1].beforeBookmark = o4);
    }
  })(e2, t2, n2, o2, r2) }, formatter: { match: (t2, n2, o2, r2) => Th(e2, t2, n2, o2, r2), matchAll: (t2, n2) => ((e3, t3, n3) => {
    const o2 = [], r2 = {}, s2 = e3.selection.getStart();
    return e3.dom.getParent(s2, (s3) => {
      for (let a2 = 0; a2 < t3.length; a2++) {
        const i3 = t3[a2];
        !r2[i3] && Oh(e3, s3, i3, n3) && (r2[i3] = true, o2.push(i3));
      }
    }, e3.dom.getRoot()), o2;
  })(e2, t2, n2), matchNode: (t2, n2, o2, r2) => Oh(e2, t2, n2, o2, r2), canApply: (t2) => ((e3, t3) => {
    const n2 = e3.formatter.get(t3), o2 = e3.dom;
    if (n2) {
      const t4 = e3.selection.getStart(), r2 = Mu(o2, t4);
      for (let e4 = n2.length - 1; e4 >= 0; e4--) {
        const t5 = n2[e4];
        if (!Uu(t5)) return true;
        for (let e5 = r2.length - 1; e5 >= 0; e5--) if (o2.is(r2[e5], t5.selector)) return true;
      }
    }
    return false;
  })(e2, t2), closest: (t2) => ((e3, t3) => {
    const n2 = (t4) => bn(t4, mn(e3.getBody()));
    return M.from(e3.selection.getStart(true)).bind((o2) => Sh(mn(o2), (n3) => ce(t3, (t4) => ((t5, n4) => Oh(e3, t5.dom, n4) ? M.some(n4) : M.none())(n3, t4)), n2)).getOrNull();
  })(e2, t2), apply: (t2, n2, o2) => kb(e2, t2, n2, o2), remove: (t2, n2, o2, r2) => yb(e2, t2, n2, o2, r2), toggle: (t2, n2, o2) => ((e3, t3, n3, o3) => {
    const r2 = e3.formatter.get(t3);
    r2 && (!Th(e3, t3, n3, o3) || "toggle" in r2[0] && !r2[0].toggle ? kb(e3, t3, n3, o3) : yb(e3, t3, n3, o3));
  })(e2, t2, n2, o2), formatChanged: (t2, n2, o2, r2, s2) => ((e3, t3, n3, o3, r3, s3) => (((e4, t4, n4, o4, r4, s4) => {
    const a2 = t4.get();
    V(n4.split(","), (t5) => {
      const n5 = we(a2, t5).getOrThunk(() => {
        const e5 = { withSimilar: { state: ra(false), similar: true, callbacks: [] }, withoutSimilar: { state: ra(false), similar: false, callbacks: [] }, withVars: [] };
        return a2[t5] = e5, e5;
      }), i3 = () => {
        const n6 = Nb(e4);
        return Eb(e4, n6, t5, r4, s4).isSome();
      };
      if (v(s4)) {
        const e5 = r4 ? n5.withSimilar : n5.withoutSimilar;
        e5.callbacks.push(o4), 1 === e5.callbacks.length && e5.state.set(i3());
      } else n5.withVars.push({ state: ra(i3()), similar: r4, vars: s4, callback: o4 });
    }), t4.set(a2);
  })(e3, t3, n3, o3, r3, s3), { unbind: () => ((e4, t4, n4) => {
    const o4 = e4.get();
    V(t4.split(","), (e5) => we(o4, e5).each((t5) => {
      o4[e5] = { withSimilar: { ...t5.withSimilar, callbacks: K(t5.withSimilar.callbacks, (e6) => e6 !== n4) }, withoutSimilar: { ...t5.withoutSimilar, callbacks: K(t5.withoutSimilar.callbacks, (e6) => e6 !== n4) }, withVars: K(t5.withVars, (e6) => e6.callback !== n4) };
    })), e4.set(o4);
  })(t3, n3, o3) }))(e2, t2, n2, o2, r2, s2) }, editor: { getContent: (t2) => ((e3, t3) => M.from(e3.getBody()).fold(N("tree" === t3.format ? new Zf("body", 11) : ""), (n2) => rg(e3, t3, n2)))(e2, t2), setContent: (t2, n2) => ((e3, t3, n3) => M.from(e3.getBody()).map((o2) => Ch(t3) ? ((e4, t4, n4, o3) => {
    sh(e4.parser.getNodeFilters(), e4.parser.getAttributeFilters(), n4);
    const r2 = ig({ validate: false }, e4.schema).serialize(n4), s2 = cr(mn(t4)) ? r2 : Tt.trim(r2);
    return wh(e4, s2, o3.no_selection), { content: n4, html: s2 };
  })(e3, o2, t3, n3) : ((e4, t4, n4, o3) => {
    if (0 === n4.length || /^\s+$/.test(n4)) {
      const r2 = '<br data-mce-bogus="1">';
      "TABLE" === t4.nodeName ? n4 = "<tr><td>" + r2 + "</td></tr>" : /^(UL|OL)$/.test(t4.nodeName) && (n4 = "<li>" + r2 + "</li>");
      const s2 = Ji(e4);
      return e4.schema.isValidChild(t4.nodeName.toLowerCase(), s2.toLowerCase()) ? (n4 = r2, n4 = e4.dom.createHTML(s2, Zi(e4), n4)) : n4 || (n4 = r2), wh(e4, n4, o3.no_selection), { content: n4, html: n4 };
    }
    {
      "raw" !== o3.format && (n4 = ig({ validate: false }, e4.schema).serialize(e4.parser.parse(n4, { isRootContent: true, insert: true })));
      const r2 = cr(mn(t4)) ? n4 : Tt.trim(n4);
      return wh(e4, r2, o3.no_selection), { content: r2, html: r2 };
    }
  })(e3, o2, t3, n3)).getOr({ content: t3, html: Ch(n3.content) ? "" : n3.content }))(e2, t2, n2), insertContent: (t2, n2) => yh(e2, t2, n2), addVisual: (t2) => ((e3, t3) => {
    const n2 = e3.dom, o2 = C(t3) ? t3 : e3.getBody();
    V(n2.select("table,a", o2), (t4) => {
      switch (t4.nodeName) {
        case "TABLE":
          const o3 = Zl(e3), r2 = n2.getAttrib(t4, "border");
          r2 && "0" !== r2 || !e3.hasVisual ? n2.removeClass(t4, o3) : n2.addClass(t4, o3);
          break;
        case "A":
          if (!n2.getAttrib(t4, "href")) {
            const o4 = n2.getAttrib(t4, "name") || t4.id, r3 = ed(e3);
            o4 && e3.hasVisual ? n2.addClass(t4, r3) : n2.removeClass(t4, r3);
          }
      }
    }), e3.dispatch("VisualAid", { element: t3, hasVisual: e3.hasVisual });
  })(e2, t2) }, selection: { getContent: (t2, n2) => ((e3, t3, n3 = {}) => {
    const o2 = ((e4, t4) => ({ ...e4, format: t4, get: true, selection: true, getInner: true }))(n3, t3);
    return ty(e3, o2).fold(R, (t4) => {
      const n4 = ((e4, t5) => {
        if ("text" === t5.format) return ((e5) => M.from(e5.selection.getRng()).map((t6) => {
          var n5;
          const o3 = M.from(e5.dom.getParent(t6.commonAncestorContainer, e5.dom.isBlock)), r2 = e5.getBody(), s2 = ((e6) => e6.map((e7) => e7.nodeName).getOr("div").toLowerCase())(o3), a2 = e5.dom.add(r2, s2, { "data-mce-bogus": "all", style: "overflow: hidden; opacity: 0;" }, t6.cloneContents()), i3 = yy(a2), l2 = Nr(null !== (n5 = a2.textContent) && void 0 !== n5 ? n5 : "");
          if (e5.dom.remove(a2), vy(l2, 0) || vy(l2, l2.length - 1)) {
            const e6 = o3.getOr(r2), t7 = yy(e6), n6 = t7.indexOf(i3);
            return -1 === n6 ? i3 : (vy(t7, n6 - 1) ? " " : "") + i3 + (vy(t7, n6 + i3.length) ? " " : "");
          }
          return i3;
        }).getOr(""))(e4);
        {
          const n5 = ((e5, t6) => {
            const n6 = e5.selection.getRng(), o3 = e5.dom.create("body"), r2 = e5.selection.getSel(), s2 = $f(e5, cu(r2)), a2 = t6.contextual ? by(mn(e5.getBody()), s2).dom : n6.cloneContents();
            return a2 && o3.appendChild(a2), e5.selection.serializer.serialize(o3, t6);
          })(e4, t5);
          return "tree" === t5.format ? n5 : e4.selection.isCollapsed() ? "" : n5;
        }
      })(e3, t4);
      return ny(e3, n4, t4);
    });
  })(e2, t2, n2) }, autocompleter: { addDecoration: (t2) => Kf(e2, t2), removeDecoration: () => ((e3, t2) => Gf(t2).each((t3) => {
    const n2 = e3.selection.getBookmark();
    no(t3), e3.selection.moveToBookmark(n2);
  }))(e2, mn(e2.getBody())) }, raw: { getModel: () => M.none() } }), By = (e2) => xe(e2.plugins, "rtc"), Dy = (e2) => e2.rtcInstance ? e2.rtcInstance : Ty(e2), Py = (e2) => {
    const t2 = e2.rtcInstance;
    if (t2) return t2;
    throw new Error("Failed to get RTC instance not yet initialized.");
  }, Ly = (e2) => Py(e2).init.bindEvents(), My = (e2) => 0 === e2.dom.length ? (to(e2), M.none()) : M.some(e2), Iy = (e2, t2, n2, o2) => {
    e2.bind((e3) => ((o2 ? sp : rp)(e3.dom, o2 ? e3.dom.length : 0), t2.filter(Ut).map((t3) => ((e4, t4, n3, o3) => {
      const r2 = e4.dom, s2 = t4.dom, a2 = o3 ? r2.length : s2.length;
      o3 ? (ap(r2, s2, false, !o3), n3.setStart(s2, a2)) : (ap(s2, r2, false, !o3), n3.setEnd(s2, a2));
    })(e3, t3, n2, o2)))).orThunk(() => {
      const e3 = ((e4, t3) => e4.filter((e5) => dm.isBookmarkNode(e5.dom)).bind(t3 ? Sn : kn))(t2, o2).or(t2).filter(Ut);
      return e3.map((e4) => ((e5, t3) => {
        xn(e5).each((n3) => {
          const o3 = e5.dom;
          t3 && Yg(n3, ai(o3, 0)) ? rp(o3, 0) : !t3 && Xg(n3, ai(o3, o3.length)) && sp(o3, o3.length);
        });
      })(e4, o2));
    });
  }, Fy = (e2, t2, n2) => {
    if (xe(e2, t2)) {
      const o2 = K(e2[t2], (e3) => e3 !== n2);
      0 === o2.length ? delete e2[t2] : e2[t2] = o2;
    }
  };
  const Uy = (e2) => !(!e2 || !e2.ownerDocument) && vn(mn(e2.ownerDocument), mn(e2)), zy = (e2, t2, n2, o2) => {
    let r2, s2;
    const { selectorChangedWithUnbind: a2 } = /* @__PURE__ */ ((e3, t3) => {
      let n3, o3;
      const r3 = (t4, n4) => Q(n4, (n5) => e3.is(n5, t4)), s3 = (t4) => e3.getParents(t4, void 0, e3.getRoot());
      return { selectorChangedWithUnbind: (e4, a3) => (n3 || (n3 = {}, o3 = {}, t3.on("NodeChange", (e5) => {
        const t4 = e5.element, a4 = s3(t4), i4 = {};
        fe(n3, (e6, t5) => {
          r3(t5, a4).each((n4) => {
            o3[t5] || (V(e6, (e7) => {
              e7(true, { node: n4, selector: t5, parents: a4 });
            }), o3[t5] = e6), i4[t5] = e6;
          });
        }), fe(o3, (e6, n4) => {
          i4[n4] || (delete o3[n4], V(e6, (e7) => {
            e7(false, { node: t4, selector: n4, parents: a4 });
          }));
        });
      })), n3[e4] || (n3[e4] = []), n3[e4].push(a3), r3(e4, s3(t3.selection.getStart())).each(() => {
        o3[e4] = n3[e4];
      }), { unbind: () => {
        Fy(n3, e4, a3), Fy(o3, e4, a3);
      } }) };
    })(e2, o2), i3 = (e3, t3) => ((e4, t4, n3 = {}) => {
      const o3 = ((e5, t5) => ({ format: "html", ...e5, set: true, selection: true, content: t5 }))(n3, t4);
      oy(e4, o3).each((t5) => {
        const n4 = ((e5, t6) => {
          if ("raw" !== t6.format) {
            const n5 = e5.selection.getRng(), o5 = e5.dom.getParent(n5.commonAncestorContainer, e5.dom.isBlock), r3 = o5 ? { context: o5.nodeName.toLowerCase() } : {}, s3 = e5.parser.parse(t6.content, { forced_root_block: false, ...r3, ...t6 });
            return ig({ validate: false }, e5.schema).serialize(s3);
          }
          return t6.content;
        })(e4, t5), o4 = e4.selection.getRng();
        ((e5, t6) => {
          const n5 = M.from(t6.firstChild).map(mn), o5 = M.from(t6.lastChild).map(mn);
          e5.deleteContents(), e5.insertNode(t6);
          const r3 = n5.bind(kn).filter(Ut).bind(My), s3 = o5.bind(Sn).filter(Ut).bind(My);
          Iy(r3, n5, e5, true), Iy(s3, o5, e5, false), e5.collapse(false);
        })(o4, o4.createContextualFragment(n4)), e4.selection.setRng(o4), hf(e4, o4), ry(e4, n4, t5);
      });
    })(o2, e3, t3), l2 = (e3) => {
      const t3 = c2();
      t3.collapse(!!e3), u2(t3);
    }, d2 = () => t2.getSelection ? t2.getSelection() : t2.document.selection, c2 = () => {
      let n3;
      const a3 = (e3, t3, n4) => {
        try {
          return t3.compareBoundaryPoints(e3, n4);
        } catch (e4) {
          return -1;
        }
      }, i4 = t2.document;
      if (C(o2.bookmark) && !If(o2)) {
        const e3 = Nf(o2);
        if (e3.isSome()) return e3.map((e4) => $f(o2, [e4])[0]).getOr(i4.createRange());
      }
      try {
        const e3 = d2();
        e3 && !vo(e3.anchorNode) && (n3 = e3.rangeCount > 0 ? e3.getRangeAt(0) : i4.createRange(), n3 = $f(o2, [n3])[0]);
      } catch (e3) {
      }
      if (n3 || (n3 = i4.createRange()), Bo(n3.startContainer) && n3.collapsed) {
        const t3 = e2.getRoot();
        n3.setStart(t3, 0), n3.setEnd(t3, 0);
      }
      return r2 && s2 && (0 === a3(n3.START_TO_START, n3, r2) && 0 === a3(n3.END_TO_END, n3, r2) ? n3 = s2 : (r2 = null, s2 = null)), n3;
    }, u2 = (e3, t3) => {
      if (!((e4) => !!e4 && Uy(e4.startContainer) && Uy(e4.endContainer))(e3)) return;
      const n3 = d2();
      if (e3 = o2.dispatch("SetSelectionRange", { range: e3, forward: t3 }).range, n3) {
        s2 = e3;
        try {
          n3.removeAllRanges(), n3.addRange(e3);
        } catch (e4) {
        }
        false === t3 && n3.extend && (n3.collapse(e3.endContainer, e3.endOffset), n3.extend(e3.startContainer, e3.startOffset)), r2 = n3.rangeCount > 0 ? n3.getRangeAt(0) : null;
      }
      if (!e3.collapsed && e3.startContainer === e3.endContainer && (null == n3 ? void 0 : n3.setBaseAndExtent) && e3.endOffset - e3.startOffset < 2 && e3.startContainer.hasChildNodes()) {
        const t4 = e3.startContainer.childNodes[e3.startOffset];
        t4 && "IMG" === t4.nodeName && (n3.setBaseAndExtent(e3.startContainer, e3.startOffset, e3.endContainer, e3.endOffset), n3.anchorNode === e3.startContainer && n3.focusNode === e3.endContainer || n3.setBaseAndExtent(t4, 0, t4, 1));
      }
      o2.dispatch("AfterSetSelectionRange", { range: e3, forward: t3 });
    }, m2 = () => {
      const t3 = d2(), n3 = null == t3 ? void 0 : t3.anchorNode, o3 = null == t3 ? void 0 : t3.focusNode;
      if (!t3 || !n3 || !o3 || vo(n3) || vo(o3)) return true;
      const r3 = e2.createRng(), s3 = e2.createRng();
      try {
        r3.setStart(n3, t3.anchorOffset), r3.collapse(true), s3.setStart(o3, t3.focusOffset), s3.collapse(true);
      } catch (e3) {
        return true;
      }
      return r3.compareBoundaryPoints(r3.START_TO_START, s3) <= 0;
    }, f2 = { dom: e2, win: t2, serializer: n2, editor: o2, collapse: l2, setCursorLocation: (t3, n3) => {
      const r3 = e2.createRng();
      C(t3) && C(n3) ? (r3.setStart(t3, n3), r3.setEnd(t3, n3), u2(r3), l2(false)) : (bu(e2, r3, o2.getBody(), true), u2(r3));
    }, getContent: (e3) => ((e4, t3 = {}) => ((e5, t4, n3) => Py(e5).selection.getContent(t4, n3))(e4, t3.format ? t3.format : "html", t3))(o2, e3), setContent: i3, getBookmark: (e3, t3) => g2.getBookmark(e3, t3), moveToBookmark: (e3) => g2.moveToBookmark(e3), select: (t3, n3) => (((e3, t4, n4) => M.from(t4).bind((t5) => M.from(t5.parentNode).map((o3) => {
      const r3 = e3.nodeIndex(t5), s3 = e3.createRng();
      return s3.setStart(o3, r3), s3.setEnd(o3, r3 + 1), n4 && (bu(e3, s3, t5, true), bu(e3, s3, t5, false)), s3;
    })))(e2, t3, n3).each(u2), t3), isCollapsed: () => {
      const e3 = c2(), t3 = d2();
      return !(!e3 || e3.item) && (e3.compareEndPoints ? 0 === e3.compareEndPoints("StartToEnd", e3) : !t3 || e3.collapsed);
    }, isForward: m2, setNode: (t3) => (i3(e2.getOuterHTML(t3)), t3), getNode: () => ((e3, t3) => {
      if (!t3) return e3;
      let n3 = t3.startContainer, o3 = t3.endContainer;
      const r3 = t3.startOffset, s3 = t3.endOffset;
      let a3 = t3.commonAncestorContainer;
      t3.collapsed || (n3 === o3 && s3 - r3 < 2 && n3.hasChildNodes() && (a3 = n3.childNodes[r3]), Ro(n3) && Ro(o3) && (n3 = n3.length === r3 ? Hf(n3.nextSibling, true) : n3.parentNode, o3 = 0 === s3 ? Hf(o3.previousSibling, false) : o3.parentNode, n3 && n3 === o3 && (a3 = n3)));
      const i4 = Ro(a3) ? a3.parentNode : a3;
      return yo(i4) ? i4 : e3;
    })(o2.getBody(), c2()), getSel: d2, setRng: u2, getRng: c2, getStart: (e3) => zf(o2.getBody(), c2(), e3), getEnd: (e3) => jf(o2.getBody(), c2(), e3), getSelectedBlocks: (t3, n3) => ((e3, t4, n4, o3) => {
      const r3 = [], s3 = e3.getRoot(), a3 = e3.getParent(n4 || zf(s3, t4, t4.collapsed), e3.isBlock), i4 = e3.getParent(o3 || jf(s3, t4, t4.collapsed), e3.isBlock);
      if (a3 && a3 !== s3 && r3.push(a3), a3 && i4 && a3 !== i4) {
        let t5 = a3;
        const n5 = new Zo(a3, s3);
        for (; (t5 = n5.next()) && t5 !== i4; ) e3.isBlock(t5) && r3.push(t5);
      }
      return i4 && a3 !== i4 && i4 !== s3 && r3.push(i4), r3;
    })(e2, c2(), t3, n3), normalize: () => {
      const t3 = c2(), n3 = d2();
      if (!(cu(n3).length > 1) && vu(o2)) {
        const n4 = Wm(e2, t3);
        return n4.each((e3) => {
          u2(e3, m2());
        }), n4.getOr(t3);
      }
      return t3;
    }, selectorChanged: (e3, t3) => (a2(e3, t3), f2), selectorChangedWithUnbind: a2, getScrollContainer: () => {
      let t3, n3 = e2.getRoot();
      for (; n3 && "BODY" !== n3.nodeName; ) {
        if (n3.scrollHeight > n3.clientHeight) {
          t3 = n3;
          break;
        }
        n3 = n3.parentNode;
      }
      return t3;
    }, scrollIntoView: (e3, t3) => {
      C(e3) ? ((e4, t4, n3) => {
        (e4.inline ? ff : pf)(e4, t4, n3);
      })(o2, e3, t3) : hf(o2, c2(), t3);
    }, placeCaretAt: (e3, t3) => u2(Fm(e3, t3, o2.getDoc())), getBoundingClientRect: () => {
      const e3 = c2();
      return e3.collapsed ? ai.fromRangeStart(e3).getClientRects()[0] : e3.getBoundingClientRect();
    }, destroy: () => {
      t2 = r2 = s2 = null, p2.destroy();
    } }, g2 = dm(f2), p2 = xm(f2, o2);
    return f2.bookmarkManager = g2, f2.controlSelection = p2, f2;
  }, jy = (e2, t2, n2) => {
    -1 === Tt.inArray(t2, n2) && (e2.addAttributeFilter(n2, (e3, t3) => {
      let n3 = e3.length;
      for (; n3--; ) e3[n3].attr(t3, null);
    }), t2.push(n2));
  }, Hy = (e2, t2) => {
    const n2 = ["data-mce-selected"], o2 = t2 && t2.dom ? t2.dom : ta.DOM, r2 = t2 && t2.schema ? t2.schema : Ls(e2);
    e2.entity_encoding = e2.entity_encoding || "named", e2.remove_trailing_brs = !("remove_trailing_brs" in e2) || e2.remove_trailing_brs;
    const s2 = Zv(e2, r2);
    return ((e3, t3, n3) => {
      e3.addAttributeFilter("data-mce-tabindex", (e4, t4) => {
        let n4 = e4.length;
        for (; n4--; ) {
          const o3 = e4[n4];
          o3.attr("tabindex", o3.attr("data-mce-tabindex")), o3.attr(t4, null);
        }
      }), e3.addAttributeFilter("src,href,style", (e4, o3) => {
        const r3 = "data-mce-" + o3, s3 = t3.url_converter, a2 = t3.url_converter_scope;
        let i3 = e4.length;
        for (; i3--; ) {
          const t4 = e4[i3];
          let l2 = t4.attr(r3);
          void 0 !== l2 ? (t4.attr(o3, l2.length > 0 ? l2 : null), t4.attr(r3, null)) : (l2 = t4.attr(o3), "style" === o3 ? l2 = n3.serializeStyle(n3.parseStyle(l2), t4.name) : s3 && (l2 = s3.call(a2, l2, o3, t4.name)), t4.attr(o3, l2.length > 0 ? l2 : null));
        }
      }), e3.addAttributeFilter("class", (e4) => {
        let t4 = e4.length;
        for (; t4--; ) {
          const n4 = e4[t4];
          let o3 = n4.attr("class");
          o3 && (o3 = o3.replace(/(?:^|\s)mce-item-\w+(?!\S)/g, ""), n4.attr("class", o3.length > 0 ? o3 : null));
        }
      }), e3.addAttributeFilter("data-mce-type", (e4, t4, n4) => {
        let o3 = e4.length;
        for (; o3--; ) {
          const t5 = e4[o3];
          if ("bookmark" === t5.attr("data-mce-type") && !n4.cleanup) {
            const e5 = M.from(t5.firstChild).exists((e6) => {
              var t6;
              return !Er(null !== (t6 = e6.value) && void 0 !== t6 ? t6 : "");
            });
            e5 ? t5.unwrap() : t5.remove();
          }
        }
      }), e3.addNodeFilter("noscript", (e4) => {
        var t4;
        let n4 = e4.length;
        for (; n4--; ) {
          const o3 = e4[n4].firstChild;
          o3 && (o3.value = xs.decode(null !== (t4 = o3.value) && void 0 !== t4 ? t4 : ""));
        }
      }), e3.addNodeFilter("script,style", (e4, n4) => {
        var o3;
        const r3 = (e5) => e5.replace(/(<!--\[CDATA\[|\]\]-->)/g, "\n").replace(/^[\r\n]*|[\r\n]*$/g, "").replace(/^\s*((<!--)?(\s*\/\/)?\s*<!\[CDATA\[|(<!--\s*)?\/\*\s*<!\[CDATA\[\s*\*\/|(\/\/)?\s*<!--|\/\*\s*<!--\s*\*\/)\s*[\r\n]*/gi, "").replace(/\s*(\/\*\s*\]\]>\s*\*\/(-->)?|\s*\/\/\s*\]\]>(-->)?|\/\/\s*(-->)?|\]\]>|\/\*\s*-->\s*\*\/|\s*-->\s*)\s*$/g, "");
        let s3 = e4.length;
        for (; s3--; ) {
          const a2 = e4[s3], i3 = a2.firstChild, l2 = null !== (o3 = null == i3 ? void 0 : i3.value) && void 0 !== o3 ? o3 : "";
          if ("script" === n4) {
            const e5 = a2.attr("type");
            e5 && a2.attr("type", "mce-no/type" === e5 ? null : e5.replace(/^mce\-/, "")), "xhtml" === t3.element_format && i3 && l2.length > 0 && (i3.value = "// <![CDATA[\n" + r3(l2) + "\n// ]]>");
          } else "xhtml" === t3.element_format && i3 && l2.length > 0 && (i3.value = "<!--\n" + r3(l2) + "\n-->");
        }
      }), e3.addNodeFilter("#comment", (e4) => {
        let o3 = e4.length;
        for (; o3--; ) {
          const r3 = e4[o3], s3 = r3.value;
          t3.preserve_cdata && 0 === (null == s3 ? void 0 : s3.indexOf("[CDATA[")) ? (r3.name = "#cdata", r3.type = 4, r3.value = n3.decode(s3.replace(/^\[CDATA\[|\]\]$/g, ""))) : 0 === (null == s3 ? void 0 : s3.indexOf("mce:protected ")) && (r3.name = "#text", r3.type = 3, r3.raw = true, r3.value = unescape(s3).substr(14));
        }
      }), e3.addNodeFilter("xml:namespace,input", (e4, t4) => {
        let n4 = e4.length;
        for (; n4--; ) {
          const o3 = e4[n4];
          7 === o3.type ? o3.remove() : 1 === o3.type && ("input" !== t4 || o3.attr("type") || o3.attr("type", "text"));
        }
      }), e3.addAttributeFilter("data-mce-type", (t4) => {
        V(t4, (t5) => {
          "format-caret" === t5.attr("data-mce-type") && (t5.isEmpty(e3.schema.getNonEmptyElements()) ? t5.remove() : t5.unwrap());
        });
      }), e3.addAttributeFilter("data-mce-src,data-mce-href,data-mce-style,data-mce-selected,data-mce-expando,data-mce-type,data-mce-resize,data-mce-placeholder", (e4, t4) => {
        let n4 = e4.length;
        for (; n4--; ) e4[n4].attr(t4, null);
      });
    })(s2, e2, o2), { schema: r2, addNodeFilter: s2.addNodeFilter, addAttributeFilter: s2.addAttributeFilter, serialize: (n3, a2 = {}) => {
      const i3 = { format: "html", ...a2 }, l2 = ((e3, t3, n4) => ((e4, t4) => C(e4) && e4.hasEventListeners("PreProcess") && !t4.no_events)(e3, n4) ? ((e4, t4, n5) => {
        let o3;
        const r3 = e4.dom;
        let s3 = t4.cloneNode(true);
        const a3 = document.implementation;
        if (a3.createHTMLDocument) {
          const e5 = a3.createHTMLDocument("");
          Tt.each("BODY" === s3.nodeName ? s3.childNodes : [s3], (t5) => {
            e5.body.appendChild(e5.importNode(t5, true));
          }), s3 = "BODY" !== s3.nodeName ? e5.body.firstChild : e5.body, o3 = r3.doc, r3.doc = e5;
        }
        return ((e5, t5) => {
          e5.dispatch("PreProcess", t5);
        })(e4, { ...n5, node: s3 }), o3 && (r3.doc = o3), s3;
      })(e3, t3, n4) : t3)(t2, n3, i3), d2 = ((e3, t3, n4) => {
        const o3 = Nr(n4.getInner ? t3.innerHTML : e3.getOuterHTML(t3));
        return n4.selection || cr(mn(t3)) ? o3 : Tt.trim(o3);
      })(o2, l2, i3), c2 = ((e3, t3, n4) => {
        const o3 = n4.selection ? { forced_root_block: false, ...n4 } : n4, r3 = e3.parse(t3, o3);
        return ((e4) => {
          const t4 = (e5) => "br" === (null == e5 ? void 0 : e5.name), n5 = e4.lastChild;
          if (t4(n5)) {
            const e5 = n5.prev;
            t4(e5) && (n5.remove(), e5.remove());
          }
        })(r3), r3;
      })(s2, d2, i3);
      return "tree" === i3.format ? c2 : ((e3, t3, n4, o3, r3) => {
        const s3 = ((e4, t4, n5) => ig(e4, t4).serialize(n5))(t3, n4, o3);
        return ((e4, t4, n5) => {
          if (!t4.no_events && e4) {
            const o4 = ((e5, t5) => e5.dispatch("PostProcess", t5))(e4, { ...t4, content: n5 });
            return o4.content;
          }
          return n5;
        })(e3, r3, s3);
      })(t2, e2, r2, c2, i3);
    }, addRules: r2.addValidElements, setRules: r2.setValidElements, addTempAttr: O(jy, s2, n2), getTempAttrs: N(n2), getNodeFilters: s2.getNodeFilters, getAttributeFilters: s2.getAttributeFilters, removeNodeFilter: s2.removeNodeFilter, removeAttributeFilter: s2.removeAttributeFilter };
  }, $y = (e2, t2) => {
    const n2 = Hy(e2, t2);
    return { schema: n2.schema, addNodeFilter: n2.addNodeFilter, addAttributeFilter: n2.addAttributeFilter, serialize: n2.serialize, addRules: n2.addRules, setRules: n2.setRules, addTempAttr: n2.addTempAttr, getTempAttrs: n2.getTempAttrs, getNodeFilters: n2.getNodeFilters, getAttributeFilters: n2.getAttributeFilters, removeNodeFilter: n2.removeNodeFilter, removeAttributeFilter: n2.removeAttributeFilter };
  }, Vy = (e2, t2, n2 = {}) => {
    const o2 = ((e3, t3) => ({ format: "html", ...e3, set: true, content: t3 }))(n2, t2);
    return oy(e2, o2).map((t3) => {
      const n3 = ((e3, t4, n4) => Dy(e3).editor.setContent(t4, n4))(e2, t3.content, t3);
      return ry(e2, n3.html, t3), n3.content;
    }).getOr(t2);
  }, qy = "autoresize_on_init,content_editable_state,padd_empty_with_br,block_elements,boolean_attributes,editor_deselector,editor_selector,elements,file_browser_callback_types,filepicker_validator_handler,force_hex_style_colors,force_p_newlines,gecko_spellcheck,images_dataimg_filter,media_scripts,mode,move_caret_before_on_enter_elements,non_empty_elements,self_closing_elements,short_ended_elements,special,spellchecker_select_languages,spellchecker_whitelist,tab_focus,tabfocus_elements,table_responsive_width,text_block_elements,text_inline_elements,toolbar_drawer,types,validate,whitespace_elements,paste_enable_default_filters,paste_filter_drop,paste_word_valid_elements,paste_retain_style_properties,paste_convert_word_fake_lists".split(","), Wy = "bbcode,colorpicker,contextmenu,fullpage,legacyoutput,spellchecker,textcolor".split(","), Ky = (e2) => {
    const t2 = K(qy, (t3) => xe(e2, t3)), n2 = e2.forced_root_block;
    return false !== n2 && "" !== n2 || t2.push("forced_root_block (false only)"), se(t2);
  }, Gy = (e2) => {
    const t2 = Tt.makeMap(e2.plugins, " "), n2 = K(Wy, (e3) => xe(t2, e3));
    return se(n2);
  }, Yy = ta.DOM, Xy = (e2) => M.from(e2).each((e3) => e3.destroy()), Qy = /* @__PURE__ */ (() => {
    const e2 = {};
    return { add: (t2, n2) => {
      e2[t2] = n2;
    }, get: (t2) => e2[t2] ? e2[t2] : { icons: {} }, has: (t2) => xe(e2, t2) };
  })(), Jy = da.ModelManager, Zy = (e2, t2) => t2.dom[e2], eC = (e2, t2) => parseInt(Vn(t2, e2), 10), tC = O(Zy, "clientWidth"), nC = O(Zy, "clientHeight"), oC = O(eC, "margin-top"), rC = O(eC, "margin-left"), sC = (e2) => {
    const t2 = [], n2 = () => {
      const t3 = e2.theme;
      return t3 && t3.getNotificationManagerImpl ? t3.getNotificationManagerImpl() : /* @__PURE__ */ (() => {
        const e3 = () => {
          throw new Error("Theme did not provide a NotificationManager implementation.");
        };
        return { open: e3, close: e3, getArgs: e3 };
      })();
    }, o2 = () => M.from(t2[0]), r2 = () => {
      V(t2, (e3) => {
        e3.reposition();
      });
    }, s2 = (e3) => {
      J(t2, (t3) => t3 === e3).each((e4) => {
        t2.splice(e4, 1);
      });
    }, a2 = (a3, i4 = true) => e2.removed || !((e3) => {
      return (t3 = e3.inline ? e3.getBody() : e3.getContentAreaContainer(), M.from(t3).map(mn)).map(zn).getOr(false);
      var t3;
    })(e2) ? {} : (i4 && e2.dispatch("BeforeOpenNotification", { notification: a3 }), Q(t2, (e3) => {
      return t3 = n2().getArgs(e3), o3 = a3, !(t3.type !== o3.type || t3.text !== o3.text || t3.progressBar || t3.timeout || o3.progressBar || o3.timeout);
      var t3, o3;
    }).getOrThunk(() => {
      e2.editorManager.setActive(e2);
      const i5 = n2().open(a3, () => {
        s2(i5), r2(), o2().fold(() => e2.focus(), (e3) => bf(mn(e3.getEl())));
      });
      return ((e3) => {
        t2.push(e3);
      })(i5), r2(), e2.dispatch("OpenNotification", { notification: { ...i5 } }), i5;
    })), i3 = N(t2);
    return ((e3) => {
      e3.on("SkinLoaded", () => {
        const t3 = Al(e3);
        t3 && a2({ text: t3, type: "warning", timeout: 0 }, false), r2();
      }), e3.on("show ResizeEditor ResizeWindow NodeChange", () => {
        requestAnimationFrame(r2);
      }), e3.on("remove", () => {
        V(t2.slice(), (e4) => {
          n2().close(e4);
        });
      });
    })(e2), { open: a2, close: () => {
      o2().each((e3) => {
        n2().close(e3), s2(e3), r2();
      });
    }, getNotifications: i3 };
  }, aC = da.PluginManager, iC = da.ThemeManager, lC = (e2) => {
    let t2 = [];
    const n2 = () => {
      const t3 = e2.theme;
      return t3 && t3.getWindowManagerImpl ? t3.getWindowManagerImpl() : /* @__PURE__ */ (() => {
        const e3 = () => {
          throw new Error("Theme did not provide a WindowManager implementation.");
        };
        return { open: e3, openUrl: e3, alert: e3, confirm: e3, close: e3 };
      })();
    }, o2 = (e3, t3) => (...n3) => t3 ? t3.apply(e3, n3) : void 0, r2 = (n3) => {
      ((t3) => {
        e2.dispatch("CloseWindow", { dialog: t3 });
      })(n3), t2 = K(t2, (e3) => e3 !== n3), 0 === t2.length && e2.focus();
    }, s2 = (n3) => {
      e2.editorManager.setActive(e2), Ef(e2), e2.ui.show();
      const o3 = n3();
      return ((n4) => {
        t2.push(n4), ((t3) => {
          e2.dispatch("OpenWindow", { dialog: t3 });
        })(n4);
      })(o3), o3;
    };
    return e2.on("remove", () => {
      V(t2, (e3) => {
        n2().close(e3);
      });
    }), { open: (e3, t3) => s2(() => n2().open(e3, t3, r2)), openUrl: (e3) => s2(() => n2().openUrl(e3, r2)), alert: (e3, t3, r3) => {
      const s3 = n2();
      s3.alert(e3, o2(r3 || s3, t3));
    }, confirm: (e3, t3, r3) => {
      const s3 = n2();
      s3.confirm(e3, o2(r3 || s3, t3));
    }, close: () => {
      M.from(t2[t2.length - 1]).each((e3) => {
        n2().close(e3), r2(e3);
      });
    } };
  }, dC = (e2, t2) => {
    e2.notificationManager.open({ type: "error", text: t2 });
  }, cC = (e2, t2) => {
    e2._skinLoaded ? dC(e2, t2) : e2.on("SkinLoaded", () => {
      dC(e2, t2);
    });
  }, uC = (e2, t2, n2) => {
    um(e2, t2, { message: n2 }), console.error(n2);
  }, mC = (e2, t2, n2) => n2 ? `Failed to load ${e2}: ${n2} from url ${t2}` : `Failed to load ${e2} url: ${t2}`, fC = (e2, ...t2) => {
    const n2 = window.console;
    n2 && (n2.error ? n2.error(e2, ...t2) : n2.log(e2, ...t2));
  }, gC = (e2, t2) => {
    const n2 = e2.editorManager.baseURL + "/skins/content", o2 = `content${e2.editorManager.suffix}.css`;
    return $2(t2, (t3) => ((e3) => /^[a-z0-9\-]+$/i.test(e3))(t3) && !e2.inline ? `${n2}/${t3}/${o2}` : e2.documentBaseURI.toAbsolute(t3));
  }, pC = L, hC = (e2, t2) => {
    const n2 = {};
    return { findAll: (o2, r2 = L) => {
      const s2 = K(((e3) => e3 ? de(e3.getElementsByTagName("img")) : [])(o2), (t3) => {
        const n3 = t3.src;
        return !t3.hasAttribute("data-mce-bogus") && !t3.hasAttribute("data-mce-placeholder") && !(!n3 || n3 === Nt.transparentSrc) && (ze(n3, "blob:") ? !e2.isUploaded(n3) && r2(t3) : !!ze(n3, "data:") && r2(t3));
      }), a2 = $2(s2, (e3) => {
        const o3 = e3.src;
        if (xe(n2, o3)) return n2[o3].then((t3) => m(t3) ? t3 : { image: e3, blobInfo: t3.blobInfo });
        {
          const r3 = ((e4, t3) => {
            const n3 = () => Promise.reject("Invalid data URI");
            if (ze(t3, "blob:")) {
              const s3 = e4.getByUri(t3);
              return C(s3) ? Promise.resolve(s3) : (o4 = t3, ze(o4, "blob:") ? ((e5) => fetch(e5).then((e6) => e6.ok ? e6.blob() : Promise.reject()).catch(() => Promise.reject(`Cannot convert ${e5} to Blob. Resource might not exist or is inaccessible.`)))(o4) : ze(o4, "data:") ? (r4 = o4, new Promise((e5, t4) => {
                Bv(r4).bind(({ type: e6, data: t5, base64Encoded: n4 }) => Dv(e6, t5, n4)).fold(() => t4("Invalid data URI"), e5);
              })) : Promise.reject("Unknown URI format")).then((t4) => Pv(t4).then((o5) => Mv(o5, false, (n4) => M.some(Iv(e4, t4, n4))).getOrThunk(n3)));
            }
            var o4, r4;
            return ze(t3, "data:") ? Fv(e4, t3).fold(n3, (e5) => Promise.resolve(e5)) : Promise.reject("Unknown image data format");
          })(t2, o3).then((t3) => (delete n2[o3], { image: e3, blobInfo: t3 })).catch((e4) => (delete n2[o3], e4));
          return n2[o3] = r3, r3;
        }
      });
      return Promise.all(a2);
    } };
  }, bC = () => {
    let e2 = {};
    const t2 = (e3, t3) => ({ status: e3, resultUri: t3 }), n2 = (t3) => t3 in e2;
    return { hasBlobUri: n2, getResultUri: (t3) => {
      const n3 = e2[t3];
      return n3 ? n3.resultUri : null;
    }, isPending: (t3) => !!n2(t3) && 1 === e2[t3].status, isUploaded: (t3) => !!n2(t3) && 2 === e2[t3].status, markPending: (n3) => {
      e2[n3] = t2(1, null);
    }, markUploaded: (n3, o2) => {
      e2[n3] = t2(2, o2);
    }, removeFailed: (t3) => {
      delete e2[t3];
    }, destroy: () => {
      e2 = {};
    } };
  };
  let vC = 0;
  const yC = (e2, t2) => {
    const n2 = {}, o2 = (e3, n3) => new Promise((o3, r3) => {
      const s3 = new XMLHttpRequest();
      s3.open("POST", t2.url), s3.withCredentials = t2.credentials, s3.upload.onprogress = (e4) => {
        n3(e4.loaded / e4.total * 100);
      }, s3.onerror = () => {
        r3("Image upload failed due to a XHR Transport error. Code: " + s3.status);
      }, s3.onload = () => {
        if (s3.status < 200 || s3.status >= 300) return void r3("HTTP Error: " + s3.status);
        const e4 = JSON.parse(s3.responseText);
        var n4, a4;
        e4 && m(e4.location) ? o3((n4 = t2.basePath, a4 = e4.location, n4 ? n4.replace(/\/$/, "") + "/" + a4.replace(/^\//, "") : a4)) : r3("Invalid JSON: " + s3.responseText);
      };
      const a3 = new FormData();
      a3.append("file", e3.blob(), e3.filename()), s3.send(a3);
    }), r2 = w(t2.handler) ? t2.handler : o2, s2 = (e3, t3) => ({ url: t3, blobInfo: e3, status: true }), a2 = (e3, t3) => ({ url: "", blobInfo: e3, status: false, error: t3 }), i3 = (e3, t3) => {
      Tt.each(n2[e3], (e4) => {
        e4(t3);
      }), delete n2[e3];
    };
    return { upload: (l2, d2) => t2.url || r2 !== o2 ? ((t3, o3) => (t3 = Tt.grep(t3, (t4) => !e2.isUploaded(t4.blobUri())), Promise.all(Tt.map(t3, (t4) => e2.isPending(t4.blobUri()) ? ((e3) => {
      const t5 = e3.blobUri();
      return new Promise((e4) => {
        n2[t5] = n2[t5] || [], n2[t5].push(e4);
      });
    })(t4) : ((t5, n3, o4) => (e2.markPending(t5.blobUri()), new Promise((r3) => {
      let l3, d3;
      try {
        const c2 = () => {
          l3 && (l3.close(), d3 = S);
        }, u2 = (n4) => {
          c2(), e2.markUploaded(t5.blobUri(), n4), i3(t5.blobUri(), s2(t5, n4)), r3(s2(t5, n4));
        }, f2 = (n4) => {
          c2(), e2.removeFailed(t5.blobUri()), i3(t5.blobUri(), a2(t5, n4)), r3(a2(t5, n4));
        };
        d3 = (e3) => {
          e3 < 0 || e3 > 100 || M.from(l3).orThunk(() => M.from(o4).map(B)).each((t6) => {
            l3 = t6, t6.progressBar.value(e3);
          });
        }, n3(t5, d3).then(u2, (e3) => {
          f2(m(e3) ? { message: e3 } : e3);
        });
      } catch (e3) {
        r3(a2(t5, e3));
      }
    })))(t4, r2, o3)))))(l2, d2) : new Promise((e3) => {
      e3([]);
    }) };
  }, CC = (e2) => () => e2.notificationManager.open({ text: e2.translate("Image uploading..."), type: "info", timeout: -1, progressBar: true }), wC = (e2, t2) => yC(t2, { url: cl(e2), basePath: ul(e2), credentials: ml(e2), handler: fl(e2) }), xC = (e2) => {
    const t2 = /* @__PURE__ */ (() => {
      let e3 = [];
      const t3 = (e4) => {
        if (!e4.blob || !e4.base64) throw new Error("blob and base64 representations of the image are required for BlobInfo to be created");
        const t4 = e4.id || "blobid" + vC++ + (() => {
          const e5 = () => Math.round(4294967295 * Math.random()).toString(36);
          return "s" + (/* @__PURE__ */ new Date()).getTime().toString(36) + e5() + e5() + e5();
        })(), n4 = e4.name || t4, o4 = e4.blob;
        var r3;
        return { id: N(t4), name: N(n4), filename: N(e4.filename || n4 + "." + (r3 = o4.type, { "image/jpeg": "jpg", "image/jpg": "jpg", "image/gif": "gif", "image/png": "png", "image/apng": "apng", "image/avif": "avif", "image/svg+xml": "svg", "image/webp": "webp", "image/bmp": "bmp", "image/tiff": "tiff" }[r3.toLowerCase()] || "dat")), blob: N(o4), base64: N(e4.base64), blobUri: N(e4.blobUri || URL.createObjectURL(o4)), uri: N(e4.uri) };
      }, n3 = (t4) => Q(e3, t4).getOrUndefined(), o3 = (e4) => n3((t4) => t4.id() === e4);
      return { create: (e4, n4, o4, r3, s3) => {
        if (m(e4)) return t3({ id: e4, name: r3, filename: s3, blob: n4, base64: o4 });
        if (f(e4)) return t3(e4);
        throw new Error("Unknown input type");
      }, add: (t4) => {
        o3(t4.id()) || e3.push(t4);
      }, get: o3, getByUri: (e4) => n3((t4) => t4.blobUri() === e4), getByData: (e4, t4) => n3((n4) => n4.base64() === e4 && n4.blob().type === t4), findFirst: n3, removeByUri: (t4) => {
        e3 = K(e3, (e4) => e4.blobUri() !== t4 || (URL.revokeObjectURL(e4.blobUri()), false));
      }, destroy: () => {
        V(e3, (e4) => {
          URL.revokeObjectURL(e4.blobUri());
        }), e3 = [];
      } };
    })();
    let n2, o2;
    const r2 = bC(), s2 = [], a2 = (t3) => (n3) => e2.selection ? t3(n3) : [], i3 = (e3, t3, n3) => {
      let o3 = 0;
      do {
        o3 = e3.indexOf(t3, o3), -1 !== o3 && (e3 = e3.substring(0, o3) + n3 + e3.substr(o3 + t3.length), o3 += n3.length - t3.length + 1);
      } while (-1 !== o3);
      return e3;
    }, l2 = (e3, t3, n3) => {
      const o3 = `src="${n3}"${n3 === Nt.transparentSrc ? ' data-mce-placeholder="1"' : ""}`;
      return e3 = i3(e3, `src="${t3}"`, o3), i3(e3, 'data-mce-src="' + t3 + '"', 'data-mce-src="' + n3 + '"');
    }, d2 = (t3, n3) => {
      V(e2.undoManager.data, (e3) => {
        "fragmented" === e3.type ? e3.fragments = $2(e3.fragments, (e4) => l2(e4, t3, n3)) : e3.content = l2(e3.content, t3, n3);
      });
    }, c2 = () => (n2 || (n2 = wC(e2, r2)), p2().then(a2((o3) => {
      const r3 = $2(o3, (e3) => e3.blobInfo);
      return n2.upload(r3, CC(e2)).then(a2((n3) => {
        const r4 = [];
        let s3 = false;
        const a3 = $2(n3, (n4, a4) => {
          const { blobInfo: i4, image: l3 } = o3[a4];
          let c3 = false;
          return n4.status && il(e2) ? (n4.url && !Ue(l3.src, n4.url) && (s3 = true), t2.removeByUri(l3.src), By(e2) || ((t3, n5) => {
            const o4 = e2.convertURL(n5, "src");
            var r5;
            d2(t3.src, n5), qt(mn(t3), { src: al(e2) ? (r5 = n5, r5 + (-1 === r5.indexOf("?") ? "?" : "&") + (/* @__PURE__ */ new Date()).getTime()) : n5, "data-mce-src": o4 });
          })(l3, n4.url)) : n4.error && (n4.error.remove && (d2(l3.src, Nt.transparentSrc), r4.push(l3), c3 = true), ((e3, t3) => {
            cC(e3, la.translate(["Failed to upload image: {0}", t3]));
          })(e2, n4.error.message)), { element: l3, status: n4.status, uploadUri: n4.url, blobInfo: i4, removed: c3 };
        });
        return r4.length > 0 && !By(e2) ? e2.undoManager.transact(() => {
          V(r4, (n4) => {
            e2.dom.remove(n4), t2.removeByUri(n4.src);
          });
        }) : s3 && e2.undoManager.dispatchChange(), a3;
      }));
    }))), u2 = () => sl(e2) ? c2() : Promise.resolve([]), g2 = (e3) => te(s2, (t3) => t3(e3)), p2 = () => (o2 || (o2 = hC(r2, t2)), o2.findAll(e2.getBody(), g2).then(a2((t3) => {
      const n3 = K(t3, (t4) => !m(t4) || (cC(e2, t4), false));
      return By(e2) || V(n3, (e3) => {
        d2(e3.image.src, e3.blobInfo.blobUri()), e3.image.src = e3.blobInfo.blobUri(), e3.image.removeAttribute("data-mce-src");
      }), n3;
    }))), h2 = (n3) => n3.replace(/src="(blob:[^"]+)"/g, (n4, o3) => {
      const s3 = r2.getResultUri(o3);
      if (s3) return 'src="' + s3 + '"';
      let a3 = t2.getByUri(o3);
      return a3 || (a3 = Y(e2.editorManager.get(), (e3, t3) => e3 || t3.editorUpload && t3.editorUpload.blobCache.getByUri(o3), void 0)), a3 ? 'src="data:' + a3.blob().type + ";base64," + a3.base64() + '"' : n4;
    });
    return e2.on("SetContent", () => {
      sl(e2) ? u2() : p2();
    }), e2.on("RawSaveContent", (e3) => {
      e3.content = h2(e3.content);
    }), e2.on("GetContent", (e3) => {
      e3.source_view || "raw" === e3.format || "tree" === e3.format || (e3.content = h2(e3.content));
    }), e2.on("PostRender", () => {
      e2.parser.addNodeFilter("img", (e3) => {
        V(e3, (e4) => {
          const n3 = e4.attr("src");
          if (!n3 || t2.getByUri(n3)) return;
          const o3 = r2.getResultUri(n3);
          o3 && e4.attr("src", o3);
        });
      });
    }), { blobCache: t2, addFilter: (e3) => {
      s2.push(e3);
    }, uploadImages: c2, uploadImagesAuto: u2, scanForImages: p2, destroy: () => {
      t2.destroy(), r2.destroy(), o2 = n2 = null;
    } };
  }, kC = { remove_similar: true, inherit: false }, SC = { selector: "td,th", ...kC }, _C = { tablecellbackgroundcolor: { styles: { backgroundColor: "%value" }, ...SC }, tablecellverticalalign: { styles: { "vertical-align": "%value" }, ...SC }, tablecellbordercolor: { styles: { borderColor: "%value" }, ...SC }, tablecellclass: { classes: ["%value"], ...SC }, tableclass: { selector: "table", classes: ["%value"], ...kC }, tablecellborderstyle: { styles: { borderStyle: "%value" }, ...SC }, tablecellborderwidth: { styles: { borderWidth: "%value" }, ...SC } }, EC = N(_C), NC = Tt.each, RC = ta.DOM, AC = (e2) => C(e2) && f(e2), OC = (e2, t2) => {
    const n2 = t2 && t2.schema || Ls({}), o2 = (e3) => {
      const t3 = m(e3) ? { name: e3, classes: [], attrs: {} } : e3, n3 = RC.create(t3.name);
      return ((e4, t4) => {
        t4.classes.length > 0 && RC.addClass(e4, t4.classes.join(" ")), RC.setAttribs(e4, t4.attrs);
      })(n3, t3), n3;
    }, r2 = (e3, t3, s3) => {
      let a2;
      const i3 = t3[0], l2 = AC(i3) ? i3.name : void 0, d2 = ((e4, t4) => {
        const o3 = n2.getElementRule(e4.nodeName.toLowerCase()), r3 = null == o3 ? void 0 : o3.parentsRequired;
        return !(!r3 || !r3.length) && (t4 && j(r3, t4) ? t4 : r3[0]);
      })(e3, l2);
      if (d2) l2 === d2 ? (a2 = i3, t3 = t3.slice(1)) : a2 = d2;
      else if (i3) a2 = i3, t3 = t3.slice(1);
      else if (!s3) return e3;
      const c2 = a2 ? o2(a2) : RC.create("div");
      c2.appendChild(e3), s3 && Tt.each(s3, (t4) => {
        const n3 = o2(t4);
        c2.insertBefore(n3, e3);
      });
      const u2 = AC(a2) ? a2.siblings : void 0;
      return r2(c2, t3, u2);
    }, s2 = RC.create("div");
    if (e2.length > 0) {
      const t3 = e2[0], n3 = o2(t3), a2 = AC(t3) ? t3.siblings : void 0;
      s2.appendChild(r2(n3, e2.slice(1), a2));
    }
    return s2;
  }, TC = (e2) => {
    let t2 = "div";
    const n2 = { name: t2, classes: [], attrs: {}, selector: e2 = Tt.trim(e2) };
    return "*" !== e2 && (t2 = e2.replace(/(?:([#\.]|::?)([\w\-]+)|(\[)([^\]]+)\]?)/g, (e3, t3, o2, r2, s2) => {
      switch (t3) {
        case "#":
          n2.attrs.id = o2;
          break;
        case ".":
          n2.classes.push(o2);
          break;
        case ":":
          -1 !== Tt.inArray("checked disabled enabled read-only required".split(" "), o2) && (n2.attrs[o2] = o2);
      }
      if ("[" === r2) {
        const e4 = s2.match(/([\w\-]+)(?:\=\"([^\"]+))?/);
        e4 && (n2.attrs[e4[1]] = e4[2]);
      }
      return "";
    })), n2.name = t2 || "div", n2;
  }, BC = (e2, t2) => {
    let n2 = "", o2 = Ml(e2);
    if ("" === o2) return "";
    const r2 = (e3) => m(e3) ? e3.replace(/%(\w+)/g, "") : "", s2 = (t3, n3) => RC.getStyle(null != n3 ? n3 : e2.getBody(), t3, true);
    if (m(t2)) {
      const n3 = e2.formatter.get(t2);
      if (!n3) return "";
      t2 = n3[0];
    }
    if ("preview" in t2) {
      const e3 = t2.preview;
      if (false === e3) return "";
      o2 = e3 || o2;
    }
    let a2, i3 = t2.block || t2.inline || "span";
    const l2 = (d2 = t2.selector, m(d2) ? (d2 = (d2 = d2.split(/\s*,\s*/)[0]).replace(/\s*(~\+|~|\+|>)\s*/g, "$1"), Tt.map(d2.split(/(?:>|\s+(?![^\[\]]+\]))/), (e3) => {
      const t3 = Tt.map(e3.split(/(?:~\+|~|\+)/), TC), n3 = t3.pop();
      return t3.length && (n3.siblings = t3), n3;
    }).reverse()) : []);
    var d2;
    l2.length > 0 ? (l2[0].name || (l2[0].name = i3), i3 = t2.selector, a2 = OC(l2, e2)) : a2 = OC([i3], e2);
    const c2 = RC.select(i3, a2)[0] || a2.firstChild;
    NC(t2.styles, (e3, t3) => {
      const n3 = r2(e3);
      n3 && RC.setStyle(c2, t3, n3);
    }), NC(t2.attributes, (e3, t3) => {
      const n3 = r2(e3);
      n3 && RC.setAttrib(c2, t3, n3);
    }), NC(t2.classes, (e3) => {
      const t3 = r2(e3);
      RC.hasClass(c2, t3) || RC.addClass(c2, t3);
    }), e2.dispatch("PreviewFormats"), RC.setStyles(a2, { position: "absolute", left: -65535 }), e2.getBody().appendChild(a2);
    const u2 = s2("fontSize"), f2 = /px$/.test(u2) ? parseInt(u2, 10) : 0;
    return NC(o2.split(" "), (e3) => {
      let t3 = s2(e3, c2);
      if (!("background-color" === e3 && /transparent|rgba\s*\([^)]+,\s*0\)/.test(t3) && (t3 = s2(e3), "#ffffff" === du(t3).toLowerCase()) || "color" === e3 && "#000000" === du(t3).toLowerCase())) {
        if ("font-size" === e3 && /em|%$/.test(t3)) {
          if (0 === f2) return;
          t3 = parseFloat(t3) / (/%$/.test(t3) ? 100 : 1) * f2 + "px";
        }
        "border" === e3 && t3 && (n2 += "padding:0 2px;"), n2 += e3 + ":" + t3 + ";";
      }
    }), e2.dispatch("AfterPreviewFormats"), RC.remove(a2), n2;
  }, DC = (e2) => {
    const t2 = ((e3) => {
      const t3 = {}, n3 = (e4, o2) => {
        e4 && (m(e4) ? (p(o2) || (o2 = [o2]), V(o2, (e5) => {
          v(e5.deep) && (e5.deep = !Uu(e5)), v(e5.split) && (e5.split = !Uu(e5) || zu(e5)), v(e5.remove) && Uu(e5) && !zu(e5) && (e5.remove = "none"), Uu(e5) && zu(e5) && (e5.mixed = true, e5.block_expand = true), m(e5.classes) && (e5.classes = e5.classes.split(/\s+/));
        }), t3[e4] = o2) : fe(e4, (e5, t4) => {
          n3(t4, e5);
        }));
      };
      return n3(((e4) => {
        const t4 = e4.dom, n4 = e4.schema.type, o2 = { valigntop: [{ selector: "td,th", styles: { verticalAlign: "top" } }], valignmiddle: [{ selector: "td,th", styles: { verticalAlign: "middle" } }], valignbottom: [{ selector: "td,th", styles: { verticalAlign: "bottom" } }], alignleft: [{ selector: "figure.image", collapsed: false, classes: "align-left", ceFalseOverride: true, preview: "font-family font-size" }, { selector: "figure,p,h1,h2,h3,h4,h5,h6,td,th,tr,div,ul,ol,li,pre", styles: { textAlign: "left" }, inherit: false, preview: false }, { selector: "img,audio,video", collapsed: false, styles: { float: "left" }, preview: "font-family font-size" }, { selector: "table", collapsed: false, styles: { marginLeft: "0px", marginRight: "auto" }, onformat: (e5) => {
          t4.setStyle(e5, "float", null);
        }, preview: "font-family font-size" }, { selector: ".mce-preview-object,[data-ephox-embed-iri]", ceFalseOverride: true, styles: { float: "left" } }], aligncenter: [{ selector: "figure,p,h1,h2,h3,h4,h5,h6,td,th,tr,div,ul,ol,li,pre", styles: { textAlign: "center" }, inherit: false, preview: "font-family font-size" }, { selector: "figure.image", collapsed: false, classes: "align-center", ceFalseOverride: true, preview: "font-family font-size" }, { selector: "img,audio,video", collapsed: false, styles: { display: "block", marginLeft: "auto", marginRight: "auto" }, preview: false }, { selector: "table", collapsed: false, styles: { marginLeft: "auto", marginRight: "auto" }, preview: "font-family font-size" }, { selector: ".mce-preview-object", ceFalseOverride: true, styles: { display: "table", marginLeft: "auto", marginRight: "auto" }, preview: false }, { selector: "[data-ephox-embed-iri]", ceFalseOverride: true, styles: { marginLeft: "auto", marginRight: "auto" }, preview: false }], alignright: [{ selector: "figure.image", collapsed: false, classes: "align-right", ceFalseOverride: true, preview: "font-family font-size" }, { selector: "figure,p,h1,h2,h3,h4,h5,h6,td,th,tr,div,ul,ol,li,pre", styles: { textAlign: "right" }, inherit: false, preview: "font-family font-size" }, { selector: "img,audio,video", collapsed: false, styles: { float: "right" }, preview: "font-family font-size" }, { selector: "table", collapsed: false, styles: { marginRight: "0px", marginLeft: "auto" }, onformat: (e5) => {
          t4.setStyle(e5, "float", null);
        }, preview: "font-family font-size" }, { selector: ".mce-preview-object,[data-ephox-embed-iri]", ceFalseOverride: true, styles: { float: "right" }, preview: false }], alignjustify: [{ selector: "figure,p,h1,h2,h3,h4,h5,h6,td,th,tr,div,ul,ol,li,pre", styles: { textAlign: "justify" }, inherit: false, preview: "font-family font-size" }], bold: [{ inline: "strong", remove: "all", preserve_attributes: ["class", "style"] }, { inline: "span", styles: { fontWeight: "bold" } }, { inline: "b", remove: "all", preserve_attributes: ["class", "style"] }], italic: [{ inline: "em", remove: "all", preserve_attributes: ["class", "style"] }, { inline: "span", styles: { fontStyle: "italic" } }, { inline: "i", remove: "all", preserve_attributes: ["class", "style"] }], underline: [{ inline: "span", styles: { textDecoration: "underline" }, exact: true }, { inline: "u", remove: "all", preserve_attributes: ["class", "style"] }], strikethrough: /* @__PURE__ */ (() => {
          const e5 = { inline: "span", styles: { textDecoration: "line-through" }, exact: true }, t5 = { inline: "strike", remove: "all", preserve_attributes: ["class", "style"] }, o3 = { inline: "s", remove: "all", preserve_attributes: ["class", "style"] };
          return "html4" !== n4 ? [o3, e5, t5] : [e5, o3, t5];
        })(), forecolor: { inline: "span", styles: { color: "%value" }, links: true, remove_similar: true, clear_child_styles: true }, hilitecolor: { inline: "span", styles: { backgroundColor: "%value" }, links: true, remove_similar: true, clear_child_styles: true }, fontname: { inline: "span", toggle: false, styles: { fontFamily: "%value" }, clear_child_styles: true }, fontsize: { inline: "span", toggle: false, styles: { fontSize: "%value" }, clear_child_styles: true }, lineheight: { selector: "h1,h2,h3,h4,h5,h6,p,li,td,th,div", styles: { lineHeight: "%value" } }, fontsize_class: { inline: "span", attributes: { class: "%value" } }, blockquote: { block: "blockquote", wrapper: true, remove: "all" }, subscript: { inline: "sub" }, superscript: { inline: "sup" }, code: { inline: "code" }, link: { inline: "a", selector: "a", remove: "all", split: true, deep: true, onmatch: (e5, t5, n5) => yo(e5) && e5.hasAttribute("href"), onformat: (e5, n5, o3) => {
          Tt.each(o3, (n6, o4) => {
            t4.setAttrib(e5, o4, n6);
          });
        } }, lang: { inline: "span", clear_child_styles: true, remove_similar: true, attributes: { lang: "%value", "data-mce-lang": (e5) => {
          var t5;
          return null !== (t5 = null == e5 ? void 0 : e5.customValue) && void 0 !== t5 ? t5 : null;
        } } }, removeformat: [{ selector: "b,strong,em,i,font,u,strike,s,sub,sup,dfn,code,samp,kbd,var,cite,mark,q,del,ins,small", remove: "all", split: true, expand: false, block_expand: true, deep: true }, { selector: "span", attributes: ["style", "class"], remove: "empty", split: true, expand: false, deep: true }, { selector: "*", attributes: ["style", "class"], split: false, expand: false, deep: true }] };
        return Tt.each("p h1 h2 h3 h4 h5 h6 div address pre dt dd samp".split(/\s/), (e5) => {
          o2[e5] = { block: e5, remove: "all" };
        }), o2;
      })(e3)), n3(EC()), n3(Ll(e3)), { get: (e4) => C(e4) ? t3[e4] : t3, has: (e4) => xe(t3, e4), register: n3, unregister: (e4) => (e4 && t3[e4] && delete t3[e4], t3) };
    })(e2), n2 = ra({});
    return ((e3) => {
      e3.addShortcut("meta+b", "", "Bold"), e3.addShortcut("meta+i", "", "Italic"), e3.addShortcut("meta+u", "", "Underline");
      for (let t3 = 1; t3 <= 6; t3++) e3.addShortcut("access+" + t3, "", ["FormatBlock", false, "h" + t3]);
      e3.addShortcut("access+7", "", ["FormatBlock", false, "p"]), e3.addShortcut("access+8", "", ["FormatBlock", false, "div"]), e3.addShortcut("access+9", "", ["FormatBlock", false, "address"]);
    })(e2), ((e3) => {
      e3.on("mouseup keydown", (t3) => {
        ((e4, t4) => {
          const n3 = e4.selection, o2 = e4.getBody();
          Ih(e4, null, false), 8 !== t4 && 46 !== t4 || !n3.isCollapsed() || n3.getStart().innerHTML !== Bh || Ih(e4, jc(o2, n3.getStart())), 37 !== t4 && 39 !== t4 || Ih(e4, jc(o2, n3.getStart()));
        })(e3, t3.keyCode);
      });
    })(e2), By(e2) || ((e3, t3) => {
      e3.set({}), t3.on("NodeChange", (n3) => {
        Rb(t3, n3.element, e3.get());
      }), t3.on("FormatApply FormatRemove", (n3) => {
        const o2 = M.from(n3.node).map((e4) => wu(e4) ? e4 : e4.startContainer).bind((e4) => yo(e4) ? M.some(e4) : M.from(e4.parentElement)).getOrThunk(() => _b(t3));
        Rb(t3, o2, e3.get());
      });
    })(n2, e2), { get: t2.get, has: t2.has, register: t2.register, unregister: t2.unregister, apply: (t3, n3, o2) => {
      ((e3, t4, n4, o3) => {
        Py(e3).formatter.apply(t4, n4, o3);
      })(e2, t3, n3, o2);
    }, remove: (t3, n3, o2, r2) => {
      ((e3, t4, n4, o3, r3) => {
        Py(e3).formatter.remove(t4, n4, o3, r3);
      })(e2, t3, n3, o2, r2);
    }, toggle: (t3, n3, o2) => {
      ((e3, t4, n4, o3) => {
        Py(e3).formatter.toggle(t4, n4, o3);
      })(e2, t3, n3, o2);
    }, match: (t3, n3, o2, r2) => ((e3, t4, n4, o3, r3) => Py(e3).formatter.match(t4, n4, o3, r3))(e2, t3, n3, o2, r2), closest: (t3) => ((e3, t4) => Py(e3).formatter.closest(t4))(e2, t3), matchAll: (t3, n3) => ((e3, t4, n4) => Py(e3).formatter.matchAll(t4, n4))(e2, t3, n3), matchNode: (t3, n3, o2, r2) => ((e3, t4, n4, o3, r3) => Py(e3).formatter.matchNode(t4, n4, o3, r3))(e2, t3, n3, o2, r2), canApply: (t3) => ((e3, t4) => Py(e3).formatter.canApply(t4))(e2, t3), formatChanged: (t3, o2, r2, s2) => ((e3, t4, n3, o3, r3, s3) => Py(e3).formatter.formatChanged(t4, n3, o3, r3, s3))(e2, n2, t3, o2, r2, s2), getCssText: O(BC, e2) };
  }, PC = (e2) => {
    switch (e2.toLowerCase()) {
      case "undo":
      case "redo":
      case "mcefocus":
        return true;
      default:
        return false;
    }
  }, LC = (e2) => {
    const t2 = ua(), n2 = ra(0), o2 = ra(0), r2 = { data: [], typing: false, beforeChange: () => {
      ((e3, t3, n3) => {
        Py(e3).undoManager.beforeChange(t3, n3);
      })(e2, n2, t2);
    }, add: (s2, a2) => ((e3, t3, n3, o3, r3, s3, a3) => Py(e3).undoManager.add(t3, n3, o3, r3, s3, a3))(e2, r2, o2, n2, t2, s2, a2), dispatchChange: () => {
      e2.setDirty(true);
      const t3 = ky(e2);
      t3.bookmark = Ni(e2.selection), e2.dispatch("change", { level: t3, lastLevel: ae(r2.data, o2.get()).getOrUndefined() });
    }, undo: () => ((e3, t3, n3, o3) => Py(e3).undoManager.undo(t3, n3, o3))(e2, r2, n2, o2), redo: () => ((e3, t3, n3) => Py(e3).undoManager.redo(t3, n3))(e2, o2, r2.data), clear: () => {
      ((e3, t3, n3) => {
        Py(e3).undoManager.clear(t3, n3);
      })(e2, r2, o2);
    }, reset: () => {
      ((e3, t3) => {
        Py(e3).undoManager.reset(t3);
      })(e2, r2);
    }, hasUndo: () => ((e3, t3, n3) => Py(e3).undoManager.hasUndo(t3, n3))(e2, r2, o2), hasRedo: () => ((e3, t3, n3) => Py(e3).undoManager.hasRedo(t3, n3))(e2, r2, o2), transact: (t3) => ((e3, t4, n3, o3) => Py(e3).undoManager.transact(t4, n3, o3))(e2, r2, n2, t3), ignore: (t3) => {
      ((e3, t4, n3) => {
        Py(e3).undoManager.ignore(t4, n3);
      })(e2, n2, t3);
    }, extra: (t3, n3) => {
      ((e3, t4, n4, o3, r3) => {
        Py(e3).undoManager.extra(t4, n4, o3, r3);
      })(e2, r2, o2, t3, n3);
    } };
    return By(e2) || ((e3, t3, n3) => {
      const o3 = ra(false), r3 = (e4) => {
        Ay(t3, false, n3), t3.add({}, e4);
      };
      e3.on("init", () => {
        t3.add();
      }), e3.on("BeforeExecCommand", (e4) => {
        const o4 = e4.command;
        PC(o4) || (Oy(t3, n3), t3.beforeChange());
      }), e3.on("ExecCommand", (e4) => {
        const t4 = e4.command;
        PC(t4) || r3(e4);
      }), e3.on("ObjectResizeStart cut", () => {
        t3.beforeChange();
      }), e3.on("SaveContent ObjectResized blur", r3), e3.on("dragend", r3), e3.on("keyup", (n4) => {
        const s2 = n4.keyCode;
        n4.isDefaultPrevented() || ((s2 >= 33 && s2 <= 36 || s2 >= 37 && s2 <= 40 || 45 === s2 || n4.ctrlKey) && (r3(), e3.nodeChanged()), 46 !== s2 && 8 !== s2 || e3.nodeChanged(), o3.get() && t3.typing && !Ny(ky(e3), t3.data[0]) && (e3.isDirty() || e3.setDirty(true), e3.dispatch("TypingUndo"), o3.set(false), e3.nodeChanged()));
      }), e3.on("keydown", (e4) => {
        const s2 = e4.keyCode;
        if (e4.isDefaultPrevented()) return;
        if (s2 >= 33 && s2 <= 36 || s2 >= 37 && s2 <= 40 || 45 === s2) return void (t3.typing && r3(e4));
        const a2 = e4.ctrlKey && !e4.altKey || e4.metaKey;
        !(s2 < 16 || s2 > 20) || 224 === s2 || 91 === s2 || t3.typing || a2 || (t3.beforeChange(), Ay(t3, true, n3), t3.add({}, e4), o3.set(true));
      }), e3.on("mousedown", (e4) => {
        t3.typing && r3(e4);
      }), e3.on("input", (e4) => {
        var t4;
        e4.inputType && ("insertReplacementText" === e4.inputType || "insertText" === (t4 = e4).inputType && null === t4.data || ((e5) => "insertFromPaste" === e5.inputType || "insertFromDrop" === e5.inputType)(e4)) && r3(e4);
      }), e3.on("AddUndo Undo Redo ClearUndos", (t4) => {
        t4.isDefaultPrevented() || e3.nodeChanged();
      });
    })(e2, r2, n2), ((e3) => {
      e3.addShortcut("meta+z", "", "Undo"), e3.addShortcut("meta+y,meta+shift+z", "", "Redo");
    })(e2), r2;
  }, MC = [9, 27, bm.HOME, bm.END, 19, 20, 44, 144, 145, 33, 34, 45, 16, 17, 18, 91, 92, 93, bm.DOWN, bm.UP, bm.LEFT, bm.RIGHT].concat(Nt.browser.isFirefox() ? [224] : []), IC = "data-mce-placeholder", FC = (e2) => "keydown" === e2.type || "keyup" === e2.type, UC = (e2) => {
    const t2 = e2.keyCode;
    return t2 === bm.BACKSPACE || t2 === bm.DELETE;
  }, zC = (e2, t2) => ({ from: e2, to: t2 }), jC = (e2, t2) => {
    const n2 = mn(e2), o2 = mn(t2.container());
    return Sp(n2, o2).map((e3) => /* @__PURE__ */ ((e4, t3) => ({ block: e4, position: t3 }))(e3, t2));
  }, HC = (e2) => {
    const t2 = Nn(e2);
    return J(t2, nr).fold(N(t2), (e3) => t2.slice(0, e3));
  }, $C = (e2) => {
    const t2 = HC(e2);
    return V(t2, to), t2;
  }, VC = (e2, t2) => {
    const n2 = Rg(t2, e2);
    return Q(n2.reverse(), (e3) => ss(e3)).each(to);
  }, qC = (e2, t2, n2, o2) => {
    if (ss(n2)) return kr(n2), Ic(n2.dom);
    0 === K(_n(o2), (e3) => !ss(e3)).length && ss(t2) && Yn(o2, cn("br"));
    const r2 = Mc(n2.dom, ai.before(o2.dom));
    return V($C(t2), (e3) => {
      Yn(o2, e3);
    }), VC(e2, t2), r2;
  }, WC = (e2, t2, n2) => {
    if (ss(n2)) return to(n2), ss(t2) && kr(t2), Ic(t2.dom);
    const o2 = Fc(n2.dom);
    return V($C(t2), (e3) => {
      Jn(n2, e3);
    }), VC(e2, t2), o2;
  }, KC = (e2, t2) => {
    Pc(e2, t2.dom).bind((e3) => M.from(e3.getNode())).map(mn).filter(rr).each(to);
  }, GC = (e2, t2, n2) => (KC(true, t2), KC(false, n2), ((e3, t3) => vn(t3, e3) ? ((e4, t4) => {
    const n3 = Rg(t4, e4);
    return M.from(n3[n3.length - 1]);
  })(t3, e3) : M.none())(t2, n2).fold(O(WC, e2, t2, n2), O(qC, e2, t2, n2))), YC = (e2, t2, n2, o2) => t2 ? GC(e2, o2, n2) : GC(e2, n2, o2), XC = (e2, t2) => {
    const n2 = mn(e2.getBody()), o2 = ((e3, t3, n3) => n3.collapsed ? ((e4, t4, n4) => {
      const o3 = jC(e4, ai.fromRangeStart(n4)), r2 = o3.bind((n5) => Tc(t4, e4, n5.position).bind((n6) => jC(e4, n6).map((n7) => ((e5, t5, n8) => Po(n8.position.getNode()) && !ss(n8.block) ? Pc(false, n8.block.dom).bind((o4) => o4.isEqual(n8.position) ? Tc(t5, e5, o4).bind((t6) => jC(e5, t6)) : M.some(n8)).getOr(n8) : n8)(e4, t4, n7))));
      return Dt(o3, r2, zC).filter((e5) => ((e6) => !bn(e6.from.block, e6.to.block))(e5) && ((e6) => xn(e6.from.block).bind((t5) => xn(e6.to.block).filter((e7) => bn(t5, e7))).isSome())(e5) && ((e6) => false === Io(e6.from.block.dom) && false === Io(e6.to.block.dom))(e5));
    })(e3, t3, n3) : M.none())(n2.dom, t2, e2.selection.getRng()).map((o3) => () => {
      YC(n2, t2, o3.from.block, o3.to.block).each((t3) => {
        e2.selection.setRng(t3.toRange());
      });
    });
    return o2;
  }, QC = (e2, t2) => {
    const n2 = mn(t2), o2 = O(bn, e2);
    return Wo(n2, dr, o2).isSome();
  }, JC = (e2) => {
    const t2 = mn(e2.getBody());
    return ((e3, t3) => {
      const n2 = Mc(e3.dom, ai.fromRangeStart(t3)).isNone(), o2 = Lc(e3.dom, ai.fromRangeEnd(t3)).isNone();
      return !((e4, t4) => QC(e4, t4.startContainer) || QC(e4, t4.endContainer))(e3, t3) && n2 && o2;
    })(t2, e2.selection.getRng()) ? ((e3) => M.some(() => {
      e3.setContent(""), e3.selection.setCursorLocation();
    }))(e2) : ((e3, t3) => {
      const n2 = t3.getRng();
      return Dt(Sp(e3, mn(n2.startContainer)), Sp(e3, mn(n2.endContainer)), (o2, r2) => bn(o2, r2) ? M.none() : M.some(() => {
        n2.deleteContents(), YC(e3, true, o2, r2).each((e4) => {
          t3.setRng(e4.toRange());
        });
      })).getOr(M.none());
    })(t2, e2.selection);
  }, ZC = (e2, t2) => e2.selection.isCollapsed() ? M.none() : JC(e2), ew = (e2, t2, n2, o2, r2) => M.from(t2._selectionOverrides.showCaret(e2, n2, o2, r2)), tw = (e2, t2) => e2.dispatch("BeforeObjectSelected", { target: t2 }).isDefaultPrevented() ? M.none() : M.some(((e3) => {
    const t3 = e3.ownerDocument.createRange();
    return t3.selectNode(e3), t3;
  })(t2)), nw = (e2, t2, n2) => t2.collapsed ? ((e3, t3, n3) => {
    const o2 = cc(1, e3.getBody(), t3), r2 = ai.fromRangeStart(o2), s2 = r2.getNode();
    if (jd(s2)) return ew(1, e3, s2, !r2.isAtEnd(), false);
    const a2 = r2.getNode(true);
    if (jd(a2)) return ew(1, e3, a2, false, false);
    const i3 = Zp(e3.dom.getRoot(), r2.getNode());
    return jd(i3) ? ew(1, e3, i3, false, n3) : M.none();
  })(e2, t2, n2).getOr(t2) : t2, ow = (e2) => _g(e2) || wg(e2), rw = (e2) => Eg(e2) || xg(e2), sw = (e2, t2, n2, o2, r2, s2) => {
    ew(o2, e2, s2.getNode(!r2), r2, true).each((n3) => {
      if (t2.collapsed) {
        const e3 = t2.cloneRange();
        r2 ? e3.setEnd(n3.startContainer, n3.startOffset) : e3.setStart(n3.endContainer, n3.endOffset), e3.deleteContents();
      } else t2.deleteContents();
      e2.selection.setRng(n3);
    }), ((e3, t3) => {
      Ro(t3) && 0 === t3.data.length && e3.remove(t3);
    })(e2.dom, n2);
  }, aw = (e2, t2) => ((e3, t3) => {
    const n2 = e3.selection.getRng();
    if (!Ro(n2.commonAncestorContainer)) return M.none();
    const o2 = t3 ? hc.Forwards : hc.Backwards, r2 = Nc(e3.getBody()), s2 = O(gc, t3 ? r2.next : r2.prev), a2 = t3 ? ow : rw, i3 = mc(o2, e3.getBody(), n2), l2 = s2(i3), d2 = l2 ? vp(t3, l2) : l2;
    if (!d2 || !pc(i3, d2)) return M.none();
    if (a2(d2)) return M.some(() => sw(e3, n2, i3.getNode(), o2, t3, d2));
    const c2 = s2(d2);
    return c2 && a2(c2) && pc(d2, c2) ? M.some(() => sw(e3, n2, i3.getNode(), o2, t3, c2)) : M.none();
  })(e2, t2), iw = (e2, t2) => {
    const n2 = e2.getBody();
    return t2 ? Ic(n2).filter(_g) : Fc(n2).filter(Eg);
  }, lw = (e2) => {
    const t2 = e2.selection.getRng();
    return !t2.collapsed && (iw(e2, true).exists((e3) => e3.isEqual(ai.fromRangeStart(t2))) || iw(e2, false).exists((e3) => e3.isEqual(ai.fromRangeEnd(t2))));
  }, dw = Ti([{ remove: ["element"] }, { moveToElement: ["element"] }, { moveToPosition: ["position"] }]), cw = (e2, t2, n2) => Tc(t2, e2, n2).bind((o2) => {
    return r2 = o2.getNode(), C(r2) && (dr(mn(r2)) || ir(mn(r2))) || ((e3, t3, n3, o3) => {
      const r3 = (t4) => or(mn(t4)) && !oc(n3, o3, e3);
      return uc(!t3, n3).fold(() => uc(t3, o3).fold(P, r3), r3);
    })(e2, t2, n2, o2) ? M.none() : t2 && Io(o2.getNode()) || !t2 && Io(o2.getNode(true)) ? ((e3, t3, n3, o3) => {
      const r3 = o3.getNode(!t3);
      return Sp(mn(e3), mn(n3.getNode())).map((e4) => ss(e4) ? dw.remove(e4.dom) : dw.moveToElement(r3)).orThunk(() => M.some(dw.moveToElement(r3)));
    })(e2, t2, n2, o2) : t2 && Eg(n2) || !t2 && _g(n2) ? M.some(dw.moveToPosition(o2)) : M.none();
    var r2;
  }), uw = (e2, t2) => M.from(Zp(e2.getBody(), t2)), mw = (e2, t2) => {
    const n2 = e2.selection.getNode();
    return uw(e2, n2).filter(Io).fold(() => ((e3, t3, n3) => {
      const o2 = cc(t3 ? 1 : -1, e3, n3), r2 = ai.fromRangeStart(o2), s2 = mn(e3);
      return !t3 && Eg(r2) ? M.some(dw.remove(r2.getNode(true))) : t3 && _g(r2) ? M.some(dw.remove(r2.getNode())) : !t3 && _g(r2) && zg(s2, r2) ? jg(s2, r2).map((e4) => dw.remove(e4.getNode())) : t3 && Eg(r2) && Ug(s2, r2) ? Hg(s2, r2).map((e4) => dw.remove(e4.getNode())) : ((e4, t4, n4) => ((e5, t5) => {
        const n5 = t5.getNode(!e5), o3 = e5 ? "after" : "before";
        return yo(n5) && n5.getAttribute("data-mce-caret") === o3;
      })(t4, n4) ? ((e5, t5) => y(t5) ? M.none() : e5 && Io(t5.nextSibling) ? M.some(dw.moveToElement(t5.nextSibling)) : !e5 && Io(t5.previousSibling) ? M.some(dw.moveToElement(t5.previousSibling)) : M.none())(t4, n4.getNode(!t4)).orThunk(() => cw(e4, t4, n4)) : cw(e4, t4, n4).bind((t5) => ((e5, t6, n5) => n5.fold((e6) => M.some(dw.remove(e6)), (e6) => M.some(dw.moveToElement(e6)), (n6) => oc(t6, n6, e5) ? M.none() : M.some(dw.moveToPosition(n6))))(e4, n4, t5)))(e3, t3, r2);
    })(e2.getBody(), t2, e2.selection.getRng()).map((n3) => () => n3.fold(/* @__PURE__ */ ((e3, t3) => (n4) => (e3._selectionOverrides.hideFakeCaret(), gp(e3, t3, mn(n4)), true))(e2, t2), /* @__PURE__ */ ((e3, t3) => (n4) => {
      const o2 = t3 ? ai.before(n4) : ai.after(n4);
      return e3.selection.setRng(o2.toRange()), true;
    })(e2, t2), /* @__PURE__ */ ((e3) => (t3) => (e3.selection.setRng(t3.toRange()), true))(e2))), () => M.some(S));
  }, fw = (e2) => {
    const t2 = e2.dom, n2 = e2.selection, o2 = Zp(e2.getBody(), n2.getNode());
    if (Mo(o2) && t2.isBlock(o2) && t2.isEmpty(o2)) {
      const e3 = t2.create("br", { "data-mce-bogus": "1" });
      t2.setHTML(o2, ""), o2.appendChild(e3), n2.setRng(ai.before(e3).toRange());
    }
    return true;
  }, gw = (e2, t2) => e2.selection.isCollapsed() ? mw(e2, t2) : ((e3, t3) => {
    const n2 = e3.selection.getNode();
    return Io(n2) && !Fo(n2) ? uw(e3, n2.parentNode).filter(Io).fold(() => M.some(() => {
      var n3;
      n3 = mn(e3.getBody()), V(vr(n3, ".mce-offscreen-selection"), to), gp(e3, t3, mn(e3.selection.getNode())), _p(e3);
    }), () => M.some(S)) : lw(e3) ? M.some(() => {
      Np(e3, e3.selection.getRng(), mn(e3.getBody()));
    }) : M.none();
  })(e2, t2), pw = (e2, t2) => e2.selection.isCollapsed() ? ((e3, t3) => {
    const n2 = ai.fromRangeStart(e3.selection.getRng());
    return Tc(t3, e3.getBody(), n2).filter((e4) => t3 ? yg(e4) : Cg(e4)).bind((e4) => rc(t3 ? 0 : -1, e4)).map((t4) => () => e3.selection.select(t4));
  })(e2, t2) : M.none(), hw = Ro, bw = (e2) => hw(e2) && e2.data[0] === _r, vw = (e2) => hw(e2) && e2.data[e2.data.length - 1] === _r, yw = (e2) => {
    var t2;
    return (null !== (t2 = e2.ownerDocument) && void 0 !== t2 ? t2 : document).createTextNode(_r);
  }, Cw = (e2, t2) => e2 ? ((e3) => {
    var t3;
    if (hw(e3.previousSibling)) return vw(e3.previousSibling) || e3.previousSibling.appendData(_r), e3.previousSibling;
    if (hw(e3)) return bw(e3) || e3.insertData(0, _r), e3;
    {
      const n2 = yw(e3);
      return null === (t3 = e3.parentNode) || void 0 === t3 || t3.insertBefore(n2, e3), n2;
    }
  })(t2) : ((e3) => {
    var t3, n2;
    if (hw(e3.nextSibling)) return bw(e3.nextSibling) || e3.nextSibling.insertData(0, _r), e3.nextSibling;
    if (hw(e3)) return vw(e3) || e3.appendData(_r), e3;
    {
      const o2 = yw(e3);
      return e3.nextSibling ? null === (t3 = e3.parentNode) || void 0 === t3 || t3.insertBefore(o2, e3.nextSibling) : null === (n2 = e3.parentNode) || void 0 === n2 || n2.appendChild(o2), o2;
    }
  })(t2), ww = O(Cw, true), xw = O(Cw, false), kw = (e2, t2) => Ro(e2.container()) ? Cw(t2, e2.container()) : Cw(t2, e2.getNode()), Sw = (e2, t2) => {
    const n2 = t2.get();
    return n2 && e2.container() === n2 && Tr(n2);
  }, _w = (e2, t2) => t2.fold((t3) => {
    Pd(e2.get());
    const n2 = ww(t3);
    return e2.set(n2), M.some(ai(n2, n2.length - 1));
  }, (t3) => Ic(t3).map((t4) => {
    if (Sw(t4, e2)) {
      const t5 = e2.get();
      return ai(t5, 1);
    }
    {
      Pd(e2.get());
      const n2 = kw(t4, true);
      return e2.set(n2), ai(n2, 1);
    }
  }), (t3) => Fc(t3).map((t4) => {
    if (Sw(t4, e2)) {
      const t5 = e2.get();
      return ai(t5, t5.length - 1);
    }
    {
      Pd(e2.get());
      const n2 = kw(t4, false);
      return e2.set(n2), ai(n2, n2.length - 1);
    }
  }), (t3) => {
    Pd(e2.get());
    const n2 = xw(t3);
    return e2.set(n2), M.some(ai(n2, 1));
  }), Ew = (e2, t2) => {
    for (let n2 = 0; n2 < e2.length; n2++) {
      const o2 = e2[n2].apply(null, t2);
      if (o2.isSome()) return o2;
    }
    return M.none();
  }, Nw = Ti([{ before: ["element"] }, { start: ["element"] }, { end: ["element"] }, { after: ["element"] }]), Rw = (e2, t2) => nc(t2, e2) || e2, Aw = (e2, t2, n2) => {
    const o2 = yp(n2), r2 = Rw(t2, o2.container());
    return bp(e2, r2, o2).fold(() => Lc(r2, o2).bind(O(bp, e2, r2)).map((e3) => Nw.before(e3)), M.none);
  }, Ow = (e2, t2) => null === jc(e2, t2), Tw = (e2, t2, n2) => bp(e2, t2, n2).filter(O(Ow, t2)), Bw = (e2, t2, n2) => {
    const o2 = Cp(n2);
    return Tw(e2, t2, o2).bind((e3) => Mc(e3, o2).isNone() ? M.some(Nw.start(e3)) : M.none());
  }, Dw = (e2, t2, n2) => {
    const o2 = yp(n2);
    return Tw(e2, t2, o2).bind((e3) => Lc(e3, o2).isNone() ? M.some(Nw.end(e3)) : M.none());
  }, Pw = (e2, t2, n2) => {
    const o2 = Cp(n2), r2 = Rw(t2, o2.container());
    return bp(e2, r2, o2).fold(() => Mc(r2, o2).bind(O(bp, e2, r2)).map((e3) => Nw.after(e3)), M.none);
  }, Lw = (e2) => {
    return t2 = Iw(e2), !("rtl" === ta.DOM.getStyle(t2, "direction", true) || ((e3) => pp.test(e3))(null !== (n2 = t2.textContent) && void 0 !== n2 ? n2 : ""));
    var t2, n2;
  }, Mw = (e2, t2, n2) => Ew([Aw, Bw, Dw, Pw], [e2, t2, n2]).filter(Lw), Iw = (e2) => e2.fold(R, R, R, R), Fw = (e2) => e2.fold(N("before"), N("start"), N("end"), N("after")), Uw = (e2) => e2.fold(Nw.before, Nw.before, Nw.after, Nw.after), zw = (e2) => e2.fold(Nw.start, Nw.start, Nw.end, Nw.end), jw = (e2, t2, n2, o2, r2, s2) => Dt(bp(t2, n2, o2), bp(t2, n2, r2), (t3, o3) => t3 !== o3 && ((e3, t4, n3) => {
    const o4 = nc(t4, e3), r3 = nc(n3, e3);
    return C(o4) && o4 === r3;
  })(n2, t3, o3) ? Nw.after(e2 ? t3 : o3) : s2).getOr(s2), Hw = (e2, t2) => e2.fold(L, (e3) => {
    return o2 = t2, !(Fw(n2 = e3) === Fw(o2) && Iw(n2) === Iw(o2));
    var n2, o2;
  }), $w = (e2, t2) => e2 ? t2.fold(_(M.some, Nw.start), M.none, _(M.some, Nw.after), M.none) : t2.fold(M.none, _(M.some, Nw.before), M.none, _(M.some, Nw.end)), Vw = (e2, t2, n2) => {
    const o2 = e2 ? 1 : -1;
    return t2.setRng(ai(n2.container(), n2.offset() + o2).toRange()), t2.getSel().modify("move", e2 ? "forward" : "backward", "word"), true;
  };
  var qw;
  !function(e2) {
    e2[e2.Br = 0] = "Br", e2[e2.Block = 1] = "Block", e2[e2.Wrap = 2] = "Wrap", e2[e2.Eol = 3] = "Eol";
  }(qw || (qw = {}));
  const Ww = (e2, t2) => e2 === hc.Backwards ? ne(t2) : t2, Kw = (e2, t2, n2) => e2 === hc.Forwards ? t2.next(n2) : t2.prev(n2), Gw = (e2, t2, n2, o2) => Po(o2.getNode(t2 === hc.Forwards)) ? qw.Br : false === oc(n2, o2) ? qw.Block : qw.Wrap, Yw = (e2, t2, n2, o2) => {
    const r2 = Nc(n2);
    let s2 = o2;
    const a2 = [];
    for (; s2; ) {
      const n3 = Kw(t2, r2, s2);
      if (!n3) break;
      if (Po(n3.getNode(false))) return t2 === hc.Forwards ? { positions: Ww(t2, a2).concat([n3]), breakType: qw.Br, breakAt: M.some(n3) } : { positions: Ww(t2, a2), breakType: qw.Br, breakAt: M.some(n3) };
      if (n3.isVisible()) {
        if (e2(s2, n3)) {
          const e3 = Gw(0, t2, s2, n3);
          return { positions: Ww(t2, a2), breakType: e3, breakAt: M.some(n3) };
        }
        a2.push(n3), s2 = n3;
      } else s2 = n3;
    }
    return { positions: Ww(t2, a2), breakType: qw.Eol, breakAt: M.none() };
  }, Xw = (e2, t2, n2, o2) => t2(n2, o2).breakAt.map((o3) => {
    const r2 = t2(n2, o3).positions;
    return e2 === hc.Backwards ? r2.concat(o3) : [o3].concat(r2);
  }).getOr([]), Qw = (e2, t2) => Y(e2, (e3, n2) => e3.fold(() => M.some(n2), (o2) => Dt(ie(o2.getClientRects()), ie(n2.getClientRects()), (e4, r2) => {
    const s2 = Math.abs(t2 - e4.left);
    return Math.abs(t2 - r2.left) <= s2 ? n2 : o2;
  }).or(e3)), M.none()), Jw = (e2, t2) => ie(t2.getClientRects()).bind((t3) => Qw(e2, t3.left)), Zw = O(Yw, ai.isAbove, -1), ex = O(Yw, ai.isBelow, 1), tx = O(Xw, -1, Zw), nx = O(Xw, 1, ex), ox = (e2, t2) => Jw(tx(e2, t2), t2), rx = (e2, t2) => Jw(nx(e2, t2), t2), sx = Io, ax = (e2, t2) => Math.abs(e2.left - t2), ix = (e2, t2) => Math.abs(e2.right - t2), lx = (e2, t2) => Oe(e2, (e3, n2) => {
    const o2 = Math.min(ax(e3, t2), ix(e3, t2)), r2 = Math.min(ax(n2, t2), ix(n2, t2));
    return r2 === o2 && ke(n2, "node") && sx(n2.node) || r2 < o2 ? n2 : e3;
  }), dx = (e2) => {
    const t2 = (t3) => $2(t3, (t4) => {
      const n2 = Da(t4);
      return n2.node = e2, n2;
    });
    if (yo(e2)) return t2(e2.getClientRects());
    if (Ro(e2)) {
      const n2 = e2.ownerDocument.createRange();
      return n2.setStart(e2, 0), n2.setEnd(e2, e2.data.length), t2(n2.getClientRects());
    }
    return [];
  }, cx = (e2) => ee(e2, dx);
  var ux;
  !function(e2) {
    e2[e2.Up = -1] = "Up", e2[e2.Down = 1] = "Down";
  }(ux || (ux = {}));
  const mx = (e2, t2, n2, o2, r2, s2) => {
    let a2 = 0;
    const i3 = [], l2 = (o3) => {
      let s3 = cx([o3]);
      -1 === e2 && (s3 = s3.reverse());
      for (let e3 = 0; e3 < s3.length; e3++) {
        const o4 = s3[e3];
        if (!n2(o4, d2)) {
          if (i3.length > 0 && t2(o4, Be(i3)) && a2++, o4.line = a2, r2(o4)) return true;
          i3.push(o4);
        }
      }
      return false;
    }, d2 = Be(s2.getClientRects());
    if (!d2) return i3;
    const c2 = s2.getNode();
    return c2 && (l2(c2), ((e3, t3, n3, o3) => {
      let r3 = o3;
      for (; r3 = tc(r3, e3, Xr, t3); ) if (n3(r3)) return;
    })(e2, o2, l2, c2)), i3;
  }, fx = O(mx, ux.Up, Ma, Ia), gx = O(mx, ux.Down, Ia, Ma), px = (e2) => Be(e2.getClientRects()), hx = (e2) => (t2) => ((e3, t3) => t3.line > e3)(e2, t2), bx = (e2) => (t2) => ((e3, t3) => t3.line === e3)(e2, t2), vx = (e2, t2) => {
    e2.selection.setRng(t2), hf(e2, e2.selection.getRng());
  }, yx = (e2, t2, n2) => M.some(nw(e2, t2, n2)), Cx = (e2, t2, n2, o2, r2, s2) => {
    const a2 = t2 === hc.Forwards, i3 = Nc(e2.getBody()), l2 = O(gc, a2 ? i3.next : i3.prev), d2 = a2 ? o2 : r2;
    if (!n2.collapsed) {
      const o3 = Ua(n2);
      if (s2(o3)) return ew(t2, e2, o3, t2 === hc.Backwards, false);
      if (lw(e2)) {
        const e3 = n2.cloneRange();
        return e3.collapse(t2 === hc.Backwards), M.from(e3);
      }
    }
    const c2 = mc(t2, e2.getBody(), n2);
    if (d2(c2)) return tw(e2, c2.getNode(!a2));
    let u2 = l2(c2);
    const m2 = Ur(n2);
    if (!u2) return m2 ? M.some(n2) : M.none();
    if (u2 = vp(a2, u2), d2(u2)) return ew(t2, e2, u2.getNode(!a2), a2, false);
    const f2 = l2(u2);
    return f2 && d2(f2) && pc(u2, f2) ? ew(t2, e2, f2.getNode(!a2), a2, false) : m2 ? yx(e2, u2.toRange(), false) : M.none();
  }, wx = (e2, t2, n2, o2, r2, s2) => {
    const a2 = mc(t2, e2.getBody(), n2), i3 = Be(a2.getClientRects()), l2 = t2 === ux.Down, d2 = e2.getBody();
    if (!i3) return M.none();
    if (lw(e2)) {
      const e3 = l2 ? ai.fromRangeEnd(n2) : ai.fromRangeStart(n2);
      return (l2 ? rx : ox)(d2, e3).orThunk(() => M.from(e3)).map((e4) => e4.toRange());
    }
    const c2 = (l2 ? gx : fx)(d2, hx(1), a2), u2 = K(c2, bx(1)), m2 = i3.left, f2 = lx(u2, m2);
    if (f2 && s2(f2.node)) {
      const n3 = Math.abs(m2 - f2.left), o3 = Math.abs(m2 - f2.right);
      return ew(t2, e2, f2.node, n3 < o3, false);
    }
    let g2;
    if (g2 = o2(a2) ? a2.getNode() : r2(a2) ? a2.getNode(true) : Ua(n2), g2) {
      const n3 = ((e3, t3, n4, o4) => {
        const r3 = Nc(t3);
        let s3, a3, i4, l3;
        const d3 = [];
        let c3 = 0;
        1 === e3 ? (s3 = r3.next, a3 = Ia, i4 = Ma, l3 = ai.after(o4)) : (s3 = r3.prev, a3 = Ma, i4 = Ia, l3 = ai.before(o4));
        const u3 = px(l3);
        do {
          if (!l3.isVisible()) continue;
          const e4 = px(l3);
          if (i4(e4, u3)) continue;
          d3.length > 0 && a3(e4, Be(d3)) && c3++;
          const t4 = Da(e4);
          if (t4.position = l3, t4.line = c3, n4(t4)) return d3;
          d3.push(t4);
        } while (l3 = s3(l3));
        return d3;
      })(t2, d2, hx(1), g2);
      let o3 = lx(K(n3, bx(1)), m2);
      if (o3) return yx(e2, o3.position.toRange(), false);
      if (o3 = Be(K(n3, bx(0))), o3) return yx(e2, o3.position.toRange(), false);
    }
    return 0 === u2.length ? xx(e2, l2).filter(l2 ? r2 : o2).map((t3) => nw(e2, t3.toRange(), false)) : M.none();
  }, xx = (e2, t2) => {
    const n2 = e2.selection.getRng(), o2 = t2 ? ai.fromRangeEnd(n2) : ai.fromRangeStart(n2), r2 = (s2 = o2.container(), a2 = e2.getBody(), Wo(mn(s2), (e3) => $d(e3.dom), (e3) => e3.dom === a2).map((e3) => e3.dom).getOr(a2));
    var s2, a2;
    if (t2) {
      const e3 = ex(r2, o2);
      return le(e3.positions);
    }
    {
      const e3 = Zw(r2, o2);
      return ie(e3.positions);
    }
  }, kx = (e2, t2, n2) => xx(e2, t2).filter(n2).exists((t3) => (e2.selection.setRng(t3.toRange()), true)), Sx = (e2, t2) => {
    const n2 = e2.dom.createRng();
    n2.setStart(t2.container(), t2.offset()), n2.setEnd(t2.container(), t2.offset()), e2.selection.setRng(n2);
  }, _x = (e2, t2) => {
    e2 ? t2.setAttribute("data-mce-selected", "inline-boundary") : t2.removeAttribute("data-mce-selected");
  }, Ex = (e2, t2, n2) => _w(t2, n2).map((t3) => (Sx(e2, t3), n2)), Nx = (e2, t2, n2) => {
    const o2 = e2.getBody(), r2 = ((e3, t3, n3) => {
      const o3 = ai.fromRangeStart(e3);
      if (e3.collapsed) return o3;
      {
        const r3 = ai.fromRangeEnd(e3);
        return n3 ? Mc(t3, r3).getOr(r3) : Lc(t3, o3).getOr(o3);
      }
    })(e2.selection.getRng(), o2, n2);
    return ((e3, t3, n3, o3) => {
      const r3 = vp(e3, o3), s2 = Mw(t3, n3, r3);
      return Mw(t3, n3, r3).bind(O($w, e3)).orThunk(() => ((e4, t4, n4, o4, r4) => {
        const s3 = vp(e4, r4);
        return Tc(e4, n4, s3).map(O(vp, e4)).fold(() => o4.map(Uw), (r5) => Mw(t4, n4, r5).map(O(jw, e4, t4, n4, s3, r5)).filter(O(Hw, o4))).filter(Lw);
      })(e3, t3, n3, s2, o3));
    })(n2, O(hp, e2), o2, r2).bind((n3) => Ex(e2, t2, n3));
  }, Rx = (e2, t2, n2) => !!Pl(e2) && Nx(e2, t2, n2).isSome(), Ax = (e2, t2, n2) => !!Pl(t2) && ((e3, t3) => {
    const n3 = t3.selection.getRng(), o2 = e3 ? ai.fromRangeEnd(n3) : ai.fromRangeStart(n3);
    return !!((e4) => w(e4.selection.getSel().modify))(t3) && (e3 && Pr(o2) ? Vw(true, t3.selection, o2) : !(e3 || !Lr(o2)) && Vw(false, t3.selection, o2));
  })(e2, t2), Ox = (e2) => {
    const t2 = ra(null), n2 = O(hp, e2);
    return e2.on("NodeChange", (o2) => {
      Pl(e2) && (((e3, t3, n3) => {
        const o3 = $2(vr(mn(t3.getRoot()), '*[data-mce-selected="inline-boundary"]'), (e4) => e4.dom), r2 = K(o3, e3), s2 = K(n3, e3);
        V(oe(r2, s2), O(_x, false)), V(oe(s2, r2), O(_x, true));
      })(n2, e2.dom, o2.parents), ((e3, t3) => {
        const n3 = t3.get();
        if (e3.selection.isCollapsed() && !e3.composing && n3) {
          const o3 = ai.fromRangeStart(e3.selection.getRng());
          ai.isTextPosition(o3) && !((e4) => Pr(e4) || Lr(e4))(o3) && (Sx(e3, Dd(n3, o3)), t3.set(null));
        }
      })(e2, t2), ((e3, t3, n3, o3) => {
        if (t3.selection.isCollapsed()) {
          const r2 = K(o3, e3);
          V(r2, (o4) => {
            const r3 = ai.fromRangeStart(t3.selection.getRng());
            Mw(e3, t3.getBody(), r3).bind((e4) => Ex(t3, n3, e4));
          });
        }
      })(n2, e2, t2, o2.parents));
    }), t2;
  }, Tx = O(Ax, true), Bx = O(Ax, false), Dx = (e2, t2, n2) => {
    if (Pl(e2)) {
      const o2 = xx(e2, t2).getOrThunk(() => {
        const n3 = e2.selection.getRng();
        return t2 ? ai.fromRangeEnd(n3) : ai.fromRangeStart(n3);
      });
      return Mw(O(hp, e2), e2.getBody(), o2).exists((t3) => {
        const o3 = Uw(t3);
        return _w(n2, o3).exists((t4) => (Sx(e2, t4), true));
      });
    }
    return false;
  }, Px = (e2, t2) => (n2) => _w(t2, n2).map((t3) => () => Sx(e2, t3)), Lx = (e2, t2, n2, o2) => {
    const r2 = e2.getBody(), s2 = O(hp, e2);
    e2.undoManager.ignore(() => {
      e2.selection.setRng(((e3, t3) => {
        const n3 = document.createRange();
        return n3.setStart(e3.container(), e3.offset()), n3.setEnd(t3.container(), t3.offset()), n3;
      })(n2, o2)), xp(e2), Mw(s2, r2, ai.fromRangeStart(e2.selection.getRng())).map(zw).bind(Px(e2, t2)).each(D);
    }), e2.nodeChanged();
  }, Mx = (e2, t2, n2) => {
    if (e2.selection.isCollapsed() && Pl(e2)) {
      const o2 = ai.fromRangeStart(e2.selection.getRng());
      return ((e3, t3, n3, o3) => {
        const r2 = ((e4, t4) => nc(t4, e4) || e4)(e3.getBody(), o3.container()), s2 = O(hp, e3), a2 = Mw(s2, r2, o3);
        return a2.bind((e4) => n3 ? e4.fold(N(M.some(zw(e4))), M.none, N(M.some(Uw(e4))), M.none) : e4.fold(M.none, N(M.some(Uw(e4))), M.none, N(M.some(zw(e4))))).map(Px(e3, t3)).getOrThunk(() => {
          const i3 = Bc(n3, r2, o3), l2 = i3.bind((e4) => Mw(s2, r2, e4));
          return Dt(a2, l2, () => bp(s2, r2, o3).bind((t4) => ((e4) => Dt(Ic(e4), Fc(e4), (t5, n4) => {
            const o4 = vp(true, t5), r3 = vp(false, n4);
            return Lc(e4, o4).forall((e5) => e5.isEqual(r3));
          }).getOr(true))(t4) ? M.some(() => {
            gp(e3, n3, mn(t4));
          }) : M.none())).getOrThunk(() => l2.bind(() => i3.map((r3) => () => {
            n3 ? Lx(e3, t3, o3, r3) : Lx(e3, t3, r3, o3);
          })));
        });
      })(e2, t2, n2, o2);
    }
    return M.none();
  }, Ix = (e2) => 1 === Tn(e2), Fx = (e2, t2) => {
    const n2 = mn(e2.getBody()), o2 = mn(e2.selection.getStart()), r2 = K(((e3, t3) => {
      const n3 = Rg(t3, e3);
      return J(n3, nr).fold(N(n3), (e4) => n3.slice(0, e4));
    })(n2, o2), Ix);
    return le(r2).bind((n3) => {
      const o3 = ai.fromRangeStart(e2.selection.getRng());
      return !((e3, t3, n4) => Dt(Ic(n4), Fc(n4), (o4, r3) => {
        const s3 = vp(true, o4), a2 = vp(false, r3), i3 = vp(false, t3);
        return e3 ? Lc(n4, i3).exists((e4) => e4.isEqual(a2) && t3.isEqual(s3)) : Mc(n4, i3).exists((e4) => e4.isEqual(s3) && t3.isEqual(a2));
      }).getOr(true))(t2, o3, n3.dom) || zc((s2 = n3).dom) && Dh(s2.dom) ? M.none() : M.some(() => ((e3, t3, n4, o4) => {
        const r3 = O(jh, t3), s3 = $2(K(o4, r3), (e4) => e4.dom);
        if (0 === s3.length) gp(t3, e3, n4);
        else {
          const e4 = ((e5, t4) => {
            const n5 = Lh(false), o5 = Uh(t4, n5.dom);
            return Yn(mn(e5), n5), to(mn(e5)), ai(o5, 0);
          })(n4.dom, s3);
          t3.selection.setRng(e4.toRange());
        }
      })(t2, e2, n3, r2));
      var s2;
    });
  }, Ux = (e2, t2) => e2.selection.isCollapsed() ? Fx(e2, t2) : M.none(), zx = (e2, t2, n2) => C(n2) ? M.some(() => {
    e2._selectionOverrides.hideFakeCaret(), gp(e2, t2, mn(n2));
  }) : M.none(), jx = (e2, t2) => e2.selection.isCollapsed() ? ((e3, t3) => {
    const n2 = t3 ? wg : xg, o2 = t3 ? hc.Forwards : hc.Backwards, r2 = mc(o2, e3.getBody(), e3.selection.getRng());
    return n2(r2) ? zx(e3, t3, r2.getNode(!t3)) : M.from(vp(t3, r2)).filter((e4) => n2(e4) && pc(r2, e4)).bind((n3) => zx(e3, t3, n3.getNode(!t3)));
  })(e2, t2) : ((e3, t3) => {
    const n2 = e3.selection.getNode();
    return zo(n2) ? zx(e3, t3, n2) : M.none();
  })(e2, t2), Hx = (e2) => Ge(null != e2 ? e2 : "").getOr(0), $x = (e2, t2) => (e2 || "table" === Lt(t2) ? "margin" : "padding") + ("rtl" === Vn(t2, "direction") ? "-right" : "-left"), Vx = (e2) => {
    const t2 = Wx(e2);
    return !e2.mode.isReadOnly() && (t2.length > 1 || ((e3, t3) => te(t3, (t4) => {
      const n2 = $x(vl(e3), t4), o2 = Wn(t4, n2).map(Hx).getOr(0);
      return "false" !== e3.dom.getContentEditable(t4.dom) && o2 > 0;
    }))(e2, t2));
  }, qx = (e2) => ar(e2) || ir(e2), Wx = (e2) => {
    return K((t2 = e2.selection.getSelectedBlocks(), $2(t2, mn)), (e3) => !qx(e3) && !((e4) => xn(e4).exists(qx))(e3) && Ko(e3, (e4) => Mo(e4.dom) || Io(e4.dom)).exists((e4) => Mo(e4.dom)));
    var t2;
  }, Kx = (e2, t2) => {
    var n2, o2;
    const { dom: r2 } = e2, s2 = yl(e2), a2 = null !== (o2 = null === (n2 = /[a-z%]+$/i.exec(s2)) || void 0 === n2 ? void 0 : n2[0]) && void 0 !== o2 ? o2 : "px", i3 = Hx(s2), l2 = vl(e2);
    V(Wx(e2), (e3) => {
      ((e4, t3, n3, o3, r3, s3) => {
        const a3 = $x(n3, mn(s3)), i4 = Hx(e4.getStyle(s3, a3));
        if ("outdent" === t3) {
          const t4 = Math.max(0, i4 - o3);
          e4.setStyle(s3, a3, t4 ? t4 + r3 : "");
        } else {
          const t4 = i4 + o3 + r3;
          e4.setStyle(s3, a3, t4);
        }
      })(r2, t2, l2, i3, a2, e3.dom);
    });
  }, Gx = (e2) => Kx(e2, "outdent"), Yx = (e2) => {
    if (e2.selection.isCollapsed() && Vx(e2)) {
      const t2 = e2.dom, n2 = e2.selection.getRng(), o2 = ai.fromRangeStart(n2), r2 = t2.getParent(n2.startContainer, t2.isBlock);
      if (null !== r2 && Dg(mn(r2), o2)) return M.some(() => Gx(e2));
    }
    return M.none();
  }, Xx = (e2, t2, n2) => ce([Yx, gw, aw, (e3, n3) => Mx(e3, t2, n3), XC, Jp, pw, jx, ZC, Ux], (t3) => t3(e2, n2)), Qx = (e2, t2) => {
    e2.addCommand("delete", () => {
      ((e3, t3) => {
        Xx(e3, t3, false).fold(() => {
          xp(e3), _p(e3);
        }, D);
      })(e2, t2);
    }), e2.addCommand("forwardDelete", () => {
      ((e3, t3) => {
        Xx(e3, t3, true).fold(() => ((e4) => wp(e4, "ForwardDelete"))(e3), D);
      })(e2, t2);
    });
  }, Jx = (e2) => void 0 === e2.touches || 1 !== e2.touches.length ? M.none() : M.some(e2.touches[0]), Zx = (e2, t2) => xe(e2, t2.nodeName), ek = (e2, t2) => !!Ro(t2) || !!yo(t2) && !Zx(e2, t2) && !Zc(t2), tk = (e2, t2) => {
    if (Ro(t2)) {
      if (0 === t2.data.length) return true;
      if (/^\s+$/.test(t2.data) && (!t2.nextSibling || Zx(e2, t2.nextSibling))) return true;
    }
    return false;
  }, nk = (e2) => e2.dom.create(Ji(e2), Zi(e2)), ok = (e2) => {
    const t2 = e2.dom, n2 = e2.selection, o2 = e2.schema, r2 = o2.getBlockElements(), s2 = n2.getStart(), a2 = e2.getBody();
    let i3, l2, d2 = false;
    const c2 = Ji(e2);
    if (!s2 || !yo(s2)) return;
    const u2 = a2.nodeName.toLowerCase();
    if (!o2.isValidChild(u2, c2.toLowerCase()) || ((e3, t3, n3) => H(Ng(mn(n3), mn(t3)), (t4) => Zx(e3, t4.dom)))(r2, a2, s2)) return;
    const m2 = n2.getRng(), { startContainer: f2, startOffset: g2, endContainer: p2, endOffset: h2 } = m2, b2 = If(e2);
    let v2 = a2.firstChild;
    for (; v2; ) if (ek(r2, v2)) {
      if (tk(r2, v2)) {
        l2 = v2, v2 = v2.nextSibling, t2.remove(l2);
        continue;
      }
      i3 || (i3 = nk(e2), a2.insertBefore(i3, v2), d2 = true), l2 = v2, v2 = v2.nextSibling, i3.appendChild(l2);
    } else i3 = null, v2 = v2.nextSibling;
    d2 && b2 && (m2.setStart(f2, g2), m2.setEnd(p2, h2), n2.setRng(m2), e2.nodeChanged());
  }, rk = (e2, t2, n2) => {
    const o2 = mn(nk(e2)), r2 = xr();
    Jn(o2, r2), n2(t2, o2);
    const s2 = document.createRange();
    return s2.setStartBefore(r2.dom), s2.setEndBefore(r2.dom), s2;
  }, sk = (e2) => (t2) => -1 !== (" " + t2.attr("class") + " ").indexOf(e2), ak = (e2, t2, n2) => function(o2) {
    const r2 = arguments, s2 = r2[r2.length - 2], a2 = s2 > 0 ? t2.charAt(s2 - 1) : "";
    if ('"' === a2) return o2;
    if (">" === a2) {
      const e3 = t2.lastIndexOf("<", s2);
      if (-1 !== e3 && -1 !== t2.substring(e3, s2).indexOf('contenteditable="false"')) return o2;
    }
    return '<span class="' + n2 + '" data-mce-content="' + e2.dom.encode(r2[0]) + '">' + e2.dom.encode("string" == typeof r2[1] ? r2[1] : r2[0]) + "</span>";
  }, ik = (e2, t2) => {
    t2.hasAttribute("data-mce-caret") && (Fr(t2), e2.selection.setRng(e2.selection.getRng()), e2.selection.scrollIntoView(t2));
  }, lk = (e2, t2) => {
    const n2 = ((e3) => Yo(mn(e3.getBody()), "*[data-mce-caret]").map((e4) => e4.dom).getOrNull())(e2);
    if (n2) return "compositionstart" === t2.type ? (t2.preventDefault(), t2.stopPropagation(), void ik(e2, n2)) : void (Dr(n2) && (ik(e2, n2), e2.undoManager.add()));
  }, dk = Io, ck = (e2, t2, n2) => {
    const o2 = Nc(e2.getBody()), r2 = O(gc, 1 === t2 ? o2.next : o2.prev);
    if (n2.collapsed) {
      const o3 = e2.dom.getParent(n2.startContainer, "PRE");
      if (!o3) return;
      if (!r2(ai.fromRangeStart(n2))) {
        const n3 = mn(((e3) => {
          const t3 = e3.dom.create(Ji(e3));
          return t3.innerHTML = '<br data-mce-bogus="1">', t3;
        })(e2));
        1 === t2 ? Xn(mn(o3), n3) : Yn(mn(o3), n3), e2.selection.select(n3.dom, true), e2.selection.collapse();
      }
    }
  }, uk = (e2, t2) => ((e3, t3) => {
    const n2 = t3 ? hc.Forwards : hc.Backwards, o2 = e3.selection.getRng();
    return ((e4, t4, n3) => Cx(t4, e4, n3, _g, Eg, dk))(n2, e3, o2).orThunk(() => (ck(e3, n2, o2), M.none()));
  })(e2, t2).exists((t3) => (vx(e2, t3), true)), mk = (e2, t2) => ((e3, t3) => {
    const n2 = t3 ? 1 : -1, o2 = e3.selection.getRng();
    return ((e4, t4, n3) => wx(t4, e4, n3, (e5) => _g(e5) || kg(e5), (e5) => Eg(e5) || Sg(e5), dk))(n2, e3, o2).orThunk(() => (ck(e3, n2, o2), M.none()));
  })(e2, t2).exists((t3) => (vx(e2, t3), true)), fk = (e2, t2) => kx(e2, t2, t2 ? Eg : _g), gk = (e2, t2) => iw(e2, !t2).map((n2) => {
    const o2 = n2.toRange(), r2 = e2.selection.getRng();
    return t2 ? o2.setStart(r2.startContainer, r2.startOffset) : o2.setEnd(r2.endContainer, r2.endOffset), o2;
  }).exists((t3) => (vx(e2, t3), true)), pk = (e2) => j(["figcaption"], Lt(e2)), hk = (e2, t2) => {
    const n2 = mn(e2.getBody()), o2 = ai.fromRangeStart(e2.selection.getRng());
    return ((e3, t3) => {
      const n3 = O(bn, t3);
      return Ko(mn(e3.container()), nr, n3).filter(pk);
    })(o2, n2).exists(() => {
      if (((e3, t3, n3) => t3 ? ((e4, t4) => ex(e4, t4).breakAt.isNone())(e3.dom, n3) : ((e4, t4) => Zw(e4, t4).breakAt.isNone())(e3.dom, n3))(n2, t2, o2)) {
        const o3 = rk(e2, n2, t2 ? Jn : Qn);
        return e2.selection.setRng(o3), true;
      }
      return false;
    });
  }, bk = (e2, t2) => !!e2.selection.isCollapsed() && hk(e2, t2), vk = { shiftKey: false, altKey: false, ctrlKey: false, metaKey: false, keyCode: 0 }, yk = (e2, t2) => t2.keyCode === e2.keyCode && t2.shiftKey === e2.shiftKey && t2.altKey === e2.altKey && t2.ctrlKey === e2.ctrlKey && t2.metaKey === e2.metaKey, Ck = (e2, ...t2) => () => e2.apply(null, t2), wk = (e2, t2) => Q(((e3, t3) => ee(((e4) => $2(e4, (e5) => ({ ...vk, ...e5 })))(e3), (e4) => yk(e4, t3) ? [e4] : []))(e2, t2), (e3) => e3.action()), xk = (e2, t2) => ce(((e3, t3) => ee(((e4) => $2(e4, (e5) => ({ ...vk, ...e5 })))(e3), (e4) => yk(e4, t3) ? [e4] : []))(e2, t2), (e3) => e3.action()), kk = (e2, t2) => {
    const n2 = t2 ? hc.Forwards : hc.Backwards, o2 = e2.selection.getRng();
    return Cx(e2, n2, o2, wg, xg, zo).exists((t3) => (vx(e2, t3), true));
  }, Sk = (e2, t2) => {
    const n2 = t2 ? 1 : -1, o2 = e2.selection.getRng();
    return wx(e2, n2, o2, wg, xg, zo).exists((t3) => (vx(e2, t3), true));
  }, _k = (e2, t2) => kx(e2, t2, t2 ? xg : wg), Ek = Ti([{ none: ["current"] }, { first: ["current"] }, { middle: ["current", "target"] }, { last: ["current"] }]), Nk = { ...Ek, none: (e2) => Ek.none(e2) }, Rk = (e2, t2, n2) => ee(Nn(e2), (e3) => pn(e3, t2) ? n2(e3) ? [e3] : [] : Rk(e3, t2, n2)), Ak = (e2, t2) => Xo(e2, "table", t2), Ok = (e2, t2, n2, o2, r2 = L) => {
    const s2 = 1 === o2;
    if (!s2 && n2 <= 0) return Nk.first(e2[0]);
    if (s2 && n2 >= e2.length - 1) return Nk.last(e2[e2.length - 1]);
    {
      const s3 = n2 + o2, a2 = e2[s3];
      return r2(a2) ? Nk.middle(t2, a2) : Ok(e2, t2, s3, o2, r2);
    }
  }, Tk = (e2, t2) => Ak(e2, t2).bind((t3) => {
    const n2 = Rk(t3, "th,td", L);
    return J(n2, (t4) => bn(e2, t4)).map((e3) => ({ index: e3, all: n2 }));
  }), Bk = (e2, t2 = false) => {
    return zn(e2) ? e2.dom.isContentEditable : (n2 = e2, Xo(n2, "[contenteditable]")).fold(N(t2), (e3) => "true" === Dk(e3));
    var n2;
  }, Dk = (e2) => e2.dom.contentEditable, Pk = (e2, t2, n2, o2, r2) => {
    const s2 = vr(mn(n2), "td,th,caption").map((e3) => e3.dom), a2 = K(((e3, t3) => ee(t3, (t4) => {
      const n3 = ((e4, t5) => ({ left: e4.left - t5, top: e4.top - t5, right: e4.right + -2, bottom: e4.bottom + -2, width: e4.width + t5, height: e4.height + t5 }))(Da(t4.getBoundingClientRect()), -1);
      return [{ x: n3.left, y: e3(n3), cell: t4 }, { x: n3.right, y: e3(n3), cell: t4 }];
    }))(e2, s2), (e3) => t2(e3, r2));
    return ((e3, t3, n3) => Y(e3, (e4, o3) => e4.fold(() => M.some(o3), (e5) => {
      const r3 = Math.sqrt(Math.abs(e5.x - t3) + Math.abs(e5.y - n3)), s3 = Math.sqrt(Math.abs(o3.x - t3) + Math.abs(o3.y - n3));
      return M.some(s3 < r3 ? o3 : e5);
    }), M.none()))(a2, o2, r2).map((e3) => e3.cell);
  }, Lk = O(Pk, (e2) => e2.bottom, (e2, t2) => e2.y < t2), Mk = O(Pk, (e2) => e2.top, (e2, t2) => e2.y > t2), Ik = (e2, t2, n2) => {
    const o2 = e2(t2, n2);
    return ((e3) => e3.breakType === qw.Wrap && 0 === e3.positions.length)(o2) || !Po(n2.getNode()) && ((e3) => e3.breakType === qw.Br && 1 === e3.positions.length)(o2) ? !((e3, t3, n3) => n3.breakAt.exists((n4) => e3(t3, n4).breakAt.isSome()))(e2, t2, o2) : o2.breakAt.isNone();
  }, Fk = O(Ik, Zw), Uk = O(Ik, ex), zk = (e2, t2, n2, o2) => {
    const r2 = e2.selection.getRng(), s2 = t2 ? 1 : -1;
    return !(!zd() || !((e3, t3, n3) => {
      const o3 = ai.fromRangeStart(t3);
      return Pc(!e3, n3).exists((e4) => e4.isEqual(o3));
    })(t2, r2, n2) || (ew(s2, e2, n2, !t2, false).each((t3) => {
      vx(e2, t3);
    }), 0));
  }, jk = (e2, t2, n2) => {
    const o2 = ((e3, t3) => {
      const n3 = t3.getNode(e3);
      return _o(n3) ? M.some(n3) : M.none();
    })(!!t2, n2), r2 = false === t2;
    o2.fold(() => vx(e2, n2.toRange()), (o3) => Pc(r2, e2.getBody()).filter((e3) => e3.isEqual(n2)).fold(() => vx(e2, n2.toRange()), (n3) => ((e3, t3, n4) => {
      t3.undoManager.transact(() => {
        const o4 = e3 ? Xn : Yn, r3 = rk(t3, mn(n4), o4);
        vx(t3, r3);
      });
    })(t2, e2, o3)));
  }, Hk = (e2, t2, n2, o2) => {
    const r2 = e2.selection.getRng(), s2 = ai.fromRangeStart(r2), a2 = e2.getBody();
    if (!t2 && Fk(o2, s2)) {
      const o3 = ((e3, t3, n3) => ((e4, t4) => ie(t4.getClientRects()).bind((t5) => Lk(e4, t5.left, t5.top)).bind((e5) => {
        return Jw(Fc(n4 = e5).map((e6) => Zw(n4, e6).positions.concat(e6)).getOr([]), t4);
        var n4;
      }))(t3, n3).orThunk(() => ie(n3.getClientRects()).bind((n4) => Qw(tx(e3, ai.before(t3)), n4.left))).getOr(ai.before(t3)))(a2, n2, s2);
      return jk(e2, t2, o3), true;
    }
    if (t2 && Uk(o2, s2)) {
      const o3 = ((e3, t3, n3) => ((e4, t4) => le(t4.getClientRects()).bind((t5) => Mk(e4, t5.left, t5.top)).bind((e5) => {
        return Jw(Ic(n4 = e5).map((e6) => [e6].concat(ex(n4, e6).positions)).getOr([]), t4);
        var n4;
      }))(t3, n3).orThunk(() => ie(n3.getClientRects()).bind((n4) => Qw(nx(e3, ai.after(t3)), n4.left))).getOr(ai.after(t3)))(a2, n2, s2);
      return jk(e2, t2, o3), true;
    }
    return false;
  }, $k = (e2, t2, n2) => M.from(e2.dom.getParent(e2.selection.getNode(), "td,th")).bind((o2) => M.from(e2.dom.getParent(o2, "table")).map((r2) => n2(e2, t2, r2, o2))).getOr(false), Vk = (e2, t2) => $k(e2, t2, zk), qk = (e2, t2) => $k(e2, t2, Hk), Wk = (e2, t2, n2) => n2.fold(M.none, M.none, (e3, t3) => {
    return (n3 = t3, ((e4, t4) => {
      const n4 = (e5) => {
        for (let o2 = 0; o2 < e5.childNodes.length; o2++) {
          const r2 = mn(e5.childNodes[o2]);
          if (t4(r2)) return M.some(r2);
          const s2 = n4(e5.childNodes[o2]);
          if (s2.isSome()) return s2;
        }
        return M.none();
      };
      return n4(e4.dom);
    })(n3, qf)).map((e4) => ((e5) => {
      const t4 = Bm.exact(e5, 0, e5, 0);
      return Im(t4);
    })(e4));
    var n3;
  }, (n3) => (e2.execCommand("mceTableInsertRowAfter"), Kk(e2, t2, n3))), Kk = (e2, t2, n2) => {
    return Wk(e2, t2, (r2 = Bk, Tk(o2 = n2, void 0).fold(() => Nk.none(o2), (e3) => Ok(e3.all, o2, e3.index, 1, r2))));
    var o2, r2;
  }, Gk = (e2, t2, n2) => {
    return Wk(e2, t2, (r2 = Bk, Tk(o2 = n2, void 0).fold(() => Nk.none(), (e3) => Ok(e3.all, o2, e3.index, -1, r2))));
    var o2, r2;
  }, Yk = (e2, t2) => {
    const n2 = ["table", "li", "dl"], o2 = mn(e2.getBody()), r2 = (e3) => {
      const t3 = Lt(e3);
      return bn(e3, o2) || j(n2, t3);
    }, s2 = e2.selection.getRng();
    return ((e3, t3) => ((e4, t4, n3 = P) => n3(t4) ? M.none() : j(e4, Lt(t4)) ? M.some(t4) : Go(t4, e4.join(","), (e5) => pn(e5, "table") || n3(e5)))(["td", "th"], e3, t3))(mn(t2 ? s2.endContainer : s2.startContainer), r2).map((n3) => (Ak(n3, r2).each((t3) => {
      e2.model.table.clearSelectedCells(t3.dom);
    }), e2.selection.collapse(!t2), (t2 ? Kk : Gk)(e2, r2, n3).each((t3) => {
      e2.selection.setRng(t3);
    }), true)).getOr(false);
  }, Xk = (e2, t2) => ({ container: e2, offset: t2 }), Qk = ta.DOM, Jk = (e2) => (t2) => e2 === t2 ? -1 : 0, Zk = (e2, t2, n2) => {
    if (Ro(e2) && t2 >= 0) return M.some(Xk(e2, t2));
    {
      const o2 = Ta(Qk);
      return M.from(o2.backwards(e2, t2, Jk(e2), n2)).map((e3) => Xk(e3.container, e3.container.data.length));
    }
  }, eS = (e2, t2, n2) => {
    if (!Ro(e2)) return M.none();
    const o2 = e2.data;
    if (t2 >= 0 && t2 <= o2.length) return M.some(Xk(e2, t2));
    {
      const o3 = Ta(Qk);
      return M.from(o3.backwards(e2, t2, Jk(e2), n2)).bind((e3) => {
        const o4 = e3.container.data;
        return eS(e3.container, t2 + o4.length, n2);
      });
    }
  }, tS = (e2, t2, n2) => {
    if (!Ro(e2)) return M.none();
    const o2 = e2.data;
    if (t2 <= o2.length) return M.some(Xk(e2, t2));
    {
      const r2 = Ta(Qk);
      return M.from(r2.forwards(e2, t2, Jk(e2), n2)).bind((e3) => tS(e3.container, t2 - o2.length, n2));
    }
  }, nS = (e2, t2, n2, o2, r2) => {
    const s2 = Ta(e2, /* @__PURE__ */ ((e3) => (t3) => e3.isBlock(t3) || j(["BR", "IMG", "HR", "INPUT"], t3.nodeName) || "false" === e3.getContentEditable(t3))(e2));
    return M.from(s2.backwards(t2, n2, o2, r2));
  }, oS = (e2) => Nr(e2.toString().replace(/\u00A0/g, " ")), rS = (e2) => "" !== e2 && -1 !== " \xA0\f\n\r	\v".indexOf(e2), sS = (e2, t2) => e2.substring(t2.length), aS = (e2, t2, n2, o2 = 0) => {
    return (r2 = mn(t2.startContainer), Xo(r2, Wf)).fold(() => ((e3, t3, n3, o3 = 0) => {
      if (!(r3 = t3).collapsed || !Ro(r3.startContainer)) return M.none();
      var r3;
      const s2 = { text: "", offset: 0 }, a2 = e3.getParent(t3.startContainer, e3.isBlock) || e3.getRoot();
      return nS(e3, t3.startContainer, t3.startOffset, (e4, t4, o4) => (s2.text = o4 + s2.text, s2.offset += t4, ((e5, t5, n4) => {
        let o5;
        const r4 = n4.charAt(0);
        for (o5 = t5 - 1; o5 >= 0; o5--) {
          const s3 = e5.charAt(o5);
          if (rS(s3)) return M.none();
          if (r4 === s3 && Ue(e5, n4, o5, t5)) break;
        }
        return M.some(o5);
      })(s2.text, s2.offset, n3).getOr(t4)), a2).bind((e4) => {
        const r4 = t3.cloneRange();
        if (r4.setStart(e4.container, e4.offset), r4.setEnd(t3.endContainer, t3.endOffset), r4.collapsed) return M.none();
        const s3 = oS(r4);
        return 0 !== s3.lastIndexOf(n3) || sS(s3, n3).length < o3 ? M.none() : M.some({ text: sS(s3, n3), range: r4, trigger: n3 });
      });
    })(e2, t2, n2, o2), (t3) => {
      const o3 = e2.createRng();
      o3.selectNode(t3.dom);
      const r3 = oS(o3);
      return M.some({ range: o3, text: sS(r3, n2), trigger: n2 });
    });
    var r2;
  }, iS = (e2) => {
    if (((e3) => 3 === e3.nodeType)(e2)) return Xk(e2, e2.data.length);
    {
      const t2 = e2.childNodes;
      return t2.length > 0 ? iS(t2[t2.length - 1]) : Xk(e2, t2.length);
    }
  }, lS = (e2, t2) => {
    const n2 = e2.childNodes;
    return n2.length > 0 && t2 < n2.length ? lS(n2[t2], 0) : n2.length > 0 && ((e3) => 1 === e3.nodeType)(e2) && n2.length === t2 ? iS(n2[n2.length - 1]) : Xk(e2, t2);
  }, dS = (e2, t2, n2, o2 = {}) => {
    var r2;
    const s2 = t2(), a2 = null !== (r2 = e2.selection.getRng().startContainer.nodeValue) && void 0 !== r2 ? r2 : "", i3 = K(s2.lookupByTrigger(n2.trigger), (t3) => n2.text.length >= t3.minChars && t3.matches.getOrThunk(() => /* @__PURE__ */ ((e3) => (t4) => {
      const n3 = lS(t4.startContainer, t4.startOffset);
      return !((e4, t5) => {
        var n4;
        const o3 = null !== (n4 = e4.getParent(t5.container, e4.isBlock)) && void 0 !== n4 ? n4 : e4.getRoot();
        return nS(e4, t5.container, t5.offset, (e5, t6) => 0 === t6 ? -1 : t6, o3).filter((e5) => {
          const t6 = e5.container.data.charAt(e5.offset - 1);
          return !rS(t6);
        }).isSome();
      })(e3, n3);
    })(e2.dom))(n2.range, a2, n2.text));
    if (0 === i3.length) return M.none();
    const l2 = Promise.all($2(i3, (e3) => e3.fetch(n2.text, e3.maxResults, o2).then((t3) => ({ matchText: n2.text, items: t3, columns: e3.columns, onAction: e3.onAction, highlightOn: e3.highlightOn }))));
    return M.some({ lookupData: l2, context: n2 });
  };
  var cS;
  !function(e2) {
    e2[e2.Error = 0] = "Error", e2[e2.Value = 1] = "Value";
  }(cS || (cS = {}));
  const uS = (e2, t2, n2) => e2.stype === cS.Error ? t2(e2.serror) : n2(e2.svalue), mS = (e2) => ({ stype: cS.Value, svalue: e2 }), fS = (e2) => ({ stype: cS.Error, serror: e2 }), gS = uS, pS = (e2) => f(e2) && ue(e2).length > 100 ? " removed due to size" : JSON.stringify(e2, null, 2), hS = (e2, t2) => fS([{ path: e2, getErrorInfo: t2 }]), bS = (e2, t2) => ({ extract: (n2, o2) => we(o2, e2).fold(() => ((e3, t3) => hS(e3, () => 'Choice schema did not contain choice key: "' + t3 + '"'))(n2, e2), (e3) => ((e4, t3, n3, o3) => we(n3, o3).fold(() => ((e5, t4, n4) => hS(e5, () => 'The chosen schema: "' + n4 + '" did not exist in branches: ' + pS(t4)))(e4, n3, o3), (n4) => n4.extract(e4.concat(["branch: " + o3]), t3)))(n2, o2, t2, e3)), toString: () => "chooseOn(" + e2 + "). Possible values: " + ue(t2) }), vS = (e2) => (...t2) => {
    if (0 === t2.length) throw new Error("Can't merge zero objects");
    const n2 = {};
    for (let o2 = 0; o2 < t2.length; o2++) {
      const r2 = t2[o2];
      for (const t3 in r2) xe(r2, t3) && (n2[t3] = e2(n2[t3], r2[t3]));
    }
    return n2;
  }, yS = vS((e2, t2) => g(e2) && g(t2) ? yS(e2, t2) : t2), CS = (vS((e2, t2) => t2), (e2) => ({ tag: "defaultedThunk", process: N(e2) })), wS = (e2) => {
    const t2 = ((e3) => {
      const t3 = [], n3 = [];
      return V(e3, (e4) => {
        uS(e4, (e5) => n3.push(e5), (e5) => t3.push(e5));
      }), { values: t3, errors: n3 };
    })(e2);
    return t2.errors.length > 0 ? (n2 = t2.errors, _(fS, Z)(n2)) : mS(t2.values);
    var n2;
  }, xS = (e2, t2, n2) => {
    switch (e2.tag) {
      case "field":
        return t2(e2.key, e2.newKey, e2.presence, e2.prop);
      case "custom":
        return n2(e2.newKey, e2.instantiator);
    }
  }, kS = (e2) => ({ extract: (t2, n2) => {
    return o2 = e2(n2), r2 = (e3) => ((e4, t3) => hS(e4, N(t3)))(t2, e3), o2.stype === cS.Error ? r2(o2.serror) : o2;
    var o2, r2;
  }, toString: N("val") }), SS = kS(mS), _S = (e2, t2, n2, o2) => o2(we(e2, t2).getOrThunk(() => n2(e2))), ES = (e2, t2, n2, o2, r2) => {
    const s2 = (e3) => r2.extract(t2.concat([o2]), e3), a2 = (e3) => e3.fold(() => mS(M.none()), (e4) => {
      const n3 = r2.extract(t2.concat([o2]), e4);
      return s3 = n3, a3 = M.some, s3.stype === cS.Value ? { stype: cS.Value, svalue: a3(s3.svalue) } : s3;
      var s3, a3;
    });
    switch (e2.tag) {
      case "required":
        return ((e3, t3, n3, o3) => we(t3, n3).fold(() => ((e4, t4, n4) => hS(e4, () => 'Could not find valid *required* value for "' + t4 + '" in ' + pS(n4)))(e3, n3, t3), o3))(t2, n2, o2, s2);
      case "defaultedThunk":
        return _S(n2, o2, e2.process, s2);
      case "option":
        return ((e3, t3, n3) => n3(we(e3, t3)))(n2, o2, a2);
      case "defaultedOptionThunk":
        return ((e3, t3, n3, o3) => o3(we(e3, t3).map((t4) => true === t4 ? n3(e3) : t4)))(n2, o2, e2.process, a2);
      case "mergeWithThunk":
        return _S(n2, o2, N({}), (t3) => {
          const o3 = yS(e2.process(n2), t3);
          return s2(o3);
        });
    }
  }, NS = (e2) => ({ extract: (t2, n2) => ((e3, t3, n3) => {
    const o2 = {}, r2 = [];
    for (const s2 of n3) xS(s2, (n4, s3, a2, i3) => {
      const l2 = ES(a2, e3, t3, n4, i3);
      gS(l2, (e4) => {
        r2.push(...e4);
      }, (e4) => {
        o2[s3] = e4;
      });
    }, (e4, n4) => {
      o2[e4] = n4(t3);
    });
    return r2.length > 0 ? fS(r2) : mS(o2);
  })(t2, n2, e2), toString: () => {
    const t2 = $2(e2, (e3) => xS(e3, (e4, t3, n2, o2) => e4 + " -> " + o2.toString(), (e4, t3) => "state(" + e4 + ")"));
    return "obj{\n" + t2.join("\n") + "}";
  } }), RS = (e2) => ({ extract: (t2, n2) => {
    const o2 = $2(n2, (n3, o3) => e2.extract(t2.concat(["[" + o3 + "]"]), n3));
    return wS(o2);
  }, toString: () => "array(" + e2.toString() + ")" }), AS = (e2, t2, n2) => {
    return o2 = ((e3, t3, n3) => ((e4, t4) => e4.stype === cS.Error ? { stype: cS.Error, serror: t4(e4.serror) } : e4)(t3.extract([e3], n3), (e4) => ({ input: n3, errors: e4 })))(e2, t2, n2), uS(o2, Oi.error, Oi.value);
    var o2;
  }, OS = (e2, t2) => bS(e2, ge(t2, NS)), TS = N(SS), BS = (e2, t2) => kS((n2) => {
    const o2 = typeof n2;
    return e2(n2) ? mS(n2) : fS(`Expected type: ${t2} but got: ${o2}`);
  }), DS = BS(x, "number"), PS = BS(m, "string"), LS = BS(b, "boolean"), MS = BS(w, "function"), IS = (e2, t2, n2, o2) => ({ tag: "field", key: e2, newKey: t2, presence: n2, prop: o2 }), FS = (e2, t2) => ({ tag: "custom", newKey: e2, instantiator: t2 }), US = (e2, t2) => IS(e2, e2, { tag: "required", process: {} }, t2), zS = (e2) => US(e2, PS), jS = (e2) => US(e2, MS), HS = (e2, t2) => IS(e2, e2, { tag: "option", process: {} }, t2), $S = (e2) => HS(e2, PS), VS = (e2, t2, n2) => IS(e2, e2, CS(t2), n2), qS = (e2, t2) => VS(e2, t2, DS), WS = (e2, t2, n2) => VS(e2, t2, ((e3) => {
    return t3 = (t4) => j(e3, t4) ? Oi.value(t4) : Oi.error(`Unsupported value: "${t4}", choose one of "${e3.join(", ")}".`), kS((e4) => t3(e4).fold(fS, mS));
    var t3;
  })(n2)), KS = (e2, t2) => VS(e2, t2, LS), GS = (e2, t2) => VS(e2, t2, MS), YS = zS("type"), XS = jS("fetch"), QS = jS("onAction"), JS = GS("onSetup", () => S), ZS = $S("text"), e_ = $S("icon"), t_ = $S("tooltip"), n_ = $S("label"), o_ = KS("active", false), r_ = KS("enabled", true), s_ = KS("primary", false), a_ = (e2) => ((e3, t2) => VS("type", t2, PS))(0, e2), i_ = NS([YS, zS("trigger"), qS("minChars", 1), (1, ((e2, t2) => IS(e2, e2, CS(1), TS()))("columns")), qS("maxResults", 10), ("matches", HS("matches", MS)), XS, QS, (l_ = PS, VS("highlightOn", [], RS(l_)))]);
  var l_;
  const d_ = [r_, t_, e_, ZS, JS], c_ = [o_].concat(d_), u_ = [GS("predicate", P), WS("scope", "node", ["node", "editor"]), WS("position", "selection", ["node", "selection", "line"])], m_ = d_.concat([a_("contextformbutton"), s_, QS, FS("original", R)]), f_ = c_.concat([a_("contextformbutton"), s_, QS, FS("original", R)]), g_ = d_.concat([a_("contextformbutton")]), p_ = c_.concat([a_("contextformtogglebutton")]), h_ = OS("type", { contextformbutton: m_, contextformtogglebutton: f_ });
  NS([a_("contextform"), GS("initValue", N("")), n_, ((e2, t2) => IS(e2, e2, { tag: "required", process: {} }, RS(t2)))("commands", h_), HS("launch", OS("type", { contextformbutton: g_, contextformtogglebutton: p_ }))].concat(u_));
  const b_ = (e2) => {
    const t2 = e2.ui.registry.getAll().popups, n2 = ge(t2, (e3) => {
      return (t3 = e3, AS("Autocompleter", i_, { trigger: t3.ch, ...t3 })).fold((e4) => {
        throw new Error("Errors: \n" + ((e5) => {
          const t5 = e5.length > 10 ? e5.slice(0, 10).concat([{ path: [], getErrorInfo: N("... (only showing first ten failures)") }]) : e5;
          return $2(t5, (e6) => "Failed path: (" + e6.path.join(" > ") + ")\n" + e6.getErrorInfo());
        })((t4 = e4).errors).join("\n") + "\n\nInput object: " + pS(t4.input));
        var t4;
      }, R);
      var t3;
    }), o2 = Se(ye(n2, (e3) => e3.trigger)), r2 = Ce(n2);
    return { dataset: n2, triggers: o2, lookupByTrigger: (e3) => K(r2, (t3) => t3.trigger === e3) };
  }, v_ = (e2) => {
    const t2 = ua(), n2 = ra(false), o2 = t2.isSet, r2 = () => {
      o2() && (((e3) => {
        Py(e3).autocompleter.removeDecoration();
      })(e2), ((e3) => {
        e3.dispatch("AutocompleterEnd");
      })(e2), n2.set(false), t2.clear());
    }, s2 = De(() => b_(e2)), a2 = (a3) => {
      ((n3) => t2.get().map((t3) => aS(e2.dom, e2.selection.getRng(), t3.trigger).bind((t4) => dS(e2, s2, t4, n3))).getOrThunk(() => ((e3, t3) => {
        const n4 = t3(), o3 = e3.selection.getRng();
        return ((e4, t4, n5) => ce(n5.triggers, (n6) => aS(e4, t4, n6)))(e3.dom, o3, n4).bind((n5) => dS(e3, t3, n5));
      })(e2, s2)))(a3).fold(r2, (s3) => {
        ((n3) => {
          o2() || (((e3, t3) => {
            Py(e3).autocompleter.addDecoration(t3);
          })(e2, n3.range), t2.set({ trigger: n3.trigger, matchLength: n3.text.length }));
        })(s3.context), s3.lookupData.then((o3) => {
          t2.get().map((a4) => {
            const i3 = s3.context;
            a4.trigger === i3.trigger && (i3.text.length - a4.matchLength >= 10 ? r2() : (t2.set({ ...a4, matchLength: i3.text.length }), n2.get() ? ((e3, t3) => {
              e3.dispatch("AutocompleterUpdate", t3);
            })(e2, { lookupData: o3 }) : (n2.set(true), ((e3, t3) => {
              e3.dispatch("AutocompleterStart", t3);
            })(e2, { lookupData: o3 }))));
          });
        });
      });
    };
    e2.addCommand("mceAutocompleterReload", (e3, t3) => {
      const n3 = f(t3) ? t3.fetchOptions : {};
      a2(n3);
    }), e2.addCommand("mceAutocompleterClose", r2), ((e3, t3) => {
      const n3 = fa(t3.load, 50);
      e3.on("keypress compositionend", (e4) => {
        27 !== e4.which && n3.throttle();
      }), e3.on("keydown", (e4) => {
        const o3 = e4.which;
        8 === o3 ? n3.throttle() : 27 === o3 && t3.cancelIfNecessary();
      }), e3.on("remove", n3.cancel);
    })(e2, { cancelIfNecessary: r2, load: a2 });
  }, y_ = (e2) => (t2, n2, o2 = {}) => {
    const r2 = t2.getBody(), s2 = { bubbles: true, composed: true, data: null, isComposing: false, detail: 0, view: null, target: r2, currentTarget: r2, eventPhase: Event.AT_TARGET, originalTarget: r2, explicitOriginalTarget: r2, isTrusted: false, srcElement: r2, cancelable: false, preventDefault: S, inputType: n2 }, a2 = Fs(new InputEvent(e2));
    return t2.dispatch(e2, { ...a2, ...s2, ...o2 });
  }, C_ = y_("input"), w_ = y_("beforeinput"), x_ = (e2, t2) => {
    const n2 = e2.dom, o2 = e2.schema.getMoveCaretBeforeOnEnterElements();
    if (!t2) return;
    if (/^(LI|DT|DD)$/.test(t2.nodeName)) {
      const e3 = ((e4) => {
        for (; e4; ) {
          if (yo(e4) || Ro(e4) && e4.data && /[\r\n\s]/.test(e4.data)) return e4;
          e4 = e4.nextSibling;
        }
        return null;
      })(t2.firstChild);
      e3 && /^(UL|OL|DL)$/.test(e3.nodeName) && t2.insertBefore(n2.doc.createTextNode(pr), t2.firstChild);
    }
    const r2 = n2.createRng();
    if (t2.normalize(), t2.hasChildNodes()) {
      const e3 = new Zo(t2, t2);
      let n3, s2 = t2;
      for (; n3 = e3.current(); ) {
        if (Ro(n3)) {
          r2.setStart(n3, 0), r2.setEnd(n3, 0);
          break;
        }
        if (o2[n3.nodeName.toLowerCase()]) {
          r2.setStartBefore(n3), r2.setEndBefore(n3);
          break;
        }
        s2 = n3, n3 = e3.next();
      }
      n3 || (r2.setStart(s2, 0), r2.setEnd(s2, 0));
    } else Po(t2) ? t2.nextSibling && n2.isBlock(t2.nextSibling) ? (r2.setStartBefore(t2), r2.setEndBefore(t2)) : (r2.setStartAfter(t2), r2.setEndAfter(t2)) : (r2.setStart(t2, 0), r2.setEnd(t2, 0));
    e2.selection.setRng(r2), hf(e2, r2);
  }, k_ = (e2, t2) => {
    const n2 = e2.getRoot();
    let o2, r2 = t2;
    for (; r2 !== n2 && r2 && "false" !== e2.getContentEditable(r2); ) "true" === e2.getContentEditable(r2) && (o2 = r2), r2 = r2.parentNode;
    return r2 !== n2 ? o2 : n2;
  }, S_ = (e2) => M.from(e2.dom.getParent(e2.selection.getStart(true), e2.dom.isBlock)), __ = (e2, t2) => {
    const n2 = null == e2 ? void 0 : e2.parentNode;
    return C(n2) && n2.nodeName === t2;
  }, E_ = (e2) => C(e2) && /^(OL|UL|LI)$/.test(e2.nodeName), N_ = (e2) => {
    const t2 = e2.parentNode;
    return C(n2 = t2) && /^(LI|DT|DD)$/.test(n2.nodeName) ? t2 : e2;
    var n2;
  }, R_ = (e2, t2, n2) => {
    let o2 = e2[n2 ? "firstChild" : "lastChild"];
    for (; o2 && !yo(o2); ) o2 = o2[n2 ? "nextSibling" : "previousSibling"];
    return o2 === t2;
  }, A_ = (e2, t2) => t2 && "A" === t2.nodeName && e2.isEmpty(t2), O_ = (e2) => {
    e2.innerHTML = '<br data-mce-bogus="1">';
  }, T_ = (e2, t2) => e2.nodeName === t2 || e2.previousSibling && e2.previousSibling.nodeName === t2, B_ = (e2, t2) => C(t2) && e2.isBlock(t2) && !/^(TD|TH|CAPTION|FORM)$/.test(t2.nodeName) && !/^(fixed|absolute)/i.test(t2.style.position) && "true" !== e2.getContentEditable(t2), D_ = (e2, t2, n2) => Ro(t2) ? e2 ? 1 === n2 && t2.data.charAt(n2 - 1) === _r ? 0 : n2 : n2 === t2.data.length - 1 && t2.data.charAt(n2) === _r ? t2.data.length : n2 : n2, P_ = (e2, t2) => {
    Ji(e2).toLowerCase() === t2.tagName.toLowerCase() && ((e3, t3, n2) => {
      const o2 = e3.dom;
      M.from(n2.style).map(o2.parseStyle).each((e4) => {
        const n3 = { ...Kn(mn(t3)), ...e4 };
        o2.setStyles(t3, n3);
      });
      const r2 = M.from(n2.class).map((e4) => e4.split(/\s+/)), s2 = M.from(t3.className).map((e4) => K(e4.split(/\s+/), (e5) => "" !== e5));
      Dt(r2, s2, (e4, n3) => {
        const r3 = K(n3, (t4) => !j(e4, t4)), s3 = [...e4, ...r3];
        o2.setAttrib(t3, "class", s3.join(" "));
      });
      const a2 = ["style", "class"], i3 = ve(n2, (e4, t4) => !j(a2, t4));
      o2.setAttribs(t3, i3);
    })(e2, t2, Zi(e2));
  }, L_ = { insert: (e2, t2) => {
    let n2, o2, r2, s2, a2 = false;
    const i3 = e2.dom, l2 = e2.schema, d2 = l2.getNonEmptyElements(), c2 = e2.selection.getRng(), u2 = Ji(e2), f2 = (t3) => {
      let o3 = n2;
      const s3 = l2.getTextInlineElements();
      let a3;
      a3 = t3 || "TABLE" === r2 || "HR" === r2 ? i3.create(t3 || u2) : w2.cloneNode(false);
      let d3 = a3;
      if (false === ol(e2)) i3.setAttrib(a3, "style", null), i3.setAttrib(a3, "class", null);
      else do {
        if (s3[o3.nodeName]) {
          if (zc(o3) || Zc(o3)) continue;
          const e3 = o3.cloneNode(false);
          i3.setAttrib(e3, "id", ""), a3.hasChildNodes() ? (e3.appendChild(a3.firstChild), a3.appendChild(e3)) : (d3 = e3, a3.appendChild(e3));
        }
      } while ((o3 = o3.parentNode) && o3 !== v2);
      return P_(e2, a3), O_(d3), a3;
    }, g2 = (e3) => {
      const t3 = D_(e3, n2, o2);
      if (Ro(n2) && (e3 ? t3 > 0 : t3 < n2.data.length)) return false;
      if (n2.parentNode === w2 && a2 && !e3) return true;
      if (e3 && yo(n2) && n2 === w2.firstChild) return true;
      if (T_(n2, "TABLE") || T_(n2, "HR")) return a2 && !e3 || !a2 && e3;
      const r3 = new Zo(n2, w2);
      let s3;
      for (Ro(n2) && (e3 && 0 === t3 ? r3.prev() : e3 || t3 !== n2.data.length || r3.next()); s3 = r3.current(); ) {
        if (yo(s3)) {
          if (!s3.getAttribute("data-mce-bogus")) {
            const e4 = s3.nodeName.toLowerCase();
            if (d2[e4] && "br" !== e4) return false;
          }
        } else if (Ro(s3) && !Jr(s3.data)) return false;
        e3 ? r3.prev() : r3.next();
      }
      return true;
    }, p2 = () => {
      let t3;
      return t3 = /^(H[1-6]|PRE|FIGURE)$/.test(r2) && "HGROUP" !== x2 ? f2(u2) : f2(), ((e3, t4) => {
        const n3 = rl(e3);
        return !y(t4) && (m(n3) ? j(Tt.explode(n3), t4.nodeName.toLowerCase()) : n3);
      })(e2, s2) && B_(i3, s2) && i3.isEmpty(w2) ? t3 = i3.split(s2, w2) : i3.insertAfter(t3, w2), x_(e2, t3), t3;
    };
    Wm(i3, c2).each((e3) => {
      c2.setStart(e3.startContainer, e3.startOffset), c2.setEnd(e3.endContainer, e3.endOffset);
    }), n2 = c2.startContainer, o2 = c2.startOffset;
    const h2 = !(!t2 || !t2.shiftKey), b2 = !(!t2 || !t2.ctrlKey);
    yo(n2) && n2.hasChildNodes() && (a2 = o2 > n2.childNodes.length - 1, n2 = n2.childNodes[Math.min(o2, n2.childNodes.length - 1)] || n2, o2 = a2 && Ro(n2) ? n2.data.length : 0);
    const v2 = k_(i3, n2);
    if (!v2 || ((e3, t3) => {
      const n3 = e3.dom.getParent(t3, "ol,ul,dl");
      return null !== n3 && "false" === e3.dom.getContentEditableParent(n3);
    })(e2, n2)) return;
    h2 || (n2 = ((e3, t3, n3, o3, r3) => {
      var s3;
      const a3 = e3.dom, i4 = null !== (s3 = k_(a3, o3)) && void 0 !== s3 ? s3 : a3.getRoot();
      let l3 = a3.getParent(o3, a3.isBlock);
      if (!l3 || !B_(a3, l3)) {
        let s4;
        if (l3 = l3 || i4, s4 = l3 === e3.getBody() || Uo(l3) ? l3.nodeName.toLowerCase() : l3.parentNode ? l3.parentNode.nodeName.toLowerCase() : "", !l3.hasChildNodes()) {
          const o4 = a3.create(t3);
          return P_(e3, o4), l3.appendChild(o4), n3.setStart(o4, 0), n3.setEnd(o4, 0), o4;
        }
        let d3, c3 = o3;
        for (; c3 && c3.parentNode !== l3; ) c3 = c3.parentNode;
        for (; c3 && !a3.isBlock(c3); ) d3 = c3, c3 = c3.previousSibling;
        if (d3 && e3.schema.isValidChild(s4, t3.toLowerCase())) {
          const s5 = d3.parentNode, i5 = a3.create(t3);
          for (P_(e3, i5), s5.insertBefore(i5, d3), c3 = d3; c3 && !a3.isBlock(c3); ) {
            const e4 = c3.nextSibling;
            i5.appendChild(c3), c3 = e4;
          }
          n3.setStart(o3, r3), n3.setEnd(o3, r3);
        }
      }
      return o3;
    })(e2, u2, c2, n2, o2));
    let w2 = i3.getParent(n2, i3.isBlock) || i3.getRoot();
    s2 = C(null == w2 ? void 0 : w2.parentNode) ? i3.getParent(w2.parentNode, i3.isBlock) : null, r2 = w2 ? w2.nodeName.toUpperCase() : "";
    const x2 = s2 ? s2.nodeName.toUpperCase() : "";
    if ("LI" !== x2 || b2 || (w2 = s2, s2 = s2.parentNode, r2 = x2), /^(LI|DT|DD)$/.test(r2) && yo(s2) && i3.isEmpty(w2)) return void ((e3, t3, n3, o3, r3) => {
      const s3 = e3.dom, a3 = e3.selection.getRng(), i4 = n3.parentNode;
      if (n3 === e3.getBody() || !i4) return;
      var l3;
      E_(l3 = n3) && E_(l3.parentNode) && (r3 = "LI");
      let d3 = t3(r3);
      if (R_(n3, o3, true) && R_(n3, o3, false)) if (__(n3, "LI")) {
        const e4 = N_(n3);
        s3.insertAfter(d3, e4), ((e5) => {
          var t4;
          return (null === (t4 = e5.parentNode) || void 0 === t4 ? void 0 : t4.firstChild) === e5;
        })(n3) ? s3.remove(e4) : s3.remove(n3);
      } else s3.replace(d3, n3);
      else if (R_(n3, o3, true)) __(n3, "LI") ? (s3.insertAfter(d3, N_(n3)), d3.appendChild(s3.doc.createTextNode(" ")), d3.appendChild(n3)) : i4.insertBefore(d3, n3), s3.remove(o3);
      else if (R_(n3, o3, false)) s3.insertAfter(d3, N_(n3)), s3.remove(o3);
      else {
        n3 = N_(n3);
        const e4 = a3.cloneRange();
        e4.setStartAfter(o3), e4.setEndAfter(n3);
        const t4 = e4.extractContents();
        "LI" === r3 && ((e5, t5) => e5.firstChild && "LI" === e5.firstChild.nodeName)(t4) ? (d3 = t4.firstChild, s3.insertAfter(t4, n3)) : (s3.insertAfter(t4, n3), s3.insertAfter(d3, n3)), s3.remove(o3);
      }
      x_(e3, d3);
    })(e2, f2, s2, w2, u2);
    if (w2 === e2.getBody()) return;
    const k2 = w2.parentNode;
    let S2;
    if (Or(w2)) S2 = Fr(w2), i3.isEmpty(w2) && O_(w2), P_(e2, S2), x_(e2, S2);
    else if (g2(false)) S2 = p2();
    else if (g2(true) && k2) S2 = k2.insertBefore(f2(), w2), x_(e2, T_(w2, "HR") ? S2 : w2);
    else {
      const t3 = ((e3) => {
        const t4 = e3.cloneRange();
        return t4.setStart(e3.startContainer, D_(true, e3.startContainer, e3.startOffset)), t4.setEnd(e3.endContainer, D_(false, e3.endContainer, e3.endOffset)), t4;
      })(c2).cloneRange();
      t3.setEndAfter(w2);
      const n3 = t3.extractContents();
      ((e3) => {
        V(br(mn(e3), Ut), (e4) => {
          const t4 = e4.dom;
          t4.nodeValue = Nr(t4.data);
        });
      })(n3), ((e3) => {
        let t4 = e3;
        do {
          Ro(t4) && (t4.data = t4.data.replace(/^[\r\n]+/, "")), t4 = t4.firstChild;
        } while (t4);
      })(n3), S2 = n3.firstChild, i3.insertAfter(n3, w2), ((e3, t4, n4) => {
        var o3;
        const r3 = [];
        if (!n4) return;
        let s3 = n4;
        for (; s3 = s3.firstChild; ) {
          if (e3.isBlock(s3)) return;
          yo(s3) && !t4[s3.nodeName.toLowerCase()] && r3.push(s3);
        }
        let a3 = r3.length;
        for (; a3--; ) s3 = r3[a3], (!s3.hasChildNodes() || s3.firstChild === s3.lastChild && "" === (null === (o3 = s3.firstChild) || void 0 === o3 ? void 0 : o3.nodeValue) || A_(e3, s3)) && e3.remove(s3);
      })(i3, d2, S2), ((e3, t4) => {
        t4.normalize();
        const n4 = t4.lastChild;
        (!n4 || yo(n4) && /^(left|right)$/gi.test(e3.getStyle(n4, "float", true))) && e3.add(t4, "br");
      })(i3, w2), i3.isEmpty(w2) && O_(w2), S2.normalize(), i3.isEmpty(S2) ? (i3.remove(S2), p2()) : (P_(e2, S2), x_(e2, S2));
    }
    i3.setAttrib(S2, "id", ""), e2.dispatch("NewBlock", { newBlock: S2 });
  }, fakeEventName: "insertParagraph" }, M_ = (e2, t2, n2) => {
    const o2 = e2.dom.createRng();
    n2 ? (o2.setStartBefore(t2), o2.setEndBefore(t2)) : (o2.setStartAfter(t2), o2.setEndAfter(t2)), e2.selection.setRng(o2), hf(e2, o2);
  }, I_ = (e2, t2) => {
    const n2 = cn("br");
    Yn(mn(t2), n2), e2.undoManager.add();
  }, F_ = (e2, t2) => {
    U_(e2.getBody(), t2) || Xn(mn(t2), cn("br"));
    const n2 = cn("br");
    Xn(mn(t2), n2), M_(e2, n2.dom, false), e2.undoManager.add();
  }, U_ = (e2, t2) => {
    return n2 = ai.after(t2), !!Po(n2.getNode()) || Lc(e2, ai.after(t2)).map((e3) => Po(e3.getNode())).getOr(false);
    var n2;
  }, z_ = (e2) => e2 && "A" === e2.nodeName && "href" in e2, j_ = (e2) => e2.fold(P, z_, z_, P), H_ = (e2, t2) => {
    t2.fold(S, O(I_, e2), O(F_, e2), S);
  }, $_ = { insert: (e2, t2) => {
    const n2 = ((e3) => {
      const t3 = O(hp, e3), n3 = ai.fromRangeStart(e3.selection.getRng());
      return Mw(t3, e3.getBody(), n3).filter(j_);
    })(e2);
    n2.isSome() ? n2.each(O(H_, e2)) : ((e3, t3) => {
      const n3 = e3.selection, o2 = e3.dom, r2 = n3.getRng();
      let s2, a2 = false;
      Wm(o2, r2).each((e4) => {
        r2.setStart(e4.startContainer, e4.startOffset), r2.setEnd(e4.endContainer, e4.endOffset);
      });
      let i3 = r2.startOffset, l2 = r2.startContainer;
      if (yo(l2) && l2.hasChildNodes()) {
        const e4 = i3 > l2.childNodes.length - 1;
        l2 = l2.childNodes[Math.min(i3, l2.childNodes.length - 1)] || l2, i3 = e4 && Ro(l2) ? l2.data.length : 0;
      }
      let d2 = o2.getParent(l2, o2.isBlock);
      const c2 = d2 && d2.parentNode ? o2.getParent(d2.parentNode, o2.isBlock) : null, u2 = c2 ? c2.nodeName.toUpperCase() : "", m2 = !(!t3 || !t3.ctrlKey);
      "LI" !== u2 || m2 || (d2 = c2), Ro(l2) && i3 >= l2.data.length && (((e4, t4, n4) => {
        const o3 = new Zo(t4, n4);
        let r3;
        const s3 = e4.getNonEmptyElements();
        for (; r3 = o3.next(); ) if (s3[r3.nodeName.toLowerCase()] || Ro(r3) && r3.length > 0) return true;
        return false;
      })(e3.schema, l2, d2 || o2.getRoot()) || (s2 = o2.create("br"), r2.insertNode(s2), r2.setStartAfter(s2), r2.setEndAfter(s2), a2 = true)), s2 = o2.create("br"), li(o2, r2, s2), M_(e3, s2, a2), e3.undoManager.add();
    })(e2, t2);
  }, fakeEventName: "insertLineBreak" }, V_ = (e2, t2) => S_(e2).filter((e3) => t2.length > 0 && pn(mn(e3), t2)).isSome(), q_ = Ti([{ br: [] }, { block: [] }, { none: [] }]), W_ = (e2, t2) => ((e3) => V_(e3, nl(e3)))(e2), K_ = (e2) => (t2, n2) => ((e3) => S_(e3).filter((e4) => ir(mn(e4))).isSome())(t2) === e2, G_ = (e2, t2) => (n2, o2) => {
    const r2 = ((e3) => S_(e3).fold(N(""), (e4) => e4.nodeName.toUpperCase()))(n2) === e2.toUpperCase();
    return r2 === t2;
  }, Y_ = (e2) => {
    const t2 = k_(e2.dom, e2.selection.getStart());
    return y(t2);
  }, X_ = (e2) => G_("pre", e2), Q_ = (e2) => (t2, n2) => Qi(t2) === e2, J_ = (e2, t2) => ((e3) => V_(e3, tl(e3)))(e2), Z_ = (e2, t2) => t2, eE = (e2) => {
    const t2 = Ji(e2), n2 = k_(e2.dom, e2.selection.getStart());
    return C(n2) && e2.schema.isValidChild(n2.nodeName, t2);
  }, tE = (e2, t2) => (n2, o2) => Y(e2, (e3, t3) => e3 && t3(n2, o2), true) ? M.some(t2) : M.none(), nE = (e2, t2, n2) => {
    t2.selection.isCollapsed() || ((e3) => {
      e3.execCommand("delete");
    })(t2), C(n2) && w_(t2, e2.fakeEventName).isDefaultPrevented() || (e2.insert(t2, n2), C(n2) && C_(t2, e2.fakeEventName));
  }, oE = (e2, t2) => {
    const n2 = () => nE($_, e2, t2), o2 = () => nE(L_, e2, t2), r2 = ((e3, t3) => Ew([tE([W_], q_.none()), tE([X_(true), Y_], q_.none()), tE([G_("summary", true)], q_.br()), tE([X_(true), Q_(false), Z_], q_.br()), tE([X_(true), Q_(false)], q_.block()), tE([X_(true), Q_(true), Z_], q_.block()), tE([X_(true), Q_(true)], q_.br()), tE([K_(true), Z_], q_.br()), tE([K_(true)], q_.block()), tE([J_], q_.br()), tE([Z_], q_.br()), tE([eE], q_.block())], [e3, !(!t3 || !t3.shiftKey)]).getOr(q_.none()))(e2, t2);
    switch (el(e2)) {
      case "linebreak":
        r2.fold(n2, n2, S);
        break;
      case "block":
        r2.fold(o2, o2, S);
        break;
      case "invert":
        r2.fold(o2, n2, S);
        break;
      default:
        r2.fold(n2, o2, S);
    }
  }, rE = Ct(), sE = (e2) => e2.stopImmediatePropagation(), aE = (e2) => e2.keyCode === bm.PAGE_UP || e2.keyCode === bm.PAGE_DOWN, iE = (e2, t2, n2) => {
    n2 && !e2.get() ? t2.on("NodeChange", sE, true) : !n2 && e2.get() && t2.off("NodeChange", sE), e2.set(n2);
  }, lE = (e2, t2) => {
    const n2 = t2.container(), o2 = t2.offset();
    return Ro(n2) ? (n2.insertData(o2, e2), M.some(ai(n2, o2 + e2.length))) : fc(t2).map((n3) => {
      const o3 = un(e2);
      return t2.isAtEnd() ? Xn(n3, o3) : Yn(n3, o3), ai(o3.dom, e2.length);
    });
  }, dE = O(lE, pr), cE = O(lE, " "), uE = (e2, t2) => (n2) => ((e3, t3) => !Kg(t3) && (((e4, t4) => ((e5, t5) => Mc(e5.dom, t5).isNone())(e4, t4) || ((e5, t5) => Lc(e5.dom, t5).isNone())(e4, t4) || Dg(e4, t4) || Pg(e4, t4) || zg(e4, t4) || Ug(e4, t4))(e3, t3) || qg(e3, t3) || Wg(e3, t3)))(e2, n2) ? dE(t2) : cE(t2), mE = (e2) => {
    const t2 = ai.fromRangeStart(e2.selection.getRng()), n2 = mn(e2.getBody());
    if (e2.selection.isCollapsed()) {
      const o2 = O(hp, e2), r2 = ai.fromRangeStart(e2.selection.getRng());
      return Mw(o2, e2.getBody(), r2).bind(/* @__PURE__ */ ((e3) => (t3) => t3.fold((t4) => Mc(e3.dom, ai.before(t4)), (e4) => Ic(e4), (e4) => Fc(e4), (t4) => Lc(e3.dom, ai.after(t4))))(n2)).map((o3) => () => uE(n2, t2)(o3).each(/* @__PURE__ */ ((e3) => (t3) => (e3.selection.setRng(t3.toRange()), e3.nodeChanged(), true))(e2)));
    }
    return M.none();
  }, fE = (e2) => Ed(e2) ? [{ keyCode: bm.TAB, action: Ck(Yk, e2, true) }, { keyCode: bm.TAB, shiftKey: true, action: Ck(Yk, e2, false) }] : [], gE = (e2) => {
    if (e2.addShortcut("Meta+P", "", "mcePrint"), v_(e2), By(e2)) return ra(null);
    {
      const t2 = Ox(e2);
      return ((e3) => {
        e3.on("keyup compositionstart", O(lk, e3));
      })(e2), ((e3, t3) => {
        e3.on("keydown", (n2) => {
          n2.isDefaultPrevented() || ((e4, t4, n3) => {
            const o2 = Nt.os.isMacOS() || Nt.os.isiOS();
            wk([{ keyCode: bm.RIGHT, action: Ck(uk, e4, true) }, { keyCode: bm.LEFT, action: Ck(uk, e4, false) }, { keyCode: bm.UP, action: Ck(mk, e4, false) }, { keyCode: bm.DOWN, action: Ck(mk, e4, true) }, ...o2 ? [{ keyCode: bm.UP, action: Ck(gk, e4, false), metaKey: true, shiftKey: true }, { keyCode: bm.DOWN, action: Ck(gk, e4, true), metaKey: true, shiftKey: true }] : [], { keyCode: bm.RIGHT, action: Ck(Vk, e4, true) }, { keyCode: bm.LEFT, action: Ck(Vk, e4, false) }, { keyCode: bm.UP, action: Ck(qk, e4, false) }, { keyCode: bm.DOWN, action: Ck(qk, e4, true) }, { keyCode: bm.RIGHT, action: Ck(kk, e4, true) }, { keyCode: bm.LEFT, action: Ck(kk, e4, false) }, { keyCode: bm.UP, action: Ck(Sk, e4, false) }, { keyCode: bm.DOWN, action: Ck(Sk, e4, true) }, { keyCode: bm.RIGHT, action: Ck(Rx, e4, t4, true) }, { keyCode: bm.LEFT, action: Ck(Rx, e4, t4, false) }, { keyCode: bm.RIGHT, ctrlKey: !o2, altKey: o2, action: Ck(Tx, e4, t4) }, { keyCode: bm.LEFT, ctrlKey: !o2, altKey: o2, action: Ck(Bx, e4, t4) }, { keyCode: bm.UP, action: Ck(bk, e4, false) }, { keyCode: bm.DOWN, action: Ck(bk, e4, true) }], n3).each((e5) => {
              n3.preventDefault();
            });
          })(e3, t3, n2);
        });
      })(e2, t2), ((e3, t3) => {
        e3.on("keydown", (n2) => {
          n2.isDefaultPrevented() || ((e4, t4, n3) => {
            const o2 = n3.keyCode === bm.BACKSPACE ? "deleteContentBackward" : "deleteContentForward";
            xk([{ keyCode: bm.BACKSPACE, action: Ck(Yx, e4) }, { keyCode: bm.BACKSPACE, action: Ck(gw, e4, false) }, { keyCode: bm.DELETE, action: Ck(gw, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(aw, e4, false) }, { keyCode: bm.DELETE, action: Ck(aw, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(Mx, e4, t4, false) }, { keyCode: bm.DELETE, action: Ck(Mx, e4, t4, true) }, { keyCode: bm.BACKSPACE, action: Ck(Jp, e4, false) }, { keyCode: bm.DELETE, action: Ck(Jp, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(pw, e4, false) }, { keyCode: bm.DELETE, action: Ck(pw, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(jx, e4, false) }, { keyCode: bm.DELETE, action: Ck(jx, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(ZC, e4, false) }, { keyCode: bm.DELETE, action: Ck(ZC, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(XC, e4, false) }, { keyCode: bm.DELETE, action: Ck(XC, e4, true) }, { keyCode: bm.BACKSPACE, action: Ck(Ux, e4, false) }, { keyCode: bm.DELETE, action: Ck(Ux, e4, true) }], n3).each((t5) => {
              n3.preventDefault(), w_(e4, o2).isDefaultPrevented() || (t5(), C_(e4, o2));
            });
          })(e3, t3, n2);
        }), e3.on("keyup", (t4) => {
          t4.isDefaultPrevented() || ((e4, t5) => {
            wk([{ keyCode: bm.BACKSPACE, action: Ck(fw, e4) }, { keyCode: bm.DELETE, action: Ck(fw, e4) }], t5);
          })(e3, t4);
        });
      })(e2, t2), ((e3) => {
        e3.on("keydown", (t3) => {
          t3.keyCode === bm.ENTER && ((e4, t4) => {
            var n2;
            t4.isDefaultPrevented() || (t4.preventDefault(), (n2 = e4.undoManager).typing && (n2.typing = false, n2.add()), e4.undoManager.transact(() => {
              oE(e4, t4);
            }));
          })(e3, t3);
        });
      })(e2), ((e3) => {
        e3.on("keydown", (t3) => {
          t3.isDefaultPrevented() || ((e4, t4) => {
            xk([{ keyCode: bm.SPACEBAR, action: Ck(mE, e4) }], t4).each((n2) => {
              t4.preventDefault(), w_(e4, "insertText", { data: " " }).isDefaultPrevented() || (n2(), C_(e4, "insertText", { data: " " }));
            });
          })(e3, t3);
        });
      })(e2), ((e3) => {
        e3.on("input", (t3) => {
          t3.isComposing || ((e4) => {
            const t4 = mn(e4.getBody());
            e4.selection.isCollapsed() && np(t4, ai.fromRangeStart(e4.selection.getRng())).each((t5) => {
              e4.selection.setRng(t5.toRange());
            });
          })(e3);
        });
      })(e2), ((e3) => {
        e3.on("keydown", (t3) => {
          t3.isDefaultPrevented() || ((e4, t4) => {
            wk([...fE(e4)], t4).each((e5) => {
              t4.preventDefault();
            });
          })(e3, t3);
        });
      })(e2), ((e3, t3) => {
        e3.on("keydown", (n2) => {
          n2.isDefaultPrevented() || ((e4, t4, n3) => {
            const o2 = Nt.os.isMacOS() || Nt.os.isiOS();
            wk([{ keyCode: bm.END, action: Ck(fk, e4, true) }, { keyCode: bm.HOME, action: Ck(fk, e4, false) }, ...o2 ? [] : [{ keyCode: bm.HOME, action: Ck(gk, e4, false), ctrlKey: true, shiftKey: true }, { keyCode: bm.END, action: Ck(gk, e4, true), ctrlKey: true, shiftKey: true }], { keyCode: bm.END, action: Ck(_k, e4, true) }, { keyCode: bm.HOME, action: Ck(_k, e4, false) }, { keyCode: bm.END, action: Ck(Dx, e4, true, t4) }, { keyCode: bm.HOME, action: Ck(Dx, e4, false, t4) }], n3).each((e5) => {
              n3.preventDefault();
            });
          })(e3, t3, n2);
        });
      })(e2, t2), ((e3, t3) => {
        if (rE.os.isMacOS()) return;
        const n2 = ra(false);
        e3.on("keydown", (t4) => {
          aE(t4) && iE(n2, e3, true);
        }), e3.on("keyup", (o2) => {
          o2.isDefaultPrevented() || ((e4, t4, n3) => {
            wk([{ keyCode: bm.PAGE_UP, action: Ck(Dx, e4, false, t4) }, { keyCode: bm.PAGE_DOWN, action: Ck(Dx, e4, true, t4) }], n3);
          })(e3, t3, o2), aE(o2) && n2.get() && (iE(n2, e3, false), e3.nodeChanged());
        });
      })(e2, t2), t2;
    }
  };
  class pE {
    constructor(e2) {
      let t2;
      this.lastPath = [], this.editor = e2;
      const n2 = this;
      "onselectionchange" in e2.getDoc() || e2.on("NodeChange click mouseup keyup focus", (n3) => {
        const o2 = e2.selection.getRng(), r2 = { startContainer: o2.startContainer, startOffset: o2.startOffset, endContainer: o2.endContainer, endOffset: o2.endOffset };
        "nodechange" !== n3.type && Um(r2, t2) || e2.dispatch("SelectionChange"), t2 = r2;
      }), e2.on("contextmenu", () => {
        e2.dispatch("SelectionChange");
      }), e2.on("SelectionChange", () => {
        const t3 = e2.selection.getStart(true);
        t3 && vu(e2) && !n2.isSameElementPath(t3) && e2.dom.isChildOf(t3, e2.getBody()) && e2.nodeChanged({ selectionChange: true });
      }), e2.on("mouseup", (t3) => {
        !t3.isDefaultPrevented() && vu(e2) && ("IMG" === e2.selection.getNode().nodeName ? Af.setEditorTimeout(e2, () => {
          e2.nodeChanged();
        }) : e2.nodeChanged());
      });
    }
    nodeChanged(e2 = {}) {
      const t2 = this.editor.selection;
      let n2;
      if (this.editor.initialized && t2 && !Wl(this.editor) && !this.editor.mode.isReadOnly()) {
        const o2 = this.editor.getBody();
        n2 = t2.getStart(true) || o2, n2.ownerDocument === this.editor.getDoc() && this.editor.dom.isChildOf(n2, o2) || (n2 = o2);
        const r2 = [];
        this.editor.dom.getParent(n2, (e3) => e3 === o2 || (r2.push(e3), false)), this.editor.dispatch("NodeChange", { ...e2, element: n2, parents: r2 });
      }
    }
    isSameElementPath(e2) {
      let t2;
      const n2 = this.editor, o2 = ne(n2.dom.getParents(e2, L, n2.getBody()));
      if (o2.length === this.lastPath.length) {
        for (t2 = o2.length; t2 >= 0 && o2[t2] === this.lastPath[t2]; t2--) ;
        if (-1 === t2) return this.lastPath = o2, true;
      }
      return this.lastPath = o2, false;
    }
  }
  const hE = N("x-tinymce/html"), bE = "<!-- x-tinymce/html -->", vE = (e2) => bE + e2, yE = (e2) => -1 !== e2.indexOf(bE), CE = "%MCEPASTEBIN%", wE = (e2) => e2.dom.get("mcepastebin"), xE = (e2) => C(e2) && "mcepastebin" === e2.id, kE = (e2) => e2 === CE, SE = (e2, t2) => (Tt.each(t2, (t3) => {
    e2 = u(t3, RegExp) ? e2.replace(t3, "") : e2.replace(t3[0], t3[1]);
  }), e2), _E = (e2) => SE(e2, [/^[\s\S]*<body[^>]*>\s*|\s*<\/body[^>]*>[\s\S]*$/gi, /<!--StartFragment-->|<!--EndFragment-->/g, [/( ?)<span class="Apple-converted-space">\u00a0<\/span>( ?)/g, (e3, t2, n2) => t2 || n2 ? pr : " "], /<br class="Apple-interchange-newline">/g, /<br>$/i]), EE = (e2, t2) => ({ content: e2, cancelled: t2 }), NE = (e2, t2) => (e2.insertContent(t2, { merge: gd(e2), paste: true }), true), RE = (e2) => /^https?:\/\/[\w\-\/+=.,!;:&%@^~(){}?#]+$/i.test(e2), AE = (e2, t2, n2) => !(e2.selection.isCollapsed() || !RE(t2)) && ((e3, t3, n3) => (e3.undoManager.extra(() => {
    n3(e3, t3);
  }, () => {
    e3.execCommand("mceInsertLink", false, t3);
  }), true))(e2, t2, n2), OE = (e2, t2, n2) => !!((e3, t3) => RE(t3) && H(_d(e3), (e4) => je(t3.toLowerCase(), `.${e4.toLowerCase()}`)))(e2, t2) && ((e3, t3, n3) => (e3.undoManager.extra(() => {
    n3(e3, t3);
  }, () => {
    e3.insertContent('<img src="' + t3 + '">');
  }), true))(e2, t2, n2), TE = /* @__PURE__ */ ((e2) => {
    let t2 = 0;
    return () => "mceclip" + t2++;
  })(), BE = (e2, t2, n2, o2) => {
    const r2 = ((e3, t3, n3) => ((e4, t4, n4) => {
      const o3 = ((e5, t5, n5) => e5.dispatch("PastePreProcess", { content: t5, internal: n5 }))(e4, t4, n4), r3 = ((e5, t5) => {
        const n5 = Zv({}, e5.schema);
        n5.addNodeFilter("meta", (e6) => {
          Tt.each(e6, (e7) => {
            e7.remove();
          });
        });
        const o4 = n5.parse(t5, { forced_root_block: false, isRootContent: true });
        return ig({ validate: true }, e5.schema).serialize(o4);
      })(e4, o3.content);
      return e4.hasEventListeners("PastePostProcess") && !o3.isDefaultPrevented() ? ((e5, t5, n5) => {
        const o4 = e5.dom.create("div", { style: "display:none" }, t5), r4 = ((e6, t6, n6) => e6.dispatch("PastePostProcess", { node: t6, internal: n6 }))(e5, o4, n5);
        return EE(r4.node.innerHTML, r4.isDefaultPrevented());
      })(e4, r3, n4) : EE(r3, o3.isDefaultPrevented());
    })(e3, t3, n3))(e2, t2, n2);
    r2.cancelled || ((e3, t3, n3) => {
      n3 || !pd(e3) ? NE(e3, t3) : ((e4, t4) => {
        Tt.each([AE, OE, NE], (n4) => !n4(e4, t4, NE));
      })(e3, t3);
    })(e2, r2.content, o2);
  }, DE = (e2, t2, n2) => {
    const o2 = n2 || yE(t2);
    BE(e2, ((e3) => e3.replace(bE, ""))(t2), o2, false);
  }, PE = (e2, t2) => {
    const n2 = e2.dom.encode(t2).replace(/\r\n/g, "\n"), o2 = ((e3, t3, n3) => {
      const o3 = e3.split(/\n\n/), r2 = ((e4, t4) => {
        let n4 = "<" + e4;
        const o4 = ye(t4, (e5, t5) => t5 + '="' + xs.encodeAllRaw(e5) + '"');
        return o4.length && (n4 += " " + o4.join(" ")), n4 + ">";
      })(t3, n3), s2 = "</" + t3 + ">", a2 = $2(o3, (e4) => e4.split(/\n/).join("<br />"));
      return 1 === a2.length ? a2[0] : $2(a2, (e4) => r2 + e4 + s2).join("");
    })(es(n2, bd(e2)), Ji(e2), Zi(e2));
    BE(e2, o2, false, true);
  }, LE = (e2) => {
    const t2 = {};
    if (e2 && e2.types) for (let n2 = 0; n2 < e2.types.length; n2++) {
      const o2 = e2.types[n2];
      try {
        t2[o2] = e2.getData(o2);
      } catch (e3) {
        t2[o2] = "";
      }
    }
    return t2;
  }, ME = (e2, t2) => t2 in e2 && e2[t2].length > 0, IE = (e2) => ME(e2, "text/html") || ME(e2, "text/plain"), FE = (e2, t2, n2) => {
    const o2 = "paste" === t2.type ? t2.clipboardData : t2.dataTransfer;
    var r2;
    if (dd(e2) && o2) {
      const s2 = ((e3, t3) => {
        const n3 = t3.items ? ee(de(t3.items), (e4) => "file" === e4.kind ? [e4.getAsFile()] : []) : [], o3 = t3.files ? de(t3.files) : [];
        return K(n3.length > 0 ? n3 : o3, ((e4) => {
          const t4 = _d(e4);
          return (e5) => ze(e5.type, "image/") && H(t4, (t5) => ((e6) => {
            const t6 = e6.toLowerCase(), n4 = { jpg: "jpeg", jpe: "jpeg", jfi: "jpeg", jif: "jpeg", jfif: "jpeg", pjpeg: "jpeg", pjp: "jpeg", svg: "svg+xml" };
            return Tt.hasOwn(n4, t6) ? "image/" + n4[t6] : "image/" + t6;
          })(t5) === e5.type);
        })(e3));
      })(e2, o2);
      if (s2.length > 0) return t2.preventDefault(), (r2 = s2, Promise.all($2(r2, (e3) => Pv(e3).then((t3) => ({ file: e3, uri: t3 }))))).then((t3) => {
        n2 && e2.selection.setRng(n2), V(t3, (t4) => {
          ((e3, t5) => {
            Bv(t5.uri).each(({ data: n3, type: o3, base64Encoded: r3 }) => {
              const s3 = r3 ? n3 : btoa(n3), a2 = t5.file, i3 = e3.editorUpload.blobCache, l2 = i3.getByData(s3, o3), d2 = null != l2 ? l2 : ((e4, t6, n4, o4) => {
                const r4 = TE(), s4 = al(e4) && C(n4.name), a3 = s4 ? ((e5, t7) => {
                  const n5 = t7.match(/([\s\S]+?)(?:\.[a-z0-9.]+)$/i);
                  return C(n5) ? e5.dom.encode(n5[1]) : void 0;
                })(e4, n4.name) : r4, i4 = s4 ? n4.name : void 0, l3 = t6.create(r4, n4, o4, a3, i4);
                return t6.add(l3), l3;
              })(e3, i3, a2, s3);
              DE(e3, `<img src="${d2.blobUri()}">`, false);
            });
          })(e2, t4);
        });
      }), true;
    }
    return false;
  }, UE = (e2, t2, n2, o2) => {
    let r2 = _E(n2);
    const s2 = ME(t2, hE()) || yE(n2), a2 = !s2 && ((e3) => !/<(?:\/?(?!(?:div|p|br|span)>)\w+|(?:(?!(?:span style="white-space:\s?pre;?">)|br\s?\/>))\w+\s[^>]+)>/i.test(e3))(r2), i3 = RE(r2);
    (kE(r2) || !r2.length || a2 && !i3) && (o2 = true), (o2 || i3) && (r2 = ME(t2, "text/plain") && a2 ? t2["text/plain"] : ((e3) => {
      const t3 = Ls(), n3 = Zv({}, t3);
      let o3 = "";
      const r3 = t3.getVoidElements(), s3 = Tt.makeMap("script noscript style textarea video audio iframe object", " "), a3 = t3.getBlockElements(), i4 = (e4) => {
        const n4 = e4.name, l2 = e4;
        if ("br" !== n4) {
          if ("wbr" !== n4) if (r3[n4] && (o3 += " "), s3[n4]) o3 += " ";
          else {
            if (3 === e4.type && (o3 += e4.value), !(e4.name in t3.getVoidElements())) {
              let t4 = e4.firstChild;
              if (t4) do {
                i4(t4);
              } while (t4 = t4.next);
            }
            a3[n4] && l2.next && (o3 += "\n", "p" === n4 && (o3 += "\n"));
          }
        } else o3 += "\n";
      };
      return e3 = SE(e3, [/<!\[[^\]]+\]>/g]), i4(n3.parse(e3)), o3;
    })(r2)), kE(r2) || (o2 ? PE(e2, r2) : DE(e2, r2, s2));
  }, zE = (e2, t2, n2) => {
    ((e3, t3, n3) => {
      let o2;
      e3.on("keydown", (e4) => {
        ((e5) => bm.metaKeyPressed(e5) && 86 === e5.keyCode || e5.shiftKey && 45 === e5.keyCode)(e4) && !e4.isDefaultPrevented() && (o2 = e4.shiftKey && 86 === e4.keyCode);
      }), e3.on("paste", (r2) => {
        if (r2.isDefaultPrevented() || ((e4) => {
          var t4, n4;
          return Nt.os.isAndroid() && 0 === (null === (n4 = null === (t4 = e4.clipboardData) || void 0 === t4 ? void 0 : t4.items) || void 0 === n4 ? void 0 : n4.length);
        })(r2)) return;
        const s2 = "text" === n3.get() || o2;
        o2 = false;
        const a2 = LE(r2.clipboardData);
        !IE(a2) && FE(e3, r2, t3.getLastRng() || e3.selection.getRng()) || (ME(a2, "text/html") ? (r2.preventDefault(), UE(e3, a2, a2["text/html"], s2)) : (t3.create(), Af.setEditorTimeout(e3, () => {
          const n4 = t3.getHtml();
          t3.remove(), UE(e3, a2, n4, s2);
        }, 0)));
      });
    })(e2, t2, n2), ((e3) => {
      const t3 = (e4) => ze(e4, "webkit-fake-url"), n3 = (e4) => ze(e4, "data:");
      e3.parser.addNodeFilter("img", (o2, r2, s2) => {
        if (!dd(e3) && ((e4) => {
          var t4;
          return true === (null === (t4 = e4.data) || void 0 === t4 ? void 0 : t4.paste);
        })(s2)) for (const r3 of o2) {
          const o3 = r3.attr("src");
          m(o3) && !r3.attr("data-mce-object") && o3 !== Nt.transparentSrc && (t3(o3) || !vd(e3) && n3(o3)) && r3.remove();
        }
      });
    })(e2);
  }, jE = (e2, t2, n2, o2) => {
    ((e3, t3, n3) => {
      if (!e3) return false;
      try {
        return e3.clearData(), e3.setData("text/html", t3), e3.setData("text/plain", n3), e3.setData(hE(), t3), true;
      } catch (e4) {
        return false;
      }
    })(e2.clipboardData, t2.html, t2.text) ? (e2.preventDefault(), o2()) : n2(t2.html, o2);
  }, HE = (e2) => (t2, n2) => {
    const { dom: o2, selection: r2 } = e2, s2 = o2.create("div", { contenteditable: "false", "data-mce-bogus": "all" }), a2 = o2.create("div", { contenteditable: "true" }, t2);
    o2.setStyles(s2, { position: "fixed", top: "0", left: "-3000px", width: "1000px", overflow: "hidden" }), s2.appendChild(a2), o2.add(e2.getBody(), s2);
    const i3 = r2.getRng();
    a2.focus();
    const l2 = o2.createRng();
    l2.selectNodeContents(a2), r2.setRng(l2), Af.setEditorTimeout(e2, () => {
      r2.setRng(i3), o2.remove(s2), n2();
    }, 0);
  }, $E = (e2) => ({ html: vE(e2.selection.getContent({ contextual: true })), text: e2.selection.getContent({ format: "text" }) }), VE = (e2) => !e2.selection.isCollapsed() || ((e3) => !!e3.dom.getParent(e3.selection.getStart(), "td[data-mce-selected],th[data-mce-selected]", e3.getBody()))(e2), qE = (e2, t2) => {
    var n2, o2;
    return Ym.getCaretRangeFromPoint(null !== (n2 = t2.clientX) && void 0 !== n2 ? n2 : 0, null !== (o2 = t2.clientY) && void 0 !== o2 ? o2 : 0, e2.getDoc());
  }, WE = (e2, t2) => {
    e2.focus(), t2 && e2.selection.setRng(t2);
  }, KE = /rgb\s*\(\s*([0-9]+)\s*,\s*([0-9]+)\s*,\s*([0-9]+)\s*\)/gi, GE = (e2) => Tt.trim(e2).replace(KE, du).toLowerCase(), YE = (e2, t2, n2) => {
    const o2 = md(e2);
    if (n2 || "all" === o2 || !fd(e2)) return t2;
    const r2 = o2 ? o2.split(/[, ]/) : [];
    if (r2 && "none" !== o2) {
      const n3 = e2.dom, o3 = e2.selection.getNode();
      t2 = t2.replace(/(<[^>]+) style="([^"]*)"([^>]*>)/gi, (e3, t3, s2, a2) => {
        const i3 = n3.parseStyle(n3.decode(s2)), l2 = {};
        for (let e4 = 0; e4 < r2.length; e4++) {
          const t4 = i3[r2[e4]];
          let s3 = t4, a3 = n3.getStyle(o3, r2[e4], true);
          /color/.test(r2[e4]) && (s3 = GE(s3), a3 = GE(a3)), a3 !== s3 && (l2[r2[e4]] = t4);
        }
        const d2 = n3.serializeStyle(l2, "span");
        return d2 ? t3 + ' style="' + d2 + '"' + a2 : t3 + a2;
      });
    } else t2 = t2.replace(/(<[^>]+) style="([^"]*)"([^>]*>)/gi, "$1$3");
    return t2 = t2.replace(/(<[^>]+) data-mce-style="([^"]+)"([^>]*>)/gi, (e3, t3, n3, o3) => t3 + ' style="' + n3 + '"' + o3), t2;
  }, XE = (e2) => {
    const t2 = ra(false), n2 = ra(hd(e2) ? "text" : "html"), o2 = ((e3) => {
      const t3 = ra(null);
      return { create: () => ((e4, t4) => {
        const { dom: n3, selection: o3 } = e4, r2 = e4.getBody();
        t4.set(o3.getRng());
        const s2 = n3.add(e4.getBody(), "div", { id: "mcepastebin", class: "mce-pastebin", contentEditable: true, "data-mce-bogus": "all", style: "position: fixed; top: 50%; width: 10px; height: 10px; overflow: hidden; opacity: 0" }, CE);
        Nt.browser.isFirefox() && n3.setStyle(s2, "left", "rtl" === n3.getStyle(r2, "direction", true) ? 65535 : -65535), n3.bind(s2, "beforedeactivate focusin focusout", (e5) => {
          e5.stopPropagation();
        }), s2.focus(), o3.select(s2, true);
      })(e3, t3), remove: () => ((e4, t4) => {
        const n3 = e4.dom;
        if (wE(e4)) {
          let o3;
          const r2 = t4.get();
          for (; o3 = wE(e4); ) n3.remove(o3), n3.unbind(o3);
          r2 && e4.selection.setRng(r2);
        }
        t4.set(null);
      })(e3, t3), getEl: () => wE(e3), getHtml: () => ((e4) => {
        const t4 = e4.dom, n3 = (e5, n4) => {
          e5.appendChild(n4), t4.remove(n4, true);
        }, [o3, ...r2] = K(e4.getBody().childNodes, xE);
        V(r2, (e5) => {
          n3(o3, e5);
        });
        const s2 = t4.select("div[id=mcepastebin]", o3);
        for (let e5 = s2.length - 1; e5 >= 0; e5--) {
          const r3 = t4.create("div");
          o3.insertBefore(r3, s2[e5]), n3(r3, s2[e5]);
        }
        return o3 ? o3.innerHTML : "";
      })(e3), getLastRng: t3.get };
    })(e2);
    ((e3) => {
      (Nt.browser.isChromium() || Nt.browser.isSafari()) && ((e4, t3) => {
        e4.on("PastePreProcess", (n3) => {
          n3.content = t3(e4, n3.content, n3.internal);
        });
      })(e3, YE);
    })(e2), ((e3, t3) => {
      e3.addCommand("mceTogglePlainTextPaste", () => {
        ((e4, t4) => {
          "text" === t4.get() ? (t4.set("html"), hm(e4, false)) : (t4.set("text"), hm(e4, true)), e4.focus();
        })(e3, t3);
      }), e3.addCommand("mceInsertClipboardContent", (t4, n3) => {
        n3.html && DE(e3, n3.html, n3.internal), n3.text && PE(e3, n3.text);
      });
    })(e2, n2), ((e3) => {
      const t3 = (t4) => (n4) => {
        t4(e3, n4);
      }, n3 = cd(e3);
      w(n3) && e3.on("PastePreProcess", t3(n3));
      const o3 = ud(e3);
      w(o3) && e3.on("PastePostProcess", t3(o3));
    })(e2), e2.on("PreInit", () => {
      ((e3) => {
        e3.on("cut", /* @__PURE__ */ ((e4) => (t3) => {
          !t3.isDefaultPrevented() && VE(e4) && jE(t3, $E(e4), HE(e4), () => {
            if (Nt.browser.isChromium() || Nt.browser.isFirefox()) {
              const t4 = e4.selection.getRng();
              Af.setEditorTimeout(e4, () => {
                e4.selection.setRng(t4), e4.execCommand("Delete");
              }, 0);
            } else e4.execCommand("Delete");
          });
        })(e3)), e3.on("copy", /* @__PURE__ */ ((e4) => (t3) => {
          !t3.isDefaultPrevented() && VE(e4) && jE(t3, $E(e4), HE(e4), S);
        })(e3));
      })(e2), ((e3, t3) => {
        ld(e3) && e3.on("dragend dragover draggesture dragdrop drop drag", (e4) => {
          e4.preventDefault(), e4.stopPropagation();
        }), dd(e3) || e3.on("drop", (e4) => {
          const t4 = e4.dataTransfer;
          t4 && ((e5) => H(e5.files, (e6) => /^image\//.test(e6.type)))(t4) && e4.preventDefault();
        }), e3.on("drop", (n3) => {
          if (n3.isDefaultPrevented() || t3.get()) return;
          const o3 = qE(e3, n3);
          if (y(o3)) return;
          const r2 = LE(n3.dataTransfer), s2 = ME(r2, hE());
          if ((!IE(r2) || ((e4) => {
            const t4 = e4["text/plain"];
            return !!t4 && 0 === t4.indexOf("file://");
          })(r2)) && FE(e3, n3, o3)) return;
          const a2 = r2[hE()], i3 = a2 || r2["text/html"] || r2["text/plain"];
          i3 && (n3.preventDefault(), Af.setEditorTimeout(e3, () => {
            e3.undoManager.transact(() => {
              a2 && e3.execCommand("Delete"), WE(e3, o3);
              const t4 = _E(i3);
              r2["text/html"] ? DE(e3, t4, s2) : PE(e3, t4);
            });
          }));
        }), e3.on("dragstart", (e4) => {
          t3.set(true);
        }), e3.on("dragover dragend", (n3) => {
          dd(e3) && !t3.get() && (n3.preventDefault(), WE(e3, qE(e3, n3))), "dragend" === n3.type && t3.set(false);
        });
      })(e2, t2), zE(e2, o2, n2);
    });
  }, QE = Po, JE = Ro, ZE = (e2) => Io(e2.dom), eN = (e2) => (t2) => bn(mn(e2), t2), tN = (e2, t2) => Ko(mn(e2), ZE, eN(t2)), nN = (e2, t2, n2) => {
    const o2 = new Zo(e2, t2), r2 = n2 ? o2.next.bind(o2) : o2.prev.bind(o2);
    let s2 = e2;
    for (let t3 = n2 ? e2 : r2(); t3 && !QE(t3); t3 = r2()) Gr(t3) && (s2 = t3);
    return s2;
  }, oN = (e2) => {
    const t2 = ((e3, t3) => {
      const n2 = ai.fromRangeStart(e3).getNode(), o2 = ((e4, t4) => Ko(mn(e4), (e5) => ((e6) => Mo(e6.dom))(e5) || nr(e5), eN(t4)).getOr(mn(t4)).dom)(n2, t3), r2 = nN(n2, o2, false), s2 = nN(n2, o2, true), a2 = document.createRange();
      return tN(r2, o2).fold(() => {
        JE(r2) ? a2.setStart(r2, 0) : a2.setStartBefore(r2);
      }, (e4) => a2.setStartBefore(e4.dom)), tN(s2, o2).fold(() => {
        JE(s2) ? a2.setEnd(s2, s2.data.length) : a2.setEndAfter(s2);
      }, (e4) => a2.setEndAfter(e4.dom)), a2;
    })(e2.selection.getRng(), e2.getBody());
    e2.selection.setRng(uh(t2));
  };
  var rN;
  !function(e2) {
    e2.Before = "before", e2.After = "after";
  }(rN || (rN = {}));
  const sN = (e2, t2) => Math.abs(e2.left - t2), aN = (e2, t2) => Math.abs(e2.right - t2), iN = (e2, t2) => ((e3) => Y(e3, (e4, t3) => e4.fold(() => M.some(t3), (e5) => {
    const n2 = Math.min(t3.left, e5.left), o2 = Math.min(t3.top, e5.top), r2 = Math.max(t3.right, e5.right), s2 = Math.max(t3.bottom, e5.bottom);
    return M.some({ top: o2, right: r2, bottom: s2, left: n2, width: r2 - n2, height: s2 - o2 });
  }), M.none()))(K(e2, (e3) => {
    return (n2 = t2) >= (o2 = e3).top && n2 <= o2.bottom;
    var n2, o2;
  })).fold(() => [[], e2], (t3) => {
    const { pass: n2, fail: o2 } = W(e2, (e3) => ((e4, t4) => {
      const n3 = ((e5, t5) => Math.max(0, Math.min(e5.bottom, t5.bottom) - Math.max(e5.top, t5.top)))(e4, t4) / Math.min(e4.height, t4.height);
      return ((e5, t5) => e5.top < t5.bottom && e5.bottom > t5.top)(e4, t4) && n3 > 0.5;
    })(e3, t3));
    return [n2, o2];
  }), lN = (e2, t2, n2) => t2 > e2.left && t2 < e2.right ? 0 : Math.min(Math.abs(e2.left - t2), Math.abs(e2.right - t2)), dN = (e2, t2, n2) => {
    const o2 = (e3) => Gr(e3.node) ? M.some(e3) : yo(e3.node) ? dN(de(e3.node.childNodes), t2, n2) : M.none(), r2 = (e3, r3) => {
      const s3 = se(e3, (e4, o3) => r3(e4, t2, n2) - r3(o3, t2, n2));
      return ((e4, r4) => {
        if (e4.length >= 2) {
          const s4 = o2(e4[0]).getOr(e4[0]), a3 = o2(e4[1]).getOr(e4[1]);
          if (Math.abs(r4(s4, t2, n2) - r4(a3, t2, n2)) < 2) {
            if (Ro(s4.node)) return M.some(s4);
            if (Ro(a3.node)) return M.some(a3);
          }
        }
        return M.none();
      })(s3, r3).orThunk(() => ce(s3, o2));
    }, [s2, a2] = iN(cx(e2), n2), { pass: i3, fail: l2 } = W(a2, (e3) => e3.top < n2);
    return r2(s2, lN).orThunk(() => r2(l2, Fa)).orThunk(() => r2(i3, Fa));
  }, cN = (e2, t2, n2) => ((e3, t3, n3) => {
    const o2 = mn(e3), r2 = Cn(o2), s2 = fn(r2, t3, n3).filter((e4) => vn(o2, e4)).getOr(o2);
    return ((e4, t4, n4, o3) => {
      const r3 = (t5, s3) => s3.fold(() => dN(de(t5.dom.childNodes), n4, o3), (e5) => {
        const r4 = K(de(t5.dom.childNodes), (t6) => t6 !== e5.dom);
        return dN(r4, n4, o3);
      }).orThunk(() => {
        var n5;
        return (bn(t5, e4) ? M.none() : (n5 = t5, M.from(n5.dom.parentElement).map(mn))).bind((e5) => r3(e5, M.some(t5)));
      });
      return r3(t4, M.none());
    })(o2, s2, t3, n3);
  })(e2, t2, n2).filter((e3) => Hd(e3.node)).map((e3) => ((e4, t3) => ({ node: e4.node, position: sN(e4, t3) < aN(e4, t3) ? rN.Before : rN.After }))(e3, t2)), uN = (e2) => {
    var t2, n2;
    const o2 = e2.getBoundingClientRect(), r2 = e2.ownerDocument, s2 = r2.documentElement, a2 = r2.defaultView;
    return { top: o2.top + (null !== (t2 = null == a2 ? void 0 : a2.scrollY) && void 0 !== t2 ? t2 : 0) - s2.clientTop, left: o2.left + (null !== (n2 = null == a2 ? void 0 : a2.scrollX) && void 0 !== n2 ? n2 : 0) - s2.clientLeft };
  }, mN = Io, fN = /* @__PURE__ */ ((...e2) => (t2) => {
    for (let n2 = 0; n2 < e2.length; n2++) if (e2[n2](t2)) return true;
    return false;
  })(mN, Mo), gN = (e2, t2, n2, o2) => {
    const r2 = e2.dom, s2 = t2.cloneNode(true);
    r2.setStyles(s2, { width: n2, height: o2 }), r2.setAttrib(s2, "data-mce-selected", null);
    const a2 = r2.create("div", { class: "mce-drag-container", "data-mce-bogus": "all", unselectable: "on", contenteditable: "false" });
    return r2.setStyles(a2, { position: "absolute", opacity: 0.5, overflow: "hidden", border: 0, padding: 0, margin: 0, width: n2, height: o2 }), r2.setStyles(s2, { margin: 0, boxSizing: "border-box" }), a2.appendChild(s2), a2;
  }, pN = (e2, t2) => (n2) => () => {
    const o2 = "left" === e2 ? n2.scrollX : n2.scrollY;
    n2.scroll({ [e2]: o2 + t2, behavior: "smooth" });
  }, hN = pN("left", -32), bN = pN("left", 32), vN = pN("top", -32), yN = pN("top", 32), CN = (e2) => {
    e2 && e2.parentNode && e2.parentNode.removeChild(e2);
  }, wN = (e2, t2) => {
    const n2 = ma((e3, n3) => {
      t2._selectionOverrides.hideFakeCaret(), t2.selection.placeCaretAt(e3, n3);
    }, 0);
    t2.on("remove", n2.cancel);
    const o2 = e2;
    return (r2) => e2.on((e3) => {
      const s2 = Math.max(Math.abs(r2.screenX - e3.screenX), Math.abs(r2.screenY - e3.screenY));
      if (!e3.dragging && s2 > 10) {
        if (t2.dispatch("dragstart", { target: e3.element }).isDefaultPrevented()) return;
        e3.dragging = true, t2.focus();
      }
      if (e3.dragging) {
        const s3 = ((e4, t3) => ({ pageX: t3.pageX - e4.relX, pageY: t3.pageY + 5 }))(e3, ((e4, t3) => {
          return n3 = ((e5) => e5.inline ? uN(e5.getBody()) : { left: 0, top: 0 })(e4), o3 = ((e5) => {
            const t4 = e5.getBody();
            return e5.inline ? { left: t4.scrollLeft, top: t4.scrollTop } : { left: 0, top: 0 };
          })(e4), r3 = ((e5, t4) => {
            if (t4.target.ownerDocument !== e5.getDoc()) {
              const n4 = uN(e5.getContentAreaContainer()), o4 = ((e6) => {
                const t5 = e6.getBody(), n5 = e6.getDoc().documentElement, o5 = { left: t5.scrollLeft, top: t5.scrollTop }, r4 = { left: t5.scrollLeft || n5.scrollLeft, top: t5.scrollTop || n5.scrollTop };
                return e6.inline ? o5 : r4;
              })(e5);
              return { left: t4.pageX - n4.left + o4.left, top: t4.pageY - n4.top + o4.top };
            }
            return { left: t4.pageX, top: t4.pageY };
          })(e4, t3), { pageX: r3.left - n3.left + o3.left, pageY: r3.top - n3.top + o3.top };
          var n3, o3, r3;
        })(t2, r2));
        a2 = e3.ghost, i3 = t2.getBody(), a2.parentNode !== i3 && i3.appendChild(a2), ((e4, t3, n3, o3, r3, s4, a3, i4, l2, d2, c2) => {
          let u2 = 0, m2 = 0;
          e4.style.left = t3.pageX + "px", e4.style.top = t3.pageY + "px", t3.pageX + n3 > r3 && (u2 = t3.pageX + n3 - r3), t3.pageY + o3 > s4 && (m2 = t3.pageY + o3 - s4), e4.style.width = n3 - u2 + "px", e4.style.height = o3 - m2 + "px";
          const f2 = l2.clientHeight, g2 = l2.clientWidth, p2 = a3 + l2.getBoundingClientRect().top, h2 = i4 + l2.getBoundingClientRect().left;
          c2.on((e5) => {
            e5.intervalId.clear(), e5.dragging && (a3 + 8 >= f2 ? e5.intervalId.set(yN(d2)) : a3 - 8 <= 0 ? e5.intervalId.set(vN(d2)) : i4 + 8 >= g2 ? e5.intervalId.set(bN(d2)) : i4 - 8 <= 0 ? e5.intervalId.set(hN(d2)) : p2 + 16 >= window.innerHeight ? e5.intervalId.set(yN(window)) : p2 - 16 <= 0 ? e5.intervalId.set(vN(window)) : h2 + 16 >= window.innerWidth ? e5.intervalId.set(bN(window)) : h2 - 16 <= 0 && e5.intervalId.set(hN(window)));
          });
        })(e3.ghost, s3, e3.width, e3.height, e3.maxX, e3.maxY, r2.clientY, r2.clientX, t2.getContentAreaContainer(), t2.getWin(), o2), n2.throttle(r2.clientX, r2.clientY);
      }
      var a2, i3;
    });
  }, xN = (e2) => {
    e2.on((e3) => {
      e3.intervalId.clear(), CN(e3.ghost);
    }), e2.clear();
  }, kN = (e2) => {
    const t2 = ua(), n2 = ta.DOM, o2 = document, r2 = /* @__PURE__ */ ((e3, t3) => (n3) => {
      if (((e4) => 0 === e4.button)(n3)) {
        const s3 = Q(t3.dom.getParents(n3.target), fN).getOr(null);
        if (C(s3) && (o3 = t3.getBody(), mN(r3 = s3) && r3 !== o3)) {
          const o4 = t3.dom.getPos(s3), r4 = t3.getBody(), a3 = t3.getDoc().documentElement;
          e3.set({ element: s3, dragging: false, screenX: n3.screenX, screenY: n3.screenY, maxX: (t3.inline ? r4.scrollWidth : a3.offsetWidth) - 2, maxY: (t3.inline ? r4.scrollHeight : a3.offsetHeight) - 2, relX: n3.pageX - o4.x, relY: n3.pageY - o4.y, width: s3.offsetWidth, height: s3.offsetHeight, ghost: gN(t3, s3, s3.offsetWidth, s3.offsetHeight), intervalId: ca(100) });
        }
      }
      var o3, r3;
    })(t2, e2), s2 = wN(t2, e2), a2 = /* @__PURE__ */ ((e3, t3) => (n3) => {
      e3.on((e4) => {
        if (e4.intervalId.clear(), e4.dragging) {
          if (((e5, t4, n4) => !y(t4) && t4 !== n4 && !e5.dom.isChildOf(t4, n4) && !mN(t4))(t3, ((e5) => {
            const t4 = e5.getSel();
            if (C(t4)) {
              const e6 = t4.getRangeAt(0).startContainer;
              return Ro(e6) ? e6.parentNode : e6;
            }
            return null;
          })(t3.selection), e4.element)) {
            const o3 = ((e5) => {
              const t4 = e5.cloneNode(true);
              return t4.removeAttribute("data-mce-selected"), t4;
            })(e4.element);
            t3.dispatch("drop", { clientX: n3.clientX, clientY: n3.clientY }).isDefaultPrevented() || t3.undoManager.transact(() => {
              CN(e4.element), t3.insertContent(t3.dom.getOuterHTML(o3)), t3._selectionOverrides.hideFakeCaret();
            });
          }
          t3.dispatch("dragend");
        }
      }), xN(e3);
    })(t2, e2), i3 = /* @__PURE__ */ ((e3, t3) => () => {
      e3.on((e4) => {
        e4.intervalId.clear(), e4.dragging && t3.dispatch("dragend");
      }), xN(e3);
    })(t2, e2);
    e2.on("mousedown", r2), e2.on("mousemove", s2), e2.on("mouseup", a2), n2.bind(o2, "mousemove", s2), n2.bind(o2, "mouseup", i3), e2.on("remove", () => {
      n2.unbind(o2, "mousemove", s2), n2.unbind(o2, "mouseup", i3);
    }), e2.on("keydown", (e3) => {
      e3.keyCode === bm.ESC && i3();
    });
  }, SN = Io, _N = (e2, t2) => Zp(e2.getBody(), t2), EN = (e2) => {
    const t2 = e2.selection, n2 = e2.dom, o2 = e2.getBody(), r2 = Ud(e2, o2, n2.isBlock, () => If(e2)), s2 = "sel-" + n2.uniqueId(), a2 = "data-mce-selected";
    let i3;
    const l2 = (e3) => e3 !== o2 && (SN(e3) || zo(e3)) && n2.isChildOf(e3, o2), d2 = (n3, o3, s3, a3 = true) => e2.dispatch("ShowCaret", { target: o3, direction: n3, before: s3 }).isDefaultPrevented() ? null : (a3 && t2.scrollIntoView(o3, -1 === n3), r2.show(s3, o3)), c2 = (e3) => Br(e3) || Mr(e3) || Ir(e3), u2 = (e3) => c2(e3.startContainer) || c2(e3.endContainer), m2 = (t3) => {
      const o3 = e2.schema.getVoidElements(), r3 = n2.createRng(), s3 = t3.startContainer, a3 = t3.startOffset, i4 = t3.endContainer, l3 = t3.endOffset;
      return xe(o3, s3.nodeName.toLowerCase()) ? 0 === a3 ? r3.setStartBefore(s3) : r3.setStartAfter(s3) : r3.setStart(s3, a3), xe(o3, i4.nodeName.toLowerCase()) ? 0 === l3 ? r3.setEndBefore(i4) : r3.setEndAfter(i4) : r3.setEnd(i4, l3), r3;
    }, f2 = (r3, c3) => {
      if (!r3) return null;
      if (r3.collapsed) {
        if (!u2(r3)) {
          const e3 = c3 ? 1 : -1, t3 = mc(e3, o2, r3), n3 = t3.getNode(!c3);
          if (Hd(n3)) return d2(e3, n3, !!c3 && !t3.isAtEnd(), false);
          const s3 = t3.getNode(c3);
          if (Hd(s3)) return d2(e3, s3, !c3 && !t3.isAtEnd(), false);
        }
        return null;
      }
      let m3 = r3.startContainer, f3 = r3.startOffset;
      const g3 = r3.endOffset;
      if (Ro(m3) && 0 === f3 && SN(m3.parentNode) && (m3 = m3.parentNode, f3 = n2.nodeIndex(m3), m3 = m3.parentNode), !yo(m3)) return null;
      if (g3 === f3 + 1 && m3 === r3.endContainer) {
        const o3 = m3.childNodes[f3];
        if (l2(o3)) return ((o4) => {
          const r4 = o4.cloneNode(true), l3 = e2.dispatch("ObjectSelected", { target: o4, targetClone: r4 });
          if (l3.isDefaultPrevented()) return null;
          const d3 = ((o5, r5) => {
            const a3 = mn(e2.getBody()), i4 = e2.getDoc(), l4 = Yo(a3, "#" + s2).getOrThunk(() => {
              const e3 = dn('<div data-mce-bogus="all" class="mce-offscreen-selection"></div>', i4);
              return Vt(e3, "id", s2), Jn(a3, e3), e3;
            }), d4 = n2.createRng();
            eo(l4), Zn(l4, [un(pr, i4), mn(r5), un(pr, i4)]), d4.setStart(l4.dom.firstChild, 1), d4.setEnd(l4.dom.lastChild, 0), $n(l4, { top: n2.getPos(o5, e2.getBody()).y + "px" }), bf(l4);
            const c5 = t2.getSel();
            return c5 && (c5.removeAllRanges(), c5.addRange(d4)), d4;
          })(o4, l3.targetClone), c4 = mn(o4);
          return V(vr(mn(e2.getBody()), "*[data-mce-selected]"), (e3) => {
            bn(c4, e3) || Yt(e3, a2);
          }), n2.getAttrib(o4, a2) || o4.setAttribute(a2, "1"), i3 = o4, p2(), d3;
        })(o3);
      }
      return null;
    }, g2 = () => {
      i3 && i3.removeAttribute(a2), Yo(mn(e2.getBody()), "#" + s2).each(to), i3 = null;
    }, p2 = () => {
      r2.hide();
    };
    return By(e2) || (e2.on("click", (t3) => {
      const n3 = _N(e2, t3.target);
      n3 && SN(n3) && (t3.preventDefault(), e2.focus());
    }), e2.on("blur NewBlock", g2), e2.on("ResizeWindow FullscreenStateChanged", r2.reposition), e2.on("tap", (t3) => {
      const n3 = t3.target, o3 = _N(e2, n3);
      SN(o3) ? (t3.preventDefault(), tw(e2, o3).each(f2)) : l2(n3) && tw(e2, n3).each(f2);
    }, true), e2.on("mousedown", (r3) => {
      const s3 = r3.target;
      if (s3 !== o2 && "HTML" !== s3.nodeName && !n2.isChildOf(s3, o2)) return;
      if (!((e3, t3, n3) => {
        const o3 = mn(e3.getBody()), r4 = e3.inline ? o3 : mn(Cn(o3).dom.documentElement), s4 = ((e4, t4, n4, o4) => {
          const r5 = ((e5) => e5.dom.getBoundingClientRect())(t4);
          return { x: n4 - (e4 ? r5.left + t4.dom.clientLeft + rC(t4) : 0), y: o4 - (e4 ? r5.top + t4.dom.clientTop + oC(t4) : 0) };
        })(e3.inline, r4, t3, n3);
        return ((e4, t4, n4) => {
          const o4 = tC(e4), r5 = nC(e4);
          return t4 >= 0 && n4 >= 0 && t4 <= o4 && n4 <= r5;
        })(r4, s4.x, s4.y);
      })(e2, r3.clientX, r3.clientY)) return;
      g2(), p2();
      const a3 = _N(e2, s3);
      SN(a3) ? (r3.preventDefault(), tw(e2, a3).each(f2)) : cN(o2, r3.clientX, r3.clientY).each((n3) => {
        var o3;
        r3.preventDefault(), (o3 = d2(1, n3.node, n3.position === rN.Before, false)) && t2.setRng(o3), yo(a3) ? a3.focus() : e2.getBody().focus();
      });
    }), e2.on("keypress", (e3) => {
      bm.modifierPressed(e3) || SN(t2.getNode()) && e3.preventDefault();
    }), e2.on("GetSelectionRange", (e3) => {
      let t3 = e3.range;
      if (i3) {
        if (!i3.parentNode) return void (i3 = null);
        t3 = t3.cloneRange(), t3.selectNode(i3), e3.range = t3;
      }
    }), e2.on("SetSelectionRange", (e3) => {
      e3.range = m2(e3.range);
      const t3 = f2(e3.range, e3.forward);
      t3 && (e3.range = t3);
    }), e2.on("AfterSetSelectionRange", (e3) => {
      const t3 = e3.range, o3 = t3.startContainer.parentElement;
      var r3;
      u2(t3) || yo(r3 = o3) && "mcepastebin" === r3.id || p2(), ((e4) => C(e4) && n2.hasClass(e4, "mce-offscreen-selection"))(o3) || g2();
    }), ((e3) => {
      kN(e3), Ql(e3) && ((e4) => {
        const t3 = (t4) => {
          if (!t4.isDefaultPrevented()) {
            const n4 = t4.dataTransfer;
            n4 && (j(n4.types, "Files") || n4.files.length > 0) && (t4.preventDefault(), "drop" === t4.type && cC(e4, "Dropped file type is not supported"));
          }
        }, n3 = (n4) => {
          Bf(e4, n4.target) && t3(n4);
        }, o3 = () => {
          const o4 = ta.DOM, r3 = e4.dom, s3 = document, a3 = e4.inline ? e4.getBody() : e4.getDoc(), i4 = ["drop", "dragover"];
          V(i4, (e5) => {
            o4.bind(s3, e5, n3), r3.bind(a3, e5, t3);
          }), e4.on("remove", () => {
            V(i4, (e5) => {
              o4.unbind(s3, e5, n3), r3.unbind(a3, e5, t3);
            });
          });
        };
        e4.on("init", () => {
          Af.setEditorTimeout(e4, o3, 0);
        });
      })(e3);
    })(e2), ((e3) => {
      const t3 = ma(() => {
        if (!e3.removed && e3.getBody().contains(document.activeElement)) {
          const t4 = e3.selection.getRng();
          if (t4.collapsed) {
            const n3 = nw(e3, t4, false);
            e3.selection.setRng(n3);
          }
        }
      }, 0);
      e3.on("focus", () => {
        t3.throttle();
      }), e3.on("blur", () => {
        t3.cancel();
      });
    })(e2), ((e3) => {
      e3.on("init", () => {
        e3.on("focusin", (t3) => {
          const n3 = t3.target;
          if (zo(n3)) {
            const t4 = Zp(e3.getBody(), n3), o3 = Io(t4) ? t4 : n3;
            e3.selection.getNode() !== o3 && tw(e3, o3).each((t5) => e3.selection.setRng(t5));
          }
        });
      });
    })(e2)), { showCaret: d2, showBlockCaretContainer: (e3) => {
      e3.hasAttribute("data-mce-caret") && (Fr(e3), t2.scrollIntoView(e3));
    }, hideFakeCaret: p2, destroy: () => {
      r2.destroy(), i3 = null;
    } };
  }, NN = (e2, t2) => {
    let n2 = t2;
    for (let t3 = e2.previousSibling; Ro(t3); t3 = t3.previousSibling) n2 += t3.data.length;
    return n2;
  }, RN = (e2, t2, n2, o2, r2) => {
    if (Ro(n2) && (o2 < 0 || o2 > n2.data.length)) return [];
    const s2 = r2 && Ro(n2) ? [NN(n2, o2)] : [o2];
    let a2 = n2;
    for (; a2 !== t2 && a2.parentNode; ) s2.push(e2.nodeIndex(a2, r2)), a2 = a2.parentNode;
    return a2 === t2 ? s2.reverse() : [];
  }, AN = (e2, t2, n2, o2, r2, s2, a2 = false) => ({ start: RN(e2, t2, n2, o2, a2), end: RN(e2, t2, r2, s2, a2) }), ON = (e2, t2) => {
    const n2 = t2.slice(), o2 = n2.pop();
    return x(o2) ? Y(n2, (e3, t3) => e3.bind((e4) => M.from(e4.childNodes[t3])), M.some(e2)).bind((e3) => Ro(e3) && (o2 < 0 || o2 > e3.data.length) ? M.none() : M.some({ node: e3, offset: o2 })) : M.none();
  }, TN = (e2, t2) => ON(e2, t2.start).bind(({ node: n2, offset: o2 }) => ON(e2, t2.end).map(({ node: e3, offset: t3 }) => {
    const r2 = document.createRange();
    return r2.setStart(n2, o2), r2.setEnd(e3, t3), r2;
  })), BN = (e2, t2, n2) => {
    if (t2 && e2.isEmpty(t2) && !n2(t2)) {
      const o2 = t2.parentNode;
      e2.remove(t2), BN(e2, o2, n2);
    }
  }, DN = (e2, t2, n2, o2 = true) => {
    const r2 = t2.startContainer.parentNode, s2 = t2.endContainer.parentNode;
    t2.deleteContents(), o2 && !n2(t2.startContainer) && (Ro(t2.startContainer) && 0 === t2.startContainer.data.length && e2.remove(t2.startContainer), Ro(t2.endContainer) && 0 === t2.endContainer.data.length && e2.remove(t2.endContainer), BN(e2, r2, n2), r2 !== s2 && BN(e2, s2, n2));
  }, PN = (e2, t2) => M.from(e2.dom.getParent(t2.startContainer, e2.dom.isBlock)), LN = (e2, t2, n2) => {
    const o2 = e2.dynamicPatternsLookup({ text: n2, block: t2 });
    return { ...e2, blockPatterns: Li(o2).concat(e2.blockPatterns), inlinePatterns: Mi(o2).concat(e2.inlinePatterns) };
  }, MN = (e2, t2, n2, o2) => {
    const r2 = e2.createRng();
    return r2.setStart(t2, 0), r2.setEnd(n2, o2), r2.toString();
  }, IN = (e2, t2, n2) => {
    ((e3, t3, n3) => {
      if (Ro(e3) && 0 >= e3.length) return M.some(Xk(e3, 0));
      {
        const t4 = Ta(Qk);
        return M.from(t4.forwards(e3, 0, Jk(e3), n3)).map((e4) => Xk(e4.container, 0));
      }
    })(t2, 0, t2).each((o2) => {
      const r2 = o2.container;
      tS(r2, n2.start.length, t2).each((n3) => {
        const o3 = e2.createRng();
        o3.setStart(r2, 0), o3.setEnd(n3.container, n3.offset), DN(e2, o3, (e3) => e3 === t2);
      });
    });
  }, FN = (e2, t2) => e2.create("span", { "data-mce-type": "bookmark", id: t2 }), UN = (e2, t2) => {
    const n2 = e2.createRng();
    return n2.setStartAfter(t2.start), n2.setEndBefore(t2.end), n2;
  }, zN = (e2, t2, n2) => {
    const o2 = TN(e2.getRoot(), n2).getOrDie("Unable to resolve path range"), r2 = o2.startContainer, s2 = o2.endContainer, a2 = 0 === o2.endOffset ? s2 : s2.splitText(o2.endOffset), i3 = 0 === o2.startOffset ? r2 : r2.splitText(o2.startOffset), l2 = i3.parentNode;
    return { prefix: t2, end: a2.parentNode.insertBefore(FN(e2, t2 + "-end"), a2), start: l2.insertBefore(FN(e2, t2 + "-start"), i3) };
  }, jN = (e2, t2, n2) => {
    BN(e2, e2.get(t2.prefix + "-end"), n2), BN(e2, e2.get(t2.prefix + "-start"), n2);
  }, HN = (e2) => 0 === e2.start.length, $N = (e2, t2, n2, o2) => {
    const r2 = t2.start;
    var s2;
    return nS(e2, o2.container, o2.offset, (s2 = r2, (e3, t3) => {
      const n3 = e3.data.substring(0, t3), o3 = n3.lastIndexOf(s2.charAt(s2.length - 1)), r3 = n3.lastIndexOf(s2);
      return -1 !== r3 ? r3 + s2.length : -1 !== o3 ? o3 + 1 : -1;
    }), n2).bind((o3) => {
      if (o3.offset >= r2.length) {
        const t3 = e2.createRng();
        return t3.setStart(o3.container, o3.offset - r2.length), t3.setEnd(o3.container, o3.offset), M.some(t3);
      }
      {
        const s3 = o3.offset - r2.length;
        return eS(o3.container, s3, n2).map((t3) => {
          const n3 = e2.createRng();
          return n3.setStart(t3.container, t3.offset), n3.setEnd(o3.container, o3.offset), n3;
        }).filter((e3) => e3.toString() === r2).orThunk(() => $N(e2, t2, n2, Xk(o3.container, 0)));
      }
    });
  }, VN = (e2, t2, n2, o2) => {
    const r2 = e2.dom, s2 = r2.getRoot(), a2 = n2.pattern, i3 = n2.position.container, l2 = n2.position.offset;
    return eS(i3, l2 - n2.pattern.end.length, t2).bind((d2) => {
      const c2 = AN(r2, s2, d2.container, d2.offset, i3, l2, o2);
      if (HN(a2)) return M.some({ matches: [{ pattern: a2, startRng: c2, endRng: c2 }], position: d2 });
      {
        const i4 = qN(e2, n2.remainingPatterns, d2.container, d2.offset, t2, o2), l3 = i4.getOr({ matches: [], position: d2 }), u2 = l3.position, m2 = ((e3, t3, n3, o3, r3, s3 = false) => {
          if (0 === t3.start.length && !s3) {
            const t4 = e3.createRng();
            return t4.setStart(n3, o3), t4.setEnd(n3, o3), M.some(t4);
          }
          return Zk(n3, o3, r3).bind((n4) => $N(e3, t3, r3, n4).bind((e4) => {
            var t4;
            if (s3) {
              if (e4.endContainer === n4.container && e4.endOffset === n4.offset) return M.none();
              if (0 === n4.offset && (null === (t4 = e4.endContainer.textContent) || void 0 === t4 ? void 0 : t4.length) === e4.endOffset) return M.none();
            }
            return M.some(e4);
          }));
        })(r2, a2, u2.container, u2.offset, t2, i4.isNone());
        return m2.map((e3) => {
          const t3 = ((e4, t4, n3, o3 = false) => AN(e4, t4, n3.startContainer, n3.startOffset, n3.endContainer, n3.endOffset, o3))(r2, s2, e3, o2);
          return { matches: l3.matches.concat([{ pattern: a2, startRng: t3, endRng: c2 }]), position: Xk(e3.startContainer, e3.startOffset) };
        });
      }
    });
  }, qN = (e2, t2, n2, o2, r2, s2) => {
    const a2 = e2.dom;
    return Zk(n2, o2, a2.getRoot()).bind((i3) => {
      const l2 = MN(a2, r2, n2, o2);
      for (let n3 = 0; n3 < t2.length; n3++) {
        const o3 = t2[n3];
        if (!je(l2, o3.end)) continue;
        const a3 = t2.slice();
        a3.splice(n3, 1);
        const d2 = VN(e2, r2, { pattern: o3, remainingPatterns: a3, position: i3 }, s2);
        if (d2.isSome()) return d2;
      }
      return M.none();
    });
  }, WN = (e2, t2, n2) => {
    e2.selection.setRng(n2), "inline-format" === t2.type ? V(t2.format, (t3) => {
      e2.formatter.apply(t3);
    }) : e2.execCommand(t2.cmd, false, t2.value);
  }, KN = (e2, t2, n2, o2, r2, s2) => qN(e2, r2.inlinePatterns, n2, o2, t2, s2).fold(() => [], (e3) => e3.matches), GN = (e2, t2) => {
    if (0 === t2.length) return;
    const n2 = e2.dom, o2 = e2.selection.getBookmark(), r2 = ((e3, t3) => {
      const n3 = Ea("mce_textpattern"), o3 = G(t3, (t4, o4) => {
        const r3 = zN(e3, n3 + `_end${t4.length}`, o4.endRng);
        return t4.concat([{ ...o4, endMarker: r3 }]);
      }, []);
      return G(o3, (t4, r3) => {
        const s2 = o3.length - t4.length - 1, a2 = HN(r3.pattern) ? r3.endMarker : zN(e3, n3 + `_start${s2}`, r3.startRng);
        return t4.concat([{ ...r3, startMarker: a2 }]);
      }, []);
    })(n2, t2);
    V(r2, (t3) => {
      const o3 = n2.getParent(t3.startMarker.start, n2.isBlock), r3 = (e3) => e3 === o3;
      HN(t3.pattern) ? ((e3, t4, n3, o4) => {
        const r4 = UN(e3.dom, n3);
        DN(e3.dom, r4, o4), WN(e3, t4, r4);
      })(e2, t3.pattern, t3.endMarker, r3) : ((e3, t4, n3, o4, r4) => {
        const s2 = e3.dom, a2 = UN(s2, o4), i3 = UN(s2, n3);
        DN(s2, i3, r4), DN(s2, a2, r4);
        const l2 = { prefix: n3.prefix, start: n3.end, end: o4.start }, d2 = UN(s2, l2);
        WN(e3, t4, d2);
      })(e2, t3.pattern, t3.startMarker, t3.endMarker, r3), jN(n2, t3.endMarker, r3), jN(n2, t3.startMarker, r3);
    }), e2.selection.moveToBookmark(o2);
  }, YN = (e2, t2) => {
    const n2 = e2.selection.getRng();
    return PN(e2, n2).map((o2) => {
      var r2;
      const s2 = Math.max(0, n2.startOffset), a2 = LN(t2, o2, null !== (r2 = o2.textContent) && void 0 !== r2 ? r2 : ""), i3 = KN(e2, o2, n2.startContainer, s2, a2, true), l2 = ((e3, t3, n3, o3) => {
        var r3;
        const s3 = e3.dom, a3 = Ji(e3);
        if (!s3.is(t3, a3)) return [];
        const i4 = null !== (r3 = t3.textContent) && void 0 !== r3 ? r3 : "";
        return ((e4, t4) => {
          const n4 = ((e5) => se(e5, (e6, t5) => t5.start.length - e6.start.length))(e4), o4 = t4.replace(pr, " ");
          return Q(n4, (e5) => 0 === t4.indexOf(e5.start) || 0 === o4.indexOf(e5.start));
        })(n3.blockPatterns, i4).map((e4) => Tt.trim(i4).length === e4.start.length ? [] : [{ pattern: e4, range: AN(s3, s3.getRoot(), t3, 0, t3, 0, true) }]).getOr([]);
      })(e2, o2, a2);
      return (l2.length > 0 || i3.length > 0) && (e2.undoManager.add(), e2.undoManager.extra(() => {
        e2.execCommand("mceInsertNewLine");
      }, () => {
        e2.insertContent(gr), GN(e2, i3), ((e3, t4) => {
          if (0 === t4.length) return;
          const n4 = e3.selection.getBookmark();
          V(t4, (t5) => ((e4, t6) => {
            const n5 = e4.dom, o3 = t6.pattern, r3 = TN(n5.getRoot(), t6.range).getOrDie("Unable to resolve path range");
            return PN(e4, r3).each((t7) => {
              "block-format" === o3.type ? ((e5, t8) => {
                const n6 = t8.get(e5);
                return p(n6) && ie(n6).exists((e6) => xe(e6, "block"));
              })(o3.format, e4.formatter) && e4.undoManager.transact(() => {
                IN(e4.dom, t7, o3), e4.formatter.apply(o3.format);
              }) : "block-command" === o3.type && e4.undoManager.transact(() => {
                IN(e4.dom, t7, o3), e4.execCommand(o3.cmd, false, o3.value);
              });
            }), true;
          })(e3, t5)), e3.selection.moveToBookmark(n4);
        })(e2, l2);
        const t3 = e2.selection.getRng(), n3 = Zk(t3.startContainer, t3.startOffset, e2.dom.getRoot());
        e2.execCommand("mceInsertNewLine"), n3.each((t4) => {
          const n4 = t4.container;
          n4.data.charAt(t4.offset - 1) === gr && (n4.deleteData(t4.offset - 1, 1), BN(e2.dom, n4.parentNode, (t5) => t5 === e2.dom.getRoot()));
        });
      }), true);
    }).getOr(false);
  }, XN = (e2, t2, n2) => {
    for (let o2 = 0; o2 < e2.length; o2++) if (n2(e2[o2], t2)) return true;
    return false;
  }, QN = (e2) => {
    const t2 = Tt.each, n2 = bm.BACKSPACE, o2 = bm.DELETE, r2 = e2.dom, s2 = e2.selection, a2 = e2.parser, i3 = Nt.browser, l2 = i3.isFirefox(), d2 = i3.isChromium() || i3.isSafari(), c2 = Nt.deviceType.isiPhone() || Nt.deviceType.isiPad(), u2 = Nt.os.isMacOS() || Nt.os.isiOS(), m2 = (t3, n3) => {
      try {
        e2.getDoc().execCommand(t3, false, String(n3));
      } catch (e3) {
      }
    }, f2 = (e3) => e3.isDefaultPrevented(), g2 = () => {
      e2.shortcuts.add("meta+a", null, "SelectAll");
    }, p2 = () => {
      e2.inline || r2.bind(e2.getDoc(), "mousedown mouseup", (t3) => {
        let n3;
        if (t3.target === e2.getDoc().documentElement) if (n3 = s2.getRng(), e2.getBody().focus(), "mousedown" === t3.type) {
          if (Br(n3.startContainer)) return;
          s2.placeCaretAt(t3.clientX, t3.clientY);
        } else s2.setRng(n3);
      });
    }, h2 = () => {
      Range.prototype.getClientRects || e2.on("mousedown", (t3) => {
        if (!f2(t3) && "HTML" === t3.target.nodeName) {
          const t4 = e2.getBody();
          t4.blur(), Af.setEditorTimeout(e2, () => {
            t4.focus();
          });
        }
      });
    }, b2 = () => {
      const t3 = ed(e2);
      e2.on("click", (n3) => {
        const o3 = n3.target;
        /^(IMG|HR)$/.test(o3.nodeName) && "false" !== r2.getContentEditableParent(o3) && (n3.preventDefault(), e2.selection.select(o3), e2.nodeChanged()), "A" === o3.nodeName && r2.hasClass(o3, t3) && 0 === o3.childNodes.length && (n3.preventDefault(), s2.select(o3));
      });
    }, v2 = () => {
      e2.on("keydown", (e3) => {
        if (!f2(e3) && e3.keyCode === n2 && s2.isCollapsed() && 0 === s2.getRng().startOffset) {
          const t3 = s2.getNode().previousSibling;
          if (t3 && t3.nodeName && "table" === t3.nodeName.toLowerCase()) return e3.preventDefault(), false;
        }
        return true;
      });
    }, y2 = () => {
      Kl(e2) || e2.on("BeforeExecCommand mousedown", () => {
        m2("StyleWithCSS", false), m2("enableInlineTableEditing", false), _l(e2) || m2("enableObjectResizing", false);
      });
    }, C2 = () => {
      e2.contentStyles.push("img:-moz-broken {-moz-force-broken-image-icon:1;min-width:24px;min-height:24px}");
    }, w2 = () => {
      e2.inline || e2.on("keydown", () => {
        document.activeElement === document.body && e2.getWin().focus();
      });
    }, x2 = () => {
      e2.inline || (e2.contentStyles.push("body {min-height: 150px}"), e2.on("click", (t3) => {
        let n3;
        "HTML" === t3.target.nodeName && (n3 = e2.selection.getRng(), e2.getBody().focus(), e2.selection.setRng(n3), e2.selection.normalize(), e2.nodeChanged());
      }));
    }, k2 = () => {
      u2 && e2.on("keydown", (t3) => {
        !bm.metaKeyPressed(t3) || t3.shiftKey || 37 !== t3.keyCode && 39 !== t3.keyCode || (t3.preventDefault(), e2.selection.getSel().modify("move", 37 === t3.keyCode ? "backward" : "forward", "lineboundary"));
      });
    }, _2 = () => {
      e2.on("click", (e3) => {
        let t3 = e3.target;
        do {
          if ("A" === t3.tagName) return void e3.preventDefault();
        } while (t3 = t3.parentNode);
      }), e2.contentStyles.push(".mce-content-body {-webkit-touch-callout: none}");
    }, E2 = () => {
      e2.on("init", () => {
        e2.dom.bind(e2.getBody(), "submit", (e3) => {
          e3.preventDefault();
        });
      });
    }, N2 = S;
    return By(e2) ? (d2 && (p2(), b2(), E2(), g2(), c2 && (w2(), x2(), _2())), l2 && (h2(), y2(), C2(), k2())) : (e2.on("keydown", (t3) => {
      if (f2(t3) || t3.keyCode !== bm.BACKSPACE) return;
      let n3 = s2.getRng();
      const o3 = n3.startContainer, a3 = n3.startOffset, i4 = r2.getRoot();
      let l3 = o3;
      if (n3.collapsed && 0 === a3) {
        for (; l3.parentNode && l3.parentNode.firstChild === l3 && l3.parentNode !== i4; ) l3 = l3.parentNode;
        "BLOCKQUOTE" === l3.nodeName && (e2.formatter.toggle("blockquote", void 0, l3), n3 = r2.createRng(), n3.setStart(o3, 0), n3.setEnd(o3, 0), s2.setRng(n3));
      }
    }), (() => {
      const t3 = (e3) => {
        const t4 = r2.create("body"), n3 = e3.cloneContents();
        return t4.appendChild(n3), s2.serializer.serialize(t4, { format: "html" });
      };
      e2.on("keydown", (s3) => {
        const a3 = s3.keyCode;
        if (!f2(s3) && (a3 === o2 || a3 === n2)) {
          const n3 = e2.selection.isCollapsed(), o3 = e2.getBody();
          if (n3 && !r2.isEmpty(o3)) return;
          if (!n3 && !((n4) => {
            const o4 = t3(n4), s4 = r2.createRng();
            return s4.selectNode(e2.getBody()), o4 === t3(s4);
          })(e2.selection.getRng())) return;
          s3.preventDefault(), e2.setContent(""), o3.firstChild && r2.isBlock(o3.firstChild) ? e2.selection.setCursorLocation(o3.firstChild, 0) : e2.selection.setCursorLocation(o3, 0), e2.nodeChanged();
        }
      });
    })(), Nt.windowsPhone || e2.on("keyup focusin mouseup", (t3) => {
      bm.modifierPressed(t3) || ((e3) => {
        const t4 = e3.getBody(), n3 = e3.selection.getRng();
        return n3.startContainer === n3.endContainer && n3.startContainer === t4 && 0 === n3.startOffset && n3.endOffset === t4.childNodes.length;
      })(e2) || s2.normalize();
    }, true), d2 && (p2(), b2(), e2.on("init", () => {
      m2("DefaultParagraphSeparator", Ji(e2));
    }), E2(), v2(), a2.addNodeFilter("br", (e3) => {
      let t3 = e3.length;
      for (; t3--; ) "Apple-interchange-newline" === e3[t3].attr("class") && e3[t3].remove();
    }), c2 ? (w2(), x2(), _2()) : g2()), l2 && (e2.on("keydown", (t3) => {
      if (!f2(t3) && t3.keyCode === n2) {
        if (!e2.getBody().getElementsByTagName("hr").length) return;
        if (s2.isCollapsed() && 0 === s2.getRng().startOffset) {
          const e3 = s2.getNode(), n3 = e3.previousSibling;
          if ("HR" === e3.nodeName) return r2.remove(e3), void t3.preventDefault();
          n3 && n3.nodeName && "hr" === n3.nodeName.toLowerCase() && (r2.remove(n3), t3.preventDefault());
        }
      }
    }), h2(), (() => {
      const n3 = () => {
        const n4 = r2.getAttribs(s2.getStart().cloneNode(false));
        return () => {
          const o4 = s2.getStart();
          o4 !== e2.getBody() && (r2.setAttrib(o4, "style", null), t2(n4, (e3) => {
            o4.setAttributeNode(e3.cloneNode(true));
          }));
        };
      }, o3 = () => !s2.isCollapsed() && r2.getParent(s2.getStart(), r2.isBlock) !== r2.getParent(s2.getEnd(), r2.isBlock);
      e2.on("keypress", (t3) => {
        let r3;
        return !(!(f2(t3) || 8 !== t3.keyCode && 46 !== t3.keyCode) && o3() && (r3 = n3(), e2.getDoc().execCommand("delete", false), r3(), t3.preventDefault(), 1));
      }), r2.bind(e2.getDoc(), "cut", (t3) => {
        if (!f2(t3) && o3()) {
          const t4 = n3();
          Af.setEditorTimeout(e2, () => {
            t4();
          });
        }
      });
    })(), y2(), e2.on("SetContent ExecCommand", (e3) => {
      "setcontent" !== e3.type && "mceInsertLink" !== e3.command || t2(r2.select("a"), (e4) => {
        var t3;
        let n3 = e4.parentNode;
        const o3 = r2.getRoot();
        if ((null == n3 ? void 0 : n3.lastChild) === e4) {
          for (; n3 && !r2.isBlock(n3); ) {
            if ((null === (t3 = n3.parentNode) || void 0 === t3 ? void 0 : t3.lastChild) !== n3 || n3 === o3) return;
            n3 = n3.parentNode;
          }
          r2.add(n3, "br", { "data-mce-bogus": 1 });
        }
      });
    }), C2(), k2(), v2())), { refreshContentEditable: N2, isHidden: () => {
      if (!l2 || e2.removed) return false;
      const t3 = e2.selection.getSel();
      return !t3 || !t3.rangeCount || 0 === t3.rangeCount;
    } };
  }, JN = ta.DOM, ZN = (e2) => e2.inline ? e2.getElement().nodeName.toLowerCase() : void 0, eR = (e2) => ve(e2, (e3) => false === v(e3)), tR = (e2) => {
    const t2 = e2.options.get, n2 = e2.editorUpload.blobCache;
    return eR({ allow_conditional_comments: t2("allow_conditional_comments"), allow_html_data_urls: t2("allow_html_data_urls"), allow_svg_data_urls: t2("allow_svg_data_urls"), allow_html_in_named_anchor: t2("allow_html_in_named_anchor"), allow_script_urls: t2("allow_script_urls"), allow_unsafe_link_target: t2("allow_unsafe_link_target"), convert_fonts_to_spans: t2("convert_fonts_to_spans"), fix_list_elements: t2("fix_list_elements"), font_size_legacy_values: t2("font_size_legacy_values"), forced_root_block: t2("forced_root_block"), forced_root_block_attrs: t2("forced_root_block_attrs"), preserve_cdata: t2("preserve_cdata"), remove_trailing_brs: t2("remove_trailing_brs"), inline_styles: t2("inline_styles"), root_name: ZN(e2), validate: true, blob_cache: n2, document: e2.getDoc() });
  }, nR = (e2) => {
    const t2 = e2.options.get;
    return eR({ custom_elements: t2("custom_elements"), extended_valid_elements: t2("extended_valid_elements"), invalid_elements: t2("invalid_elements"), invalid_styles: t2("invalid_styles"), schema: t2("schema"), valid_children: t2("valid_children"), valid_classes: t2("valid_classes"), valid_elements: t2("valid_elements"), valid_styles: t2("valid_styles"), verify_html: t2("verify_html"), padd_empty_block_inline_children: t2("format_empty_lines") });
  }, oR = (e2) => e2.inline ? e2.ui.styleSheetLoader : e2.dom.styleSheetLoader, rR = (e2) => {
    const t2 = oR(e2), n2 = xl(e2), o2 = e2.contentCSS, r2 = () => {
      t2.unloadAll(o2), e2.inline || e2.ui.styleSheetLoader.unloadAll(n2);
    }, s2 = () => {
      e2.removed ? r2() : e2.on("remove", r2);
    };
    if (e2.contentStyles.length > 0) {
      let t3 = "";
      Tt.each(e2.contentStyles, (e3) => {
        t3 += e3 + "\r\n";
      }), e2.dom.addStyle(t3);
    }
    const a2 = Promise.all(((e3, t3, n3) => {
      const o3 = [oR(e3).loadAll(t3)];
      return e3.inline ? o3 : o3.concat([e3.ui.styleSheetLoader.loadAll(n3)]);
    })(e2, o2, n2)).then(s2).catch(s2), i3 = wl(e2);
    return i3 && ((e3, t3) => {
      const n3 = mn(e3.getBody()), o3 = Mn(Ln(n3)), r3 = cn("style");
      Vt(r3, "type", "text/css"), Jn(r3, un(t3)), Jn(o3, r3), e3.on("remove", () => {
        to(r3);
      });
    })(e2, i3), a2;
  }, sR = (e2) => {
    true !== e2.removed && (((e3) => {
      By(e3) || e3.load({ initial: true, format: "html" }), e3.startContent = e3.getContent({ format: "raw" });
    })(e2), ((e3) => {
      e3.bindPendingEventDelegates(), e3.initialized = true, ((e4) => {
        e4.dispatch("Init");
      })(e3), e3.focus(true), ((e4) => {
        const t3 = e4.dom.getRoot();
        e4.inline || vu(e4) && e4.selection.getStart(true) !== t3 || Ic(t3).each((t4) => {
          const n2 = t4.getNode(), o2 = _o(n2) ? Ic(n2).getOr(t4) : t4;
          e4.selection.setRng(o2.toRange());
        });
      })(e3), e3.nodeChanged({ initial: true });
      const t2 = od(e3);
      w(t2) && t2.call(e3, e3), ((e4) => {
        const t3 = sd(e4);
        t3 && Af.setEditorTimeout(e4, () => {
          let n2;
          n2 = true === t3 ? e4 : e4.editorManager.get(t3), n2 && !n2.destroyed && (n2.focus(), n2.selection.scrollIntoView());
        }, 100);
      })(e3);
    })(e2));
  }, aR = (e2) => {
    const t2 = e2.getElement();
    let n2 = e2.getDoc();
    e2.inline && (JN.addClass(t2, "mce-content-body"), e2.contentDocument = n2 = document, e2.contentWindow = window, e2.bodyElement = t2, e2.contentAreaContainer = t2);
    const o2 = e2.getBody();
    o2.disabled = true, e2.readonly = Kl(e2), e2.readonly || (e2.inline && "static" === JN.getStyle(o2, "position", true) && (o2.style.position = "relative"), o2.contentEditable = "true"), o2.disabled = false, e2.editorUpload = xC(e2), e2.schema = Ls(nR(e2)), e2.dom = ta(n2, { keep_values: true, url_converter: e2.convertURL, url_converter_scope: e2, update_styles: true, root_element: e2.inline ? e2.getBody() : null, collect: e2.inline, schema: e2.schema, contentCssCors: gl(e2), referrerPolicy: pl(e2), onSetAttrib: (t3) => {
      e2.dispatch("SetAttrib", t3);
    } }), e2.parser = ((e3) => {
      const t3 = Zv(tR(e3), e3.schema);
      return t3.addAttributeFilter("src,href,style,tabindex", (t4, n3) => {
        const o3 = e3.dom, r3 = "data-mce-" + n3;
        let s3 = t4.length;
        for (; s3--; ) {
          const a2 = t4[s3];
          let i3 = a2.attr(n3);
          if (i3 && !a2.attr(r3)) {
            if (0 === i3.indexOf("data:") || 0 === i3.indexOf("blob:")) continue;
            "style" === n3 ? (i3 = o3.serializeStyle(o3.parseStyle(i3), a2.name), i3.length || (i3 = null), a2.attr(r3, i3), a2.attr(n3, i3)) : "tabindex" === n3 ? (a2.attr(r3, i3), a2.attr(n3, null)) : a2.attr(r3, e3.convertURL(i3, n3, a2.name));
          }
        }
      }), t3.addNodeFilter("script", (e4) => {
        let t4 = e4.length;
        for (; t4--; ) {
          const n3 = e4[t4], o3 = n3.attr("type") || "no/type";
          0 !== o3.indexOf("mce-") && n3.attr("type", "mce-" + o3);
        }
      }), Sd(e3) && t3.addNodeFilter("#cdata", (t4) => {
        var n3;
        let o3 = t4.length;
        for (; o3--; ) {
          const r3 = t4[o3];
          r3.type = 8, r3.name = "#comment", r3.value = "[CDATA[" + e3.dom.encode(null !== (n3 = r3.value) && void 0 !== n3 ? n3 : "") + "]]";
        }
      }), t3.addNodeFilter("p,h1,h2,h3,h4,h5,h6,div", (t4) => {
        let n3 = t4.length;
        const o3 = e3.schema.getNonEmptyElements();
        for (; n3--; ) {
          const e4 = t4[n3];
          e4.isEmpty(o3) && 0 === e4.getAll("br").length && e4.append(new Zf("br", 1));
        }
      }), t3;
    })(e2), e2.serializer = $y(((e3) => {
      const t3 = e3.options.get;
      return { ...tR(e3), ...nR(e3), ...eR({ url_converter: t3("url_converter"), url_converter_scope: t3("url_converter_scope"), element_format: t3("element_format"), entities: t3("entities"), entity_encoding: t3("entity_encoding"), indent: t3("indent"), indent_after: t3("indent_after"), indent_before: t3("indent_before") }) };
    })(e2), e2), e2.selection = zy(e2.dom, e2.getWin(), e2.serializer, e2), e2.annotator = lm(e2), e2.formatter = DC(e2), e2.undoManager = LC(e2), e2._nodeChangeDispatcher = new pE(e2), e2._selectionOverrides = EN(e2), ((e3) => {
      const t3 = ua(), n3 = ra(false), o3 = fa((t4) => {
        e3.dispatch("longpress", { ...t4, type: "longpress" }), n3.set(true);
      }, 400);
      e3.on("touchstart", (e4) => {
        Jx(e4).each((r3) => {
          o3.cancel();
          const s3 = { x: r3.clientX, y: r3.clientY, target: e4.target };
          o3.throttle(e4), n3.set(false), t3.set(s3);
        });
      }, true), e3.on("touchmove", (r3) => {
        o3.cancel(), Jx(r3).each((o4) => {
          t3.on((r4) => {
            ((e4, t4) => {
              const n4 = Math.abs(e4.clientX - t4.x), o5 = Math.abs(e4.clientY - t4.y);
              return n4 > 5 || o5 > 5;
            })(o4, r4) && (t3.clear(), n3.set(false), e3.dispatch("longpresscancel"));
          });
        });
      }, true), e3.on("touchend touchcancel", (r3) => {
        o3.cancel(), "touchcancel" !== r3.type && t3.get().filter((e4) => e4.target.isEqualNode(r3.target)).each(() => {
          n3.get() ? r3.preventDefault() : e3.dispatch("tap", { ...r3, type: "tap" });
        });
      }, true);
    })(e2), ((e3) => {
      ((e4) => {
        e4.on("click", (t3) => {
          e4.dom.getParent(t3.target, "details") && t3.preventDefault();
        });
      })(e3), ((e4) => {
        e4.parser.addNodeFilter("details", (e5) => {
          V(e5, (e6) => {
            e6.attr("data-mce-open", e6.attr("open")), e6.attr("open", "open");
          });
        }), e4.serializer.addNodeFilter("details", (e5) => {
          V(e5, (e6) => {
            const t3 = e6.attr("data-mce-open");
            e6.attr("open", m(t3) ? t3 : null), e6.attr("data-mce-open", null);
          });
        });
      })(e3);
    })(e2), ((e3) => {
      const t3 = "contenteditable", n3 = " " + Tt.trim(xd(e3)) + " ", o3 = " " + Tt.trim(wd(e3)) + " ", r3 = sk(n3), s3 = sk(o3), a2 = kd(e3);
      a2.length > 0 && e3.on("BeforeSetContent", (t4) => {
        ((e4, t5, n4) => {
          let o4 = t5.length, r4 = n4.content;
          if ("raw" !== n4.format) {
            for (; o4--; ) r4 = r4.replace(t5[o4], ak(e4, r4, wd(e4)));
            n4.content = r4;
          }
        })(e3, a2, t4);
      }), e3.parser.addAttributeFilter("class", (e4) => {
        let n4 = e4.length;
        for (; n4--; ) {
          const o4 = e4[n4];
          r3(o4) ? o4.attr(t3, "true") : s3(o4) && o4.attr(t3, "false");
        }
      }), e3.serializer.addAttributeFilter(t3, (e4) => {
        let n4 = e4.length;
        for (; n4--; ) {
          const o4 = e4[n4];
          (r3(o4) || s3(o4)) && (a2.length > 0 && o4.attr("data-mce-content") ? (o4.name = "#text", o4.type = 3, o4.raw = true, o4.value = o4.attr("data-mce-content")) : o4.attr(t3, null));
        }
      });
    })(e2), By(e2) || (((e3) => {
      e3.on("mousedown", (t3) => {
        t3.detail >= 3 && (t3.preventDefault(), oN(e3));
      });
    })(e2), ((e3) => {
      ((e4) => {
        const t3 = [",", ".", ";", ":", "!", "?"], n3 = [32], o3 = () => {
          return t4 = yd(e4), n4 = Cd(e4), { inlinePatterns: Mi(t4), blockPatterns: Li(t4), dynamicPatternsLookup: n4 };
          var t4, n4;
        }, r3 = () => ((e5) => e5.options.isSet("text_patterns_lookup"))(e4);
        e4.on("keydown", (t4) => {
          if (13 === t4.keyCode && !bm.modifierPressed(t4) && e4.selection.isCollapsed()) {
            const n4 = o3();
            (n4.inlinePatterns.length > 0 || n4.blockPatterns.length > 0 || r3()) && YN(e4, n4) && t4.preventDefault();
          }
        }, true);
        const s3 = () => {
          if (e4.selection.isCollapsed()) {
            const t4 = o3();
            (t4.inlinePatterns.length > 0 || r3()) && ((e5, t5) => {
              const n4 = e5.selection.getRng();
              PN(e5, n4).map((o4) => {
                const r4 = Math.max(0, n4.startOffset - 1), s4 = MN(e5.dom, o4, n4.startContainer, r4), a2 = LN(t5, o4, s4), i3 = KN(e5, o4, n4.startContainer, r4, a2, false);
                i3.length > 0 && e5.undoManager.transact(() => {
                  GN(e5, i3);
                });
              });
            })(e4, t4);
          }
        };
        e4.on("keyup", (e5) => {
          XN(n3, e5, (e6, t4) => e6 === t4.keyCode && !bm.modifierPressed(t4)) && s3();
        }), e4.on("keypress", (n4) => {
          XN(t3, n4, (e5, t4) => e5.charCodeAt(0) === t4.charCode) && Af.setEditorTimeout(e4, s3);
        });
      })(e3);
    })(e2));
    const r2 = gE(e2);
    Qx(e2, r2), ((e3) => {
      e3.on("NodeChange", O(ok, e3));
    })(e2), ((e3) => {
      var t3;
      const n3 = e3.dom, o3 = Ji(e3), r3 = null !== (t3 = Nl(e3)) && void 0 !== t3 ? t3 : "", s3 = (t4, a2) => {
        if (((e4) => {
          if (FC(e4)) {
            const t5 = e4.keyCode;
            return !UC(e4) && (bm.metaKeyPressed(e4) || e4.altKey || t5 >= 112 && t5 <= 123 || j(MC, t5));
          }
          return false;
        })(t4)) return;
        const i3 = e3.getBody(), l2 = !((e4) => FC(e4) && !(UC(e4) || "keyup" === e4.type && 229 === e4.keyCode))(t4) && ((e4, t5, n4) => {
          if (ss(mn(t5), false)) {
            const o4 = t5.firstElementChild;
            return !o4 || !e4.getStyle(t5.firstElementChild, "padding-left") && !e4.getStyle(t5.firstElementChild, "padding-right") && n4 === o4.nodeName.toLowerCase();
          }
          return false;
        })(n3, i3, o3);
        ("" !== n3.getAttrib(i3, IC) !== l2 || a2) && (n3.setAttrib(i3, IC, l2 ? r3 : null), n3.setAttrib(i3, "aria-placeholder", l2 ? r3 : null), ((e4, t5) => {
          e4.dispatch("PlaceholderToggle", { state: t5 });
        })(e3, l2), e3.on(l2 ? "keydown" : "keyup", s3), e3.off(l2 ? "keyup" : "keydown", s3));
      };
      We(r3) && e3.on("init", (t4) => {
        s3(t4, true), e3.on("change SetContent ExecCommand", s3), e3.on("paste", (t5) => Af.setEditorTimeout(e3, () => s3(t5)));
      });
    })(e2), XE(e2);
    const s2 = ((e3) => {
      const t3 = e3;
      return ((e4) => we(e4.plugins, "rtc").bind((e5) => M.from(e5.setup)))(e3).fold(() => (t3.rtcInstance = Ty(e3), M.none()), (e4) => (t3.rtcInstance = (() => {
        const e5 = N(null), t4 = N("");
        return { init: { bindEvents: S }, undoManager: { beforeChange: S, add: e5, undo: e5, redo: e5, clear: S, reset: S, hasUndo: P, hasRedo: P, transact: e5, ignore: S, extra: S }, formatter: { match: P, matchAll: N([]), matchNode: N(void 0), canApply: P, closest: t4, apply: S, remove: S, toggle: S, formatChanged: N({ unbind: S }) }, editor: { getContent: t4, setContent: N({ content: "", html: "" }), insertContent: N(""), addVisual: S }, selection: { getContent: t4 }, autocompleter: { addDecoration: S, removeDecoration: S }, raw: { getModel: N(M.none()) } };
      })(), M.some(() => e4().then((e5) => (t3.rtcInstance = ((e6) => {
        const t4 = (e7) => f(e7) ? e7 : {}, { init: n3, undoManager: o3, formatter: r3, editor: s3, selection: a2, autocompleter: i3, raw: l2 } = e6;
        return { init: { bindEvents: n3.bindEvents }, undoManager: { beforeChange: o3.beforeChange, add: o3.add, undo: o3.undo, redo: o3.redo, clear: o3.clear, reset: o3.reset, hasUndo: o3.hasUndo, hasRedo: o3.hasRedo, transact: (e7, t5, n4) => o3.transact(n4), ignore: (e7, t5) => o3.ignore(t5), extra: (e7, t5, n4, r4) => o3.extra(n4, r4) }, formatter: { match: (e7, n4, o4, s4) => r3.match(e7, t4(n4), s4), matchAll: r3.matchAll, matchNode: r3.matchNode, canApply: (e7) => r3.canApply(e7), closest: (e7) => r3.closest(e7), apply: (e7, n4, o4) => r3.apply(e7, t4(n4)), remove: (e7, n4, o4, s4) => r3.remove(e7, t4(n4)), toggle: (e7, n4, o4) => r3.toggle(e7, t4(n4)), formatChanged: (e7, t5, n4, o4, s4) => r3.formatChanged(t5, n4, o4, s4) }, editor: { getContent: (e7) => s3.getContent(e7), setContent: (e7, t5) => ({ content: s3.setContent(e7, t5), html: "" }), insertContent: (e7, t5) => (s3.insertContent(e7), ""), addVisual: s3.addVisual }, selection: { getContent: (e7, t5) => a2.getContent(t5) }, autocompleter: { addDecoration: i3.addDecoration, removeDecoration: i3.removeDecoration }, raw: { getModel: () => M.some(l2.getRawModel()) } };
      })(e5), e5.rtc.isRemote)))));
    })(e2);
    ((e3) => {
      const t3 = e3.getDoc(), n3 = e3.getBody();
      ((e4) => {
        e4.dispatch("PreInit");
      })(e3), ad(e3) || (t3.body.spellcheck = false, JN.setAttrib(n3, "spellcheck", "false")), e3.quirks = QN(e3), ((e4) => {
        e4.dispatch("PostRender");
      })(e3);
      const o3 = kl(e3);
      void 0 !== o3 && (n3.dir = o3);
      const r3 = id(e3);
      r3 && e3.on("BeforeSetContent", (e4) => {
        Tt.each(r3, (t4) => {
          e4.content = e4.content.replace(t4, (e5) => "<!--mce:protected " + escape(e5) + "-->");
        });
      }), e3.on("SetContent", () => {
        e3.addVisual(e3.getBody());
      }), e3.on("compositionstart compositionend", (t4) => {
        e3.composing = "compositionstart" === t4.type;
      });
    })(e2), s2.fold(() => {
      rR(e2).then(() => sR(e2));
    }, (t3) => {
      e2.setProgressState(true), rR(e2).then(() => {
        t3().then((t4) => {
          e2.setProgressState(false), sR(e2), Ly(e2);
        }, (t4) => {
          e2.notificationManager.open({ type: "error", text: String(t4) }), sR(e2), Ly(e2);
        });
      });
    });
  }, iR = (e2, t2) => {
    if (e2.inline || (e2.getElement().style.visibility = e2.orgVisibility), t2 || e2.inline) aR(e2);
    else {
      const t3 = e2.iframeElement, o2 = (n2 = mn(t3), so(n2, "load", pC, () => {
        o2.unbind(), e2.contentDocument = t3.contentDocument, aR(e2);
      }));
      if (Nt.browser.isFirefox()) {
        const t4 = e2.getDoc();
        t4.open(), t4.write(e2.iframeHTML), t4.close();
      } else t3.srcdoc = e2.iframeHTML;
    }
    var n2;
  }, lR = ta.DOM, dR = ta.DOM, cR = (e2, t2) => ({ editorContainer: e2, iframeContainer: t2, api: {} }), uR = (e2) => {
    const t2 = e2.getElement();
    return e2.inline ? cR(null) : ((e3) => {
      const t3 = dR.create("div");
      return dR.insertAfter(t3, e3), cR(t3, t3);
    })(t2);
  }, mR = (e2) => {
    e2.dispatch("ScriptsLoaded"), ((e3) => {
      const t3 = Tt.trim(ll(e3)), n2 = e3.ui.registry.getAll().icons, o2 = { ...Qy.get("default").icons, ...Qy.get(t3).icons };
      fe(o2, (t4, o3) => {
        xe(n2, o3) || e3.ui.registry.addIcon(o3, t4);
      });
    })(e2), ((e3) => {
      const t3 = Ol(e3);
      if (m(t3)) {
        const n2 = iC.get(t3);
        e3.theme = n2(e3, iC.urls[t3]) || {}, w(e3.theme.init) && e3.theme.init(e3, iC.urls[t3] || e3.documentBaseUrl.replace(/\/$/, ""));
      } else e3.theme = {};
    })(e2), ((e3) => {
      const t3 = Bl(e3), n2 = Jy.get(t3);
      e3.model = n2(e3, Jy.urls[t3]);
    })(e2), ((e3) => {
      const t3 = [];
      V(Yl(e3), (n2) => {
        ((e4, t4, n3) => {
          const o2 = aC.get(n3), r2 = aC.urls[n3] || e4.documentBaseUrl.replace(/\/$/, "");
          if (n3 = Tt.trim(n3), o2 && -1 === Tt.inArray(t4, n3)) {
            if (e4.plugins[n3]) return;
            try {
              const s2 = o2(e4, r2) || {};
              e4.plugins[n3] = s2, w(s2.init) && (s2.init(e4, r2), t4.push(n3));
            } catch (t5) {
              ((e5, t6, n4) => {
                const o3 = la.translate(["Failed to initialize plugin: {0}", t6]);
                um(e5, "PluginLoadError", { message: o3 }), fC(o3, n4), cC(e5, o3);
              })(e4, n3, t5);
            }
          }
        })(e3, t3, ((e4) => e4.replace(/^\-/, ""))(n2));
      });
    })(e2);
    const t2 = ((e3) => {
      const t3 = e3.getElement();
      return e3.orgDisplay = t3.style.display, m(Ol(e3)) ? ((e4) => {
        const t4 = e4.theme.renderUI;
        return t4 ? t4() : uR(e4);
      })(e3) : w(Ol(e3)) ? ((e4) => {
        const t4 = e4.getElement(), n2 = Ol(e4)(e4, t4);
        return n2.editorContainer.nodeType && (n2.editorContainer.id = n2.editorContainer.id || e4.id + "_parent"), n2.iframeContainer && n2.iframeContainer.nodeType && (n2.iframeContainer.id = n2.iframeContainer.id || e4.id + "_iframecontainer"), n2.height = n2.iframeHeight ? n2.iframeHeight : t4.offsetHeight, n2;
      })(e3) : uR(e3);
    })(e2);
    ((e3, t3) => {
      const n2 = { show: M.from(t3.show).getOr(S), hide: M.from(t3.hide).getOr(S), isEnabled: M.from(t3.isEnabled).getOr(L), setEnabled: (n3) => {
        e3.mode.isReadOnly() || M.from(t3.setEnabled).each((e4) => e4(n3));
      } };
      e3.ui = { ...e3.ui, ...n2 };
    })(e2, M.from(t2.api).getOr({})), e2.editorContainer = t2.editorContainer, ((e3) => {
      e3.contentCSS = e3.contentCSS.concat(((e4) => gC(e4, Cl(e4)))(e3), ((e4) => gC(e4, xl(e4)))(e3));
    })(e2), e2.inline ? iR(e2) : ((e3, t3) => {
      ((e4, t4) => {
        const n2 = e4.translate("Rich Text Area"), o2 = Kt(mn(e4.getElement()), "tabindex").bind(Ge), r2 = ((e5, t5, n3, o3) => {
          const r3 = cn("iframe");
          return o3.each((e6) => Vt(r3, "tabindex", e6)), qt(r3, n3), qt(r3, { id: e5 + "_ifr", frameBorder: "0", allowTransparency: "true", title: t5 }), nn(r3, "tox-edit-area__iframe"), r3;
        })(e4.id, n2, qi(e4), o2).dom;
        r2.onload = () => {
          r2.onload = null, e4.dispatch("load");
        }, e4.contentAreaContainer = t4.iframeContainer, e4.iframeElement = r2, e4.iframeHTML = ((e5) => {
          let t5 = Wi(e5) + "<html><head>";
          Ki(e5) !== e5.documentBaseUrl && (t5 += '<base href="' + e5.documentBaseURI.getURI() + '" />'), t5 += '<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />';
          const n3 = Gi(e5), o3 = Yi(e5), r3 = e5.translate(td(e5));
          return Xi(e5) && (t5 += '<meta http-equiv="Content-Security-Policy" content="' + Xi(e5) + '" />'), t5 += `</head><body id="${n3}" class="mce-content-body ${o3}" data-id="${e5.id}" aria-label="${r3}"><br></body></html>`, t5;
        })(e4), lR.add(t4.iframeContainer, r2);
      })(e3, t3), t3.editorContainer && (t3.editorContainer.style.display = e3.orgDisplay, e3.hidden = lR.isHidden(t3.editorContainer)), e3.getElement().style.display = "none", lR.setAttrib(e3.id, "aria-hidden", "true"), iR(e3);
    })(e2, { editorContainer: t2.editorContainer, iframeContainer: t2.iframeContainer });
  }, fR = ta.DOM, gR = (e2) => "-" === e2.charAt(0), pR = (e2, t2, n2) => M.from(t2).filter((e3) => We(e3) && !Qy.has(e3)).map((t3) => ({ url: `${e2.editorManager.baseURL}/icons/${t3}/icons${n2}.js`, name: M.some(t3) })), hR = (e2, t2) => {
    const n2 = oa.ScriptLoader, o2 = () => {
      !e2.removed && ((e3) => {
        const t3 = Ol(e3);
        return !m(t3) || C(iC.get(t3));
      })(e2) && ((e3) => {
        const t3 = Bl(e3);
        return C(Jy.get(t3));
      })(e2) && mR(e2);
    };
    ((e3, t3) => {
      const n3 = Ol(e3);
      if (m(n3) && !gR(n3) && !xe(iC.urls, n3)) {
        const o3 = Tl(e3), r2 = o3 ? e3.documentBaseURI.toAbsolute(o3) : `themes/${n3}/theme${t3}.js`;
        iC.load(n3, r2).catch(() => {
          ((e4, t4, n4) => {
            uC(e4, "ThemeLoadError", mC("theme", t4, n4));
          })(e3, r2, n3);
        });
      }
    })(e2, t2), ((e3, t3) => {
      const n3 = Bl(e3);
      if ("plugin" !== n3 && !xe(Jy.urls, n3)) {
        const o3 = Dl(e3), r2 = m(o3) ? e3.documentBaseURI.toAbsolute(o3) : `models/${n3}/model${t3}.js`;
        Jy.load(n3, r2).catch(() => {
          ((e4, t4, n4) => {
            uC(e4, "ModelLoadError", mC("model", t4, n4));
          })(e3, r2, n3);
        });
      }
    })(e2, t2), ((e3, t3) => {
      const n3 = hl(t3), o3 = bl(t3);
      if (!la.hasCode(n3) && "en" !== n3) {
        const r2 = We(o3) ? o3 : `${t3.editorManager.baseURL}/langs/${n3}.js`;
        e3.add(r2).catch(() => {
          ((e4, t4, n4) => {
            uC(e4, "LanguageLoadError", mC("language", t4, n4));
          })(t3, r2, n3);
        });
      }
    })(n2, e2), ((e3, t3, n3) => {
      const o3 = pR(t3, "default", n3), r2 = ((e4) => M.from(dl(e4)).filter(We).map((e5) => ({ url: e5, name: M.none() })))(t3).orThunk(() => pR(t3, ll(t3), ""));
      V(((e4) => {
        const t4 = [], n4 = (e5) => {
          t4.push(e5);
        };
        for (let t5 = 0; t5 < e4.length; t5++) e4[t5].each(n4);
        return t4;
      })([o3, r2]), (n4) => {
        e3.add(n4.url).catch(() => {
          ((e4, t4, n5) => {
            uC(e4, "IconsLoadError", mC("icons", t4, n5));
          })(t3, n4.url, n4.name.getOrUndefined());
        });
      });
    })(n2, e2, t2), ((e3, t3) => {
      const n3 = (t4, n4) => {
        aC.load(t4, n4).catch(() => {
          ((e4, t5, n5) => {
            uC(e4, "PluginLoadError", mC("plugin", t5, n5));
          })(e3, n4, t4);
        });
      };
      fe(Xl(e3), (t4, o3) => {
        n3(o3, t4), e3.options.set("plugins", Yl(e3).concat(o3));
      }), V(Yl(e3), (e4) => {
        !(e4 = Tt.trim(e4)) || aC.urls[e4] || gR(e4) || n3(e4, `plugins/${e4}/plugin${t3}.js`);
      });
    })(e2, t2), n2.loadQueue().then(o2, o2);
  }, bR = Ct().deviceType, vR = bR.isPhone(), yR = bR.isTablet(), CR = (e2) => {
    if (y(e2)) return [];
    {
      const t2 = p(e2) ? e2 : e2.split(/[ ,]/), n2 = $2(t2, $e);
      return K(n2, We);
    }
  }, wR = (e2, t2) => {
    const n2 = ((t3, n3) => {
      const o3 = {}, r3 = {};
      return be(t3, (t4, n4) => j(e2, n4), he(o3), he(r3)), { t: o3, f: r3 };
    })(t2);
    return o2 = n2.t, r2 = n2.f, { sections: N(o2), options: N(r2) };
    var o2, r2;
  }, xR = (e2, t2) => xe(e2.sections(), t2), kR = (e2, t2) => ({ table_grid: false, object_resizing: false, resize: false, toolbar_mode: we(e2, "toolbar_mode").getOr("scrolling"), toolbar_sticky: false, ...t2 ? { menubar: false } : {} }), SR = (e2, t2) => {
    var n2;
    const o2 = null !== (n2 = t2.external_plugins) && void 0 !== n2 ? n2 : {};
    return e2 && e2.external_plugins ? Tt.extend({}, e2.external_plugins, o2) : o2;
  }, _R = (e2, t2, n2, o2, r2) => {
    var s2;
    const a2 = e2 ? { mobile: kR(null !== (s2 = r2.mobile) && void 0 !== s2 ? s2 : {}, t2) } : {}, i3 = wR(["mobile"], yS(a2, r2)), l2 = Tt.extend(n2, o2, i3.options(), ((e3, t3) => e3 && xR(t3, "mobile"))(e2, i3) ? ((e3, t3, n3 = {}) => {
      const o3 = e3.sections(), r3 = we(o3, t3).getOr({});
      return Tt.extend({}, n3, r3);
    })(i3, "mobile") : {}, { external_plugins: SR(o2, i3.options()) });
    return ((e3, t3, n3, o3) => {
      const r3 = CR(n3.forced_plugins), s3 = CR(o3.plugins), a3 = ((e4, t4) => xR(e4, t4) ? e4.sections()[t4] : {})(t3, "mobile"), i4 = ((e4, t4, n4, o4) => e4 && xR(t4, "mobile") ? o4 : n4)(e3, t3, s3, a3.plugins ? CR(a3.plugins) : s3), l3 = ((e4, t4) => [...CR(e4), ...CR(t4)])(r3, i4);
      return Tt.extend(o3, { forced_plugins: r3, plugins: l3 });
    })(e2, i3, o2, l2);
  }, ER = (e2) => {
    ((e3) => {
      const t2 = (t3) => () => {
        V("left,center,right,justify".split(","), (n2) => {
          t3 !== n2 && e3.formatter.remove("align" + n2);
        }), "none" !== t3 && ((t4, n2) => {
          e3.formatter.toggle(t4, void 0), e3.nodeChanged();
        })("align" + t3);
      };
      e3.editorCommands.addCommands({ JustifyLeft: t2("left"), JustifyCenter: t2("center"), JustifyRight: t2("right"), JustifyFull: t2("justify"), JustifyNone: t2("none") });
    })(e2), ((e3) => {
      const t2 = (t3) => () => {
        const n2 = e3.selection, o2 = n2.isCollapsed() ? [e3.dom.getParent(n2.getNode(), e3.dom.isBlock)] : n2.getSelectedBlocks();
        return H(o2, (n3) => C(e3.formatter.matchNode(n3, t3)));
      };
      e3.editorCommands.addCommands({ JustifyLeft: t2("alignleft"), JustifyCenter: t2("aligncenter"), JustifyRight: t2("alignright"), JustifyFull: t2("alignjustify") }, "state");
    })(e2);
  }, NR = (e2, t2) => {
    const n2 = e2.selection, o2 = e2.dom;
    return /^ | $/.test(t2) ? ((e3, t3, n3) => {
      const o3 = mn(e3.getRoot());
      return n3 = Yg(o3, ai.fromRangeStart(t3)) ? n3.replace(/^ /, "&nbsp;") : n3.replace(/^&nbsp;/, " "), Xg(o3, ai.fromRangeEnd(t3)) ? n3.replace(/(&nbsp;| )(<br( \/)>)?$/, "&nbsp;") : n3.replace(/&nbsp;(<br( \/)?>)?$/, " ");
    })(o2, n2.getRng(), t2) : t2;
  }, RR = (e2, t2) => {
    const { content: n2, details: o2 } = ((e3) => {
      if ("string" != typeof e3) {
        const t3 = Tt.extend({ paste: e3.paste, data: { paste: e3.paste } }, e3);
        return { content: e3.content, details: t3 };
      }
      return { content: e3, details: {} };
    })(t2);
    oy(e2, { ...o2, content: NR(e2, n2), format: "html", set: false, selection: true }).each((t3) => {
      const n3 = ((e3, t4, n4) => Dy(e3).editor.insertContent(t4, n4))(e2, t3.content, o2);
      ry(e2, n3, t3), e2.addVisual();
    });
  }, AR = { "font-size": "size", "font-family": "face" }, OR = Ht("font"), TR = (e2) => (t2, n2) => M.from(n2).map(mn).filter(Ft).bind((n3) => ((e3, t3, n4) => Sh(mn(n4), (t4) => ((t5) => Wn(t5, e3).orThunk(() => OR(t5) ? we(AR, e3).bind((e4) => Kt(t5, e4)) : M.none()))(t4), (e4) => bn(mn(t3), e4)))(e2, t2, n3.dom).or(((e3, t3) => M.from(ta.DOM.getStyle(t3, e3, true)))(e2, n3.dom))).getOr(""), BR = TR("font-size"), DR = _((e2) => e2.replace(/[\'\"\\]/g, "").replace(/,\s+/g, ","), TR("font-family")), PR = (e2) => Ic(e2.getBody()).bind((e3) => {
    const t2 = e3.container();
    return M.from(Ro(t2) ? t2.parentNode : t2);
  }), LR = (e2, t2) => ((e3, t3) => ((e4) => M.from(e4.selection.getRng()).bind((t4) => {
    const n2 = e4.getBody();
    return t4.startContainer === n2 && 0 === t4.startOffset ? M.none() : M.from(e4.selection.getStart(true));
  }))(e3).orThunk(O(PR, e3)).map(mn).filter(Ft).bind(t3))(e2, E(M.some, t2)), MR = (e2, t2) => {
    if (/^[0-9.]+$/.test(t2)) {
      const n2 = parseInt(t2, 10);
      if (n2 >= 1 && n2 <= 7) {
        const o2 = ((e3) => Tt.explode(e3.options.get("font_size_style_values")))(e2), r2 = ((e3) => Tt.explode(e3.options.get("font_size_classes")))(e2);
        return r2.length > 0 ? r2[n2 - 1] || t2 : o2[n2 - 1] || t2;
      }
      return t2;
    }
    return t2;
  }, IR = (e2) => {
    const t2 = e2.split(/\s*,\s*/);
    return $2(t2, (e3) => -1 === e3.indexOf(" ") || ze(e3, '"') || ze(e3, "'") ? e3 : `'${e3}'`).join(",");
  }, FR = (e2) => {
    ER(e2), ((e3) => {
      e3.editorCommands.addCommands({ "Cut,Copy,Paste": (t2) => {
        const n2 = e3.getDoc();
        let o2;
        try {
          n2.execCommand(t2);
        } catch (e4) {
          o2 = true;
        }
        if ("paste" !== t2 || n2.queryCommandEnabled(t2) || (o2 = true), o2 || !n2.queryCommandSupported(t2)) {
          let t3 = e3.translate("Your browser doesn't support direct access to the clipboard. Please use the Ctrl+X/C/V keyboard shortcuts instead.");
          (Nt.os.isMacOS() || Nt.os.isiOS()) && (t3 = t3.replace(/Ctrl\+/g, "\u2318+")), e3.notificationManager.open({ text: t3, type: "error" });
        }
      } });
    })(e2), ((e3) => {
      e3.editorCommands.addCommands({ mceAddUndoLevel: () => {
        e3.undoManager.add();
      }, mceEndUndoLevel: () => {
        e3.undoManager.add();
      }, Undo: () => {
        e3.undoManager.undo();
      }, Redo: () => {
        e3.undoManager.redo();
      } });
    })(e2), ((e3) => {
      e3.editorCommands.addCommands({ mceSelectNodeDepth: (t2, n2, o2) => {
        let r2 = 0;
        e3.dom.getParent(e3.selection.getNode(), (t3) => !yo(t3) || r2++ !== o2 || (e3.selection.select(t3), false), e3.getBody());
      }, mceSelectNode: (t2, n2, o2) => {
        e3.selection.select(o2);
      }, selectAll: () => {
        const t2 = e3.dom.getParent(e3.selection.getStart(), Mo);
        if (t2) {
          const n2 = e3.dom.createRng();
          n2.selectNodeContents(t2), e3.selection.setRng(n2);
        }
      } });
    })(e2), ((e3) => {
      e3.editorCommands.addCommands({ mceCleanup: () => {
        const t2 = e3.selection.getBookmark();
        e3.setContent(e3.getContent()), e3.selection.moveToBookmark(t2);
      }, insertImage: (t2, n2, o2) => {
        RR(e3, e3.dom.createHTML("img", { src: o2 }));
      }, insertHorizontalRule: () => {
        e3.execCommand("mceInsertContent", false, "<hr>");
      }, insertText: (t2, n2, o2) => {
        RR(e3, e3.dom.encode(o2));
      }, insertHTML: (t2, n2, o2) => {
        RR(e3, o2);
      }, mceInsertContent: (t2, n2, o2) => {
        RR(e3, o2);
      }, mceSetContent: (t2, n2, o2) => {
        e3.setContent(o2);
      }, mceReplaceContent: (t2, n2, o2) => {
        e3.execCommand("mceInsertContent", false, o2.replace(/\{\$selection\}/g, e3.selection.getContent({ format: "text" })));
      }, mceNewDocument: () => {
        e3.setContent("");
      } });
    })(e2), ((e3) => {
      const t2 = (t3, n2, o2) => {
        const r2 = m(o2) ? { href: o2 } : o2, s2 = e3.dom.getParent(e3.selection.getNode(), "a");
        f(r2) && m(r2.href) && (r2.href = r2.href.replace(/ /g, "%20"), s2 && r2.href || e3.formatter.remove("link"), r2.href && e3.formatter.apply("link", r2, s2));
      };
      e3.editorCommands.addCommands({ unlink: () => {
        if (e3.selection.isCollapsed()) {
          const t3 = e3.dom.getParent(e3.selection.getStart(), "a");
          t3 && e3.dom.remove(t3, true);
        } else e3.formatter.remove("link");
      }, mceInsertLink: t2, createLink: t2 });
    })(e2), ((e3) => {
      e3.editorCommands.addCommands({ Indent: () => {
        ((e4) => {
          Kx(e4, "indent");
        })(e3);
      }, Outdent: () => {
        Gx(e3);
      } }), e3.editorCommands.addCommands({ Outdent: () => Vx(e3) }, "state");
    })(e2), ((e3) => {
      e3.editorCommands.addCommands({ insertParagraph: () => {
        nE(L_, e3);
      }, mceInsertNewLine: (t2, n2, o2) => {
        oE(e3, o2);
      }, InsertLineBreak: (t2, n2, o2) => {
        nE($_, e3);
      } });
    })(e2), ((e3) => {
      ((e4) => {
        e4.editorCommands.addCommands({ "InsertUnorderedList,InsertOrderedList": (t2) => {
          e4.getDoc().execCommand(t2);
          const n2 = e4.dom.getParent(e4.selection.getNode(), "ol,ul");
          if (n2) {
            const t3 = n2.parentNode;
            if (t3 && /^(H[1-6]|P|ADDRESS|PRE)$/.test(t3.nodeName)) {
              const o2 = e4.selection.getBookmark();
              e4.dom.split(t3, n2), e4.selection.moveToBookmark(o2);
            }
          }
        } });
      })(e3), ((e4) => {
        e4.editorCommands.addCommands({ "InsertUnorderedList,InsertOrderedList": (t2) => {
          const n2 = e4.dom.getParent(e4.selection.getNode(), "ul,ol");
          return n2 && ("insertunorderedlist" === t2 && "UL" === n2.tagName || "insertorderedlist" === t2 && "OL" === n2.tagName);
        } }, "state");
      })(e3);
    })(e2), ((e3) => {
      ((e4) => {
        const t2 = (t3, n2) => {
          e4.formatter.toggle(t3, n2), e4.nodeChanged();
        };
        e4.editorCommands.addCommands({ "Bold,Italic,Underline,Strikethrough,Superscript,Subscript": (e5) => {
          t2(e5);
        }, "ForeColor,HiliteColor": (e5, n2, o2) => {
          t2(e5, { value: o2 });
        }, BackColor: (e5, n2, o2) => {
          t2("hilitecolor", { value: o2 });
        }, FontName: (t3, n2, o2) => {
          ((e5, t4) => {
            const n3 = MR(e5, t4);
            e5.formatter.toggle("fontname", { value: IR(n3) }), e5.nodeChanged();
          })(e4, o2);
        }, FontSize: (t3, n2, o2) => {
          ((e5, t4) => {
            e5.formatter.toggle("fontsize", { value: MR(e5, t4) }), e5.nodeChanged();
          })(e4, o2);
        }, LineHeight: (t3, n2, o2) => {
          ((e5, t4) => {
            e5.formatter.toggle("lineheight", { value: String(t4) }), e5.nodeChanged();
          })(e4, o2);
        }, Lang: (e5, n2, o2) => {
          var r2;
          t2(e5, { value: o2.code, customValue: null !== (r2 = o2.customCode) && void 0 !== r2 ? r2 : null });
        }, RemoveFormat: (t3) => {
          e4.formatter.remove(t3);
        }, mceBlockQuote: () => {
          t2("blockquote");
        }, FormatBlock: (e5, n2, o2) => {
          t2(m(o2) ? o2 : "p");
        }, mceToggleFormat: (e5, n2, o2) => {
          t2(o2);
        } });
      })(e3), ((e4) => {
        const t2 = (t3) => e4.formatter.match(t3);
        e4.editorCommands.addCommands({ "Bold,Italic,Underline,Strikethrough,Superscript,Subscript": (e5) => t2(e5), mceBlockQuote: () => t2("blockquote") }, "state"), e4.editorCommands.addQueryValueHandler("FontName", () => ((e5) => LR(e5, (t3) => DR(e5.getBody(), t3.dom)).getOr(""))(e4)), e4.editorCommands.addQueryValueHandler("FontSize", () => ((e5) => LR(e5, (t3) => BR(e5.getBody(), t3.dom)).getOr(""))(e4)), e4.editorCommands.addQueryValueHandler("LineHeight", () => ((e5) => LR(e5, (t3) => {
          const n2 = mn(e5.getBody()), o2 = Sh(t3, (e6) => Wn(e6, "line-height"), O(bn, n2));
          return o2.getOrThunk(() => {
            const e6 = parseFloat(Vn(t3, "line-height")), n3 = parseFloat(Vn(t3, "font-size"));
            return String(e6 / n3);
          });
        }).getOr(""))(e4));
      })(e3);
    })(e2), ((e3) => {
      e3.editorCommands.addCommands({ mceRemoveNode: (t2, n2, o2) => {
        const r2 = null != o2 ? o2 : e3.selection.getNode();
        if (r2 !== e3.getBody()) {
          const t3 = e3.selection.getBookmark();
          e3.dom.remove(r2, true), e3.selection.moveToBookmark(t3);
        }
      }, mcePrint: () => {
        e3.getWin().print();
      }, mceFocus: (t2, n2, o2) => {
        ((e4, t3) => {
          e4.removed || (t3 ? Ff(e4) : ((e5) => {
            const t4 = e5.selection, n3 = e5.getBody();
            let o3 = t4.getRng();
            e5.quirks.refreshContentEditable(), C(e5.bookmark) && !If(e5) && Nf(e5).each((t5) => {
              e5.selection.setRng(t5), o3 = t5;
            });
            const r2 = ((e6, t5) => e6.dom.getParent(t5, (t6) => "true" === e6.dom.getContentEditable(t6)))(e5, t4.getNode());
            if (r2 && e5.dom.isChildOf(r2, n3)) return Mf(r2), Lf(e5, o3), void Ff(e5);
            e5.inline || (Nt.browser.isOpera() || Mf(n3), e5.getWin().focus()), (Nt.browser.isFirefox() || e5.inline) && (Mf(n3), Lf(e5, o3)), Ff(e5);
          })(e4));
        })(e3, true === o2);
      }, mceToggleVisualAid: () => {
        e3.hasVisual = !e3.hasVisual, e3.addVisual();
      } });
    })(e2);
  };
  class UR {
    constructor(e2) {
      this.commands = { state: {}, exec: {}, value: {} }, this.editor = e2;
    }
    execCommand(e2, t2 = false, n2, o2) {
      const r2 = this.editor, s2 = e2.toLowerCase(), a2 = null == o2 ? void 0 : o2.skip_focus;
      if (r2.removed) return false;
      if ("mcefocus" !== s2 && (/^(mceAddUndoLevel|mceEndUndoLevel)$/i.test(s2) || a2 ? ((e3) => {
        Nf(e3).each((t3) => e3.selection.setRng(t3));
      })(r2) : r2.focus()), r2.dispatch("BeforeExecCommand", { command: e2, ui: t2, value: n2 }).isDefaultPrevented()) return false;
      const i3 = this.commands.exec[s2];
      return !!w(i3) && (i3(s2, t2, n2), r2.dispatch("ExecCommand", { command: e2, ui: t2, value: n2 }), true);
    }
    queryCommandState(e2) {
      if (this.editor.quirks.isHidden() || this.editor.removed) return false;
      const t2 = e2.toLowerCase(), n2 = this.commands.state[t2];
      return !!w(n2) && n2(t2);
    }
    queryCommandValue(e2) {
      if (this.editor.quirks.isHidden() || this.editor.removed) return "";
      const t2 = e2.toLowerCase(), n2 = this.commands.value[t2];
      return w(n2) ? n2(t2) : "";
    }
    addCommands(e2, t2 = "exec") {
      const n2 = this.commands;
      fe(e2, (e3, o2) => {
        V(o2.toLowerCase().split(","), (o3) => {
          n2[t2][o3] = e3;
        });
      });
    }
    addCommand(e2, t2, n2) {
      const o2 = e2.toLowerCase();
      this.commands.exec[o2] = (e3, o3, r2) => t2.call(null != n2 ? n2 : this.editor, o3, r2);
    }
    queryCommandSupported(e2) {
      const t2 = e2.toLowerCase();
      return !!this.commands.exec[t2];
    }
    addQueryStateHandler(e2, t2, n2) {
      this.commands.state[e2.toLowerCase()] = () => t2.call(null != n2 ? n2 : this.editor);
    }
    addQueryValueHandler(e2, t2, n2) {
      this.commands.value[e2.toLowerCase()] = () => t2.call(null != n2 ? n2 : this.editor);
    }
  }
  const zR = "data-mce-contenteditable", jR = (e2, t2, n2) => {
    try {
      e2.getDoc().execCommand(t2, false, String(n2));
    } catch (e3) {
    }
  }, HR = (e2, t2) => {
    e2.dom.contentEditable = t2 ? "true" : "false";
  }, $R = (e2, t2) => {
    const n2 = mn(e2.getBody());
    ((e3, t3, n3) => {
      sn(e3, t3) && !n3 ? rn(e3, t3) : n3 && nn(e3, t3);
    })(n2, "mce-content-readonly", t2), t2 ? (e2.selection.controlSelection.hideResizeRect(), e2._selectionOverrides.hideFakeCaret(), ((e3) => {
      M.from(e3.selection.getNode()).each((e4) => {
        e4.removeAttribute("data-mce-selected");
      });
    })(e2), e2.readonly = true, HR(n2, false), V(vr(n2, '*[contenteditable="true"]'), (e3) => {
      Vt(e3, zR, "true"), HR(e3, false);
    })) : (e2.readonly = false, HR(n2, true), V(vr(n2, '*[data-mce-contenteditable="true"]'), (e3) => {
      Yt(e3, zR), HR(e3, true);
    }), jR(e2, "StyleWithCSS", false), jR(e2, "enableInlineTableEditing", false), jR(e2, "enableObjectResizing", false), ((e3) => If(e3) || ((e4) => {
      const t3 = Ln(mn(e4.getElement()));
      return yf(t3).filter((t4) => !((e5) => {
        const t5 = e5.classList;
        return void 0 !== t5 && (t5.contains("tox-edit-area") || t5.contains("tox-edit-area__iframe") || t5.contains("mce-content-body"));
      })(t4.dom) && Bf(e4, t4.dom)).isSome();
    })(e3))(e2) && e2.focus(), ((e3) => {
      e3.selection.setRng(e3.selection.getRng());
    })(e2), e2.nodeChanged());
  }, VR = (e2) => e2.readonly, qR = (e2) => {
    e2.parser.addAttributeFilter("contenteditable", (t2) => {
      VR(e2) && V(t2, (e3) => {
        e3.attr(zR, e3.attr("contenteditable")), e3.attr("contenteditable", "false");
      });
    }), e2.serializer.addAttributeFilter(zR, (t2) => {
      VR(e2) && V(t2, (e3) => {
        e3.attr("contenteditable", e3.attr(zR));
      });
    }), e2.serializer.addTempAttr(zR);
  }, WR = ["copy"], KR = Tt.makeMap("focus blur focusin focusout click dblclick mousedown mouseup mousemove mouseover beforepaste paste cut copy selectionchange mouseout mouseenter mouseleave wheel keydown keypress keyup input beforeinput contextmenu dragstart dragend dragover draggesture dragdrop drop drag submit compositionstart compositionend compositionupdate touchstart touchmove touchend touchcancel", " ");
  class GR {
    constructor(e2) {
      this.bindings = {}, this.settings = e2 || {}, this.scope = this.settings.scope || this, this.toggleEvent = this.settings.toggleEvent || P;
    }
    static isNative(e2) {
      return !!KR[e2.toLowerCase()];
    }
    fire(e2, t2) {
      return this.dispatch(e2, t2);
    }
    dispatch(e2, t2) {
      const n2 = e2.toLowerCase(), o2 = Us(n2, null != t2 ? t2 : {}, this.scope);
      this.settings.beforeFire && this.settings.beforeFire(o2);
      const r2 = this.bindings[n2];
      if (r2) for (let e3 = 0, t3 = r2.length; e3 < t3; e3++) {
        const t4 = r2[e3];
        if (!t4.removed) {
          if (t4.once && this.off(n2, t4.func), o2.isImmediatePropagationStopped()) return o2;
          if (false === t4.func.call(this.scope, o2)) return o2.preventDefault(), o2;
        }
      }
      return o2;
    }
    on(e2, t2, n2, o2) {
      if (false === t2 && (t2 = P), t2) {
        const r2 = { func: t2, removed: false };
        o2 && Tt.extend(r2, o2);
        const s2 = e2.toLowerCase().split(" ");
        let a2 = s2.length;
        for (; a2--; ) {
          const e3 = s2[a2];
          let t3 = this.bindings[e3];
          t3 || (t3 = [], this.toggleEvent(e3, true)), t3 = n2 ? [r2, ...t3] : [...t3, r2], this.bindings[e3] = t3;
        }
      }
      return this;
    }
    off(e2, t2) {
      if (e2) {
        const n2 = e2.toLowerCase().split(" ");
        let o2 = n2.length;
        for (; o2--; ) {
          const r2 = n2[o2];
          let s2 = this.bindings[r2];
          if (!r2) return fe(this.bindings, (e3, t3) => {
            this.toggleEvent(t3, false), delete this.bindings[t3];
          }), this;
          if (s2) {
            if (t2) {
              const e3 = W(s2, (e4) => e4.func === t2);
              s2 = e3.fail, this.bindings[r2] = s2, V(e3.pass, (e4) => {
                e4.removed = true;
              });
            } else s2.length = 0;
            s2.length || (this.toggleEvent(e2, false), delete this.bindings[r2]);
          }
        }
      } else fe(this.bindings, (e3, t3) => {
        this.toggleEvent(t3, false);
      }), this.bindings = {};
      return this;
    }
    once(e2, t2, n2) {
      return this.on(e2, t2, n2, { once: true });
    }
    has(e2) {
      e2 = e2.toLowerCase();
      const t2 = this.bindings[e2];
      return !(!t2 || 0 === t2.length);
    }
  }
  const YR = (e2) => (e2._eventDispatcher || (e2._eventDispatcher = new GR({ scope: e2, toggleEvent: (t2, n2) => {
    GR.isNative(t2) && e2.toggleNativeEvent && e2.toggleNativeEvent(t2, n2);
  } })), e2._eventDispatcher), XR = { fire(e2, t2, n2) {
    return this.dispatch(e2, t2, n2);
  }, dispatch(e2, t2, n2) {
    const o2 = this;
    if (o2.removed && "remove" !== e2 && "detach" !== e2) return Us(e2.toLowerCase(), null != t2 ? t2 : {}, o2);
    const r2 = YR(o2).dispatch(e2, t2);
    if (false !== n2 && o2.parent) {
      let t3 = o2.parent();
      for (; t3 && !r2.isPropagationStopped(); ) t3.dispatch(e2, r2, false), t3 = t3.parent ? t3.parent() : void 0;
    }
    return r2;
  }, on(e2, t2, n2) {
    return YR(this).on(e2, t2, n2);
  }, off(e2, t2) {
    return YR(this).off(e2, t2);
  }, once(e2, t2) {
    return YR(this).once(e2, t2);
  }, hasEventListeners(e2) {
    return YR(this).has(e2);
  } }, QR = ta.DOM;
  let JR;
  const ZR = (e2, t2) => {
    if ("selectionchange" === t2) return e2.getDoc();
    if (!e2.inline && /^mouse|touch|click|contextmenu|drop|dragover|dragend/.test(t2)) return e2.getDoc().documentElement;
    const n2 = Rl(e2);
    return n2 ? (e2.eventRoot || (e2.eventRoot = QR.select(n2)[0]), e2.eventRoot) : e2.getBody();
  }, eA = (e2, t2, n2) => {
    ((e3) => !e3.hidden && !VR(e3))(e2) ? e2.dispatch(t2, n2) : VR(e2) && ((e3, t3) => {
      if (((e4) => "click" === e4.type)(t3) && !bm.metaKeyPressed(t3)) {
        const n3 = mn(t3.target);
        ((e4, t4) => Xo(t4, "a", (t5) => bn(t5, mn(e4.getBody()))).bind((e5) => Kt(e5, "href")))(e3, n3).each((n4) => {
          if (t3.preventDefault(), /^#/.test(n4)) {
            const t4 = e3.dom.select(`${n4},[name="${o2 = n4, ze(o2, "#") ? ((e4, t5) => e4.substring(t5))(o2, "#".length) : o2}"]`);
            t4.length && e3.selection.scrollIntoView(t4[0], true);
          } else window.open(n4, "_blank", "rel=noopener noreferrer,menubar=yes,toolbar=yes,location=yes,status=yes,resizable=yes,scrollbars=yes");
          var o2;
        });
      } else ((e4) => j(WR, e4.type))(t3) && e3.dispatch(t3.type, t3);
    })(e2, n2);
  }, tA = (e2, t2) => {
    if (e2.delegates || (e2.delegates = {}), e2.delegates[t2] || e2.removed) return;
    const n2 = ZR(e2, t2);
    if (Rl(e2)) {
      if (JR || (JR = {}, e2.editorManager.on("removeEditor", () => {
        e2.editorManager.activeEditor || JR && (fe(JR, (t3, n3) => {
          e2.dom.unbind(ZR(e2, n3));
        }), JR = null);
      })), JR[t2]) return;
      const o2 = (n3) => {
        const o3 = n3.target, r2 = e2.editorManager.get();
        let s2 = r2.length;
        for (; s2--; ) {
          const e3 = r2[s2].getBody();
          (e3 === o3 || QR.isChildOf(o3, e3)) && eA(r2[s2], t2, n3);
        }
      };
      JR[t2] = o2, QR.bind(n2, t2, o2);
    } else {
      const o2 = (n3) => {
        eA(e2, t2, n3);
      };
      QR.bind(n2, t2, o2), e2.delegates[t2] = o2;
    }
  }, nA = { ...XR, bindPendingEventDelegates() {
    const e2 = this;
    Tt.each(e2._pendingNativeEvents, (t2) => {
      tA(e2, t2);
    });
  }, toggleNativeEvent(e2, t2) {
    const n2 = this;
    "focus" !== e2 && "blur" !== e2 && (n2.removed || (t2 ? n2.initialized ? tA(n2, e2) : n2._pendingNativeEvents ? n2._pendingNativeEvents.push(e2) : n2._pendingNativeEvents = [e2] : n2.initialized && n2.delegates && (n2.dom.unbind(ZR(n2, e2), e2, n2.delegates[e2]), delete n2.delegates[e2])));
  }, unbindAllNativeEvents() {
    const e2 = this, t2 = e2.getBody(), n2 = e2.dom;
    e2.delegates && (fe(e2.delegates, (t3, n3) => {
      e2.dom.unbind(ZR(e2, n3), n3, t3);
    }), delete e2.delegates), !e2.inline && t2 && n2 && (t2.onload = null, n2.unbind(e2.getWin()), n2.unbind(e2.getDoc())), n2 && (n2.unbind(t2), n2.unbind(e2.getContainer()));
  } }, oA = (e2) => m(e2) ? { value: e2.split(/[ ,]/), valid: true } : k(e2, m) ? { value: e2, valid: true } : { valid: false, message: "The value must be a string[] or a comma/space separated string." }, rA = (e2, t2) => e2 + (Ke(t2.message) ? "" : `. ${t2.message}`), sA = (e2) => e2.valid, aA = (e2, t2, n2 = "") => {
    const o2 = t2(e2);
    return b(o2) ? o2 ? { value: e2, valid: true } : { valid: false, message: n2 } : o2;
  }, iA = ["design", "readonly"], lA = (e2, t2, n2, o2) => {
    const r2 = n2[t2.get()], s2 = n2[o2];
    try {
      s2.activate();
    } catch (e3) {
      return void console.error(`problem while activating editor mode ${o2}:`, e3);
    }
    r2.deactivate(), r2.editorReadOnly !== s2.editorReadOnly && $R(e2, s2.editorReadOnly), t2.set(o2), ((e3, t3) => {
      e3.dispatch("SwitchMode", { mode: t3 });
    })(e2, o2);
  }, dA = Tt.each, cA = Tt.explode, uA = { f1: 112, f2: 113, f3: 114, f4: 115, f5: 116, f6: 117, f7: 118, f8: 119, f9: 120, f10: 121, f11: 122, f12: 123 }, mA = Tt.makeMap("alt,ctrl,shift,meta,access"), fA = (e2) => {
    const t2 = {}, n2 = Nt.os.isMacOS() || Nt.os.isiOS();
    dA(cA(e2.toLowerCase(), "+"), (e3) => {
      ((e4) => e4 in mA)(e3) ? t2[e3] = true : /^[0-9]{2,}$/.test(e3) ? t2.keyCode = parseInt(e3, 10) : (t2.charCode = e3.charCodeAt(0), t2.keyCode = uA[e3] || e3.toUpperCase().charCodeAt(0));
    });
    const o2 = [t2.keyCode];
    let r2;
    for (r2 in mA) t2[r2] ? o2.push(r2) : t2[r2] = false;
    return t2.id = o2.join(","), t2.access && (t2.alt = true, n2 ? t2.ctrl = true : t2.shift = true), t2.meta && (n2 ? t2.meta = true : (t2.ctrl = true, t2.meta = false)), t2;
  };
  class gA {
    constructor(e2) {
      this.shortcuts = {}, this.pendingPatterns = [], this.editor = e2;
      const t2 = this;
      e2.on("keyup keypress keydown", (e3) => {
        !t2.hasModifier(e3) && !t2.isFunctionKey(e3) || e3.isDefaultPrevented() || (dA(t2.shortcuts, (n2) => {
          t2.matchShortcut(e3, n2) && (t2.pendingPatterns = n2.subpatterns.slice(0), "keydown" === e3.type && t2.executeShortcutAction(n2));
        }), t2.matchShortcut(e3, t2.pendingPatterns[0]) && (1 === t2.pendingPatterns.length && "keydown" === e3.type && t2.executeShortcutAction(t2.pendingPatterns[0]), t2.pendingPatterns.shift()));
      });
    }
    add(e2, t2, n2, o2) {
      const r2 = this, s2 = r2.normalizeCommandFunc(n2);
      return dA(cA(Tt.trim(e2)), (e3) => {
        const n3 = r2.createShortcut(e3, t2, s2, o2);
        r2.shortcuts[n3.id] = n3;
      }), true;
    }
    remove(e2) {
      const t2 = this.createShortcut(e2);
      return !!this.shortcuts[t2.id] && (delete this.shortcuts[t2.id], true);
    }
    normalizeCommandFunc(e2) {
      const t2 = this, n2 = e2;
      return "string" == typeof n2 ? () => {
        t2.editor.execCommand(n2, false, null);
      } : Tt.isArray(n2) ? () => {
        t2.editor.execCommand(n2[0], n2[1], n2[2]);
      } : n2;
    }
    createShortcut(e2, t2, n2, o2) {
      const r2 = Tt.map(cA(e2, ">"), fA);
      return r2[r2.length - 1] = Tt.extend(r2[r2.length - 1], { func: n2, scope: o2 || this.editor }), Tt.extend(r2[0], { desc: this.editor.translate(t2), subpatterns: r2.slice(1) });
    }
    hasModifier(e2) {
      return e2.altKey || e2.ctrlKey || e2.metaKey;
    }
    isFunctionKey(e2) {
      return "keydown" === e2.type && e2.keyCode >= 112 && e2.keyCode <= 123;
    }
    matchShortcut(e2, t2) {
      return !!t2 && t2.ctrl === e2.ctrlKey && t2.meta === e2.metaKey && t2.alt === e2.altKey && t2.shift === e2.shiftKey && !!(e2.keyCode === t2.keyCode || e2.charCode && e2.charCode === t2.charCode) && (e2.preventDefault(), true);
    }
    executeShortcutAction(e2) {
      return e2.func ? e2.func.call(e2.scope) : null;
    }
  }
  const pA = () => {
    const e2 = (() => {
      const e3 = {}, t2 = {}, n2 = {}, o2 = {}, r2 = {}, s2 = {}, a2 = {}, i3 = (e4, t3) => (n3, o3) => {
        e4[n3.toLowerCase()] = { ...o3, type: t3 };
      };
      return { addButton: i3(e3, "button"), addGroupToolbarButton: i3(e3, "grouptoolbarbutton"), addToggleButton: i3(e3, "togglebutton"), addMenuButton: i3(e3, "menubutton"), addSplitButton: i3(e3, "splitbutton"), addMenuItem: i3(t2, "menuitem"), addNestedMenuItem: i3(t2, "nestedmenuitem"), addToggleMenuItem: i3(t2, "togglemenuitem"), addAutocompleter: i3(n2, "autocompleter"), addContextMenu: i3(r2, "contextmenu"), addContextToolbar: i3(s2, "contexttoolbar"), addContextForm: i3(s2, "contextform"), addSidebar: i3(a2, "sidebar"), addIcon: (e4, t3) => o2[e4.toLowerCase()] = t3, getAll: () => ({ buttons: e3, menuItems: t2, icons: o2, popups: n2, contextMenus: r2, contextToolbars: s2, sidebars: a2 }) };
    })();
    return { addAutocompleter: e2.addAutocompleter, addButton: e2.addButton, addContextForm: e2.addContextForm, addContextMenu: e2.addContextMenu, addContextToolbar: e2.addContextToolbar, addIcon: e2.addIcon, addMenuButton: e2.addMenuButton, addMenuItem: e2.addMenuItem, addNestedMenuItem: e2.addNestedMenuItem, addSidebar: e2.addSidebar, addSplitButton: e2.addSplitButton, addToggleButton: e2.addToggleButton, addGroupToolbarButton: e2.addGroupToolbarButton, addToggleMenuItem: e2.addToggleMenuItem, getAll: e2.getAll };
  }, hA = ta.DOM, bA = Tt.extend, vA = Tt.each;
  class yA {
    constructor(e2, t2, n2) {
      this.plugins = {}, this.contentCSS = [], this.contentStyles = [], this.loadedCSS = {}, this.isNotDirty = false, this.composing = false, this.destroyed = false, this.hasHiddenInput = false, this.iframeElement = null, this.initialized = false, this.readonly = false, this.removed = false, this.startContent = "", this._pendingNativeEvents = [], this._skinLoaded = false, this.editorManager = n2, this.documentBaseUrl = n2.documentBaseURL, bA(this, nA);
      const o2 = this;
      this.id = e2, this.hidden = false;
      const r2 = ((e3, t3) => _R(vR || yR, vR, t3, e3, t3))(n2.defaultOptions, t2);
      this.options = /* @__PURE__ */ ((e3, t3) => {
        const n3 = {}, o3 = {}, r3 = (e4, t4, n4) => {
          const r4 = aA(t4, n4);
          return sA(r4) ? (o3[e4] = r4.value, true) : (console.warn(rA(`Invalid value passed for the ${e4} option`, r4)), false);
        }, s3 = (e4) => xe(n3, e4);
        return { register: (e4, s4) => {
          const a3 = ((e5) => m(e5.processor))(s4) ? ((e5) => {
            const t4 = (() => {
              switch (e5) {
                case "array":
                  return p;
                case "boolean":
                  return b;
                case "function":
                  return w;
                case "number":
                  return x;
                case "object":
                  return f;
                case "string":
                  return m;
                case "string[]":
                  return oA;
                case "object[]":
                  return (e6) => k(e6, f);
                case "regexp":
                  return (e6) => u(e6, RegExp);
                default:
                  return L;
              }
            })();
            return (n4) => aA(n4, t4, `The value must be a ${e5}.`);
          })(s4.processor) : s4.processor, i4 = ((e5, t4, n4) => {
            if (!v(t4)) {
              const o4 = aA(t4, n4);
              if (sA(o4)) return o4.value;
              console.error(rA(`Invalid default value passed for the "${e5}" option`, o4));
            }
          })(e4, s4.default, a3);
          n3[e4] = { ...s4, default: i4, processor: a3 }, we(o3, e4).orThunk(() => we(t3, e4)).each((t4) => r3(e4, t4, a3));
        }, isRegistered: s3, get: (e4) => we(o3, e4).orThunk(() => we(n3, e4).map((e5) => e5.default)).getOrUndefined(), set: (e4, t4) => {
          if (s3(e4)) {
            const o4 = n3[e4];
            return o4.immutable ? (console.error(`"${e4}" is an immutable option and cannot be updated`), false) : r3(e4, t4, o4.processor);
          }
          return console.warn(`"${e4}" is not a registered option. Ensure the option has been registered before setting a value.`), false;
        }, unset: (e4) => {
          const t4 = s3(e4);
          return t4 && delete o3[e4], t4;
        }, isSet: (e4) => xe(o3, e4) };
      })(0, r2), ((e3) => {
        const t3 = e3.options.register;
        t3("id", { processor: "string", default: e3.id }), t3("selector", { processor: "string" }), t3("target", { processor: "object" }), t3("suffix", { processor: "string" }), t3("cache_suffix", { processor: "string" }), t3("base_url", { processor: "string" }), t3("referrer_policy", { processor: "string", default: "" }), t3("language_load", { processor: "boolean", default: true }), t3("inline", { processor: "boolean", default: false }), t3("iframe_attrs", { processor: "object", default: {} }), t3("doctype", { processor: "string", default: "<!DOCTYPE html>" }), t3("document_base_url", { processor: "string", default: e3.documentBaseUrl }), t3("body_id", { processor: Vi(e3, "tinymce"), default: "tinymce" }), t3("body_class", { processor: Vi(e3), default: "" }), t3("content_security_policy", { processor: "string", default: "" }), t3("br_in_pre", { processor: "boolean", default: true }), t3("forced_root_block", { processor: (e4) => {
          const t4 = m(e4) && We(e4);
          return t4 ? { value: e4, valid: t4 } : { valid: false, message: "Must be a non-empty string." };
        }, default: "p" }), t3("forced_root_block_attrs", { processor: "object", default: {} }), t3("newline_behavior", { processor: (e4) => {
          const t4 = j(["block", "linebreak", "invert", "default"], e4);
          return t4 ? { value: e4, valid: t4 } : { valid: false, message: "Must be one of: block, linebreak, invert or default." };
        }, default: "default" }), t3("br_newline_selector", { processor: "string", default: ".mce-toc h2,figcaption,caption" }), t3("no_newline_selector", { processor: "string", default: "" }), t3("keep_styles", { processor: "boolean", default: true }), t3("end_container_on_empty_block", { processor: (e4) => b(e4) || m(e4) ? { valid: true, value: e4 } : { valid: false, message: "Must be boolean or a string" }, default: "blockquote" }), t3("font_size_style_values", { processor: "string", default: "xx-small,x-small,small,medium,large,x-large,xx-large" }), t3("font_size_legacy_values", { processor: "string", default: "xx-small,small,medium,large,x-large,xx-large,300%" }), t3("font_size_classes", { processor: "string", default: "" }), t3("automatic_uploads", { processor: "boolean", default: true }), t3("images_reuse_filename", { processor: "boolean", default: false }), t3("images_replace_blob_uris", { processor: "boolean", default: true }), t3("icons", { processor: "string", default: "" }), t3("icons_url", { processor: "string", default: "" }), t3("images_upload_url", { processor: "string", default: "" }), t3("images_upload_base_path", { processor: "string", default: "" }), t3("images_upload_credentials", { processor: "boolean", default: false }), t3("images_upload_handler", { processor: "function" }), t3("language", { processor: "string", default: "en" }), t3("language_url", { processor: "string", default: "" }), t3("entity_encoding", { processor: "string", default: "named" }), t3("indent", { processor: "boolean", default: true }), t3("indent_before", { processor: "string", default: "p,h1,h2,h3,h4,h5,h6,blockquote,div,title,style,pre,script,td,th,ul,ol,li,dl,dt,dd,area,table,thead,tfoot,tbody,tr,section,summary,article,hgroup,aside,figure,figcaption,option,optgroup,datalist" }), t3("indent_after", { processor: "string", default: "p,h1,h2,h3,h4,h5,h6,blockquote,div,title,style,pre,script,td,th,ul,ol,li,dl,dt,dd,area,table,thead,tfoot,tbody,tr,section,summary,article,hgroup,aside,figure,figcaption,option,optgroup,datalist" }), t3("indent_use_margin", { processor: "boolean", default: false }), t3("indentation", { processor: "string", default: "40px" }), t3("content_css", { processor: (e4) => {
          const t4 = false === e4 || m(e4) || k(e4, m);
          return t4 ? m(e4) ? { value: $2(e4.split(","), $e), valid: t4 } : p(e4) ? { value: e4, valid: t4 } : false === e4 ? { value: [], valid: t4 } : { value: e4, valid: t4 } : { valid: false, message: "Must be false, a string or an array of strings." };
        }, default: zl(e3) ? [] : ["default"] }), t3("content_style", { processor: "string" }), t3("content_css_cors", { processor: "boolean", default: false }), t3("font_css", { processor: (e4) => {
          const t4 = m(e4) || k(e4, m);
          return t4 ? { value: p(e4) ? e4 : $2(e4.split(","), $e), valid: t4 } : { valid: false, message: "Must be a string or an array of strings." };
        }, default: [] }), t3("inline_boundaries", { processor: "boolean", default: true }), t3("inline_boundaries_selector", { processor: "string", default: "a[href],code,span.mce-annotation" }), t3("object_resizing", { processor: (e4) => {
          const t4 = b(e4) || m(e4);
          return t4 ? false === e4 || Fi.isiPhone() || Fi.isiPad() ? { value: "", valid: t4 } : { value: true === e4 ? "table,img,figure.image,div,video,iframe" : e4, valid: t4 } : { valid: false, message: "Must be boolean or a string" };
        }, default: !Ui }), t3("resize_img_proportional", { processor: "boolean", default: true }), t3("event_root", { processor: "object" }), t3("service_message", { processor: "string" }), t3("theme", { processor: (e4) => false === e4 || m(e4) || w(e4), default: "silver" }), t3("theme_url", { processor: "string" }), t3("formats", { processor: "object" }), t3("format_empty_lines", { processor: "boolean", default: false }), t3("format_noneditable_selector", { processor: "string", default: "" }), t3("preview_styles", { processor: (e4) => {
          const t4 = false === e4 || m(e4);
          return t4 ? { value: false === e4 ? "" : e4, valid: t4 } : { valid: false, message: "Must be false or a string" };
        }, default: "font-family font-size font-weight font-style text-decoration text-transform color background-color border border-radius outline text-shadow" }), t3("custom_ui_selector", { processor: "string", default: "" }), t3("hidden_input", { processor: "boolean", default: true }), t3("submit_patch", { processor: "boolean", default: true }), t3("encoding", { processor: "string" }), t3("add_form_submit_trigger", { processor: "boolean", default: true }), t3("add_unload_trigger", { processor: "boolean", default: true }), t3("custom_undo_redo_levels", { processor: "number", default: 0 }), t3("disable_nodechange", { processor: "boolean", default: false }), t3("readonly", { processor: "boolean", default: false }), t3("plugins", { processor: "string[]", default: [] }), t3("external_plugins", { processor: "object" }), t3("forced_plugins", { processor: "string[]" }), t3("model", { processor: "string", default: e3.hasPlugin("rtc") ? "plugin" : "dom" }), t3("model_url", { processor: "string" }), t3("block_unsupported_drop", { processor: "boolean", default: true }), t3("visual", { processor: "boolean", default: true }), t3("visual_table_class", { processor: "string", default: "mce-item-table" }), t3("visual_anchor_class", { processor: "string", default: "mce-item-anchor" }), t3("iframe_aria_text", { processor: "string", default: "Rich Text Area. Press ALT-0 for help." }), t3("setup", { processor: "function" }), t3("init_instance_callback", { processor: "function" }), t3("url_converter", { processor: "function", default: e3.convertURL }), t3("url_converter_scope", { processor: "object", default: e3 }), t3("urlconverter_callback", { processor: "function" }), t3("allow_conditional_comments", { processor: "boolean", default: false }), t3("allow_html_data_urls", { processor: "boolean", default: false }), t3("allow_svg_data_urls", { processor: "boolean" }), t3("allow_html_in_named_anchor", { processor: "boolean", default: false }), t3("allow_script_urls", { processor: "boolean", default: false }), t3("allow_unsafe_link_target", { processor: "boolean", default: false }), t3("convert_fonts_to_spans", { processor: "boolean", default: true, deprecated: true }), t3("fix_list_elements", { processor: "boolean", default: false }), t3("preserve_cdata", { processor: "boolean", default: false }), t3("remove_trailing_brs", { processor: "boolean" }), t3("inline_styles", { processor: "boolean", default: true, deprecated: true }), t3("element_format", { processor: "string", default: "html" }), t3("entities", { processor: "string" }), t3("schema", { processor: "string", default: "html5" }), t3("convert_urls", { processor: "boolean", default: true }), t3("relative_urls", { processor: "boolean", default: true }), t3("remove_script_host", { processor: "boolean", default: true }), t3("custom_elements", { processor: "string" }), t3("extended_valid_elements", { processor: "string" }), t3("invalid_elements", { processor: "string" }), t3("invalid_styles", { processor: $i }), t3("valid_children", { processor: "string" }), t3("valid_classes", { processor: $i }), t3("valid_elements", { processor: "string" }), t3("valid_styles", { processor: $i }), t3("verify_html", { processor: "boolean", default: true }), t3("auto_focus", { processor: (e4) => m(e4) || true === e4 }), t3("browser_spellcheck", { processor: "boolean", default: false }), t3("protect", { processor: "array" }), t3("images_file_types", { processor: "string", default: "jpeg,jpg,jpe,jfi,jif,jfif,png,gif,bmp,webp" }), t3("deprecation_warnings", { processor: "boolean", default: true }), t3("a11y_advanced_options", { processor: "boolean", default: false }), t3("api_key", { processor: "string" }), t3("paste_block_drop", { processor: "boolean", default: false }), t3("paste_data_images", { processor: "boolean", default: true }), t3("paste_preprocess", { processor: "function" }), t3("paste_postprocess", { processor: "function" }), t3("paste_webkit_styles", { processor: "string", default: "none" }), t3("paste_remove_styles_if_webkit", { processor: "boolean", default: true }), t3("paste_merge_formats", { processor: "boolean", default: true }), t3("smart_paste", { processor: "boolean", default: true }), t3("paste_as_text", { processor: "boolean", default: false }), t3("paste_tab_spaces", { processor: "number", default: 4 }), t3("text_patterns", { processor: (e4) => k(e4, f) || false === e4 ? { value: Ii(false === e4 ? [] : e4), valid: true } : { valid: false, message: "Must be an array of objects or false." }, default: [{ start: "*", end: "*", format: "italic" }, { start: "**", end: "**", format: "bold" }, { start: "#", format: "h1" }, { start: "##", format: "h2" }, { start: "###", format: "h3" }, { start: "####", format: "h4" }, { start: "#####", format: "h5" }, { start: "######", format: "h6" }, { start: "1. ", cmd: "InsertOrderedList" }, { start: "* ", cmd: "InsertUnorderedList" }, { start: "- ", cmd: "InsertUnorderedList" }] }), t3("text_patterns_lookup", { processor: (e4) => {
          return w(e4) ? { value: (t4 = e4, (e5) => {
            const n3 = t4(e5);
            return Ii(n3);
          }), valid: true } : { valid: false, message: "Must be a single function" };
          var t4;
        }, default: (e4) => [] }), t3("noneditable_class", { processor: "string", default: "mceNonEditable" }), t3("editable_class", { processor: "string", default: "mceEditable" }), t3("noneditable_regexp", { processor: (e4) => k(e4, ji) ? { value: e4, valid: true } : ji(e4) ? { value: [e4], valid: true } : { valid: false, message: "Must be a RegExp or an array of RegExp." }, default: [] }), t3("table_tab_navigation", { processor: "boolean", default: true }), e3.on("ScriptsLoaded", () => {
          t3("directionality", { processor: "string", default: la.isRtl() ? "rtl" : void 0 }), t3("placeholder", { processor: "string", default: zi.getAttrib(e3.getElement(), "placeholder") });
        });
      })(o2);
      const s2 = this.options.get;
      s2("deprecation_warnings") && ((e3, t3) => {
        ((e4, t4) => {
          const n3 = Ky(e4), o3 = Gy(t4), r3 = o3.length > 0, s3 = n3.length > 0, a3 = "mobile" === t4.theme;
          if (r3 || s3 || a3) {
            const e5 = "\n- ", t5 = a3 ? `

Themes:${e5}mobile` : "", i4 = r3 ? `

Plugins:${e5}${o3.join(e5)}` : "", l3 = s3 ? `

Options:${e5}${n3.join(e5)}` : "";
            console.warn("The following deprecated features are currently enabled and have been removed in TinyMCE 6.0. These features will no longer work and should be removed from the TinyMCE configuration. See https://www.tiny.cloud/docs/tinymce/6/migration-from-5x/ for more information." + t5 + i4 + l3);
          }
        })(e3, t3);
      })(t2, r2);
      const a2 = s2("suffix");
      a2 && (n2.suffix = a2), this.suffix = n2.suffix;
      const i3 = s2("base_url");
      i3 && n2._setBaseUrl(i3), this.baseUri = n2.baseURI;
      const l2 = pl(o2);
      l2 && (oa.ScriptLoader._setReferrerPolicy(l2), ta.DOM.styleSheetLoader._setReferrerPolicy(l2)), da.languageLoad = s2("language_load"), da.baseURL = n2.baseURL, this.setDirty(false), this.documentBaseURI = new qv(Ki(o2), { base_uri: this.baseUri }), this.baseURI = this.baseUri, this.inline = zl(o2), this.hasVisual = Jl(o2), this.shortcuts = new gA(this), this.editorCommands = new UR(this), FR(this);
      const d2 = s2("cache_suffix");
      d2 && (Nt.cacheSuffix = d2.replace(/^[\?\&]+/, "")), this.ui = { registry: pA(), styleSheetLoader: void 0, show: S, hide: S, setEnabled: S, isEnabled: L }, this.mode = ((e3) => {
        const t3 = ra("design"), n3 = ra({ design: { activate: S, deactivate: S, editorReadOnly: false }, readonly: { activate: S, deactivate: S, editorReadOnly: true } });
        return ((e4) => {
          e4.serializer ? qR(e4) : e4.on("PreInit", () => {
            qR(e4);
          });
        })(e3), ((e4) => {
          e4.on("ShowCaret", (t4) => {
            VR(e4) && t4.preventDefault();
          }), e4.on("ObjectSelected", (t4) => {
            VR(e4) && t4.preventDefault();
          });
        })(e3), { isReadOnly: () => VR(e3), set: (o3) => ((e4, t4, n4, o4) => {
          if (o4 !== n4.get()) {
            if (!xe(t4, o4)) throw new Error(`Editor mode '${o4}' is invalid`);
            e4.initialized ? lA(e4, n4, t4, o4) : e4.on("init", () => lA(e4, n4, t4, o4));
          }
        })(e3, n3.get(), t3, o3), get: () => t3.get(), register: (e4, t4) => {
          n3.set(((e5, t5, n4) => {
            if (j(iA, t5)) throw new Error(`Cannot override default mode ${t5}`);
            return { ...e5, [t5]: { ...n4, deactivate: () => {
              try {
                n4.deactivate();
              } catch (e6) {
                console.error(`problem while deactivating editor mode ${t5}:`, e6);
              }
            } } };
          })(n3.get(), e4, t4));
        } };
      })(o2), n2.dispatch("SetupEditor", { editor: this });
      const c2 = nd(o2);
      w(c2) && c2.call(o2, o2);
    }
    render() {
      ((e2) => {
        const t2 = e2.id;
        la.setCode(hl(e2));
        const n2 = () => {
          fR.unbind(window, "ready", n2), e2.render();
        };
        if (!qs.Event.domLoaded) return void fR.bind(window, "ready", n2);
        if (!e2.getElement()) return;
        const o2 = mn(e2.getElement()), r2 = Xt(o2);
        e2.on("remove", () => {
          q(o2.dom.attributes, (e3) => Yt(o2, e3.name)), qt(o2, r2);
        }), e2.ui.styleSheetLoader = ((e3, t3) => Jo.forElement(e3, { contentCssCors: Gl(t3), referrerPolicy: pl(t3) }))(o2, e2), zl(e2) ? e2.inline = true : (e2.orgVisibility = e2.getElement().style.visibility, e2.getElement().style.visibility = "hidden");
        const s2 = e2.getElement().form || fR.getParent(t2, "form");
        s2 && (e2.formElement = s2, jl(e2) && !No(e2.getElement()) && (fR.insertAfter(fR.create("input", { type: "hidden", name: t2 }), t2), e2.hasHiddenInput = true), e2.formEventDelegate = (t3) => {
          e2.dispatch(t3.type, t3);
        }, fR.bind(s2, "submit reset", e2.formEventDelegate), e2.on("reset", () => {
          e2.resetContent();
        }), !Hl(e2) || s2.submit.nodeType || s2.submit.length || s2._mceOldSubmit || (s2._mceOldSubmit = s2.submit, s2.submit = () => (e2.editorManager.triggerSave(), e2.setDirty(false), s2._mceOldSubmit(s2)))), e2.windowManager = lC(e2), e2.notificationManager = sC(e2), ((e3) => "xml" === e3.options.get("encoding"))(e2) && e2.on("GetContent", (e3) => {
          e3.save && (e3.content = fR.encode(e3.content));
        }), $l(e2) && e2.on("submit", () => {
          e2.initialized && e2.save();
        }), Vl(e2) && (e2._beforeUnload = () => {
          !e2.initialized || e2.destroyed || e2.isHidden() || e2.save({ format: "raw", no_events: true, set_dirty: false });
        }, e2.editorManager.on("BeforeUnload", e2._beforeUnload)), e2.editorManager.add(e2), hR(e2, e2.suffix);
      })(this);
    }
    focus(e2) {
      this.execCommand("mceFocus", false, e2);
    }
    hasFocus() {
      return If(this);
    }
    translate(e2) {
      return la.translate(e2);
    }
    getParam(e2, t2, n2) {
      const o2 = this.options;
      return o2.isRegistered(e2) || (C(n2) ? o2.register(e2, { processor: n2, default: t2 }) : o2.register(e2, { processor: L, default: t2 })), o2.isSet(e2) || v(t2) ? o2.get(e2) : t2;
    }
    hasPlugin(e2, t2) {
      return !(!j(Yl(this), e2) || t2 && void 0 === aC.get(e2));
    }
    nodeChanged(e2) {
      this._nodeChangeDispatcher.nodeChanged(e2);
    }
    addCommand(e2, t2, n2) {
      this.editorCommands.addCommand(e2, t2, n2);
    }
    addQueryStateHandler(e2, t2, n2) {
      this.editorCommands.addQueryStateHandler(e2, t2, n2);
    }
    addQueryValueHandler(e2, t2, n2) {
      this.editorCommands.addQueryValueHandler(e2, t2, n2);
    }
    addShortcut(e2, t2, n2, o2) {
      this.shortcuts.add(e2, t2, n2, o2);
    }
    execCommand(e2, t2, n2, o2) {
      return this.editorCommands.execCommand(e2, t2, n2, o2);
    }
    queryCommandState(e2) {
      return this.editorCommands.queryCommandState(e2);
    }
    queryCommandValue(e2) {
      return this.editorCommands.queryCommandValue(e2);
    }
    queryCommandSupported(e2) {
      return this.editorCommands.queryCommandSupported(e2);
    }
    show() {
      const e2 = this;
      e2.hidden && (e2.hidden = false, e2.inline ? e2.getBody().contentEditable = "true" : (hA.show(e2.getContainer()), hA.hide(e2.id)), e2.load(), e2.dispatch("show"));
    }
    hide() {
      const e2 = this;
      e2.hidden || (e2.save(), e2.inline ? (e2.getBody().contentEditable = "false", e2 === e2.editorManager.focusedEditor && (e2.editorManager.focusedEditor = null)) : (hA.hide(e2.getContainer()), hA.setStyle(e2.id, "display", e2.orgDisplay)), e2.hidden = true, e2.dispatch("hide"));
    }
    isHidden() {
      return this.hidden;
    }
    setProgressState(e2, t2) {
      this.dispatch("ProgressState", { state: e2, time: t2 });
    }
    load(e2 = {}) {
      const t2 = this, n2 = t2.getElement();
      if (t2.removed) return "";
      if (n2) {
        const o2 = { ...e2, load: true }, r2 = No(n2) ? n2.value : n2.innerHTML, s2 = t2.setContent(r2, o2);
        return o2.no_events || t2.dispatch("LoadContent", { ...o2, element: n2 }), s2;
      }
      return "";
    }
    save(e2 = {}) {
      const t2 = this;
      let n2 = t2.getElement();
      if (!n2 || !t2.initialized || t2.removed) return "";
      const o2 = { ...e2, save: true, element: n2 };
      let r2 = t2.getContent(o2);
      const s2 = { ...o2, content: r2 };
      if (s2.no_events || t2.dispatch("SaveContent", s2), "raw" === s2.format && t2.dispatch("RawSaveContent", s2), r2 = s2.content, No(n2)) n2.value = r2;
      else {
        !e2.is_removing && t2.inline || (n2.innerHTML = r2);
        const o3 = hA.getParent(t2.id, "form");
        o3 && vA(o3.elements, (e3) => e3.name !== t2.id || (e3.value = r2, false));
      }
      return s2.element = o2.element = n2 = null, false !== s2.set_dirty && t2.setDirty(false), r2;
    }
    setContent(e2, t2) {
      return Vy(this, e2, t2);
    }
    getContent(e2) {
      return ((e3, t2 = {}) => {
        const n2 = ((e4, t3) => ({ ...e4, format: t3, get: true, getInner: true }))(t2, t2.format ? t2.format : "html");
        return ty(e3, n2).fold(R, (t3) => {
          const n3 = ((e4, t4) => Dy(e4).editor.getContent(t4))(e3, t3);
          return ny(e3, n3, t3);
        });
      })(this, e2);
    }
    insertContent(e2, t2) {
      t2 && (e2 = bA({ content: e2 }, t2)), this.execCommand("mceInsertContent", false, e2);
    }
    resetContent(e2) {
      void 0 === e2 ? Vy(this, this.startContent, { format: "raw" }) : Vy(this, e2), this.undoManager.reset(), this.setDirty(false), this.nodeChanged();
    }
    isDirty() {
      return !this.isNotDirty;
    }
    setDirty(e2) {
      const t2 = !this.isNotDirty;
      this.isNotDirty = !e2, e2 && e2 !== t2 && this.dispatch("dirty");
    }
    getContainer() {
      const e2 = this;
      return e2.container || (e2.container = e2.editorContainer || hA.get(e2.id + "_parent")), e2.container;
    }
    getContentAreaContainer() {
      return this.contentAreaContainer;
    }
    getElement() {
      return this.targetElm || (this.targetElm = hA.get(this.id)), this.targetElm;
    }
    getWin() {
      const e2 = this;
      if (!e2.contentWindow) {
        const t2 = e2.iframeElement;
        t2 && (e2.contentWindow = t2.contentWindow);
      }
      return e2.contentWindow;
    }
    getDoc() {
      const e2 = this;
      if (!e2.contentDocument) {
        const t2 = e2.getWin();
        t2 && (e2.contentDocument = t2.document);
      }
      return e2.contentDocument;
    }
    getBody() {
      var e2, t2;
      const n2 = this.getDoc();
      return null !== (t2 = null !== (e2 = this.bodyElement) && void 0 !== e2 ? e2 : null == n2 ? void 0 : n2.body) && void 0 !== t2 ? t2 : null;
    }
    convertURL(e2, t2, n2) {
      const o2 = this, r2 = o2.options.get, s2 = rd(o2);
      return w(s2) ? s2.call(o2, e2, n2, true, t2) : !r2("convert_urls") || "link" === n2 || f(n2) && "LINK" === n2.nodeName || 0 === e2.indexOf("file:") || 0 === e2.length ? e2 : r2("relative_urls") ? o2.documentBaseURI.toRelative(e2) : e2 = o2.documentBaseURI.toAbsolute(e2, r2("remove_script_host"));
    }
    addVisual(e2) {
      ((e3, t2) => {
        ((e4, t3) => {
          Py(e4).editor.addVisual(t3);
        })(e3, t2);
      })(this, e2);
    }
    remove() {
      ((e2) => {
        if (!e2.removed) {
          const { _selectionOverrides: t2, editorUpload: n2 } = e2, o2 = e2.getBody(), r2 = e2.getElement();
          o2 && e2.save({ is_removing: true }), e2.removed = true, e2.unbindAllNativeEvents(), e2.hasHiddenInput && C(null == r2 ? void 0 : r2.nextSibling) && Yy.remove(r2.nextSibling), ((e3) => {
            e3.dispatch("remove");
          })(e2), e2.editorManager.remove(e2), !e2.inline && o2 && ((e3) => {
            Yy.setStyle(e3.id, "display", e3.orgDisplay);
          })(e2), ((e3) => {
            e3.dispatch("detach");
          })(e2), Yy.remove(e2.getContainer()), Xy(t2), Xy(n2), e2.destroy();
        }
      })(this);
    }
    destroy(e2) {
      ((e3, t2) => {
        const { selection: n2, dom: o2 } = e3;
        e3.destroyed || (t2 || e3.removed ? (t2 || (e3.editorManager.off("beforeunload", e3._beforeUnload), e3.theme && e3.theme.destroy && e3.theme.destroy(), Xy(n2), Xy(o2)), ((e4) => {
          const t3 = e4.formElement;
          t3 && (t3._mceOldSubmit && (t3.submit = t3._mceOldSubmit, delete t3._mceOldSubmit), Yy.unbind(t3, "submit reset", e4.formEventDelegate));
        })(e3), ((e4) => {
          const t3 = e4;
          t3.contentAreaContainer = t3.formElement = t3.container = t3.editorContainer = null, t3.bodyElement = t3.contentDocument = t3.contentWindow = null, t3.iframeElement = t3.targetElm = null;
          const n3 = e4.selection;
          if (n3) {
            const e5 = n3.dom;
            t3.selection = n3.win = n3.dom = e5.doc = null;
          }
        })(e3), e3.destroyed = true) : e3.remove());
      })(this, e2);
    }
    uploadImages() {
      return this.editorUpload.uploadImages();
    }
    _scanForImages() {
      return this.editorUpload.scanForImages();
    }
  }
  const CA = ta.DOM, wA = Tt.each;
  let xA, kA = false, SA = [];
  const _A = (e2) => {
    const t2 = e2.type;
    wA(AA.get(), (n2) => {
      switch (t2) {
        case "scroll":
          n2.dispatch("ScrollWindow", e2);
          break;
        case "resize":
          n2.dispatch("ResizeWindow", e2);
      }
    });
  }, EA = (e2) => {
    if (e2 !== kA) {
      const t2 = ta.DOM;
      e2 ? (t2.bind(window, "resize", _A), t2.bind(window, "scroll", _A)) : (t2.unbind(window, "resize", _A), t2.unbind(window, "scroll", _A)), kA = e2;
    }
  }, NA = (e2) => {
    const t2 = SA;
    return SA = K(SA, (t3) => e2 !== t3), AA.activeEditor === e2 && (AA.activeEditor = SA.length > 0 ? SA[0] : null), AA.focusedEditor === e2 && (AA.focusedEditor = null), t2.length !== SA.length;
  }, RA = "CSS1Compat" !== document.compatMode, AA = { ...XR, baseURI: null, baseURL: null, defaultOptions: {}, documentBaseURL: null, suffix: null, majorVersion: "6", minorVersion: "2.0", releaseDate: "2022-09-08", i18n: la, activeEditor: null, focusedEditor: null, setup() {
    const e2 = this;
    let t2 = "", n2 = "", o2 = qv.getDocumentBaseUrl(document.location);
    /^[^:]+:\/\/\/?[^\/]+\//.test(o2) && (o2 = o2.replace(/[\?#].*$/, "").replace(/[\/\\][^\/]+$/, ""), /[\/\\]$/.test(o2) || (o2 += "/"));
    const r2 = window.tinymce || window.tinyMCEPreInit;
    if (r2) t2 = r2.base || r2.baseURL, n2 = r2.suffix;
    else {
      const e3 = document.getElementsByTagName("script");
      for (let o3 = 0; o3 < e3.length; o3++) {
        const r3 = e3[o3].src || "";
        if ("" === r3) continue;
        const s3 = r3.substring(r3.lastIndexOf("/"));
        if (/tinymce(\.full|\.jquery|)(\.min|\.dev|)\.js/.test(r3)) {
          -1 !== s3.indexOf(".min") && (n2 = ".min"), t2 = r3.substring(0, r3.lastIndexOf("/"));
          break;
        }
      }
      if (!t2 && document.currentScript) {
        const e4 = document.currentScript.src;
        -1 !== e4.indexOf(".min") && (n2 = ".min"), t2 = e4.substring(0, e4.lastIndexOf("/"));
      }
    }
    var s2;
    e2.baseURL = new qv(o2).toAbsolute(t2), e2.documentBaseURL = o2, e2.baseURI = new qv(e2.baseURL), e2.suffix = n2, (s2 = e2).on("AddEditor", O(Df, s2)), s2.on("RemoveEditor", O(Pf, s2));
  }, overrideDefaults(e2) {
    const t2 = e2.base_url;
    t2 && this._setBaseUrl(t2);
    const n2 = e2.suffix;
    n2 && (this.suffix = n2), this.defaultOptions = e2;
    const o2 = e2.plugin_base_urls;
    void 0 !== o2 && fe(o2, (e3, t3) => {
      da.PluginManager.urls[t3] = e3;
    });
  }, init(e2) {
    const t2 = this;
    let n2;
    const o2 = Tt.makeMap("area base basefont br col frame hr img input isindex link meta param embed source wbr track colgroup option table tbody tfoot thead tr th td script noscript style textarea video audio iframe object menu", " ");
    let r2 = (e3) => {
      n2 = e3;
    };
    const s2 = () => {
      let n3 = 0;
      const a2 = [];
      let i3;
      CA.unbind(window, "ready", s2), ((n4) => {
        const o3 = e2.onpageload;
        o3 && o3.apply(t2, []);
      })(), i3 = ((e3, t3) => {
        const n4 = [], o3 = w(t3) ? (e4) => H(n4, (n5) => t3(n5, e4)) : (e4) => j(n4, e4);
        for (let t4 = 0, r3 = e3.length; t4 < r3; t4++) {
          const r4 = e3[t4];
          o3(r4) || n4.push(r4);
        }
        return n4;
      })(((e3) => Nt.browser.isIE() || Nt.browser.isEdge() ? (fC("TinyMCE does not support the browser you are using. For a list of supported browsers please see: https://www.tiny.cloud/docs/tinymce/6/support/#supportedwebbrowsers"), []) : RA ? (fC("Failed to initialize the editor as the document is not in standards mode. TinyMCE requires standards mode."), []) : m(e3.selector) ? CA.select(e3.selector) : C(e3.target) ? [e3.target] : [])(e2)), Tt.each(i3, (e3) => {
        var n4;
        (n4 = t2.get(e3.id)) && n4.initialized && !(n4.getContainer() || n4.getBody()).parentNode && (NA(n4), n4.unbindAllNativeEvents(), n4.destroy(true), n4.removed = true);
      }), i3 = Tt.grep(i3, (e3) => !t2.get(e3.id)), 0 === i3.length ? r2([]) : wA(i3, (s3) => {
        ((e3, t3) => e3.inline && t3.tagName.toLowerCase() in o2)(e2, s3) ? fC("Could not initialize inline editor on invalid inline target element", s3) : ((e3, o3, s4) => {
          const l2 = new yA(e3, o3, t2);
          a2.push(l2), l2.on("init", () => {
            ++n3 === i3.length && r2(a2);
          }), l2.targetElm = l2.targetElm || s4, l2.render();
        })(((e3) => {
          let t3 = e3.id;
          return t3 || (t3 = we(e3, "name").filter((e4) => !CA.get(e4)).getOrThunk(CA.uniqueId), e3.setAttribute("id", t3)), t3;
        })(s3), e2, s3);
      });
    };
    return CA.bind(window, "ready", s2), new Promise((e3) => {
      n2 ? e3(n2) : r2 = (t3) => {
        e3(t3);
      };
    });
  }, get(e2) {
    return 0 === arguments.length ? SA.slice(0) : m(e2) ? Q(SA, (t2) => t2.id === e2).getOr(null) : x(e2) && SA[e2] ? SA[e2] : null;
  }, add(e2) {
    const t2 = this, n2 = t2.get(e2.id);
    return n2 === e2 || (null === n2 && SA.push(e2), EA(true), t2.activeEditor = e2, t2.dispatch("AddEditor", { editor: e2 }), xA || (xA = (e3) => {
      const n3 = t2.dispatch("BeforeUnload");
      if (n3.returnValue) return e3.preventDefault(), e3.returnValue = n3.returnValue, n3.returnValue;
    }, window.addEventListener("beforeunload", xA))), e2;
  }, createEditor(e2, t2) {
    return this.add(new yA(e2, t2, this));
  }, remove(e2) {
    const t2 = this;
    let n2;
    if (e2) {
      if (!m(e2)) return n2 = e2, h(t2.get(n2.id)) ? null : (NA(n2) && t2.dispatch("RemoveEditor", { editor: n2 }), 0 === SA.length && window.removeEventListener("beforeunload", xA), n2.remove(), EA(SA.length > 0), n2);
      wA(CA.select(e2), (e3) => {
        n2 = t2.get(e3.id), n2 && t2.remove(n2);
      });
    } else for (let e3 = SA.length - 1; e3 >= 0; e3--) t2.remove(SA[e3]);
  }, execCommand(e2, t2, n2) {
    var o2;
    const r2 = this, s2 = f(n2) ? null !== (o2 = n2.id) && void 0 !== o2 ? o2 : n2.index : n2;
    switch (e2) {
      case "mceAddEditor":
        if (!r2.get(s2)) {
          const e3 = n2.options;
          new yA(s2, e3, r2).render();
        }
        return true;
      case "mceRemoveEditor": {
        const e3 = r2.get(s2);
        return e3 && e3.remove(), true;
      }
      case "mceToggleEditor": {
        const e3 = r2.get(s2);
        return e3 ? (e3.isHidden() ? e3.show() : e3.hide(), true) : (r2.execCommand("mceAddEditor", false, n2), true);
      }
    }
    return !!r2.activeEditor && r2.activeEditor.execCommand(e2, t2, n2);
  }, triggerSave: () => {
    wA(SA, (e2) => {
      e2.save();
    });
  }, addI18n: (e2, t2) => {
    la.add(e2, t2);
  }, translate: (e2) => la.translate(e2), setActive(e2) {
    const t2 = this.activeEditor;
    this.activeEditor !== e2 && (t2 && t2.dispatch("deactivate", { relatedTarget: e2 }), e2.dispatch("activate", { relatedTarget: t2 })), this.activeEditor = e2;
  }, _setBaseUrl(e2) {
    this.baseURL = new qv(this.documentBaseURL).toAbsolute(e2.replace(/\/+$/, "")), this.baseURI = new qv(this.baseURL);
  } };
  AA.setup();
  const OA = (() => {
    const e2 = ua();
    return { FakeClipboardItem: (e3) => ({ items: e3, types: ue(e3), getType: (t2) => we(e3, t2).getOrUndefined() }), write: (t2) => {
      e2.set(t2);
    }, read: () => e2.get().getOrUndefined(), clear: e2.clear };
  })(), TA = Math.min, BA = Math.max, DA = Math.round, PA = (e2, t2, n2) => {
    let o2 = t2.x, r2 = t2.y;
    const s2 = e2.w, a2 = e2.h, i3 = t2.w, l2 = t2.h, d2 = (n2 || "").split("");
    return "b" === d2[0] && (r2 += l2), "r" === d2[1] && (o2 += i3), "c" === d2[0] && (r2 += DA(l2 / 2)), "c" === d2[1] && (o2 += DA(i3 / 2)), "b" === d2[3] && (r2 -= a2), "r" === d2[4] && (o2 -= s2), "c" === d2[3] && (r2 -= DA(a2 / 2)), "c" === d2[4] && (o2 -= DA(s2 / 2)), LA(o2, r2, s2, a2);
  }, LA = (e2, t2, n2, o2) => ({ x: e2, y: t2, w: n2, h: o2 }), MA = { inflate: (e2, t2, n2) => LA(e2.x - t2, e2.y - n2, e2.w + 2 * t2, e2.h + 2 * n2), relativePosition: PA, findBestRelativePosition: (e2, t2, n2, o2) => {
    for (let r2 = 0; r2 < o2.length; r2++) {
      const s2 = PA(e2, t2, o2[r2]);
      if (s2.x >= n2.x && s2.x + s2.w <= n2.w + n2.x && s2.y >= n2.y && s2.y + s2.h <= n2.h + n2.y) return o2[r2];
    }
    return null;
  }, intersect: (e2, t2) => {
    const n2 = BA(e2.x, t2.x), o2 = BA(e2.y, t2.y), r2 = TA(e2.x + e2.w, t2.x + t2.w), s2 = TA(e2.y + e2.h, t2.y + t2.h);
    return r2 - n2 < 0 || s2 - o2 < 0 ? null : LA(n2, o2, r2 - n2, s2 - o2);
  }, clamp: (e2, t2, n2) => {
    let o2 = e2.x, r2 = e2.y, s2 = e2.x + e2.w, a2 = e2.y + e2.h;
    const i3 = t2.x + t2.w, l2 = t2.y + t2.h, d2 = BA(0, t2.x - o2), c2 = BA(0, t2.y - r2), u2 = BA(0, s2 - i3), m2 = BA(0, a2 - l2);
    return o2 += d2, r2 += c2, n2 && (s2 += d2, a2 += c2, o2 -= u2, r2 -= m2), s2 -= u2, a2 -= m2, LA(o2, r2, s2 - o2, a2 - r2);
  }, create: LA, fromClientRect: (e2) => LA(e2.left, e2.top, e2.width, e2.height) }, IA = /* @__PURE__ */ (() => {
    const e2 = {}, t2 = {};
    return { load: (n2, o2) => {
      const r2 = `Script at URL "${o2}" failed to load`, s2 = `Script at URL "${o2}" did not call \`tinymce.Resource.add('${n2}', data)\` within 1 second`;
      if (void 0 !== e2[n2]) return e2[n2];
      {
        const a2 = new Promise((e3, a3) => {
          const i3 = ((e4, t3, n3 = 1e3) => {
            let o3 = false, r3 = null;
            const s3 = (e5) => (...t4) => {
              o3 || (o3 = true, null !== r3 && (clearTimeout(r3), r3 = null), e5.apply(null, t4));
            }, a4 = s3(e4), i4 = s3(t3);
            return { start: (...e5) => {
              o3 || null !== r3 || (r3 = setTimeout(() => i4.apply(null, e5), n3));
            }, resolve: a4, reject: i4 };
          })(e3, a3);
          t2[n2] = i3.resolve, oa.ScriptLoader.loadScript(o2).then(() => i3.start(s2), () => i3.reject(r2));
        });
        return e2[n2] = a2, a2;
      }
    }, add: (n2, o2) => {
      void 0 !== t2[n2] && (t2[n2](o2), delete t2[n2]), e2[n2] = Promise.resolve(o2);
    }, unload: (t3) => {
      delete e2[t3];
    } };
  })();
  let FA;
  try {
    const e2 = "__storage_test__";
    FA = window.localStorage, FA.setItem(e2, e2), FA.removeItem(e2);
  } catch (e2) {
    FA = (() => {
      let e3 = {}, t2 = [];
      const n2 = { getItem: (t3) => e3[t3] || null, setItem: (n3, o2) => {
        t2.push(n3), e3[n3] = String(o2);
      }, key: (e4) => t2[e4], removeItem: (n3) => {
        t2 = t2.filter((e4) => e4 === n3), delete e3[n3];
      }, clear: () => {
        t2 = [], e3 = {};
      }, length: 0 };
      return Object.defineProperty(n2, "length", { get: () => t2.length, configurable: false, enumerable: false }), n2;
    })();
  }
  const UA = { geom: { Rect: MA }, util: { Delay: Af, Tools: Tt, VK: bm, URI: qv, EventDispatcher: GR, Observable: XR, I18n: la, LocalStorage: FA, ImageUploader: (e2) => {
    const t2 = bC(), n2 = wC(e2, t2);
    return { upload: (t3, o2 = true) => n2.upload(t3, o2 ? CC(e2) : void 0) };
  } }, dom: { EventUtils: qs, TreeWalker: Zo, TextSeeker: Ta, DOMUtils: ta, ScriptLoader: oa, RangeUtils: Ym, Serializer: $y, StyleSheetLoader: Qo, ControlSelection: xm, BookmarkManager: dm, Selection: zy, Event: qs.Event }, html: { Styles: Ms, Entities: xs, Node: Zf, Schema: Ls, DomParser: Zv, Writer: ag, Serializer: ig }, Env: Nt, AddOnManager: da, Annotator: lm, Formatter: DC, UndoManager: LC, EditorCommands: UR, WindowManager: lC, NotificationManager: sC, EditorObservable: nA, Shortcuts: gA, Editor: yA, FocusManager: Rf, EditorManager: AA, DOM: ta.DOM, ScriptLoader: oa.ScriptLoader, PluginManager: aC, ThemeManager: iC, ModelManager: Jy, IconManager: Qy, Resource: IA, FakeClipboard: OA, trim: Tt.trim, isArray: Tt.isArray, is: Tt.is, toArray: Tt.toArray, makeMap: Tt.makeMap, each: Tt.each, map: Tt.map, grep: Tt.grep, inArray: Tt.inArray, extend: Tt.extend, walk: Tt.walk, resolve: Tt.resolve, explode: Tt.explode, _addCacheSuffix: Tt._addCacheSuffix }, zA = Tt.extend(AA, UA);
  ((e2) => {
    window.tinymce = e2, window.tinyMCE = e2;
  })(zA), ((e2) => {
    if ("object" == typeof module) try {
      module.exports = e2;
    } catch (e3) {
    }
  })(zA);
}();
!function() {
  "use strict";
  var e = tinymce.util.Tools.resolve("tinymce.ModelManager");
  const t = (e2) => (t2) => ((e3) => {
    const t3 = typeof e3;
    return null === e3 ? "null" : "object" === t3 && Array.isArray(e3) ? "array" : "object" === t3 && (o2 = n2 = e3, (r2 = String).prototype.isPrototypeOf(o2) || (null === (s2 = n2.constructor) || void 0 === s2 ? void 0 : s2.name) === r2.name) ? "string" : t3;
    var o2, n2, r2, s2;
  })(t2) === e2, o = (e2) => (t2) => typeof t2 === e2, n = (e2) => (t2) => e2 === t2, r = t("string"), s = t("object"), l = t("array"), a = n(null), c = o("boolean"), i2 = n(void 0), m = (e2) => !((e3) => null == e3)(e2), d = o("function"), u = o("number"), f = () => {
  }, g = (e2) => () => e2, h = (e2) => e2, p = (e2, t2) => e2 === t2;
  function w(e2, ...t2) {
    return (...o2) => {
      const n2 = t2.concat(o2);
      return e2.apply(null, n2);
    };
  }
  const b = (e2) => (t2) => !e2(t2), v = (e2) => e2(), y = g(false), x = g(true);
  class C {
    constructor(e2, t2) {
      this.tag = e2, this.value = t2;
    }
    static some(e2) {
      return new C(true, e2);
    }
    static none() {
      return C.singletonNone;
    }
    fold(e2, t2) {
      return this.tag ? t2(this.value) : e2();
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(e2) {
      return this.tag ? C.some(e2(this.value)) : C.none();
    }
    bind(e2) {
      return this.tag ? e2(this.value) : C.none();
    }
    exists(e2) {
      return this.tag && e2(this.value);
    }
    forall(e2) {
      return !this.tag || e2(this.value);
    }
    filter(e2) {
      return !this.tag || e2(this.value) ? this : C.none();
    }
    getOr(e2) {
      return this.tag ? this.value : e2;
    }
    or(e2) {
      return this.tag ? this : e2;
    }
    getOrThunk(e2) {
      return this.tag ? this.value : e2();
    }
    orThunk(e2) {
      return this.tag ? this : e2();
    }
    getOrDie(e2) {
      if (this.tag) return this.value;
      throw new Error(null != e2 ? e2 : "Called getOrDie on None");
    }
    static from(e2) {
      return m(e2) ? C.some(e2) : C.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(e2) {
      this.tag && e2(this.value);
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  C.singletonNone = new C(false);
  const S = Array.prototype.slice, T = Array.prototype.indexOf, R = Array.prototype.push, D = (e2, t2) => {
    return o2 = e2, n2 = t2, T.call(o2, n2) > -1;
    var o2, n2;
  }, O = (e2, t2) => {
    for (let o2 = 0, n2 = e2.length; o2 < n2; o2++) if (t2(e2[o2], o2)) return true;
    return false;
  }, k = (e2, t2) => {
    const o2 = [];
    for (let n2 = 0; n2 < e2; n2++) o2.push(t2(n2));
    return o2;
  }, E = (e2, t2) => {
    const o2 = e2.length, n2 = new Array(o2);
    for (let r2 = 0; r2 < o2; r2++) {
      const o3 = e2[r2];
      n2[r2] = t2(o3, r2);
    }
    return n2;
  }, N = (e2, t2) => {
    for (let o2 = 0, n2 = e2.length; o2 < n2; o2++) t2(e2[o2], o2);
  }, B = (e2, t2) => {
    const o2 = [], n2 = [];
    for (let r2 = 0, s2 = e2.length; r2 < s2; r2++) {
      const s3 = e2[r2];
      (t2(s3, r2) ? o2 : n2).push(s3);
    }
    return { pass: o2, fail: n2 };
  }, z = (e2, t2) => {
    const o2 = [];
    for (let n2 = 0, r2 = e2.length; n2 < r2; n2++) {
      const r3 = e2[n2];
      t2(r3, n2) && o2.push(r3);
    }
    return o2;
  }, A = (e2, t2, o2) => (((e3, t3) => {
    for (let o3 = e3.length - 1; o3 >= 0; o3--) t3(e3[o3], o3);
  })(e2, (e3, n2) => {
    o2 = t2(o2, e3, n2);
  }), o2), W = (e2, t2, o2) => (N(e2, (e3, n2) => {
    o2 = t2(o2, e3, n2);
  }), o2), L = (e2, t2) => ((e3, t3, o2) => {
    for (let n2 = 0, r2 = e3.length; n2 < r2; n2++) {
      const r3 = e3[n2];
      if (t3(r3, n2)) return C.some(r3);
      if (o2(r3, n2)) break;
    }
    return C.none();
  })(e2, t2, y), _ = (e2, t2) => {
    for (let o2 = 0, n2 = e2.length; o2 < n2; o2++) if (t2(e2[o2], o2)) return C.some(o2);
    return C.none();
  }, M = (e2) => {
    const t2 = [];
    for (let o2 = 0, n2 = e2.length; o2 < n2; ++o2) {
      if (!l(e2[o2])) throw new Error("Arr.flatten item " + o2 + " was not an array, input: " + e2);
      R.apply(t2, e2[o2]);
    }
    return t2;
  }, j = (e2, t2) => M(E(e2, t2)), I = (e2, t2) => {
    for (let o2 = 0, n2 = e2.length; o2 < n2; ++o2) if (true !== t2(e2[o2], o2)) return false;
    return true;
  }, P = (e2, t2) => {
    const o2 = {};
    for (let n2 = 0, r2 = e2.length; n2 < r2; n2++) {
      const r3 = e2[n2];
      o2[String(r3)] = t2(r3, n2);
    }
    return o2;
  }, F = (e2, t2) => t2 >= 0 && t2 < e2.length ? C.some(e2[t2]) : C.none(), H = (e2) => F(e2, 0), q = (e2) => F(e2, e2.length - 1), V = (e2, t2) => {
    for (let o2 = 0; o2 < e2.length; o2++) {
      const n2 = t2(e2[o2], o2);
      if (n2.isSome()) return n2;
    }
    return C.none();
  }, $2 = Object.keys, U = Object.hasOwnProperty, G = (e2, t2) => {
    const o2 = $2(e2);
    for (let n2 = 0, r2 = o2.length; n2 < r2; n2++) {
      const r3 = o2[n2];
      t2(e2[r3], r3);
    }
  }, K = (e2, t2) => Y(e2, (e3, o2) => ({ k: o2, v: t2(e3, o2) })), Y = (e2, t2) => {
    const o2 = {};
    return G(e2, (e3, n2) => {
      const r2 = t2(e3, n2);
      o2[r2.k] = r2.v;
    }), o2;
  }, J = (e2, t2) => {
    const o2 = [];
    return G(e2, (e3, n2) => {
      o2.push(t2(e3, n2));
    }), o2;
  }, Q = (e2) => J(e2, h), X = (e2, t2) => U.call(e2, t2);
  "undefined" != typeof window ? window : Function("return this;")();
  const Z = (e2) => e2.dom.nodeName.toLowerCase(), ee = (e2) => e2.dom.nodeType, te = (e2) => (t2) => ee(t2) === e2, oe = (e2) => 8 === ee(e2) || "#comment" === Z(e2), ne = te(1), re = te(3), se = te(9), le = te(11), ae = (e2) => (t2) => ne(t2) && Z(t2) === e2, ce = (e2, t2, o2) => {
    if (!(r(o2) || c(o2) || u(o2))) throw console.error("Invalid call to Attribute.set. Key ", t2, ":: Value ", o2, ":: Element ", e2), new Error("Attribute value was not simple");
    e2.setAttribute(t2, o2 + "");
  }, ie = (e2, t2, o2) => {
    ce(e2.dom, t2, o2);
  }, me = (e2, t2) => {
    const o2 = e2.dom;
    G(t2, (e3, t3) => {
      ce(o2, t3, e3);
    });
  }, de = (e2, t2) => {
    const o2 = e2.dom.getAttribute(t2);
    return null === o2 ? void 0 : o2;
  }, ue = (e2, t2) => C.from(de(e2, t2)), fe = (e2, t2) => {
    e2.dom.removeAttribute(t2);
  }, ge = (e2) => W(e2.dom.attributes, (e3, t2) => (e3[t2.name] = t2.value, e3), {}), he = (e2) => {
    if (null == e2) throw new Error("Node cannot be null or undefined");
    return { dom: e2 };
  }, pe = { fromHtml: (e2, t2) => {
    const o2 = (t2 || document).createElement("div");
    if (o2.innerHTML = e2, !o2.hasChildNodes() || o2.childNodes.length > 1) {
      const t3 = "HTML does not have a single root node";
      throw console.error(t3, e2), new Error(t3);
    }
    return he(o2.childNodes[0]);
  }, fromTag: (e2, t2) => {
    const o2 = (t2 || document).createElement(e2);
    return he(o2);
  }, fromText: (e2, t2) => {
    const o2 = (t2 || document).createTextNode(e2);
    return he(o2);
  }, fromDom: he, fromPoint: (e2, t2, o2) => C.from(e2.dom.elementFromPoint(t2, o2)).map(he) }, we = (e2, t2) => {
    const o2 = e2.dom;
    if (1 !== o2.nodeType) return false;
    {
      const e3 = o2;
      if (void 0 !== e3.matches) return e3.matches(t2);
      if (void 0 !== e3.msMatchesSelector) return e3.msMatchesSelector(t2);
      if (void 0 !== e3.webkitMatchesSelector) return e3.webkitMatchesSelector(t2);
      if (void 0 !== e3.mozMatchesSelector) return e3.mozMatchesSelector(t2);
      throw new Error("Browser lacks native selectors");
    }
  }, be = (e2) => 1 !== e2.nodeType && 9 !== e2.nodeType && 11 !== e2.nodeType || 0 === e2.childElementCount, ve = (e2, t2) => {
    const o2 = void 0 === t2 ? document : t2.dom;
    return be(o2) ? C.none() : C.from(o2.querySelector(e2)).map(pe.fromDom);
  }, ye = (e2, t2) => e2.dom === t2.dom, xe = (e2, t2) => {
    const o2 = e2.dom, n2 = t2.dom;
    return o2 !== n2 && o2.contains(n2);
  }, Ce = we, Se = (e2) => pe.fromDom(e2.dom.ownerDocument), Te = (e2) => se(e2) ? e2 : Se(e2), Re = (e2) => C.from(e2.dom.parentNode).map(pe.fromDom), De = (e2, t2) => {
    const o2 = d(t2) ? t2 : y;
    let n2 = e2.dom;
    const r2 = [];
    for (; null !== n2.parentNode && void 0 !== n2.parentNode; ) {
      const e3 = n2.parentNode, t3 = pe.fromDom(e3);
      if (r2.push(t3), true === o2(t3)) break;
      n2 = e3;
    }
    return r2;
  }, Oe = (e2) => C.from(e2.dom.previousSibling).map(pe.fromDom), ke = (e2) => C.from(e2.dom.nextSibling).map(pe.fromDom), Ee = (e2) => E(e2.dom.childNodes, pe.fromDom), Ne = (e2, t2) => {
    const o2 = e2.dom.childNodes;
    return C.from(o2[t2]).map(pe.fromDom);
  }, Be = (e2, t2) => {
    Re(e2).each((o2) => {
      o2.dom.insertBefore(t2.dom, e2.dom);
    });
  }, ze = (e2, t2) => {
    ke(e2).fold(() => {
      Re(e2).each((e3) => {
        We(e3, t2);
      });
    }, (e3) => {
      Be(e3, t2);
    });
  }, Ae = (e2, t2) => {
    const o2 = ((e3) => Ne(e3, 0))(e2);
    o2.fold(() => {
      We(e2, t2);
    }, (o3) => {
      e2.dom.insertBefore(t2.dom, o3.dom);
    });
  }, We = (e2, t2) => {
    e2.dom.appendChild(t2.dom);
  }, Le = (e2, t2) => {
    Be(e2, t2), We(t2, e2);
  }, _e = (e2, t2) => {
    N(t2, (o2, n2) => {
      const r2 = 0 === n2 ? e2 : t2[n2 - 1];
      ze(r2, o2);
    });
  }, Me = (e2, t2) => {
    N(t2, (t3) => {
      We(e2, t3);
    });
  }, je = (e2) => {
    e2.dom.textContent = "", N(Ee(e2), (e3) => {
      Ie(e3);
    });
  }, Ie = (e2) => {
    const t2 = e2.dom;
    null !== t2.parentNode && t2.parentNode.removeChild(t2);
  }, Pe = (e2) => {
    const t2 = Ee(e2);
    t2.length > 0 && _e(e2, t2), Ie(e2);
  }, Fe = (e2, t2) => pe.fromDom(e2.dom.cloneNode(t2)), He = (e2) => Fe(e2, false), qe = (e2) => Fe(e2, true), Ve = (e2, t2) => {
    const o2 = pe.fromTag(t2), n2 = ge(e2);
    return me(o2, n2), o2;
  }, $e = ["tfoot", "thead", "tbody", "colgroup"], Ue = (e2, t2, o2) => ({ element: e2, rowspan: t2, colspan: o2 }), Ge = (e2, t2, o2) => ({ element: e2, cells: t2, section: o2 }), Ke = (e2, t2, o2) => ({ element: e2, isNew: t2, isLocked: o2 }), Ye = (e2, t2, o2, n2) => ({ element: e2, cells: t2, section: o2, isNew: n2 }), Je = d(Element.prototype.attachShadow) && d(Node.prototype.getRootNode), Qe = g(Je), Xe = Je ? (e2) => pe.fromDom(e2.dom.getRootNode()) : Te, Ze = (e2) => pe.fromDom(e2.dom.host), et = (e2) => {
    const t2 = re(e2) ? e2.dom.parentNode : e2.dom;
    if (null == t2 || null === t2.ownerDocument) return false;
    const o2 = t2.ownerDocument;
    return ((e3) => {
      const t3 = Xe(e3);
      return le(o3 = t3) && m(o3.dom.host) ? C.some(t3) : C.none();
      var o3;
    })(pe.fromDom(t2)).fold(() => o2.body.contains(t2), (n2 = et, r2 = Ze, (e3) => n2(r2(e3))));
    var n2, r2;
  }, tt = (e2) => {
    const t2 = e2.dom.body;
    if (null == t2) throw new Error("Body is not available yet");
    return pe.fromDom(t2);
  }, ot = (e2, t2) => {
    let o2 = [];
    return N(Ee(e2), (e3) => {
      t2(e3) && (o2 = o2.concat([e3])), o2 = o2.concat(ot(e3, t2));
    }), o2;
  }, nt = (e2, t2, o2) => ((e3, o3, n2) => z(De(e3, n2), (e4) => we(e4, t2)))(e2, 0, o2), rt = (e2, t2) => ((e3, o2) => z(Ee(e3), (e4) => we(e4, t2)))(e2), st = (e2, t2) => ((e3, t3) => {
    const o2 = void 0 === t3 ? document : t3.dom;
    return be(o2) ? [] : E(o2.querySelectorAll(e3), pe.fromDom);
  })(t2, e2);
  var lt = (e2, t2, o2, n2, r2) => e2(o2, n2) ? C.some(o2) : d(r2) && r2(o2) ? C.none() : t2(o2, n2, r2);
  const at = (e2, t2, o2) => {
    let n2 = e2.dom;
    const r2 = d(o2) ? o2 : y;
    for (; n2.parentNode; ) {
      n2 = n2.parentNode;
      const e3 = pe.fromDom(n2);
      if (t2(e3)) return C.some(e3);
      if (r2(e3)) break;
    }
    return C.none();
  }, ct = (e2, t2, o2) => at(e2, (e3) => we(e3, t2), o2), it = (e2, t2) => ((e3, o2) => L(e3.dom.childNodes, (e4) => {
    return o3 = pe.fromDom(e4), we(o3, t2);
    var o3;
  }).map(pe.fromDom))(e2), mt = (e2, t2) => ve(t2, e2), dt = (e2, t2, o2) => lt((e3, t3) => we(e3, t3), ct, e2, t2, o2), ut = (e2, t2, o2 = p) => e2.exists((e3) => o2(e3, t2)), ft = (e2) => {
    const t2 = [], o2 = (e3) => {
      t2.push(e3);
    };
    for (let t3 = 0; t3 < e2.length; t3++) e2[t3].each(o2);
    return t2;
  }, gt = (e2, t2) => e2 ? C.some(t2) : C.none(), ht = (e2, t2, o2) => "" === t2 || e2.length >= t2.length && e2.substr(o2, o2 + t2.length) === t2, pt = (e2, t2, o2 = 0, n2) => {
    const r2 = e2.indexOf(t2, o2);
    return -1 !== r2 && (!!i2(n2) || r2 + t2.length <= n2);
  }, wt = (e2, t2) => ht(e2, t2, 0), bt = (e2, t2) => ht(e2, t2, e2.length - t2.length), vt = /* @__PURE__ */ ((e2) => (t2) => t2.replace(e2, ""))(/^\s+|\s+$/g), yt = (e2) => e2.length > 0, xt = (e2) => void 0 !== e2.style && d(e2.style.getPropertyValue), Ct = (e2, t2, o2) => {
    if (!r(o2)) throw console.error("Invalid call to CSS.set. Property ", t2, ":: Value ", o2, ":: Element ", e2), new Error("CSS value must be a string: " + o2);
    xt(e2) && e2.style.setProperty(t2, o2);
  }, St = (e2, t2, o2) => {
    const n2 = e2.dom;
    Ct(n2, t2, o2);
  }, Tt = (e2, t2) => {
    const o2 = e2.dom;
    G(t2, (e3, t3) => {
      Ct(o2, t3, e3);
    });
  }, Rt = (e2, t2) => {
    const o2 = e2.dom, n2 = window.getComputedStyle(o2).getPropertyValue(t2);
    return "" !== n2 || et(e2) ? n2 : Dt(o2, t2);
  }, Dt = (e2, t2) => xt(e2) ? e2.style.getPropertyValue(t2) : "", Ot = (e2, t2) => {
    const o2 = e2.dom, n2 = Dt(o2, t2);
    return C.from(n2).filter((e3) => e3.length > 0);
  }, kt = (e2, t2) => {
    ((e3, t3) => {
      xt(e3) && e3.style.removeProperty(t3);
    })(e2.dom, t2), ut(ue(e2, "style").map(vt), "") && fe(e2, "style");
  }, Et = (e2, t2, o2 = 0) => ue(e2, t2).map((e3) => parseInt(e3, 10)).getOr(o2), Nt = (e2, t2) => Et(e2, t2, 1), Bt = (e2) => ae("col")(e2) ? Et(e2, "span", 1) > 1 : Nt(e2, "colspan") > 1, zt = (e2) => Nt(e2, "rowspan") > 1, At = (e2, t2) => parseInt(Rt(e2, t2), 10), Wt = g(10), Lt = g(10), _t = (e2, t2) => Mt(e2, t2, x), Mt = (e2, t2, o2) => j(Ee(e2), (e3) => we(e3, t2) ? o2(e3) ? [e3] : [] : Mt(e3, t2, o2)), jt = (e2, t2) => ((e3, t3, o2 = y) => o2(t3) ? C.none() : D(e3, Z(t3)) ? C.some(t3) : ct(t3, e3.join(","), (e4) => we(e4, "table") || o2(e4)))(["td", "th"], e2, t2), It = (e2) => _t(e2, "th,td"), Pt = (e2) => we(e2, "colgroup") ? rt(e2, "col") : j(qt(e2), (e3) => rt(e3, "col")), Ft = (e2, t2) => dt(e2, "table", t2), Ht = (e2) => _t(e2, "tr"), qt = (e2) => Ft(e2).fold(g([]), (e3) => rt(e3, "colgroup")), Vt = (e2, t2) => E(e2, (e3) => {
    if ("colgroup" === Z(e3)) {
      const t3 = E(Pt(e3), (e4) => {
        const t4 = Et(e4, "span", 1);
        return Ue(e4, 1, t4);
      });
      return Ge(e3, t3, "colgroup");
    }
    {
      const o2 = E(It(e3), (e4) => {
        const t3 = Et(e4, "rowspan", 1), o3 = Et(e4, "colspan", 1);
        return Ue(e4, t3, o3);
      });
      return Ge(e3, o2, t2(e3));
    }
  }), $t = (e2) => Re(e2).map((e3) => {
    const t2 = Z(e3);
    return ((e4) => D($e, e4))(t2) ? t2 : "tbody";
  }).getOr("tbody"), Ut = (e2) => {
    const t2 = Ht(e2), o2 = [...qt(e2), ...t2];
    return Vt(o2, $t);
  }, Gt = (e2) => {
    let t2, o2 = false;
    return (...n2) => (o2 || (o2 = true, t2 = e2.apply(null, n2)), t2);
  }, Kt = () => Yt(0, 0), Yt = (e2, t2) => ({ major: e2, minor: t2 }), Jt = { nu: Yt, detect: (e2, t2) => {
    const o2 = String(t2).toLowerCase();
    return 0 === e2.length ? Kt() : ((e3, t3) => {
      const o3 = ((e4, t4) => {
        for (let o4 = 0; o4 < e4.length; o4++) {
          const n3 = e4[o4];
          if (n3.test(t4)) return n3;
        }
      })(e3, t3);
      if (!o3) return { major: 0, minor: 0 };
      const n2 = (e4) => Number(t3.replace(o3, "$" + e4));
      return Yt(n2(1), n2(2));
    })(e2, o2);
  }, unknown: Kt }, Qt = (e2, t2) => {
    const o2 = String(t2).toLowerCase();
    return L(e2, (e3) => e3.search(o2));
  }, Xt = /.*?version\/\ ?([0-9]+)\.([0-9]+).*/, Zt = (e2) => (t2) => pt(t2, e2), eo = [{ name: "Edge", versionRegexes: [/.*?edge\/ ?([0-9]+)\.([0-9]+)$/], search: (e2) => pt(e2, "edge/") && pt(e2, "chrome") && pt(e2, "safari") && pt(e2, "applewebkit") }, { name: "Chromium", brand: "Chromium", versionRegexes: [/.*?chrome\/([0-9]+)\.([0-9]+).*/, Xt], search: (e2) => pt(e2, "chrome") && !pt(e2, "chromeframe") }, { name: "IE", versionRegexes: [/.*?msie\ ?([0-9]+)\.([0-9]+).*/, /.*?rv:([0-9]+)\.([0-9]+).*/], search: (e2) => pt(e2, "msie") || pt(e2, "trident") }, { name: "Opera", versionRegexes: [Xt, /.*?opera\/([0-9]+)\.([0-9]+).*/], search: Zt("opera") }, { name: "Firefox", versionRegexes: [/.*?firefox\/\ ?([0-9]+)\.([0-9]+).*/], search: Zt("firefox") }, { name: "Safari", versionRegexes: [Xt, /.*?cpu os ([0-9]+)_([0-9]+).*/], search: (e2) => (pt(e2, "safari") || pt(e2, "mobile/")) && pt(e2, "applewebkit") }], to = [{ name: "Windows", search: Zt("win"), versionRegexes: [/.*?windows\ nt\ ?([0-9]+)\.([0-9]+).*/] }, { name: "iOS", search: (e2) => pt(e2, "iphone") || pt(e2, "ipad"), versionRegexes: [/.*?version\/\ ?([0-9]+)\.([0-9]+).*/, /.*cpu os ([0-9]+)_([0-9]+).*/, /.*cpu iphone os ([0-9]+)_([0-9]+).*/] }, { name: "Android", search: Zt("android"), versionRegexes: [/.*?android\ ?([0-9]+)\.([0-9]+).*/] }, { name: "macOS", search: Zt("mac os x"), versionRegexes: [/.*?mac\ os\ x\ ?([0-9]+)_([0-9]+).*/] }, { name: "Linux", search: Zt("linux"), versionRegexes: [] }, { name: "Solaris", search: Zt("sunos"), versionRegexes: [] }, { name: "FreeBSD", search: Zt("freebsd"), versionRegexes: [] }, { name: "ChromeOS", search: Zt("cros"), versionRegexes: [/.*?chrome\/([0-9]+)\.([0-9]+).*/] }], oo = { browsers: g(eo), oses: g(to) }, no = "Edge", ro = "Chromium", so = "Opera", lo = "Firefox", ao = "Safari", co = (e2) => {
    const t2 = e2.current, o2 = e2.version, n2 = (e3) => () => t2 === e3;
    return { current: t2, version: o2, isEdge: n2(no), isChromium: n2(ro), isIE: n2("IE"), isOpera: n2(so), isFirefox: n2(lo), isSafari: n2(ao) };
  }, io = () => co({ current: void 0, version: Jt.unknown() }), mo = co, uo = (g(no), g(ro), g("IE"), g(so), g(lo), g(ao), "Windows"), fo = "Android", go = "Linux", ho = "macOS", po = "Solaris", wo = "FreeBSD", bo = "ChromeOS", vo = (e2) => {
    const t2 = e2.current, o2 = e2.version, n2 = (e3) => () => t2 === e3;
    return { current: t2, version: o2, isWindows: n2(uo), isiOS: n2("iOS"), isAndroid: n2(fo), isMacOS: n2(ho), isLinux: n2(go), isSolaris: n2(po), isFreeBSD: n2(wo), isChromeOS: n2(bo) };
  }, yo = () => vo({ current: void 0, version: Jt.unknown() }), xo = vo, Co = (g(uo), g("iOS"), g(fo), g(go), g(ho), g(po), g(wo), g(bo), (e2) => window.matchMedia(e2).matches);
  let So = Gt(() => ((e2, t2, o2) => {
    const n2 = oo.browsers(), r2 = oo.oses(), s2 = t2.bind((e3) => ((e4, t3) => V(t3.brands, (t4) => {
      const o3 = t4.brand.toLowerCase();
      return L(e4, (e5) => {
        var t5;
        return o3 === (null === (t5 = e5.brand) || void 0 === t5 ? void 0 : t5.toLowerCase());
      }).map((e5) => ({ current: e5.name, version: Jt.nu(parseInt(t4.version, 10), 0) }));
    }))(n2, e3)).orThunk(() => ((e3, t3) => Qt(e3, t3).map((e4) => {
      const o3 = Jt.detect(e4.versionRegexes, t3);
      return { current: e4.name, version: o3 };
    }))(n2, e2)).fold(io, mo), l2 = ((e3, t3) => Qt(e3, t3).map((e4) => {
      const o3 = Jt.detect(e4.versionRegexes, t3);
      return { current: e4.name, version: o3 };
    }))(r2, e2).fold(yo, xo), a2 = ((e3, t3, o3, n3) => {
      const r3 = e3.isiOS() && true === /ipad/i.test(o3), s3 = e3.isiOS() && !r3, l3 = e3.isiOS() || e3.isAndroid(), a3 = l3 || n3("(pointer:coarse)"), c2 = r3 || !s3 && l3 && n3("(min-device-width:768px)"), i3 = s3 || l3 && !c2, m2 = t3.isSafari() && e3.isiOS() && false === /safari/i.test(o3), d2 = !i3 && !c2 && !m2;
      return { isiPad: g(r3), isiPhone: g(s3), isTablet: g(c2), isPhone: g(i3), isTouch: g(a3), isAndroid: e3.isAndroid, isiOS: e3.isiOS, isWebView: g(m2), isDesktop: g(d2) };
    })(l2, s2, e2, o2);
    return { browser: s2, os: l2, deviceType: a2 };
  })(navigator.userAgent, C.from(navigator.userAgentData), Co));
  const To = () => So(), Ro = (e2, t2) => {
    const o2 = (o3) => {
      const n3 = t2(o3);
      if (n3 <= 0 || null === n3) {
        const t3 = Rt(o3, e2);
        return parseFloat(t3) || 0;
      }
      return n3;
    }, n2 = (e3, t3) => W(t3, (t4, o3) => {
      const n3 = Rt(e3, o3), r2 = void 0 === n3 ? 0 : parseInt(n3, 10);
      return isNaN(r2) ? t4 : t4 + r2;
    }, 0);
    return { set: (t3, o3) => {
      if (!u(o3) && !o3.match(/^[0-9]+$/)) throw new Error(e2 + ".set accepts only positive integer values. Value was " + o3);
      const n3 = t3.dom;
      xt(n3) && (n3.style[e2] = o3 + "px");
    }, get: o2, getOuter: o2, aggregate: n2, max: (e3, t3, o3) => {
      const r2 = n2(e3, o3);
      return t3 > r2 ? t3 - r2 : 0;
    } };
  }, Do = (e2, t2, o2) => ((e3, t3) => ((e4) => {
    const t4 = parseFloat(e4);
    return isNaN(t4) ? C.none() : C.some(t4);
  })(e3).getOr(t3))(Rt(e2, t2), o2), Oo = Ro("width", (e2) => e2.dom.offsetWidth), ko = (e2) => Oo.get(e2), Eo = (e2) => Oo.getOuter(e2), No = (e2) => ((e3, t2) => {
    const o2 = e3.dom, n2 = o2.getBoundingClientRect().width || o2.offsetWidth;
    return "border-box" === t2 ? n2 : ((e4, t3, o3, n3) => t3 - Do(e4, "padding-left", 0) - Do(e4, "padding-right", 0) - Do(e4, "border-left-width", 0) - Do(e4, "border-right-width", 0))(e3, n2);
  })(e2, "content-box"), Bo = (e2, t2, o2) => {
    const n2 = e2.cells, r2 = n2.slice(0, t2), s2 = n2.slice(t2), l2 = r2.concat(o2).concat(s2);
    return Wo(e2, l2);
  }, zo = (e2, t2, o2) => Bo(e2, t2, [o2]), Ao = (e2, t2, o2) => {
    e2.cells[t2] = o2;
  }, Wo = (e2, t2) => Ye(e2.element, t2, e2.section, e2.isNew), Lo = (e2, t2) => e2.cells[t2], _o = (e2, t2) => Lo(e2, t2).element, Mo = (e2) => e2.cells.length, jo = (e2) => {
    const t2 = B(e2, (e3) => "colgroup" === e3.section);
    return { rows: t2.fail, cols: t2.pass };
  }, Io = (e2, t2, o2) => {
    const n2 = E(e2.cells, o2);
    return Ye(t2(e2.element), n2, e2.section, true);
  }, Po = "data-snooker-locked-cols", Fo = (e2) => ue(e2, Po).bind((e3) => C.from(e3.match(/\d+/g))).map((e3) => P(e3, x)), Ho = (e2) => {
    const t2 = W(jo(e2).rows, (e3, t3) => (N(t3.cells, (t4, o3) => {
      t4.isLocked && (e3[o3] = true);
    }), e3), {}), o2 = J(t2, (e3, t3) => parseInt(t3, 10));
    return ((e3, t3) => {
      const o3 = S.call(e3, 0);
      return o3.sort(void 0), o3;
    })(o2);
  }, qo = (e2, t2) => e2 + "," + t2, Vo = (e2, t2) => {
    const o2 = j(e2.all, (e3) => e3.cells);
    return z(o2, t2);
  }, $o = (e2) => {
    const t2 = {}, o2 = [], n2 = H(e2).map((e3) => e3.element).bind(Ft).bind(Fo).getOr({});
    let r2 = 0, s2 = 0, l2 = 0;
    const { pass: a2, fail: c2 } = B(e2, (e3) => "colgroup" === e3.section);
    N(c2, (e3) => {
      const a3 = [];
      N(e3.cells, (e4) => {
        let o3 = 0;
        for (; void 0 !== t2[qo(l2, o3)]; ) o3++;
        const r3 = ((e5, t3) => X(e5, t3) && void 0 !== e5[t3] && null !== e5[t3])(n2, o3.toString()), c3 = /* @__PURE__ */ ((e5, t3, o4, n3, r4, s3) => ({ element: e5, rowspan: t3, colspan: o4, row: n3, column: r4, isLocked: s3 }))(e4.element, e4.rowspan, e4.colspan, l2, o3, r3);
        for (let n3 = 0; n3 < e4.colspan; n3++) for (let r4 = 0; r4 < e4.rowspan; r4++) {
          const e5 = o3 + n3, a4 = qo(l2 + r4, e5);
          t2[a4] = c3, s2 = Math.max(s2, e5 + 1);
        }
        a3.push(c3);
      }), r2++, o2.push(Ge(e3.element, a3, e3.section)), l2++;
    });
    const { columns: i3, colgroups: m2 } = q(a2).map((e3) => {
      const t3 = ((e4) => {
        const t4 = {};
        let o4 = 0;
        return N(e4.cells, (e5) => {
          const n3 = e5.colspan;
          k(n3, (r3) => {
            const s3 = o4 + r3;
            t4[s3] = /* @__PURE__ */ ((e6, t5, o5) => ({ element: e6, colspan: t5, column: o5 }))(e5.element, n3, s3);
          }), o4 += n3;
        }), t4;
      })(e3), o3 = /* @__PURE__ */ ((e4, t4) => ({ element: e4, columns: t4 }))(e3.element, Q(t3));
      return { colgroups: [o3], columns: t3 };
    }).getOrThunk(() => ({ colgroups: [], columns: {} })), d2 = /* @__PURE__ */ ((e3, t3) => ({ rows: e3, columns: t3 }))(r2, s2);
    return { grid: d2, access: t2, all: o2, columns: i3, colgroups: m2 };
  }, Uo = (e2) => {
    const t2 = Ut(e2);
    return $o(t2);
  }, Go = $o, Ko = (e2, t2, o2) => C.from(e2.access[qo(t2, o2)]), Yo = (e2, t2, o2) => {
    const n2 = Vo(e2, (e3) => o2(t2, e3.element));
    return n2.length > 0 ? C.some(n2[0]) : C.none();
  }, Jo = Vo, Qo = (e2) => j(e2.all, (e3) => e3.cells), Xo = (e2) => Q(e2.columns), Zo = (e2) => $2(e2.columns).length > 0, en = (e2, t2) => C.from(e2.columns[t2]), tn = (e2, t2 = x) => {
    const o2 = e2.grid, n2 = k(o2.columns, h), r2 = k(o2.rows, h);
    return E(n2, (o3) => on(() => j(r2, (t3) => Ko(e2, t3, o3).filter((e3) => e3.column === o3).toArray()), (e3) => 1 === e3.colspan && t2(e3.element), () => Ko(e2, 0, o3)));
  }, on = (e2, t2, o2) => {
    const n2 = e2();
    return L(n2, t2).orThunk(() => C.from(n2[0]).orThunk(o2)).map((e3) => e3.element);
  }, nn = (e2) => {
    const t2 = e2.grid, o2 = k(t2.rows, h), n2 = k(t2.columns, h);
    return E(o2, (t3) => on(() => j(n2, (o3) => Ko(e2, t3, o3).filter((e3) => e3.row === t3).fold(g([]), (e3) => [e3])), (e3) => 1 === e3.rowspan, () => Ko(e2, t3, 0)));
  }, rn = (e2, t2) => (o2) => "rtl" === sn(o2) ? t2 : e2, sn = (e2) => "rtl" === Rt(e2, "direction") ? "rtl" : "ltr", ln = Ro("height", (e2) => {
    const t2 = e2.dom;
    return et(e2) ? t2.getBoundingClientRect().height : t2.offsetHeight;
  }), an = (e2) => ln.get(e2), cn = (e2) => ln.getOuter(e2), mn = (e2, t2) => ({ left: e2, top: t2, translate: (o2, n2) => mn(e2 + o2, t2 + n2) }), dn = mn, un = (e2, t2) => void 0 !== e2 ? e2 : void 0 !== t2 ? t2 : 0, fn = (e2) => {
    const t2 = e2.dom.ownerDocument, o2 = t2.body, n2 = t2.defaultView, r2 = t2.documentElement;
    if (o2 === e2.dom) return dn(o2.offsetLeft, o2.offsetTop);
    const s2 = un(null == n2 ? void 0 : n2.pageYOffset, r2.scrollTop), l2 = un(null == n2 ? void 0 : n2.pageXOffset, r2.scrollLeft), a2 = un(r2.clientTop, o2.clientTop), c2 = un(r2.clientLeft, o2.clientLeft);
    return gn(e2).translate(l2 - c2, s2 - a2);
  }, gn = (e2) => {
    const t2 = e2.dom, o2 = t2.ownerDocument.body;
    return o2 === t2 ? dn(o2.offsetLeft, o2.offsetTop) : et(e2) ? ((e3) => {
      const t3 = e3.getBoundingClientRect();
      return dn(t3.left, t3.top);
    })(t2) : dn(0, 0);
  }, hn = (e2, t2) => ({ row: e2, y: t2 }), pn = (e2, t2) => ({ col: e2, x: t2 }), wn = (e2) => fn(e2).left + Eo(e2), bn = (e2) => fn(e2).left, vn = (e2, t2) => pn(e2, bn(t2)), yn = (e2, t2) => pn(e2, wn(t2)), xn = (e2) => fn(e2).top, Cn = (e2, t2) => hn(e2, xn(t2)), Sn = (e2, t2) => hn(e2, xn(t2) + cn(t2)), Tn = (e2, t2, o2) => {
    if (0 === o2.length) return [];
    const n2 = E(o2.slice(1), (t3, o3) => t3.map((t4) => e2(o3, t4))), r2 = o2[o2.length - 1].map((e3) => t2(o2.length - 1, e3));
    return n2.concat([r2]);
  }, Rn = { delta: h, positions: (e2) => Tn(Cn, Sn, e2), edge: xn }, Dn = rn({ delta: h, edge: bn, positions: (e2) => Tn(vn, yn, e2) }, { delta: (e2) => -e2, edge: wn, positions: (e2) => Tn(yn, vn, e2) }), On = { delta: (e2, t2) => Dn(t2).delta(e2, t2), positions: (e2, t2) => Dn(t2).positions(e2, t2), edge: (e2) => Dn(e2).edge(e2) }, kn = { unsupportedLength: ["em", "ex", "cap", "ch", "ic", "rem", "lh", "rlh", "vw", "vh", "vi", "vb", "vmin", "vmax", "cm", "mm", "Q", "in", "pc", "pt", "px"], fixed: ["px", "pt"], relative: ["%"], empty: [""] }, En = (() => {
    const e2 = "[0-9]+", t2 = "[eE][+-]?[0-9]+", o2 = (e3) => `(?:${e3})?`, n2 = ["Infinity", "[0-9]+\\." + o2(e2) + o2(t2), "\\.[0-9]+" + o2(t2), e2 + o2(t2)].join("|");
    return new RegExp(`^([+-]?(?:${n2}))(.*)$`);
  })(), Nn = /(\d+(\.\d+)?)%/, Bn = /(\d+(\.\d+)?)px|em/, zn = ae("col"), An = (e2, t2, o2) => {
    const n2 = (r2 = e2, C.from(r2.dom.parentElement).map(pe.fromDom)).getOrThunk(() => tt(Se(e2)));
    var r2;
    return t2(e2) / o2(n2) * 100;
  }, Wn = (e2, t2) => {
    St(e2, "width", t2 + "px");
  }, Ln = (e2, t2) => {
    St(e2, "width", t2 + "%");
  }, _n = (e2, t2) => {
    St(e2, "height", t2 + "px");
  }, Mn = (e2) => {
    const t2 = ((e3) => {
      return Do(t3 = e3, "height", t3.dom.offsetHeight) + "px";
      var t3;
    })(e2);
    return t2 ? ((e3, t3, o2, n2) => {
      const r2 = parseFloat(e3);
      return bt(e3, "%") && "table" !== Z(t3) ? ((e4, t4, o3, n3) => {
        const r3 = Ft(e4).map((e5) => {
          const n4 = o3(e5);
          return Math.floor(t4 / 100 * n4);
        }).getOr(t4);
        return n3(e4, r3), r3;
      })(t3, r2, o2, n2) : r2;
    })(t2, e2, an, _n) : an(e2);
  }, jn = (e2, t2) => Ot(e2, t2).orThunk(() => ue(e2, t2).map((e3) => e3 + "px")), In = (e2) => jn(e2, "width"), Pn = (e2) => An(e2, ko, No), Fn = (e2) => {
    return zn(e2) ? ko(e2) : Do(t2 = e2, "width", t2.dom.offsetWidth);
    var t2;
  }, Hn = (e2) => ((e3, t2, o2) => o2(e3) / Nt(e3, "rowspan"))(e2, 0, Mn), qn = (e2, t2, o2) => {
    St(e2, "width", t2 + o2);
  }, Vn = (e2) => An(e2, ko, No) + "%", $n = g(Nn), Un = ae("col"), Gn = (e2) => In(e2).getOrThunk(() => Fn(e2) + "px"), Kn = (e2) => {
    return (t2 = e2, jn(t2, "height")).getOrThunk(() => Hn(e2) + "px");
    var t2;
  }, Yn = (e2, t2, o2, n2, r2, s2) => e2.filter(n2).fold(() => s2(((e3, t3) => {
    if (t3 < 0 || t3 >= e3.length - 1) return C.none();
    const o3 = e3[t3].fold(() => {
      const o4 = ((e4) => {
        const t4 = S.call(e4, 0);
        return t4.reverse(), t4;
      })(e3.slice(0, t3));
      return V(o4, (e4, t4) => e4.map((e5) => ({ value: e5, delta: t4 + 1 })));
    }, (e4) => C.some({ value: e4, delta: 0 })), n3 = e3[t3 + 1].fold(() => {
      const o4 = e3.slice(t3 + 1);
      return V(o4, (e4, t4) => e4.map((e5) => ({ value: e5, delta: t4 + 1 })));
    }, (e4) => C.some({ value: e4, delta: 1 }));
    return o3.bind((e4) => n3.map((t4) => {
      const o4 = t4.delta + e4.delta;
      return Math.abs(t4.value - e4.value) / o4;
    }));
  })(o2, t2)), (e3) => r2(e3)), Jn = (e2, t2, o2, n2) => {
    const r2 = tn(e2), s2 = Zo(e2) ? ((e3) => E(Xo(e3), (e4) => C.from(e4.element)))(e2) : r2, l2 = [C.some(On.edge(t2))].concat(E(On.positions(r2, t2), (e3) => e3.map((e4) => e4.x))), a2 = b(Bt);
    return E(s2, (e3, t3) => Yn(e3, t3, l2, a2, (e4) => {
      if (((e5) => {
        const t4 = To().browser, o3 = t4.isChromium() || t4.isFirefox();
        return !Un(e5) || o3;
      })(e4)) return o2(e4);
      {
        const e5 = null != (s3 = r2[t3]) ? h(s3) : C.none();
        return Yn(e5, t3, l2, a2, (e6) => n2(C.some(ko(e6))), n2);
      }
      var s3;
    }, n2));
  }, Qn = (e2) => e2.map((e3) => e3 + "px").getOr(""), Xn = (e2, t2, o2) => Jn(e2, t2, Fn, (e3) => e3.getOrThunk(o2.minCellWidth)), Zn = (e2, t2, o2, n2, r2) => {
    const s2 = nn(e2), l2 = [C.some(o2.edge(t2))].concat(E(o2.positions(s2, t2), (e3) => e3.map((e4) => e4.y)));
    return E(s2, (e3, t3) => Yn(e3, t3, l2, b(zt), n2, r2));
  }, er = (e2, t2) => () => et(e2) ? t2(e2) : parseFloat(Ot(e2, "width").getOr("0")), tr = (e2) => {
    const t2 = er(e2, (e3) => parseFloat(Vn(e3))), o2 = er(e2, ko);
    return { width: t2, pixelWidth: o2, getWidths: (t3, o3) => ((e3, t4, o4) => Jn(e3, t4, Pn, (e4) => e4.fold(() => o4.minCellWidth(), (e5) => e5 / o4.pixelWidth() * 100)))(t3, e2, o3), getCellDelta: (e3) => e3 / o2() * 100, singleColumnWidth: (e3, t3) => [100 - e3], minCellWidth: () => Wt() / o2() * 100, setElementWidth: Ln, adjustTableWidth: (o3) => {
      const n2 = t2();
      Ln(e2, n2 + o3 / 100 * n2);
    }, isRelative: true, label: "percent" };
  }, or = (e2) => {
    const t2 = er(e2, ko);
    return { width: t2, pixelWidth: t2, getWidths: (t3, o2) => Xn(t3, e2, o2), getCellDelta: h, singleColumnWidth: (e3, t3) => [Math.max(Wt(), e3 + t3) - e3], minCellWidth: Wt, setElementWidth: Wn, adjustTableWidth: (o2) => {
      const n2 = t2() + o2;
      Wn(e2, n2);
    }, isRelative: false, label: "pixel" };
  }, nr = (e2) => In(e2).fold(() => ((e3) => {
    const t2 = er(e3, ko), o2 = g(0);
    return { width: t2, pixelWidth: t2, getWidths: (t3, o3) => Xn(t3, e3, o3), getCellDelta: o2, singleColumnWidth: g([0]), minCellWidth: o2, setElementWidth: f, adjustTableWidth: f, isRelative: true, label: "none" };
  })(e2), (t2) => ((e3, t3) => null !== $n().exec(t3) ? tr(e3) : or(e3))(e2, t2)), rr = or, sr = tr, lr = (e2, t2, o2) => {
    const n2 = e2[o2].element, r2 = pe.fromTag("td");
    We(r2, pe.fromTag("br")), (t2 ? We : Ae)(n2, r2);
  }, ar = /* @__PURE__ */ ((e2, t2) => {
    const o2 = (t3) => e2(t3) ? C.from(t3.dom.nodeValue) : C.none();
    return { get: (t3) => {
      if (!e2(t3)) throw new Error("Can only get text value of a text node");
      return o2(t3).getOr("");
    }, getOption: o2, set: (t3, o3) => {
      if (!e2(t3)) throw new Error("Can only set raw text value of a text node");
      t3.dom.nodeValue = o3;
    } };
  })(re), cr = (e2) => ar.get(e2), ir = (e2) => ar.getOption(e2), mr = (e2, t2) => ar.set(e2, t2), dr = (e2) => "img" === Z(e2) ? 1 : ir(e2).fold(() => Ee(e2).length, (e3) => e3.length), ur = ["img", "br"], fr = (e2) => ir(e2).filter((e3) => 0 !== e3.trim().length || e3.indexOf("\xA0") > -1).isSome() || D(ur, Z(e2)), gr = (e2) => ((e3, t2) => {
    const o2 = (e4) => {
      for (let n2 = 0; n2 < e4.childNodes.length; n2++) {
        const r2 = pe.fromDom(e4.childNodes[n2]);
        if (t2(r2)) return C.some(r2);
        const s2 = o2(e4.childNodes[n2]);
        if (s2.isSome()) return s2;
      }
      return C.none();
    };
    return o2(e3.dom);
  })(e2, fr), hr = (e2) => pr(e2, fr), pr = (e2, t2) => {
    const o2 = (e3) => {
      const n2 = Ee(e3);
      for (let e4 = n2.length - 1; e4 >= 0; e4--) {
        const r2 = n2[e4];
        if (t2(r2)) return C.some(r2);
        const s2 = o2(r2);
        if (s2.isSome()) return s2;
      }
      return C.none();
    };
    return o2(e2);
  }, wr = { scope: ["row", "col"] }, br = (e2) => () => {
    const t2 = pe.fromTag("td", e2.dom);
    return We(t2, pe.fromTag("br", e2.dom)), t2;
  }, vr = (e2) => () => pe.fromTag("col", e2.dom), yr = (e2) => () => pe.fromTag("colgroup", e2.dom), xr = (e2) => () => pe.fromTag("tr", e2.dom), Cr = (e2, t2, o2) => {
    const n2 = ((e3, t3) => {
      const o3 = Ve(e3, t3), n3 = Ee(qe(e3));
      return Me(o3, n3), o3;
    })(e2, t2);
    return G(o2, (e3, t3) => {
      null === e3 ? fe(n2, t3) : ie(n2, t3, e3);
    }), n2;
  }, Sr = (e2) => e2, Tr = (e2, t2, o2) => {
    const n2 = (e3, t3) => {
      ((e4, t4) => {
        const o3 = e4.dom, n3 = t4.dom;
        xt(o3) && xt(n3) && (n3.style.cssText = o3.style.cssText);
      })(e3.element, t3), kt(t3, "height"), 1 !== e3.colspan && kt(t3, "width");
    };
    return { col: (o3) => {
      const r2 = pe.fromTag(Z(o3.element), t2.dom);
      return n2(o3, r2), e2(o3.element, r2), r2;
    }, colgroup: yr(t2), row: xr(t2), cell: (r2) => {
      const s2 = pe.fromTag(Z(r2.element), t2.dom), l2 = o2.getOr(["strong", "em", "b", "i", "span", "font", "h1", "h2", "h3", "h4", "h5", "h6", "p", "div"]), a2 = l2.length > 0 ? ((e3, t3, o3) => gr(e3).map((n3) => {
        const r3 = o3.join(","), s3 = nt(n3, r3, (t4) => ye(t4, e3));
        return A(s3, (e4, t4) => {
          const o4 = He(t4);
          return fe(o4, "contenteditable"), We(e4, o4), o4;
        }, t3);
      }).getOr(t3))(r2.element, s2, l2) : s2;
      return We(a2, pe.fromTag("br")), n2(r2, s2), ((e3, t3) => {
        G(wr, (o3, n3) => ue(e3, n3).filter((e4) => D(o3, e4)).each((e4) => ie(t3, n3, e4)));
      })(r2.element, s2), e2(r2.element, s2), s2;
    }, replace: Cr, colGap: vr(t2), gap: br(t2) };
  }, Rr = (e2) => ({ col: vr(e2), colgroup: yr(e2), row: xr(e2), cell: br(e2), replace: Sr, colGap: vr(e2), gap: br(e2) }), Dr = (e2) => pe.fromDom(e2.getBody()), Or = (e2) => (t2) => ye(t2, Dr(e2)), kr = (e2) => {
    fe(e2, "data-mce-style");
    const t2 = (e3) => fe(e3, "data-mce-style");
    N(It(e2), t2), N(Pt(e2), t2), N(Ht(e2), t2);
  }, Er = (e2) => pe.fromDom(e2.selection.getStart()), Nr = (e2) => e2.getBoundingClientRect().width, Br = (e2) => e2.getBoundingClientRect().height, zr = (e2, t2) => {
    const o2 = t2.column, n2 = t2.column + t2.colspan - 1, r2 = t2.row, s2 = t2.row + t2.rowspan - 1;
    return o2 <= e2.finishCol && n2 >= e2.startCol && r2 <= e2.finishRow && s2 >= e2.startRow;
  }, Ar = (e2, t2) => t2.column >= e2.startCol && t2.column + t2.colspan - 1 <= e2.finishCol && t2.row >= e2.startRow && t2.row + t2.rowspan - 1 <= e2.finishRow, Wr = (e2, t2, o2) => {
    const n2 = Yo(e2, t2, ye), r2 = Yo(e2, o2, ye);
    return n2.bind((e3) => r2.map((t3) => {
      return o3 = e3, n3 = t3, { startRow: Math.min(o3.row, n3.row), startCol: Math.min(o3.column, n3.column), finishRow: Math.max(o3.row + o3.rowspan - 1, n3.row + n3.rowspan - 1), finishCol: Math.max(o3.column + o3.colspan - 1, n3.column + n3.colspan - 1) };
      var o3, n3;
    }));
  }, Lr = (e2, t2, o2) => Wr(e2, t2, o2).map((t3) => {
    const o3 = Jo(e2, w(zr, t3));
    return E(o3, (e3) => e3.element);
  }), _r = (e2, t2) => Yo(e2, t2, (e3, t3) => xe(t3, e3)).map((e3) => e3.element), Mr = (e2, t2, o2) => {
    const n2 = Ir(e2);
    return Lr(n2, t2, o2);
  }, jr = (e2, t2, o2, n2, r2) => {
    const s2 = Ir(e2), l2 = ye(e2, o2) ? C.some(t2) : _r(s2, t2), a2 = ye(e2, r2) ? C.some(n2) : _r(s2, n2);
    return l2.bind((e3) => a2.bind((t3) => Lr(s2, e3, t3)));
  }, Ir = Uo;
  var Pr = ["body", "p", "div", "article", "aside", "figcaption", "figure", "footer", "header", "nav", "section", "ol", "ul", "li", "table", "thead", "tbody", "tfoot", "caption", "tr", "td", "th", "h1", "h2", "h3", "h4", "h5", "h6", "blockquote", "pre", "address"], Fr = () => ({ up: g({ selector: ct, closest: dt, predicate: at, all: De }), down: g({ selector: st, predicate: ot }), styles: g({ get: Rt, getRaw: Ot, set: St, remove: kt }), attrs: g({ get: de, set: ie, remove: fe, copyTo: (e2, t2) => {
    const o2 = ge(e2);
    me(t2, o2);
  } }), insert: g({ before: Be, after: ze, afterAll: _e, append: We, appendAll: Me, prepend: Ae, wrap: Le }), remove: g({ unwrap: Pe, remove: Ie }), create: g({ nu: pe.fromTag, clone: (e2) => pe.fromDom(e2.dom.cloneNode(false)), text: pe.fromText }), query: g({ comparePosition: (e2, t2) => e2.dom.compareDocumentPosition(t2.dom), prevSibling: Oe, nextSibling: ke }), property: g({ children: Ee, name: Z, parent: Re, document: (e2) => Te(e2).dom, isText: re, isComment: oe, isElement: ne, isSpecial: (e2) => {
    const t2 = Z(e2);
    return D(["script", "noscript", "iframe", "noframes", "noembed", "title", "style", "textarea", "xmp"], t2);
  }, getLanguage: (e2) => ne(e2) ? ue(e2, "lang") : C.none(), getText: cr, setText: mr, isBoundary: (e2) => !!ne(e2) && ("body" === Z(e2) || D(Pr, Z(e2))), isEmptyTag: (e2) => !!ne(e2) && D(["br", "img", "hr", "input"], Z(e2)), isNonEditable: (e2) => ne(e2) && "false" === de(e2, "contenteditable") }), eq: ye, is: Ce });
  const Hr = (e2, t2, o2, n2) => {
    const r2 = t2(e2, o2);
    return A(n2, (o3, n3) => {
      const r3 = t2(e2, n3);
      return qr(e2, o3, r3);
    }, r2);
  }, qr = (e2, t2, o2) => t2.bind((t3) => o2.filter(w(e2.eq, t3))), Vr = Fr(), $r = (e2, t2) => ((e3, t3, o2) => o2.length > 0 ? ((e4, t4, o3, n2) => n2(e4, t4, o3[0], o3.slice(1)))(e3, t3, o2, Hr) : C.none())(Vr, (t3, o2) => e2(o2), t2), Ur = (e2) => ct(e2, "table"), Gr = (e2, t2, o2) => {
    const n2 = (e3) => (t3) => void 0 !== o2 && o2(t3) || ye(t3, e3);
    return ye(e2, t2) ? C.some({ boxes: C.some([e2]), start: e2, finish: t2 }) : Ur(e2).bind((r2) => Ur(t2).bind((s2) => {
      if (ye(r2, s2)) return C.some({ boxes: Mr(r2, e2, t2), start: e2, finish: t2 });
      if (xe(r2, s2)) {
        const o3 = nt(t2, "td,th", n2(r2)), l2 = o3.length > 0 ? o3[o3.length - 1] : t2;
        return C.some({ boxes: jr(r2, e2, r2, t2, s2), start: e2, finish: l2 });
      }
      if (xe(s2, r2)) {
        const o3 = nt(e2, "td,th", n2(s2)), l2 = o3.length > 0 ? o3[o3.length - 1] : e2;
        return C.some({ boxes: jr(s2, e2, r2, t2, s2), start: e2, finish: l2 });
      }
      return ((e3, t3, o3) => ((e4, t4, o4, n3 = y) => {
        const r3 = [t4].concat(e4.up().all(t4)), s3 = [o4].concat(e4.up().all(o4)), l2 = (e5) => _(e5, n3).fold(() => e5, (t5) => e5.slice(0, t5 + 1)), a2 = l2(r3), c2 = l2(s3), i3 = L(a2, (t5) => O(c2, ((e5, t6) => w(e5.eq, t6))(e4, t5)));
        return { firstpath: a2, secondpath: c2, shared: i3 };
      })(Vr, e3, t3, void 0))(e2, t2).shared.bind((l2) => dt(l2, "table", o2).bind((o3) => {
        const l3 = nt(t2, "td,th", n2(o3)), a2 = l3.length > 0 ? l3[l3.length - 1] : t2, c2 = nt(e2, "td,th", n2(o3)), i3 = c2.length > 0 ? c2[c2.length - 1] : e2;
        return C.some({ boxes: jr(o3, e2, r2, t2, s2), start: i3, finish: a2 });
      }));
    }));
  }, Kr = (e2, t2) => {
    const o2 = st(e2, t2);
    return o2.length > 0 ? C.some(o2) : C.none();
  }, Yr = (e2, t2, o2) => mt(e2, t2).bind((t3) => mt(e2, o2).bind((e3) => $r(Ur, [t3, e3]).map((o3) => ({ first: t3, last: e3, table: o3 })))), Jr = (e2, t2, o2, n2, r2) => ((e3, t3) => L(e3, (e4) => we(e4, t3)))(e2, r2).bind((e3) => ((e4, t3, o3) => Ft(e4).bind((n3) => ((e5, t4, o4, n4) => Yo(e5, t4, ye).bind((t5) => {
    const r3 = o4 > 0 ? t5.row + t5.rowspan - 1 : t5.row, s2 = n4 > 0 ? t5.column + t5.colspan - 1 : t5.column;
    return Ko(e5, r3 + o4, s2 + n4).map((e6) => e6.element);
  }))(Ir(n3), e4, t3, o3)))(e3, t2, o2).bind((e4) => ((e5, t3) => ct(e5, "table").bind((o3) => mt(o3, t3).bind((t4) => Gr(t4, e5).bind((e6) => e6.boxes.map((t5) => ({ boxes: t5, start: e6.start, finish: e6.finish }))))))(e4, n2))), Qr = (e2, t2) => Kr(e2, t2), Xr = (e2, t2, o2) => Yr(e2, t2, o2).bind((t3) => {
    const o3 = (t4) => ye(e2, t4), n2 = "thead,tfoot,tbody,table", r2 = ct(t3.first, n2, o3), s2 = ct(t3.last, n2, o3);
    return r2.bind((e3) => s2.bind((o4) => ye(e3, o4) ? ((e4, t4, o5) => ((e5, t5, o6) => Wr(e5, t5, o6).bind((t6) => ((e6, t7) => {
      let o7 = true;
      const n3 = w(Ar, t7);
      for (let r3 = t7.startRow; r3 <= t7.finishRow; r3++) for (let s3 = t7.startCol; s3 <= t7.finishCol; s3++) o7 = o7 && Ko(e6, r3, s3).exists(n3);
      return o7 ? C.some(t7) : C.none();
    })(e5, t6)))(Ir(e4), t4, o5))(t3.table, t3.first, t3.last) : C.none()));
  }), Zr = h, es = (e2) => {
    const t2 = (e3, t3) => ue(e3, t3).exists((e4) => parseInt(e4, 10) > 1);
    return e2.length > 0 && I(e2, (e3) => t2(e3, "rowspan") || t2(e3, "colspan")) ? C.some(e2) : C.none();
  }, ts = (e2, t2, o2) => t2.length <= 1 ? C.none() : Xr(e2, o2.firstSelectedSelector, o2.lastSelectedSelector).map((e3) => ({ bounds: e3, cells: t2 })), os = { selected: "data-mce-selected", selectedSelector: "td[data-mce-selected],th[data-mce-selected]", firstSelected: "data-mce-first-selected", firstSelectedSelector: "td[data-mce-first-selected],th[data-mce-first-selected]", lastSelected: "data-mce-last-selected", lastSelectedSelector: "td[data-mce-last-selected],th[data-mce-last-selected]" }, ns = (e2, t2, o2) => ({ element: o2, mergable: ts(t2, e2, os), unmergable: es(e2), selection: Zr(e2) }), rs = (e2) => (t2, o2) => {
    const n2 = Z(t2), r2 = "col" === n2 || "colgroup" === n2 ? Ft(s2 = t2).bind((e3) => Qr(e3, os.firstSelectedSelector)).fold(g(s2), (e3) => e3[0]) : t2;
    var s2;
    return dt(r2, e2, o2);
  }, ss = rs("th,td,caption"), ls = rs("th,td"), as = (e2) => {
    return t2 = e2.model.table.getSelectedCells(), E(t2, pe.fromDom);
    var t2;
  }, cs = (e2, t2) => {
    e2.on("BeforeGetContent", (t3) => {
      const o2 = (o3) => {
        t3.preventDefault(), ((e3) => Ft(e3[0]).map((e4) => {
          const t4 = ((e5, t5) => {
            const o4 = (e6) => we(e6.element, t5), n2 = qe(e5), r2 = Ut(n2), s2 = nr(e5), l2 = Go(r2), a2 = ((e6, t6) => {
              const o5 = e6.grid.columns;
              let n3 = e6.grid.rows, r3 = o5, s3 = 0, l3 = 0;
              const a3 = [], c3 = [];
              return G(e6.access, (e7) => {
                if (a3.push(e7), t6(e7)) {
                  c3.push(e7);
                  const t7 = e7.row, o6 = t7 + e7.rowspan - 1, a4 = e7.column, i4 = a4 + e7.colspan - 1;
                  t7 < n3 ? n3 = t7 : o6 > s3 && (s3 = o6), a4 < r3 ? r3 = a4 : i4 > l3 && (l3 = i4);
                }
              }), /* @__PURE__ */ ((e7, t7, o6, n4, r4, s4) => ({ minRow: e7, minCol: t7, maxRow: o6, maxCol: n4, allCells: r4, selectedCells: s4 }))(n3, r3, s3, l3, a3, c3);
            })(l2, o4), c2 = "th:not(" + t5 + "),td:not(" + t5 + ")", i3 = Mt(n2, "th,td", (e6) => we(e6, c2));
            N(i3, Ie), ((e6, t6, o5, n3) => {
              const r3 = z(e6, (e7) => "colgroup" !== e7.section), s3 = t6.grid.columns, l3 = t6.grid.rows;
              for (let e7 = 0; e7 < l3; e7++) {
                let l4 = false;
                for (let a3 = 0; a3 < s3; a3++) e7 < o5.minRow || e7 > o5.maxRow || a3 < o5.minCol || a3 > o5.maxCol || (Ko(t6, e7, a3).filter(n3).isNone() ? lr(r3, l4, e7) : l4 = true);
              }
            })(r2, l2, a2, o4);
            const m2 = ((e6, t6, o5, n3) => {
              if (0 === n3.minCol && t6.grid.columns === n3.maxCol + 1) return 0;
              const r3 = Xn(t6, e6, o5), s3 = W(r3, (e7, t7) => e7 + t7, 0), l3 = W(r3.slice(n3.minCol, n3.maxCol + 1), (e7, t7) => e7 + t7, 0), a3 = l3 / s3 * o5.pixelWidth() - o5.pixelWidth();
              return o5.getCellDelta(a3);
            })(e5, Uo(e5), s2, a2);
            return ((e6, t6, o5, n3) => {
              G(o5.columns, (e7) => {
                (e7.column < t6.minCol || e7.column > t6.maxCol) && Ie(e7.element);
              });
              const r3 = z(_t(e6, "tr"), (e7) => 0 === e7.dom.childElementCount);
              N(r3, Ie), t6.minCol !== t6.maxCol && t6.minRow !== t6.maxRow || N(_t(e6, "th,td"), (e7) => {
                fe(e7, "rowspan"), fe(e7, "colspan");
              }), fe(e6, Po), fe(e6, "data-snooker-col-series"), nr(e6).adjustTableWidth(n3);
            })(n2, a2, l2, m2), n2;
          })(e4, "[data-mce-selected]");
          return kr(t4), [t4];
        }))(o3).each((o4) => {
          t3.content = "text" === t3.format ? ((e3) => E(e3, (e4) => e4.dom.innerText).join(""))(o4) : ((e3, t4) => E(t4, (t5) => e3.selection.serializer.serialize(t5.dom, {})).join(""))(e2, o4);
        });
      };
      if (true === t3.selection) {
        const t4 = ((e3) => z(as(e3), (e4) => we(e4, os.selectedSelector)))(e2);
        t4.length >= 1 && o2(t4);
      }
    }), e2.on("BeforeSetContent", (o2) => {
      if (true === o2.selection && true === o2.paste) {
        const n2 = as(e2);
        H(n2).each((n3) => {
          Ft(n3).each((r2) => {
            const s2 = z(((e3, t3) => {
              const o3 = document.createElement("div");
              return o3.innerHTML = e3, Ee(pe.fromDom(o3));
            })(o2.content), (e3) => "meta" !== Z(e3)), l2 = ae("table");
            if (1 === s2.length && l2(s2[0])) {
              o2.preventDefault();
              const l3 = pe.fromDom(e2.getDoc()), a2 = Rr(l3), c2 = /* @__PURE__ */ ((e3, t3, o3) => ({ element: e3, clipboard: t3, generators: o3 }))(n3, s2[0], a2);
              t2.pasteCells(r2, c2).each(() => {
                e2.focus();
              });
            }
          });
        });
      }
    });
  }, is = (e2, t2) => ({ element: e2, offset: t2 }), ms = (e2, t2, o2) => e2.property().isText(t2) && 0 === e2.property().getText(t2).trim().length || e2.property().isComment(t2) ? o2(t2).bind((t3) => ms(e2, t3, o2).orThunk(() => C.some(t3))) : C.none(), ds = (e2, t2) => e2.property().isText(t2) ? e2.property().getText(t2).length : e2.property().children(t2).length, us = (e2, t2) => {
    const o2 = ms(e2, t2, e2.query().prevSibling).getOr(t2);
    if (e2.property().isText(o2)) return is(o2, ds(e2, o2));
    const n2 = e2.property().children(o2);
    return n2.length > 0 ? us(e2, n2[n2.length - 1]) : is(o2, ds(e2, o2));
  }, fs = us, gs = Fr(), hs = (e2, t2) => {
    if (!Bt(e2)) {
      const o2 = ((e3) => In(e3).bind((e4) => {
        return t3 = e4, o3 = ["fixed", "relative", "empty"], C.from(En.exec(t3)).bind((e5) => {
          const t4 = Number(e5[1]), n2 = e5[2];
          return ((e6, t5) => O(t5, (t6) => O(kn[t6], (t7) => e6 === t7)))(n2, o3) ? C.some({ value: t4, unit: n2 }) : C.none();
        });
        var t3, o3;
      }))(e2);
      o2.each((o3) => {
        const n2 = o3.value / 2;
        qn(e2, n2, o3.unit), qn(t2, n2, o3.unit);
      });
    }
  }, ps = (e2) => E(e2, g(0)), ws = (e2, t2, o2, n2, r2) => r2(e2.slice(0, t2)).concat(n2).concat(r2(e2.slice(o2))), bs = (e2) => (t2, o2, n2, r2) => {
    if (e2(n2)) {
      const e3 = Math.max(r2, t2[o2] - Math.abs(n2)), s2 = Math.abs(e3 - t2[o2]);
      return n2 >= 0 ? s2 : -s2;
    }
    return n2;
  }, vs = bs((e2) => e2 < 0), ys = bs(x), xs = () => {
    const e2 = (e3, t3, o2, n2) => {
      const r2 = (100 + o2) / 100, s2 = Math.max(n2, (e3[t3] + o2) / r2);
      return E(e3, (e4, o3) => (o3 === t3 ? s2 : e4 / r2) - e4);
    }, t2 = (t3, o2, n2, r2, s2, l2) => l2 ? e2(t3, o2, r2, s2) : ((e3, t4, o3, n3, r3) => {
      const s3 = vs(e3, t4, n3, r3);
      return ws(e3, t4, o3 + 1, [s3, 0], ps);
    })(t3, o2, n2, r2, s2);
    return { resizeTable: (e3, t3) => e3(t3), clampTableDelta: vs, calcLeftEdgeDeltas: t2, calcMiddleDeltas: (e3, o2, n2, r2, s2, l2, a2) => t2(e3, n2, r2, s2, l2, a2), calcRightEdgeDeltas: (t3, o2, n2, r2, s2, l2) => {
      if (l2) return e2(t3, n2, r2, s2);
      {
        const e3 = vs(t3, n2, r2, s2);
        return ps(t3.slice(0, n2)).concat([e3]);
      }
    }, calcRedestributedWidths: (e3, t3, o2, n2) => {
      if (n2) {
        const n3 = (t3 + o2) / t3, r2 = E(e3, (e4) => e4 / n3);
        return { delta: 100 * n3 - 100, newSizes: r2 };
      }
      return { delta: o2, newSizes: e3 };
    } };
  }, Cs = () => {
    const e2 = (e3, t2, o2, n2, r2) => {
      const s2 = ys(e3, n2 >= 0 ? o2 : t2, n2, r2);
      return ws(e3, t2, o2 + 1, [s2, -s2], ps);
    };
    return { resizeTable: (e3, t2, o2) => {
      o2 && e3(t2);
    }, clampTableDelta: (e3, t2, o2, n2, r2) => {
      if (r2) {
        if (o2 >= 0) return o2;
        {
          const t3 = W(e3, (e4, t4) => e4 + t4 - n2, 0);
          return Math.max(-t3, o2);
        }
      }
      return vs(e3, t2, o2, n2);
    }, calcLeftEdgeDeltas: e2, calcMiddleDeltas: (t2, o2, n2, r2, s2, l2) => e2(t2, n2, r2, s2, l2), calcRightEdgeDeltas: (e3, t2, o2, n2, r2, s2) => {
      if (s2) return ps(e3);
      {
        const t3 = n2 / e3.length;
        return E(e3, g(t3));
      }
    }, calcRedestributedWidths: (e3, t2, o2, n2) => ({ delta: 0, newSizes: e3 }) };
  }, Ss = (e2) => Uo(e2).grid, Ts = ae("th"), Rs = (e2) => I(e2, (e3) => Ts(e3.element)), Ds = (e2, t2) => e2 && t2 ? "sectionCells" : e2 ? "section" : "cells", Os = (e2) => {
    const t2 = "thead" === e2.section, o2 = ut(ks(e2.cells), "th");
    return "tfoot" === e2.section ? { type: "footer" } : t2 || o2 ? { type: "header", subType: Ds(t2, o2) } : { type: "body" };
  }, ks = (e2) => {
    const t2 = z(e2, (e3) => Ts(e3.element));
    return 0 === t2.length ? C.some("td") : t2.length === e2.length ? C.some("th") : C.none();
  }, Es = (e2, t2, o2) => Ke(o2(e2.element, t2), true, e2.isLocked), Ns = (e2, t2) => e2.section !== t2 ? Ye(e2.element, e2.cells, t2, e2.isNew) : e2, Bs = () => ({ transformRow: Ns, transformCell: (e2, t2, o2) => {
    const n2 = o2(e2.element, t2), r2 = "td" !== Z(n2) ? ((e3, t3) => {
      const o3 = Ve(e3, "td");
      ze(e3, o3);
      const n3 = Ee(e3);
      return Me(o3, n3), Ie(e3), o3;
    })(n2) : n2;
    return Ke(r2, e2.isNew, e2.isLocked);
  } }), zs = () => ({ transformRow: Ns, transformCell: Es }), As = () => ({ transformRow: (e2, t2) => Ns(e2, "thead" === t2 ? "tbody" : t2), transformCell: Es }), Ws = Bs, Ls = zs, _s = As, Ms = () => ({ transformRow: h, transformCell: Es }), js = (e2) => dt(e2, "[contenteditable]"), Is = (e2, t2 = false) => et(e2) ? e2.dom.isContentEditable : js(e2).fold(g(t2), (e3) => "true" === Ps(e3)), Ps = (e2) => e2.dom.contentEditable, Fs = (e2, t2, o2, n2) => {
    o2 === n2 ? fe(e2, t2) : ie(e2, t2, o2);
  }, Hs = (e2, t2, o2) => {
    q(rt(e2, t2)).fold(() => Ae(e2, o2), (e3) => ze(e3, o2));
  }, qs = (e2, t2) => {
    const o2 = [], n2 = [], r2 = (e3) => E(e3, (e4) => {
      e4.isNew && o2.push(e4.element);
      const t3 = e4.element;
      return je(t3), N(e4.cells, (e5) => {
        e5.isNew && n2.push(e5.element), Fs(e5.element, "colspan", e5.colspan, 1), Fs(e5.element, "rowspan", e5.rowspan, 1), We(t3, e5.element);
      }), t3;
    }), s2 = (e3) => j(e3, (e4) => E(e4.cells, (e5) => (Fs(e5.element, "span", e5.colspan, 1), e5.element))), l2 = (t3, o3) => {
      const n3 = ((e3, t4) => {
        const o4 = it(e3, t4).getOrThunk(() => {
          const o5 = pe.fromTag(t4, Se(e3).dom);
          return "thead" === t4 ? Hs(e3, "caption,colgroup", o5) : "colgroup" === t4 ? Hs(e3, "caption", o5) : We(e3, o5), o5;
        });
        return je(o4), o4;
      })(e2, o3), l3 = ("colgroup" === o3 ? s2 : r2)(t3);
      Me(n3, l3);
    }, a2 = (t3, o3) => {
      t3.length > 0 ? l2(t3, o3) : ((t4) => {
        it(e2, t4).each(Ie);
      })(o3);
    }, c2 = [], i3 = [], m2 = [], d2 = [];
    return N(t2, (e3) => {
      switch (e3.section) {
        case "thead":
          c2.push(e3);
          break;
        case "tbody":
          i3.push(e3);
          break;
        case "tfoot":
          m2.push(e3);
          break;
        case "colgroup":
          d2.push(e3);
      }
    }), a2(d2, "colgroup"), a2(c2, "thead"), a2(i3, "tbody"), a2(m2, "tfoot"), { newRows: o2, newCells: n2 };
  }, Vs = (e2, t2) => {
    if (0 === e2.length) return 0;
    const o2 = e2[0];
    return _(e2, (e3) => !t2(o2.element, e3.element)).getOr(e2.length);
  }, $s = (e2, t2) => {
    const o2 = E(e2, (e3) => E(e3.cells, y));
    return E(e2, (n2, r2) => {
      const s2 = j(n2.cells, (n3, s3) => {
        if (false === o2[r2][s3]) {
          const m2 = ((e3, t3, o3, n4) => {
            const r3 = ((e4, t4) => e4[t4])(e3, t3), s4 = "colgroup" === r3.section, l3 = Vs(r3.cells.slice(o3), n4), a3 = s4 ? 1 : Vs(((e4, t4) => E(e4, (e5) => Lo(e5, t4)))(e3.slice(t3), o3), n4);
            return { colspan: l3, rowspan: a3 };
          })(e2, r2, s3, t2);
          return ((e3, t3, n4, r3) => {
            for (let s4 = e3; s4 < e3 + n4; s4++) for (let e4 = t3; e4 < t3 + r3; e4++) o2[s4][e4] = true;
          })(r2, s3, m2.rowspan, m2.colspan), [(l2 = n3.element, a2 = m2.rowspan, c2 = m2.colspan, i3 = n3.isNew, { element: l2, rowspan: a2, colspan: c2, isNew: i3 })];
        }
        return [];
        var l2, a2, c2, i3;
      });
      return /* @__PURE__ */ ((e3, t3, o3, n3) => ({ element: e3, cells: t3, section: o3, isNew: n3 }))(n2.element, s2, n2.section, n2.isNew);
    });
  }, Us = (e2, t2, o2) => {
    const n2 = [];
    N(e2.colgroups, (r2) => {
      const s2 = [];
      for (let n3 = 0; n3 < e2.grid.columns; n3++) {
        const r3 = en(e2, n3).map((e3) => Ke(e3.element, o2, false)).getOrThunk(() => Ke(t2.colGap(), true, false));
        s2.push(r3);
      }
      n2.push(Ye(r2.element, s2, "colgroup", o2));
    });
    for (let r2 = 0; r2 < e2.grid.rows; r2++) {
      const s2 = [];
      for (let n3 = 0; n3 < e2.grid.columns; n3++) {
        const l3 = Ko(e2, r2, n3).map((e3) => Ke(e3.element, o2, e3.isLocked)).getOrThunk(() => Ke(t2.gap(), true, false));
        s2.push(l3);
      }
      const l2 = e2.all[r2], a2 = Ye(l2.element, s2, l2.section, o2);
      n2.push(a2);
    }
    return n2;
  }, Gs = (e2) => $s(e2, ye), Ks = (e2, t2) => V(e2.all, (e3) => L(e3.cells, (e4) => ye(t2, e4.element))), Ys = (e2, t2, o2) => {
    const n2 = E(t2.selection, (t3) => jt(t3).bind((t4) => Ks(e2, t4)).filter(o2)), r2 = ft(n2);
    return gt(r2.length > 0, r2);
  }, Js = (e2, t2, o2, n2, r2) => (s2, l2, a2, c2) => {
    const i3 = Uo(s2), m2 = C.from(null == c2 ? void 0 : c2.section).getOrThunk(Ms);
    return t2(i3, l2).map((t3) => {
      const o3 = ((e3, t4) => Us(e3, t4, false))(i3, a2), n3 = e2(o3, t3, ye, r2(a2), m2), s3 = Ho(n3.grid);
      return { info: t3, grid: Gs(n3.grid), cursor: n3.cursor, lockedColumns: s3 };
    }).bind((e3) => {
      const t3 = qs(s2, e3.grid), r3 = C.from(null == c2 ? void 0 : c2.sizing).getOrThunk(() => nr(s2)), l3 = C.from(null == c2 ? void 0 : c2.resize).getOrThunk(Cs);
      return o2(s2, e3.grid, e3.info, { sizing: r3, resize: l3, section: m2 }), n2(s2), fe(s2, Po), e3.lockedColumns.length > 0 && ie(s2, Po, e3.lockedColumns.join(",")), C.some({ cursor: e3.cursor, newRows: t3.newRows, newCells: t3.newCells });
    });
  }, Qs = (e2, t2) => Ys(e2, t2, x).map((e3) => ({ cells: e3, generators: t2.generators, clipboard: t2.clipboard })), Xs = (e2, t2) => Ys(e2, t2, x), Zs = (e2, t2) => Ys(e2, t2, (e3) => !e3.isLocked), el = (e2, t2) => I(t2, (t3) => ((e3, t4) => Ks(e3, t4).exists((e4) => !e4.isLocked))(e2, t3)), tl = (e2, t2, o2, n2) => {
    const r2 = jo(e2).rows;
    let s2 = true;
    for (let e3 = 0; e3 < r2.length; e3++) for (let l2 = 0; l2 < Mo(r2[0]); l2++) {
      const a2 = r2[e3], c2 = Lo(a2, l2), i3 = o2(c2.element, t2);
      i3 && !s2 ? Ao(a2, l2, Ke(n2(), true, c2.isLocked)) : i3 && (s2 = false);
    }
    return e2;
  }, ol = (e2) => {
    const t2 = (t3) => t3(e2), o2 = g(e2), n2 = () => r2, r2 = { tag: true, inner: e2, fold: (t3, o3) => o3(e2), isValue: x, isError: y, map: (t3) => rl.value(t3(e2)), mapError: n2, bind: t2, exists: t2, forall: t2, getOr: o2, or: n2, getOrThunk: o2, orThunk: n2, getOrDie: o2, each: (t3) => {
      t3(e2);
    }, toOptional: () => C.some(e2) };
    return r2;
  }, nl = (e2) => {
    const t2 = () => o2, o2 = { tag: false, inner: e2, fold: (t3, o3) => t3(e2), isValue: y, isError: x, map: t2, mapError: (t3) => rl.error(t3(e2)), bind: t2, exists: y, forall: x, getOr: h, or: h, getOrThunk: v, orThunk: v, getOrDie: (n2 = String(e2), () => {
      throw new Error(n2);
    }), each: f, toOptional: C.none };
    var n2;
    return o2;
  }, rl = { value: ol, error: nl, fromOption: (e2, t2) => e2.fold(() => nl(t2), ol) }, sl = (e2, t2) => ({ rowDelta: 0, colDelta: Mo(e2[0]) - Mo(t2[0]) }), ll = (e2, t2) => ({ rowDelta: e2.length - t2.length, colDelta: 0 }), al = (e2, t2, o2, n2) => {
    const r2 = "colgroup" === t2.section ? o2.col : o2.cell;
    return k(e2, (e3) => Ke(r2(), true, n2(e3)));
  }, cl = (e2, t2, o2, n2) => {
    const r2 = e2[e2.length - 1];
    return e2.concat(k(t2, () => {
      const e3 = "colgroup" === r2.section ? o2.colgroup : o2.row, t3 = Io(r2, e3, h), s2 = al(t3.cells.length, t3, o2, (e4) => X(n2, e4.toString()));
      return Wo(t3, s2);
    }));
  }, il = (e2, t2, o2, n2) => E(e2, (e3) => {
    const r2 = al(t2, e3, o2, y);
    return Bo(e3, n2, r2);
  }), ml = (e2, t2, o2) => {
    const n2 = t2.colDelta < 0 ? il : h, r2 = t2.rowDelta < 0 ? cl : h, s2 = Ho(e2), l2 = Mo(e2[0]), a2 = O(s2, (e3) => e3 === l2 - 1), c2 = n2(e2, Math.abs(t2.colDelta), o2, a2 ? l2 - 1 : l2), i3 = Ho(c2);
    return r2(c2, Math.abs(t2.rowDelta), o2, P(i3, x));
  }, dl = (e2, t2, o2, n2) => {
    const r2 = w(n2, Lo(e2[t2], o2).element), s2 = e2[t2];
    return e2.length > 1 && Mo(s2) > 1 && (o2 > 0 && r2(_o(s2, o2 - 1)) || o2 < s2.cells.length - 1 && r2(_o(s2, o2 + 1)) || t2 > 0 && r2(_o(e2[t2 - 1], o2)) || t2 < e2.length - 1 && r2(_o(e2[t2 + 1], o2)));
  }, ul = (e2, t2, o2) => z(o2, (o3) => o3 >= e2.column && o3 <= Mo(t2[0]) + e2.column), fl = (e2, t2, o2, n2, r2) => {
    ((e3, t3, o3, n3) => {
      t3 > 0 && t3 < e3[0].cells.length && N(e3, (e4) => {
        const r3 = e4.cells[t3 - 1];
        let s3 = 0;
        const l3 = n3();
        for (; e4.cells.length > t3 + s3 && o3(r3.element, e4.cells[t3 + s3].element); ) Ao(e4, t3 + s3, Ke(l3, true, e4.cells[t3 + s3].isLocked)), s3++;
      });
    })(t2, e2, r2, n2.cell);
    const s2 = ll(o2, t2), l2 = ml(o2, s2, n2), a2 = ll(t2, l2), c2 = ml(t2, a2, n2);
    return E(c2, (t3, o3) => Bo(t3, e2, l2[o3].cells));
  }, gl = (e2, t2, o2, n2, r2) => {
    ((e3, t3, o3, n3) => {
      const r3 = jo(e3).rows;
      if (t3 > 0 && t3 < r3.length) {
        const e4 = ((e5, t4) => W(e5, (e6, o4) => O(e6, (e7) => t4(e7.element, o4.element)) ? e6 : e6.concat([o4]), []))(r3[t3 - 1].cells, o3);
        N(e4, (e5) => {
          let s3 = C.none();
          for (let l3 = t3; l3 < r3.length; l3++) for (let t4 = 0; t4 < Mo(r3[0]); t4++) {
            const a3 = r3[l3], c3 = Lo(a3, t4);
            o3(c3.element, e5.element) && (s3.isNone() && (s3 = C.some(n3())), s3.each((e6) => {
              Ao(a3, t4, Ke(e6, true, c3.isLocked));
            }));
          }
        });
      }
    })(t2, e2, r2, n2.cell);
    const s2 = Ho(t2), l2 = sl(t2, o2), a2 = { ...l2, colDelta: l2.colDelta - s2.length }, c2 = ml(t2, a2, n2), { cols: i3, rows: m2 } = jo(c2), d2 = Ho(c2), u2 = sl(o2, t2), f2 = { ...u2, colDelta: u2.colDelta + d2.length }, g2 = (p2 = n2, w2 = d2, E(o2, (e3) => W(w2, (t3, o3) => {
      const n3 = al(1, e3, p2, x)[0];
      return zo(t3, o3, n3);
    }, e3))), h2 = ml(g2, f2, n2);
    var p2, w2;
    return [...i3, ...m2.slice(0, e2), ...h2, ...m2.slice(e2, m2.length)];
  }, hl = (e2, t2, o2, n2, r2) => {
    const { rows: s2, cols: l2 } = jo(e2), a2 = s2.slice(0, t2), c2 = s2.slice(t2);
    return [...l2, ...a2, ((e3, t3, o3, n3) => Io(e3, (e4) => n3(e4, o3), t3))(s2[o2], (e3, o3) => t2 > 0 && t2 < s2.length && n2(_o(s2[t2 - 1], o3), _o(s2[t2], o3)) ? Lo(s2[t2], o3) : Ke(r2(e3.element, n2), true, e3.isLocked), n2, r2), ...c2];
  }, pl = (e2, t2, o2, n2, r2) => E(e2, (e3) => {
    const s2 = t2 > 0 && t2 < Mo(e3) && n2(_o(e3, t2 - 1), _o(e3, t2)), l2 = ((e4, t3, o3, n3, r3, s3, l3) => {
      if ("colgroup" !== o3 && n3) return Lo(e4, t3);
      {
        const t4 = Lo(e4, r3);
        return Ke(l3(t4.element, s3), true, false);
      }
    })(e3, t2, e3.section, s2, o2, n2, r2);
    return zo(e3, t2, l2);
  }), wl = (e2, t2, o2, n2) => ((e3, t3, o3, n3) => void 0 !== _o(e3[t3], o3) && t3 > 0 && n3(_o(e3[t3 - 1], o3), _o(e3[t3], o3)))(e2, t2, o2, n2) || ((e3, t3, o3) => t3 > 0 && o3(_o(e3, t3 - 1), _o(e3, t3)))(e2[t2], o2, n2), bl = (e2, t2, o2, n2) => {
    const r2 = (e3) => ((e4) => "row" === e4 ? zt(t2) : Bt(t2))(e3) ? `${e3}group` : e3;
    return e2 ? Ts(t2) ? r2(o2) : null : n2 && Ts(t2) ? r2("row" === o2 ? "col" : "row") : null;
  }, vl = (e2, t2, o2) => Ke(o2(e2.element, t2), true, e2.isLocked), yl = (e2, t2, o2, n2, r2, s2, l2) => E(e2, (e3, a2) => ((e4, c2) => {
    const i3 = e4.cells, m2 = E(i3, (e5, c3) => {
      if (((e6) => O(t2, (t3) => o2(e6.element, t3.element)))(e5)) {
        const t3 = l2(e5, a2, c3) ? r2(e5, o2, n2) : e5;
        return s2(t3, a2, c3).each((e6) => {
          var o3, n3;
          o3 = t3.element, n3 = { scope: C.from(e6) }, G(n3, (e7, t4) => {
            e7.fold(() => {
              fe(o3, t4);
            }, (e8) => {
              ce(o3.dom, t4, e8);
            });
          });
        }), t3;
      }
      return e5;
    });
    return Ye(e4.element, m2, e4.section, e4.isNew);
  })(e3)), xl = (e2, t2, o2) => j(e2, (n2, r2) => wl(e2, r2, t2, o2) ? [] : [Lo(n2, t2)]), Cl = (e2, t2, o2, n2, r2) => {
    const s2 = jo(e2).rows, l2 = j(t2, (e3) => xl(s2, e3, n2)), a2 = E(s2, (e3) => Rs(e3.cells)), c2 = ((e3, t3) => I(t3, h) && Rs(e3) ? x : (e4, o3, n3) => !("th" === Z(e4.element) && t3[o3]))(l2, a2), i3 = /* @__PURE__ */ ((e3, t3) => (o3, n3) => C.some(bl(e3, o3.element, "row", t3[n3])))(o2, a2);
    return yl(e2, l2, n2, r2, vl, i3, c2);
  }, Sl = (e2, t2, o2, n2) => {
    const r2 = jo(e2).rows, s2 = E(t2, (e3) => Lo(r2[e3.row], e3.column));
    return yl(e2, s2, o2, n2, vl, C.none, x);
  }, Tl = (e2) => {
    if (!l(e2)) throw new Error("cases must be an array");
    if (0 === e2.length) throw new Error("there must be at least one case");
    const t2 = [], o2 = {};
    return N(e2, (n2, r2) => {
      const s2 = $2(n2);
      if (1 !== s2.length) throw new Error("one and only one name per case");
      const a2 = s2[0], c2 = n2[a2];
      if (void 0 !== o2[a2]) throw new Error("duplicate key detected:" + a2);
      if ("cata" === a2) throw new Error("cannot have a case named cata (sorry)");
      if (!l(c2)) throw new Error("case arguments must be an array");
      t2.push(a2), o2[a2] = (...o3) => {
        const n3 = o3.length;
        if (n3 !== c2.length) throw new Error("Wrong number of arguments to case " + a2 + ". Expected " + c2.length + " (" + c2 + "), got " + n3);
        return { fold: (...t3) => {
          if (t3.length !== e2.length) throw new Error("Wrong number of arguments to fold. Expected " + e2.length + ", got " + t3.length);
          return t3[r2].apply(null, o3);
        }, match: (e3) => {
          const n4 = $2(e3);
          if (t2.length !== n4.length) throw new Error("Wrong number of arguments to match. Expected: " + t2.join(",") + "\nActual: " + n4.join(","));
          if (!I(t2, (e4) => D(n4, e4))) throw new Error("Not all branches were specified when using match. Specified: " + n4.join(", ") + "\nRequired: " + t2.join(", "));
          return e3[a2].apply(null, o3);
        }, log: (e3) => {
          console.log(e3, { constructors: t2, constructor: a2, params: o3 });
        } };
      };
    }), o2;
  }, Rl = { ...Tl([{ none: [] }, { only: ["index"] }, { left: ["index", "next"] }, { middle: ["prev", "index", "next"] }, { right: ["prev", "index"] }]) }, Dl = (e2, t2, o2) => {
    let n2 = 0;
    for (let r2 = e2; r2 < t2; r2++) n2 += void 0 !== o2[r2] ? o2[r2] : 0;
    return n2;
  }, Ol = (e2, t2) => {
    const o2 = Qo(e2);
    return E(o2, (e3) => {
      const o3 = Dl(e3.row, e3.row + e3.rowspan, t2);
      return { element: e3.element, height: o3, rowspan: e3.rowspan };
    });
  }, kl = (e2, t2, o2) => {
    const n2 = ((e3, t3) => Zo(e3) ? ((e4, t4) => {
      const o3 = Xo(e4);
      return E(o3, (e5, o4) => ({ element: e5.element, width: t4[o4], colspan: e5.colspan }));
    })(e3, t3) : ((e4, t4) => {
      const o3 = Qo(e4);
      return E(o3, (e5) => {
        const o4 = Dl(e5.column, e5.column + e5.colspan, t4);
        return { element: e5.element, width: o4, colspan: e5.colspan };
      });
    })(e3, t3))(e2, t2);
    N(n2, (e3) => {
      o2.setElementWidth(e3.element, e3.width);
    });
  }, El = (e2, t2, o2, n2, r2) => {
    const s2 = Uo(e2), l2 = r2.getCellDelta(t2), a2 = r2.getWidths(s2, r2), c2 = o2 === s2.grid.columns - 1, i3 = n2.clampTableDelta(a2, o2, l2, r2.minCellWidth(), c2), m2 = ((e3, t3, o3, n3, r3) => {
      const s3 = e3.slice(0), l3 = ((e4, t4) => 0 === e4.length ? Rl.none() : 1 === e4.length ? Rl.only(0) : 0 === t4 ? Rl.left(0, 1) : t4 === e4.length - 1 ? Rl.right(t4 - 1, t4) : t4 > 0 && t4 < e4.length - 1 ? Rl.middle(t4 - 1, t4, t4 + 1) : Rl.none())(e3, t3), a3 = g(E(s3, g(0)));
      return l3.fold(a3, (e4) => n3.singleColumnWidth(s3[e4], o3), (e4, t4) => r3.calcLeftEdgeDeltas(s3, e4, t4, o3, n3.minCellWidth(), n3.isRelative), (e4, t4, l4) => r3.calcMiddleDeltas(s3, e4, t4, l4, o3, n3.minCellWidth(), n3.isRelative), (e4, t4) => r3.calcRightEdgeDeltas(s3, e4, t4, o3, n3.minCellWidth(), n3.isRelative));
    })(a2, o2, i3, r2, n2), d2 = E(m2, (e3, t3) => e3 + a2[t3]);
    kl(s2, d2, r2), n2.resizeTable(r2.adjustTableWidth, i3, c2);
  }, Nl = (e2) => W(e2, (e3, t2) => O(e3, (e4) => e4.column === t2.column) ? e3 : e3.concat([t2]), []).sort((e3, t2) => e3.column - t2.column), Bl = ae("col"), zl = ae("colgroup"), Al = (e2) => "tr" === Z(e2) || zl(e2), Wl = (e2) => ({ element: e2, colspan: Et(e2, "colspan", 1), rowspan: Et(e2, "rowspan", 1) }), Ll = (e2) => ue(e2, "scope").map((e3) => e3.substr(0, 3)), _l = (e2, t2 = Wl) => {
    const o2 = (o3) => {
      if (Al(o3)) return zl((r2 = { element: o3 }).element) ? e2.colgroup(r2) : e2.row(r2);
      {
        const r3 = o3, s2 = ((t3) => Bl(t3.element) ? e2.col(t3) : e2.cell(t3))(t2(r3));
        return n2 = C.some({ item: r3, replacement: s2 }), s2;
      }
      var r2;
    };
    let n2 = C.none();
    return { getOrInit: (e3, t3) => n2.fold(() => o2(e3), (n3) => t3(e3, n3.item) ? n3.replacement : o2(e3)) };
  }, Ml = (e2) => (t2) => {
    const o2 = [], n2 = (n3) => {
      const r2 = "td" === e2 ? { scope: null } : {}, s2 = t2.replace(n3, e2, r2);
      return o2.push({ item: n3, sub: s2 }), s2;
    };
    return { replaceOrInit: (e3, t3) => {
      if (Al(e3) || Bl(e3)) return e3;
      {
        const r2 = e3;
        return ((e4, t4) => L(o2, (o3) => t4(o3.item, e4)))(r2, t3).fold(() => n2(r2), (o3) => t3(e3, o3.item) ? o3.sub : n2(r2));
      }
    } };
  }, jl = (e2) => ({ unmerge: (t2) => {
    const o2 = Ll(t2);
    return o2.each((e3) => ie(t2, "scope", e3)), () => {
      const n2 = e2.cell({ element: t2, colspan: 1, rowspan: 1 });
      return kt(n2, "width"), kt(t2, "width"), o2.each((e3) => ie(n2, "scope", e3)), n2;
    };
  }, merge: (e3) => (kt(e3[0], "width"), (() => {
    const t2 = ft(E(e3, Ll));
    if (0 === t2.length) return C.none();
    {
      const e4 = t2[0], o2 = ["row", "col"];
      return O(t2, (t3) => t3 !== e4 && D(o2, t3)) ? C.none() : C.from(e4);
    }
  })().fold(() => fe(e3[0], "scope"), (t2) => ie(e3[0], "scope", t2 + "group")), g(e3[0])) }), Il = ["body", "p", "div", "article", "aside", "figcaption", "figure", "footer", "header", "nav", "section", "ol", "ul", "table", "thead", "tfoot", "tbody", "caption", "tr", "td", "th", "h1", "h2", "h3", "h4", "h5", "h6", "blockquote", "pre", "address"], Pl = Fr(), Fl = (e2) => ((e3, t2) => {
    const o2 = e3.property().name(t2);
    return D(Il, o2);
  })(Pl, e2), Hl = (e2) => ((e3, t2) => {
    const o2 = e3.property().name(t2);
    return D(["ol", "ul"], o2);
  })(Pl, e2), ql = (e2) => {
    const t2 = ae("br"), o2 = (e3) => hr(e3).bind((o3) => {
      const n3 = ke(o3).map((e4) => !!Fl(e4) || !!((e5, t3) => D(["br", "img", "hr", "input"], e5.property().name(t3)))(Pl, e4) && "img" !== Z(e4)).getOr(false);
      return Re(o3).map((r2) => {
        return true === n3 || ("li" === Z(s2 = r2) || at(s2, Hl).isSome()) || t2(o3) || Fl(r2) && !ye(e3, r2) ? [] : [pe.fromTag("br")];
        var s2;
      });
    }).getOr([]), n2 = (() => {
      const n3 = j(e2, (e3) => {
        const n4 = Ee(e3);
        return ((e4) => I(e4, (e5) => t2(e5) || re(e5) && 0 === cr(e5).trim().length))(n4) ? [] : n4.concat(o2(e3));
      });
      return 0 === n3.length ? [pe.fromTag("br")] : n3;
    })();
    je(e2[0]), Me(e2[0], n2);
  }, Vl = (e2) => Is(e2, true), $l = (e2) => {
    0 === It(e2).length && Ie(e2);
  }, Ul = (e2, t2) => ({ grid: e2, cursor: t2 }), Gl = (e2, t2, o2) => {
    const n2 = ((e3, t3, o3) => {
      var n3, r2;
      const s2 = jo(e3).rows;
      return C.from(null === (r2 = null === (n3 = s2[t3]) || void 0 === n3 ? void 0 : n3.cells[o3]) || void 0 === r2 ? void 0 : r2.element).filter(Vl).orThunk(() => ((e4) => V(e4, (e5) => V(e5.cells, (e6) => {
        const t4 = e6.element;
        return gt(Vl(t4), t4);
      })))(s2));
    })(e2, t2, o2);
    return Ul(e2, n2);
  }, Kl = (e2) => W(e2, (e3, t2) => O(e3, (e4) => e4.row === t2.row) ? e3 : e3.concat([t2]), []).sort((e3, t2) => e3.row - t2.row), Yl = (e2, t2) => (o2, n2, r2, s2, l2) => {
    const a2 = Kl(n2), c2 = E(a2, (e3) => e3.row), i3 = ((e3, t3, o3, n3, r3, s3, l3) => {
      const { cols: a3, rows: c3 } = jo(e3), i4 = c3[t3[0]], m2 = j(t3, (e4) => ((e5, t4, o4) => {
        const n4 = e5[t4];
        return j(n4.cells, (n5, r4) => wl(e5, t4, r4, o4) ? [] : [n5]);
      })(c3, e4, r3)), d2 = E(i4.cells, (e4, t4) => Rs(xl(c3, t4, r3))), u2 = [...c3];
      N(t3, (e4) => {
        u2[e4] = l3.transformRow(c3[e4], o3);
      });
      const f2 = [...a3, ...u2], g2 = ((e4, t4) => I(t4, h) && Rs(e4.cells) ? x : (e5, o4, n4) => !("th" === Z(e5.element) && t4[n4]))(i4, d2), p2 = /* @__PURE__ */ ((e4, t4) => (o4, n4, r4) => C.some(bl(e4, o4.element, "col", t4[r4])))(n3, d2);
      return yl(f2, m2, r3, s3, l3.transformCell, p2, g2);
    })(o2, c2, e2, t2, r2, s2.replaceOrInit, l2);
    return Gl(i3, n2[0].row, n2[0].column);
  }, Jl = Yl("thead", true), Ql = Yl("tbody", false), Xl = Yl("tfoot", false), Zl = (e2, t2, o2) => {
    const n2 = ((e3, t3) => Vt(e3, () => t3))(e2, o2.section), r2 = Go(n2);
    return Us(r2, t2, true);
  }, ea = (e2, t2, o2, n2) => ((e3, t3, o3, n3) => {
    const r2 = Go(t3), s2 = n3.getWidths(r2, n3);
    kl(r2, s2, n3);
  })(0, t2, 0, n2.sizing), ta = (e2, t2, o2, n2) => ((e3, t3, o3, n3, r2) => {
    const s2 = Go(t3), l2 = n3.getWidths(s2, n3), a2 = n3.pixelWidth(), { newSizes: c2, delta: i3 } = r2.calcRedestributedWidths(l2, a2, o3.pixelDelta, n3.isRelative);
    kl(s2, c2, n3), n3.adjustTableWidth(i3);
  })(0, t2, o2, n2.sizing, n2.resize), oa = (e2, t2) => O(t2, (e3) => 0 === e3.column && e3.isLocked), na = (e2, t2) => O(t2, (t3) => t3.column + t3.colspan >= e2.grid.columns && t3.isLocked), ra = (e2, t2) => {
    const o2 = tn(e2), n2 = Nl(t2);
    return W(n2, (e3, t3) => e3 + o2[t3.column].map(Eo).getOr(0), 0);
  }, sa = (e2) => (t2, o2) => Xs(t2, o2).filter((o3) => !(e2 ? oa : na)(t2, o3)).map((e3) => ({ details: e3, pixelDelta: ra(t2, e3) })), la = (e2) => (t2, o2) => Qs(t2, o2).filter((o3) => !(e2 ? oa : na)(t2, o3.cells)), aa = Ml("th"), ca = Ml("td"), ia = Js((e2, t2, o2, n2) => {
    const r2 = t2[0].row, s2 = Kl(t2), l2 = A(s2, (e3, t3) => ({ grid: hl(e3.grid, r2, t3.row + e3.delta, o2, n2.getOrInit), delta: e3.delta + 1 }), { grid: e2, delta: 0 }).grid;
    return Gl(l2, r2, t2[0].column);
  }, Xs, f, f, _l), ma = Js((e2, t2, o2, n2) => {
    const r2 = Kl(t2), s2 = r2[r2.length - 1], l2 = s2.row + s2.rowspan, a2 = A(r2, (e3, t3) => hl(e3, l2, t3.row, o2, n2.getOrInit), e2);
    return Gl(a2, l2, t2[0].column);
  }, Xs, f, f, _l), da = Js((e2, t2, o2, n2) => {
    const r2 = t2.details, s2 = Nl(r2), l2 = s2[0].column, a2 = A(s2, (e3, t3) => ({ grid: pl(e3.grid, l2, t3.column + e3.delta, o2, n2.getOrInit), delta: e3.delta + 1 }), { grid: e2, delta: 0 }).grid;
    return Gl(a2, r2[0].row, l2);
  }, sa(true), ta, f, _l), ua = Js((e2, t2, o2, n2) => {
    const r2 = t2.details, s2 = r2[r2.length - 1], l2 = s2.column + s2.colspan, a2 = Nl(r2), c2 = A(a2, (e3, t3) => pl(e3, l2, t3.column, o2, n2.getOrInit), e2);
    return Gl(c2, r2[0].row, l2);
  }, sa(false), ta, f, _l), fa = Js((e2, t2, o2, n2) => {
    const r2 = Nl(t2.details), s2 = ((e3, t3) => j(e3, (e4) => {
      const o3 = e4.cells, n3 = A(t3, (e5, t4) => t4 >= 0 && t4 < e5.length ? e5.slice(0, t4).concat(e5.slice(t4 + 1)) : e5, o3);
      return n3.length > 0 ? [Ye(e4.element, n3, e4.section, e4.isNew)] : [];
    }))(e2, E(r2, (e3) => e3.column)), l2 = s2.length > 0 ? s2[0].cells.length - 1 : 0;
    return Gl(s2, r2[0].row, Math.min(r2[0].column, l2));
  }, (e2, t2) => Zs(e2, t2).map((t3) => ({ details: t3, pixelDelta: -ra(e2, t3) })), ta, $l, _l), ga = Js((e2, t2, o2, n2) => {
    const r2 = Kl(t2), s2 = ((e3, t3, o3) => {
      const { rows: n3, cols: r3 } = jo(e3);
      return [...r3, ...n3.slice(0, t3), ...n3.slice(o3 + 1)];
    })(e2, r2[0].row, r2[r2.length - 1].row), l2 = s2.length > 0 ? s2.length - 1 : 0;
    return Gl(s2, Math.min(t2[0].row, l2), t2[0].column);
  }, Xs, f, $l, _l), ha = Js((e2, t2, o2, n2) => {
    const r2 = Nl(t2), s2 = E(r2, (e3) => e3.column), l2 = Cl(e2, s2, true, o2, n2.replaceOrInit);
    return Gl(l2, t2[0].row, t2[0].column);
  }, Zs, f, f, aa), pa = Js((e2, t2, o2, n2) => {
    const r2 = Nl(t2), s2 = E(r2, (e3) => e3.column), l2 = Cl(e2, s2, false, o2, n2.replaceOrInit);
    return Gl(l2, t2[0].row, t2[0].column);
  }, Zs, f, f, ca), wa = Js(Jl, Zs, f, f, aa), ba = Js(Ql, Zs, f, f, ca), va = Js(Xl, Zs, f, f, ca), ya = Js((e2, t2, o2, n2) => {
    const r2 = Sl(e2, t2, o2, n2.replaceOrInit);
    return Gl(r2, t2[0].row, t2[0].column);
  }, Zs, f, f, aa), xa = Js((e2, t2, o2, n2) => {
    const r2 = Sl(e2, t2, o2, n2.replaceOrInit);
    return Gl(r2, t2[0].row, t2[0].column);
  }, Zs, f, f, ca), Ca = Js((e2, t2, o2, n2) => {
    const r2 = t2.cells;
    ql(r2);
    const s2 = ((e3, t3, o3, n3) => {
      const r3 = jo(e3).rows;
      if (0 === r3.length) return e3;
      for (let e4 = t3.startRow; e4 <= t3.finishRow; e4++) for (let o4 = t3.startCol; o4 <= t3.finishCol; o4++) {
        const t4 = r3[e4], s3 = Lo(t4, o4).isLocked;
        Ao(t4, o4, Ke(n3(), false, s3));
      }
      return e3;
    })(e2, t2.bounds, 0, n2.merge(r2));
    return Ul(s2, C.from(r2[0]));
  }, (e2, t2) => ((e3, t3) => t3.mergable)(0, t2).filter((t3) => el(e2, t3.cells)), ea, f, jl), Sa = Js((e2, t2, o2, n2) => {
    const r2 = A(t2, (e3, t3) => tl(e3, t3, o2, n2.unmerge(t3)), e2);
    return Ul(r2, C.from(t2[0]));
  }, (e2, t2) => ((e3, t3) => t3.unmergable)(0, t2).filter((t3) => el(e2, t3)), ea, f, jl), Ta = Js((e2, t2, o2, n2) => {
    const r2 = ((e3, t3) => {
      const o3 = Uo(e3);
      return Us(o3, t3, true);
    })(t2.clipboard, t2.generators);
    var s2, l2;
    return ((e3, t3, o3, n3, r3) => {
      const s3 = Ho(t3), l3 = ((e4, t4, o4) => {
        const n4 = Mo(t4[0]), r4 = jo(t4).cols.length + e4.row, s4 = k(n4 - e4.column, (t5) => t5 + e4.column);
        return { row: r4, column: L(s4, (e5) => I(o4, (t5) => t5 !== e5)).getOr(n4 - 1) };
      })(e3, t3, s3), a2 = jo(o3).rows, c2 = ul(l3, a2, s3), i3 = ((e4, t4, o4) => {
        if (e4.row >= t4.length || e4.column > Mo(t4[0])) return rl.error("invalid start address out of table bounds, row: " + e4.row + ", column: " + e4.column);
        const n4 = t4.slice(e4.row), r4 = n4[0].cells.slice(e4.column), s4 = Mo(o4[0]), l4 = o4.length;
        return rl.value({ rowDelta: n4.length - l4, colDelta: r4.length - s4 });
      })(l3, t3, a2);
      return i3.map((e4) => {
        const o4 = { ...e4, colDelta: e4.colDelta - c2.length }, s4 = ml(t3, o4, n3), i4 = Ho(s4), m2 = ul(l3, a2, i4);
        return ((e5, t4, o5, n4, r4, s5) => {
          const l4 = e5.row, a3 = e5.column, c3 = l4 + o5.length, i5 = a3 + Mo(o5[0]) + s5.length, m3 = P(s5, x);
          for (let e6 = l4; e6 < c3; e6++) {
            let s6 = 0;
            for (let c4 = a3; c4 < i5; c4++) {
              if (m3[c4]) {
                s6++;
                continue;
              }
              dl(t4, e6, c4, r4) && tl(t4, _o(t4[e6], c4), r4, n4.cell);
              const i6 = c4 - a3 - s6, d2 = Lo(o5[e6 - l4], i6), u2 = d2.element, f2 = n4.replace(u2);
              Ao(t4[e6], c4, Ke(f2, true, d2.isLocked));
            }
          }
          return t4;
        })(l3, s4, a2, n3, r3, m2);
      });
    })((s2 = t2.row, l2 = t2.column, { row: s2, column: l2 }), e2, r2, t2.generators, o2).fold(() => Ul(e2, C.some(t2.element)), (e3) => Gl(e3, t2.row, t2.column));
  }, (e2, t2) => jt(t2.element).bind((o2) => Ks(e2, o2).map((e3) => ({ ...e3, generators: t2.generators, clipboard: t2.clipboard }))), ea, f, _l), Ra = Js((e2, t2, o2, n2) => {
    const r2 = jo(e2).rows, s2 = t2.cells[0].column, l2 = r2[t2.cells[0].row], a2 = Zl(t2.clipboard, t2.generators, l2), c2 = fl(s2, e2, a2, t2.generators, o2);
    return Gl(c2, t2.cells[0].row, t2.cells[0].column);
  }, la(true), f, f, _l), Da = Js((e2, t2, o2, n2) => {
    const r2 = jo(e2).rows, s2 = t2.cells[t2.cells.length - 1].column + t2.cells[t2.cells.length - 1].colspan, l2 = r2[t2.cells[0].row], a2 = Zl(t2.clipboard, t2.generators, l2), c2 = fl(s2, e2, a2, t2.generators, o2);
    return Gl(c2, t2.cells[0].row, t2.cells[0].column);
  }, la(false), f, f, _l), Oa = Js((e2, t2, o2, n2) => {
    const r2 = jo(e2).rows, s2 = t2.cells[0].row, l2 = r2[s2], a2 = Zl(t2.clipboard, t2.generators, l2), c2 = gl(s2, e2, a2, t2.generators, o2);
    return Gl(c2, t2.cells[0].row, t2.cells[0].column);
  }, Qs, f, f, _l), ka = Js((e2, t2, o2, n2) => {
    const r2 = jo(e2).rows, s2 = t2.cells[t2.cells.length - 1].row + t2.cells[t2.cells.length - 1].rowspan, l2 = r2[t2.cells[0].row], a2 = Zl(t2.clipboard, t2.generators, l2), c2 = gl(s2, e2, a2, t2.generators, o2);
    return Gl(c2, t2.cells[0].row, t2.cells[0].column);
  }, Qs, f, f, _l), Ea = (e2, t2) => {
    const o2 = Uo(e2);
    return Xs(o2, t2).bind((e3) => {
      const t3 = e3[e3.length - 1], n2 = e3[0].column, r2 = t3.column + t3.colspan, s2 = M(E(o2.all, (e4) => z(e4.cells, (e5) => e5.column >= n2 && e5.column < r2)));
      return ks(s2);
    }).getOr("");
  }, Na = (e2, t2) => {
    const o2 = Uo(e2);
    return Xs(o2, t2).bind(ks).getOr("");
  }, Ba = (e2, t2) => {
    const o2 = Uo(e2);
    return Xs(o2, t2).bind((e3) => {
      const t3 = e3[e3.length - 1], n2 = e3[0].row, r2 = t3.row + t3.rowspan;
      return ((e4) => {
        const t4 = E(e4, (e5) => Os(e5).type), o3 = D(t4, "header"), n3 = D(t4, "footer");
        if (o3 || n3) {
          const e5 = D(t4, "body");
          return !o3 || e5 || n3 ? o3 || e5 || !n3 ? C.none() : C.some("footer") : C.some("header");
        }
        return C.some("body");
      })(o2.all.slice(n2, r2));
    }).getOr("");
  }, za = (e2, t2) => e2.dispatch("NewRow", { node: t2 }), Aa = (e2, t2) => e2.dispatch("NewCell", { node: t2 }), Wa = (e2, t2, o2) => {
    e2.dispatch("TableModified", { ...o2, table: t2 });
  }, La = { structure: false, style: true }, _a = { structure: true, style: false }, Ma = { structure: true, style: true }, ja = (e2) => (t2) => t2.options.get(e2), Ia = "100%", Pa = (e2) => {
    var t2;
    const o2 = e2.dom, n2 = null !== (t2 = o2.getParent(e2.selection.getStart(), o2.isBlock)) && void 0 !== t2 ? t2 : e2.getBody();
    return No(pe.fromDom(n2)) + "px";
  }, Fa = (e2) => C.from(e2.options.get("table_clone_elements")), Ha = ja("table_header_type"), qa = ja("table_column_resizing"), Va = (e2) => "preservetable" === qa(e2), $a = (e2) => "resizetable" === qa(e2), Ua = ja("table_sizing_mode"), Ga = (e2) => "relative" === Ua(e2), Ka = (e2) => "fixed" === Ua(e2), Ya = (e2) => "responsive" === Ua(e2), Ja = ja("table_resize_bars"), Qa = ja("table_style_by_css"), Xa = (e2) => {
    const t2 = e2.options, o2 = t2.get("table_default_attributes");
    return t2.isSet("table_default_attributes") ? o2 : ((e3, t3) => Ya(e3) || Qa(e3) ? t3 : Ka(e3) ? { ...t3, width: Pa(e3) } : { ...t3, width: Ia })(e2, o2);
  }, Za = ja("table_use_colgroups"), ec = (e2, t2) => Ga(e2) ? sr(t2) : Ka(e2) ? rr(t2) : nr(t2), tc = (e2, t2, o2) => {
    const n2 = (e3) => "table" === Z(Dr(e3)), r2 = Fa(e2), s2 = $a(e2) ? f : hs, l2 = (t3) => {
      switch (Ha(e2)) {
        case "section":
          return Ws();
        case "sectionCells":
          return Ls();
        case "cells":
          return _s();
        default:
          return ((e3, t4) => {
            var o3;
            switch ((o3 = Uo(e3), V(o3.all, (e4) => {
              const t5 = Os(e4);
              return "header" === t5.type ? C.from(t5.subType) : C.none();
            })).getOr(t4)) {
              case "section":
                return Bs();
              case "sectionCells":
                return zs();
              case "cells":
                return As();
            }
          })(t3, "section");
      }
    }, a2 = (n3, s3, a3, c3) => (i4, m2, d2 = false) => {
      kr(i4);
      const u2 = pe.fromDom(e2.getDoc()), f2 = Tr(a3, u2, r2), g2 = { sizing: ec(e2, i4), resize: $a(e2) ? xs() : Cs(), section: l2(i4) };
      return s3(i4) ? n3(i4, m2, f2, g2).bind((n4) => {
        t2.refresh(i4.dom), N(n4.newRows, (t3) => {
          za(e2, t3.dom);
        }), N(n4.newCells, (t3) => {
          Aa(e2, t3.dom);
        });
        const r3 = ((t3, n5) => n5.cursor.fold(() => {
          const n6 = It(t3);
          return H(n6).filter(et).map((n7) => {
            o2.clearSelectedCells(t3.dom);
            const r4 = e2.dom.createRng();
            return r4.selectNode(n7.dom), e2.selection.setRng(r4), ie(n7, "data-mce-selected", "1"), r4;
          });
        }, (n6) => {
          const r4 = fs(gs, n6), s4 = e2.dom.createRng();
          return s4.setStart(r4.element.dom, r4.offset), s4.setEnd(r4.element.dom, r4.offset), e2.selection.setRng(s4), o2.clearSelectedCells(t3.dom), C.some(s4);
        }))(i4, n4);
        return et(i4) && (kr(i4), d2 || Wa(e2, i4.dom, c3)), r3.map((e3) => ({ rng: e3, effect: c3 }));
      }) : C.none();
    }, c2 = a2(ga, (t3) => !n2(e2) || Ss(t3).rows > 1, f, _a), i3 = a2(fa, (t3) => !n2(e2) || Ss(t3).columns > 1, f, _a);
    return { deleteRow: c2, deleteColumn: i3, insertRowsBefore: a2(ia, x, f, _a), insertRowsAfter: a2(ma, x, f, _a), insertColumnsBefore: a2(da, x, s2, _a), insertColumnsAfter: a2(ua, x, s2, _a), mergeCells: a2(Ca, x, f, _a), unmergeCells: a2(Sa, x, f, _a), pasteColsBefore: a2(Ra, x, f, _a), pasteColsAfter: a2(Da, x, f, _a), pasteRowsBefore: a2(Oa, x, f, _a), pasteRowsAfter: a2(ka, x, f, _a), pasteCells: a2(Ta, x, f, Ma), makeCellsHeader: a2(ya, x, f, _a), unmakeCellsHeader: a2(xa, x, f, _a), makeColumnsHeader: a2(ha, x, f, _a), unmakeColumnsHeader: a2(pa, x, f, _a), makeRowsHeader: a2(wa, x, f, _a), makeRowsBody: a2(ba, x, f, _a), makeRowsFooter: a2(va, x, f, _a), getTableRowType: Ba, getTableCellType: Na, getTableColType: Ea };
  }, oc = (e2, t2, o2) => {
    const n2 = Et(e2, t2, 1);
    1 === o2 || n2 <= 1 ? fe(e2, t2) : ie(e2, t2, Math.min(o2, n2));
  }, nc = (e2, t2) => (o2) => {
    const n2 = o2.column + o2.colspan - 1, r2 = o2.column;
    return n2 >= e2 && r2 < t2;
  }, rc = Tl([{ invalid: ["raw"] }, { pixels: ["value"] }, { percent: ["value"] }]), sc = (e2, t2, o2) => {
    const n2 = o2.substring(0, o2.length - e2.length), r2 = parseFloat(n2);
    return n2 === r2.toString() ? t2(r2) : rc.invalid(o2);
  }, lc = { ...rc, from: (e2) => bt(e2, "%") ? sc("%", rc.percent, e2) : bt(e2, "px") ? sc("px", rc.pixels, e2) : rc.invalid(e2) }, ac = (e2, t2, o2) => {
    const n2 = lc.from(o2), r2 = I(e2, (e3) => "0px" === e3) ? ((e3, t3) => {
      const o3 = e3.fold(() => g(""), (e4) => g(e4 / t3 + "px"), () => g(100 / t3 + "%"));
      return k(t3, o3);
    })(n2, e2.length) : ((e3, t3, o3) => e3.fold(() => t3, (e4) => ((e5, t4, o4) => {
      const n3 = o4 / t4;
      return E(e5, (e6) => lc.from(e6).fold(() => e6, (e7) => e7 * n3 + "px", (e7) => e7 / 100 * o4 + "px"));
    })(t3, o3, e4), (e4) => ((e5, t4) => E(e5, (e6) => lc.from(e6).fold(() => e6, (e7) => e7 / t4 * 100 + "%", (e7) => e7 + "%")))(t3, o3)))(n2, e2, t2);
    return mc(r2);
  }, cc = (e2, t2) => 0 === e2.length ? t2 : A(e2, (e3, t3) => lc.from(t3).fold(g(0), h, h) + e3, 0), ic = (e2, t2) => lc.from(e2).fold(g(e2), (e3) => e3 + t2 + "px", (e3) => e3 + t2 + "%"), mc = (e2) => {
    if (0 === e2.length) return e2;
    const t2 = A(e2, (e3, t3) => {
      const o3 = lc.from(t3).fold(() => ({ value: t3, remainder: 0 }), (e4) => ((e5, t4) => {
        const o4 = Math.floor(e5);
        return { value: o4 + "px", remainder: e5 - o4 };
      })(e4), (e4) => ({ value: e4 + "%", remainder: 0 }));
      return { output: [o3.value].concat(e3.output), remainder: e3.remainder + o3.remainder };
    }, { output: [], remainder: 0 }), o2 = t2.output;
    return o2.slice(0, o2.length - 1).concat([ic(o2[o2.length - 1], Math.round(t2.remainder))]);
  }, dc = lc.from, uc = (e2) => dc(e2).fold(g("px"), g("px"), g("%")), fc = (e2, t2, o2) => {
    const n2 = Uo(e2), r2 = n2.all, s2 = Qo(n2), l2 = Xo(n2);
    t2.each((t3) => {
      const o3 = uc(t3), r3 = ko(e2), a2 = ((e3, t4) => Jn(e3, t4, Gn, Qn))(n2, e2), c2 = ac(a2, r3, t3);
      Zo(n2) ? ((e3, t4, o4) => {
        N(t4, (t5, n3) => {
          const r4 = cc([e3[n3]], Wt());
          St(t5.element, "width", r4 + o4);
        });
      })(c2, l2, o3) : ((e3, t4, o4) => {
        N(t4, (t5) => {
          const n3 = e3.slice(t5.column, t5.colspan + t5.column), r4 = cc(n3, Wt());
          St(t5.element, "width", r4 + o4);
        });
      })(c2, s2, o3), St(e2, "width", t3);
    }), o2.each((t3) => {
      const o3 = uc(t3), l3 = an(e2), a2 = ((e3, t4, o4) => Zn(e3, t4, o4, Kn, Qn))(n2, e2, Rn);
      ((e3, t4, o4, n3) => {
        N(o4, (t5) => {
          const o5 = e3.slice(t5.row, t5.rowspan + t5.row), r3 = cc(o5, Lt());
          St(t5.element, "height", r3 + n3);
        }), N(t4, (t5, o5) => {
          St(t5.element, "height", e3[o5]);
        });
      })(ac(a2, l3, t3), r2, s2, o3), St(e2, "height", t3);
    });
  }, gc = (e2) => In(e2).exists((e3) => Nn.test(e3)), hc = (e2) => In(e2).exists((e3) => Bn.test(e3)), pc = (e2) => In(e2).isNone(), wc = (e2) => {
    fe(e2, "width");
  }, bc = (e2) => {
    const t2 = Vn(e2);
    fc(e2, C.some(t2), C.none()), wc(e2);
  }, vc = (e2) => {
    const t2 = ((e3) => ko(e3) + "px")(e2);
    fc(e2, C.some(t2), C.none()), wc(e2);
  }, yc = (e2) => {
    kt(e2, "width");
    const t2 = Pt(e2), o2 = t2.length > 0 ? t2 : It(e2);
    N(o2, (e3) => {
      kt(e3, "width"), wc(e3);
    }), wc(e2);
  }, xc = { styles: { "border-collapse": "collapse", width: "100%" }, attributes: { border: "1" }, colGroups: false }, Cc = (e2, t2, o2, n2) => k(e2, (e3) => ((e4, t3, o3, n3) => {
    const r2 = pe.fromTag("tr");
    for (let s2 = 0; s2 < e4; s2++) {
      const e5 = pe.fromTag(n3 < t3 || s2 < o3 ? "th" : "td");
      s2 < o3 && ie(e5, "scope", "row"), n3 < t3 && ie(e5, "scope", "col"), We(e5, pe.fromTag("br")), We(r2, e5);
    }
    return r2;
  })(t2, o2, n2, e3)), Sc = (e2, t2) => {
    e2.selection.select(t2.dom, true), e2.selection.collapse(true);
  }, Tc = (e2, t2, o2, n2, s2) => {
    const l2 = ((e3) => {
      const t3 = e3.options, o3 = t3.get("table_default_styles");
      return t3.isSet("table_default_styles") ? o3 : ((e4, t4) => Ya(e4) || !Qa(e4) ? t4 : Ka(e4) ? { ...t4, width: Pa(e4) } : { ...t4, width: Ia })(e3, o3);
    })(e2), a2 = { styles: l2, attributes: Xa(e2), colGroups: Za(e2) };
    return e2.undoManager.ignore(() => {
      const r2 = ((e3, t3, o3, n3, r3, s3 = xc) => {
        const l4 = pe.fromTag("table"), a3 = "cells" !== r3;
        Tt(l4, s3.styles), me(l4, s3.attributes), s3.colGroups && We(l4, ((e4) => {
          const t4 = pe.fromTag("colgroup");
          return k(e4, () => We(t4, pe.fromTag("col"))), t4;
        })(t3));
        const c2 = Math.min(e3, o3);
        if (a3 && o3 > 0) {
          const e4 = pe.fromTag("thead");
          We(l4, e4);
          const s4 = Cc(o3, t3, "sectionCells" === r3 ? c2 : 0, n3);
          Me(e4, s4);
        }
        const i3 = pe.fromTag("tbody");
        We(l4, i3);
        const m2 = Cc(a3 ? e3 - c2 : e3, t3, a3 ? 0 : o3, n3);
        return Me(i3, m2), l4;
      })(o2, t2, s2, n2, Ha(e2), a2);
      ie(r2, "data-mce-id", "__mce");
      const l3 = ((e3) => {
        const t3 = pe.fromTag("div"), o3 = pe.fromDom(e3.dom.cloneNode(true));
        return We(t3, o3), ((e4) => e4.dom.innerHTML)(t3);
      })(r2);
      e2.insertContent(l3), e2.addVisual();
    }), mt(Dr(e2), 'table[data-mce-id="__mce"]').map((t3) => (Ka(e2) ? vc(t3) : Ya(e2) ? yc(t3) : (Ga(e2) || ((e3) => r(e3) && -1 !== e3.indexOf("%"))(l2.width)) && bc(t3), kr(t3), fe(t3, "data-mce-id"), ((e3, t4) => {
      N(st(t4, "tr"), (t5) => {
        za(e3, t5.dom), N(st(t5, "th,td"), (t6) => {
          Aa(e3, t6.dom);
        });
      });
    })(e2, t3), ((e3, t4) => {
      mt(t4, "td,th").each(w(Sc, e3));
    })(e2, t3), t3.dom)).getOrNull();
  };
  var Rc = tinymce.util.Tools.resolve("tinymce.FakeClipboard");
  const Dc = "x-tinymce/dom-table-", Oc = Dc + "rows", kc = Dc + "columns", Ec = (e2) => {
    const t2 = Rc.FakeClipboardItem(e2);
    Rc.write([t2]);
  }, Nc = (e2) => {
    var t2;
    const o2 = null !== (t2 = Rc.read()) && void 0 !== t2 ? t2 : [];
    return V(o2, (t3) => C.from(t3.getType(e2)));
  }, Bc = (e2) => {
    Nc(e2).isSome() && Rc.clear();
  }, zc = (e2) => {
    e2.fold(Wc, (e3) => Ec({ [Oc]: e3 }));
  }, Ac = () => Nc(Oc), Wc = () => Bc(Oc), Lc = (e2) => {
    e2.fold(Mc, (e3) => Ec({ [kc]: e3 }));
  }, _c = () => Nc(kc), Mc = () => Bc(kc), jc = (e2) => ss(Er(e2), Or(e2)), Ic = (e2, t2) => {
    const o2 = Or(e2), n2 = (e3) => Ft(e3, o2), l2 = (t3) => ((e3) => ls(Er(e3), Or(e3)))(e2).bind((e3) => n2(e3).map((o3) => t3(o3, e3))), a2 = (t3) => {
      e2.focus();
    }, c2 = (t3, o3 = false) => l2((n3, r2) => {
      const s2 = ns(as(e2), n3, r2);
      t3(n3, s2, o3).each(a2);
    }), i3 = () => l2((t3, o3) => ((e3, t4, o4) => {
      const n3 = Uo(e3);
      return Xs(n3, t4).bind((e4) => {
        const t5 = Us(n3, o4, false), r2 = jo(t5).rows.slice(e4[0].row, e4[e4.length - 1].row + e4[e4.length - 1].rowspan), s2 = j(r2, (e5) => {
          const t6 = z(e5.cells, (e6) => !e6.isLocked);
          return t6.length > 0 ? [{ ...e5, cells: t6 }] : [];
        }), l3 = Gs(s2);
        return gt(l3.length > 0, l3);
      }).map((e4) => E(e4, (e5) => {
        const t5 = He(e5.element);
        return N(e5.cells, (e6) => {
          const o5 = qe(e6.element);
          Fs(o5, "colspan", e6.colspan, 1), Fs(o5, "rowspan", e6.rowspan, 1), We(t5, o5);
        }), t5;
      }));
    })(t3, ns(as(e2), t3, o3), Tr(f, pe.fromDom(e2.getDoc()), C.none()))), m2 = () => l2((t3, o3) => ((e3, t4) => {
      const o4 = Uo(e3);
      return Zs(o4, t4).map((e4) => {
        const t5 = e4[e4.length - 1], n3 = e4[0].column, r2 = t5.column + t5.colspan, s2 = ((e5, t6, o5) => {
          if (Zo(e5)) {
            const n4 = z(Xo(e5), nc(t6, o5)), r3 = E(n4, (e6) => {
              const n5 = qe(e6.element);
              return oc(n5, "span", o5 - t6), n5;
            }), s3 = pe.fromTag("colgroup");
            return Me(s3, r3), [s3];
          }
          return [];
        })(o4, n3, r2), l3 = ((e5, t6, o5) => E(e5.all, (e6) => {
          const n4 = z(e6.cells, nc(t6, o5)), r3 = E(n4, (e7) => {
            const n5 = qe(e7.element);
            return oc(n5, "colspan", o5 - t6), n5;
          }), s3 = pe.fromTag("tr");
          return Me(s3, r3), s3;
        }))(o4, n3, r2);
        return [...s2, ...l3];
      });
    })(t3, ns(as(e2), t3, o3))), d2 = (t3, o3) => o3().each((o4) => {
      const n3 = E(o4, (e3) => qe(e3));
      l2((o5, r2) => {
        const s2 = Rr(pe.fromDom(e2.getDoc())), l3 = ((e3, t4, o6, n4) => ({ selection: Zr(e3), clipboard: o6, generators: n4 }))(as(e2), 0, n3, s2);
        t3(o5, l3).each(a2);
      });
    }), g2 = (e3) => (t3, o3) => ((e4, t4) => X(e4, t4) ? C.from(e4.type) : C.none())(o3, "type").each((t4) => {
      c2(e3(t4), o3.no_events);
    });
    G({ mceTableSplitCells: () => c2(t2.unmergeCells), mceTableMergeCells: () => c2(t2.mergeCells), mceTableInsertRowBefore: () => c2(t2.insertRowsBefore), mceTableInsertRowAfter: () => c2(t2.insertRowsAfter), mceTableInsertColBefore: () => c2(t2.insertColumnsBefore), mceTableInsertColAfter: () => c2(t2.insertColumnsAfter), mceTableDeleteCol: () => c2(t2.deleteColumn), mceTableDeleteRow: () => c2(t2.deleteRow), mceTableCutCol: () => m2().each((e3) => {
      Lc(e3), c2(t2.deleteColumn);
    }), mceTableCutRow: () => i3().each((e3) => {
      zc(e3), c2(t2.deleteRow);
    }), mceTableCopyCol: () => m2().each((e3) => Lc(e3)), mceTableCopyRow: () => i3().each((e3) => zc(e3)), mceTablePasteColBefore: () => d2(t2.pasteColsBefore, _c), mceTablePasteColAfter: () => d2(t2.pasteColsAfter, _c), mceTablePasteRowBefore: () => d2(t2.pasteRowsBefore, Ac), mceTablePasteRowAfter: () => d2(t2.pasteRowsAfter, Ac), mceTableDelete: () => jc(e2).each((t3) => {
      Ft(t3, o2).filter(b(o2)).each((t4) => {
        const o3 = pe.fromText("");
        if (ze(t4, o3), Ie(t4), e2.dom.isEmpty(e2.getBody())) e2.setContent(""), e2.selection.setCursorLocation();
        else {
          const t5 = e2.dom.createRng();
          t5.setStart(o3.dom, 0), t5.setEnd(o3.dom, 0), e2.selection.setRng(t5), e2.nodeChanged();
        }
      });
    }), mceTableCellToggleClass: (t3, o3) => {
      l2((t4) => {
        const n3 = as(e2), r2 = I(n3, (t5) => e2.formatter.match("tablecellclass", { value: o3 }, t5.dom)), s2 = r2 ? e2.formatter.remove : e2.formatter.apply;
        N(n3, (e3) => s2("tablecellclass", { value: o3 }, e3.dom)), Wa(e2, t4.dom, La);
      });
    }, mceTableToggleClass: (t3, o3) => {
      l2((t4) => {
        e2.formatter.toggle("tableclass", { value: o3 }, t4.dom), Wa(e2, t4.dom, La);
      });
    }, mceTableToggleCaption: () => {
      jc(e2).each((t3) => {
        Ft(t3, o2).each((o3) => {
          it(o3, "caption").fold(() => {
            const t4 = pe.fromTag("caption");
            We(t4, pe.fromText("Caption")), ((e3, t5, o4) => {
              Ne(e3, 0).fold(() => {
                We(e3, t5);
              }, (e4) => {
                Be(e4, t5);
              });
            })(o3, t4), e2.selection.setCursorLocation(t4.dom, 0);
          }, (n3) => {
            ae("caption")(t3) && ve("td", o3).each((t4) => e2.selection.setCursorLocation(t4.dom, 0)), Ie(n3);
          }), Wa(e2, o3.dom, _a);
        });
      });
    }, mceTableSizingMode: (t3, n3) => ((t4) => jc(e2).each((n4) => {
      Ya(e2) || Ka(e2) || Ga(e2) || Ft(n4, o2).each((o3) => {
        "relative" !== t4 || gc(o3) ? "fixed" !== t4 || hc(o3) ? "responsive" !== t4 || pc(o3) || yc(o3) : vc(o3) : bc(o3), kr(o3), Wa(e2, o3.dom, _a);
      });
    }))(n3), mceTableCellType: g2((e3) => "th" === e3 ? t2.makeCellsHeader : t2.unmakeCellsHeader), mceTableColType: g2((e3) => "th" === e3 ? t2.makeColumnsHeader : t2.unmakeColumnsHeader), mceTableRowType: g2((e3) => {
      switch (e3) {
        case "header":
          return t2.makeRowsHeader;
        case "footer":
          return t2.makeRowsFooter;
        default:
          return t2.makeRowsBody;
      }
    }) }, (t3, o3) => e2.addCommand(o3, t3)), e2.addCommand("mceInsertTable", (t3, o3) => {
      ((e3, t4, o4, n3 = {}) => {
        const r2 = (e4) => u(e4) && e4 > 0;
        if (r2(t4) && r2(o4)) {
          const r3 = n3.headerRows || 0, s2 = n3.headerColumns || 0;
          return Tc(e3, o4, t4, s2, r3);
        }
        console.error("Invalid values for mceInsertTable - rows and columns values are required to insert a table.");
      })(e2, o3.rows, o3.columns, o3.options);
    }), e2.addCommand("mceTableApplyCellStyle", (t3, o3) => {
      const l3 = (e3) => "tablecell" + e3.toLowerCase().replace("-", "");
      if (!s(o3)) return;
      const a3 = as(e2);
      if (0 === a3.length) return;
      const c3 = ((e3, t4) => {
        const o4 = {};
        return ((e4, t5, o5, n3) => {
          G(e4, (e5, r2) => {
            (t5(e5, r2) ? o5 : n3)(e5, r2);
          });
        })(e3, t4, /* @__PURE__ */ ((e4) => (t5, o5) => {
          e4[o5] = t5;
        })(o4), f), o4;
      })(o3, (t4, o4) => e2.formatter.has(l3(o4)) && r(t4));
      ((e3) => {
        for (const t4 in e3) if (U.call(e3, t4)) return false;
        return true;
      })(c3) || (G(c3, (t4, o4) => {
        const n3 = l3(o4);
        N(a3, (o5) => {
          "" === t4 ? e2.formatter.remove(n3, { value: null }, o5.dom, true) : e2.formatter.apply(n3, { value: t4 }, o5.dom);
        });
      }), n2(a3[0]).each((t4) => Wa(e2, t4.dom, La)));
    });
  }, Pc = Tl([{ before: ["element"] }, { on: ["element", "offset"] }, { after: ["element"] }]), Fc = { before: Pc.before, on: Pc.on, after: Pc.after, cata: (e2, t2, o2, n2) => e2.fold(t2, o2, n2), getStart: (e2) => e2.fold(h, h, h) }, Hc = (e2, t2) => ({ selection: e2, kill: t2 }), qc = (e2, t2) => {
    const o2 = e2.document.createRange();
    return o2.selectNode(t2.dom), o2;
  }, Vc = (e2, t2) => {
    const o2 = e2.document.createRange();
    return $c(o2, t2), o2;
  }, $c = (e2, t2) => e2.selectNodeContents(t2.dom), Uc = (e2, t2, o2) => {
    const n2 = e2.document.createRange();
    var r2;
    return r2 = n2, t2.fold((e3) => {
      r2.setStartBefore(e3.dom);
    }, (e3, t3) => {
      r2.setStart(e3.dom, t3);
    }, (e3) => {
      r2.setStartAfter(e3.dom);
    }), ((e3, t3) => {
      t3.fold((t4) => {
        e3.setEndBefore(t4.dom);
      }, (t4, o3) => {
        e3.setEnd(t4.dom, o3);
      }, (t4) => {
        e3.setEndAfter(t4.dom);
      });
    })(n2, o2), n2;
  }, Gc = (e2, t2, o2, n2, r2) => {
    const s2 = e2.document.createRange();
    return s2.setStart(t2.dom, o2), s2.setEnd(n2.dom, r2), s2;
  }, Kc = (e2) => ({ left: e2.left, top: e2.top, right: e2.right, bottom: e2.bottom, width: e2.width, height: e2.height }), Yc = Tl([{ ltr: ["start", "soffset", "finish", "foffset"] }, { rtl: ["start", "soffset", "finish", "foffset"] }]), Jc = (e2, t2, o2) => t2(pe.fromDom(o2.startContainer), o2.startOffset, pe.fromDom(o2.endContainer), o2.endOffset), Qc = (e2, t2) => {
    const o2 = ((e3, t3) => t3.match({ domRange: (e4) => ({ ltr: g(e4), rtl: C.none }), relative: (t4, o3) => ({ ltr: Gt(() => Uc(e3, t4, o3)), rtl: Gt(() => C.some(Uc(e3, o3, t4))) }), exact: (t4, o3, n2, r2) => ({ ltr: Gt(() => Gc(e3, t4, o3, n2, r2)), rtl: Gt(() => C.some(Gc(e3, n2, r2, t4, o3))) }) }))(e2, t2);
    return ((e3, t3) => {
      const o3 = t3.ltr();
      return o3.collapsed ? t3.rtl().filter((e4) => false === e4.collapsed).map((e4) => Yc.rtl(pe.fromDom(e4.endContainer), e4.endOffset, pe.fromDom(e4.startContainer), e4.startOffset)).getOrThunk(() => Jc(0, Yc.ltr, o3)) : Jc(0, Yc.ltr, o3);
    })(0, o2);
  }, Xc = (e2, t2) => Qc(e2, t2).match({ ltr: (t3, o2, n2, r2) => {
    const s2 = e2.document.createRange();
    return s2.setStart(t3.dom, o2), s2.setEnd(n2.dom, r2), s2;
  }, rtl: (t3, o2, n2, r2) => {
    const s2 = e2.document.createRange();
    return s2.setStart(n2.dom, r2), s2.setEnd(t3.dom, o2), s2;
  } });
  Yc.ltr, Yc.rtl;
  const Zc = (e2, t2, o2, n2) => ({ start: e2, soffset: t2, finish: o2, foffset: n2 }), ei = (e2, t2, o2, n2) => ({ start: Fc.on(e2, t2), finish: Fc.on(o2, n2) }), ti = (e2, t2) => {
    const o2 = Xc(e2, t2);
    return Zc(pe.fromDom(o2.startContainer), o2.startOffset, pe.fromDom(o2.endContainer), o2.endOffset);
  }, oi = ei, ni = (e2, t2, o2, n2, r2) => ye(o2, n2) ? C.none() : Gr(o2, n2, t2).bind((t3) => {
    const n3 = t3.boxes.getOr([]);
    return n3.length > 1 ? (r2(e2, n3, t3.start, t3.finish), C.some(Hc(C.some(oi(o2, 0, o2, dr(o2))), true))) : C.none();
  }), ri = (e2, t2) => ({ item: e2, mode: t2 }), si = (e2, t2, o2, n2 = li) => e2.property().parent(t2).map((e3) => ri(e3, n2)), li = (e2, t2, o2, n2 = ai) => o2.sibling(e2, t2).map((e3) => ri(e3, n2)), ai = (e2, t2, o2, n2 = ai) => {
    const r2 = e2.property().children(t2);
    return o2.first(r2).map((e3) => ri(e3, n2));
  }, ci = [{ current: si, next: li, fallback: C.none() }, { current: li, next: ai, fallback: C.some(si) }, { current: ai, next: ai, fallback: C.some(li) }], ii = (e2, t2, o2, n2, r2 = ci) => L(r2, (e3) => e3.current === o2).bind((o3) => o3.current(e2, t2, n2, o3.next).orThunk(() => o3.fallback.bind((o4) => ii(e2, t2, o4, n2)))), mi = (e2, t2, o2, n2, r2, s2) => ii(e2, t2, n2, r2).bind((t3) => s2(t3.item) ? C.none() : o2(t3.item) ? C.some(t3.item) : mi(e2, t3.item, o2, t3.mode, r2, s2)), di = (e2) => (t2) => 0 === e2.property().children(t2).length, ui = (e2, t2, o2, n2) => mi(e2, t2, o2, li, { sibling: (e3, t3) => e3.query().prevSibling(t3), first: (e3) => e3.length > 0 ? C.some(e3[e3.length - 1]) : C.none() }, n2), fi = (e2, t2, o2, n2) => mi(e2, t2, o2, li, { sibling: (e3, t3) => e3.query().nextSibling(t3), first: (e3) => e3.length > 0 ? C.some(e3[0]) : C.none() }, n2), gi = Fr(), hi = (e2, t2) => ((e3, t3, o2) => ui(e3, t3, di(e3), o2))(gi, e2, t2), pi = (e2, t2) => ((e3, t3, o2) => fi(e3, t3, di(e3), o2))(gi, e2, t2), wi = Tl([{ none: ["message"] }, { success: [] }, { failedUp: ["cell"] }, { failedDown: ["cell"] }]), bi = (e2) => dt(e2, "tr"), vi = { ...wi, verify: (e2, t2, o2, n2, r2, s2, l2) => dt(n2, "td,th", l2).bind((o3) => dt(t2, "td,th", l2).map((t3) => ye(o3, t3) ? ye(n2, o3) && dr(o3) === r2 ? s2(t3) : wi.none("in same cell") : $r(bi, [o3, t3]).fold(() => ((e3, t4, o4) => {
    const n3 = e3.getRect(t4), r3 = e3.getRect(o4);
    return r3.right > n3.left && r3.left < n3.right;
  })(e2, t3, o3) ? wi.success() : s2(t3), (e3) => s2(t3)))).getOr(wi.none("default")), cata: (e2, t2, o2, n2, r2) => e2.fold(t2, o2, n2, r2) }, yi = ae("br"), xi = (e2, t2, o2) => t2(e2, o2).bind((e3) => re(e3) && 0 === cr(e3).trim().length ? xi(e3, t2, o2) : C.some(e3)), Ci = (e2, t2, o2, n2) => ((e3, t3) => Ne(e3, t3).filter(yi).orThunk(() => Ne(e3, t3 - 1).filter(yi)))(t2, o2).bind((t3) => n2.traverse(t3).fold(() => xi(t3, n2.gather, e2).map(n2.relative), (e3) => ((e4) => Re(e4).bind((t4) => {
    const o3 = Ee(t4);
    return ((e5, t5) => _(e5, w(ye, t5)))(o3, e4).map((n3) => /* @__PURE__ */ ((e5, t5, o4, n4) => ({ parent: e5, children: t5, element: o4, index: n4 }))(t4, o3, e4, n3));
  }))(e3).map((e4) => Fc.on(e4.parent, e4.index)))), Si = (e2, t2) => ({ left: e2.left, top: e2.top + t2, right: e2.right, bottom: e2.bottom + t2 }), Ti = (e2, t2) => ({ left: e2.left, top: e2.top - t2, right: e2.right, bottom: e2.bottom - t2 }), Ri = (e2, t2, o2) => ({ left: e2.left + t2, top: e2.top + o2, right: e2.right + t2, bottom: e2.bottom + o2 }), Di = (e2) => ({ left: e2.left, top: e2.top, right: e2.right, bottom: e2.bottom }), Oi = (e2, t2) => C.some(e2.getRect(t2)), ki = (e2, t2, o2) => ne(t2) ? Oi(e2, t2).map(Di) : re(t2) ? ((e3, t3, o3) => o3 >= 0 && o3 < dr(t3) ? e3.getRangedRect(t3, o3, t3, o3 + 1) : o3 > 0 ? e3.getRangedRect(t3, o3 - 1, t3, o3) : C.none())(e2, t2, o2).map(Di) : C.none(), Ei = (e2, t2) => ne(t2) ? Oi(e2, t2).map(Di) : re(t2) ? e2.getRangedRect(t2, 0, t2, dr(t2)).map(Di) : C.none(), Ni = Tl([{ none: [] }, { retry: ["caret"] }]), Bi = (e2, t2, o2) => {
    return (n2 = t2, r2 = Fl, lt((e3, t3) => t3(e3), at, n2, r2, void 0)).fold(y, (t3) => Ei(e2, t3).exists((e3) => ((e4, t4) => e4.left < t4.left || Math.abs(t4.right - e4.left) < 1 || e4.left > t4.right)(o2, e3)));
    var n2, r2;
  }, zi = { point: (e2) => e2.bottom, adjuster: (e2, t2, o2, n2, r2) => {
    const s2 = Si(r2, 5);
    return Math.abs(o2.bottom - n2.bottom) < 1 || o2.top > r2.bottom ? Ni.retry(s2) : o2.top === r2.bottom ? Ni.retry(Si(r2, 1)) : Bi(e2, t2, r2) ? Ni.retry(Ri(s2, 5, 0)) : Ni.none();
  }, move: Si, gather: pi }, Ai = (e2, t2, o2, n2, r2) => 0 === r2 ? C.some(n2) : ((e3, t3, o3) => e3.elementFromPoint(t3, o3).filter((e4) => "table" === Z(e4)).isSome())(e2, n2.left, t2.point(n2)) ? ((e3, t3, o3, n3, r3) => Ai(e3, t3, o3, t3.move(n3, 5), r3))(e2, t2, o2, n2, r2 - 1) : e2.situsFromPoint(n2.left, t2.point(n2)).bind((s2) => s2.start.fold(C.none, (s3) => Ei(e2, s3).bind((l2) => t2.adjuster(e2, s3, l2, o2, n2).fold(C.none, (n3) => Ai(e2, t2, o2, n3, r2 - 1))).orThunk(() => C.some(n2)), C.none)), Wi = (e2, t2, o2) => {
    const n2 = e2.move(o2, 5), r2 = Ai(t2, e2, o2, n2, 100).getOr(n2);
    return ((e3, t3, o3) => e3.point(t3) > o3.getInnerHeight() ? C.some(e3.point(t3) - o3.getInnerHeight()) : e3.point(t3) < 0 ? C.some(-e3.point(t3)) : C.none())(e2, r2, t2).fold(() => t2.situsFromPoint(r2.left, e2.point(r2)), (o3) => (t2.scrollBy(0, o3), t2.situsFromPoint(r2.left, e2.point(r2) - o3)));
  }, Li = { tryUp: w(Wi, { point: (e2) => e2.top, adjuster: (e2, t2, o2, n2, r2) => {
    const s2 = Ti(r2, 5);
    return Math.abs(o2.top - n2.top) < 1 || o2.bottom < r2.top ? Ni.retry(s2) : o2.bottom === r2.top ? Ni.retry(Ti(r2, 1)) : Bi(e2, t2, r2) ? Ni.retry(Ri(s2, 5, 0)) : Ni.none();
  }, move: Ti, gather: hi }), tryDown: w(Wi, zi), getJumpSize: g(5) }, _i = (e2, t2, o2) => e2.getSelection().bind((n2) => ((e3, t3, o3, n3) => {
    const r2 = yi(t3) ? ((e4, t4, o4) => o4.traverse(t4).orThunk(() => xi(t4, o4.gather, e4)).map(o4.relative))(e3, t3, n3) : Ci(e3, t3, o3, n3);
    return r2.map((e4) => ({ start: e4, finish: e4 }));
  })(t2, n2.finish, n2.foffset, o2).fold(() => C.some(is(n2.finish, n2.foffset)), (r2) => {
    const s2 = e2.fromSitus(r2);
    return l2 = vi.verify(e2, n2.finish, n2.foffset, s2.finish, s2.foffset, o2.failure, t2), vi.cata(l2, (e3) => C.none(), () => C.none(), (e3) => C.some(is(e3, 0)), (e3) => C.some(is(e3, dr(e3))));
    var l2;
  })), Mi = (e2, t2, o2, n2, r2, s2) => 0 === s2 ? C.none() : Pi(e2, t2, o2, n2, r2).bind((l2) => {
    const a2 = e2.fromSitus(l2), c2 = vi.verify(e2, o2, n2, a2.finish, a2.foffset, r2.failure, t2);
    return vi.cata(c2, () => C.none(), () => C.some(l2), (l3) => ye(o2, l3) && 0 === n2 ? ji(e2, o2, n2, Ti, r2) : Mi(e2, t2, l3, 0, r2, s2 - 1), (l3) => ye(o2, l3) && n2 === dr(l3) ? ji(e2, o2, n2, Si, r2) : Mi(e2, t2, l3, dr(l3), r2, s2 - 1));
  }), ji = (e2, t2, o2, n2, r2) => ki(e2, t2, o2).bind((t3) => Ii(e2, r2, n2(t3, Li.getJumpSize()))), Ii = (e2, t2, o2) => {
    const n2 = To().browser;
    return n2.isChromium() || n2.isSafari() || n2.isFirefox() ? t2.retry(e2, o2) : C.none();
  }, Pi = (e2, t2, o2, n2, r2) => ki(e2, o2, n2).bind((t3) => Ii(e2, r2, t3)), Fi = (e2, t2, o2, n2, r2) => dt(n2, "td,th", t2).bind((n3) => dt(n3, "table", t2).bind((s2) => ((e3, t3) => at(e3, (e4) => Re(e4).exists((e5) => ye(e5, t3)), void 0).isSome())(r2, s2) ? ((e3, t3, o3) => _i(e3, t3, o3).bind((n4) => Mi(e3, t3, n4.element, n4.offset, o3, 20).map(e3.fromSitus)))(e2, t2, o2).bind((e3) => dt(e3.finish, "td,th", t2).map((t3) => ({ start: n3, finish: t3, range: e3 }))) : C.none())), Hi = (e2, t2, o2, n2, r2, s2) => s2(n2, t2).orThunk(() => Fi(e2, t2, o2, n2, r2).map((e3) => {
    const t3 = e3.range;
    return Hc(C.some(oi(t3.start, t3.soffset, t3.finish, t3.foffset)), true);
  })), qi = (e2, t2) => dt(e2, "tr", t2).bind((e3) => dt(e3, "table", t2).bind((o2) => {
    const n2 = st(o2, "tr");
    return ye(e3, n2[0]) ? ((e4, t3, o3) => ui(gi, e4, (e5) => hr(e5).isSome(), o3))(o2, 0, t2).map((e4) => {
      const t3 = dr(e4);
      return Hc(C.some(oi(e4, t3, e4, t3)), true);
    }) : C.none();
  })), Vi = (e2, t2) => dt(e2, "tr", t2).bind((e3) => dt(e3, "table", t2).bind((o2) => {
    const n2 = st(o2, "tr");
    return ye(e3, n2[n2.length - 1]) ? ((e4, t3, o3) => fi(gi, e4, (e5) => gr(e5).isSome(), o3))(o2, 0, t2).map((e4) => Hc(C.some(oi(e4, 0, e4, 0)), true)) : C.none();
  })), $i = (e2, t2, o2, n2, r2, s2, l2) => Fi(e2, o2, n2, r2, s2).bind((e3) => ni(t2, o2, e3.start, e3.finish, l2)), Ui = (e2) => {
    let t2 = e2;
    return { get: () => t2, set: (e3) => {
      t2 = e3;
    } };
  }, Gi = () => {
    const e2 = ((e3) => {
      const t2 = Ui(C.none()), o2 = () => t2.get().each(e3);
      return { clear: () => {
        o2(), t2.set(C.none());
      }, isSet: () => t2.get().isSome(), get: () => t2.get(), set: (e4) => {
        o2(), t2.set(C.some(e4));
      } };
    })(f);
    return { ...e2, on: (t2) => e2.get().each(t2) };
  }, Ki = (e2, t2) => dt(e2, "td,th", t2), Yi = { traverse: ke, gather: pi, relative: Fc.before, retry: Li.tryDown, failure: vi.failedDown }, Ji = { traverse: Oe, gather: hi, relative: Fc.before, retry: Li.tryUp, failure: vi.failedUp }, Qi = (e2) => (t2) => t2 === e2, Xi = Qi(38), Zi = Qi(40), em = (e2) => e2 >= 37 && e2 <= 40, tm = { isBackward: Qi(37), isForward: Qi(39) }, om = { isBackward: Qi(39), isForward: Qi(37) }, nm = Tl([{ domRange: ["rng"] }, { relative: ["startSitu", "finishSitu"] }, { exact: ["start", "soffset", "finish", "foffset"] }]), rm = { domRange: nm.domRange, relative: nm.relative, exact: nm.exact, exactFromRange: (e2) => nm.exact(e2.start, e2.soffset, e2.finish, e2.foffset), getWin: (e2) => {
    const t2 = ((e3) => e3.match({ domRange: (e4) => pe.fromDom(e4.startContainer), relative: (e4, t3) => Fc.getStart(e4), exact: (e4, t3, o2, n2) => e4 }))(e2);
    return pe.fromDom(Te(t2).dom.defaultView);
  }, range: Zc }, sm = document.caretPositionFromPoint ? (e2, t2, o2) => {
    var n2, r2;
    return C.from(null === (r2 = (n2 = e2.dom).caretPositionFromPoint) || void 0 === r2 ? void 0 : r2.call(n2, t2, o2)).bind((t3) => {
      if (null === t3.offsetNode) return C.none();
      const o3 = e2.dom.createRange();
      return o3.setStart(t3.offsetNode, t3.offset), o3.collapse(), C.some(o3);
    });
  } : document.caretRangeFromPoint ? (e2, t2, o2) => {
    var n2, r2;
    return C.from(null === (r2 = (n2 = e2.dom).caretRangeFromPoint) || void 0 === r2 ? void 0 : r2.call(n2, t2, o2));
  } : C.none, lm = (e2, t2) => {
    const o2 = Z(e2);
    return "input" === o2 ? Fc.after(e2) : D(["br", "img"], o2) ? 0 === t2 ? Fc.before(e2) : Fc.after(e2) : Fc.on(e2, t2);
  }, am = (e2) => C.from(e2.getSelection()), cm = (e2, t2) => {
    am(e2).each((e3) => {
      e3.removeAllRanges(), e3.addRange(t2);
    });
  }, im = (e2, t2, o2, n2, r2) => {
    const s2 = Gc(e2, t2, o2, n2, r2);
    cm(e2, s2);
  }, mm = (e2, t2) => Qc(e2, t2).match({ ltr: (t3, o2, n2, r2) => {
    im(e2, t3, o2, n2, r2);
  }, rtl: (t3, o2, n2, r2) => {
    am(e2).each((s2) => {
      if (s2.setBaseAndExtent) s2.setBaseAndExtent(t3.dom, o2, n2.dom, r2);
      else if (s2.extend) try {
        ((e3, t4, o3, n3, r3, s3) => {
          t4.collapse(o3.dom, n3), t4.extend(r3.dom, s3);
        })(0, s2, t3, o2, n2, r2);
      } catch (s3) {
        im(e2, n2, r2, t3, o2);
      }
      else im(e2, n2, r2, t3, o2);
    });
  } }), dm = (e2, t2, o2, n2, r2) => {
    const s2 = ((e3, t3, o3, n3) => {
      const r3 = lm(e3, t3), s3 = lm(o3, n3);
      return rm.relative(r3, s3);
    })(t2, o2, n2, r2);
    mm(e2, s2);
  }, um = (e2, t2, o2) => {
    const n2 = ((e3, t3) => {
      const o3 = e3.fold(Fc.before, lm, Fc.after), n3 = t3.fold(Fc.before, lm, Fc.after);
      return rm.relative(o3, n3);
    })(t2, o2);
    mm(e2, n2);
  }, fm = (e2) => {
    if (e2.rangeCount > 0) {
      const t2 = e2.getRangeAt(0), o2 = e2.getRangeAt(e2.rangeCount - 1);
      return C.some(Zc(pe.fromDom(t2.startContainer), t2.startOffset, pe.fromDom(o2.endContainer), o2.endOffset));
    }
    return C.none();
  }, gm = (e2) => {
    if (null === e2.anchorNode || null === e2.focusNode) return fm(e2);
    {
      const t2 = pe.fromDom(e2.anchorNode), o2 = pe.fromDom(e2.focusNode);
      return ((e3, t3, o3, n2) => {
        const r2 = ((e4, t4, o4, n3) => {
          const r3 = Se(e4).dom.createRange();
          return r3.setStart(e4.dom, t4), r3.setEnd(o4.dom, n3), r3;
        })(e3, t3, o3, n2), s2 = ye(e3, o3) && t3 === n2;
        return r2.collapsed && !s2;
      })(t2, e2.anchorOffset, o2, e2.focusOffset) ? C.some(Zc(t2, e2.anchorOffset, o2, e2.focusOffset)) : fm(e2);
    }
  }, hm = (e2, t2, o2 = true) => {
    const n2 = (o2 ? Vc : qc)(e2, t2);
    cm(e2, n2);
  }, pm = (e2) => ((e3) => am(e3).filter((e4) => e4.rangeCount > 0).bind(gm))(e2).map((e3) => rm.exact(e3.start, e3.soffset, e3.finish, e3.foffset)), wm = (e2) => ({ elementFromPoint: (t2, o2) => pe.fromPoint(pe.fromDom(e2.document), t2, o2), getRect: (e3) => e3.dom.getBoundingClientRect(), getRangedRect: (t2, o2, n2, r2) => {
    const s2 = rm.exact(t2, o2, n2, r2);
    return ((e3, t3) => ((e4) => {
      const t4 = e4.getClientRects(), o3 = t4.length > 0 ? t4[0] : e4.getBoundingClientRect();
      return o3.width > 0 || o3.height > 0 ? C.some(o3).map(Kc) : C.none();
    })(Xc(e3, t3)))(e2, s2);
  }, getSelection: () => pm(e2).map((t2) => ti(e2, t2)), fromSitus: (t2) => {
    const o2 = rm.relative(t2.start, t2.finish);
    return ti(e2, o2);
  }, situsFromPoint: (t2, o2) => ((e3, t3, o3) => ((e4, t4, o4) => {
    const n2 = pe.fromDom(e4.document);
    return sm(n2, t4, o4).map((e5) => Zc(pe.fromDom(e5.startContainer), e5.startOffset, pe.fromDom(e5.endContainer), e5.endOffset));
  })(e3, t3, o3))(e2, t2, o2).map((e3) => ei(e3.start, e3.soffset, e3.finish, e3.foffset)), clearSelection: () => {
    ((e3) => {
      am(e3).each((e4) => e4.removeAllRanges());
    })(e2);
  }, collapseSelection: (t2 = false) => {
    pm(e2).each((o2) => o2.fold((e3) => e3.collapse(t2), (o3, n2) => {
      const r2 = t2 ? o3 : n2;
      um(e2, r2, r2);
    }, (o3, n2, r2, s2) => {
      const l2 = t2 ? o3 : r2, a2 = t2 ? n2 : s2;
      dm(e2, l2, a2, l2, a2);
    }));
  }, setSelection: (t2) => {
    dm(e2, t2.start, t2.soffset, t2.finish, t2.foffset);
  }, setRelativeSelection: (t2, o2) => {
    um(e2, t2, o2);
  }, selectNode: (t2) => {
    hm(e2, t2, false);
  }, selectContents: (t2) => {
    hm(e2, t2);
  }, getInnerHeight: () => e2.innerHeight, getScrollY: () => ((e3) => {
    const t2 = void 0 !== e3 ? e3.dom : document, o2 = t2.body.scrollLeft || t2.documentElement.scrollLeft, n2 = t2.body.scrollTop || t2.documentElement.scrollTop;
    return dn(o2, n2);
  })(pe.fromDom(e2.document)).top, scrollBy: (t2, o2) => {
    ((e3, t3, o3) => {
      const n2 = (void 0 !== o3 ? o3.dom : document).defaultView;
      n2 && n2.scrollBy(e3, t3);
    })(t2, o2, pe.fromDom(e2.document));
  } }), bm = (e2, t2) => ({ rows: e2, cols: t2 }), vm = (e2) => void 0 !== e2.dom.classList, ym = (e2, t2) => ((e3, t3, o2) => {
    const n2 = ((e4, t4) => {
      const o3 = de(e4, t4);
      return void 0 === o3 || "" === o3 ? [] : o3.split(" ");
    })(e3, t3).concat([o2]);
    return ie(e3, t3, n2.join(" ")), true;
  })(e2, "class", t2), xm = (e2, t2) => {
    vm(e2) ? e2.dom.classList.add(t2) : ym(e2, t2);
  }, Cm = (e2, t2) => vm(e2) && e2.dom.classList.contains(t2), Sm = () => ({ tag: "none" }), Tm = (e2) => ({ tag: "multiple", elements: e2 }), Rm = (e2) => ({ tag: "single", element: e2 }), Dm = (e2) => {
    const t2 = pe.fromDom(((e3) => {
      if (Qe() && m(e3.target)) {
        const t3 = pe.fromDom(e3.target);
        if (ne(t3) && m(t3.dom.shadowRoot) && e3.composed && e3.composedPath) {
          const t4 = e3.composedPath();
          if (t4) return H(t4);
        }
      }
      return C.from(e3.target);
    })(e2).getOr(e2.target)), o2 = () => e2.stopPropagation(), n2 = () => e2.preventDefault(), r2 = (s2 = n2, l2 = o2, (...e3) => s2(l2.apply(null, e3)));
    var s2, l2;
    return /* @__PURE__ */ ((e3, t3, o3, n3, r3, s3, l3) => ({ target: e3, x: t3, y: o3, stop: n3, prevent: r3, kill: s3, raw: l3 }))(t2, e2.clientX, e2.clientY, o2, n2, r2, e2);
  }, Om = (e2, t2, o2, n2) => {
    e2.dom.removeEventListener(t2, o2, n2);
  }, km = x, Em = (e2, t2, o2) => ((e3, t3, o3, n2) => ((e4, t4, o4, n3, r2) => {
    const s2 = /* @__PURE__ */ ((e5, t5) => (o5) => {
      e5(o5) && t5(Dm(o5));
    })(o4, n3);
    return e4.dom.addEventListener(t4, s2, r2), { unbind: w(Om, e4, t4, s2, r2) };
  })(e3, t3, o3, n2, false))(e2, t2, km, o2), Nm = Dm, Bm = (e2) => !Cm(pe.fromDom(e2.target), "ephox-snooker-resizer-bar"), zm = (e2, t2) => {
    const o2 = (r2 = os.selectedSelector, { get: () => Qr(pe.fromDom(e2.getBody()), r2).fold(() => ls(Er(e2), Or(e2)).fold(Sm, Rm), Tm) }), n2 = ((e3, t3, o3) => {
      const n3 = (t4) => {
        fe(t4, e3.selected), fe(t4, e3.firstSelected), fe(t4, e3.lastSelected);
      }, r3 = (t4) => {
        ie(t4, e3.selected, "1");
      }, s2 = (e4) => {
        l2(e4), o3();
      }, l2 = (t4) => {
        const o4 = st(t4, `${e3.selectedSelector},${e3.firstSelectedSelector},${e3.lastSelectedSelector}`);
        N(o4, n3);
      };
      return { clearBeforeUpdate: l2, clear: s2, selectRange: (o4, n4, l3, a2) => {
        s2(o4), N(n4, r3), ie(l3, e3.firstSelected, "1"), ie(a2, e3.lastSelected, "1"), t3(n4, l3, a2);
      }, selectedSelector: e3.selectedSelector, firstSelectedSelector: e3.firstSelectedSelector, lastSelectedSelector: e3.lastSelectedSelector };
    })(os, (t3, o3, n3) => {
      Ft(o3).each((r3) => {
        const s2 = Fa(e2), l2 = Tr(f, pe.fromDom(e2.getDoc()), s2), a2 = ((e3, t4, o4) => {
          const n4 = Uo(e3);
          return Xs(n4, t4).map((e4) => {
            const t5 = Us(n4, o4, false), { rows: r4 } = jo(t5), s3 = ((e5, t6) => {
              const o5 = e5.slice(0, t6[t6.length - 1].row + 1), n5 = Gs(o5);
              return j(n5, (e6) => {
                const o6 = e6.cells.slice(0, t6[t6.length - 1].column + 1);
                return E(o6, (e7) => e7.element);
              });
            })(r4, e4), l3 = ((e5, t6) => {
              const o5 = e5.slice(t6[0].row + t6[0].rowspan - 1, e5.length), n5 = Gs(o5);
              return j(n5, (e6) => {
                const o6 = e6.cells.slice(t6[0].column + t6[0].colspan - 1, e6.cells.length);
                return E(o6, (e7) => e7.element);
              });
            })(r4, e4);
            return { upOrLeftCells: s3, downOrRightCells: l3 };
          });
        })(r3, { selection: as(e2) }, l2);
        ((e3, t4, o4, n4, r4) => {
          e3.dispatch("TableSelectionChange", { cells: t4, start: o4, finish: n4, otherCells: r4 });
        })(e2, t3, o3, n3, a2);
      });
    }, () => ((e3) => {
      e3.dispatch("TableSelectionClear");
    })(e2));
    var r2;
    return e2.on("init", (o3) => {
      const r3 = e2.getWin(), s2 = Dr(e2), l2 = Or(e2), a2 = ((e3, t3, o4, n3) => {
        const r4 = ((e4, t4, o5, n4) => {
          const r5 = Gi(), s3 = r5.clear, l3 = (s4) => {
            r5.on((r6) => {
              n4.clearBeforeUpdate(t4), Ki(s4.target, o5).each((l4) => {
                Gr(r6, l4, o5).each((o6) => {
                  const r7 = o6.boxes.getOr([]);
                  if (1 === r7.length) {
                    const o7 = r7[0], l5 = "false" === Ps(o7), a3 = ut(js(s4.target), o7, ye);
                    l5 && a3 && (n4.selectRange(t4, r7, o7, o7), e4.selectContents(o7));
                  } else r7.length > 1 && (n4.selectRange(t4, r7, o6.start, o6.finish), e4.selectContents(l4));
                });
              });
            });
          };
          return { clearstate: s3, mousedown: (e5) => {
            n4.clear(t4), Ki(e5.target, o5).each(r5.set);
          }, mouseover: (e5) => {
            l3(e5);
          }, mouseup: (e5) => {
            l3(e5), s3();
          } };
        })(wm(e3), t3, o4, n3);
        return { clearstate: r4.clearstate, mousedown: r4.mousedown, mouseover: r4.mouseover, mouseup: r4.mouseup };
      })(r3, s2, l2, n2), c2 = ((e3, t3, o4, n3) => {
        const r4 = wm(e3), s3 = () => (n3.clear(t3), C.none());
        return { keydown: (e4, l3, a3, c3, i4, m3) => {
          const d3 = e4.raw, u3 = d3.which, f2 = true === d3.shiftKey, g2 = Kr(t3, n3.selectedSelector).fold(() => (em(u3) && !f2 && n3.clearBeforeUpdate(t3), Zi(u3) && f2 ? w($i, r4, t3, o4, Yi, c3, l3, n3.selectRange) : Xi(u3) && f2 ? w($i, r4, t3, o4, Ji, c3, l3, n3.selectRange) : Zi(u3) ? w(Hi, r4, o4, Yi, c3, l3, Vi) : Xi(u3) ? w(Hi, r4, o4, Ji, c3, l3, qi) : C.none), (e5) => {
            const o5 = (o6) => () => {
              const s4 = V(o6, (o7) => ((e6, t4, o8, n4, r5) => Jr(n4, e6, t4, r5.firstSelectedSelector, r5.lastSelectedSelector).map((e7) => (r5.clearBeforeUpdate(o8), r5.selectRange(o8, e7.boxes, e7.start, e7.finish), e7.boxes)))(o7.rows, o7.cols, t3, e5, n3));
              return s4.fold(() => Yr(t3, n3.firstSelectedSelector, n3.lastSelectedSelector).map((e6) => {
                const o7 = Zi(u3) || m3.isForward(u3) ? Fc.after : Fc.before;
                return r4.setRelativeSelection(Fc.on(e6.first, 0), o7(e6.table)), n3.clear(t3), Hc(C.none(), true);
              }), (e6) => C.some(Hc(C.none(), true)));
            };
            return Zi(u3) && f2 ? o5([bm(1, 0)]) : Xi(u3) && f2 ? o5([bm(-1, 0)]) : m3.isBackward(u3) && f2 ? o5([bm(0, -1), bm(-1, 0)]) : m3.isForward(u3) && f2 ? o5([bm(0, 1), bm(1, 0)]) : em(u3) && !f2 ? s3 : C.none;
          });
          return g2();
        }, keyup: (e4, r5, s4, l3, a3) => Kr(t3, n3.selectedSelector).fold(() => {
          const c3 = e4.raw, i4 = c3.which;
          return true === c3.shiftKey && em(i4) ? ((e5, t4, o5, n4, r6, s5, l4) => ye(o5, r6) && n4 === s5 ? C.none() : dt(o5, "td,th", t4).bind((o6) => dt(r6, "td,th", t4).bind((n5) => ni(e5, t4, o6, n5, l4))))(t3, o4, r5, s4, l3, a3, n3.selectRange) : C.none();
        }, C.none) };
      })(r3, s2, l2, n2), i3 = ((e3, t3, o4, n3) => {
        const r4 = wm(e3);
        return (e4, s3) => {
          n3.clearBeforeUpdate(t3), Gr(e4, s3, o4).each((e5) => {
            const o5 = e5.boxes.getOr([]);
            n3.selectRange(t3, o5, e5.start, e5.finish), r4.selectContents(s3), r4.collapseSelection();
          });
        };
      })(r3, s2, l2, n2);
      e2.on("TableSelectorChange", (e3) => i3(e3.start, e3.finish));
      const m2 = (t3, o4) => {
        ((e3) => true === e3.raw.shiftKey)(t3) && (o4.kill && t3.kill(), o4.selection.each((t4) => {
          const o5 = rm.relative(t4.start, t4.finish), n3 = Xc(r3, o5);
          e2.selection.setRng(n3);
        }));
      }, d2 = (e3) => 0 === e3.button, u2 = (() => {
        const e3 = Ui(pe.fromDom(s2)), t3 = Ui(0);
        return { touchEnd: (o4) => {
          const n3 = pe.fromDom(o4.target);
          if (ae("td")(n3) || ae("th")(n3)) {
            const r4 = e3.get(), s3 = t3.get();
            ye(r4, n3) && o4.timeStamp - s3 < 300 && (o4.preventDefault(), i3(n3, n3));
          }
          e3.set(n3), t3.set(o4.timeStamp);
        } };
      })();
      e2.on("dragstart", (e3) => {
        a2.clearstate();
      }), e2.on("mousedown", (e3) => {
        d2(e3) && Bm(e3) && a2.mousedown(Nm(e3));
      }), e2.on("mouseover", (e3) => {
        var t3;
        void 0 !== (t3 = e3).buttons && 0 == (1 & t3.buttons) || !Bm(e3) || a2.mouseover(Nm(e3));
      }), e2.on("mouseup", (e3) => {
        d2(e3) && Bm(e3) && a2.mouseup(Nm(e3));
      }), e2.on("touchend", u2.touchEnd), e2.on("keyup", (t3) => {
        const o4 = Nm(t3);
        if (o4.raw.shiftKey && em(o4.raw.which)) {
          const t4 = e2.selection.getRng(), n3 = pe.fromDom(t4.startContainer), r4 = pe.fromDom(t4.endContainer);
          c2.keyup(o4, n3, t4.startOffset, r4, t4.endOffset).each((e3) => {
            m2(o4, e3);
          });
        }
      }), e2.on("keydown", (o4) => {
        const n3 = Nm(o4);
        t2.hide();
        const r4 = e2.selection.getRng(), s3 = pe.fromDom(r4.startContainer), l3 = pe.fromDom(r4.endContainer), a3 = rn(tm, om)(pe.fromDom(e2.selection.getStart()));
        c2.keydown(n3, s3, r4.startOffset, l3, r4.endOffset, a3).each((e3) => {
          m2(n3, e3);
        }), t2.show();
      }), e2.on("NodeChange", () => {
        const t3 = e2.selection, o4 = pe.fromDom(t3.getStart()), r4 = pe.fromDom(t3.getEnd());
        $r(Ft, [o4, r4]).fold(() => n2.clear(s2), f);
      });
    }), e2.on("PreInit", () => {
      e2.serializer.addTempAttr(os.firstSelected), e2.serializer.addTempAttr(os.lastSelected);
    }), { getSelectedCells: () => ((e3, t3, o3, n3) => {
      switch (e3.tag) {
        case "none":
          return t3();
        case "single":
          return ((e4) => [e4.dom])(e3.element);
        case "multiple":
          return ((e4) => E(e4, (e5) => e5.dom))(e3.elements);
      }
    })(o2.get(), g([])), clearSelectedCells: (e3) => n2.clear(pe.fromDom(e3)) };
  }, Am = (e2) => {
    let t2 = [];
    return { bind: (e3) => {
      if (void 0 === e3) throw new Error("Event bind error: undefined handler");
      t2.push(e3);
    }, unbind: (e3) => {
      t2 = z(t2, (t3) => t3 !== e3);
    }, trigger: (...o2) => {
      const n2 = {};
      N(e2, (e3, t3) => {
        n2[e3] = o2[t3];
      }), N(t2, (e3) => {
        e3(n2);
      });
    } };
  }, Wm = (e2) => ({ registry: K(e2, (e3) => ({ bind: e3.bind, unbind: e3.unbind })), trigger: K(e2, (e3) => e3.trigger) }), Lm = (e2) => e2.slice(0).sort(), _m = (e2, t2) => {
    const o2 = z(t2, (t3) => !D(e2, t3));
    o2.length > 0 && ((e3) => {
      throw new Error("Unsupported keys for object: " + Lm(e3).join(", "));
    })(o2);
  }, Mm = (e2) => ((e3, t2) => ((e4, t3, o2) => {
    if (0 === t3.length) throw new Error("You must specify at least one required field.");
    return ((e5, t4) => {
      if (!l(t4)) throw new Error("The required fields must be an array. Was: " + t4 + ".");
      N(t4, (t5) => {
        if (!r(t5)) throw new Error("The value " + t5 + " in the " + e5 + " fields was not a string.");
      });
    })("required", t3), ((e5) => {
      const t4 = Lm(e5);
      L(t4, (e6, o3) => o3 < t4.length - 1 && e6 === t4[o3 + 1]).each((e6) => {
        throw new Error("The field: " + e6 + " occurs more than once in the combined fields: [" + t4.join(", ") + "].");
      });
    })(t3), (n2) => {
      const r2 = $2(n2);
      I(t3, (e5) => D(r2, e5)) || ((e5, t4) => {
        throw new Error("All required keys (" + Lm(e5).join(", ") + ") were not specified. Specified keys were: " + Lm(t4).join(", ") + ".");
      })(t3, r2), e4(t3, r2);
      const s2 = z(t3, (e5) => !o2.validate(n2[e5], e5));
      return s2.length > 0 && ((e5, t4) => {
        throw new Error("All values need to be of type: " + t4 + ". Keys (" + Lm(e5).join(", ") + ") were not.");
      })(s2, o2.label), n2;
    };
  })(e3, t2, { validate: d, label: "function" }))(_m, e2), jm = Mm(["compare", "extract", "mutate", "sink"]), Im = Mm(["element", "start", "stop", "destroy"]), Pm = Mm(["forceDrop", "drop", "move", "delayDrop"]), Fm = () => {
    const e2 = (() => {
      const e3 = Wm({ move: Am(["info"]) });
      return { onEvent: f, reset: f, events: e3.registry };
    })(), t2 = (() => {
      let e3 = C.none();
      const t3 = Wm({ move: Am(["info"]) });
      return { onEvent: (o3, n2) => {
        n2.extract(o3).each((o4) => {
          const r2 = ((t4, o5) => {
            const n3 = e3.map((e4) => t4.compare(e4, o5));
            return e3 = C.some(o5), n3;
          })(n2, o4);
          r2.each((e4) => {
            t3.trigger.move(e4);
          });
        });
      }, reset: () => {
        e3 = C.none();
      }, events: t3.registry };
    })();
    let o2 = e2;
    return { on: () => {
      o2.reset(), o2 = t2;
    }, off: () => {
      o2.reset(), o2 = e2;
    }, isOn: () => o2 === t2, onEvent: (e3, t3) => {
      o2.onEvent(e3, t3);
    }, events: t2.events };
  }, Hm = (e2) => {
    const t2 = e2.replace(/\./g, "-");
    return { resolve: (e3) => t2 + "-" + e3 };
  }, qm = Hm("ephox-dragster").resolve;
  var Vm = jm({ compare: (e2, t2) => dn(t2.left - e2.left, t2.top - e2.top), extract: (e2) => C.some(dn(e2.x, e2.y)), sink: (e2, t2) => {
    const o2 = ((e3) => {
      const t3 = { layerClass: qm("blocker"), ...e3 }, o3 = pe.fromTag("div");
      return ie(o3, "role", "presentation"), Tt(o3, { position: "fixed", left: "0px", top: "0px", width: "100%", height: "100%" }), xm(o3, qm("blocker")), xm(o3, t3.layerClass), { element: g(o3), destroy: () => {
        Ie(o3);
      } };
    })(t2), n2 = Em(o2.element(), "mousedown", e2.forceDrop), r2 = Em(o2.element(), "mouseup", e2.drop), s2 = Em(o2.element(), "mousemove", e2.move), l2 = Em(o2.element(), "mouseout", e2.delayDrop);
    return Im({ element: o2.element, start: (e3) => {
      We(e3, o2.element());
    }, stop: () => {
      Ie(o2.element());
    }, destroy: () => {
      o2.destroy(), r2.unbind(), s2.unbind(), l2.unbind(), n2.unbind();
    } });
  }, mutate: (e2, t2) => {
    e2.mutate(t2.left, t2.top);
  } });
  const $m = Hm("ephox-snooker").resolve, Um = $m("resizer-bar"), Gm = $m("resizer-rows"), Km = $m("resizer-cols"), Ym = (e2) => {
    const t2 = st(e2.parent(), "." + Um);
    N(t2, Ie);
  }, Jm = (e2, t2, o2) => {
    const n2 = e2.origin();
    N(t2, (t3) => {
      t3.each((t4) => {
        const r2 = o2(n2, t4);
        xm(r2, Um), We(e2.parent(), r2);
      });
    });
  }, Qm = (e2, t2, o2, n2, r2) => {
    const s2 = fn(o2), l2 = t2.isResizable, a2 = n2.length > 0 ? Rn.positions(n2, o2) : [], c2 = a2.length > 0 ? ((e3, t3) => j(e3.all, (e4, o3) => t3(e4.element) ? [o3] : []))(e2, l2) : [];
    ((e3, t3, o3, n3) => {
      Jm(e3, t3, (e4, t4) => {
        const r3 = ((e5, t5, o4, n4, r4) => {
          const s3 = pe.fromTag("div");
          return Tt(s3, { position: "absolute", left: t5 + "px", top: o4 - 3.5 + "px", height: "7px", width: n4 + "px" }), me(s3, { "data-row": e5, role: "presentation" }), s3;
        })(t4.row, o3.left - e4.left, t4.y - e4.top, n3);
        return xm(r3, Gm), r3;
      });
    })(t2, z(a2, (e3, t3) => O(c2, (e4) => t3 === e4)), s2, Eo(o2));
    const i3 = r2.length > 0 ? On.positions(r2, o2) : [], m2 = i3.length > 0 ? ((e3, t3) => {
      const o3 = [];
      return k(e3.grid.columns, (n3) => {
        en(e3, n3).map((e4) => e4.element).forall(t3) && o3.push(n3);
      }), z(o3, (o4) => {
        const n3 = Jo(e3, (e4) => e4.column === o4);
        return I(n3, (e4) => t3(e4.element));
      });
    })(e2, l2) : [];
    ((e3, t3, o3, n3) => {
      Jm(e3, t3, (e4, t4) => {
        const r3 = ((e5, t5, o4, n4, r4) => {
          const s3 = pe.fromTag("div");
          return Tt(s3, { position: "absolute", left: t5 - 3.5 + "px", top: o4 + "px", height: r4 + "px", width: "7px" }), me(s3, { "data-column": e5, role: "presentation" }), s3;
        })(t4.col, t4.x - e4.left, o3.top - e4.top, 0, n3);
        return xm(r3, Km), r3;
      });
    })(t2, z(i3, (e3, t3) => O(m2, (e4) => t3 === e4)), s2, cn(o2));
  }, Xm = (e2, t2) => {
    if (Ym(e2), e2.isResizable(t2)) {
      const o2 = Uo(t2), n2 = nn(o2), r2 = tn(o2);
      Qm(o2, e2, t2, n2, r2);
    }
  }, Zm = (e2, t2) => {
    const o2 = st(e2.parent(), "." + Um);
    N(o2, t2);
  }, ed = (e2) => {
    Zm(e2, (e3) => {
      St(e3, "display", "none");
    });
  }, td = (e2) => {
    Zm(e2, (e3) => {
      St(e3, "display", "block");
    });
  }, od = $m("resizer-bar-dragging"), nd = (e2) => {
    const t2 = (() => {
      const e3 = Wm({ drag: Am(["xDelta", "yDelta", "target"]) });
      let t3 = C.none();
      const o3 = (() => {
        const e4 = Wm({ drag: Am(["xDelta", "yDelta"]) });
        return { mutate: (t4, o4) => {
          e4.trigger.drag(t4, o4);
        }, events: e4.registry };
      })();
      return o3.events.drag.bind((o4) => {
        t3.each((t4) => {
          e3.trigger.drag(o4.xDelta, o4.yDelta, t4);
        });
      }), { assign: (e4) => {
        t3 = C.some(e4);
      }, get: () => t3, mutate: o3.mutate, events: e3.registry };
    })(), o2 = ((e3, t3 = {}) => {
      var o3;
      return ((e4, t4, o4) => {
        let n3 = false;
        const r3 = Wm({ start: Am([]), stop: Am([]) }), s3 = Fm(), l3 = () => {
          m3.stop(), s3.isOn() && (s3.off(), r3.trigger.stop());
        }, c3 = /* @__PURE__ */ ((e5, t5) => {
          let o5 = null;
          const n4 = () => {
            a(o5) || (clearTimeout(o5), o5 = null);
          };
          return { cancel: n4, throttle: (...t6) => {
            n4(), o5 = setTimeout(() => {
              o5 = null, e5.apply(null, t6);
            }, 200);
          } };
        })(l3);
        s3.events.move.bind((o5) => {
          t4.mutate(e4, o5.info);
        });
        const i4 = (e5) => (...t5) => {
          n3 && e5.apply(null, t5);
        }, m3 = t4.sink(Pm({ forceDrop: l3, drop: i4(l3), move: i4((e5) => {
          c3.cancel(), s3.onEvent(e5, t4);
        }), delayDrop: i4(c3.throttle) }), o4);
        return { element: m3.element, go: (e5) => {
          m3.start(e5), s3.on(), r3.trigger.start();
        }, on: () => {
          n3 = true;
        }, off: () => {
          n3 = false;
        }, destroy: () => {
          m3.destroy();
        }, events: r3.registry };
      })(e3, null !== (o3 = t3.mode) && void 0 !== o3 ? o3 : Vm, t3);
    })(t2, {});
    let n2 = C.none();
    const r2 = (e3, t3) => C.from(de(e3, t3));
    t2.events.drag.bind((e3) => {
      r2(e3.target, "data-row").each((t3) => {
        const o3 = At(e3.target, "top");
        St(e3.target, "top", o3 + e3.yDelta + "px");
      }), r2(e3.target, "data-column").each((t3) => {
        const o3 = At(e3.target, "left");
        St(e3.target, "left", o3 + e3.xDelta + "px");
      });
    });
    const s2 = (e3, t3) => At(e3, t3) - Et(e3, "data-initial-" + t3, 0);
    o2.events.stop.bind(() => {
      t2.get().each((t3) => {
        n2.each((o3) => {
          r2(t3, "data-row").each((e3) => {
            const n3 = s2(t3, "top");
            fe(t3, "data-initial-top"), d2.trigger.adjustHeight(o3, n3, parseInt(e3, 10));
          }), r2(t3, "data-column").each((e3) => {
            const n3 = s2(t3, "left");
            fe(t3, "data-initial-left"), d2.trigger.adjustWidth(o3, n3, parseInt(e3, 10));
          }), Xm(e2, o3);
        });
      });
    });
    const l2 = (n3, r3) => {
      d2.trigger.startAdjust(), t2.assign(n3), ie(n3, "data-initial-" + r3, At(n3, r3)), xm(n3, od), St(n3, "opacity", "0.2"), o2.go(e2.parent());
    }, c2 = Em(e2.parent(), "mousedown", (e3) => {
      var t3;
      t3 = e3.target, Cm(t3, Gm) && l2(e3.target, "top"), ((e4) => Cm(e4, Km))(e3.target) && l2(e3.target, "left");
    }), i3 = (t3) => ye(t3, e2.view()), m2 = Em(e2.view(), "mouseover", (t3) => {
      var o3;
      (o3 = t3.target, dt(o3, "table", i3).filter(Is)).fold(() => {
        et(t3.target) && Ym(e2);
      }, (t4) => {
        n2 = C.some(t4), Xm(e2, t4);
      });
    }), d2 = Wm({ adjustHeight: Am(["table", "delta", "row"]), adjustWidth: Am(["table", "delta", "column"]), startAdjust: Am([]) });
    return { destroy: () => {
      c2.unbind(), m2.unbind(), o2.destroy(), Ym(e2);
    }, refresh: (t3) => {
      Xm(e2, t3);
    }, on: o2.on, off: o2.off, hideBars: w(ed, e2), showBars: w(td, e2), events: d2.registry };
  }, rd = (e2, t2, o2) => {
    const n2 = Rn, r2 = On, s2 = nd(e2), l2 = Wm({ beforeResize: Am(["table", "type"]), afterResize: Am(["table", "type"]), startDrag: Am([]) });
    return s2.events.adjustHeight.bind((e3) => {
      const t3 = e3.table;
      l2.trigger.beforeResize(t3, "row");
      ((e4, t4, o3, n3) => {
        const r3 = Uo(e4), s3 = ((e5, t5, o4) => Zn(e5, t5, o4, Hn, (e6) => e6.getOrThunk(Lt)))(r3, e4, n3), l3 = E(s3, (e5, n4) => o3 === n4 ? Math.max(t4 + e5, Lt()) : e5), a2 = Ol(r3, l3), c2 = ((e5, t5) => E(e5.all, (e6, o4) => ({ element: e6.element, height: t5[o4] })))(r3, l3);
        N(c2, (e5) => {
          _n(e5.element, e5.height);
        }), N(a2, (e5) => {
          _n(e5.element, e5.height);
        });
        const i3 = A(l3, (e5, t5) => e5 + t5, 0);
        _n(e4, i3);
      })(t3, n2.delta(e3.delta, t3), e3.row, n2), l2.trigger.afterResize(t3, "row");
    }), s2.events.startAdjust.bind((e3) => {
      l2.trigger.startDrag();
    }), s2.events.adjustWidth.bind((e3) => {
      const n3 = e3.table;
      l2.trigger.beforeResize(n3, "col");
      const s3 = r2.delta(e3.delta, n3), a2 = o2(n3);
      El(n3, s3, e3.column, t2, a2), l2.trigger.afterResize(n3, "col");
    }), { on: s2.on, off: s2.off, refreshBars: s2.refresh, hideBars: s2.hideBars, showBars: s2.showBars, destroy: s2.destroy, events: l2.registry };
  }, sd = (e2) => m(e2) && "TABLE" === e2.nodeName, ld = "bar-", ad = (e2) => "false" !== de(e2, "data-mce-resize"), cd = (e2) => {
    const t2 = Gi(), o2 = Gi(), n2 = Gi();
    let r2, s2;
    const l2 = (t3) => ec(e2, t3), a2 = () => Va(e2) ? Cs() : xs();
    return e2.on("init", () => {
      const r3 = ((e3, t3) => e3.inline ? ((e4, t4, o3) => ({ parent: g(t4), view: g(e4), origin: g(dn(0, 0)), isResizable: o3 }))(pe.fromDom(e3.getBody()), (() => {
        const e4 = pe.fromTag("div");
        return Tt(e4, { position: "static", height: "0", width: "0", padding: "0", margin: "0", border: "0" }), We(tt(pe.fromDom(document)), e4), e4;
      })(), t3) : ((e4, t4) => {
        const o3 = se(e4) ? ((e5) => pe.fromDom(Te(e5).dom.documentElement))(e4) : e4;
        return { parent: g(o3), view: g(e4), origin: g(dn(0, 0)), isResizable: t4 };
      })(pe.fromDom(e3.getDoc()), t3))(e2, ad);
      if (n2.set(r3), ((e3) => {
        const t3 = e3.options.get("object_resizing");
        return D(t3.split(","), "table");
      })(e2) && Ja(e2)) {
        const n3 = a2(), s3 = rd(r3, n3, l2);
        s3.on(), s3.events.startDrag.bind((o3) => {
          t2.set(e2.selection.getRng());
        }), s3.events.beforeResize.bind((t3) => {
          const o3 = t3.table.dom;
          ((e3, t4, o4, n4, r4) => {
            e3.dispatch("ObjectResizeStart", { target: t4, width: o4, height: n4, origin: r4 });
          })(e2, o3, Nr(o3), Br(o3), ld + t3.type);
        }), s3.events.afterResize.bind((o3) => {
          const n4 = o3.table, r4 = n4.dom;
          kr(n4), t2.on((t3) => {
            e2.selection.setRng(t3), e2.focus();
          }), ((e3, t3, o4, n5, r5) => {
            e3.dispatch("ObjectResized", { target: t3, width: o4, height: n5, origin: r5 });
          })(e2, r4, Nr(r4), Br(r4), ld + o3.type), e2.undoManager.add();
        }), o2.set(s3);
      }
    }), e2.on("ObjectResizeStart", (t3) => {
      const o3 = t3.target;
      if (sd(o3)) {
        const n3 = pe.fromDom(o3);
        N(e2.dom.select(".mce-clonedresizable"), (t4) => {
          e2.dom.addClass(t4, "mce-" + qa(e2) + "-columns");
        }), !hc(n3) && Ka(e2) ? vc(n3) : !gc(n3) && Ga(e2) && bc(n3), pc(n3) && wt(t3.origin, ld) && bc(n3), r2 = t3.width, s2 = Ya(e2) ? "" : ((e3, t4) => {
          const o4 = e3.dom.getStyle(t4, "width") || e3.dom.getAttrib(t4, "width");
          return C.from(o4).filter(yt);
        })(e2, o3).getOr("");
      }
    }), e2.on("ObjectResized", (t3) => {
      const o3 = t3.target;
      if (sd(o3)) {
        const n3 = pe.fromDom(o3), c2 = t3.origin;
        wt(c2, "corner-") && ((t4, o4, n4) => {
          const c3 = bt(o4, "e");
          if ("" === s2 && bc(t4), n4 !== r2 && "" !== s2) {
            St(t4, "width", s2);
            const o5 = a2(), i3 = l2(t4), m2 = Va(e2) || c3 ? ((e3) => Ss(e3).columns)(t4) - 1 : 0;
            El(t4, n4 - r2, m2, o5, i3);
          } else if (((e3) => /^(\d+(\.\d+)?)%$/.test(e3))(s2)) {
            const e3 = parseFloat(s2.replace("%", ""));
            St(t4, "width", n4 * e3 / r2 + "%");
          }
          ((e3) => /^(\d+(\.\d+)?)px$/.test(e3))(s2) && ((e3) => {
            const t5 = Uo(e3);
            Zo(t5) || N(It(e3), (e4) => {
              const t6 = Rt(e4, "width");
              St(e4, "width", t6), fe(e4, "width");
            });
          })(t4);
        })(n3, c2, t3.width), kr(n3), Wa(e2, n3.dom, La);
      }
    }), e2.on("SwitchMode", () => {
      o2.on((t3) => {
        e2.mode.isReadOnly() ? t3.hideBars() : t3.showBars();
      });
    }), e2.on("remove", () => {
      o2.on((e3) => {
        e3.destroy();
      }), n2.on((t3) => {
        ((e3, t4) => {
          e3.inline && Ie(t4.parent());
        })(e2, t3);
      });
    }), { refresh: (e3) => {
      o2.on((t3) => t3.refreshBars(pe.fromDom(e3)));
    }, hide: () => {
      o2.on((e3) => e3.hideBars());
    }, show: () => {
      o2.on((e3) => e3.showBars());
    } };
  }, id = (e2) => {
    ((e3) => {
      const t3 = e3.options.register;
      t3("table_clone_elements", { processor: "string[]" }), t3("table_use_colgroups", { processor: "boolean", default: true }), t3("table_header_type", { processor: (e4) => {
        const t4 = D(["section", "cells", "sectionCells", "auto"], e4);
        return t4 ? { value: e4, valid: t4 } : { valid: false, message: "Must be one of: section, cells, sectionCells or auto." };
      }, default: "section" }), t3("table_sizing_mode", { processor: "string", default: "auto" }), t3("table_default_attributes", { processor: "object", default: { border: "1" } }), t3("table_default_styles", { processor: "object", default: { "border-collapse": "collapse" } }), t3("table_column_resizing", { processor: (e4) => {
        const t4 = D(["preservetable", "resizetable"], e4);
        return t4 ? { value: e4, valid: t4 } : { valid: false, message: "Must be preservetable, or resizetable." };
      }, default: "preservetable" }), t3("table_resize_bars", { processor: "boolean", default: true }), t3("table_style_by_css", { processor: "boolean", default: true });
    })(e2);
    const t2 = cd(e2), o2 = zm(e2, t2), n2 = tc(e2, t2, o2);
    return Ic(e2, n2), ((e3, t3) => {
      const o3 = Or(e3), n3 = (t4) => ls(Er(e3)).bind((n4) => Ft(n4, o3).map((o4) => {
        const r2 = ns(as(e3), o4, n4);
        return t4(o4, r2);
      })).getOr("");
      G({ mceTableRowType: () => n3(t3.getTableRowType), mceTableCellType: () => n3(t3.getTableCellType), mceTableColType: () => n3(t3.getTableColType) }, (t4, o4) => e3.addQueryValueHandler(o4, t4));
    })(e2, n2), cs(e2, n2), { getSelectedCells: o2.getSelectedCells, clearSelectedCells: o2.clearSelectedCells };
  };
  e.add("dom", (e2) => ({ table: id(e2) }));
}();
tinymce.IconManager.add("default", { icons: { "accessibility-check": '<svg width="24" height="24"><path d="M12 2a2 2 0 0 1 2 2 2 2 0 0 1-2 2 2 2 0 0 1-2-2c0-1.1.9-2 2-2Zm8 7h-5v12c0 .6-.4 1-1 1a1 1 0 0 1-1-1v-5c0-.6-.4-1-1-1a1 1 0 0 0-1 1v5c0 .6-.4 1-1 1a1 1 0 0 1-1-1V9H4a1 1 0 1 1 0-2h16c.6 0 1 .4 1 1s-.4 1-1 1Z" fill-rule="nonzero"/></svg>', "action-next": '<svg width="24" height="24"><path fill-rule="nonzero" d="M5.7 7.3a1 1 0 0 0-1.4 1.4l7.7 7.7 7.7-7.7a1 1 0 1 0-1.4-1.4L12 13.6 5.7 7.3Z"/></svg>', "action-prev": '<svg width="24" height="24"><path fill-rule="nonzero" d="M18.3 15.7a1 1 0 0 0 1.4-1.4L12 6.6l-7.7 7.7a1 1 0 0 0 1.4 1.4L12 9.4l6.3 6.3Z"/></svg>', addtag: '<svg width="24" height="24"><path fill-rule="evenodd" clip-rule="evenodd" d="M15 5a2 2 0 0 1 1.6.8L21 12l-4.4 6.2a2 2 0 0 1-1.6.8h-3v-2h3l3.5-5L15 7H5v3H3V7c0-1.1.9-2 2-2h10Z"/><path fill-rule="evenodd" clip-rule="evenodd" d="M6 12a1 1 0 0 0-1 1v2H3a1 1 0 1 0 0 2h2v2a1 1 0 1 0 2 0v-2h2a1 1 0 1 0 0-2H7v-2c0-.6-.4-1-1-1Z"/></svg>', "align-center": '<svg width="24" height="24"><path d="M5 5h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2Zm3 4h8c.6 0 1 .4 1 1s-.4 1-1 1H8a1 1 0 1 1 0-2Zm0 8h8c.6 0 1 .4 1 1s-.4 1-1 1H8a1 1 0 0 1 0-2Zm-3-4h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2Z" fill-rule="evenodd"/></svg>', "align-justify": '<svg width="24" height="24"><path d="M5 5h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2Zm0 4h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2Zm0 4h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2Zm0 4h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2Z" fill-rule="evenodd"/></svg>', "align-left": '<svg width="24" height="24"><path d="M5 5h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2Zm0 4h8c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2Zm0 8h8c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2Zm0-4h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2Z" fill-rule="evenodd"/></svg>', "align-none": '<svg width="24" height="24"><path d="M14.2 5 13 7H5a1 1 0 1 1 0-2h9.2Zm4 0h.8a1 1 0 0 1 0 2h-2l1.2-2Zm-6.4 4-1.2 2H5a1 1 0 0 1 0-2h6.8Zm4 0H19a1 1 0 0 1 0 2h-4.4l1.2-2Zm-6.4 4-1.2 2H5a1 1 0 0 1 0-2h4.4Zm4 0H19a1 1 0 0 1 0 2h-6.8l1.2-2ZM7 17l-1.2 2H5a1 1 0 0 1 0-2h2Zm4 0h8a1 1 0 0 1 0 2H9.8l1.2-2Zm5.2-13.5 1.3.7-9.7 16.3-1.3-.7 9.7-16.3Z" fill-rule="evenodd"/></svg>', "align-right": '<svg width="24" height="24"><path d="M5 5h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2Zm6 4h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0 8h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm-6-4h14c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2Z" fill-rule="evenodd"/></svg>', "arrow-left": '<svg width="24" height="24"><path d="m5.6 13 12 6a1 1 0 0 0 1.4-1V6a1 1 0 0 0-1.4-.9l-12 6a1 1 0 0 0 0 1.8Z" fill-rule="evenodd"/></svg>', "arrow-right": '<svg width="24" height="24"><path d="m18.5 13-12 6A1 1 0 0 1 5 18V6a1 1 0 0 1 1.4-.9l12 6a1 1 0 0 1 0 1.8Z" fill-rule="evenodd"/></svg>', bold: '<svg width="24" height="24"><path d="M7.8 19c-.3 0-.5 0-.6-.2l-.2-.5V5.7c0-.2 0-.4.2-.5l.6-.2h5c1.5 0 2.7.3 3.5 1 .7.6 1.1 1.4 1.1 2.5a3 3 0 0 1-.6 1.9c-.4.6-1 1-1.6 1.2.4.1.9.3 1.3.6s.8.7 1 1.2c.4.4.5 1 .5 1.6 0 1.3-.4 2.3-1.3 3-.8.7-2.1 1-3.8 1H7.8Zm5-8.3c.6 0 1.2-.1 1.6-.5.4-.3.6-.7.6-1.3 0-1.1-.8-1.7-2.3-1.7H9.3v3.5h3.4Zm.5 6c.7 0 1.3-.1 1.7-.4.4-.4.6-.9.6-1.5s-.2-1-.7-1.4c-.4-.3-1-.4-2-.4H9.4v3.8h4Z" fill-rule="evenodd"/></svg>', bookmark: '<svg width="24" height="24"><path d="M6 4v17l6-4 6 4V4c0-.6-.4-1-1-1H7a1 1 0 0 0-1 1Z" fill-rule="nonzero"/></svg>', "border-style": '<svg width="24" height="24"><g fill-rule="evenodd"><rect width="18" height="2" x="3" y="6" rx="1"/><rect width="2.8" height="2" x="3" y="16" rx="1"/><rect width="2.8" height="2" x="6.8" y="16" rx="1"/><rect width="2.8" height="2" x="10.6" y="16" rx="1"/><rect width="2.8" height="2" x="14.4" y="16" rx="1"/><rect width="2.8" height="2" x="18.2" y="16" rx="1"/><rect width="8" height="2" x="3" y="11" rx="1"/><rect width="8" height="2" x="13" y="11" rx="1"/></g></svg>', "border-width": '<svg width="24" height="24"><g fill-rule="evenodd"><rect width="18" height="5" x="3" y="5" rx="1"/><rect width="18" height="3.5" x="3" y="11.5" rx="1"/><rect width="18" height="2" x="3" y="17" rx="1"/></g></svg>', brightness: '<svg width="24" height="24"><path d="M12 17c.3 0 .5.1.7.3.2.2.3.4.3.7v1c0 .3-.1.5-.3.7a1 1 0 0 1-.7.3 1 1 0 0 1-.7-.3 1 1 0 0 1-.3-.7v-1c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3Zm0-10a1 1 0 0 1-.7-.3A1 1 0 0 1 11 6V5c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3.3 0 .5.1.7.3.2.2.3.4.3.7v1c0 .3-.1.5-.3.7a1 1 0 0 1-.7.3Zm7 4c.3 0 .5.1.7.3.2.2.3.4.3.7 0 .3-.1.5-.3.7a1 1 0 0 1-.7.3h-1a1 1 0 0 1-.7-.3 1 1 0 0 1-.3-.7c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3h1ZM7 12c0 .3-.1.5-.3.7a1 1 0 0 1-.7.3H5a1 1 0 0 1-.7-.3A1 1 0 0 1 4 12c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3h1c.3 0 .5.1.7.3.2.2.3.4.3.7Zm10 3.5.7.8c.2.1.3.4.3.6 0 .3-.1.6-.3.8a1 1 0 0 1-.8.3 1 1 0 0 1-.6-.3l-.8-.7a1 1 0 0 1-.3-.8c0-.2.1-.5.3-.7a1 1 0 0 1 1.4 0Zm-10-7-.7-.8a1 1 0 0 1-.3-.6c0-.3.1-.6.3-.8.2-.2.5-.3.8-.3.2 0 .5.1.7.3l.7.7c.2.2.3.5.3.8 0 .2-.1.5-.3.7a1 1 0 0 1-.7.3 1 1 0 0 1-.8-.3Zm10 0a1 1 0 0 1-.8.3 1 1 0 0 1-.7-.3 1 1 0 0 1-.3-.7c0-.3.1-.6.3-.8l.8-.7c.1-.2.4-.3.6-.3.3 0 .6.1.8.3.2.2.3.5.3.8 0 .2-.1.5-.3.7l-.7.7Zm-10 7c.2-.2.5-.3.8-.3.2 0 .5.1.7.3a1 1 0 0 1 0 1.4l-.8.8a1 1 0 0 1-.6.3 1 1 0 0 1-.8-.3 1 1 0 0 1-.3-.8c0-.2.1-.5.3-.6l.7-.8ZM12 8a4 4 0 0 1 3.7 2.4 4 4 0 0 1 0 3.2A4 4 0 0 1 12 16a4 4 0 0 1-3.7-2.4 4 4 0 0 1 0-3.2A4 4 0 0 1 12 8Zm0 6.5c.7 0 1.3-.2 1.8-.7.5-.5.7-1.1.7-1.8s-.2-1.3-.7-1.8c-.5-.5-1.1-.7-1.8-.7s-1.3.2-1.8.7c-.5.5-.7 1.1-.7 1.8s.2 1.3.7 1.8c.5.5 1.1.7 1.8.7Z" fill-rule="evenodd"/></svg>', browse: '<svg width="24" height="24"><path d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2h-4v-2h4V8H5v10h4v2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm-8 9.4-2.3 2.3a1 1 0 1 1-1.4-1.4l4-4a1 1 0 0 1 1.4 0l4 4a1 1 0 0 1-1.4 1.4L13 13.4V20a1 1 0 0 1-2 0v-6.6Z" fill-rule="nonzero"/></svg>', cancel: '<svg width="24" height="24"><path d="M12 4.6a7.4 7.4 0 1 1 0 14.8 7.4 7.4 0 0 1 0-14.8ZM12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18Zm0 8L14.8 8l1 1.1-2.7 2.8 2.7 2.7-1.1 1.1-2.7-2.7-2.7 2.7-1-1.1 2.6-2.7-2.7-2.7 1-1.1 2.8 2.7Z" fill-rule="nonzero"/></svg>', "cell-background-color": '<svg width="24" height="24"><path d="m15.7 2 1.6 1.6-2.7 2.6 5.9 5.8c.7.7.7 1.7 0 2.4l-6.3 6.1a1.7 1.7 0 0 1-2.4 0l-6.3-6.1c-.7-.7-.7-1.7 0-2.4L15.7 2ZM18 12l-4.5-4L9 12h9ZM4 16s2 2.4 2 3.8C6 21 5.1 22 4 22s-2-1-2-2.2C2 18.4 4 16 4 16Z"/></svg>', "cell-border-color": '<svg width="24" height="24"><g fill-rule="evenodd"><path fill-rule="nonzero" d="M5 13v5h2v2H5a2 2 0 0 1-2-2v-5h2zm8-7V4h6a2 2 0 0 1 2 2h-8z" opacity=".2"/><path fill-rule="nonzero" d="M13 4v2H5v7H3V6c0-1.1.9-2 2-2h8zm-2.6 14.1.1-.1.1.1.2.3.2.2.2.2c.4.6.8 1.2.8 1.7 0 .8-.7 1.5-1.5 1.5S9 21.3 9 20.5c0-.5.4-1.1.8-1.7l.2-.2.2-.2.2-.3z"/><path d="m13 11-2 2H5v-2h6V6h2z"/><path fill-rule="nonzero" d="m18.4 8 1 1-1.8 1.9 4 4c.5.4.5 1.1 0 1.6l-4.3 4.2a1.2 1.2 0 0 1-1.6 0l-4.4-4.2c-.4-.5-.4-1.2 0-1.7l7-6.8Zm1.6 7-3-3-3 3h6Z"/></g></svg>', "change-case": '<svg width="24" height="24"><path d="M18.4 18.2v-.6c-.5.8-1.3 1.2-2.4 1.2-2.2 0-3.3-1.6-3.3-4.8 0-3.1 1-4.7 3.3-4.7 1.1 0 1.8.3 2.4 1.1v-.6c0-.5.4-.8.8-.8s.8.3.8.8v8.4c0 .5-.4.8-.8.8a.8.8 0 0 1-.8-.8zm-2-7.4c-1.3 0-1.8.9-1.8 3.2 0 2.4.5 3.3 1.7 3.3 1.3 0 1.8-.9 1.8-3.2 0-2.4-.5-3.3-1.7-3.3zM10 15.7H5.5l-.8 2.6a1 1 0 0 1-1 .7h-.2a.7.7 0 0 1-.7-1l4-12a1 1 0 0 1 2 0l4 12a.7.7 0 0 1-.8 1h-.2a1 1 0 0 1-1-.7l-.8-2.6zm-.3-1.5-2-6.5-1.9 6.5h3.9z" fill-rule="evenodd"/></svg>', "character-count": '<svg width="24" height="24"><path d="M4 11.5h16v1H4v-1Zm4.8-6.8V10H7.7V5.8h-1v-1h2ZM11 8.3V9h2v1h-3V7.7l2-1v-.9h-2v-1h3v2.4l-2 1Zm6.3-3.4V10h-3.1V9h2.1V8h-2.1V6.8h2.1v-1h-2.1v-1h3.1ZM5.8 16.4c0-.5.2-.8.5-1 .2-.2.6-.3 1.2-.3l.8.1c.2 0 .4.2.5.3l.4.4v2.8l.2.3H8.2V18.7l-.6.3H7c-.4 0-.7 0-1-.2a1 1 0 0 1-.3-.9c0-.3 0-.6.3-.8.3-.2.7-.4 1.2-.4l.6-.2h.3v-.2l-.1-.2a.8.8 0 0 0-.5-.1 1 1 0 0 0-.4 0l-.3.4h-1Zm2.3.8h-.2l-.2.1-.4.1a1 1 0 0 0-.4.2l-.2.2.1.3.5.1h.4l.4-.4v-.6Zm2-3.4h1.2v1.7l.5-.3h.5c.5 0 .9.1 1.2.5.3.4.5.8.5 1.4 0 .6-.2 1.1-.5 1.5-.3.4-.7.6-1.3.6l-.6-.1-.4-.4v.4h-1.1v-5.4Zm1.1 3.3c0 .3 0 .6.2.8a.7.7 0 0 0 1.2 0l.2-.8c0-.4 0-.6-.2-.8a.7.7 0 0 0-.6-.3l-.6.3-.2.8Zm6.1-.5c0-.2 0-.3-.2-.4a.8.8 0 0 0-.5-.2c-.3 0-.5.1-.6.3l-.2.9c0 .3 0 .6.2.8.1.2.3.3.6.3.2 0 .4 0 .5-.2l.2-.4h1.1c0 .5-.3.8-.6 1.1a2 2 0 0 1-1.3.4c-.5 0-1-.2-1.3-.6a2 2 0 0 1-.5-1.4c0-.6.1-1.1.5-1.5.3-.4.8-.5 1.4-.5.5 0 1 0 1.2.3.4.3.5.7.5 1.2h-1v-.1Z" fill-rule="evenodd"/></svg>', "checklist-rtl": '<svg width="24" height="24"><path d="M5 17h8c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2zm0-6h8c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2zm0-6h8c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2zm14.2 11c.2-.4.6-.5.9-.3.3.2.4.6.2 1L18 20c-.2.3-.7.4-1 0l-1.3-1.3a.7.7 0 0 1 0-1c.3-.2.7-.2 1 0l.7.9 1.7-2.8zm0-6c.2-.4.6-.5.9-.3.3.2.4.6.2 1L18 14c-.2.3-.7.4-1 0l-1.3-1.3a.7.7 0 0 1 0-1c.3-.2.7-.2 1 0l.7.9 1.7-2.8zm0-6c.2-.4.6-.5.9-.3.3.2.4.6.2 1L18 8c-.2.3-.7.4-1 0l-1.3-1.3a.7.7 0 0 1 0-1c.3-.2.7-.2 1 0l.7.9 1.7-2.8z" fill-rule="evenodd"/></svg>', checklist: '<svg width="24" height="24"><path d="M11 17h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0-6h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0-6h8a1 1 0 0 1 0 2h-8a1 1 0 0 1 0-2ZM7.2 16c.2-.4.6-.5.9-.3.3.2.4.6.2 1L6 20c-.2.3-.7.4-1 0l-1.3-1.3a.7.7 0 0 1 0-1c.3-.2.7-.2 1 0l.7.9 1.7-2.8Zm0-6c.2-.4.6-.5.9-.3.3.2.4.6.2 1L6 14c-.2.3-.7.4-1 0l-1.3-1.3a.7.7 0 0 1 0-1c.3-.2.7-.2 1 0l.7.9 1.7-2.8Zm0-6c.2-.4.6-.5.9-.3.3.2.4.6.2 1L6 8c-.2.3-.7.4-1 0L3.8 6.9a.7.7 0 0 1 0-1c.3-.2.7-.2 1 0l.7.9 1.7-2.8Z" fill-rule="evenodd"/></svg>', checkmark: '<svg width="24" height="24"><path d="M18.2 5.4a1 1 0 0 1 1.6 1.2l-8 12a1 1 0 0 1-1.5.1l-5-5a1 1 0 1 1 1.4-1.4l4.1 4.1 7.4-11Z" fill-rule="nonzero"/></svg>', "chevron-down": '<svg width="10" height="10"><path d="M8.7 2.2c.3-.3.8-.3 1 0 .4.4.4.9 0 1.2L5.7 7.8c-.3.3-.9.3-1.2 0L.2 3.4a.8.8 0 0 1 0-1.2c.3-.3.8-.3 1.1 0L5 6l3.7-3.8Z" fill-rule="nonzero"/></svg>', "chevron-left": '<svg width="10" height="10"><path d="M7.8 1.3 4 5l3.8 3.7c.3.3.3.8 0 1-.4.4-.9.4-1.2 0L2.2 5.7a.8.8 0 0 1 0-1.2L6.6.2C7 0 7.4 0 7.8.2c.3.3.3.8 0 1.1Z" fill-rule="nonzero"/></svg>', "chevron-right": '<svg width="10" height="10"><path d="M2.2 1.3a.8.8 0 0 1 0-1c.4-.4.9-.4 1.2 0l4.4 4.1c.3.4.3.9 0 1.2L3.4 9.8c-.3.3-.8.3-1.2 0a.8.8 0 0 1 0-1.1L6 5 2.2 1.3Z" fill-rule="nonzero"/></svg>', "chevron-up": '<svg width="10" height="10"><path d="M8.7 7.8 5 4 1.3 7.8c-.3.3-.8.3-1 0a.8.8 0 0 1 0-1.2l4.1-4.4c.3-.3.9-.3 1.2 0l4.2 4.4c.3.3.3.9 0 1.2-.3.3-.8.3-1.1 0Z" fill-rule="nonzero"/></svg>', close: '<svg width="24" height="24"><path d="M17.3 8.2 13.4 12l3.9 3.8a1 1 0 0 1-1.5 1.5L12 13.4l-3.8 3.9a1 1 0 0 1-1.5-1.5l3.9-3.8-3.9-3.8a1 1 0 0 1 1.5-1.5l3.8 3.9 3.8-3.9a1 1 0 0 1 1.5 1.5Z" fill-rule="evenodd"/></svg>', "code-sample": '<svg width="24" height="26"><path d="M7.1 11a2.8 2.8 0 0 1-.8 2 2.8 2.8 0 0 1 .8 2v1.7c0 .3.1.6.4.8.2.3.5.4.8.4.3 0 .4.2.4.4v.8c0 .2-.1.4-.4.4-.7 0-1.4-.3-2-.8-.5-.6-.8-1.3-.8-2V15c0-.3-.1-.6-.4-.8-.2-.3-.5-.4-.8-.4a.4.4 0 0 1-.4-.4v-.8c0-.2.2-.4.4-.4.3 0 .6-.1.8-.4.3-.2.4-.5.4-.8V9.3c0-.7.3-1.4.8-2 .6-.5 1.3-.8 2-.8.3 0 .4.2.4.4v.8c0 .2-.1.4-.4.4-.3 0-.6.1-.8.4-.3.2-.4.5-.4.8V11Zm9.8 0V9.3c0-.3-.1-.6-.4-.8-.2-.3-.5-.4-.8-.4a.4.4 0 0 1-.4-.4V7c0-.2.1-.4.4-.4.7 0 1.4.3 2 .8.5.6.8 1.3.8 2V11c0 .3.1.6.4.8.2.3.5.4.8.4.2 0 .4.2.4.4v.8c0 .2-.2.4-.4.4-.3 0-.6.1-.8.4-.3.2-.4.5-.4.8v1.7c0 .7-.3 1.4-.8 2-.6.5-1.3.8-2 .8a.4.4 0 0 1-.4-.4v-.8c0-.2.1-.4.4-.4.3 0 .6-.1.8-.4.3-.2.4-.5.4-.8V15a2.8 2.8 0 0 1 .8-2 2.8 2.8 0 0 1-.8-2Zm-3.3-.4c0 .4-.1.8-.5 1.1-.3.3-.7.5-1.1.5-.4 0-.8-.2-1.1-.5-.4-.3-.5-.7-.5-1.1 0-.5.1-.9.5-1.2.3-.3.7-.4 1.1-.4.4 0 .8.1 1.1.4.4.3.5.7.5 1.2ZM12 13c.4 0 .8.1 1.1.5.4.3.5.7.5 1.1 0 1-.1 1.6-.5 2a3 3 0 0 1-1.1 1c-.4.3-.8.4-1.1.4a.5.5 0 0 1-.5-.5V17a3 3 0 0 0 1-.2l.6-.6c-.6 0-1-.2-1.3-.5-.2-.3-.3-.7-.3-1 0-.5.1-1 .5-1.2.3-.4.7-.5 1.1-.5Z" fill-rule="evenodd"/></svg>', "color-levels": '<svg width="24" height="24"><path d="M17.5 11.4A9 9 0 0 1 18 14c0 .5 0 1-.2 1.4 0 .4-.3.9-.5 1.3a6.2 6.2 0 0 1-3.7 3 5.7 5.7 0 0 1-3.2 0A5.9 5.9 0 0 1 7.6 18a6.2 6.2 0 0 1-1.4-2.6 6.7 6.7 0 0 1 0-2.8c0-.4.1-.9.3-1.3a13.6 13.6 0 0 1 2.3-4A20 20 0 0 1 12 4a26.4 26.4 0 0 1 3.2 3.4 18.2 18.2 0 0 1 2.3 4Zm-2 4.5c.4-.7.5-1.4.5-2a7.3 7.3 0 0 0-1-3.2c.2.6.2 1.2.2 1.9a4.5 4.5 0 0 1-1.3 3 5.3 5.3 0 0 1-2.3 1.5 4.9 4.9 0 0 1-2 .1 4.3 4.3 0 0 0 2.4.8 4 4 0 0 0 2-.6 4 4 0 0 0 1.5-1.5Z" fill-rule="evenodd"/></svg>', "color-picker": '<svg width="24" height="24"><path d="M12 3a9 9 0 0 0 0 18 1.5 1.5 0 0 0 1.1-2.5c-.2-.3-.4-.6-.4-1 0-.8.7-1.5 1.5-1.5H16a5 5 0 0 0 5-5c0-4.4-4-8-9-8Zm-5.5 9a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3Zm3-4a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3Zm5 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3Zm3 4a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3Z" fill-rule="nonzero"/></svg>', "color-swatch-remove-color": '<svg width="24" height="24"><path stroke="#000" stroke-width="2" d="M21 3 3 21" fill-rule="evenodd"/></svg>', "color-swatch": '<svg width="24" height="24"><rect x="3" y="3" width="18" height="18" rx="1" fill-rule="evenodd"/></svg>', "comment-add": '<svg width="24" height="24"><g fill-rule="nonzero"><path d="m9 19 3-2h7c.6 0 1-.4 1-1V6c0-.6-.4-1-1-1H5a1 1 0 0 0-1 1v10c0 .6.4 1 1 1h4v2Zm-2 4v-4H5a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3h14a3 3 0 0 1 3 3v10a3 3 0 0 1-3 3h-6.4L7 23Z"/><path d="M13 10h2a1 1 0 0 1 0 2h-2v2a1 1 0 0 1-2 0v-2H9a1 1 0 0 1 0-2h2V8a1 1 0 0 1 2 0v2Z"/></g></svg>', comment: '<svg width="24" height="24"><path fill-rule="nonzero" d="m9 19 3-2h7c.6 0 1-.4 1-1V6c0-.6-.4-1-1-1H5a1 1 0 0 0-1 1v10c0 .6.4 1 1 1h4v2Zm-2 4v-4H5a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3h14a3 3 0 0 1 3 3v10a3 3 0 0 1-3 3h-6.4L7 23Z"/></svg>', contrast: '<svg width="24" height="24"><path d="M12 4a7.8 7.8 0 0 1 5.7 2.3A8 8 0 1 1 12 4Zm-6 8a6 6 0 0 0 6 6V6a6 6 0 0 0-6 6Z" fill-rule="evenodd"/></svg>', copy: '<svg width="24" height="24"><path d="M16 3H6a2 2 0 0 0-2 2v11h2V5h10V3Zm1 4a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2h-7a2 2 0 0 1-2-2V9c0-1.2.9-2 2-2h7Zm0 12V9h-7v10h7Z" fill-rule="nonzero"/></svg>', crop: '<svg width="24" height="24"><path d="M17 8v7h2c.6 0 1 .4 1 1s-.4 1-1 1h-2v2c0 .6-.4 1-1 1a1 1 0 0 1-1-1v-2H7V9H5a1 1 0 1 1 0-2h2V5c0-.6.4-1 1-1s1 .4 1 1v2h7l3-3 1 1-3 3ZM9 9v5l5-5H9Zm1 6h5v-5l-5 5Z" fill-rule="evenodd"/></svg>', "cut-column": '<svg width="24" height="24"><path fill-rule="evenodd" d="M7.2 4.5c.9 0 1.6.4 2.2 1A3.7 3.7 0 0 1 10.5 8v.5l1 1 4-4 1-.5a3.3 3.3 0 0 1 2 0c.4 0 .7.3 1 .5L17 8h4v13h-6V10l-1.5 1.5.5.5v4l-2.5-2.5-1 1v.5c0 .4 0 .8-.3 1.2-.2.5-.4.9-.8 1.2-.6.7-1.3 1-2.2 1-.8.2-1.5 0-2-.6l-.5-.8-.2-1c0-.4 0-.8.3-1.2A3.9 3.9 0 0 1 7 12.7c.5-.2 1-.3 1.5-.2l1-1-1-1c-.5 0-1 0-1.5-.2-.5-.1-1-.4-1.4-.9-.4-.3-.6-.7-.8-1.2L4.5 7c0-.4 0-.7.2-1 0-.3.3-.6.5-.8.5-.5 1.2-.8 2-.7Zm12.3 5h-3v10h3v-10ZM8 13.8h-.3l-.4.2a2.8 2.8 0 0 0-.7.4v.1a2.8 2.8 0 0 0-.6.8l-.1.4v.7l.2.5.5.2h.7a2.6 2.6 0 0 0 .8-.3 2.4 2.4 0 0 0 .7-.7 2.5 2.5 0 0 0 .3-.8 1.5 1.5 0 0 0 0-.8 1 1 0 0 0-.2-.4 1 1 0 0 0-.5-.2H8Zm3.5-3.7c-.4 0-.7.1-1 .4-.3.3-.4.6-.4 1s.1.7.4 1c.3.3.6.4 1 .4s.7-.1 1-.4c.3-.3.4-.6.4-1s-.1-.7-.4-1c-.3-.3-.6-.4-1-.4ZM7 5.8h-.4a1 1 0 0 0-.5.3 1 1 0 0 0-.2.5v.7a2.5 2.5 0 0 0 .3.8l.2.3h.1l.4.4.4.2.4.1h.7L9 9l.2-.4a1.6 1.6 0 0 0 0-.8 2.6 2.6 0 0 0-.3-.8A2.5 2.5 0 0 0 7.7 6l-.4-.1H7Z"/></svg>', "cut-row": '<svg width="24" height="24"><path fill-rule="evenodd" d="M22 3v5H9l3 3 2-2h4l-4 4 1 1h.5c.4 0 .8 0 1.2.3.5.2.9.4 1.2.8.7.6 1 1.3 1 2.2.2.8 0 1.5-.6 2l-.8.5-1 .2c-.4 0-.8 0-1.2-.3a3.9 3.9 0 0 1-2.1-2.2c-.2-.5-.3-1-.2-1.5l-1-1-1 1c0 .5 0 1-.2 1.5-.1.5-.4 1-.9 1.4-.3.4-.7.6-1.2.8l-1.2.3c-.4 0-.7 0-1-.2-.3 0-.6-.3-.8-.5-.5-.5-.8-1.2-.7-2 0-.9.4-1.6 1-2.2A3.7 3.7 0 0 1 8.6 14H9l1-1-4-4-.5-1a3.3 3.3 0 0 1 0-2c0-.4.3-.7.5-1l2 2V3h14ZM8.5 15.3h-.3a2.6 2.6 0 0 0-.8.4 2.5 2.5 0 0 0-.9 1.1l-.1.4v.7l.2.5.5.2h.7a2.5 2.5 0 0 0 .8-.3L9 18V18l.4-.4.2-.4.1-.4v-.7a1 1 0 0 0-.2-.5 1 1 0 0 0-.4-.2h-.5Zm7 0H15a1 1 0 0 0-.4.3 1 1 0 0 0-.2.5 1.5 1.5 0 0 0 0 .7v.4a2.8 2.8 0 0 0 .5.7h.1a2.8 2.8 0 0 0 .8.6l.4.1h.7l.5-.2.2-.5v-.7a2.6 2.6 0 0 0-.3-.8 2.4 2.4 0 0 0-.7-.7 2.5 2.5 0 0 0-.8-.3h-.3ZM12 11.6c-.4 0-.7.1-1 .4-.3.3-.4.6-.4 1s.1.7.4 1c.3.3.6.4 1 .4s.7-.1 1-.4c.3-.3.4-.6.4-1s-.1-.7-.4-1c-.3-.3-.6-.4-1-.4Zm8.5-7.1h-11v2h11v-2Z"/></svg>', cut: '<svg width="24" height="24"><path d="M18 15c.6.7 1 1.4 1 2.3 0 .8-.2 1.5-.7 2l-.8.5-1 .2c-.4 0-.8 0-1.2-.3a3.9 3.9 0 0 1-2.1-2.2c-.2-.5-.3-1-.2-1.5l-1-1-1 1c0 .5 0 1-.2 1.5-.1.5-.4 1-.9 1.4-.3.4-.7.6-1.2.8l-1.2.3c-.4 0-.7 0-1-.2-.3 0-.6-.3-.8-.5-.5-.5-.8-1.2-.7-2 0-.9.4-1.6 1-2.2A3.7 3.7 0 0 1 8.6 14H9l1-1-4-4-.5-1a3.3 3.3 0 0 1 0-2c0-.4.3-.7.5-1l6 6 6-6 .5 1a3.3 3.3 0 0 1 0 2c0 .4-.3.7-.5 1l-4 4 1 1h.5c.4 0 .8 0 1.2.3.5.2.9.4 1.2.8Zm-8.5 2.2.1-.4v-.7a1 1 0 0 0-.2-.5 1 1 0 0 0-.4-.2 1.6 1.6 0 0 0-.8 0 2.6 2.6 0 0 0-.8.3 2.5 2.5 0 0 0-.9 1.1l-.1.4v.7l.2.5.5.2h.7a2.5 2.5 0 0 0 .8-.3 2.8 2.8 0 0 0 1-1Zm2.5-2.8c.4 0 .7-.1 1-.4.3-.3.4-.6.4-1s-.1-.7-.4-1c-.3-.3-.6-.4-1-.4s-.7.1-1 .4c-.3.3-.4.6-.4 1s.1.7.4 1c.3.3.6.4 1 .4Zm5.4 4 .2-.5v-.7a2.6 2.6 0 0 0-.3-.8 2.4 2.4 0 0 0-.7-.7 2.5 2.5 0 0 0-.8-.3 1.5 1.5 0 0 0-.8 0 1 1 0 0 0-.4.2 1 1 0 0 0-.2.5 1.5 1.5 0 0 0 0 .7v.4l.3.4.3.4a2.8 2.8 0 0 0 .8.5l.4.1h.7l.5-.2Z" fill-rule="evenodd"/></svg>', "document-properties": '<svg width="24" height="24"><path d="M14.4 3H7a2 2 0 0 0-2 2v14c0 1.1.9 2 2 2h10a2 2 0 0 0 2-2V7.6L14.4 3ZM17 19H7V5h6v4h4v10Z" fill-rule="nonzero"/></svg>', drag: '<svg width="24" height="24"><path d="M13 5h2v2h-2V5Zm0 4h2v2h-2V9ZM9 9h2v2H9V9Zm4 4h2v2h-2v-2Zm-4 0h2v2H9v-2Zm0 4h2v2H9v-2Zm4 0h2v2h-2v-2ZM9 5h2v2H9V5Z" fill-rule="evenodd"/></svg>', "duplicate-column": '<svg width="24" height="24"><path d="M17 6v16h-7V6h7Zm-2 2h-3v12h3V8Zm-2-6v2H8v15H6V2h7Z"/></svg>', "duplicate-row": '<svg width="24" height="24"><path d="M22 11v7H6v-7h16Zm-2 2H8v3h12v-3Zm-1-6v2H4v5H2V7h17Z"/></svg>', duplicate: '<svg width="24" height="24"><g fill-rule="nonzero"><path d="M16 3v2H6v11H4V5c0-1.1.9-2 2-2h10Zm3 8h-2V9h-7v10h9a2 2 0 0 1-2 2h-7a2 2 0 0 1-2-2V9c0-1.2.9-2 2-2h7a2 2 0 0 1 2 2v2Z"/><path d="M17 14h1a1 1 0 0 1 0 2h-1v1a1 1 0 0 1-2 0v-1h-1a1 1 0 0 1 0-2h1v-1a1 1 0 0 1 2 0v1Z"/></g></svg>', "edit-block": '<svg width="24" height="24"><path fill-rule="nonzero" d="m19.8 8.8-9.4 9.4c-.2.2-.5.4-.9.4l-5.4 1.2 1.2-5.4.5-.8 9.4-9.4c.7-.7 1.8-.7 2.5 0l2.1 2.1c.7.7.7 1.8 0 2.5Zm-2-.2 1-.9v-.3l-2.2-2.2a.3.3 0 0 0-.3 0l-1 1L18 8.5Zm-1 1-2.5-2.4-6 6 2.5 2.5 6-6Zm-7 7.1-2.6-2.4-.3.3-.1.2-.7 3 3.1-.6h.1l.4-.5Z"/></svg>', "edit-image": '<svg width="24" height="24"><path d="M18 16h2V7a2 2 0 0 0-2-2H7v2h11v9ZM6 17h15a1 1 0 0 1 0 2h-1v1a1 1 0 0 1-2 0v-1H6a2 2 0 0 1-2-2V7H3a1 1 0 1 1 0-2h1V4a1 1 0 1 1 2 0v13Zm3-5.3 1.3 2 3-4.7 3.7 6H7l2-3.3Z" fill-rule="nonzero"/></svg>', "embed-page": '<svg width="24" height="24"><path d="M19 6V5H5v14h2A13 13 0 0 1 19 6Zm0 1.4c-.8.8-1.6 2.4-2.2 4.6H19V7.4Zm0 5.6h-2.4c-.4 1.8-.6 3.8-.6 6h3v-6Zm-4 6c0-2.2.2-4.2.6-6H13c-.7 1.8-1.1 3.8-1.1 6h3Zm-4 0c0-2.2.4-4.2 1-6H9.6A12 12 0 0 0 8 19h3ZM4 3h16c.6 0 1 .4 1 1v16c0 .6-.4 1-1 1H4a1 1 0 0 1-1-1V4c0-.6.4-1 1-1Zm11.8 9c.4-1.9 1-3.4 1.8-4.5a9.2 9.2 0 0 0-4 4.5h2.2Zm-3.4 0a12 12 0 0 1 2.8-4 12 12 0 0 0-5 4h2.2Z" fill-rule="nonzero"/></svg>', embed: '<svg width="24" height="24"><path d="M4 3h16c.6 0 1 .4 1 1v16c0 .6-.4 1-1 1H4a1 1 0 0 1-1-1V4c0-.6.4-1 1-1Zm1 2v14h14V5H5Zm4.8 2.6 5.6 4a.5.5 0 0 1 0 .8l-5.6 4A.5.5 0 0 1 9 16V8a.5.5 0 0 1 .8-.4Z" fill-rule="nonzero"/></svg>', emoji: '<svg width="24" height="24"><path d="M9 11c.6 0 1-.4 1-1s-.4-1-1-1a1 1 0 0 0-1 1c0 .6.4 1 1 1Zm6 0c.6 0 1-.4 1-1s-.4-1-1-1a1 1 0 0 0-1 1c0 .6.4 1 1 1Zm-3 5.5c2.1 0 4-1.5 4.4-3.5H7.6c.5 2 2.3 3.5 4.4 3.5ZM12 4a8 8 0 1 0 0 16 8 8 0 0 0 0-16Zm0 14.5a6.5 6.5 0 1 1 0-13 6.5 6.5 0 0 1 0 13Z" fill-rule="nonzero"/></svg>', export: '<svg width="24" height="24"><g fill-rule="nonzero"><path d="M14.4 3 18 7v1h-5V5H7v14h9a1 1 0 0 1 2 0c0 1-.8 2-1.9 2H7c-1 0-2-.8-2-1.9V5c0-1 .8-2 1.9-2h7.5Z"/><path d="M18.1 12c.5 0 .9.4.9 1 0 .5-.3 1-.8 1h-7.3c-.5 0-.9-.4-.9-1 0-.5.3-1 .8-1h7.3Z"/><path d="M16.4 9.2a1 1 0 0 1 1.4.2l2.4 3.6-2.4 3.6a1 1 0 0 1-1.7-1v-.2l1.7-2.4-1.6-2.4a1 1 0 0 1 .2-1.4Z"/></g></svg>', fill: '<svg width="24" height="26"><path d="m16.6 12-9-9-1.4 1.4 2.4 2.4-5.2 5.1c-.5.6-.5 1.6 0 2.2L9 19.6a1.5 1.5 0 0 0 2.2 0l5.5-5.5c.5-.6.5-1.6 0-2.2ZM5.2 13 10 8.2l4.8 4.8H5.2ZM19 14.5s-2 2.2-2 3.5c0 1.1.9 2 2 2a2 2 0 0 0 2-2c0-1.3-2-3.5-2-3.5Z" fill-rule="nonzero"/></svg>', "flip-horizontally": '<svg width="24" height="24"><path d="M14 19h2v-2h-2v2Zm4-8h2V9h-2v2ZM4 7v10c0 1.1.9 2 2 2h3v-2H6V7h3V5H6a2 2 0 0 0-2 2Zm14-2v2h2a2 2 0 0 0-2-2Zm-7 16h2V3h-2v18Zm7-6h2v-2h-2v2Zm-4-8h2V5h-2v2Zm4 12a2 2 0 0 0 2-2h-2v2Z" fill-rule="nonzero"/></svg>', "flip-vertically": '<svg width="24" height="24"><path d="M5 14v2h2v-2H5Zm8 4v2h2v-2h-2Zm4-14H7a2 2 0 0 0-2 2v3h2V6h10v3h2V6a2 2 0 0 0-2-2Zm2 14h-2v2a2 2 0 0 0 2-2ZM3 11v2h18v-2H3Zm6 7v2h2v-2H9Zm8-4v2h2v-2h-2ZM5 18c0 1.1.9 2 2 2v-2H5Z" fill-rule="nonzero"/></svg>', footnote: '<svg width="24" height="24"><path d="M19 13c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2h14Z"/><path fill-rule="evenodd" clip-rule="evenodd" d="M19 4v6h-1V5h-1.5V4h2.6Z"/><path d="M12 18c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 1 1 0-2h7ZM14 8c.6 0 1 .4 1 1s-.4 1-1 1H5a1 1 0 0 1 0-2h9Z"/></svg>', "format-painter": '<svg width="24" height="24"><path d="M18 5V4c0-.5-.4-1-1-1H5a1 1 0 0 0-1 1v4c0 .6.5 1 1 1h12c.6 0 1-.4 1-1V7h1v4H9v9c0 .6.4 1 1 1h2c.6 0 1-.4 1-1v-7h8V5h-3Z" fill-rule="nonzero"/></svg>', format: '<svg width="24" height="24"><path fill-rule="evenodd" d="M17 5a1 1 0 0 1 0 2h-4v11a1 1 0 0 1-2 0V7H7a1 1 0 1 1 0-2h10Z"/></svg>', fullscreen: '<svg width="24" height="24"><path d="m15.3 10-1.2-1.3 2.9-3h-2.3a.9.9 0 1 1 0-1.7H19c.5 0 .9.4.9.9v4.4a.9.9 0 1 1-1.8 0V7l-2.9 3Zm0 4 3 3v-2.3a.9.9 0 1 1 1.7 0V19c0 .5-.4.9-.9.9h-4.4a.9.9 0 1 1 0-1.8H17l-3-2.9 1.3-1.2ZM10 15.4l-2.9 3h2.3a.9.9 0 1 1 0 1.7H5a.9.9 0 0 1-.9-.9v-4.4a.9.9 0 1 1 1.8 0V17l2.9-3 1.2 1.3ZM8.7 10 5.7 7v2.3a.9.9 0 0 1-1.7 0V5c0-.5.4-.9.9-.9h4.4a.9.9 0 0 1 0 1.8H7l3 2.9-1.3 1.2Z" fill-rule="nonzero"/></svg>', gallery: '<svg width="24" height="24"><path fill-rule="nonzero" d="m5 15.7 2.3-2.2c.3-.3.7-.3 1 0L11 16l5.1-5c.3-.4.8-.4 1 0l2 1.9V8H5v7.7ZM5 18V19h3l1.8-1.9-2-2L5 17.9Zm14-3-2.5-2.4-6.4 6.5H19v-4ZM4 6h16c.6 0 1 .4 1 1v13c0 .6-.4 1-1 1H4a1 1 0 0 1-1-1V7c0-.6.4-1 1-1Zm6 7a2 2 0 1 1 0-4 2 2 0 0 1 0 4ZM4.5 4h15a.5.5 0 1 1 0 1h-15a.5.5 0 0 1 0-1Zm2-2h11a.5.5 0 1 1 0 1h-11a.5.5 0 0 1 0-1Z"/></svg>', gamma: '<svg width="24" height="24"><path d="M4 3h16c.6 0 1 .4 1 1v16c0 .6-.4 1-1 1H4a1 1 0 0 1-1-1V4c0-.6.4-1 1-1Zm1 2v14h14V5H5Zm6.5 11.8V14L9.2 8.7a5.1 5.1 0 0 0-.4-.8l-.1-.2H8v-1l.3-.1.3-.1h.7a1 1 0 0 1 .6.5l.1.3a8.5 8.5 0 0 1 .3.6l1.9 4.6 2-5.2a1 1 0 0 1 1-.6.5.5 0 0 1 .5.6L13 14v2.8a.7.7 0 0 1-1.4 0Z" fill-rule="nonzero"/></svg>', help: '<svg width="24" height="24"><g fill-rule="evenodd"><path d="M12 5.5a6.5 6.5 0 0 0-6 9 6.3 6.3 0 0 0 1.4 2l1 1a6.3 6.3 0 0 0 3.6 1 6.5 6.5 0 0 0 6-9 6.3 6.3 0 0 0-1.4-2l-1-1a6.3 6.3 0 0 0-3.6-1ZM12 4a7.8 7.8 0 0 1 5.7 2.3A8 8 0 1 1 12 4Z"/><path d="M9.6 9.7a.7.7 0 0 1-.7-.8c0-1.1 1.5-1.8 3.2-1.8 1.8 0 3.2.8 3.2 2.4 0 1.4-.4 2.1-1.5 2.8-.2 0-.3.1-.3.2a2 2 0 0 0-.8.8.8.8 0 0 1-1.4-.6c.3-.7.8-1 1.3-1.5l.4-.2c.7-.4.8-.6.8-1.5 0-.5-.6-.9-1.7-.9-.5 0-1 .1-1.4.3-.2 0-.3.1-.3.2v-.2c0 .4-.4.8-.8.8Z" fill-rule="nonzero"/><circle cx="12" cy="16" r="1"/></g></svg>', "highlight-bg-color": '<svg width="24" height="24"><g fill-rule="evenodd"><path id="tox-icon-highlight-bg-color__color" d="M3 18h18v3H3z"/><path fill-rule="nonzero" d="M7.7 16.7H3l3.3-3.3-.7-.8L10.2 8l4 4.1-4 4.2c-.2.2-.6.2-.8 0l-.6-.7-1.1 1.1zm5-7.5L11 7.4l3-2.9a2 2 0 0 1 2.6 0L18 6c.7.7.7 2 0 2.7l-2.9 2.9-1.8-1.8-.5-.6"/></g></svg>', home: '<svg width="24" height="24"><path fill-rule="nonzero" d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/></svg>', "horizontal-rule": '<svg width="24" height="24"><path d="M4 11h16v2H4z" fill-rule="evenodd"/></svg>', "image-options": '<svg width="24" height="24"><path d="M6 10a2 2 0 0 0-2 2c0 1.1.9 2 2 2a2 2 0 0 0 2-2 2 2 0 0 0-2-2Zm12 0a2 2 0 0 0-2 2c0 1.1.9 2 2 2a2 2 0 0 0 2-2 2 2 0 0 0-2-2Zm-6 0a2 2 0 0 0-2 2c0 1.1.9 2 2 2a2 2 0 0 0 2-2 2 2 0 0 0-2-2Z" fill-rule="nonzero"/></svg>', image: '<svg width="24" height="24"><path d="m5 15.7 3.3-3.2c.3-.3.7-.3 1 0L12 15l4.1-4c.3-.4.8-.4 1 0l2 1.9V5H5v10.7ZM5 18V19h3l2.8-2.9-2-2L5 17.9Zm14-3-2.5-2.4-6.4 6.5H19v-4ZM4 3h16c.6 0 1 .4 1 1v16c0 .6-.4 1-1 1H4a1 1 0 0 1-1-1V4c0-.6.4-1 1-1Zm6 8a2 2 0 1 0 0-4 2 2 0 0 0 0 4Z" fill-rule="nonzero"/></svg>', indent: '<svg width="24" height="24"><path d="M7 5h12c.6 0 1 .4 1 1s-.4 1-1 1H7a1 1 0 1 1 0-2Zm5 4h7c.6 0 1 .4 1 1s-.4 1-1 1h-7a1 1 0 0 1 0-2Zm0 4h7c.6 0 1 .4 1 1s-.4 1-1 1h-7a1 1 0 0 1 0-2Zm-5 4h12a1 1 0 0 1 0 2H7a1 1 0 0 1 0-2Zm-2.6-3.8L6.2 12l-1.8-1.2a1 1 0 0 1 1.2-1.6l3 2a1 1 0 0 1 0 1.6l-3 2a1 1 0 1 1-1.2-1.6Z" fill-rule="evenodd"/></svg>', info: '<svg width="24" height="24"><path d="M12 4a7.8 7.8 0 0 1 5.7 2.3A8 8 0 1 1 12 4Zm-1 3v2h2V7h-2Zm3 10v-1h-1v-5h-3v1h1v4h-1v1h4Z" fill-rule="evenodd"/></svg>', "insert-character": '<svg width="24" height="24"><path d="M15 18h4l1-2v4h-6v-3.3l1.4-1a6 6 0 0 0 1.8-2.9 6.3 6.3 0 0 0-.1-4.1 5.8 5.8 0 0 0-3-3.2c-.6-.3-1.3-.5-2.1-.5a5.1 5.1 0 0 0-3.9 1.8 6.3 6.3 0 0 0-1.3 6 6.2 6.2 0 0 0 1.8 3l1.4.9V20H4v-4l1 2h4v-.5l-2-1L5.4 15A6.5 6.5 0 0 1 4 11c0-1 .2-1.9.6-2.7A7 7 0 0 1 6.3 6C7.1 5.4 8 5 9 4.5c1-.3 2-.5 3.1-.5a8.8 8.8 0 0 1 5.7 2 7 7 0 0 1 1.7 2.3 6 6 0 0 1 .2 4.8c-.2.7-.6 1.3-1 1.9a7.6 7.6 0 0 1-3.6 2.5v.5Z" fill-rule="evenodd"/></svg>', "insert-time": '<svg width="24" height="24"><g fill-rule="nonzero"><path d="M12 19a7 7 0 1 0 0-14 7 7 0 0 0 0 14Zm0 2a9 9 0 1 1 0-18 9 9 0 0 1 0 18Z"/><path d="M16 12h-3V7c0-.6-.4-1-1-1a1 1 0 0 0-1 1v7h5c.6 0 1-.4 1-1s-.4-1-1-1Z"/></g></svg>', invert: '<svg width="24" height="24"><path d="M18 19.3 16.5 18a5.8 5.8 0 0 1-3.1 1.9 6.1 6.1 0 0 1-5.5-1.6A5.8 5.8 0 0 1 6 14v-.3l.1-1.2A13.9 13.9 0 0 1 7.7 9l-3-3 .7-.8 2.8 2.9 9 8.9 1.5 1.6-.7.6Zm0-5.5v.3l-.1 1.1-.4 1-1.2-1.2a4.3 4.3 0 0 0 .2-1v-.2c0-.4 0-.8-.2-1.3l-.5-1.4a14.8 14.8 0 0 0-3-4.2L12 6a26.1 26.1 0 0 0-2.2 2.5l-1-1a20.9 20.9 0 0 1 2.9-3.3L12 4l1 .8a22.2 22.2 0 0 1 4 5.4c.6 1.2 1 2.4 1 3.6Z" fill-rule="evenodd"/></svg>', italic: '<svg width="24" height="24"><path d="m16.7 4.7-.1.9h-.3c-.6 0-1 0-1.4.3-.3.3-.4.6-.5 1.1l-2.1 9.8v.6c0 .5.4.8 1.4.8h.2l-.2.8H8l.2-.8h.2c1.1 0 1.8-.5 2-1.5l2-9.8.1-.5c0-.6-.4-.8-1.4-.8h-.3l.2-.9h5.8Z" fill-rule="evenodd"/></svg>', language: '<svg width="24" height="24"><path d="M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18Zm4.3 13.3c-.5 1-1.2 2-2 2.9a7.5 7.5 0 0 0 3.2-2.1l-.2-.2a6 6 0 0 0-1-.6Zm-8.6 0c-.5.2-.9.5-1.2.8.9 1 2 1.7 3.2 2a10 10 0 0 1-2-2.8Zm3.6-.8c-.8 0-1.6.1-2.2.3.5 1 1.2 1.9 2.1 2.7Zm1.5 0v3c.9-.8 1.6-1.7 2.1-2.7-.6-.2-1.4-.3-2.1-.3Zm-6-2.7H4.5c.2 1 .5 2.1 1 3h.3l1.3-1a10 10 0 0 1-.3-2Zm12.7 0h-2.3c0 .7-.1 1.4-.3 2l1.6 1.1c.5-1 .9-2 1-3.1Zm-3.8 0h-3V14c1 0 2 .1 2.7.4.2-.5.3-1 .3-1.6Zm-4.4 0h-3l.3 1.6c.8-.3 1.7-.4 2.7-.4v-1.3Zm-5.5-5c-.7 1-1.1 2.2-1.3 3.5h2.3c0-1 .2-1.8.5-2.6l-1.5-1Zm2.9 1.4v.1c-.2.6-.4 1.3-.4 2h3V9.4c-1 0-1.8-.1-2.6-.3Zm6.6 0h-.1l-2.4.3v1.8h3l-.5-2.1Zm3-1.4-.3.1-1.3.8c.3.8.5 1.6.5 2.6h2.3a7.5 7.5 0 0 0-1.3-3.5Zm-9 0 2 .2V5.5a9 9 0 0 0-2 2.2Zm3.5-2.3V8c.6 0 1.3 0 1.9-.2a9 9 0 0 0-2-2.3Zm-3-.7h-.1c-1.1.4-2.1 1-3 1.8l1.2.7a10 10 0 0 1 1.9-2.5Zm4.4 0 .1.1a10 10 0 0 1 1.8 2.4l1.1-.7a7.5 7.5 0 0 0-3-1.8Z"/></svg>', "line-height": '<svg width="24" height="24"><path d="M21 5a1 1 0 0 1 .1 2H13a1 1 0 0 1-.1-2H21zm0 4a1 1 0 0 1 .1 2H13a1 1 0 0 1-.1-2H21zm0 4a1 1 0 0 1 .1 2H13a1 1 0 0 1-.1-2H21zm0 4a1 1 0 0 1 .1 2H13a1 1 0 0 1-.1-2H21zM7 3.6l3.7 3.7a1 1 0 0 1-1.3 1.5h-.1L8 7.3v9.2l1.3-1.3a1 1 0 0 1 1.3 0h.1c.4.4.4 1 0 1.3v.1L7 20.4l-3.7-3.7a1 1 0 0 1 1.3-1.5h.1L6 16.7V7.4L4.7 8.7a1 1 0 0 1-1.3 0h-.1a1 1 0 0 1 0-1.3v-.1L7 3.6z"/></svg>', line: '<svg width="24" height="24"><path d="m15 9-8 8H4v-3l8-8 3 3Zm1-1-3-3 1-1h1c-.2 0 0 0 0 0l2 2s0 .2 0 0v1l-1 1ZM4 18h16v2H4v-2Z" fill-rule="evenodd"/></svg>', link: '<svg width="24" height="24"><path d="M6.2 12.3a1 1 0 0 1 1.4 1.4l-2 2a2 2 0 1 0 2.6 2.8l4.8-4.8a1 1 0 0 0 0-1.4 1 1 0 1 1 1.4-1.3 2.9 2.9 0 0 1 0 4L9.6 20a3.9 3.9 0 0 1-5.5-5.5l2-2Zm11.6-.6a1 1 0 0 1-1.4-1.4l2-2a2 2 0 1 0-2.6-2.8L11 10.3a1 1 0 0 0 0 1.4A1 1 0 1 1 9.6 13a2.9 2.9 0 0 1 0-4L14.4 4a3.9 3.9 0 0 1 5.5 5.5l-2 2Z" fill-rule="nonzero"/></svg>', "list-bull-circle": '<svg width="48" height="48"><g fill-rule="evenodd"><path d="M11 16a2 2 0 1 0 0-4 2 2 0 0 0 0 4Zm0 1a3 3 0 1 1 0-6 3 3 0 0 1 0 6ZM11 26a2 2 0 1 0 0-4 2 2 0 0 0 0 4Zm0 1a3 3 0 1 1 0-6 3 3 0 0 1 0 6ZM11 36a2 2 0 1 0 0-4 2 2 0 0 0 0 4Zm0 1a3 3 0 1 1 0-6 3 3 0 0 1 0 6Z" fill-rule="nonzero"/><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/></g></svg>', "list-bull-default": '<svg width="48" height="48"><g fill-rule="evenodd"><circle cx="11" cy="14" r="3"/><circle cx="11" cy="24" r="3"/><circle cx="11" cy="34" r="3"/><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/></g></svg>', "list-bull-square": '<svg width="48" height="48"><g fill-rule="evenodd"><path d="M8 11h6v6H8zM8 21h6v6H8zM8 31h6v6H8z"/><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/></g></svg>', "list-num-default-rtl": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M8 12h22v4H8zM8 22h22v4H8zM8 32h22v4H8z"/><path d="M37.4 17v-4.8h-.1l-1.5 1v-1.1l1.6-1.1h1.2v6zM33.3 17.1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7zm1.7 5.7c0-1.2 1-2 2.2-2 1.3 0 2.1.8 2.1 1.8 0 .7-.3 1.2-1.3 2.2l-1.2 1v.2h2.6v1h-4.3v-.9l2-1.9c.8-.8 1-1.1 1-1.5 0-.5-.4-.8-1-.8-.5 0-.9.3-.9.9H35zm-1.7 4.3c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7zm3.2 7.3v-1h.7c.6 0 1-.3 1-.8 0-.4-.4-.7-1-.7s-1 .3-1 .8H35c0-1.1 1-1.8 2.2-1.8 1.2 0 2.1.6 2.1 1.6 0 .7-.4 1.2-1 1.3v.1c.7.1 1.3.7 1.3 1.4 0 1-1 1.9-2.4 1.9-1.3 0-2.2-.8-2.3-2h1.2c0 .6.5 1 1.1 1 .6 0 1-.4 1-1 0-.5-.3-.8-1-.8h-.7zm-3.3 2.7c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .8.3.8.7 0 .4-.3.7-.8.7z"/></g></svg>', "list-num-default": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/><path d="M10 17v-4.8l-1.5 1v-1.1l1.6-1h1.2V17h-1.2Zm3.6.1c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .7.3.7.7 0 .4-.2.7-.7.7Zm-5 5.7c0-1.2.8-2 2.1-2s2.1.8 2.1 1.8c0 .7-.3 1.2-1.4 2.2l-1.1 1v.2h2.6v1H8.6v-.9l2-1.9c.8-.8 1-1.1 1-1.5 0-.5-.4-.8-1-.8-.5 0-.9.3-.9.9H8.5Zm6.3 4.3c-.5 0-.7-.3-.7-.7 0-.4.2-.7.7-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7ZM10 34.4v-1h.7c.6 0 1-.3 1-.8 0-.4-.4-.7-1-.7s-1 .3-1 .8H8.6c0-1.1 1-1.8 2.2-1.8 1.3 0 2.1.6 2.1 1.6 0 .7-.4 1.2-1 1.3v.1c.8.1 1.3.7 1.3 1.4 0 1-1 1.9-2.4 1.9-1.3 0-2.2-.8-2.3-2h1.2c0 .6.5 1 1.1 1 .7 0 1-.4 1-1 0-.5-.3-.8-1-.8h-.7Zm4.7 2.7c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .8.3.8.7 0 .4-.3.7-.8.7Z"/></g></svg>', "list-num-lower-alpha-rtl": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M8 12h22v4H8zM8 22h22v4H8zM8 32h22v4H8z"/><path d="M36.5 16c-.9 0-1.5-.5-1.5-1.3s.6-1.3 1.8-1.4h1v-.4c0-.4-.2-.6-.7-.6-.4 0-.7.1-.8.4h-1.1c0-.8.8-1.4 2-1.4S39 12 39 13V16h-1.2v-.6c-.3.4-.8.7-1.4.7Zm.4-.8c.6 0 1-.4 1-.9V14h-1c-.5.1-.7.3-.7.6 0 .4.3.6.7.6ZM33.1 16.1c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .8.3.8.7 0 .4-.3.7-.8.7ZM37.7 26c-.7 0-1.2-.2-1.5-.7v.7H35v-6.3h1.2v2.5c.3-.5.8-.9 1.5-.9 1.1 0 1.8 1 1.8 2.4 0 1.5-.7 2.4-1.8 2.4Zm-.5-3.6c-.6 0-1 .5-1 1.3s.4 1.4 1 1.4c.7 0 1-.6 1-1.4 0-.8-.3-1.3-1-1.3ZM33.2 26.1c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .8.3.8.7 0 .4-.3.7-.8.7zm6 7h-1c-.1-.5-.4-.8-1-.8s-1 .5-1 1.4c0 1 .4 1.4 1 1.4.5 0 .9-.2 1-.7h1c0 1-.8 1.7-2 1.7-1.4 0-2.2-.9-2.2-2.4s.8-2.4 2.2-2.4c1.2 0 2 .7 2 1.7zm-6.1 3c-.5 0-.7-.3-.7-.7 0-.4.2-.7.7-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7z"/></g></svg>', "list-num-lower-alpha": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/><path d="M10.3 15.2c.5 0 1-.4 1-.9V14h-1c-.5.1-.8.3-.8.6 0 .4.3.6.8.6Zm-.4.9c-1 0-1.5-.6-1.5-1.4 0-.8.6-1.3 1.7-1.4h1.1v-.4c0-.4-.2-.6-.7-.6-.5 0-.8.1-.9.4h-1c0-.8.8-1.4 2-1.4 1.1 0 1.8.6 1.8 1.6V16h-1.1v-.6h-.1c-.2.4-.7.7-1.3.7Zm4.6 0c-.5 0-.7-.3-.7-.7 0-.4.2-.7.7-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Zm-3.2 10c-.6 0-1.2-.3-1.4-.8v.7H8.5v-6.3H10v2.5c.3-.5.8-.9 1.4-.9 1.2 0 1.9 1 1.9 2.4 0 1.5-.7 2.4-1.9 2.4Zm-.4-3.7c-.7 0-1 .5-1 1.3s.3 1.4 1 1.4c.6 0 1-.6 1-1.4 0-.8-.4-1.3-1-1.3Zm4 3.7c-.5 0-.7-.3-.7-.7 0-.4.2-.7.7-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Zm-2.2 7h-1.2c0-.5-.4-.8-.9-.8-.6 0-1 .5-1 1.4 0 1 .4 1.4 1 1.4.5 0 .8-.2 1-.7h1c0 1-.8 1.7-2 1.7-1.4 0-2.2-.9-2.2-2.4s.8-2.4 2.2-2.4c1.2 0 2 .7 2 1.7Zm1.8 3c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Z"/></g></svg>', "list-num-lower-greek-rtl": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M8 12h22v4H8zM8 22h22v4H8zM8 32h22v4H8z"/><path d="M37.4 16c-1.2 0-2-.8-2-2.3 0-1.5.8-2.4 2-2.4.6 0 1 .4 1.3 1v-.9H40v3.2c0 .4.1.5.4.5h.2v.9h-.6c-.6 0-1-.2-1-.7h-.2c-.2.4-.7.8-1.3.8Zm.3-1c.6 0 1-.5 1-1.3s-.4-1.3-1-1.3-1 .5-1 1.3.4 1.4 1 1.4ZM33.3 16.1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7ZM36 21.9c0-1.5.8-2.3 2.1-2.3 1.2 0 2 .6 2 1.6 0 .6-.3 1-.9 1.3.9.3 1.3.8 1.3 1.7 0 1.2-.7 1.9-1.8 1.9-.6 0-1.1-.3-1.4-.8v2.2H36V22Zm1.8 1.2v-1h.3c.5 0 .9-.2.9-.7 0-.5-.3-.8-.9-.8-.5 0-.8.3-.8 1v2.2c0 .8.4 1.3 1 1.3s1-.4 1-1-.4-1-1.2-1h-.3ZM33.3 26.1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7ZM37.1 34.6 34.8 30h1.4l1.7 3.5 1.7-3.5h1.1l-2.2 4.6v.1c.5.8.7 1.4.7 1.8 0 .4-.2.8-.4 1-.2.2-.6.3-1 .3-.9 0-1.3-.4-1.3-1.2 0-.5.2-1 .5-1.7l.1-.2Zm.7 1a2 2 0 0 0-.4.9c0 .3.1.4.4.4.3 0 .4-.1.4-.4 0-.2-.1-.6-.4-1ZM33.3 36.1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Z"/></g></svg>', "list-num-lower-greek": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/><path d="M10.5 15c.7 0 1-.5 1-1.3s-.3-1.3-1-1.3c-.5 0-.9.5-.9 1.3s.4 1.4 1 1.4Zm-.3 1c-1.1 0-1.8-.8-1.8-2.3 0-1.5.7-2.4 1.8-2.4.7 0 1.1.4 1.3 1h.1v-.9h1.2v3.2c0 .4.1.5.4.5h.2v.9h-.6c-.6 0-1-.2-1.1-.7h-.1c-.2.4-.7.8-1.4.8Zm5 .1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.7-.7.5 0 .8.3.8.7 0 .4-.3.7-.8.7Zm-4.9 7v-1h.3c.6 0 1-.2 1-.7 0-.5-.4-.8-1-.8-.5 0-.8.3-.8 1v2.2c0 .8.4 1.3 1.1 1.3.6 0 1-.4 1-1s-.5-1-1.3-1h-.3ZM8.6 22c0-1.5.7-2.3 2-2.3 1.2 0 2 .6 2 1.6 0 .6-.3 1-.8 1.3.8.3 1.3.8 1.3 1.7 0 1.2-.8 1.9-1.9 1.9-.6 0-1.1-.3-1.3-.8v2.2H8.5V22Zm6.2 4.2c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .7.3.7.7 0 .4-.2.7-.7.7Zm-4.5 8.5L8 30h1.4l1.7 3.5 1.7-3.5h1.1l-2.2 4.6v.1c.5.8.7 1.4.7 1.8 0 .4-.1.8-.4 1-.2.2-.6.3-1 .3-.9 0-1.3-.4-1.3-1.2 0-.5.2-1 .5-1.7l.1-.2Zm.7 1a2 2 0 0 0-.4.9c0 .3.1.4.4.4.3 0 .4-.1.4-.4 0-.2-.1-.6-.4-1Zm4.5.5c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Z"/></g></svg>', "list-num-lower-roman-rtl": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M8 12h22v4H8zM8 22h22v4H8zM8 32h22v4H8z"/><path d="M32.9 16v-1.2h-1.3V16H33Zm0 10v-1.2h-1.3V26H33Zm0 10v-1.2h-1.3V36H33Z"/><path fill-rule="nonzero" d="M36 21h-1.5v5H36zM36 31h-1.5v5H36zM39 21h-1.5v5H39zM39 31h-1.5v5H39zM42 31h-1.5v5H42zM36 11h-1.5v5H36zM36 19h-1.5v1H36zM36 29h-1.5v1H36zM39 19h-1.5v1H39zM39 29h-1.5v1H39zM42 29h-1.5v1H42zM36 9h-1.5v1H36z"/></g></svg>', "list-num-lower-roman": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/><path d="M15.1 16v-1.2h1.3V16H15Zm0 10v-1.2h1.3V26H15Zm0 10v-1.2h1.3V36H15Z"/><path fill-rule="nonzero" d="M12 21h1.5v5H12zM12 31h1.5v5H12zM9 21h1.5v5H9zM9 31h1.5v5H9zM6 31h1.5v5H6zM12 11h1.5v5H12zM12 19h1.5v1H12zM12 29h1.5v1H12zM9 19h1.5v1H9zM9 29h1.5v1H9zM6 29h1.5v1H6zM12 9h1.5v1H12z"/></g></svg>', "list-num-upper-alpha-rtl": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M8 12h22v4H8zM8 22h22v4H8zM8 32h22v4H8z"/><path d="m39.3 17-.5-1.4h-2l-.5 1.4H35l2-6h1.6l2 6h-1.3Zm-1.6-4.7-.7 2.3h1.6l-.8-2.3ZM33.4 17c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .7.3.7.7 0 .4-.2.7-.7.7Zm4.7 9.9h-2.7v-6H38c1.2 0 1.9.6 1.9 1.5 0 .6-.5 1.2-1 1.3.7.1 1.3.7 1.3 1.5 0 1-.8 1.7-2 1.7Zm-1.4-5v1.5h1c.6 0 1-.3 1-.8 0-.4-.4-.7-1-.7h-1Zm0 4h1.1c.7 0 1.1-.3 1.1-.8 0-.6-.4-.9-1.1-.9h-1.1V26ZM33 27.1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Zm4.9 10c-1.8 0-2.8-1.1-2.8-3.1s1-3.1 2.8-3.1c1.4 0 2.5.9 2.6 2.2h-1.3c0-.7-.6-1.1-1.3-1.1-1 0-1.6.7-1.6 2s.6 2 1.6 2c.7 0 1.2-.4 1.4-1h1.2c-.1 1.3-1.2 2.2-2.6 2.2Zm-4.5 0c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Z"/></g></svg>', "list-num-upper-alpha": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/><path d="m12.6 17-.5-1.4h-2L9.5 17H8.3l2-6H12l2 6h-1.3ZM11 12.3l-.7 2.3h1.6l-.8-2.3Zm4.7 4.8c-.4 0-.7-.3-.7-.7 0-.4.3-.7.7-.7.5 0 .7.3.7.7 0 .4-.2.7-.7.7ZM11.4 27H8.7v-6h2.6c1.2 0 1.9.6 1.9 1.5 0 .6-.5 1.2-1 1.3.7.1 1.3.7 1.3 1.5 0 1-.8 1.7-2 1.7ZM10 22v1.5h1c.6 0 1-.3 1-.8 0-.4-.4-.7-1-.7h-1Zm0 4H11c.7 0 1.1-.3 1.1-.8 0-.6-.4-.9-1.1-.9H10V26Zm5.4 1.1c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Zm-4.1 10c-1.8 0-2.8-1.1-2.8-3.1s1-3.1 2.8-3.1c1.4 0 2.5.9 2.6 2.2h-1.3c0-.7-.6-1.1-1.3-1.1-1 0-1.6.7-1.6 2s.6 2 1.6 2c.7 0 1.2-.4 1.4-1h1.2c-.1 1.3-1.2 2.2-2.6 2.2Zm4.5 0c-.5 0-.8-.3-.8-.7 0-.4.3-.7.8-.7.4 0 .7.3.7.7 0 .4-.3.7-.7.7Z"/></g></svg>', "list-num-upper-roman-rtl": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M8 12h22v4H8zM8 22h22v4H8zM8 32h22v4H8z"/><path d="M31.6 17v-1.2H33V17h-1.3Zm0 10v-1.2H33V27h-1.3Zm0 10v-1.2H33V37h-1.3Z"/><path fill-rule="nonzero" d="M34.5 20H36v7h-1.5zM34.5 30H36v7h-1.5zM37.5 20H39v7h-1.5zM37.5 30H39v7h-1.5zM40.5 30H42v7h-1.5zM34.5 10H36v7h-1.5z"/></g></svg>', "list-num-upper-roman": '<svg width="48" height="48"><g fill-rule="evenodd"><path opacity=".2" d="M18 12h22v4H18zM18 22h22v4H18zM18 32h22v4H18z"/><path d="M15.1 17v-1.2h1.3V17H15Zm0 10v-1.2h1.3V27H15Zm0 10v-1.2h1.3V37H15Z"/><path fill-rule="nonzero" d="M12 20h1.5v7H12zM12 30h1.5v7H12zM9 20h1.5v7H9zM9 30h1.5v7H9zM6 30h1.5v7H6zM12 10h1.5v7H12z"/></g></svg>', lock: '<svg width="24" height="24"><path d="M16.3 11c.2 0 .3 0 .5.2l.2.6v7.4c0 .3 0 .4-.2.6l-.6.2H7.8c-.3 0-.4 0-.6-.2a.7.7 0 0 1-.2-.6v-7.4c0-.3 0-.4.2-.6l.5-.2H8V8c0-.8.3-1.5.9-2.1.6-.6 1.3-.9 2.1-.9h2c.8 0 1.5.3 2.1.9.6.6.9 1.3.9 2.1v3h.3ZM10 8v3h4V8a1 1 0 0 0-.3-.7A1 1 0 0 0 13 7h-2a1 1 0 0 0-.7.3 1 1 0 0 0-.3.7Z" fill-rule="evenodd"/></svg>', ltr: '<svg width="24" height="24"><path d="M11 5h7a1 1 0 0 1 0 2h-1v11a1 1 0 0 1-2 0V7h-2v11a1 1 0 0 1-2 0v-6c-.5 0-1 0-1.4-.3A3.4 3.4 0 0 1 7.8 10a3.3 3.3 0 0 1 0-2.8 3.4 3.4 0 0 1 1.8-1.8L11 5ZM4.4 16.2 6.2 15l-1.8-1.2a1 1 0 0 1 1.2-1.6l3 2a1 1 0 0 1 0 1.6l-3 2a1 1 0 1 1-1.2-1.6Z" fill-rule="evenodd"/></svg>', "more-drawer": '<svg width="24" height="24"><path d="M6 10a2 2 0 0 0-2 2c0 1.1.9 2 2 2a2 2 0 0 0 2-2 2 2 0 0 0-2-2Zm12 0a2 2 0 0 0-2 2c0 1.1.9 2 2 2a2 2 0 0 0 2-2 2 2 0 0 0-2-2Zm-6 0a2 2 0 0 0-2 2c0 1.1.9 2 2 2a2 2 0 0 0 2-2 2 2 0 0 0-2-2Z" fill-rule="nonzero"/></svg>', "new-document": '<svg width="24" height="24"><path d="M14.4 3H7a2 2 0 0 0-2 2v14c0 1.1.9 2 2 2h10a2 2 0 0 0 2-2V7.6L14.4 3ZM17 19H7V5h6v4h4v10Z" fill-rule="nonzero"/></svg>', "new-tab": '<svg width="24" height="24"><path d="m15 13 2-2v8H5V7h8l-2 2H7v8h8v-4Zm4-8v5.5l-2-2-5.6 5.5H10v-1.4L15.5 7l-2-2H19Z" fill-rule="evenodd"/></svg>', "non-breaking": '<svg width="24" height="24"><path d="M11 11H8a1 1 0 1 1 0-2h3V6c0-.6.4-1 1-1s1 .4 1 1v3h3c.6 0 1 .4 1 1s-.4 1-1 1h-3v3c0 .6-.4 1-1 1a1 1 0 0 1-1-1v-3Zm10 4v5H3v-5c0-.6.4-1 1-1s1 .4 1 1v3h14v-3c0-.6.4-1 1-1s1 .4 1 1Z" fill-rule="evenodd"/></svg>', notice: '<svg width="24" height="24"><path d="M17.8 9.8 15.4 4 20 8.5v7L15.5 20h-7L4 15.5v-7L8.5 4h7l2.3 5.8Zm0 0 2.2 5.7-2.3-5.8ZM13 17v-2h-2v2h2Zm0-4V7h-2v6h2Z" fill-rule="evenodd"/></svg>', "ordered-list-rtl": '<svg width="24" height="24"><path d="M6 17h8a1 1 0 0 1 0 2H6a1 1 0 0 1 0-2Zm0-6h8a1 1 0 0 1 0 2H6a1 1 0 0 1 0-2Zm0-6h8a1 1 0 0 1 0 2H6a1 1 0 1 1 0-2Zm13-1v3.5a.5.5 0 1 1-1 0V5h-.5a.5.5 0 1 1 0-1H19Zm-1 8.8.2.2h1.3a.5.5 0 1 1 0 1h-1.6a1 1 0 0 1-.9-1V13c0-.4.3-.8.6-1l1.2-.4.2-.3a.2.2 0 0 0-.2-.2h-1.3a.5.5 0 0 1-.5-.5c0-.3.2-.5.5-.5h1.6c.5 0 .9.4.9 1v.1c0 .4-.3.8-.6 1l-1.2.4-.2.3Zm2 4.2v2c0 .6-.4 1-1 1h-1.5a.5.5 0 0 1 0-1h1.2a.3.3 0 1 0 0-.6h-1.3a.4.4 0 1 1 0-.8h1.3a.3.3 0 0 0 0-.6h-1.2a.5.5 0 1 1 0-1H19c.6 0 1 .4 1 1Z" fill-rule="evenodd"/></svg>', "ordered-list": '<svg width="24" height="24"><path d="M10 17h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0-6h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0-6h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 1 1 0-2ZM6 4v3.5c0 .3-.2.5-.5.5a.5.5 0 0 1-.5-.5V5h-.5a.5.5 0 0 1 0-1H6Zm-1 8.8.2.2h1.3c.3 0 .5.2.5.5s-.2.5-.5.5H4.9a1 1 0 0 1-.9-1V13c0-.4.3-.8.6-1l1.2-.4.2-.3a.2.2 0 0 0-.2-.2H4.5a.5.5 0 0 1-.5-.5c0-.3.2-.5.5-.5h1.6c.5 0 .9.4.9 1v.1c0 .4-.3.8-.6 1l-1.2.4-.2.3ZM7 17v2c0 .6-.4 1-1 1H4.5a.5.5 0 0 1 0-1h1.2c.2 0 .3-.1.3-.3 0-.2-.1-.3-.3-.3H4.4a.4.4 0 1 1 0-.8h1.3c.2 0 .3-.1.3-.3 0-.2-.1-.3-.3-.3H4.5a.5.5 0 1 1 0-1H6c.6 0 1 .4 1 1Z" fill-rule="evenodd"/></svg>', orientation: '<svg width="24" height="24"><path d="M7.3 6.4 1 13l6.4 6.5 6.5-6.5-6.5-6.5ZM3.7 13l3.6-3.7L11 13l-3.7 3.7-3.6-3.7ZM12 6l2.8 2.7c.3.3.3.8 0 1-.3.4-.9.4-1.2 0L9.2 5.7a.8.8 0 0 1 0-1.2L13.6.2c.3-.3.9-.3 1.2 0 .3.3.3.8 0 1.1L12 4h1a9 9 0 1 1-4.3 16.9l1.5-1.5A7 7 0 1 0 13 6h-1Z" fill-rule="nonzero"/></svg>', outdent: '<svg width="24" height="24"><path d="M7 5h12c.6 0 1 .4 1 1s-.4 1-1 1H7a1 1 0 1 1 0-2Zm5 4h7c.6 0 1 .4 1 1s-.4 1-1 1h-7a1 1 0 0 1 0-2Zm0 4h7c.6 0 1 .4 1 1s-.4 1-1 1h-7a1 1 0 0 1 0-2Zm-5 4h12a1 1 0 0 1 0 2H7a1 1 0 0 1 0-2Zm1.6-3.8a1 1 0 0 1-1.2 1.6l-3-2a1 1 0 0 1 0-1.6l3-2a1 1 0 0 1 1.2 1.6L6.8 12l1.8 1.2Z" fill-rule="evenodd"/></svg>', "page-break": '<svg width="24" height="24"><g fill-rule="evenodd"><path d="M5 11c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 0 1 0-2Zm3 0h1c.6 0 1 .4 1 1s-.4 1-1 1H8a1 1 0 0 1 0-2Zm4 0c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 0 1 0-2Zm3 0h1c.6 0 1 .4 1 1s-.4 1-1 1h-1a1 1 0 0 1 0-2Zm4 0c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 0 1 0-2ZM7 3v5h10V3c0-.6.4-1 1-1s1 .4 1 1v7H5V3c0-.6.4-1 1-1s1 .4 1 1ZM6 22a1 1 0 0 1-1-1v-7h14v7c0 .6-.4 1-1 1a1 1 0 0 1-1-1v-5H7v5c0 .6-.4 1-1 1Z"/></g></svg>', paragraph: '<svg width="24" height="24"><path fill-rule="evenodd" d="M10 5h7a1 1 0 0 1 0 2h-1v11a1 1 0 0 1-2 0V7h-2v11a1 1 0 0 1-2 0v-6c-.5 0-1 0-1.4-.3A3.4 3.4 0 0 1 6.8 10a3.3 3.3 0 0 1 0-2.8 3.4 3.4 0 0 1 1.8-1.8L10 5Z"/></svg>', "paste-column-after": '<svg width="24" height="24"><path fill-rule="evenodd" d="M12 1a3 3 0 0 1 2.8 2H18c1 0 2 .8 2 1.9V7h-2V5h-2v1c0 .6-.4 1-1 1H9a1 1 0 0 1-1-1V5H6v13h7v2H6c-1 0-2-.8-2-1.9V5c0-1 .8-2 1.9-2H9.2A3 3 0 0 1 12 1Zm8 7v12h-6V8h6Zm-1.5 1.5h-3v9h3v-9ZM12 3a1 1 0 1 0 0 2 1 1 0 0 0 0-2Z"/></svg>', "paste-column-before": '<svg width="24" height="24"><path fill-rule="evenodd" d="M12 1a3 3 0 0 1 2.8 2H18c1 0 2 .8 2 1.9V18c0 1-.8 2-1.9 2H11v-2h7V5h-2v1c0 .6-.4 1-1 1H9a1 1 0 0 1-1-1V5H6v2H4V5c0-1 .8-2 1.9-2H9.2A3 3 0 0 1 12 1Zm-2 7v12H4V8h6ZM8.5 9.5h-3v9h3v-9ZM12 3a1 1 0 1 0 0 2 1 1 0 0 0 0-2Z"/></svg>', "paste-row-after": '<svg width="24" height="24"><path fill-rule="evenodd" d="M12 1a3 3 0 0 1 2.8 2H18c1 0 2 .8 2 1.9V11h-2V5h-2v1c0 .6-.4 1-1 1H9a1 1 0 0 1-1-1V5H6v13h14c0 1-.8 2-1.9 2H6c-1 0-2-.8-2-1.9V5c0-1 .8-2 1.9-2H9.2A3 3 0 0 1 12 1Zm10 11v5H8v-5h14Zm-1.5 1.5h-11v2h11v-2ZM12 3a1 1 0 1 0 0 2 1 1 0 0 0 0-2Z"/></svg>', "paste-row-before": '<svg width="24" height="24"><path fill-rule="evenodd" d="M12 1a3 3 0 0 1 2.8 2H18c1 0 2 .8 2 1.9V7h-2V5h-2v1c0 .6-.4 1-1 1H9a1 1 0 0 1-1-1V5H6v13h12v-4h2v4c0 1-.8 2-1.9 2H6c-1 0-2-.8-2-1.9V5c0-1 .8-2 1.9-2H9.2A3 3 0 0 1 12 1Zm10 7v5H8V8h14Zm-1.5 1.5h-11v2h11v-2ZM12 3a1 1 0 1 0 0 2 1 1 0 0 0 0-2Z"/></svg>', "paste-text": '<svg width="24" height="24"><path d="M18 9V5h-2v1c0 .6-.4 1-1 1H9a1 1 0 0 1-1-1V5H6v13h3V9h9ZM9 20H6a2 2 0 0 1-2-2V5c0-1.1.9-2 2-2h3.2A3 3 0 0 1 12 1a3 3 0 0 1 2.8 2H18a2 2 0 0 1 2 2v4h1v12H9v-1Zm1.5-9.5v9h9v-9h-9ZM12 3a1 1 0 0 0-1 1c0 .5.4 1 1 1s1-.5 1-1-.4-1-1-1Zm0 9h6v2h-.5l-.5-1h-1v4h.8v1h-3.6v-1h.8v-4h-1l-.5 1H12v-2Z" fill-rule="nonzero"/></svg>', paste: '<svg width="24" height="24"><path d="M18 9V5h-2v1c0 .6-.4 1-1 1H9a1 1 0 0 1-1-1V5H6v13h3V9h9ZM9 20H6a2 2 0 0 1-2-2V5c0-1.1.9-2 2-2h3.2A3 3 0 0 1 12 1a3 3 0 0 1 2.8 2H18a2 2 0 0 1 2 2v4h1v12H9v-1Zm1.5-9.5v9h9v-9h-9ZM12 3a1 1 0 0 0-1 1c0 .5.4 1 1 1s1-.5 1-1-.4-1-1-1Z" fill-rule="nonzero"/></svg>', "permanent-pen": '<svg width="24" height="24"><path d="M10.5 17.5 8 20H3v-3l3.5-3.5a2 2 0 0 1 0-3L14 3l1 1-7.3 7.3a1 1 0 0 0 0 1.4l3.6 3.6c.4.4 1 .4 1.4 0L20 9l1 1-7.6 7.6a2 2 0 0 1-2.8 0l-.1-.1Z" fill-rule="nonzero"/></svg>', plus: '<svg width="24" height="24"><path d="M12 4c.5 0 1 .4 1 .9V11h6a1 1 0 0 1 .1 2H13v6a1 1 0 0 1-2 .1V13H5a1 1 0 0 1-.1-2H11V5c0-.6.4-1 1-1Z"/></svg>', preferences: '<svg width="24" height="24"><path d="m20.1 13.5-1.9.2a5.8 5.8 0 0 1-.6 1.5l1.2 1.5c.4.4.3 1 0 1.4l-.7.7a1 1 0 0 1-1.4 0l-1.5-1.2a6.2 6.2 0 0 1-1.5.6l-.2 1.9c0 .5-.5.9-1 .9h-1a1 1 0 0 1-1-.9l-.2-1.9a5.8 5.8 0 0 1-1.5-.6l-1.5 1.2a1 1 0 0 1-1.4 0l-.7-.7a1 1 0 0 1 0-1.4l1.2-1.5a6.2 6.2 0 0 1-.6-1.5l-1.9-.2a1 1 0 0 1-.9-1v-1c0-.5.4-1 .9-1l1.9-.2a5.8 5.8 0 0 1 .6-1.5L5.2 7.3a1 1 0 0 1 0-1.4l.7-.7a1 1 0 0 1 1.4 0l1.5 1.2a6.2 6.2 0 0 1 1.5-.6l.2-1.9c0-.5.5-.9 1-.9h1c.5 0 1 .4 1 .9l.2 1.9a5.8 5.8 0 0 1 1.5.6l1.5-1.2a1 1 0 0 1 1.4 0l.7.7c.3.4.4 1 0 1.4l-1.2 1.5a6.2 6.2 0 0 1 .6 1.5l1.9.2c.5 0 .9.5.9 1v1c0 .5-.4 1-.9 1ZM12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z" fill-rule="evenodd"/></svg>', preview: '<svg width="24" height="24"><path d="M3.5 12.5c.5.8 1.1 1.6 1.8 2.3 2 2 4.2 3.2 6.7 3.2s4.7-1.2 6.7-3.2a16.2 16.2 0 0 0 2.1-2.8 15.7 15.7 0 0 0-2.1-2.8c-2-2-4.2-3.2-6.7-3.2a9.3 9.3 0 0 0-6.7 3.2A16.2 16.2 0 0 0 3.2 12c0 .2.2.3.3.5Zm-2.4-1 .7-1.2L4 7.8C6.2 5.4 8.9 4 12 4c3 0 5.8 1.4 8.1 3.8a18.2 18.2 0 0 1 2.8 3.7v1l-.7 1.2-2.1 2.5c-2.3 2.4-5 3.8-8.1 3.8-3 0-5.8-1.4-8.1-3.8a18.2 18.2 0 0 1-2.8-3.7 1 1 0 0 1 0-1Zm12-3.3a2 2 0 1 0 2.7 2.6 4 4 0 1 1-2.6-2.6Z" fill-rule="nonzero"/></svg>', print: '<svg width="24" height="24"><path d="M18 8H6a3 3 0 0 0-3 3v6h2v3h14v-3h2v-6a3 3 0 0 0-3-3Zm-1 10H7v-4h10v4Zm.5-5c-.8 0-1.5-.7-1.5-1.5s.7-1.5 1.5-1.5 1.5.7 1.5 1.5-.7 1.5-1.5 1.5Zm.5-8H6v2h12V5Z" fill-rule="nonzero"/></svg>', quote: '<svg width="24" height="24"><path d="M7.5 17h.9c.4 0 .7-.2.9-.6L11 13V8c0-.6-.4-1-1-1H6a1 1 0 0 0-1 1v4c0 .6.4 1 1 1h2l-1.3 2.7a1 1 0 0 0 .8 1.3Zm8 0h.9c.4 0 .7-.2.9-.6L19 13V8c0-.6-.4-1-1-1h-4a1 1 0 0 0-1 1v4c0 .6.4 1 1 1h2l-1.3 2.7a1 1 0 0 0 .8 1.3Z" fill-rule="nonzero"/></svg>', redo: '<svg width="24" height="24"><path d="M17.6 10H12c-2.8 0-4.4 1.4-4.9 3.5-.4 2 .3 4 1.4 4.6a1 1 0 1 1-1 1.8c-2-1.2-2.9-4.1-2.3-6.8.6-3 3-5.1 6.8-5.1h5.6l-3.3-3.3a1 1 0 1 1 1.4-1.4l5 5a1 1 0 0 1 0 1.4l-5 5a1 1 0 0 1-1.4-1.4l3.3-3.3Z" fill-rule="nonzero"/></svg>', reload: '<svg width="24" height="24"><g fill-rule="nonzero"><path d="m5 22.1-1.2-4.7v-.2a1 1 0 0 1 1-1l5 .4a1 1 0 1 1-.2 2l-2.2-.2a7.8 7.8 0 0 0 8.4.2 7.5 7.5 0 0 0 3.5-6.4 1 1 0 1 1 2 0 9.5 9.5 0 0 1-4.5 8 9.9 9.9 0 0 1-10.2 0l.4 1.4a1 1 0 1 1-2 .5ZM13.6 7.4c0-.5.5-1 1-.9l2.8.2a8 8 0 0 0-9.5-1 7.5 7.5 0 0 0-3.6 7 1 1 0 0 1-2 0 9.5 9.5 0 0 1 4.5-8.6 10 10 0 0 1 10.9.3l-.3-1a1 1 0 0 1 2-.5l1.1 4.8a1 1 0 0 1-1 1.2l-5-.4a1 1 0 0 1-.9-1Z"/></g></svg>', "remove-formatting": '<svg width="24" height="24"><path d="M13.2 6a1 1 0 0 1 0 .2l-2.6 10a1 1 0 0 1-1 .8h-.2a.8.8 0 0 1-.8-1l2.6-10H8a1 1 0 1 1 0-2h9a1 1 0 0 1 0 2h-3.8ZM5 18h7a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Zm13 1.5L16.5 18 15 19.5a.7.7 0 0 1-1-1l1.5-1.5-1.5-1.5a.7.7 0 0 1 1-1l1.5 1.5 1.5-1.5a.7.7 0 0 1 1 1L17.5 17l1.5 1.5a.7.7 0 0 1-1 1Z" fill-rule="evenodd"/></svg>', remove: '<svg width="24" height="24"><path d="M16 7h3a1 1 0 0 1 0 2h-1v9a3 3 0 0 1-3 3H9a3 3 0 0 1-3-3V9H5a1 1 0 1 1 0-2h3V6a3 3 0 0 1 3-3h2a3 3 0 0 1 3 3v1Zm-2 0V6c0-.6-.4-1-1-1h-2a1 1 0 0 0-1 1v1h4Zm2 2H8v9c0 .6.4 1 1 1h6c.6 0 1-.4 1-1V9Zm-7 3a1 1 0 0 1 2 0v4a1 1 0 0 1-2 0v-4Zm4 0a1 1 0 0 1 2 0v4a1 1 0 0 1-2 0v-4Z" fill-rule="nonzero"/></svg>', "resize-handle": '<svg width="10" height="10"><g fill-rule="nonzero"><path d="M8.1 1.1A.5.5 0 1 1 9 2l-7 7A.5.5 0 1 1 1 8l7-7ZM8.1 5.1A.5.5 0 1 1 9 6l-3 3A.5.5 0 1 1 5 8l3-3Z"/></g></svg>', resize: '<svg width="24" height="24"><path d="M4 5c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3h6c.3 0 .5.1.7.3.2.2.3.4.3.7 0 .3-.1.5-.3.7a1 1 0 0 1-.7.3H7.4L18 16.6V13c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3.3 0 .5.1.7.3.2.2.3.4.3.7v6c0 .3-.1.5-.3.7a1 1 0 0 1-.7.3h-6a1 1 0 0 1-.7-.3 1 1 0 0 1-.3-.7c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3h3.6L6 7.4V11c0 .3-.1.5-.3.7a1 1 0 0 1-.7.3 1 1 0 0 1-.7-.3A1 1 0 0 1 4 11V5Z" fill-rule="evenodd"/></svg>', "restore-draft": '<svg width="24" height="24"><g fill-rule="evenodd"><path d="M17 13c0 .6-.4 1-1 1h-4V8c0-.6.4-1 1-1s1 .4 1 1v4h2c.6 0 1 .4 1 1Z"/><path d="M4.7 10H9a1 1 0 0 1 0 2H3a1 1 0 0 1-1-1V5a1 1 0 1 1 2 0v3l2.5-2.4a9.2 9.2 0 0 1 10.8-1.5A9 9 0 0 1 13.4 21c-2.4.1-4.7-.7-6.5-2.2a1 1 0 1 1 1.3-1.5 7.2 7.2 0 0 0 11.6-3.7 7 7 0 0 0-3.5-7.7A7.2 7.2 0 0 0 8 7L4.7 10Z" fill-rule="nonzero"/></g></svg>', "rotate-left": '<svg width="24" height="24"><path d="M4.7 10H9a1 1 0 0 1 0 2H3a1 1 0 0 1-1-1V5a1 1 0 1 1 2 0v3l2.5-2.4a9.2 9.2 0 0 1 10.8-1.5A9 9 0 0 1 13.4 21c-2.4.1-4.7-.7-6.5-2.2a1 1 0 1 1 1.3-1.5 7.2 7.2 0 0 0 11.6-3.7 7 7 0 0 0-3.5-7.7A7.2 7.2 0 0 0 8 7L4.7 10Z" fill-rule="nonzero"/></svg>', "rotate-right": '<svg width="24" height="24"><path d="M20 8V5a1 1 0 0 1 2 0v6c0 .6-.4 1-1 1h-6a1 1 0 0 1 0-2h4.3L16 7A7.2 7.2 0 0 0 7.7 6a7 7 0 0 0 3 13.1c1.9.1 3.7-.5 5-1.7a1 1 0 0 1 1.4 1.5A9.2 9.2 0 0 1 2.2 14c-.9-3.9 1-8 4.5-9.9 3.5-1.9 8-1.3 10.8 1.5L20 8Z" fill-rule="nonzero"/></svg>', rtl: '<svg width="24" height="24"><path d="M8 5h8v2h-2v12h-2V7h-2v12H8v-7c-.5 0-1 0-1.4-.3A3.4 3.4 0 0 1 4.8 10a3.3 3.3 0 0 1 0-2.8 3.4 3.4 0 0 1 1.8-1.8L8 5Zm12 11.2a1 1 0 1 1-1 1.6l-3-2a1 1 0 0 1 0-1.6l3-2a1 1 0 1 1 1 1.6L18.4 15l1.8 1.2Z" fill-rule="evenodd"/></svg>', save: '<svg width="24" height="24"><path d="M5 16h14a2 2 0 0 1 2 2v2a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-2c0-1.1.9-2 2-2Zm0 2v2h14v-2H5Zm10 0h2v2h-2v-2Zm-4-6.4L8.7 9.3a1 1 0 1 0-1.4 1.4l4 4c.4.4 1 .4 1.4 0l4-4a1 1 0 1 0-1.4-1.4L13 11.6V4a1 1 0 0 0-2 0v7.6Z" fill-rule="nonzero"/></svg>', search: '<svg width="24" height="24"><path d="M16 17.3a8 8 0 1 1 1.4-1.4l4.3 4.4a1 1 0 0 1-1.4 1.4l-4.4-4.3Zm-5-.3a6 6 0 1 0 0-12 6 6 0 0 0 0 12Z" fill-rule="nonzero"/></svg>', "select-all": '<svg width="24" height="24"><path d="M3 5h2V3a2 2 0 0 0-2 2Zm0 8h2v-2H3v2Zm4 8h2v-2H7v2ZM3 9h2V7H3v2Zm10-6h-2v2h2V3Zm6 0v2h2a2 2 0 0 0-2-2ZM5 21v-2H3c0 1.1.9 2 2 2Zm-2-4h2v-2H3v2ZM9 3H7v2h2V3Zm2 18h2v-2h-2v2Zm8-8h2v-2h-2v2Zm0 8a2 2 0 0 0 2-2h-2v2Zm0-12h2V7h-2v2Zm0 8h2v-2h-2v2Zm-4 4h2v-2h-2v2Zm0-16h2V3h-2v2ZM7 17h10V7H7v10Zm2-8h6v6H9V9Z" fill-rule="nonzero"/></svg>', selected: '<svg width="24" height="24"><path fill-rule="nonzero" d="M6 4h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2Zm3.6 10.9L7 12.3a.7.7 0 0 0-1 1L9.6 17 18 8.6a.7.7 0 0 0 0-1 .7.7 0 0 0-1 0l-7.4 7.3Z"/></svg>', settings: '<svg width="24" height="24"><path d="M11 6h8c.6 0 1 .4 1 1s-.4 1-1 1h-8v.3c0 .2 0 .3-.2.5l-.6.2H7.8c-.3 0-.4 0-.6-.2a.7.7 0 0 1-.2-.6V8H5a1 1 0 1 1 0-2h2v-.3c0-.2 0-.3.2-.5l.5-.2h2.5c.3 0 .4 0 .6.2l.2.5V6ZM8 8h2V6H8v2Zm9 2.8v.2h2c.6 0 1 .4 1 1s-.4 1-1 1h-2v.3c0 .2 0 .3-.2.5l-.6.2h-2.4c-.3 0-.4 0-.6-.2a.7.7 0 0 1-.2-.6V13H5a1 1 0 0 1 0-2h8v-.3c0-.2 0-.3.2-.5l.6-.2h2.4c.3 0 .4 0 .6.2l.2.6ZM14 13h2v-2h-2v2Zm-3 2.8v.2h8c.6 0 1 .4 1 1s-.4 1-1 1h-8v.3c0 .2 0 .3-.2.5l-.6.2H7.8c-.3 0-.4 0-.6-.2a.7.7 0 0 1-.2-.6V18H5a1 1 0 0 1 0-2h2v-.3c0-.2 0-.3.2-.5l.5-.2h2.5c.3 0 .4 0 .6.2l.2.6ZM8 18h2v-2H8v2Z" fill-rule="evenodd"/></svg>', sharpen: '<svg width="24" height="24"><path d="m16 6 4 4-8 9-8-9 4-4h8Zm-4 10.2 5.5-6.2-.1-.1H12v-.3h5.1l-.2-.2H12V9h4.6l-.2-.2H12v-.3h4.1l-.2-.2H12V8h3.6l-.2-.2H8.7L6.5 10l.1.1H12v.3H6.9l.2.2H12v.3H7.3l.2.2H12v.3H7.7l.3.2h4v.3H8.2l.2.2H12v.3H8.6l.3.2H12v.3H9l.3.2H12v.3H9.5l.2.2H12v.3h-2l.2.2H12v.3h-1.6l.2.2H12v.3h-1.1l.2.2h.9v.3h-.7l.2.2h.5v.3h-.3l.3.2Z" fill-rule="evenodd"/></svg>', sourcecode: '<svg width="24" height="24"><g fill-rule="nonzero"><path d="M9.8 15.7c.3.3.3.8 0 1-.3.4-.9.4-1.2 0l-4.4-4.1a.8.8 0 0 1 0-1.2l4.4-4.2c.3-.3.9-.3 1.2 0 .3.3.3.8 0 1.1L6 12l3.8 3.7ZM14.2 15.7c-.3.3-.3.8 0 1 .4.4.9.4 1.2 0l4.4-4.1c.3-.3.3-.9 0-1.2l-4.4-4.2a.8.8 0 0 0-1.2 0c-.3.3-.3.8 0 1.1L18 12l-3.8 3.7Z"/></g></svg>', "spell-check": '<svg width="24" height="24"><path d="M6 8v3H5V5c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3h2c.3 0 .5.1.7.3.2.2.3.4.3.7v6H8V8H6Zm0-3v2h2V5H6Zm13 0h-3v5h3v1h-3a1 1 0 0 1-.7-.3 1 1 0 0 1-.3-.7V5c0-.3.1-.5.3-.7.2-.2.4-.3.7-.3h3v1Zm-5 1.5-.1.7c-.1.2-.3.3-.6.3.3 0 .5.1.6.3l.1.7V10c0 .3-.1.5-.3.7a1 1 0 0 1-.7.3h-3V4h3c.3 0 .5.1.7.3.2.2.3.4.3.7v1.5ZM13 10V8h-2v2h2Zm0-3V5h-2v2h2Zm3 5 1 1-6.5 7L7 15.5l1.3-1 2.2 2.2L16 12Z" fill-rule="evenodd"/></svg>', "strike-through": '<svg width="24" height="24"><g fill-rule="evenodd"><path d="M15.6 8.5c-.5-.7-1-1.1-1.3-1.3-.6-.4-1.3-.6-2-.6-2.7 0-2.8 1.7-2.8 2.1 0 1.6 1.8 2 3.2 2.3 4.4.9 4.6 2.8 4.6 3.9 0 1.4-.7 4.1-5 4.1A6.2 6.2 0 0 1 7 16.4l1.5-1.1c.4.6 1.6 2 3.7 2 1.6 0 2.5-.4 3-1.2.4-.8.3-2-.8-2.6-.7-.4-1.6-.7-2.9-1-1-.2-3.9-.8-3.9-3.6C7.6 6 10.3 5 12.4 5c2.9 0 4.2 1.6 4.7 2.4l-1.5 1.1Z"/><path d="M5 11h14a1 1 0 0 1 0 2H5a1 1 0 0 1 0-2Z" fill-rule="nonzero"/></g></svg>', subscript: '<svg width="24" height="24"><path d="m10.4 10 4.6 4.6-1.4 1.4L9 11.4 4.4 16 3 14.6 7.6 10 3 5.4 4.4 4 9 8.6 13.6 4 15 5.4 10.4 10ZM21 19h-5v-1l1-.8 1.7-1.6c.3-.4.5-.8.5-1.2 0-.3 0-.6-.2-.7-.2-.2-.5-.3-.9-.3a2 2 0 0 0-.8.2l-.7.3-.4-1.1 1-.6 1.2-.2c.8 0 1.4.3 1.8.7.4.4.6.9.6 1.5s-.2 1.1-.5 1.6a8 8 0 0 1-1.3 1.3l-.6.6h2.6V19Z" fill-rule="nonzero"/></svg>', superscript: '<svg width="24" height="24"><path d="M15 9.4 10.4 14l4.6 4.6-1.4 1.4L9 15.4 4.4 20 3 18.6 7.6 14 3 9.4 4.4 8 9 12.6 13.6 8 15 9.4Zm5.9 1.6h-5v-1l1-.8 1.7-1.6c.3-.5.5-.9.5-1.3 0-.3 0-.5-.2-.7-.2-.2-.5-.3-.9-.3l-.8.2-.7.4-.4-1.2c.2-.2.5-.4 1-.5.3-.2.8-.2 1.2-.2.8 0 1.4.2 1.8.6.4.4.6 1 .6 1.6 0 .5-.2 1-.5 1.5l-1.3 1.4-.6.5h2.6V11Z" fill-rule="nonzero"/></svg>', "table-caption": '<svg width="24" height="24"><g fill-rule="nonzero"><rect width="12" height="2" x="3" y="4" rx="1"/><path d="M19 8a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-8c0-1.1.9-2 2-2h14ZM5 15v3h6v-3H5Zm14 0h-6v3h6v-3Zm0-5h-6v3h6v-3ZM5 13h6v-3H5v3Z"/></g></svg>', "table-cell-classes": '<svg width="24" height="24"><g fill-rule="evenodd"><path fill-rule="nonzero" d="M13 4v9H3V6c0-1.1.9-2 2-2h8Zm-2 2H5v5h6V6Z"/><path fill-rule="nonzero" d="M13 4h6a2 2 0 0 1 2 2v7h-8v-2h6V6h-6V4Z" opacity=".2"/><path d="m18 20-2.6 1.6.7-3-2.4-2 3.1-.2 1.2-2.9 1.2 2.9 3.1.2-2.4 2 .7 3z"/><path fill-rule="nonzero" d="M3 13v5c0 1.1.9 2 2 2h8v-7h-2v5H5v-5H3Z" opacity=".2"/></g></svg>', "table-cell-properties": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm-8 9H5v5h6v-5Zm8 0h-6v5h6v-5Zm-8-7H5v5h6V6Z"/></svg>', "table-cell-select-all": '<svg width="24" height="24"><g fill-rule="evenodd"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm0 2H5v12h14V6Z"/><path d="M13 6v5h6v2h-6v5h-2v-5H5v-2h6V6h2Z" opacity=".2"/></g></svg>', "table-cell-select-inner": '<svg width="24" height="24"><g fill-rule="evenodd"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm0 2H5v12h14V6Z" opacity=".2"/><path d="M13 6v5h6v2h-6v5h-2v-5H5v-2h6V6h2Z"/></g></svg>', "table-classes": '<svg width="24" height="24"><g fill-rule="evenodd"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v7h-8v7H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm-8 9H5v5h6v-5Zm8-7h-6v5h6V6Zm-8 0H5v5h6V6Z"/><path d="m18 20-2.6 1.6.7-3-2.4-2 3.1-.2 1.2-2.9 1.2 2.9 3.1.2-2.4 2 .7 3z"/></g></svg>', "table-delete-column": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm-4 4h-2V6h-2v2H9V6H5v12h4v-2h2v2h2v-2h2v2h4V6h-4v2Zm.3.5 1 1.2-3 2.3 3 2.3-1 1.2L12 13l-3.3 2.6-1-1.2 3-2.3-3-2.3 1-1.2L12 11l3.3-2.5Z"/></svg>', "table-delete-row": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm0 2H5v3h2.5v2H5v2h2.5v2H5v3h14v-3h-2.5v-2H19v-2h-2.5V9H19V6Zm-4.7 1.8 1.2 1L13 12l2.6 3.3-1.2 1-2.3-3-2.3 3-1.2-1L11 12 8.5 8.7l1.2-1 2.3 3 2.3-3Z"/></svg>', "table-delete-table": '<svg width="24" height="24"><g fill-rule="nonzero"><path d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14ZM5 6v12h14V6H5Z"/><path d="m14.4 8.6 1.1 1-2.4 2.4 2.4 2.4-1.1 1.1-2.4-2.4-2.4 2.4-1-1.1 2.3-2.4-2.3-2.4 1-1 2.4 2.3z"/></g></svg>', "table-insert-column-after": '<svg width="24" height="24"><path fill-rule="nonzero" d="M20 4c.6 0 1 .4 1 1v2a1 1 0 0 1-2 0V6h-8v12h8v-1a1 1 0 0 1 2 0v2c0 .5-.4 1-.9 1H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h15ZM9 13H5v5h4v-5Zm7-5c.5 0 1 .4 1 .9V11h2a1 1 0 0 1 .1 2H17v2a1 1 0 0 1-2 .1V13h-2a1 1 0 0 1-.1-2H15V9c0-.6.4-1 1-1ZM9 6H5v5h4V6Z"/></svg>', "table-insert-column-before": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a1 1 0 0 1-1-1v-2a1 1 0 0 1 2 0v1h8V6H5v1a1 1 0 1 1-2 0V5c0-.6.4-1 1-1h15Zm0 9h-4v5h4v-5ZM8 8c.5 0 1 .4 1 .9V11h2a1 1 0 0 1 .1 2H9v2a1 1 0 0 1-2 .1V13H5a1 1 0 0 1-.1-2H7V9c0-.6.4-1 1-1Zm11-2h-4v5h4V6Z"/></svg>', "table-insert-row-above": '<svg width="24" height="24"><path fill-rule="nonzero" d="M6 4a1 1 0 1 1 0 2H5v6h14V6h-1a1 1 0 0 1 0-2h2c.6 0 1 .4 1 1v13a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5c0-.6.4-1 1-1h2Zm5 10H5v4h6v-4Zm8 0h-6v4h6v-4ZM12 3c.5 0 1 .4 1 .9V6h2a1 1 0 0 1 0 2h-2v2a1 1 0 0 1-2 .1V8H9a1 1 0 0 1 0-2h2V4c0-.6.4-1 1-1Z"/></svg>', "table-insert-row-after": '<svg width="24" height="24"><path fill-rule="nonzero" d="M12 13c.5 0 1 .4 1 .9V16h2a1 1 0 0 1 .1 2H13v2a1 1 0 0 1-2 .1V18H9a1 1 0 0 1-.1-2H11v-2c0-.6.4-1 1-1Zm6 7a1 1 0 0 1 0-2h1v-6H5v6h1a1 1 0 0 1 0 2H4a1 1 0 0 1-1-1V6c0-1.1.9-2 2-2h14a2 2 0 0 1 2 2v13c0 .5-.4 1-.9 1H18ZM11 6H5v4h6V6Zm8 0h-6v4h6V6Z"/></svg>', "table-left-header": '<svg width="24" height="24"><path d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm0 9h-4v5h4v-5Zm-6 0H9v5h4v-5Zm0-7H9v5h4V6Zm6 0h-4v5h4V6Z"/></svg>', "table-merge-cells": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14ZM5 15.5V18h3v-2.5H5Zm14-5h-9V18h9v-7.5ZM19 6h-4v2.5h4V6ZM8 6H5v2.5h3V6Zm5 0h-3v2.5h3V6Zm-8 7.5h3v-3H5v3Z"/></svg>', "table-row-numbering-rtl": '<svg width="24" height="24"><path d="M6 4a2 2 0 0 0-2 2v13c0 1.1.9 2 2 2h12a2 2 0 0 0 2-2V6a2 2 0 0 0-2-2H6Zm0 12h8v3H6v-3Zm11 0c.6 0 1 .4 1 1v1a1 1 0 0 1-2 0v-1c0-.6.4-1 1-1ZM6 11h8v3H6v-3Zm11 0c.6 0 1 .4 1 1v1a1 1 0 0 1-2 0v-1c0-.6.4-1 1-1ZM6 6h8v3H6V6Zm11 0c.6 0 1 .4 1 1v1a1 1 0 1 1-2 0V7c0-.6.4-1 1-1Z"/></svg>', "table-row-numbering": '<svg width="24" height="24"><path d="M18 4a2 2 0 0 1 2 2v13a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h12Zm0 12h-8v3h8v-3ZM7 16a1 1 0 0 0-1 1v1a1 1 0 0 0 2 0v-1c0-.6-.4-1-1-1Zm11-5h-8v3h8v-3ZM7 11a1 1 0 0 0-1 1v1a1 1 0 0 0 2 0v-1c0-.6-.4-1-1-1Zm11-5h-8v3h8V6ZM7 6a1 1 0 0 0-1 1v1a1 1 0 1 0 2 0V7c0-.6-.4-1-1-1Z"/></svg>', "table-row-properties": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14ZM5 15v3h6v-3H5Zm14 0h-6v3h6v-3Zm0-9h-6v3h6V6ZM5 9h6V6H5v3Z"/></svg>', "table-split-cells": '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14ZM8 15.5H5V18h3v-2.5Zm11-5h-9V18h9v-7.5Zm-2.5 1 1 1-2 2 2 2-1 1-2-2-2 2-1-1 2-2-2-2 1-1 2 2 2-2Zm-8.5-1H5v3h3v-3ZM19 6h-4v2.5h4V6ZM8 6H5v2.5h3V6Zm5 0h-3v2.5h3V6Z"/></svg>', "table-top-header": '<svg width="24" height="24"><path d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14Zm-8 11H5v3h6v-3Zm8 0h-6v3h6v-3Zm0-5h-6v3h6v-3ZM5 13h6v-3H5v3Z"/></svg>', table: '<svg width="24" height="24"><path fill-rule="nonzero" d="M19 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2h14ZM5 14v4h6v-4H5Zm14 0h-6v4h6v-4Zm0-6h-6v4h6V8ZM5 12h6V8H5v4Z"/></svg>', template: '<svg width="24" height="24"><path d="M19 19v-1H5v1h14ZM9 16v-4a5 5 0 1 1 6 0v4h4a2 2 0 0 1 2 2v3H3v-3c0-1.1.9-2 2-2h4Zm4 0v-5l.8-.6a3 3 0 1 0-3.6 0l.8.6v5h2Z" fill-rule="nonzero"/></svg>', "temporary-placeholder": '<svg width="24" height="24"><g fill-rule="evenodd"><path d="M9 7.6V6h2.5V4.5a.5.5 0 1 1 1 0V6H15v1.6a8 8 0 1 1-6 0Zm-2.6 5.3a.5.5 0 0 0 .3.6c.3 0 .6 0 .6-.3l.1-.2a5 5 0 0 1 3.3-2.8c.3-.1.4-.4.4-.6-.1-.3-.4-.5-.6-.4a6 6 0 0 0-4.1 3.7Z"/><circle cx="14" cy="4" r="1"/><circle cx="12" cy="2" r="1"/><circle cx="10" cy="4" r="1"/></g></svg>', "text-color": '<svg width="24" height="24"><g fill-rule="evenodd"><path id="tox-icon-text-color__color" d="M3 18h18v3H3z"/><path d="M8.7 16h-.8a.5.5 0 0 1-.5-.6l2.7-9c.1-.3.3-.4.5-.4h2.8c.2 0 .4.1.5.4l2.7 9a.5.5 0 0 1-.5.6h-.8a.5.5 0 0 1-.4-.4l-.7-2.2c0-.3-.3-.4-.5-.4h-3.4c-.2 0-.4.1-.5.4l-.7 2.2c0 .3-.2.4-.4.4Zm2.6-7.6-.6 2a.5.5 0 0 0 .5.6h1.6a.5.5 0 0 0 .5-.6l-.6-2c0-.3-.3-.4-.5-.4h-.4c-.2 0-.4.1-.5.4Z"/></g></svg>', toc: '<svg width="24" height="24"><path d="M5 5c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 1 1 0-2Zm3 0h11c.6 0 1 .4 1 1s-.4 1-1 1H8a1 1 0 1 1 0-2Zm-3 8c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 0 1 0-2Zm3 0h11c.6 0 1 .4 1 1s-.4 1-1 1H8a1 1 0 0 1 0-2Zm0-4c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 1 1 0-2Zm3 0h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm-3 8c.6 0 1 .4 1 1s-.4 1-1 1a1 1 0 0 1 0-2Zm3 0h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Z" fill-rule="evenodd"/></svg>', translate: '<svg width="24" height="24"><path d="m12.7 14.3-.3.7-.4.7-2.2-2.2-3.1 3c-.3.4-.8.4-1 0a.7.7 0 0 1 0-1l3.1-3A12.4 12.4 0 0 1 6.7 9H8a10.1 10.1 0 0 0 1.7 2.4c.5-.5 1-1.1 1.4-1.8l.9-2H4.7a.7.7 0 1 1 0-1.5h4.4v-.7c0-.4.3-.8.7-.8.4 0 .7.4.7.8v.7H15c.4 0 .8.3.8.7 0 .4-.4.8-.8.8h-1.4a12.3 12.3 0 0 1-1 2.4 13.5 13.5 0 0 1-1.7 2.3l1.9 1.8Zm4.3-3 2.7 7.3a.5.5 0 0 1-.4.7 1 1 0 0 1-1-.7l-.6-1.5h-3.4l-.6 1.5a1 1 0 0 1-1 .7.5.5 0 0 1-.4-.7l2.7-7.4a1 1 0 0 1 2 0Zm-2.2 4.4h2.4L16 12.5l-1.2 3.2Z" fill-rule="evenodd"/></svg>', underline: '<svg width="24" height="24"><path d="M16 5c.6 0 1 .4 1 1v5.5a4 4 0 0 1-.4 1.8l-1 1.4a5.3 5.3 0 0 1-5.5 1 5 5 0 0 1-1.6-1c-.5-.4-.8-.9-1.1-1.4a4 4 0 0 1-.4-1.8V6c0-.6.4-1 1-1s1 .4 1 1v5.5c0 .3 0 .6.2 1l.6.7a3.3 3.3 0 0 0 2.2.8 3.4 3.4 0 0 0 2.2-.8c.3-.2.4-.5.6-.8l.2-.9V6c0-.6.4-1 1-1ZM8 17h8c.6 0 1 .4 1 1s-.4 1-1 1H8a1 1 0 0 1 0-2Z" fill-rule="evenodd"/></svg>', undo: '<svg width="24" height="24"><path d="M6.4 8H12c3.7 0 6.2 2 6.8 5.1.6 2.7-.4 5.6-2.3 6.8a1 1 0 0 1-1-1.8c1.1-.6 1.8-2.7 1.4-4.6-.5-2.1-2.1-3.5-4.9-3.5H6.4l3.3 3.3a1 1 0 1 1-1.4 1.4l-5-5a1 1 0 0 1 0-1.4l5-5a1 1 0 0 1 1.4 1.4L6.4 8Z" fill-rule="nonzero"/></svg>', unlink: '<svg width="24" height="24"><path d="M6.2 12.3a1 1 0 0 1 1.4 1.4l-2 2a2 2 0 1 0 2.6 2.8l4.8-4.8a1 1 0 0 0 0-1.4 1 1 0 1 1 1.4-1.3 2.9 2.9 0 0 1 0 4L9.6 20a3.9 3.9 0 0 1-5.5-5.5l2-2Zm11.6-.6a1 1 0 0 1-1.4-1.4l2.1-2a2 2 0 1 0-2.7-2.8L11 10.3a1 1 0 0 0 0 1.4A1 1 0 1 1 9.6 13a2.9 2.9 0 0 1 0-4L14.4 4a3.9 3.9 0 0 1 5.5 5.5l-2 2ZM7.6 6.3a.8.8 0 0 1-1 1.1L3.3 4.2a.7.7 0 1 1 1-1l3.2 3.1ZM5.1 8.6a.8.8 0 0 1 0 1.5H3a.8.8 0 0 1 0-1.5H5Zm5-3.5a.8.8 0 0 1-1.5 0V3a.8.8 0 0 1 1.5 0V5Zm6 11.8a.8.8 0 0 1 1-1l3.2 3.2a.8.8 0 0 1-1 1L16 17Zm-2.2 2a.8.8 0 0 1 1.5 0V21a.8.8 0 0 1-1.5 0V19Zm5-3.5a.7.7 0 1 1 0-1.5H21a.8.8 0 0 1 0 1.5H19Z" fill-rule="nonzero"/></svg>', unlock: '<svg width="24" height="24"><path d="M16 5c.8 0 1.5.3 2.1.9.6.6.9 1.3.9 2.1v3h-2V8a1 1 0 0 0-.3-.7A1 1 0 0 0 16 7h-2a1 1 0 0 0-.7.3 1 1 0 0 0-.3.7v3h.3c.2 0 .3 0 .5.2l.2.6v7.4c0 .3 0 .4-.2.6l-.6.2H4.8c-.3 0-.4 0-.6-.2a.7.7 0 0 1-.2-.6v-7.4c0-.3 0-.4.2-.6l.5-.2H11V8c0-.8.3-1.5.9-2.1.6-.6 1.3-.9 2.1-.9h2Z" fill-rule="evenodd"/></svg>', "unordered-list": '<svg width="24" height="24"><path d="M11 5h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0 6h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2Zm0 6h8c.6 0 1 .4 1 1s-.4 1-1 1h-8a1 1 0 0 1 0-2ZM4.5 6c0-.4.1-.8.4-1 .3-.4.7-.5 1.1-.5.4 0 .8.1 1 .4.4.3.5.7.5 1.1 0 .4-.1.8-.4 1-.3.4-.7.5-1.1.5-.4 0-.8-.1-1-.4-.4-.3-.5-.7-.5-1.1Zm0 6c0-.4.1-.8.4-1 .3-.4.7-.5 1.1-.5.4 0 .8.1 1 .4.4.3.5.7.5 1.1 0 .4-.1.8-.4 1-.3.4-.7.5-1.1.5-.4 0-.8-.1-1-.4-.4-.3-.5-.7-.5-1.1Zm0 6c0-.4.1-.8.4-1 .3-.4.7-.5 1.1-.5.4 0 .8.1 1 .4.4.3.5.7.5 1.1 0 .4-.1.8-.4 1-.3.4-.7.5-1.1.5-.4 0-.8-.1-1-.4-.4-.3-.5-.7-.5-1.1Z" fill-rule="evenodd"/></svg>', unselected: '<svg width="24" height="24"><path fill-rule="nonzero" d="M6 4h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6c0-1.1.9-2 2-2Zm0 1a1 1 0 0 0-1 1v12c0 .6.4 1 1 1h12c.6 0 1-.4 1-1V6c0-.6-.4-1-1-1H6Z"/></svg>', upload: '<svg width="24" height="24"><path d="M18 19v-2a1 1 0 0 1 2 0v3c0 .6-.4 1-1 1H5a1 1 0 0 1-1-1v-3a1 1 0 0 1 2 0v2h12ZM11 6.4 8.7 8.7a1 1 0 0 1-1.4-1.4l4-4a1 1 0 0 1 1.4 0l4 4a1 1 0 1 1-1.4 1.4L13 6.4V16a1 1 0 0 1-2 0V6.4Z" fill-rule="nonzero"/></svg>', user: '<svg width="24" height="24"><path d="M12 24a12 12 0 1 1 0-24 12 12 0 0 1 0 24Zm-8.7-5.3a11 11 0 0 0 17.4 0C19.4 16.3 14.6 15 12 15c-2.6 0-7.4 1.3-8.7 3.7ZM12 13c2.2 0 4-2 4-4.5S14.2 4 12 4 8 6 8 8.5 9.8 13 12 13Z" fill-rule="nonzero"/></svg>', "vertical-align": '<svg width="24" height="24"><g fill-rule="nonzero"><rect width="18" height="2" x="3" y="11" rx="1"/><path d="M12 2c.6 0 1 .4 1 1v4l2-1.3a1 1 0 0 1 1.2 1.5l-.1.1-4.1 3-4-3a1 1 0 0 1 1-1.7l2 1.5V3c0-.6.4-1 1-1zm0 11.8 4 2.9a1 1 0 0 1-1 1.7l-2-1.5V21c0 .5-.4 1-.9 1H12a1 1 0 0 1-1-1v-4l-2 1.3a1 1 0 0 1-1.2-.1l-.1-.1a1 1 0 0 1 .1-1.3l.1-.1 4.1-3z"/></g></svg>', visualblocks: '<svg width="24" height="24"><path d="M9 19v2H7v-2h2Zm-4 0v2a2 2 0 0 1-2-2h2Zm8 0v2h-2v-2h2Zm8 0a2 2 0 0 1-2 2v-2h2Zm-4 0v2h-2v-2h2ZM15 7a1 1 0 0 1 0 2v7a1 1 0 0 1-2 0V9h-1v7a1 1 0 0 1-2 0v-4a2.5 2.5 0 0 1-.2-5H15ZM5 15v2H3v-2h2Zm16 0v2h-2v-2h2ZM5 11v2H3v-2h2Zm16 0v2h-2v-2h2ZM5 7v2H3V7h2Zm16 0v2h-2V7h2ZM5 3v2H3c0-1.1.9-2 2-2Zm8 0v2h-2V3h2Zm6 0a2 2 0 0 1 2 2h-2V3ZM9 3v2H7V3h2Zm8 0v2h-2V3h2Z" fill-rule="evenodd"/></svg>', visualchars: '<svg width="24" height="24"><path d="M10 5h7a1 1 0 0 1 0 2h-1v11a1 1 0 0 1-2 0V7h-2v11a1 1 0 0 1-2 0v-6c-.5 0-1 0-1.4-.3A3.4 3.4 0 0 1 6.8 10a3.3 3.3 0 0 1 0-2.8 3.4 3.4 0 0 1 1.8-1.8L10 5Z" fill-rule="evenodd"/></svg>', warning: '<svg width="24" height="24"><path d="M19.8 18.3c.2.5.3.9 0 1.2-.1.3-.5.5-1 .5H5.2c-.5 0-.9-.2-1-.5-.3-.3-.2-.7 0-1.2L11 4.7l.5-.5.5-.2c.2 0 .3 0 .5.2.2 0 .3.3.5.5l6.8 13.6ZM12 18c.3 0 .5-.1.7-.3.2-.2.3-.4.3-.7a1 1 0 0 0-.3-.7 1 1 0 0 0-.7-.3 1 1 0 0 0-.7.3 1 1 0 0 0-.3.7c0 .3.1.5.3.7.2.2.4.3.7.3Zm.7-3 .3-4a1 1 0 0 0-.3-.7 1 1 0 0 0-.7-.3 1 1 0 0 0-.7.3 1 1 0 0 0-.3.7l.3 4h1.4Z" fill-rule="evenodd"/></svg>', "zoom-in": '<svg width="24" height="24"><path d="M16 17.3a8 8 0 1 1 1.4-1.4l4.3 4.4a1 1 0 0 1-1.4 1.4l-4.4-4.3Zm-5-.3a6 6 0 1 0 0-12 6 6 0 0 0 0 12Zm-1-9a1 1 0 0 1 2 0v6a1 1 0 0 1-2 0V8Zm-2 4a1 1 0 0 1 0-2h6a1 1 0 0 1 0 2H8Z" fill-rule="nonzero"/></svg>', "zoom-out": '<svg width="24" height="24"><path d="M16 17.3a8 8 0 1 1 1.4-1.4l4.3 4.4a1 1 0 0 1-1.4 1.4l-4.4-4.3Zm-5-.3a6 6 0 1 0 0-12 6 6 0 0 0 0 12Zm-3-5a1 1 0 0 1 0-2h6a1 1 0 0 1 0 2H8Z" fill-rule="nonzero"/></svg>' } });
(function() {
  "use strict";
  const getPrototypeOf$1 = Object.getPrototypeOf;
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t2 = typeof x;
    if (x === null) {
      return "null";
    } else if (t2 === "object" && Array.isArray(x)) {
      return "array";
    } else if (t2 === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t2;
    }
  };
  const isType$1 = (type2) => (value2) => typeOf(value2) === type2;
  const isSimpleType = (type2) => (value2) => typeof value2 === type2;
  const eq$1 = (t2) => (a) => t2 === a;
  const is$2 = (value2, constructor) => isObject(value2) && hasProto(value2, constructor, (o, proto) => getPrototypeOf$1(o) === proto);
  const isString = isType$1("string");
  const isObject = isType$1("object");
  const isPlainObject = (value2) => is$2(value2, Object);
  const isArray = isType$1("array");
  const isNull = eq$1(null);
  const isBoolean = isSimpleType("boolean");
  const isUndefined = eq$1(void 0);
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const isNumber = isSimpleType("number");
  const isArrayOf = (value2, pred) => {
    if (isArray(value2)) {
      for (let i2 = 0, len = value2.length; i2 < len; ++i2) {
        if (!pred(value2[i2])) {
          return false;
        }
      }
      return true;
    }
    return false;
  };
  const noop = () => {
  };
  const noarg = (f) => () => f();
  const compose = (fa, fb) => {
    return (...args) => {
      return fa(fb.apply(null, args));
    };
  };
  const compose1 = (fbc, fab) => (a) => fbc(fab(a));
  const constant$1 = (value2) => {
    return () => {
      return value2;
    };
  };
  const identity = (x) => {
    return x;
  };
  const tripleEquals = (a, b2) => {
    return a === b2;
  };
  function curry(fn, ...initialArgs) {
    return (...restArgs) => {
      const all2 = initialArgs.concat(restArgs);
      return fn.apply(null, all2);
    };
  }
  const not = (f) => (t2) => !f(t2);
  const die = (msg) => {
    return () => {
      throw new Error(msg);
    };
  };
  const apply = (f) => {
    return f();
  };
  const never = constant$1(false);
  const always = constant$1(true);
  var global$a = tinymce.util.Tools.resolve("tinymce.ThemeManager");
  class Optional {
    constructor(tag, value2) {
      this.tag = tag;
      this.value = value2;
    }
    static some(value2) {
      return new Optional(true, value2);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder2) {
      if (this.tag) {
        return binder2(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk2) {
      return this.tag ? this.value : thunk2();
    }
    orThunk(thunk2) {
      return this.tag ? this : thunk2();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value2) {
      return isNonNullable(value2) ? Optional.some(value2) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const nativeSlice = Array.prototype.slice;
  const nativeIndexOf = Array.prototype.indexOf;
  const nativePush = Array.prototype.push;
  const rawIndexOf = (ts, t2) => nativeIndexOf.call(ts, t2);
  const indexOf = (xs, x) => {
    const r2 = rawIndexOf(xs, x);
    return r2 === -1 ? Optional.none() : Optional.some(r2);
  };
  const contains$2 = (xs, x) => rawIndexOf(xs, x) > -1;
  const exists = (xs, pred) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return true;
      }
    }
    return false;
  };
  const range$2 = (num, f) => {
    const r2 = [];
    for (let i2 = 0; i2 < num; i2++) {
      r2.push(f(i2));
    }
    return r2;
  };
  const chunk$1 = (array, size) => {
    const r2 = [];
    for (let i2 = 0; i2 < array.length; i2 += size) {
      const s = nativeSlice.call(array, i2, i2 + size);
      r2.push(s);
    }
    return r2;
  };
  const map$2 = (xs, f) => {
    const len = xs.length;
    const r2 = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r2[i2] = f(x, i2);
    }
    return r2;
  };
  const each$1 = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const eachr = (xs, f) => {
    for (let i2 = xs.length - 1; i2 >= 0; i2--) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const partition$3 = (xs, pred) => {
    const pass = [];
    const fail = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      const arr = pred(x, i2) ? pass : fail;
      arr.push(x);
    }
    return {
      pass,
      fail
    };
  };
  const filter$2 = (xs, pred) => {
    const r2 = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        r2.push(x);
      }
    }
    return r2;
  };
  const foldr = (xs, f, acc) => {
    eachr(xs, (x, i2) => {
      acc = f(acc, x, i2);
    });
    return acc;
  };
  const foldl = (xs, f, acc) => {
    each$1(xs, (x, i2) => {
      acc = f(acc, x, i2);
    });
    return acc;
  };
  const findUntil = (xs, pred, until) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return Optional.some(x);
      } else if (until(x, i2)) {
        break;
      }
    }
    return Optional.none();
  };
  const find$5 = (xs, pred) => {
    return findUntil(xs, pred, never);
  };
  const findIndex$1 = (xs, pred) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return Optional.some(i2);
      }
    }
    return Optional.none();
  };
  const flatten = (xs) => {
    const r2 = [];
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      if (!isArray(xs[i2])) {
        throw new Error("Arr.flatten item " + i2 + " was not an array, input: " + xs);
      }
      nativePush.apply(r2, xs[i2]);
    }
    return r2;
  };
  const bind$3 = (xs, f) => flatten(map$2(xs, f));
  const forall = (xs, pred) => {
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      const x = xs[i2];
      if (pred(x, i2) !== true) {
        return false;
      }
    }
    return true;
  };
  const reverse = (xs) => {
    const r2 = nativeSlice.call(xs, 0);
    r2.reverse();
    return r2;
  };
  const difference = (a1, a2) => filter$2(a1, (x) => !contains$2(a2, x));
  const mapToObject = (xs, f) => {
    const r2 = {};
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      r2[String(x)] = f(x, i2);
    }
    return r2;
  };
  const pure$2 = (x) => [x];
  const sort = (xs, comparator) => {
    const copy = nativeSlice.call(xs, 0);
    copy.sort(comparator);
    return copy;
  };
  const get$h = (xs, i2) => i2 >= 0 && i2 < xs.length ? Optional.some(xs[i2]) : Optional.none();
  const head = (xs) => get$h(xs, 0);
  const last$1 = (xs) => get$h(xs, xs.length - 1);
  const from = isFunction(Array.from) ? Array.from : (x) => nativeSlice.call(x);
  const findMap = (arr, f) => {
    for (let i2 = 0; i2 < arr.length; i2++) {
      const r2 = f(arr[i2], i2);
      if (r2.isSome()) {
        return r2;
      }
    }
    return Optional.none();
  };
  const keys = Object.keys;
  const hasOwnProperty = Object.hasOwnProperty;
  const each = (obj, f) => {
    const props = keys(obj);
    for (let k = 0, len = props.length; k < len; k++) {
      const i2 = props[k];
      const x = obj[i2];
      f(x, i2);
    }
  };
  const map$1 = (obj, f) => {
    return tupleMap(obj, (x, i2) => ({
      k: i2,
      v: f(x, i2)
    }));
  };
  const tupleMap = (obj, f) => {
    const r2 = {};
    each(obj, (x, i2) => {
      const tuple = f(x, i2);
      r2[tuple.k] = tuple.v;
    });
    return r2;
  };
  const objAcc = (r2) => (x, i2) => {
    r2[i2] = x;
  };
  const internalFilter = (obj, pred, onTrue, onFalse) => {
    each(obj, (x, i2) => {
      (pred(x, i2) ? onTrue : onFalse)(x, i2);
    });
  };
  const bifilter = (obj, pred) => {
    const t2 = {};
    const f = {};
    internalFilter(obj, pred, objAcc(t2), objAcc(f));
    return {
      t: t2,
      f
    };
  };
  const filter$1 = (obj, pred) => {
    const t2 = {};
    internalFilter(obj, pred, objAcc(t2), noop);
    return t2;
  };
  const mapToArray = (obj, f) => {
    const r2 = [];
    each(obj, (value2, name2) => {
      r2.push(f(value2, name2));
    });
    return r2;
  };
  const find$4 = (obj, pred) => {
    const props = keys(obj);
    for (let k = 0, len = props.length; k < len; k++) {
      const i2 = props[k];
      const x = obj[i2];
      if (pred(x, i2, obj)) {
        return Optional.some(x);
      }
    }
    return Optional.none();
  };
  const values = (obj) => {
    return mapToArray(obj, identity);
  };
  const get$g = (obj, key) => {
    return has$2(obj, key) ? Optional.from(obj[key]) : Optional.none();
  };
  const has$2 = (obj, key) => hasOwnProperty.call(obj, key);
  const hasNonNullableKey = (obj, key) => has$2(obj, key) && obj[key] !== void 0 && obj[key] !== null;
  const is$1 = (lhs, rhs, comparator = tripleEquals) => lhs.exists((left2) => comparator(left2, rhs));
  const equals = (lhs, rhs, comparator = tripleEquals) => lift2(lhs, rhs, comparator).getOr(lhs.isNone() && rhs.isNone());
  const cat = (arr) => {
    const r2 = [];
    const push = (x) => {
      r2.push(x);
    };
    for (let i2 = 0; i2 < arr.length; i2++) {
      arr[i2].each(push);
    }
    return r2;
  };
  const sequence = (arr) => {
    const r2 = [];
    for (let i2 = 0; i2 < arr.length; i2++) {
      const x = arr[i2];
      if (x.isSome()) {
        r2.push(x.getOrDie());
      } else {
        return Optional.none();
      }
    }
    return Optional.some(r2);
  };
  const lift2 = (oa, ob, f) => oa.isSome() && ob.isSome() ? Optional.some(f(oa.getOrDie(), ob.getOrDie())) : Optional.none();
  const lift3 = (oa, ob, oc, f) => oa.isSome() && ob.isSome() && oc.isSome() ? Optional.some(f(oa.getOrDie(), ob.getOrDie(), oc.getOrDie())) : Optional.none();
  const mapFrom = (a, f) => a !== void 0 && a !== null ? Optional.some(f(a)) : Optional.none();
  const someIf = (b2, a) => b2 ? Optional.some(a) : Optional.none();
  const addToEnd = (str, suffix2) => {
    return str + suffix2;
  };
  const removeFromStart = (str, numChars) => {
    return str.substring(numChars);
  };
  const checkRange = (str, substr, start) => substr === "" || str.length >= substr.length && str.substr(start, start + substr.length) === substr;
  const removeLeading = (str, prefix2) => {
    return startsWith(str, prefix2) ? removeFromStart(str, prefix2.length) : str;
  };
  const ensureTrailing = (str, suffix2) => {
    return endsWith(str, suffix2) ? str : addToEnd(str, suffix2);
  };
  const contains$1 = (str, substr, start = 0, end) => {
    const idx = str.indexOf(substr, start);
    if (idx !== -1) {
      return isUndefined(end) ? true : idx + substr.length <= end;
    } else {
      return false;
    }
  };
  const startsWith = (str, prefix2) => {
    return checkRange(str, prefix2, 0);
  };
  const endsWith = (str, suffix2) => {
    return checkRange(str, suffix2, str.length - suffix2.length);
  };
  const blank = (r2) => (s) => s.replace(r2, "");
  const trim$1 = blank(/^\s+|\s+$/g);
  const isNotEmpty = (s) => s.length > 0;
  const isEmpty = (s) => !isNotEmpty(s);
  const isSupported$1 = (dom2) => dom2.style !== void 0 && isFunction(dom2.style.getPropertyValue);
  const fromHtml$2 = (html, scope) => {
    const doc = scope || document;
    const div = doc.createElement("div");
    div.innerHTML = html;
    if (!div.hasChildNodes() || div.childNodes.length > 1) {
      const message = "HTML does not have a single root node";
      console.error(message, html);
      throw new Error(message);
    }
    return fromDom(div.childNodes[0]);
  };
  const fromTag = (tag, scope) => {
    const doc = scope || document;
    const node = doc.createElement(tag);
    return fromDom(node);
  };
  const fromText = (text2, scope) => {
    const doc = scope || document;
    const node = doc.createTextNode(text2);
    return fromDom(node);
  };
  const fromDom = (node) => {
    if (node === null || node === void 0) {
      throw new Error("Node cannot be null or undefined");
    }
    return { dom: node };
  };
  const fromPoint = (docElm, x, y) => Optional.from(docElm.dom.elementFromPoint(x, y)).map(fromDom);
  const SugarElement = {
    fromHtml: fromHtml$2,
    fromTag,
    fromText,
    fromDom,
    fromPoint
  };
  const Global = typeof window !== "undefined" ? window : Function("return this;")();
  const path$1 = (parts2, scope) => {
    let o = scope !== void 0 && scope !== null ? scope : Global;
    for (let i2 = 0; i2 < parts2.length && o !== void 0 && o !== null; ++i2) {
      o = o[parts2[i2]];
    }
    return o;
  };
  const resolve = (p, scope) => {
    const parts2 = p.split(".");
    return path$1(parts2, scope);
  };
  const unsafe = (name2, scope) => {
    return resolve(name2, scope);
  };
  const getOrDie$1 = (name2, scope) => {
    const actual = unsafe(name2, scope);
    if (actual === void 0 || actual === null) {
      throw new Error(name2 + " not available on this browser");
    }
    return actual;
  };
  const getPrototypeOf = Object.getPrototypeOf;
  const sandHTMLElement = (scope) => {
    return getOrDie$1("HTMLElement", scope);
  };
  const isPrototypeOf = (x) => {
    const scope = resolve("ownerDocument.defaultView", x);
    return isObject(x) && (sandHTMLElement(scope).prototype.isPrototypeOf(x) || /^HTML\w*Element$/.test(getPrototypeOf(x).constructor.name));
  };
  const DOCUMENT = 9;
  const DOCUMENT_FRAGMENT = 11;
  const ELEMENT = 1;
  const TEXT = 3;
  const name$3 = (element2) => {
    const r2 = element2.dom.nodeName;
    return r2.toLowerCase();
  };
  const type$1 = (element2) => element2.dom.nodeType;
  const isType = (t2) => (element2) => type$1(element2) === t2;
  const isHTMLElement = (element2) => isElement$1(element2) && isPrototypeOf(element2.dom);
  const isElement$1 = isType(ELEMENT);
  const isText = isType(TEXT);
  const isDocument = isType(DOCUMENT);
  const isDocumentFragment = isType(DOCUMENT_FRAGMENT);
  const isTag = (tag) => (e) => isElement$1(e) && name$3(e) === tag;
  const is = (element2, selector) => {
    const dom2 = element2.dom;
    if (dom2.nodeType !== ELEMENT) {
      return false;
    } else {
      const elem = dom2;
      if (elem.matches !== void 0) {
        return elem.matches(selector);
      } else if (elem.msMatchesSelector !== void 0) {
        return elem.msMatchesSelector(selector);
      } else if (elem.webkitMatchesSelector !== void 0) {
        return elem.webkitMatchesSelector(selector);
      } else if (elem.mozMatchesSelector !== void 0) {
        return elem.mozMatchesSelector(selector);
      } else {
        throw new Error("Browser lacks native selectors");
      }
    }
  };
  const bypassSelector = (dom2) => dom2.nodeType !== ELEMENT && dom2.nodeType !== DOCUMENT && dom2.nodeType !== DOCUMENT_FRAGMENT || dom2.childElementCount === 0;
  const all$3 = (selector, scope) => {
    const base2 = scope === void 0 ? document : scope.dom;
    return bypassSelector(base2) ? [] : map$2(base2.querySelectorAll(selector), SugarElement.fromDom);
  };
  const one = (selector, scope) => {
    const base2 = scope === void 0 ? document : scope.dom;
    return bypassSelector(base2) ? Optional.none() : Optional.from(base2.querySelector(selector)).map(SugarElement.fromDom);
  };
  const eq = (e1, e2) => e1.dom === e2.dom;
  const contains = (e1, e2) => {
    const d1 = e1.dom;
    const d2 = e2.dom;
    return d1 === d2 ? false : d1.contains(d2);
  };
  const owner$4 = (element2) => SugarElement.fromDom(element2.dom.ownerDocument);
  const documentOrOwner = (dos) => isDocument(dos) ? dos : owner$4(dos);
  const documentElement = (element2) => SugarElement.fromDom(documentOrOwner(element2).dom.documentElement);
  const defaultView = (element2) => SugarElement.fromDom(documentOrOwner(element2).dom.defaultView);
  const parent = (element2) => Optional.from(element2.dom.parentNode).map(SugarElement.fromDom);
  const parentElement = (element2) => Optional.from(element2.dom.parentElement).map(SugarElement.fromDom);
  const offsetParent = (element2) => Optional.from(element2.dom.offsetParent).map(SugarElement.fromDom);
  const nextSibling = (element2) => Optional.from(element2.dom.nextSibling).map(SugarElement.fromDom);
  const children = (element2) => map$2(element2.dom.childNodes, SugarElement.fromDom);
  const child$2 = (element2, index) => {
    const cs = element2.dom.childNodes;
    return Optional.from(cs[index]).map(SugarElement.fromDom);
  };
  const firstChild = (element2) => child$2(element2, 0);
  const spot = (element2, offset2) => ({
    element: element2,
    offset: offset2
  });
  const leaf = (element2, offset2) => {
    const cs = children(element2);
    return cs.length > 0 && offset2 < cs.length ? spot(cs[offset2], 0) : spot(element2, offset2);
  };
  const isShadowRoot = (dos) => isDocumentFragment(dos) && isNonNullable(dos.dom.host);
  const supported = isFunction(Element.prototype.attachShadow) && isFunction(Node.prototype.getRootNode);
  const isSupported = constant$1(supported);
  const getRootNode = supported ? (e) => SugarElement.fromDom(e.dom.getRootNode()) : documentOrOwner;
  const getContentContainer = (dos) => isShadowRoot(dos) ? dos : SugarElement.fromDom(documentOrOwner(dos).dom.body);
  const isInShadowRoot = (e) => getShadowRoot(e).isSome();
  const getShadowRoot = (e) => {
    const r2 = getRootNode(e);
    return isShadowRoot(r2) ? Optional.some(r2) : Optional.none();
  };
  const getShadowHost = (e) => SugarElement.fromDom(e.dom.host);
  const getOriginalEventTarget = (event2) => {
    if (isSupported() && isNonNullable(event2.target)) {
      const el = SugarElement.fromDom(event2.target);
      if (isElement$1(el) && isOpenShadowHost(el)) {
        if (event2.composed && event2.composedPath) {
          const composedPath = event2.composedPath();
          if (composedPath) {
            return head(composedPath);
          }
        }
      }
    }
    return Optional.from(event2.target);
  };
  const isOpenShadowHost = (element2) => isNonNullable(element2.dom.shadowRoot);
  const inBody = (element2) => {
    const dom2 = isText(element2) ? element2.dom.parentNode : element2.dom;
    if (dom2 === void 0 || dom2 === null || dom2.ownerDocument === null) {
      return false;
    }
    const doc = dom2.ownerDocument;
    return getShadowRoot(SugarElement.fromDom(dom2)).fold(() => doc.body.contains(dom2), compose1(inBody, getShadowHost));
  };
  const body = () => getBody(SugarElement.fromDom(document));
  const getBody = (doc) => {
    const b2 = doc.dom.body;
    if (b2 === null || b2 === void 0) {
      throw new Error("Body is not available yet");
    }
    return SugarElement.fromDom(b2);
  };
  const rawSet = (dom2, key, value2) => {
    if (isString(value2) || isBoolean(value2) || isNumber(value2)) {
      dom2.setAttribute(key, value2 + "");
    } else {
      console.error("Invalid call to Attribute.set. Key ", key, ":: Value ", value2, ":: Element ", dom2);
      throw new Error("Attribute value was not simple");
    }
  };
  const set$9 = (element2, key, value2) => {
    rawSet(element2.dom, key, value2);
  };
  const setAll$1 = (element2, attrs) => {
    const dom2 = element2.dom;
    each(attrs, (v, k) => {
      rawSet(dom2, k, v);
    });
  };
  const get$f = (element2, key) => {
    const v = element2.dom.getAttribute(key);
    return v === null ? void 0 : v;
  };
  const getOpt = (element2, key) => Optional.from(get$f(element2, key));
  const has$1 = (element2, key) => {
    const dom2 = element2.dom;
    return dom2 && dom2.hasAttribute ? dom2.hasAttribute(key) : false;
  };
  const remove$7 = (element2, key) => {
    element2.dom.removeAttribute(key);
  };
  const clone$1 = (element2) => foldl(element2.dom.attributes, (acc, attr) => {
    acc[attr.name] = attr.value;
    return acc;
  }, {});
  const internalSet = (dom2, property, value2) => {
    if (!isString(value2)) {
      console.error("Invalid call to CSS.set. Property ", property, ":: Value ", value2, ":: Element ", dom2);
      throw new Error("CSS value must be a string: " + value2);
    }
    if (isSupported$1(dom2)) {
      dom2.style.setProperty(property, value2);
    }
  };
  const internalRemove = (dom2, property) => {
    if (isSupported$1(dom2)) {
      dom2.style.removeProperty(property);
    }
  };
  const set$8 = (element2, property, value2) => {
    const dom2 = element2.dom;
    internalSet(dom2, property, value2);
  };
  const setAll = (element2, css) => {
    const dom2 = element2.dom;
    each(css, (v, k) => {
      internalSet(dom2, k, v);
    });
  };
  const setOptions = (element2, css) => {
    const dom2 = element2.dom;
    each(css, (v, k) => {
      v.fold(() => {
        internalRemove(dom2, k);
      }, (value2) => {
        internalSet(dom2, k, value2);
      });
    });
  };
  const get$e = (element2, property) => {
    const dom2 = element2.dom;
    const styles = window.getComputedStyle(dom2);
    const r2 = styles.getPropertyValue(property);
    return r2 === "" && !inBody(element2) ? getUnsafeProperty(dom2, property) : r2;
  };
  const getUnsafeProperty = (dom2, property) => isSupported$1(dom2) ? dom2.style.getPropertyValue(property) : "";
  const getRaw = (element2, property) => {
    const dom2 = element2.dom;
    const raw = getUnsafeProperty(dom2, property);
    return Optional.from(raw).filter((r2) => r2.length > 0);
  };
  const getAllRaw = (element2) => {
    const css = {};
    const dom2 = element2.dom;
    if (isSupported$1(dom2)) {
      for (let i2 = 0; i2 < dom2.style.length; i2++) {
        const ruleName = dom2.style.item(i2);
        css[ruleName] = dom2.style[ruleName];
      }
    }
    return css;
  };
  const isValidValue = (tag, property, value2) => {
    const element2 = SugarElement.fromTag(tag);
    set$8(element2, property, value2);
    const style = getRaw(element2, property);
    return style.isSome();
  };
  const remove$6 = (element2, property) => {
    const dom2 = element2.dom;
    internalRemove(dom2, property);
    if (is$1(getOpt(element2, "style").map(trim$1), "")) {
      remove$7(element2, "style");
    }
  };
  const reflow = (e) => e.dom.offsetWidth;
  const Dimension = (name2, getOffset2) => {
    const set2 = (element2, h) => {
      if (!isNumber(h) && !h.match(/^[0-9]+$/)) {
        throw new Error(name2 + ".set accepts only positive integer values. Value was " + h);
      }
      const dom2 = element2.dom;
      if (isSupported$1(dom2)) {
        dom2.style[name2] = h + "px";
      }
    };
    const get2 = (element2) => {
      const r2 = getOffset2(element2);
      if (r2 <= 0 || r2 === null) {
        const css = get$e(element2, name2);
        return parseFloat(css) || 0;
      }
      return r2;
    };
    const getOuter2 = get2;
    const aggregate = (element2, properties2) => foldl(properties2, (acc, property) => {
      const val = get$e(element2, property);
      const value2 = val === void 0 ? 0 : parseInt(val, 10);
      return isNaN(value2) ? acc : acc + value2;
    }, 0);
    const max2 = (element2, value2, properties2) => {
      const cumulativeInclusions = aggregate(element2, properties2);
      const absoluteMax = value2 > cumulativeInclusions ? value2 - cumulativeInclusions : 0;
      return absoluteMax;
    };
    return {
      set: set2,
      get: get2,
      getOuter: getOuter2,
      aggregate,
      max: max2
    };
  };
  const api$2 = Dimension("height", (element2) => {
    const dom2 = element2.dom;
    return inBody(element2) ? dom2.getBoundingClientRect().height : dom2.offsetHeight;
  });
  const get$d = (element2) => api$2.get(element2);
  const getOuter$2 = (element2) => api$2.getOuter(element2);
  const setMax$1 = (element2, value2) => {
    const inclusions = [
      "margin-top",
      "border-top-width",
      "padding-top",
      "padding-bottom",
      "border-bottom-width",
      "margin-bottom"
    ];
    const absMax = api$2.max(element2, value2, inclusions);
    set$8(element2, "max-height", absMax + "px");
  };
  const r$1 = (left2, top2) => {
    const translate2 = (x, y) => r$1(left2 + x, top2 + y);
    return {
      left: left2,
      top: top2,
      translate: translate2
    };
  };
  const SugarPosition = r$1;
  const boxPosition = (dom2) => {
    const box2 = dom2.getBoundingClientRect();
    return SugarPosition(box2.left, box2.top);
  };
  const firstDefinedOrZero = (a, b2) => {
    if (a !== void 0) {
      return a;
    } else {
      return b2 !== void 0 ? b2 : 0;
    }
  };
  const absolute$3 = (element2) => {
    const doc = element2.dom.ownerDocument;
    const body2 = doc.body;
    const win2 = doc.defaultView;
    const html = doc.documentElement;
    if (body2 === element2.dom) {
      return SugarPosition(body2.offsetLeft, body2.offsetTop);
    }
    const scrollTop = firstDefinedOrZero(win2 === null || win2 === void 0 ? void 0 : win2.pageYOffset, html.scrollTop);
    const scrollLeft = firstDefinedOrZero(win2 === null || win2 === void 0 ? void 0 : win2.pageXOffset, html.scrollLeft);
    const clientTop = firstDefinedOrZero(html.clientTop, body2.clientTop);
    const clientLeft = firstDefinedOrZero(html.clientLeft, body2.clientLeft);
    return viewport$1(element2).translate(scrollLeft - clientLeft, scrollTop - clientTop);
  };
  const viewport$1 = (element2) => {
    const dom2 = element2.dom;
    const doc = dom2.ownerDocument;
    const body2 = doc.body;
    if (body2 === dom2) {
      return SugarPosition(body2.offsetLeft, body2.offsetTop);
    }
    if (!inBody(element2)) {
      return SugarPosition(0, 0);
    }
    return boxPosition(dom2);
  };
  const api$1 = Dimension("width", (element2) => element2.dom.offsetWidth);
  const set$7 = (element2, h) => api$1.set(element2, h);
  const get$c = (element2) => api$1.get(element2);
  const getOuter$1 = (element2) => api$1.getOuter(element2);
  const setMax = (element2, value2) => {
    const inclusions = [
      "margin-left",
      "border-left-width",
      "padding-left",
      "padding-right",
      "border-right-width",
      "margin-right"
    ];
    const absMax = api$1.max(element2, value2, inclusions);
    set$8(element2, "max-width", absMax + "px");
  };
  const cached = (f) => {
    let called = false;
    let r2;
    return (...args) => {
      if (!called) {
        called = true;
        r2 = f.apply(null, args);
      }
      return r2;
    };
  };
  const DeviceType = (os, browser, userAgent, mediaMatch2) => {
    const isiPad = os.isiOS() && /ipad/i.test(userAgent) === true;
    const isiPhone = os.isiOS() && !isiPad;
    const isMobile = os.isiOS() || os.isAndroid();
    const isTouch2 = isMobile || mediaMatch2("(pointer:coarse)");
    const isTablet = isiPad || !isiPhone && isMobile && mediaMatch2("(min-device-width:768px)");
    const isPhone = isiPhone || isMobile && !isTablet;
    const iOSwebview = browser.isSafari() && os.isiOS() && /safari/i.test(userAgent) === false;
    const isDesktop = !isPhone && !isTablet && !iOSwebview;
    return {
      isiPad: constant$1(isiPad),
      isiPhone: constant$1(isiPhone),
      isTablet: constant$1(isTablet),
      isPhone: constant$1(isPhone),
      isTouch: constant$1(isTouch2),
      isAndroid: os.isAndroid,
      isiOS: os.isiOS,
      isWebView: constant$1(iOSwebview),
      isDesktop: constant$1(isDesktop)
    };
  };
  const firstMatch = (regexes, s) => {
    for (let i2 = 0; i2 < regexes.length; i2++) {
      const x = regexes[i2];
      if (x.test(s)) {
        return x;
      }
    }
    return void 0;
  };
  const find$3 = (regexes, agent) => {
    const r2 = firstMatch(regexes, agent);
    if (!r2) {
      return {
        major: 0,
        minor: 0
      };
    }
    const group2 = (i2) => {
      return Number(agent.replace(r2, "$" + i2));
    };
    return nu$d(group2(1), group2(2));
  };
  const detect$4 = (versionRegexes, agent) => {
    const cleanedAgent = String(agent).toLowerCase();
    if (versionRegexes.length === 0) {
      return unknown$3();
    }
    return find$3(versionRegexes, cleanedAgent);
  };
  const unknown$3 = () => {
    return nu$d(0, 0);
  };
  const nu$d = (major, minor) => {
    return {
      major,
      minor
    };
  };
  const Version = {
    nu: nu$d,
    detect: detect$4,
    unknown: unknown$3
  };
  const detectBrowser$1 = (browsers2, userAgentData) => {
    return findMap(userAgentData.brands, (uaBrand) => {
      const lcBrand = uaBrand.brand.toLowerCase();
      return find$5(browsers2, (browser) => {
        var _a;
        return lcBrand === ((_a = browser.brand) === null || _a === void 0 ? void 0 : _a.toLowerCase());
      }).map((info) => ({
        current: info.name,
        version: Version.nu(parseInt(uaBrand.version, 10), 0)
      }));
    });
  };
  const detect$3 = (candidates, userAgent) => {
    const agent = String(userAgent).toLowerCase();
    return find$5(candidates, (candidate) => {
      return candidate.search(agent);
    });
  };
  const detectBrowser = (browsers2, userAgent) => {
    return detect$3(browsers2, userAgent).map((browser) => {
      const version = Version.detect(browser.versionRegexes, userAgent);
      return {
        current: browser.name,
        version
      };
    });
  };
  const detectOs = (oses2, userAgent) => {
    return detect$3(oses2, userAgent).map((os) => {
      const version = Version.detect(os.versionRegexes, userAgent);
      return {
        current: os.name,
        version
      };
    });
  };
  const normalVersionRegex = /.*?version\/\ ?([0-9]+)\.([0-9]+).*/;
  const checkContains = (target) => {
    return (uastring) => {
      return contains$1(uastring, target);
    };
  };
  const browsers = [
    {
      name: "Edge",
      versionRegexes: [/.*?edge\/ ?([0-9]+)\.([0-9]+)$/],
      search: (uastring) => {
        return contains$1(uastring, "edge/") && contains$1(uastring, "chrome") && contains$1(uastring, "safari") && contains$1(uastring, "applewebkit");
      }
    },
    {
      name: "Chromium",
      brand: "Chromium",
      versionRegexes: [
        /.*?chrome\/([0-9]+)\.([0-9]+).*/,
        normalVersionRegex
      ],
      search: (uastring) => {
        return contains$1(uastring, "chrome") && !contains$1(uastring, "chromeframe");
      }
    },
    {
      name: "IE",
      versionRegexes: [
        /.*?msie\ ?([0-9]+)\.([0-9]+).*/,
        /.*?rv:([0-9]+)\.([0-9]+).*/
      ],
      search: (uastring) => {
        return contains$1(uastring, "msie") || contains$1(uastring, "trident");
      }
    },
    {
      name: "Opera",
      versionRegexes: [
        normalVersionRegex,
        /.*?opera\/([0-9]+)\.([0-9]+).*/
      ],
      search: checkContains("opera")
    },
    {
      name: "Firefox",
      versionRegexes: [/.*?firefox\/\ ?([0-9]+)\.([0-9]+).*/],
      search: checkContains("firefox")
    },
    {
      name: "Safari",
      versionRegexes: [
        normalVersionRegex,
        /.*?cpu os ([0-9]+)_([0-9]+).*/
      ],
      search: (uastring) => {
        return (contains$1(uastring, "safari") || contains$1(uastring, "mobile/")) && contains$1(uastring, "applewebkit");
      }
    }
  ];
  const oses = [
    {
      name: "Windows",
      search: checkContains("win"),
      versionRegexes: [/.*?windows\ nt\ ?([0-9]+)\.([0-9]+).*/]
    },
    {
      name: "iOS",
      search: (uastring) => {
        return contains$1(uastring, "iphone") || contains$1(uastring, "ipad");
      },
      versionRegexes: [
        /.*?version\/\ ?([0-9]+)\.([0-9]+).*/,
        /.*cpu os ([0-9]+)_([0-9]+).*/,
        /.*cpu iphone os ([0-9]+)_([0-9]+).*/
      ]
    },
    {
      name: "Android",
      search: checkContains("android"),
      versionRegexes: [/.*?android\ ?([0-9]+)\.([0-9]+).*/]
    },
    {
      name: "macOS",
      search: checkContains("mac os x"),
      versionRegexes: [/.*?mac\ os\ x\ ?([0-9]+)_([0-9]+).*/]
    },
    {
      name: "Linux",
      search: checkContains("linux"),
      versionRegexes: []
    },
    {
      name: "Solaris",
      search: checkContains("sunos"),
      versionRegexes: []
    },
    {
      name: "FreeBSD",
      search: checkContains("freebsd"),
      versionRegexes: []
    },
    {
      name: "ChromeOS",
      search: checkContains("cros"),
      versionRegexes: [/.*?chrome\/([0-9]+)\.([0-9]+).*/]
    }
  ];
  const PlatformInfo = {
    browsers: constant$1(browsers),
    oses: constant$1(oses)
  };
  const edge = "Edge";
  const chromium = "Chromium";
  const ie = "IE";
  const opera = "Opera";
  const firefox = "Firefox";
  const safari = "Safari";
  const unknown$2 = () => {
    return nu$c({
      current: void 0,
      version: Version.unknown()
    });
  };
  const nu$c = (info) => {
    const current = info.current;
    const version = info.version;
    const isBrowser = (name2) => () => current === name2;
    return {
      current,
      version,
      isEdge: isBrowser(edge),
      isChromium: isBrowser(chromium),
      isIE: isBrowser(ie),
      isOpera: isBrowser(opera),
      isFirefox: isBrowser(firefox),
      isSafari: isBrowser(safari)
    };
  };
  const Browser = {
    unknown: unknown$2,
    nu: nu$c,
    edge: constant$1(edge),
    chromium: constant$1(chromium),
    ie: constant$1(ie),
    opera: constant$1(opera),
    firefox: constant$1(firefox),
    safari: constant$1(safari)
  };
  const windows = "Windows";
  const ios = "iOS";
  const android = "Android";
  const linux = "Linux";
  const macos = "macOS";
  const solaris = "Solaris";
  const freebsd = "FreeBSD";
  const chromeos = "ChromeOS";
  const unknown$1 = () => {
    return nu$b({
      current: void 0,
      version: Version.unknown()
    });
  };
  const nu$b = (info) => {
    const current = info.current;
    const version = info.version;
    const isOS = (name2) => () => current === name2;
    return {
      current,
      version,
      isWindows: isOS(windows),
      isiOS: isOS(ios),
      isAndroid: isOS(android),
      isMacOS: isOS(macos),
      isLinux: isOS(linux),
      isSolaris: isOS(solaris),
      isFreeBSD: isOS(freebsd),
      isChromeOS: isOS(chromeos)
    };
  };
  const OperatingSystem = {
    unknown: unknown$1,
    nu: nu$b,
    windows: constant$1(windows),
    ios: constant$1(ios),
    android: constant$1(android),
    linux: constant$1(linux),
    macos: constant$1(macos),
    solaris: constant$1(solaris),
    freebsd: constant$1(freebsd),
    chromeos: constant$1(chromeos)
  };
  const detect$2 = (userAgent, userAgentDataOpt, mediaMatch2) => {
    const browsers2 = PlatformInfo.browsers();
    const oses2 = PlatformInfo.oses();
    const browser = userAgentDataOpt.bind((userAgentData) => detectBrowser$1(browsers2, userAgentData)).orThunk(() => detectBrowser(browsers2, userAgent)).fold(Browser.unknown, Browser.nu);
    const os = detectOs(oses2, userAgent).fold(OperatingSystem.unknown, OperatingSystem.nu);
    const deviceType = DeviceType(os, browser, userAgent, mediaMatch2);
    return {
      browser,
      os,
      deviceType
    };
  };
  const PlatformDetection = { detect: detect$2 };
  const mediaMatch = (query2) => window.matchMedia(query2).matches;
  let platform = cached(() => PlatformDetection.detect(navigator.userAgent, Optional.from(navigator.userAgentData), mediaMatch));
  const detect$1 = () => platform();
  const mkEvent = (target, x, y, stop2, prevent, kill, raw) => ({
    target,
    x,
    y,
    stop: stop2,
    prevent,
    kill,
    raw
  });
  const fromRawEvent$1 = (rawEvent) => {
    const target = SugarElement.fromDom(getOriginalEventTarget(rawEvent).getOr(rawEvent.target));
    const stop2 = () => rawEvent.stopPropagation();
    const prevent = () => rawEvent.preventDefault();
    const kill = compose(prevent, stop2);
    return mkEvent(target, rawEvent.clientX, rawEvent.clientY, stop2, prevent, kill, rawEvent);
  };
  const handle = (filter2, handler) => (rawEvent) => {
    if (filter2(rawEvent)) {
      handler(fromRawEvent$1(rawEvent));
    }
  };
  const binder = (element2, event2, filter2, handler, useCapture) => {
    const wrapped = handle(filter2, handler);
    element2.dom.addEventListener(event2, wrapped, useCapture);
    return { unbind: curry(unbind, element2, event2, wrapped, useCapture) };
  };
  const bind$2 = (element2, event2, filter2, handler) => binder(element2, event2, filter2, handler, false);
  const capture$1 = (element2, event2, filter2, handler) => binder(element2, event2, filter2, handler, true);
  const unbind = (element2, event2, handler, useCapture) => {
    element2.dom.removeEventListener(event2, handler, useCapture);
  };
  const before$1 = (marker, element2) => {
    const parent$1 = parent(marker);
    parent$1.each((v) => {
      v.dom.insertBefore(element2.dom, marker.dom);
    });
  };
  const after$2 = (marker, element2) => {
    const sibling = nextSibling(marker);
    sibling.fold(() => {
      const parent$1 = parent(marker);
      parent$1.each((v) => {
        append$2(v, element2);
      });
    }, (v) => {
      before$1(v, element2);
    });
  };
  const prepend$1 = (parent2, element2) => {
    const firstChild$1 = firstChild(parent2);
    firstChild$1.fold(() => {
      append$2(parent2, element2);
    }, (v) => {
      parent2.dom.insertBefore(element2.dom, v.dom);
    });
  };
  const append$2 = (parent2, element2) => {
    parent2.dom.appendChild(element2.dom);
  };
  const appendAt = (parent2, element2, index) => {
    child$2(parent2, index).fold(() => {
      append$2(parent2, element2);
    }, (v) => {
      before$1(v, element2);
    });
  };
  const append$1 = (parent2, elements) => {
    each$1(elements, (x) => {
      append$2(parent2, x);
    });
  };
  const empty = (element2) => {
    element2.dom.textContent = "";
    each$1(children(element2), (rogue) => {
      remove$5(rogue);
    });
  };
  const remove$5 = (element2) => {
    const dom2 = element2.dom;
    if (dom2.parentNode !== null) {
      dom2.parentNode.removeChild(dom2);
    }
  };
  const get$b = (_DOC) => {
    const doc = _DOC !== void 0 ? _DOC.dom : document;
    const x = doc.body.scrollLeft || doc.documentElement.scrollLeft;
    const y = doc.body.scrollTop || doc.documentElement.scrollTop;
    return SugarPosition(x, y);
  };
  const to = (x, y, _DOC) => {
    const doc = _DOC !== void 0 ? _DOC.dom : document;
    const win2 = doc.defaultView;
    if (win2) {
      win2.scrollTo(x, y);
    }
  };
  const get$a = (_win) => {
    const win2 = _win === void 0 ? window : _win;
    if (detect$1().browser.isFirefox()) {
      return Optional.none();
    } else {
      return Optional.from(win2.visualViewport);
    }
  };
  const bounds$1 = (x, y, width2, height2) => ({
    x,
    y,
    width: width2,
    height: height2,
    right: x + width2,
    bottom: y + height2
  });
  const getBounds$3 = (_win) => {
    const win2 = _win === void 0 ? window : _win;
    const doc = win2.document;
    const scroll = get$b(SugarElement.fromDom(doc));
    return get$a(win2).fold(() => {
      const html = win2.document.documentElement;
      const width2 = html.clientWidth;
      const height2 = html.clientHeight;
      return bounds$1(scroll.left, scroll.top, width2, height2);
    }, (visualViewport) => bounds$1(Math.max(visualViewport.pageLeft, scroll.left), Math.max(visualViewport.pageTop, scroll.top), visualViewport.width, visualViewport.height));
  };
  const getDocument = () => SugarElement.fromDom(document);
  const walkUp = (navigation, doc) => {
    const frame = navigation.view(doc);
    return frame.fold(constant$1([]), (f) => {
      const parent2 = navigation.owner(f);
      const rest = walkUp(navigation, parent2);
      return [f].concat(rest);
    });
  };
  const pathTo = (element2, navigation) => {
    const d = navigation.owner(element2);
    const paths = walkUp(navigation, d);
    return Optional.some(paths);
  };
  const view = (doc) => {
    var _a;
    const element2 = doc.dom === document ? Optional.none() : Optional.from((_a = doc.dom.defaultView) === null || _a === void 0 ? void 0 : _a.frameElement);
    return element2.map(SugarElement.fromDom);
  };
  const owner$3 = (element2) => owner$4(element2);
  var Navigation = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    view,
    owner: owner$3
  });
  const find$2 = (element2) => {
    const doc = getDocument();
    const scroll = get$b(doc);
    const path2 = pathTo(element2, Navigation);
    return path2.fold(curry(absolute$3, element2), (frames) => {
      const offset2 = viewport$1(element2);
      const r2 = foldr(frames, (b2, a) => {
        const loc = viewport$1(a);
        return {
          left: b2.left + loc.left,
          top: b2.top + loc.top
        };
      }, {
        left: 0,
        top: 0
      });
      return SugarPosition(r2.left + offset2.left + scroll.left, r2.top + offset2.top + scroll.top);
    });
  };
  const pointed = (point2, width2, height2) => ({
    point: point2,
    width: width2,
    height: height2
  });
  const rect = (x, y, width2, height2) => ({
    x,
    y,
    width: width2,
    height: height2
  });
  const bounds = (x, y, width2, height2) => ({
    x,
    y,
    width: width2,
    height: height2,
    right: x + width2,
    bottom: y + height2
  });
  const box$1 = (element2) => {
    const xy = absolute$3(element2);
    const w = getOuter$1(element2);
    const h = getOuter$2(element2);
    return bounds(xy.left, xy.top, w, h);
  };
  const absolute$2 = (element2) => {
    const position2 = find$2(element2);
    const width2 = getOuter$1(element2);
    const height2 = getOuter$2(element2);
    return bounds(position2.left, position2.top, width2, height2);
  };
  const win = () => getBounds$3(window);
  const value$4 = (value2) => {
    const applyHelper = (fn) => fn(value2);
    const constHelper = constant$1(value2);
    const outputHelper = () => output2;
    const output2 = {
      tag: true,
      inner: value2,
      fold: (_onError, onValue) => onValue(value2),
      isValue: always,
      isError: never,
      map: (mapper) => Result.value(mapper(value2)),
      mapError: outputHelper,
      bind: applyHelper,
      exists: applyHelper,
      forall: applyHelper,
      getOr: constHelper,
      or: outputHelper,
      getOrThunk: constHelper,
      orThunk: outputHelper,
      getOrDie: constHelper,
      each: (fn) => {
        fn(value2);
      },
      toOptional: () => Optional.some(value2)
    };
    return output2;
  };
  const error$1 = (error2) => {
    const outputHelper = () => output2;
    const output2 = {
      tag: false,
      inner: error2,
      fold: (onError, _onValue) => onError(error2),
      isValue: never,
      isError: always,
      map: outputHelper,
      mapError: (mapper) => Result.error(mapper(error2)),
      bind: outputHelper,
      exists: never,
      forall: always,
      getOr: identity,
      or: identity,
      getOrThunk: apply,
      orThunk: apply,
      getOrDie: die(String(error2)),
      each: noop,
      toOptional: Optional.none
    };
    return output2;
  };
  const fromOption = (optional2, err) => optional2.fold(() => error$1(err), value$4);
  const Result = {
    value: value$4,
    error: error$1,
    fromOption
  };
  var SimpleResultType;
  (function(SimpleResultType2) {
    SimpleResultType2[SimpleResultType2["Error"] = 0] = "Error";
    SimpleResultType2[SimpleResultType2["Value"] = 1] = "Value";
  })(SimpleResultType || (SimpleResultType = {}));
  const fold$1 = (res, onError, onValue) => res.stype === SimpleResultType.Error ? onError(res.serror) : onValue(res.svalue);
  const partition$2 = (results) => {
    const values2 = [];
    const errors = [];
    each$1(results, (obj) => {
      fold$1(obj, (err) => errors.push(err), (val) => values2.push(val));
    });
    return {
      values: values2,
      errors
    };
  };
  const mapError = (res, f) => {
    if (res.stype === SimpleResultType.Error) {
      return {
        stype: SimpleResultType.Error,
        serror: f(res.serror)
      };
    } else {
      return res;
    }
  };
  const map = (res, f) => {
    if (res.stype === SimpleResultType.Value) {
      return {
        stype: SimpleResultType.Value,
        svalue: f(res.svalue)
      };
    } else {
      return res;
    }
  };
  const bind$1 = (res, f) => {
    if (res.stype === SimpleResultType.Value) {
      return f(res.svalue);
    } else {
      return res;
    }
  };
  const bindError = (res, f) => {
    if (res.stype === SimpleResultType.Error) {
      return f(res.serror);
    } else {
      return res;
    }
  };
  const svalue = (v) => ({
    stype: SimpleResultType.Value,
    svalue: v
  });
  const serror = (e) => ({
    stype: SimpleResultType.Error,
    serror: e
  });
  const toResult$1 = (res) => fold$1(res, Result.error, Result.value);
  const fromResult$1 = (res) => res.fold(serror, svalue);
  const SimpleResult = {
    fromResult: fromResult$1,
    toResult: toResult$1,
    svalue,
    partition: partition$2,
    serror,
    bind: bind$1,
    bindError,
    map,
    mapError,
    fold: fold$1
  };
  const field$2 = (key, newKey, presence, prop) => ({
    tag: "field",
    key,
    newKey,
    presence,
    prop
  });
  const customField$1 = (newKey, instantiator) => ({
    tag: "custom",
    newKey,
    instantiator
  });
  const fold = (value2, ifField, ifCustom) => {
    switch (value2.tag) {
      case "field":
        return ifField(value2.key, value2.newKey, value2.presence, value2.prop);
      case "custom":
        return ifCustom(value2.newKey, value2.instantiator);
    }
  };
  const shallow$1 = (old, nu2) => {
    return nu2;
  };
  const deep = (old, nu2) => {
    const bothObjects = isPlainObject(old) && isPlainObject(nu2);
    return bothObjects ? deepMerge(old, nu2) : nu2;
  };
  const baseMerge = (merger) => {
    return (...objects) => {
      if (objects.length === 0) {
        throw new Error(`Can't merge zero objects`);
      }
      const ret = {};
      for (let j = 0; j < objects.length; j++) {
        const curObject = objects[j];
        for (const key in curObject) {
          if (has$2(curObject, key)) {
            ret[key] = merger(ret[key], curObject[key]);
          }
        }
      }
      return ret;
    };
  };
  const deepMerge = baseMerge(deep);
  const merge$1 = baseMerge(shallow$1);
  const required$2 = () => ({
    tag: "required",
    process: {}
  });
  const defaultedThunk = (fallbackThunk) => ({
    tag: "defaultedThunk",
    process: fallbackThunk
  });
  const defaulted$1 = (fallback2) => defaultedThunk(constant$1(fallback2));
  const asOption = () => ({
    tag: "option",
    process: {}
  });
  const mergeWithThunk = (baseThunk) => ({
    tag: "mergeWithThunk",
    process: baseThunk
  });
  const mergeWith = (base2) => mergeWithThunk(constant$1(base2));
  const mergeValues$1 = (values2, base2) => values2.length > 0 ? SimpleResult.svalue(deepMerge(base2, merge$1.apply(void 0, values2))) : SimpleResult.svalue(base2);
  const mergeErrors$1 = (errors) => compose(SimpleResult.serror, flatten)(errors);
  const consolidateObj = (objects, base2) => {
    const partition2 = SimpleResult.partition(objects);
    return partition2.errors.length > 0 ? mergeErrors$1(partition2.errors) : mergeValues$1(partition2.values, base2);
  };
  const consolidateArr = (objects) => {
    const partitions = SimpleResult.partition(objects);
    return partitions.errors.length > 0 ? mergeErrors$1(partitions.errors) : SimpleResult.svalue(partitions.values);
  };
  const ResultCombine = {
    consolidateObj,
    consolidateArr
  };
  const formatObj = (input2) => {
    return isObject(input2) && keys(input2).length > 100 ? " removed due to size" : JSON.stringify(input2, null, 2);
  };
  const formatErrors = (errors) => {
    const es = errors.length > 10 ? errors.slice(0, 10).concat([{
      path: [],
      getErrorInfo: constant$1("... (only showing first ten failures)")
    }]) : errors;
    return map$2(es, (e) => {
      return "Failed path: (" + e.path.join(" > ") + ")\n" + e.getErrorInfo();
    });
  };
  const nu$a = (path2, getErrorInfo) => {
    return SimpleResult.serror([{
      path: path2,
      getErrorInfo
    }]);
  };
  const missingRequired = (path2, key, obj) => nu$a(path2, () => 'Could not find valid *required* value for "' + key + '" in ' + formatObj(obj));
  const missingKey = (path2, key) => nu$a(path2, () => 'Choice schema did not contain choice key: "' + key + '"');
  const missingBranch = (path2, branches, branch) => nu$a(path2, () => 'The chosen schema: "' + branch + '" did not exist in branches: ' + formatObj(branches));
  const unsupportedFields = (path2, unsupported) => nu$a(path2, () => "There are unsupported fields: [" + unsupported.join(", ") + "] specified");
  const custom = (path2, err) => nu$a(path2, constant$1(err));
  const value$3 = (validator) => {
    const extract2 = (path2, val) => {
      return SimpleResult.bindError(validator(val), (err) => custom(path2, err));
    };
    const toString2 = constant$1("val");
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const anyValue$1 = value$3(SimpleResult.svalue);
  const requiredAccess = (path2, obj, key, bundle) => get$g(obj, key).fold(() => missingRequired(path2, key, obj), bundle);
  const fallbackAccess = (obj, key, fallback2, bundle) => {
    const v = get$g(obj, key).getOrThunk(() => fallback2(obj));
    return bundle(v);
  };
  const optionAccess = (obj, key, bundle) => bundle(get$g(obj, key));
  const optionDefaultedAccess = (obj, key, fallback2, bundle) => {
    const opt = get$g(obj, key).map((val) => val === true ? fallback2(obj) : val);
    return bundle(opt);
  };
  const extractField = (field2, path2, obj, key, prop) => {
    const bundle = (av) => prop.extract(path2.concat([key]), av);
    const bundleAsOption = (optValue) => optValue.fold(() => SimpleResult.svalue(Optional.none()), (ov) => {
      const result = prop.extract(path2.concat([key]), ov);
      return SimpleResult.map(result, Optional.some);
    });
    switch (field2.tag) {
      case "required":
        return requiredAccess(path2, obj, key, bundle);
      case "defaultedThunk":
        return fallbackAccess(obj, key, field2.process, bundle);
      case "option":
        return optionAccess(obj, key, bundleAsOption);
      case "defaultedOptionThunk":
        return optionDefaultedAccess(obj, key, field2.process, bundleAsOption);
      case "mergeWithThunk": {
        return fallbackAccess(obj, key, constant$1({}), (v) => {
          const result = deepMerge(field2.process(obj), v);
          return bundle(result);
        });
      }
    }
  };
  const extractFields = (path2, obj, fields) => {
    const success = {};
    const errors = [];
    for (const field2 of fields) {
      fold(field2, (key, newKey, presence, prop) => {
        const result = extractField(presence, path2, obj, key, prop);
        SimpleResult.fold(result, (err) => {
          errors.push(...err);
        }, (res) => {
          success[newKey] = res;
        });
      }, (newKey, instantiator) => {
        success[newKey] = instantiator(obj);
      });
    }
    return errors.length > 0 ? SimpleResult.serror(errors) : SimpleResult.svalue(success);
  };
  const valueThunk = (getDelegate) => {
    const extract2 = (path2, val) => getDelegate().extract(path2, val);
    const toString2 = () => getDelegate().toString();
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const getSetKeys = (obj) => keys(filter$1(obj, isNonNullable));
  const objOfOnly = (fields) => {
    const delegate = objOf(fields);
    const fieldNames = foldr(fields, (acc, value2) => {
      return fold(value2, (key) => deepMerge(acc, { [key]: true }), constant$1(acc));
    }, {});
    const extract2 = (path2, o) => {
      const keys2 = isBoolean(o) ? [] : getSetKeys(o);
      const extra = filter$2(keys2, (k) => !hasNonNullableKey(fieldNames, k));
      return extra.length === 0 ? delegate.extract(path2, o) : unsupportedFields(path2, extra);
    };
    return {
      extract: extract2,
      toString: delegate.toString
    };
  };
  const objOf = (values2) => {
    const extract2 = (path2, o) => extractFields(path2, o, values2);
    const toString2 = () => {
      const fieldStrings = map$2(values2, (value2) => fold(value2, (key, _okey, _presence, prop) => key + " -> " + prop.toString(), (newKey, _instantiator) => "state(" + newKey + ")"));
      return "obj{\n" + fieldStrings.join("\n") + "}";
    };
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const arrOf = (prop) => {
    const extract2 = (path2, array) => {
      const results = map$2(array, (a, i2) => prop.extract(path2.concat(["[" + i2 + "]"]), a));
      return ResultCombine.consolidateArr(results);
    };
    const toString2 = () => "array(" + prop.toString() + ")";
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const oneOf = (props, rawF) => {
    const f = rawF !== void 0 ? rawF : identity;
    const extract2 = (path2, val) => {
      const errors = [];
      for (const prop of props) {
        const res = prop.extract(path2, val);
        if (res.stype === SimpleResultType.Value) {
          return {
            stype: SimpleResultType.Value,
            svalue: f(res.svalue)
          };
        }
        errors.push(res);
      }
      return ResultCombine.consolidateArr(errors);
    };
    const toString2 = () => "oneOf(" + map$2(props, (prop) => prop.toString()).join(", ") + ")";
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const setOf$1 = (validator, prop) => {
    const validateKeys = (path2, keys2) => arrOf(value$3(validator)).extract(path2, keys2);
    const extract2 = (path2, o) => {
      const keys$1 = keys(o);
      const validatedKeys = validateKeys(path2, keys$1);
      return SimpleResult.bind(validatedKeys, (validKeys) => {
        const schema2 = map$2(validKeys, (vk) => {
          return field$2(vk, vk, required$2(), prop);
        });
        return objOf(schema2).extract(path2, o);
      });
    };
    const toString2 = () => "setOf(" + prop.toString() + ")";
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const thunk = (_desc, processor) => {
    const getP = cached(processor);
    const extract2 = (path2, val) => getP().extract(path2, val);
    const toString2 = () => getP().toString();
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const arrOfObj = compose(arrOf, objOf);
  const anyValue = constant$1(anyValue$1);
  const typedValue = (validator, expectedType) => value$3((a) => {
    const actualType = typeof a;
    return validator(a) ? SimpleResult.svalue(a) : SimpleResult.serror(`Expected type: ${expectedType} but got: ${actualType}`);
  });
  const number = typedValue(isNumber, "number");
  const string = typedValue(isString, "string");
  const boolean = typedValue(isBoolean, "boolean");
  const functionProcessor = typedValue(isFunction, "function");
  const isPostMessageable = (val) => {
    if (Object(val) !== val) {
      return true;
    }
    switch ({}.toString.call(val).slice(8, -1)) {
      case "Boolean":
      case "Number":
      case "String":
      case "Date":
      case "RegExp":
      case "Blob":
      case "FileList":
      case "ImageData":
      case "ImageBitmap":
      case "ArrayBuffer":
        return true;
      case "Array":
      case "Object":
        return Object.keys(val).every((prop) => isPostMessageable(val[prop]));
      default:
        return false;
    }
  };
  const postMessageable = value$3((a) => {
    if (isPostMessageable(a)) {
      return SimpleResult.svalue(a);
    } else {
      return SimpleResult.serror("Expected value to be acceptable for sending via postMessage");
    }
  });
  const chooseFrom = (path2, input2, branches, ch) => {
    const fields = get$g(branches, ch);
    return fields.fold(() => missingBranch(path2, branches, ch), (vp) => vp.extract(path2.concat(["branch: " + ch]), input2));
  };
  const choose$2 = (key, branches) => {
    const extract2 = (path2, input2) => {
      const choice = get$g(input2, key);
      return choice.fold(() => missingKey(path2, key), (chosen) => chooseFrom(path2, input2, branches, chosen));
    };
    const toString2 = () => "chooseOn(" + key + "). Possible values: " + keys(branches);
    return {
      extract: extract2,
      toString: toString2
    };
  };
  const arrOfVal = () => arrOf(anyValue$1);
  const valueOf = (validator) => value$3((v) => validator(v).fold(SimpleResult.serror, SimpleResult.svalue));
  const setOf = (validator, prop) => setOf$1((v) => SimpleResult.fromResult(validator(v)), prop);
  const extractValue = (label2, prop, obj) => {
    const res = prop.extract([label2], obj);
    return SimpleResult.mapError(res, (errs) => ({
      input: obj,
      errors: errs
    }));
  };
  const asRaw = (label2, prop, obj) => SimpleResult.toResult(extractValue(label2, prop, obj));
  const getOrDie = (extraction) => {
    return extraction.fold((errInfo) => {
      throw new Error(formatError(errInfo));
    }, identity);
  };
  const asRawOrDie$1 = (label2, prop, obj) => getOrDie(asRaw(label2, prop, obj));
  const formatError = (errInfo) => {
    return "Errors: \n" + formatErrors(errInfo.errors).join("\n") + "\n\nInput object: " + formatObj(errInfo.input);
  };
  const choose$1 = (key, branches) => choose$2(key, map$1(branches, objOf));
  const thunkOf = (desc, schema2) => thunk(desc, schema2);
  const field$1 = field$2;
  const customField = customField$1;
  const validateEnum = (values2) => valueOf((value2) => contains$2(values2, value2) ? Result.value(value2) : Result.error(`Unsupported value: "${value2}", choose one of "${values2.join(", ")}".`));
  const required$1 = (key) => field$1(key, key, required$2(), anyValue());
  const requiredOf = (key, schema2) => field$1(key, key, required$2(), schema2);
  const requiredNumber = (key) => requiredOf(key, number);
  const requiredString = (key) => requiredOf(key, string);
  const requiredStringEnum = (key, values2) => field$1(key, key, required$2(), validateEnum(values2));
  const requiredBoolean = (key) => requiredOf(key, boolean);
  const requiredFunction = (key) => requiredOf(key, functionProcessor);
  const forbid = (key, message) => field$1(key, key, asOption(), value$3((_v) => SimpleResult.serror("The field: " + key + " is forbidden. " + message)));
  const requiredObjOf = (key, objSchema) => field$1(key, key, required$2(), objOf(objSchema));
  const requiredArrayOfObj = (key, objFields) => field$1(key, key, required$2(), arrOfObj(objFields));
  const requiredArrayOf = (key, schema2) => field$1(key, key, required$2(), arrOf(schema2));
  const option$3 = (key) => field$1(key, key, asOption(), anyValue());
  const optionOf = (key, schema2) => field$1(key, key, asOption(), schema2);
  const optionNumber = (key) => optionOf(key, number);
  const optionString = (key) => optionOf(key, string);
  const optionStringEnum = (key, values2) => optionOf(key, validateEnum(values2));
  const optionFunction = (key) => optionOf(key, functionProcessor);
  const optionArrayOf = (key, schema2) => optionOf(key, arrOf(schema2));
  const optionObjOf = (key, objSchema) => optionOf(key, objOf(objSchema));
  const optionObjOfOnly = (key, objSchema) => optionOf(key, objOfOnly(objSchema));
  const defaulted = (key, fallback2) => field$1(key, key, defaulted$1(fallback2), anyValue());
  const defaultedOf = (key, fallback2, schema2) => field$1(key, key, defaulted$1(fallback2), schema2);
  const defaultedNumber = (key, fallback2) => defaultedOf(key, fallback2, number);
  const defaultedString = (key, fallback2) => defaultedOf(key, fallback2, string);
  const defaultedStringEnum = (key, fallback2, values2) => defaultedOf(key, fallback2, validateEnum(values2));
  const defaultedBoolean = (key, fallback2) => defaultedOf(key, fallback2, boolean);
  const defaultedFunction = (key, fallback2) => defaultedOf(key, fallback2, functionProcessor);
  const defaultedPostMsg = (key, fallback2) => defaultedOf(key, fallback2, postMessageable);
  const defaultedArrayOf = (key, fallback2, schema2) => defaultedOf(key, fallback2, arrOf(schema2));
  const defaultedObjOf = (key, fallback2, objSchema) => defaultedOf(key, fallback2, objOf(objSchema));
  const Cell = (initial) => {
    let value2 = initial;
    const get2 = () => {
      return value2;
    };
    const set2 = (v) => {
      value2 = v;
    };
    return {
      get: get2,
      set: set2
    };
  };
  const generate$7 = (cases) => {
    if (!isArray(cases)) {
      throw new Error("cases must be an array");
    }
    if (cases.length === 0) {
      throw new Error("there must be at least one case");
    }
    const constructors = [];
    const adt2 = {};
    each$1(cases, (acase, count) => {
      const keys$1 = keys(acase);
      if (keys$1.length !== 1) {
        throw new Error("one and only one name per case");
      }
      const key = keys$1[0];
      const value2 = acase[key];
      if (adt2[key] !== void 0) {
        throw new Error("duplicate key detected:" + key);
      } else if (key === "cata") {
        throw new Error("cannot have a case named cata (sorry)");
      } else if (!isArray(value2)) {
        throw new Error("case arguments must be an array");
      }
      constructors.push(key);
      adt2[key] = (...args) => {
        const argLength = args.length;
        if (argLength !== value2.length) {
          throw new Error("Wrong number of arguments to case " + key + ". Expected " + value2.length + " (" + value2 + "), got " + argLength);
        }
        const match = (branches) => {
          const branchKeys = keys(branches);
          if (constructors.length !== branchKeys.length) {
            throw new Error("Wrong number of arguments to match. Expected: " + constructors.join(",") + "\nActual: " + branchKeys.join(","));
          }
          const allReqd = forall(constructors, (reqKey) => {
            return contains$2(branchKeys, reqKey);
          });
          if (!allReqd) {
            throw new Error("Not all branches were specified when using match. Specified: " + branchKeys.join(", ") + "\nRequired: " + constructors.join(", "));
          }
          return branches[key].apply(null, args);
        };
        return {
          fold: (...foldArgs) => {
            if (foldArgs.length !== cases.length) {
              throw new Error("Wrong number of arguments to fold. Expected " + cases.length + ", got " + foldArgs.length);
            }
            const target = foldArgs[count];
            return target.apply(null, args);
          },
          match,
          log: (label2) => {
            console.log(label2, {
              constructors,
              constructor: key,
              params: args
            });
          }
        };
      };
    });
    return adt2;
  };
  const Adt = { generate: generate$7 };
  Adt.generate([
    {
      bothErrors: [
        "error1",
        "error2"
      ]
    },
    {
      firstError: [
        "error1",
        "value2"
      ]
    },
    {
      secondError: [
        "value1",
        "error2"
      ]
    },
    {
      bothValues: [
        "value1",
        "value2"
      ]
    }
  ]);
  const partition$1 = (results) => {
    const errors = [];
    const values2 = [];
    each$1(results, (result) => {
      result.fold((err) => {
        errors.push(err);
      }, (value2) => {
        values2.push(value2);
      });
    });
    return {
      errors,
      values: values2
    };
  };
  const exclude$1 = (obj, fields) => {
    const r2 = {};
    each(obj, (v, k) => {
      if (!contains$2(fields, k)) {
        r2[k] = v;
      }
    });
    return r2;
  };
  const wrap$2 = (key, value2) => ({ [key]: value2 });
  const wrapAll$1 = (keyvalues) => {
    const r2 = {};
    each$1(keyvalues, (kv) => {
      r2[kv.key] = kv.value;
    });
    return r2;
  };
  const exclude = (obj, fields) => exclude$1(obj, fields);
  const wrap$1 = (key, value2) => wrap$2(key, value2);
  const wrapAll = (keyvalues) => wrapAll$1(keyvalues);
  const mergeValues = (values2, base2) => {
    return values2.length === 0 ? Result.value(base2) : Result.value(deepMerge(base2, merge$1.apply(void 0, values2)));
  };
  const mergeErrors = (errors) => Result.error(flatten(errors));
  const consolidate = (objs, base2) => {
    const partitions = partition$1(objs);
    return partitions.errors.length > 0 ? mergeErrors(partitions.errors) : mergeValues(partitions.values, base2);
  };
  const ensureIsRoot = (isRoot) => isFunction(isRoot) ? isRoot : never;
  const ancestor$2 = (scope, transform2, isRoot) => {
    let element2 = scope.dom;
    const stop2 = ensureIsRoot(isRoot);
    while (element2.parentNode) {
      element2 = element2.parentNode;
      const el = SugarElement.fromDom(element2);
      const transformed = transform2(el);
      if (transformed.isSome()) {
        return transformed;
      } else if (stop2(el)) {
        break;
      }
    }
    return Optional.none();
  };
  const closest$4 = (scope, transform2, isRoot) => {
    const current = transform2(scope);
    const stop2 = ensureIsRoot(isRoot);
    return current.orThunk(() => stop2(scope) ? Optional.none() : ancestor$2(scope, transform2, stop2));
  };
  const isSource = (component, simulatedEvent) => eq(component.element, simulatedEvent.event.target);
  const defaultEventHandler = {
    can: always,
    abort: never,
    run: noop
  };
  const nu$9 = (parts2) => {
    if (!hasNonNullableKey(parts2, "can") && !hasNonNullableKey(parts2, "abort") && !hasNonNullableKey(parts2, "run")) {
      throw new Error("EventHandler defined by: " + JSON.stringify(parts2, null, 2) + " does not have can, abort, or run!");
    }
    return {
      ...defaultEventHandler,
      ...parts2
    };
  };
  const all$2 = (handlers2, f) => (...args) => foldl(handlers2, (acc, handler) => acc && f(handler).apply(void 0, args), true);
  const any = (handlers2, f) => (...args) => foldl(handlers2, (acc, handler) => acc || f(handler).apply(void 0, args), false);
  const read$2 = (handler) => isFunction(handler) ? {
    can: always,
    abort: never,
    run: handler
  } : handler;
  const fuse$1 = (handlers2) => {
    const can2 = all$2(handlers2, (handler) => handler.can);
    const abort2 = any(handlers2, (handler) => handler.abort);
    const run2 = (...args) => {
      each$1(handlers2, (handler) => {
        handler.run.apply(void 0, args);
      });
    };
    return {
      can: can2,
      abort: abort2,
      run: run2
    };
  };
  const constant = constant$1;
  const touchstart = constant("touchstart");
  const touchmove = constant("touchmove");
  const touchend = constant("touchend");
  const touchcancel = constant("touchcancel");
  const mousedown = constant("mousedown");
  const mousemove = constant("mousemove");
  const mouseout = constant("mouseout");
  const mouseup = constant("mouseup");
  const mouseover = constant("mouseover");
  const focusin = constant("focusin");
  const focusout = constant("focusout");
  const keydown = constant("keydown");
  const keyup = constant("keyup");
  const input = constant("input");
  const change = constant("change");
  const click = constant("click");
  const transitioncancel = constant("transitioncancel");
  const transitionend = constant("transitionend");
  const transitionstart = constant("transitionstart");
  const selectstart = constant("selectstart");
  const prefixName = (name2) => constant$1("alloy." + name2);
  const alloy = { tap: prefixName("tap") };
  const focus$4 = prefixName("focus");
  const postBlur = prefixName("blur.post");
  const postPaste = prefixName("paste.post");
  const receive = prefixName("receive");
  const execute$5 = prefixName("execute");
  const focusItem = prefixName("focus.item");
  const tap = alloy.tap;
  const longpress = prefixName("longpress");
  const sandboxClose = prefixName("sandbox.close");
  const typeaheadCancel = prefixName("typeahead.cancel");
  const systemInit = prefixName("system.init");
  const documentTouchmove = prefixName("system.touchmove");
  const documentTouchend = prefixName("system.touchend");
  const windowScroll = prefixName("system.scroll");
  const windowResize = prefixName("system.resize");
  const attachedToDom = prefixName("system.attached");
  const detachedFromDom = prefixName("system.detached");
  const dismissRequested = prefixName("system.dismissRequested");
  const repositionRequested = prefixName("system.repositionRequested");
  const focusShifted = prefixName("focusmanager.shifted");
  const slotVisibility = prefixName("slotcontainer.visibility");
  const changeTab = prefixName("change.tab");
  const dismissTab = prefixName("dismiss.tab");
  const highlight$1 = prefixName("highlight");
  const dehighlight$1 = prefixName("dehighlight");
  const emit = (component, event2) => {
    dispatchWith(component, component.element, event2, {});
  };
  const emitWith = (component, event2, properties2) => {
    dispatchWith(component, component.element, event2, properties2);
  };
  const emitExecute = (component) => {
    emit(component, execute$5());
  };
  const dispatch = (component, target, event2) => {
    dispatchWith(component, target, event2, {});
  };
  const dispatchWith = (component, target, event2, properties2) => {
    const data = {
      target,
      ...properties2
    };
    component.getSystem().triggerEvent(event2, target, data);
  };
  const retargetAndDispatchWith = (component, target, eventName, properties2) => {
    const data = {
      ...properties2,
      target
    };
    component.getSystem().triggerEvent(eventName, target, data);
  };
  const dispatchEvent = (component, target, event2, simulatedEvent) => {
    component.getSystem().triggerEvent(event2, target, simulatedEvent.event);
  };
  const derive$2 = (configs) => wrapAll(configs);
  const abort = (name2, predicate) => {
    return {
      key: name2,
      value: nu$9({ abort: predicate })
    };
  };
  const can = (name2, predicate) => {
    return {
      key: name2,
      value: nu$9({ can: predicate })
    };
  };
  const preventDefault = (name2) => {
    return {
      key: name2,
      value: nu$9({
        run: (component, simulatedEvent) => {
          simulatedEvent.event.prevent();
        }
      })
    };
  };
  const run$1 = (name2, handler) => {
    return {
      key: name2,
      value: nu$9({ run: handler })
    };
  };
  const runActionExtra = (name2, action, extra) => {
    return {
      key: name2,
      value: nu$9({
        run: (component, simulatedEvent) => {
          action.apply(void 0, [
            component,
            simulatedEvent
          ].concat(extra));
        }
      })
    };
  };
  const runOnName = (name2) => {
    return (handler) => run$1(name2, handler);
  };
  const runOnSourceName = (name2) => {
    return (handler) => ({
      key: name2,
      value: nu$9({
        run: (component, simulatedEvent) => {
          if (isSource(component, simulatedEvent)) {
            handler(component, simulatedEvent);
          }
        }
      })
    });
  };
  const redirectToUid = (name2, uid) => {
    return run$1(name2, (component, simulatedEvent) => {
      component.getSystem().getByUid(uid).each((redirectee) => {
        dispatchEvent(redirectee, redirectee.element, name2, simulatedEvent);
      });
    });
  };
  const redirectToPart = (name2, detail, partName) => {
    const uid = detail.partUids[partName];
    return redirectToUid(name2, uid);
  };
  const runWithTarget = (name2, f) => {
    return run$1(name2, (component, simulatedEvent) => {
      const ev = simulatedEvent.event;
      const target = component.getSystem().getByDom(ev.target).getOrThunk(() => {
        const closest2 = closest$4(ev.target, (el) => component.getSystem().getByDom(el).toOptional(), never);
        return closest2.getOr(component);
      });
      f(component, target, simulatedEvent);
    });
  };
  const cutter = (name2) => {
    return run$1(name2, (component, simulatedEvent) => {
      simulatedEvent.cut();
    });
  };
  const stopper = (name2) => {
    return run$1(name2, (component, simulatedEvent) => {
      simulatedEvent.stop();
    });
  };
  const runOnSource = (name2, f) => {
    return runOnSourceName(name2)(f);
  };
  const runOnAttached = runOnSourceName(attachedToDom());
  const runOnDetached = runOnSourceName(detachedFromDom());
  const runOnInit = runOnSourceName(systemInit());
  const runOnExecute$1 = runOnName(execute$5());
  const fromHtml$1 = (html, scope) => {
    const doc = scope || document;
    const div = doc.createElement("div");
    div.innerHTML = html;
    return children(SugarElement.fromDom(div));
  };
  const get$9 = (element2) => element2.dom.innerHTML;
  const set$6 = (element2, content) => {
    const owner2 = owner$4(element2);
    const docDom = owner2.dom;
    const fragment = SugarElement.fromDom(docDom.createDocumentFragment());
    const contentElements = fromHtml$1(content, docDom);
    append$1(fragment, contentElements);
    empty(element2);
    append$2(element2, fragment);
  };
  const getOuter = (element2) => {
    const container = SugarElement.fromTag("div");
    const clone2 = SugarElement.fromDom(element2.dom.cloneNode(true));
    append$2(container, clone2);
    return get$9(container);
  };
  const clone = (original2, isDeep) => SugarElement.fromDom(original2.dom.cloneNode(isDeep));
  const shallow = (original2) => clone(original2, false);
  const getHtml = (element2) => {
    if (isShadowRoot(element2)) {
      return "#shadow-root";
    } else {
      const clone2 = shallow(element2);
      return getOuter(clone2);
    }
  };
  const element = (elem) => getHtml(elem);
  const isRecursive = (component, originator, target) => eq(originator, component.element) && !eq(originator, target);
  const events$i = derive$2([can(focus$4(), (component, simulatedEvent) => {
    const event2 = simulatedEvent.event;
    const originator = event2.originator;
    const target = event2.target;
    if (isRecursive(component, originator, target)) {
      console.warn(focus$4() + " did not get interpreted by the desired target. \nOriginator: " + element(originator) + "\nTarget: " + element(target) + "\nCheck the " + focus$4() + " event handlers");
      return false;
    } else {
      return true;
    }
  })]);
  var DefaultEvents = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$i
  });
  let unique = 0;
  const generate$6 = (prefix2) => {
    const date = /* @__PURE__ */ new Date();
    const time = date.getTime();
    const random = Math.floor(Math.random() * 1e9);
    unique++;
    return prefix2 + "_" + random + unique + String(time);
  };
  const prefix$1 = constant$1("alloy-id-");
  const idAttr$1 = constant$1("data-alloy-id");
  const prefix = prefix$1();
  const idAttr = idAttr$1();
  const write = (label2, elem) => {
    const id = generate$6(prefix + label2);
    writeOnly(elem, id);
    return id;
  };
  const writeOnly = (elem, uid) => {
    Object.defineProperty(elem.dom, idAttr, {
      value: uid,
      writable: true
    });
  };
  const read$1 = (elem) => {
    const id = isElement$1(elem) ? elem.dom[idAttr] : null;
    return Optional.from(id);
  };
  const generate$5 = (prefix2) => generate$6(prefix2);
  const make$8 = identity;
  const NoContextApi = (getComp) => {
    const getMessage = (event2) => `The component must be in a context to execute: ${event2}` + (getComp ? "\n" + element(getComp().element) + " is not in context." : "");
    const fail = (event2) => () => {
      throw new Error(getMessage(event2));
    };
    const warn = (event2) => () => {
      console.warn(getMessage(event2));
    };
    return {
      debugInfo: constant$1("fake"),
      triggerEvent: warn("triggerEvent"),
      triggerFocus: warn("triggerFocus"),
      triggerEscape: warn("triggerEscape"),
      broadcast: warn("broadcast"),
      broadcastOn: warn("broadcastOn"),
      broadcastEvent: warn("broadcastEvent"),
      build: fail("build"),
      buildOrPatch: fail("buildOrPatch"),
      addToWorld: fail("addToWorld"),
      removeFromWorld: fail("removeFromWorld"),
      addToGui: fail("addToGui"),
      removeFromGui: fail("removeFromGui"),
      getByUid: fail("getByUid"),
      getByDom: fail("getByDom"),
      isConnected: never
    };
  };
  const singleton$1 = NoContextApi();
  const markAsBehaviourApi = (f, apiName, apiFunction) => {
    const delegate = apiFunction.toString();
    const endIndex = delegate.indexOf(")") + 1;
    const openBracketIndex = delegate.indexOf("(");
    const parameters = delegate.substring(openBracketIndex + 1, endIndex - 1).split(/,\s*/);
    f.toFunctionAnnotation = () => ({
      name: apiName,
      parameters: cleanParameters(parameters.slice(0, 1).concat(parameters.slice(3)))
    });
    return f;
  };
  const cleanParameters = (parameters) => map$2(parameters, (p) => endsWith(p, "/*") ? p.substring(0, p.length - "/*".length) : p);
  const markAsExtraApi = (f, extraName) => {
    const delegate = f.toString();
    const endIndex = delegate.indexOf(")") + 1;
    const openBracketIndex = delegate.indexOf("(");
    const parameters = delegate.substring(openBracketIndex + 1, endIndex - 1).split(/,\s*/);
    f.toFunctionAnnotation = () => ({
      name: extraName,
      parameters: cleanParameters(parameters)
    });
    return f;
  };
  const markAsSketchApi = (f, apiFunction) => {
    const delegate = apiFunction.toString();
    const endIndex = delegate.indexOf(")") + 1;
    const openBracketIndex = delegate.indexOf("(");
    const parameters = delegate.substring(openBracketIndex + 1, endIndex - 1).split(/,\s*/);
    f.toFunctionAnnotation = () => ({
      name: "OVERRIDE",
      parameters: cleanParameters(parameters.slice(1))
    });
    return f;
  };
  const premadeTag = generate$6("alloy-premade");
  const premade$1 = (comp) => {
    Object.defineProperty(comp.element.dom, premadeTag, {
      value: comp.uid,
      writable: true
    });
    return wrap$1(premadeTag, comp);
  };
  const isPremade = (element2) => has$2(element2.dom, premadeTag);
  const getPremade = (spec) => get$g(spec, premadeTag);
  const makeApi = (f) => markAsSketchApi((component, ...rest) => f(component.getApis(), component, ...rest), f);
  const NoState = { init: () => nu$8({ readState: constant$1("No State required") }) };
  const nu$8 = (spec) => spec;
  const generateFrom$1 = (spec, all2) => {
    const schema2 = map$2(all2, (a) => optionObjOf(a.name(), [
      required$1("config"),
      defaulted("state", NoState)
    ]));
    const validated = asRaw("component.behaviours", objOf(schema2), spec.behaviours).fold((errInfo) => {
      throw new Error(formatError(errInfo) + "\nComplete spec:\n" + JSON.stringify(spec, null, 2));
    }, identity);
    return {
      list: all2,
      data: map$1(validated, (optBlobThunk) => {
        const output2 = optBlobThunk.map((blob) => ({
          config: blob.config,
          state: blob.state.init(blob.config)
        }));
        return constant$1(output2);
      })
    };
  };
  const getBehaviours$3 = (bData) => bData.list;
  const getData$2 = (bData) => bData.data;
  const byInnerKey = (data, tuple) => {
    const r2 = {};
    each(data, (detail, key) => {
      each(detail, (value2, indexKey) => {
        const chain = get$g(r2, indexKey).getOr([]);
        r2[indexKey] = chain.concat([tuple(key, value2)]);
      });
    });
    return r2;
  };
  const nu$7 = (s) => ({
    classes: isUndefined(s.classes) ? [] : s.classes,
    attributes: isUndefined(s.attributes) ? {} : s.attributes,
    styles: isUndefined(s.styles) ? {} : s.styles
  });
  const merge = (defnA, mod) => ({
    ...defnA,
    attributes: {
      ...defnA.attributes,
      ...mod.attributes
    },
    styles: {
      ...defnA.styles,
      ...mod.styles
    },
    classes: defnA.classes.concat(mod.classes)
  });
  const combine$2 = (info, baseMod, behaviours2, base2) => {
    const modsByBehaviour = { ...baseMod };
    each$1(behaviours2, (behaviour) => {
      modsByBehaviour[behaviour.name()] = behaviour.exhibit(info, base2);
    });
    const byAspect = byInnerKey(modsByBehaviour, (name2, modification) => ({
      name: name2,
      modification
    }));
    const combineObjects = (objects) => foldr(objects, (b2, a) => ({
      ...a.modification,
      ...b2
    }), {});
    const combinedClasses = foldr(byAspect.classes, (b2, a) => a.modification.concat(b2), []);
    const combinedAttributes = combineObjects(byAspect.attributes);
    const combinedStyles = combineObjects(byAspect.styles);
    return nu$7({
      classes: combinedClasses,
      attributes: combinedAttributes,
      styles: combinedStyles
    });
  };
  const sortKeys = (label2, keyName, array, order) => {
    try {
      const sorted = sort(array, (a, b2) => {
        const aKey = a[keyName];
        const bKey = b2[keyName];
        const aIndex = order.indexOf(aKey);
        const bIndex = order.indexOf(bKey);
        if (aIndex === -1) {
          throw new Error("The ordering for " + label2 + " does not have an entry for " + aKey + ".\nOrder specified: " + JSON.stringify(order, null, 2));
        }
        if (bIndex === -1) {
          throw new Error("The ordering for " + label2 + " does not have an entry for " + bKey + ".\nOrder specified: " + JSON.stringify(order, null, 2));
        }
        if (aIndex < bIndex) {
          return -1;
        } else if (bIndex < aIndex) {
          return 1;
        } else {
          return 0;
        }
      });
      return Result.value(sorted);
    } catch (err) {
      return Result.error([err]);
    }
  };
  const uncurried = (handler, purpose) => ({
    handler,
    purpose
  });
  const curried = (handler, purpose) => ({
    cHandler: handler,
    purpose
  });
  const curryArgs = (descHandler, extraArgs) => curried(curry.apply(void 0, [descHandler.handler].concat(extraArgs)), descHandler.purpose);
  const getCurried = (descHandler) => descHandler.cHandler;
  const behaviourTuple = (name2, handler) => ({
    name: name2,
    handler
  });
  const nameToHandlers = (behaviours2, info) => {
    const r2 = {};
    each$1(behaviours2, (behaviour) => {
      r2[behaviour.name()] = behaviour.handlers(info);
    });
    return r2;
  };
  const groupByEvents = (info, behaviours2, base2) => {
    const behaviourEvents = {
      ...base2,
      ...nameToHandlers(behaviours2, info)
    };
    return byInnerKey(behaviourEvents, behaviourTuple);
  };
  const combine$1 = (info, eventOrder, behaviours2, base2) => {
    const byEventName = groupByEvents(info, behaviours2, base2);
    return combineGroups(byEventName, eventOrder);
  };
  const assemble = (rawHandler) => {
    const handler = read$2(rawHandler);
    return (component, simulatedEvent, ...rest) => {
      const args = [
        component,
        simulatedEvent
      ].concat(rest);
      if (handler.abort.apply(void 0, args)) {
        simulatedEvent.stop();
      } else if (handler.can.apply(void 0, args)) {
        handler.run.apply(void 0, args);
      }
    };
  };
  const missingOrderError = (eventName, tuples) => Result.error(["The event (" + eventName + ') has more than one behaviour that listens to it.\nWhen this occurs, you must specify an event ordering for the behaviours in your spec (e.g. [ "listing", "toggling" ]).\nThe behaviours that can trigger it are: ' + JSON.stringify(map$2(tuples, (c) => c.name), null, 2)]);
  const fuse = (tuples, eventOrder, eventName) => {
    const order = eventOrder[eventName];
    if (!order) {
      return missingOrderError(eventName, tuples);
    } else {
      return sortKeys("Event: " + eventName, "name", tuples, order).map((sortedTuples) => {
        const handlers2 = map$2(sortedTuples, (tuple) => tuple.handler);
        return fuse$1(handlers2);
      });
    }
  };
  const combineGroups = (byEventName, eventOrder) => {
    const r2 = mapToArray(byEventName, (tuples, eventName) => {
      const combined = tuples.length === 1 ? Result.value(tuples[0].handler) : fuse(tuples, eventOrder, eventName);
      return combined.map((handler) => {
        const assembled = assemble(handler);
        const purpose = tuples.length > 1 ? filter$2(eventOrder[eventName], (o) => exists(tuples, (t2) => t2.name === o)).join(" > ") : tuples[0].name;
        return wrap$1(eventName, uncurried(assembled, purpose));
      });
    });
    return consolidate(r2, {});
  };
  const baseBehaviour = "alloy.base.behaviour";
  const schema$z = objOf([
    field$1("dom", "dom", required$2(), objOf([
      required$1("tag"),
      defaulted("styles", {}),
      defaulted("classes", []),
      defaulted("attributes", {}),
      option$3("value"),
      option$3("innerHtml")
    ])),
    required$1("components"),
    required$1("uid"),
    defaulted("events", {}),
    defaulted("apis", {}),
    field$1("eventOrder", "eventOrder", mergeWith({
      [execute$5()]: [
        "disabling",
        baseBehaviour,
        "toggling",
        "typeaheadevents"
      ],
      [focus$4()]: [
        baseBehaviour,
        "focusing",
        "keying"
      ],
      [systemInit()]: [
        baseBehaviour,
        "disabling",
        "toggling",
        "representing"
      ],
      [input()]: [
        baseBehaviour,
        "representing",
        "streaming",
        "invalidating"
      ],
      [detachedFromDom()]: [
        baseBehaviour,
        "representing",
        "item-events",
        "tooltipping"
      ],
      [mousedown()]: [
        "focusing",
        baseBehaviour,
        "item-type-events"
      ],
      [touchstart()]: [
        "focusing",
        baseBehaviour,
        "item-type-events"
      ],
      [mouseover()]: [
        "item-type-events",
        "tooltipping"
      ],
      [receive()]: [
        "receiving",
        "reflecting",
        "tooltipping"
      ]
    }), anyValue()),
    option$3("domModification")
  ]);
  const toInfo = (spec) => asRaw("custom.definition", schema$z, spec);
  const toDefinition = (detail) => ({
    ...detail.dom,
    uid: detail.uid,
    domChildren: map$2(detail.components, (comp) => comp.element)
  });
  const toModification = (detail) => detail.domModification.fold(() => nu$7({}), nu$7);
  const toEvents = (info) => info.events;
  const read = (element2, attr) => {
    const value2 = get$f(element2, attr);
    return value2 === void 0 || value2 === "" ? [] : value2.split(" ");
  };
  const add$4 = (element2, attr, id) => {
    const old = read(element2, attr);
    const nu2 = old.concat([id]);
    set$9(element2, attr, nu2.join(" "));
    return true;
  };
  const remove$4 = (element2, attr, id) => {
    const nu2 = filter$2(read(element2, attr), (v) => v !== id);
    if (nu2.length > 0) {
      set$9(element2, attr, nu2.join(" "));
    } else {
      remove$7(element2, attr);
    }
    return false;
  };
  const supports = (element2) => element2.dom.classList !== void 0;
  const get$8 = (element2) => read(element2, "class");
  const add$3 = (element2, clazz) => add$4(element2, "class", clazz);
  const remove$3 = (element2, clazz) => remove$4(element2, "class", clazz);
  const add$2 = (element2, clazz) => {
    if (supports(element2)) {
      element2.dom.classList.add(clazz);
    } else {
      add$3(element2, clazz);
    }
  };
  const cleanClass = (element2) => {
    const classList = supports(element2) ? element2.dom.classList : get$8(element2);
    if (classList.length === 0) {
      remove$7(element2, "class");
    }
  };
  const remove$2 = (element2, clazz) => {
    if (supports(element2)) {
      const classList = element2.dom.classList;
      classList.remove(clazz);
    } else {
      remove$3(element2, clazz);
    }
    cleanClass(element2);
  };
  const has = (element2, clazz) => supports(element2) && element2.dom.classList.contains(clazz);
  const add$1 = (element2, classes2) => {
    each$1(classes2, (x) => {
      add$2(element2, x);
    });
  };
  const remove$1 = (element2, classes2) => {
    each$1(classes2, (x) => {
      remove$2(element2, x);
    });
  };
  const hasAll = (element2, classes2) => forall(classes2, (clazz) => has(element2, clazz));
  const getNative = (element2) => {
    const classList = element2.dom.classList;
    const r2 = new Array(classList.length);
    for (let i2 = 0; i2 < classList.length; i2++) {
      const item2 = classList.item(i2);
      if (item2 !== null) {
        r2[i2] = item2;
      }
    }
    return r2;
  };
  const get$7 = (element2) => supports(element2) ? getNative(element2) : get$8(element2);
  const get$6 = (element2) => element2.dom.value;
  const set$5 = (element2, value2) => {
    if (value2 === void 0) {
      throw new Error("Value.set was undefined");
    }
    element2.dom.value = value2;
  };
  const determineObsoleted = (parent2, index, oldObsoleted) => {
    const newObsoleted = child$2(parent2, index);
    return newObsoleted.map((newObs) => {
      const elemChanged = oldObsoleted.exists((o) => !eq(o, newObs));
      if (elemChanged) {
        const oldTag = oldObsoleted.map(name$3).getOr("span");
        const marker = SugarElement.fromTag(oldTag);
        before$1(newObs, marker);
        return marker;
      } else {
        return newObs;
      }
    });
  };
  const ensureInDom = (parent2, child2, obsoleted) => {
    obsoleted.fold(() => append$2(parent2, child2), (obs) => {
      if (!eq(obs, child2)) {
        before$1(obs, child2);
        remove$5(obs);
      }
    });
  };
  const patchChildrenWith = (parent2, nu2, f) => {
    const builtChildren = map$2(nu2, f);
    const currentChildren = children(parent2);
    each$1(currentChildren.slice(builtChildren.length), remove$5);
    return builtChildren;
  };
  const patchSpecChild = (parent2, index, spec, build2) => {
    const oldObsoleted = child$2(parent2, index);
    const childComp = build2(spec, oldObsoleted);
    const obsoleted = determineObsoleted(parent2, index, oldObsoleted);
    ensureInDom(parent2, childComp.element, obsoleted);
    return childComp;
  };
  const patchSpecChildren = (parent2, specs, build2) => patchChildrenWith(parent2, specs, (spec, index) => patchSpecChild(parent2, index, spec, build2));
  const patchDomChildren = (parent2, nodes) => patchChildrenWith(parent2, nodes, (node, index) => {
    const optObsoleted = child$2(parent2, index);
    ensureInDom(parent2, node, optObsoleted);
    return node;
  });
  const diffKeyValueSet = (newObj, oldObj) => {
    const newKeys = keys(newObj);
    const oldKeys = keys(oldObj);
    const toRemove = difference(oldKeys, newKeys);
    const toSet = bifilter(newObj, (v, k) => {
      return !has$2(oldObj, k) || v !== oldObj[k];
    }).t;
    return {
      toRemove,
      toSet
    };
  };
  const reconcileToDom = (definition, obsoleted) => {
    const {
      class: clazz,
      style,
      ...existingAttributes
    } = clone$1(obsoleted);
    const {
      toSet: attrsToSet,
      toRemove: attrsToRemove
    } = diffKeyValueSet(definition.attributes, existingAttributes);
    const updateAttrs = () => {
      each$1(attrsToRemove, (a) => remove$7(obsoleted, a));
      setAll$1(obsoleted, attrsToSet);
    };
    const existingStyles = getAllRaw(obsoleted);
    const {
      toSet: stylesToSet,
      toRemove: stylesToRemove
    } = diffKeyValueSet(definition.styles, existingStyles);
    const updateStyles = () => {
      each$1(stylesToRemove, (s) => remove$6(obsoleted, s));
      setAll(obsoleted, stylesToSet);
    };
    const existingClasses = get$7(obsoleted);
    const classesToRemove = difference(existingClasses, definition.classes);
    const classesToAdd = difference(definition.classes, existingClasses);
    const updateClasses = () => {
      add$1(obsoleted, classesToAdd);
      remove$1(obsoleted, classesToRemove);
    };
    const updateHtml = (html) => {
      set$6(obsoleted, html);
    };
    const updateChildren = () => {
      const children2 = definition.domChildren;
      patchDomChildren(obsoleted, children2);
    };
    const updateValue = () => {
      const valueElement = obsoleted;
      const value2 = definition.value.getOrUndefined();
      if (value2 !== get$6(valueElement)) {
        set$5(valueElement, value2 !== null && value2 !== void 0 ? value2 : "");
      }
    };
    updateAttrs();
    updateClasses();
    updateStyles();
    definition.innerHtml.fold(updateChildren, updateHtml);
    updateValue();
    return obsoleted;
  };
  const introduceToDom = (definition) => {
    const subject = SugarElement.fromTag(definition.tag);
    setAll$1(subject, definition.attributes);
    add$1(subject, definition.classes);
    setAll(subject, definition.styles);
    definition.innerHtml.each((html) => set$6(subject, html));
    const children2 = definition.domChildren;
    append$1(subject, children2);
    definition.value.each((value2) => {
      set$5(subject, value2);
    });
    return subject;
  };
  const attemptPatch = (definition, obsoleted) => {
    try {
      const e = reconcileToDom(definition, obsoleted);
      return Optional.some(e);
    } catch (err) {
      return Optional.none();
    }
  };
  const hasMixedChildren = (definition) => definition.innerHtml.isSome() && definition.domChildren.length > 0;
  const renderToDom = (definition, optObsoleted) => {
    const canBePatched = (candidate) => name$3(candidate) === definition.tag && !hasMixedChildren(definition) && !isPremade(candidate);
    const elem = optObsoleted.filter(canBePatched).bind((obsoleted) => attemptPatch(definition, obsoleted)).getOrThunk(() => introduceToDom(definition));
    writeOnly(elem, definition.uid);
    return elem;
  };
  const getBehaviours$2 = (spec) => {
    const behaviours2 = get$g(spec, "behaviours").getOr({});
    return bind$3(keys(behaviours2), (name2) => {
      const behaviour = behaviours2[name2];
      return isNonNullable(behaviour) ? [behaviour.me] : [];
    });
  };
  const generateFrom = (spec, all2) => generateFrom$1(spec, all2);
  const generate$4 = (spec) => {
    const all2 = getBehaviours$2(spec);
    return generateFrom(spec, all2);
  };
  const getDomDefinition = (info, bList, bData) => {
    const definition = toDefinition(info);
    const infoModification = toModification(info);
    const baseModification = { "alloy.base.modification": infoModification };
    const modification = bList.length > 0 ? combine$2(bData, baseModification, bList, definition) : infoModification;
    return merge(definition, modification);
  };
  const getEvents = (info, bList, bData) => {
    const baseEvents = { "alloy.base.behaviour": toEvents(info) };
    return combine$1(bData, info.eventOrder, bList, baseEvents).getOrDie();
  };
  const build$2 = (spec, obsoleted) => {
    const getMe = () => me;
    const systemApi = Cell(singleton$1);
    const info = getOrDie(toInfo(spec));
    const bBlob = generate$4(spec);
    const bList = getBehaviours$3(bBlob);
    const bData = getData$2(bBlob);
    const modDefinition = getDomDefinition(info, bList, bData);
    const item2 = renderToDom(modDefinition, obsoleted);
    const events2 = getEvents(info, bList, bData);
    const subcomponents = Cell(info.components);
    const connect = (newApi) => {
      systemApi.set(newApi);
    };
    const disconnect = () => {
      systemApi.set(NoContextApi(getMe));
    };
    const syncComponents = () => {
      const children$1 = children(item2);
      const subs2 = bind$3(children$1, (child2) => systemApi.get().getByDom(child2).fold(() => [], pure$2));
      subcomponents.set(subs2);
    };
    const config2 = (behaviour) => {
      const b2 = bData;
      const f = isFunction(b2[behaviour.name()]) ? b2[behaviour.name()] : () => {
        throw new Error("Could not find " + behaviour.name() + " in " + JSON.stringify(spec, null, 2));
      };
      return f();
    };
    const hasConfigured = (behaviour) => isFunction(bData[behaviour.name()]);
    const getApis = () => info.apis;
    const readState = (behaviourName) => bData[behaviourName]().map((b2) => b2.state.readState()).getOr("not enabled");
    const me = {
      uid: spec.uid,
      getSystem: systemApi.get,
      config: config2,
      hasConfigured,
      spec,
      readState,
      getApis,
      connect,
      disconnect,
      element: item2,
      syncComponents,
      components: subcomponents.get,
      events: events2
    };
    return me;
  };
  const buildSubcomponents = (spec, obsoleted) => {
    const components2 = get$g(spec, "components").getOr([]);
    return obsoleted.fold(() => map$2(components2, build$1), (obs) => map$2(components2, (c, i2) => {
      return buildOrPatch(c, child$2(obs, i2));
    }));
  };
  const buildFromSpec = (userSpec, obsoleted) => {
    const {
      events: specEvents,
      ...spec
    } = make$8(userSpec);
    const components2 = buildSubcomponents(spec, obsoleted);
    const completeSpec = {
      ...spec,
      events: {
        ...DefaultEvents,
        ...specEvents
      },
      components: components2
    };
    return Result.value(build$2(completeSpec, obsoleted));
  };
  const text$1 = (textContent) => {
    const element2 = SugarElement.fromText(textContent);
    return external$1({ element: element2 });
  };
  const external$1 = (spec) => {
    const extSpec = asRawOrDie$1("external.component", objOfOnly([
      required$1("element"),
      option$3("uid")
    ]), spec);
    const systemApi = Cell(NoContextApi());
    const connect = (newApi) => {
      systemApi.set(newApi);
    };
    const disconnect = () => {
      systemApi.set(NoContextApi(() => me));
    };
    const uid = extSpec.uid.getOrThunk(() => generate$5("external"));
    writeOnly(extSpec.element, uid);
    const me = {
      uid,
      getSystem: systemApi.get,
      config: Optional.none,
      hasConfigured: never,
      connect,
      disconnect,
      getApis: () => ({}),
      element: extSpec.element,
      spec,
      readState: constant$1("No state"),
      syncComponents: noop,
      components: constant$1([]),
      events: {}
    };
    return premade$1(me);
  };
  const uids = generate$5;
  const isSketchSpec$1 = (spec) => has$2(spec, "uid");
  const buildOrPatch = (spec, obsoleted) => getPremade(spec).getOrThunk(() => {
    const userSpecWithUid = isSketchSpec$1(spec) ? spec : {
      uid: uids(""),
      ...spec
    };
    return buildFromSpec(userSpecWithUid, obsoleted).getOrDie();
  });
  const build$1 = (spec) => buildOrPatch(spec, Optional.none());
  const premade = premade$1;
  var ClosestOrAncestor = (is2, ancestor2, scope, a, isRoot) => {
    if (is2(scope, a)) {
      return Optional.some(scope);
    } else if (isFunction(isRoot) && isRoot(scope)) {
      return Optional.none();
    } else {
      return ancestor2(scope, a, isRoot);
    }
  };
  const ancestor$1 = (scope, predicate, isRoot) => {
    let element2 = scope.dom;
    const stop2 = isFunction(isRoot) ? isRoot : never;
    while (element2.parentNode) {
      element2 = element2.parentNode;
      const el = SugarElement.fromDom(element2);
      if (predicate(el)) {
        return Optional.some(el);
      } else if (stop2(el)) {
        break;
      }
    }
    return Optional.none();
  };
  const closest$3 = (scope, predicate, isRoot) => {
    const is2 = (s, test) => test(s);
    return ClosestOrAncestor(is2, ancestor$1, scope, predicate, isRoot);
  };
  const child$1 = (scope, predicate) => {
    const pred = (node) => predicate(SugarElement.fromDom(node));
    const result = find$5(scope.dom.childNodes, pred);
    return result.map(SugarElement.fromDom);
  };
  const descendant$1 = (scope, predicate) => {
    const descend = (node) => {
      for (let i2 = 0; i2 < node.childNodes.length; i2++) {
        const child2 = SugarElement.fromDom(node.childNodes[i2]);
        if (predicate(child2)) {
          return Optional.some(child2);
        }
        const res = descend(node.childNodes[i2]);
        if (res.isSome()) {
          return res;
        }
      }
      return Optional.none();
    };
    return descend(scope.dom);
  };
  const closest$2 = (scope, predicate, isRoot) => closest$3(scope, predicate, isRoot).isSome();
  const ancestor = (scope, selector, isRoot) => ancestor$1(scope, (e) => is(e, selector), isRoot);
  const child = (scope, selector) => child$1(scope, (e) => is(e, selector));
  const descendant = (scope, selector) => one(selector, scope);
  const closest$1 = (scope, selector, isRoot) => {
    const is$12 = (element2, selector2) => is(element2, selector2);
    return ClosestOrAncestor(is$12, ancestor, scope, selector, isRoot);
  };
  const attribute = "aria-controls";
  const find$1 = (queryElem) => {
    const dependent = closest$3(queryElem, (elem) => {
      if (!isElement$1(elem)) {
        return false;
      }
      const id = get$f(elem, "id");
      return id !== void 0 && id.indexOf(attribute) > -1;
    });
    return dependent.bind((dep) => {
      const id = get$f(dep, "id");
      const dos = getRootNode(dep);
      return descendant(dos, `[${attribute}="${id}"]`);
    });
  };
  const manager = () => {
    const ariaId = generate$6(attribute);
    const link = (elem) => {
      set$9(elem, attribute, ariaId);
    };
    const unlink = (elem) => {
      remove$7(elem, attribute);
    };
    return {
      id: ariaId,
      link,
      unlink
    };
  };
  const isAriaPartOf = (component, queryElem) => find$1(queryElem).exists((owner2) => isPartOf$1(component, owner2));
  const isPartOf$1 = (component, queryElem) => closest$2(queryElem, (el) => eq(el, component.element), never) || isAriaPartOf(component, queryElem);
  const unknown = "unknown";
  var EventConfiguration;
  (function(EventConfiguration2) {
    EventConfiguration2[EventConfiguration2["STOP"] = 0] = "STOP";
    EventConfiguration2[EventConfiguration2["NORMAL"] = 1] = "NORMAL";
    EventConfiguration2[EventConfiguration2["LOGGING"] = 2] = "LOGGING";
  })(EventConfiguration || (EventConfiguration = {}));
  const eventConfig = Cell({});
  const makeEventLogger = (eventName, initialTarget) => {
    const sequence2 = [];
    const startTime = (/* @__PURE__ */ new Date()).getTime();
    return {
      logEventCut: (_name, target, purpose) => {
        sequence2.push({
          outcome: "cut",
          target,
          purpose
        });
      },
      logEventStopped: (_name, target, purpose) => {
        sequence2.push({
          outcome: "stopped",
          target,
          purpose
        });
      },
      logNoParent: (_name, target, purpose) => {
        sequence2.push({
          outcome: "no-parent",
          target,
          purpose
        });
      },
      logEventNoHandlers: (_name, target) => {
        sequence2.push({
          outcome: "no-handlers-left",
          target
        });
      },
      logEventResponse: (_name, target, purpose) => {
        sequence2.push({
          outcome: "response",
          purpose,
          target
        });
      },
      write: () => {
        const finishTime = (/* @__PURE__ */ new Date()).getTime();
        if (contains$2([
          "mousemove",
          "mouseover",
          "mouseout",
          systemInit()
        ], eventName)) {
          return;
        }
        console.log(eventName, {
          event: eventName,
          time: finishTime - startTime,
          target: initialTarget.dom,
          sequence: map$2(sequence2, (s) => {
            if (!contains$2([
              "cut",
              "stopped",
              "response"
            ], s.outcome)) {
              return s.outcome;
            } else {
              return "{" + s.purpose + "} " + s.outcome + " at (" + element(s.target) + ")";
            }
          })
        });
      }
    };
  };
  const processEvent = (eventName, initialTarget, f) => {
    const status = get$g(eventConfig.get(), eventName).orThunk(() => {
      const patterns = keys(eventConfig.get());
      return findMap(patterns, (p) => eventName.indexOf(p) > -1 ? Optional.some(eventConfig.get()[p]) : Optional.none());
    }).getOr(EventConfiguration.NORMAL);
    switch (status) {
      case EventConfiguration.NORMAL:
        return f(noLogger());
      case EventConfiguration.LOGGING: {
        const logger = makeEventLogger(eventName, initialTarget);
        const output2 = f(logger);
        logger.write();
        return output2;
      }
      case EventConfiguration.STOP:
        return true;
    }
  };
  const path = [
    "alloy/data/Fields",
    "alloy/debugging/Debugging"
  ];
  const getTrace = () => {
    const err = new Error();
    if (err.stack !== void 0) {
      const lines = err.stack.split("\n");
      return find$5(lines, (line) => line.indexOf("alloy") > 0 && !exists(path, (p) => line.indexOf(p) > -1)).getOr(unknown);
    } else {
      return unknown;
    }
  };
  const ignoreEvent = {
    logEventCut: noop,
    logEventStopped: noop,
    logNoParent: noop,
    logEventNoHandlers: noop,
    logEventResponse: noop,
    write: noop
  };
  const monitorEvent = (eventName, initialTarget, f) => processEvent(eventName, initialTarget, f);
  const noLogger = constant$1(ignoreEvent);
  const menuFields = constant$1([
    required$1("menu"),
    required$1("selectedMenu")
  ]);
  const itemFields = constant$1([
    required$1("item"),
    required$1("selectedItem")
  ]);
  constant$1(objOf(itemFields().concat(menuFields())));
  const itemSchema$3 = constant$1(objOf(itemFields()));
  const _initSize = requiredObjOf("initSize", [
    required$1("numColumns"),
    required$1("numRows")
  ]);
  const itemMarkers = () => requiredOf("markers", itemSchema$3());
  const tieredMenuMarkers = () => requiredObjOf("markers", [required$1("backgroundMenu")].concat(menuFields()).concat(itemFields()));
  const markers$1 = (required2) => requiredObjOf("markers", map$2(required2, required$1));
  const onPresenceHandler = (label2, fieldName, presence) => {
    getTrace();
    return field$1(fieldName, fieldName, presence, valueOf((f) => Result.value((...args) => {
      return f.apply(void 0, args);
    })));
  };
  const onHandler = (fieldName) => onPresenceHandler("onHandler", fieldName, defaulted$1(noop));
  const onKeyboardHandler = (fieldName) => onPresenceHandler("onKeyboardHandler", fieldName, defaulted$1(Optional.none));
  const onStrictHandler = (fieldName) => onPresenceHandler("onHandler", fieldName, required$2());
  const onStrictKeyboardHandler = (fieldName) => onPresenceHandler("onKeyboardHandler", fieldName, required$2());
  const output$1 = (name2, value2) => customField(name2, constant$1(value2));
  const snapshot = (name2) => customField(name2, identity);
  const initSize = constant$1(_initSize);
  const nu$6 = (x, y, bubble, direction, placement2, boundsRestriction2, labelPrefix2, alwaysFit = false) => ({
    x,
    y,
    bubble,
    direction,
    placement: placement2,
    restriction: boundsRestriction2,
    label: `${labelPrefix2}-${placement2}`,
    alwaysFit
  });
  const adt$a = Adt.generate([
    { southeast: [] },
    { southwest: [] },
    { northeast: [] },
    { northwest: [] },
    { south: [] },
    { north: [] },
    { east: [] },
    { west: [] }
  ]);
  const cata$2 = (subject, southeast2, southwest2, northeast2, northwest2, south2, north2, east2, west2) => subject.fold(southeast2, southwest2, northeast2, northwest2, south2, north2, east2, west2);
  const cataVertical = (subject, south2, middle, north2) => subject.fold(south2, south2, north2, north2, south2, north2, middle, middle);
  const cataHorizontal = (subject, east2, middle, west2) => subject.fold(east2, west2, east2, west2, middle, middle, east2, west2);
  const southeast$3 = adt$a.southeast;
  const southwest$3 = adt$a.southwest;
  const northeast$3 = adt$a.northeast;
  const northwest$3 = adt$a.northwest;
  const south$3 = adt$a.south;
  const north$3 = adt$a.north;
  const east$3 = adt$a.east;
  const west$3 = adt$a.west;
  const cycleBy = (value2, delta, min2, max2) => {
    const r2 = value2 + delta;
    if (r2 > max2) {
      return min2;
    } else if (r2 < min2) {
      return max2;
    } else {
      return r2;
    }
  };
  const clamp = (value2, min2, max2) => Math.min(Math.max(value2, min2), max2);
  const getRestriction = (anchor2, restriction) => {
    switch (restriction) {
      case 1:
        return anchor2.x;
      case 0:
        return anchor2.x + anchor2.width;
      case 2:
        return anchor2.y;
      case 3:
        return anchor2.y + anchor2.height;
    }
  };
  const boundsRestriction = (anchor2, restrictions) => mapToObject([
    "left",
    "right",
    "top",
    "bottom"
  ], (dir) => get$g(restrictions, dir).map((restriction) => getRestriction(anchor2, restriction)));
  const adjustBounds = (bounds$12, restriction, bubbleOffset) => {
    const applyRestriction = (dir, current) => restriction[dir].map((pos) => {
      const isVerticalAxis = dir === "top" || dir === "bottom";
      const offset2 = isVerticalAxis ? bubbleOffset.top : bubbleOffset.left;
      const comparator = dir === "left" || dir === "top" ? Math.max : Math.min;
      const newPos = comparator(pos, current) + offset2;
      return isVerticalAxis ? clamp(newPos, bounds$12.y, bounds$12.bottom) : clamp(newPos, bounds$12.x, bounds$12.right);
    }).getOr(current);
    const adjustedLeft = applyRestriction("left", bounds$12.x);
    const adjustedTop = applyRestriction("top", bounds$12.y);
    const adjustedRight = applyRestriction("right", bounds$12.right);
    const adjustedBottom = applyRestriction("bottom", bounds$12.bottom);
    return bounds(adjustedLeft, adjustedTop, adjustedRight - adjustedLeft, adjustedBottom - adjustedTop);
  };
  const labelPrefix$2 = "layout";
  const eastX$1 = (anchor2) => anchor2.x;
  const middleX$1 = (anchor2, element2) => anchor2.x + anchor2.width / 2 - element2.width / 2;
  const westX$1 = (anchor2, element2) => anchor2.x + anchor2.width - element2.width;
  const northY$2 = (anchor2, element2) => anchor2.y - element2.height;
  const southY$2 = (anchor2) => anchor2.y + anchor2.height;
  const centreY$1 = (anchor2, element2) => anchor2.y + anchor2.height / 2 - element2.height / 2;
  const eastEdgeX$1 = (anchor2) => anchor2.x + anchor2.width;
  const westEdgeX$1 = (anchor2, element2) => anchor2.x - element2.width;
  const southeast$2 = (anchor2, element2, bubbles) => nu$6(eastX$1(anchor2), southY$2(anchor2), bubbles.southeast(), southeast$3(), "southeast", boundsRestriction(anchor2, {
    left: 1,
    top: 3
  }), labelPrefix$2);
  const southwest$2 = (anchor2, element2, bubbles) => nu$6(westX$1(anchor2, element2), southY$2(anchor2), bubbles.southwest(), southwest$3(), "southwest", boundsRestriction(anchor2, {
    right: 0,
    top: 3
  }), labelPrefix$2);
  const northeast$2 = (anchor2, element2, bubbles) => nu$6(eastX$1(anchor2), northY$2(anchor2, element2), bubbles.northeast(), northeast$3(), "northeast", boundsRestriction(anchor2, {
    left: 1,
    bottom: 2
  }), labelPrefix$2);
  const northwest$2 = (anchor2, element2, bubbles) => nu$6(westX$1(anchor2, element2), northY$2(anchor2, element2), bubbles.northwest(), northwest$3(), "northwest", boundsRestriction(anchor2, {
    right: 0,
    bottom: 2
  }), labelPrefix$2);
  const north$2 = (anchor2, element2, bubbles) => nu$6(middleX$1(anchor2, element2), northY$2(anchor2, element2), bubbles.north(), north$3(), "north", boundsRestriction(anchor2, { bottom: 2 }), labelPrefix$2);
  const south$2 = (anchor2, element2, bubbles) => nu$6(middleX$1(anchor2, element2), southY$2(anchor2), bubbles.south(), south$3(), "south", boundsRestriction(anchor2, { top: 3 }), labelPrefix$2);
  const east$2 = (anchor2, element2, bubbles) => nu$6(eastEdgeX$1(anchor2), centreY$1(anchor2, element2), bubbles.east(), east$3(), "east", boundsRestriction(anchor2, { left: 0 }), labelPrefix$2);
  const west$2 = (anchor2, element2, bubbles) => nu$6(westEdgeX$1(anchor2, element2), centreY$1(anchor2, element2), bubbles.west(), west$3(), "west", boundsRestriction(anchor2, { right: 1 }), labelPrefix$2);
  const all$1 = () => [
    southeast$2,
    southwest$2,
    northeast$2,
    northwest$2,
    south$2,
    north$2,
    east$2,
    west$2
  ];
  const allRtl$1 = () => [
    southwest$2,
    southeast$2,
    northwest$2,
    northeast$2,
    south$2,
    north$2,
    east$2,
    west$2
  ];
  const aboveOrBelow = () => [
    northeast$2,
    northwest$2,
    southeast$2,
    southwest$2,
    north$2,
    south$2
  ];
  const aboveOrBelowRtl = () => [
    northwest$2,
    northeast$2,
    southwest$2,
    southeast$2,
    north$2,
    south$2
  ];
  const belowOrAbove = () => [
    southeast$2,
    southwest$2,
    northeast$2,
    northwest$2,
    south$2,
    north$2
  ];
  const belowOrAboveRtl = () => [
    southwest$2,
    southeast$2,
    northwest$2,
    northeast$2,
    south$2,
    north$2
  ];
  const chooseChannels = (channels, message) => message.universal ? channels : filter$2(channels, (ch) => contains$2(message.channels, ch));
  const events$h = (receiveConfig) => derive$2([run$1(receive(), (component, message) => {
    const channelMap = receiveConfig.channels;
    const channels = keys(channelMap);
    const receivingData = message;
    const targetChannels = chooseChannels(channels, receivingData);
    each$1(targetChannels, (ch) => {
      const channelInfo = channelMap[ch];
      const channelSchema = channelInfo.schema;
      const data = asRawOrDie$1("channel[" + ch + "] data\nReceiver: " + element(component.element), channelSchema, receivingData.data);
      channelInfo.onReceive(component, data);
    });
  })]);
  var ActiveReceiving = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$h
  });
  var ReceivingSchema = [requiredOf("channels", setOf(Result.value, objOfOnly([
    onStrictHandler("onReceive"),
    defaulted("schema", anyValue())
  ])))];
  const executeEvent = (bConfig, bState, executor) => runOnExecute$1((component) => {
    executor(component, bConfig, bState);
  });
  const loadEvent = (bConfig, bState, f) => runOnInit((component, _simulatedEvent) => {
    f(component, bConfig, bState);
  });
  const create$4 = (schema2, name2, active2, apis, extra, state) => {
    const configSchema = objOfOnly(schema2);
    const schemaSchema = optionObjOf(name2, [optionObjOfOnly("config", schema2)]);
    return doCreate(configSchema, schemaSchema, name2, active2, apis, extra, state);
  };
  const createModes$1 = (modes, name2, active2, apis, extra, state) => {
    const configSchema = modes;
    const schemaSchema = optionObjOf(name2, [optionOf("config", modes)]);
    return doCreate(configSchema, schemaSchema, name2, active2, apis, extra, state);
  };
  const wrapApi = (bName, apiFunction, apiName) => {
    const f = (component, ...rest) => {
      const args = [component].concat(rest);
      return component.config({ name: constant$1(bName) }).fold(() => {
        throw new Error("We could not find any behaviour configuration for: " + bName + ". Using API: " + apiName);
      }, (info) => {
        const rest2 = Array.prototype.slice.call(args, 1);
        return apiFunction.apply(void 0, [
          component,
          info.config,
          info.state
        ].concat(rest2));
      });
    };
    return markAsBehaviourApi(f, apiName, apiFunction);
  };
  const revokeBehaviour = (name2) => ({
    key: name2,
    value: void 0
  });
  const doCreate = (configSchema, schemaSchema, name2, active2, apis, extra, state) => {
    const getConfig = (info) => hasNonNullableKey(info, name2) ? info[name2]() : Optional.none();
    const wrappedApis = map$1(apis, (apiF, apiName) => wrapApi(name2, apiF, apiName));
    const wrappedExtra = map$1(extra, (extraF, extraName) => markAsExtraApi(extraF, extraName));
    const me = {
      ...wrappedExtra,
      ...wrappedApis,
      revoke: curry(revokeBehaviour, name2),
      config: (spec) => {
        const prepared = asRawOrDie$1(name2 + "-config", configSchema, spec);
        return {
          key: name2,
          value: {
            config: prepared,
            me,
            configAsRaw: cached(() => asRawOrDie$1(name2 + "-config", configSchema, spec)),
            initialConfig: spec,
            state
          }
        };
      },
      schema: constant$1(schemaSchema),
      exhibit: (info, base2) => {
        return lift2(getConfig(info), get$g(active2, "exhibit"), (behaviourInfo, exhibitor) => {
          return exhibitor(base2, behaviourInfo.config, behaviourInfo.state);
        }).getOrThunk(() => nu$7({}));
      },
      name: constant$1(name2),
      handlers: (info) => {
        return getConfig(info).map((behaviourInfo) => {
          const getEvents2 = get$g(active2, "events").getOr(() => ({}));
          return getEvents2(behaviourInfo.config, behaviourInfo.state);
        }).getOr({});
      }
    };
    return me;
  };
  const derive$1 = (capabilities) => wrapAll(capabilities);
  const simpleSchema = objOfOnly([
    required$1("fields"),
    required$1("name"),
    defaulted("active", {}),
    defaulted("apis", {}),
    defaulted("state", NoState),
    defaulted("extra", {})
  ]);
  const create$3 = (data) => {
    const value2 = asRawOrDie$1("Creating behaviour: " + data.name, simpleSchema, data);
    return create$4(value2.fields, value2.name, value2.active, value2.apis, value2.extra, value2.state);
  };
  const modeSchema = objOfOnly([
    required$1("branchKey"),
    required$1("branches"),
    required$1("name"),
    defaulted("active", {}),
    defaulted("apis", {}),
    defaulted("state", NoState),
    defaulted("extra", {})
  ]);
  const createModes = (data) => {
    const value2 = asRawOrDie$1("Creating behaviour: " + data.name, modeSchema, data);
    return createModes$1(choose$1(value2.branchKey, value2.branches), value2.name, value2.active, value2.apis, value2.extra, value2.state);
  };
  const revoke = constant$1(void 0);
  const Receiving = create$3({
    fields: ReceivingSchema,
    name: "receiving",
    active: ActiveReceiving
  });
  const exhibit$6 = (base2, posConfig) => nu$7({
    classes: [],
    styles: posConfig.useFixed() ? {} : { position: "relative" }
  });
  var ActivePosition = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    exhibit: exhibit$6
  });
  const focus$3 = (element2) => element2.dom.focus();
  const blur$1 = (element2) => element2.dom.blur();
  const hasFocus = (element2) => {
    const root = getRootNode(element2).dom;
    return element2.dom === root.activeElement;
  };
  const active$1 = (root = getDocument()) => Optional.from(root.dom.activeElement).map(SugarElement.fromDom);
  const search = (element2) => active$1(getRootNode(element2)).filter((e) => element2.dom.contains(e.dom));
  const preserve$1 = (f, container) => {
    const dos = getRootNode(container);
    const refocus = active$1(dos).bind((focused) => {
      const hasFocus2 = (elem) => eq(focused, elem);
      return hasFocus2(container) ? Optional.some(container) : descendant$1(container, hasFocus2);
    });
    const result = f(container);
    refocus.each((oldFocus) => {
      active$1(dos).filter((newFocus) => eq(newFocus, oldFocus)).fold(() => {
        focus$3(oldFocus);
      }, noop);
    });
    return result;
  };
  const NuPositionCss = (position2, left2, top2, right2, bottom2) => {
    const toPx = (num) => num + "px";
    return {
      position: position2,
      left: left2.map(toPx),
      top: top2.map(toPx),
      right: right2.map(toPx),
      bottom: bottom2.map(toPx)
    };
  };
  const toOptions = (position2) => ({
    ...position2,
    position: Optional.some(position2.position)
  });
  const applyPositionCss = (element2, position2) => {
    setOptions(element2, toOptions(position2));
  };
  const adt$9 = Adt.generate([
    { none: [] },
    {
      relative: [
        "x",
        "y",
        "width",
        "height"
      ]
    },
    {
      fixed: [
        "x",
        "y",
        "width",
        "height"
      ]
    }
  ]);
  const positionWithDirection = (posName, decision, x, y, width2, height2) => {
    const decisionRect = decision.rect;
    const decisionX = decisionRect.x - x;
    const decisionY = decisionRect.y - y;
    const decisionWidth = decisionRect.width;
    const decisionHeight = decisionRect.height;
    const decisionRight = width2 - (decisionX + decisionWidth);
    const decisionBottom = height2 - (decisionY + decisionHeight);
    const left2 = Optional.some(decisionX);
    const top2 = Optional.some(decisionY);
    const right2 = Optional.some(decisionRight);
    const bottom2 = Optional.some(decisionBottom);
    const none = Optional.none();
    return cata$2(decision.direction, () => NuPositionCss(posName, left2, top2, none, none), () => NuPositionCss(posName, none, top2, right2, none), () => NuPositionCss(posName, left2, none, none, bottom2), () => NuPositionCss(posName, none, none, right2, bottom2), () => NuPositionCss(posName, left2, top2, none, none), () => NuPositionCss(posName, left2, none, none, bottom2), () => NuPositionCss(posName, left2, top2, none, none), () => NuPositionCss(posName, none, top2, right2, none));
  };
  const reposition = (origin, decision) => origin.fold(() => {
    const decisionRect = decision.rect;
    return NuPositionCss("absolute", Optional.some(decisionRect.x), Optional.some(decisionRect.y), Optional.none(), Optional.none());
  }, (x, y, width2, height2) => {
    return positionWithDirection("absolute", decision, x, y, width2, height2);
  }, (x, y, width2, height2) => {
    return positionWithDirection("fixed", decision, x, y, width2, height2);
  });
  const toBox = (origin, element2) => {
    const rel = curry(find$2, element2);
    const position2 = origin.fold(rel, rel, () => {
      const scroll = get$b();
      return find$2(element2).translate(-scroll.left, -scroll.top);
    });
    const width2 = getOuter$1(element2);
    const height2 = getOuter$2(element2);
    return bounds(position2.left, position2.top, width2, height2);
  };
  const viewport = (origin, getBounds2) => getBounds2.fold(() => origin.fold(win, win, bounds), (b2) => origin.fold(b2, b2, () => {
    const bounds$12 = b2();
    const pos = translate$2(origin, bounds$12.x, bounds$12.y);
    return bounds(pos.left, pos.top, bounds$12.width, bounds$12.height);
  }));
  const translate$2 = (origin, x, y) => {
    const pos = SugarPosition(x, y);
    const removeScroll = () => {
      const outerScroll = get$b();
      return pos.translate(-outerScroll.left, -outerScroll.top);
    };
    return origin.fold(constant$1(pos), constant$1(pos), removeScroll);
  };
  const cata$1 = (subject, onNone, onRelative, onFixed) => subject.fold(onNone, onRelative, onFixed);
  adt$9.none;
  const relative$1 = adt$9.relative;
  const fixed$1 = adt$9.fixed;
  const anchor = (anchorBox, origin) => ({
    anchorBox,
    origin
  });
  const box = (anchorBox, origin) => anchor(anchorBox, origin);
  const placementAttribute = "data-alloy-placement";
  const setPlacement$1 = (element2, placement2) => {
    set$9(element2, placementAttribute, placement2);
  };
  const getPlacement = (element2) => getOpt(element2, placementAttribute);
  const reset$2 = (element2) => remove$7(element2, placementAttribute);
  const adt$8 = Adt.generate([
    { fit: ["reposition"] },
    {
      nofit: [
        "reposition",
        "visibleW",
        "visibleH",
        "isVisible"
      ]
    }
  ]);
  const determinePosition = (box2, bounds2) => {
    const {
      x: boundsX,
      y: boundsY,
      right: boundsRight,
      bottom: boundsBottom
    } = bounds2;
    const { x, y, right: right2, bottom: bottom2, width: width2, height: height2 } = box2;
    const xInBounds = x >= boundsX && x <= boundsRight;
    const yInBounds = y >= boundsY && y <= boundsBottom;
    const originInBounds = xInBounds && yInBounds;
    const rightInBounds = right2 <= boundsRight && right2 >= boundsX;
    const bottomInBounds = bottom2 <= boundsBottom && bottom2 >= boundsY;
    const sizeInBounds = rightInBounds && bottomInBounds;
    const visibleW = Math.min(width2, x >= boundsX ? boundsRight - x : right2 - boundsX);
    const visibleH = Math.min(height2, y >= boundsY ? boundsBottom - y : bottom2 - boundsY);
    return {
      originInBounds,
      sizeInBounds,
      visibleW,
      visibleH
    };
  };
  const calcReposition = (box2, bounds$12) => {
    const {
      x: boundsX,
      y: boundsY,
      right: boundsRight,
      bottom: boundsBottom
    } = bounds$12;
    const { x, y, width: width2, height: height2 } = box2;
    const maxX2 = Math.max(boundsX, boundsRight - width2);
    const maxY2 = Math.max(boundsY, boundsBottom - height2);
    const restrictedX = clamp(x, boundsX, maxX2);
    const restrictedY = clamp(y, boundsY, maxY2);
    const restrictedWidth = Math.min(restrictedX + width2, boundsRight) - restrictedX;
    const restrictedHeight = Math.min(restrictedY + height2, boundsBottom) - restrictedY;
    return bounds(restrictedX, restrictedY, restrictedWidth, restrictedHeight);
  };
  const calcMaxSizes = (direction, box2, bounds2) => {
    const upAvailable = constant$1(box2.bottom - bounds2.y);
    const downAvailable = constant$1(bounds2.bottom - box2.y);
    const maxHeight = cataVertical(direction, downAvailable, downAvailable, upAvailable);
    const westAvailable = constant$1(box2.right - bounds2.x);
    const eastAvailable = constant$1(bounds2.right - box2.x);
    const maxWidth = cataHorizontal(direction, eastAvailable, eastAvailable, westAvailable);
    return {
      maxWidth,
      maxHeight
    };
  };
  const attempt = (candidate, width2, height2, bounds$12) => {
    const bubble = candidate.bubble;
    const bubbleOffset = bubble.offset;
    const adjustedBounds = adjustBounds(bounds$12, candidate.restriction, bubbleOffset);
    const newX = candidate.x + bubbleOffset.left;
    const newY = candidate.y + bubbleOffset.top;
    const box2 = bounds(newX, newY, width2, height2);
    const { originInBounds, sizeInBounds, visibleW, visibleH } = determinePosition(box2, adjustedBounds);
    const fits = originInBounds && sizeInBounds;
    const fittedBox = fits ? box2 : calcReposition(box2, adjustedBounds);
    const isPartlyVisible = fittedBox.width > 0 && fittedBox.height > 0;
    const { maxWidth, maxHeight } = calcMaxSizes(candidate.direction, fittedBox, bounds$12);
    const reposition2 = {
      rect: fittedBox,
      maxHeight,
      maxWidth,
      direction: candidate.direction,
      placement: candidate.placement,
      classes: {
        on: bubble.classesOn,
        off: bubble.classesOff
      },
      layout: candidate.label,
      testY: newY
    };
    return fits || candidate.alwaysFit ? adt$8.fit(reposition2) : adt$8.nofit(reposition2, visibleW, visibleH, isPartlyVisible);
  };
  const attempts = (element2, candidates, anchorBox, elementBox, bubbles, bounds2) => {
    const panelWidth = elementBox.width;
    const panelHeight = elementBox.height;
    const attemptBestFit = (layout2, reposition2, visibleW, visibleH, isVisible2) => {
      const next = layout2(anchorBox, elementBox, bubbles, element2, bounds2);
      const attemptLayout = attempt(next, panelWidth, panelHeight, bounds2);
      return attemptLayout.fold(constant$1(attemptLayout), (newReposition, newVisibleW, newVisibleH, newIsVisible) => {
        const improved = isVisible2 === newIsVisible ? newVisibleH > visibleH || newVisibleW > visibleW : !isVisible2 && newIsVisible;
        return improved ? attemptLayout : adt$8.nofit(reposition2, visibleW, visibleH, isVisible2);
      });
    };
    const abc = foldl(candidates, (b2, a) => {
      const bestNext = curry(attemptBestFit, a);
      return b2.fold(constant$1(b2), bestNext);
    }, adt$8.nofit({
      rect: anchorBox,
      maxHeight: elementBox.height,
      maxWidth: elementBox.width,
      direction: southeast$3(),
      placement: "southeast",
      classes: {
        on: [],
        off: []
      },
      layout: "none",
      testY: anchorBox.y
    }, -1, -1, false));
    return abc.fold(identity, identity);
  };
  const singleton = (doRevoke) => {
    const subject = Cell(Optional.none());
    const revoke2 = () => subject.get().each(doRevoke);
    const clear2 = () => {
      revoke2();
      subject.set(Optional.none());
    };
    const isSet = () => subject.get().isSome();
    const get2 = () => subject.get();
    const set2 = (s) => {
      revoke2();
      subject.set(Optional.some(s));
    };
    return {
      clear: clear2,
      isSet,
      get: get2,
      set: set2
    };
  };
  const destroyable = () => singleton((s) => s.destroy());
  const unbindable = () => singleton((s) => s.unbind());
  const value$2 = () => {
    const subject = singleton(noop);
    const on2 = (f) => subject.get().each(f);
    return {
      ...subject,
      on: on2
    };
  };
  const filter = always;
  const bind = (element2, event2, handler) => bind$2(element2, event2, filter, handler);
  const capture = (element2, event2, handler) => capture$1(element2, event2, filter, handler);
  const fromRawEvent = fromRawEvent$1;
  const properties = [
    "top",
    "bottom",
    "right",
    "left"
  ];
  const timerAttr = "data-alloy-transition-timer";
  const isTransitioning$1 = (element2, transition) => hasAll(element2, transition.classes);
  const shouldApplyTransitionCss = (transition, decision, lastPlacement) => {
    return lastPlacement.exists((placer) => {
      const mode = transition.mode;
      return mode === "all" ? true : placer[mode] !== decision[mode];
    });
  };
  const hasChanges = (position2, intermediate) => {
    const round2 = (value2) => parseFloat(value2).toFixed(3);
    return find$4(intermediate, (value2, key) => {
      const newValue = position2[key].map(round2);
      const val = value2.map(round2);
      return !equals(newValue, val);
    }).isSome();
  };
  const getTransitionDuration = (element2) => {
    const get2 = (name2) => {
      const style = get$e(element2, name2);
      const times = style.split(/\s*,\s*/);
      return filter$2(times, isNotEmpty);
    };
    const parse2 = (value2) => {
      if (isString(value2) && /^[\d.]+/.test(value2)) {
        const num = parseFloat(value2);
        return endsWith(value2, "ms") ? num : num * 1e3;
      } else {
        return 0;
      }
    };
    const delay = get2("transition-delay");
    const duration = get2("transition-duration");
    return foldl(duration, (acc, dur, i2) => {
      const time = parse2(delay[i2]) + parse2(dur);
      return Math.max(acc, time);
    }, 0);
  };
  const setupTransitionListeners = (element2, transition) => {
    const transitionEnd = unbindable();
    const transitionCancel = unbindable();
    let timer;
    const isSourceTransition = (e) => {
      var _a;
      const pseudoElement = (_a = e.raw.pseudoElement) !== null && _a !== void 0 ? _a : "";
      return eq(e.target, element2) && isEmpty(pseudoElement) && contains$2(properties, e.raw.propertyName);
    };
    const transitionDone = (e) => {
      if (isNullable(e) || isSourceTransition(e)) {
        transitionEnd.clear();
        transitionCancel.clear();
        const type2 = e === null || e === void 0 ? void 0 : e.raw.type;
        if (isNullable(type2) || type2 === transitionend()) {
          clearTimeout(timer);
          remove$7(element2, timerAttr);
          remove$1(element2, transition.classes);
        }
      }
    };
    const transitionStart = bind(element2, transitionstart(), (e) => {
      if (isSourceTransition(e)) {
        transitionStart.unbind();
        transitionEnd.set(bind(element2, transitionend(), transitionDone));
        transitionCancel.set(bind(element2, transitioncancel(), transitionDone));
      }
    });
    const duration = getTransitionDuration(element2);
    requestAnimationFrame(() => {
      timer = setTimeout(transitionDone, duration + 17);
      set$9(element2, timerAttr, timer);
    });
  };
  const startTransitioning = (element2, transition) => {
    add$1(element2, transition.classes);
    getOpt(element2, timerAttr).each((timerId) => {
      clearTimeout(parseInt(timerId, 10));
      remove$7(element2, timerAttr);
    });
    setupTransitionListeners(element2, transition);
  };
  const applyTransitionCss = (element2, origin, position2, transition, decision, lastPlacement) => {
    const shouldTransition = shouldApplyTransitionCss(transition, decision, lastPlacement);
    if (shouldTransition || isTransitioning$1(element2, transition)) {
      set$8(element2, "position", position2.position);
      const rect2 = toBox(origin, element2);
      const intermediatePosition = reposition(origin, {
        ...decision,
        rect: rect2
      });
      const intermediateCssOptions = mapToObject(properties, (prop) => intermediatePosition[prop]);
      if (hasChanges(position2, intermediateCssOptions)) {
        setOptions(element2, intermediateCssOptions);
        if (shouldTransition) {
          startTransitioning(element2, transition);
        }
        reflow(element2);
      }
    } else {
      remove$1(element2, transition.classes);
    }
  };
  const elementSize = (p) => ({
    width: getOuter$1(p),
    height: getOuter$2(p)
  });
  const layout = (anchorBox, element2, bubbles, options) => {
    remove$6(element2, "max-height");
    remove$6(element2, "max-width");
    const elementBox = elementSize(element2);
    return attempts(element2, options.preference, anchorBox, elementBox, bubbles, options.bounds);
  };
  const setClasses = (element2, decision) => {
    const classInfo = decision.classes;
    remove$1(element2, classInfo.off);
    add$1(element2, classInfo.on);
  };
  const setHeight = (element2, decision, options) => {
    const maxHeightFunction = options.maxHeightFunction;
    maxHeightFunction(element2, decision.maxHeight);
  };
  const setWidth = (element2, decision, options) => {
    const maxWidthFunction = options.maxWidthFunction;
    maxWidthFunction(element2, decision.maxWidth);
  };
  const position$2 = (element2, decision, options) => {
    const positionCss = reposition(options.origin, decision);
    options.transition.each((transition) => {
      applyTransitionCss(element2, options.origin, positionCss, transition, decision, options.lastPlacement);
    });
    applyPositionCss(element2, positionCss);
  };
  const setPlacement = (element2, decision) => {
    setPlacement$1(element2, decision.placement);
  };
  const setMaxHeight = (element2, maxHeight) => {
    setMax$1(element2, Math.floor(maxHeight));
  };
  const anchored = constant$1((element2, available) => {
    setMaxHeight(element2, available);
    setAll(element2, {
      "overflow-x": "hidden",
      "overflow-y": "auto"
    });
  });
  const expandable$1 = constant$1((element2, available) => {
    setMaxHeight(element2, available);
  });
  const defaultOr = (options, key, dephault) => options[key] === void 0 ? dephault : options[key];
  const simple = (anchor2, element2, bubble, layouts2, lastPlacement, getBounds2, overrideOptions, transition) => {
    const maxHeightFunction = defaultOr(overrideOptions, "maxHeightFunction", anchored());
    const maxWidthFunction = defaultOr(overrideOptions, "maxWidthFunction", noop);
    const anchorBox = anchor2.anchorBox;
    const origin = anchor2.origin;
    const options = {
      bounds: viewport(origin, getBounds2),
      origin,
      preference: layouts2,
      maxHeightFunction,
      maxWidthFunction,
      lastPlacement,
      transition
    };
    return go(anchorBox, element2, bubble, options);
  };
  const go = (anchorBox, element2, bubble, options) => {
    const decision = layout(anchorBox, element2, bubble, options);
    position$2(element2, decision, options);
    setPlacement(element2, decision);
    setClasses(element2, decision);
    setHeight(element2, decision, options);
    setWidth(element2, decision, options);
    return {
      layout: decision.layout,
      placement: decision.placement
    };
  };
  const allAlignments = [
    "valignCentre",
    "alignLeft",
    "alignRight",
    "alignCentre",
    "top",
    "bottom",
    "left",
    "right",
    "inset"
  ];
  const nu$5 = (xOffset, yOffset, classes2, insetModifier = 1) => {
    const insetXOffset = xOffset * insetModifier;
    const insetYOffset = yOffset * insetModifier;
    const getClasses2 = (prop) => get$g(classes2, prop).getOr([]);
    const make2 = (xDelta, yDelta, alignmentsOn) => {
      const alignmentsOff = difference(allAlignments, alignmentsOn);
      return {
        offset: SugarPosition(xDelta, yDelta),
        classesOn: bind$3(alignmentsOn, getClasses2),
        classesOff: bind$3(alignmentsOff, getClasses2)
      };
    };
    return {
      southeast: () => make2(-xOffset, yOffset, [
        "top",
        "alignLeft"
      ]),
      southwest: () => make2(xOffset, yOffset, [
        "top",
        "alignRight"
      ]),
      south: () => make2(-xOffset / 2, yOffset, [
        "top",
        "alignCentre"
      ]),
      northeast: () => make2(-xOffset, -yOffset, [
        "bottom",
        "alignLeft"
      ]),
      northwest: () => make2(xOffset, -yOffset, [
        "bottom",
        "alignRight"
      ]),
      north: () => make2(-xOffset / 2, -yOffset, [
        "bottom",
        "alignCentre"
      ]),
      east: () => make2(xOffset, -yOffset / 2, [
        "valignCentre",
        "left"
      ]),
      west: () => make2(-xOffset, -yOffset / 2, [
        "valignCentre",
        "right"
      ]),
      insetNortheast: () => make2(insetXOffset, insetYOffset, [
        "top",
        "alignLeft",
        "inset"
      ]),
      insetNorthwest: () => make2(-insetXOffset, insetYOffset, [
        "top",
        "alignRight",
        "inset"
      ]),
      insetNorth: () => make2(-insetXOffset / 2, insetYOffset, [
        "top",
        "alignCentre",
        "inset"
      ]),
      insetSoutheast: () => make2(insetXOffset, -insetYOffset, [
        "bottom",
        "alignLeft",
        "inset"
      ]),
      insetSouthwest: () => make2(-insetXOffset, -insetYOffset, [
        "bottom",
        "alignRight",
        "inset"
      ]),
      insetSouth: () => make2(-insetXOffset / 2, -insetYOffset, [
        "bottom",
        "alignCentre",
        "inset"
      ]),
      insetEast: () => make2(-insetXOffset, -insetYOffset / 2, [
        "valignCentre",
        "right",
        "inset"
      ]),
      insetWest: () => make2(insetXOffset, -insetYOffset / 2, [
        "valignCentre",
        "left",
        "inset"
      ])
    };
  };
  const fallback = () => nu$5(0, 0, {});
  const nu$4 = identity;
  const onDirection = (isLtr, isRtl) => (element2) => getDirection(element2) === "rtl" ? isRtl : isLtr;
  const getDirection = (element2) => get$e(element2, "direction") === "rtl" ? "rtl" : "ltr";
  var AttributeValue;
  (function(AttributeValue2) {
    AttributeValue2["TopToBottom"] = "toptobottom";
    AttributeValue2["BottomToTop"] = "bottomtotop";
  })(AttributeValue || (AttributeValue = {}));
  const Attribute = "data-alloy-vertical-dir";
  const isBottomToTopDir = (el) => closest$2(el, (current) => isElement$1(current) && get$f(current, "data-alloy-vertical-dir") === AttributeValue.BottomToTop);
  const schema$y = () => optionObjOf("layouts", [
    required$1("onLtr"),
    required$1("onRtl"),
    option$3("onBottomLtr"),
    option$3("onBottomRtl")
  ]);
  const get$5 = (elem, info, defaultLtr, defaultRtl, defaultBottomLtr, defaultBottomRtl, dirElement) => {
    const isBottomToTop = dirElement.map(isBottomToTopDir).getOr(false);
    const customLtr = info.layouts.map((ls) => ls.onLtr(elem));
    const customRtl = info.layouts.map((ls) => ls.onRtl(elem));
    const ltr = isBottomToTop ? info.layouts.bind((ls) => ls.onBottomLtr.map((f2) => f2(elem))).or(customLtr).getOr(defaultBottomLtr) : customLtr.getOr(defaultLtr);
    const rtl = isBottomToTop ? info.layouts.bind((ls) => ls.onBottomRtl.map((f2) => f2(elem))).or(customRtl).getOr(defaultBottomRtl) : customRtl.getOr(defaultRtl);
    const f = onDirection(ltr, rtl);
    return f(elem);
  };
  const placement$4 = (component, anchorInfo, origin) => {
    const hotspot = anchorInfo.hotspot;
    const anchorBox = toBox(origin, hotspot.element);
    const layouts2 = get$5(component.element, anchorInfo, belowOrAbove(), belowOrAboveRtl(), aboveOrBelow(), aboveOrBelowRtl(), Optional.some(anchorInfo.hotspot.element));
    return Optional.some(nu$4({
      anchorBox,
      bubble: anchorInfo.bubble.getOr(fallback()),
      overrides: anchorInfo.overrides,
      layouts: layouts2,
      placer: Optional.none()
    }));
  };
  var HotspotAnchor = [
    required$1("hotspot"),
    option$3("bubble"),
    defaulted("overrides", {}),
    schema$y(),
    output$1("placement", placement$4)
  ];
  const placement$3 = (component, anchorInfo, origin) => {
    const pos = translate$2(origin, anchorInfo.x, anchorInfo.y);
    const anchorBox = bounds(pos.left, pos.top, anchorInfo.width, anchorInfo.height);
    const layouts2 = get$5(component.element, anchorInfo, all$1(), allRtl$1(), all$1(), allRtl$1(), Optional.none());
    return Optional.some(nu$4({
      anchorBox,
      bubble: anchorInfo.bubble,
      overrides: anchorInfo.overrides,
      layouts: layouts2,
      placer: Optional.none()
    }));
  };
  var MakeshiftAnchor = [
    required$1("x"),
    required$1("y"),
    defaulted("height", 0),
    defaulted("width", 0),
    defaulted("bubble", fallback()),
    defaulted("overrides", {}),
    schema$y(),
    output$1("placement", placement$3)
  ];
  const adt$7 = Adt.generate([
    { screen: ["point"] },
    {
      absolute: [
        "point",
        "scrollLeft",
        "scrollTop"
      ]
    }
  ]);
  const toFixed = (pos) => pos.fold(identity, (point2, scrollLeft, scrollTop) => point2.translate(-scrollLeft, -scrollTop));
  const toAbsolute = (pos) => pos.fold(identity, identity);
  const sum = (points) => foldl(points, (b2, a) => b2.translate(a.left, a.top), SugarPosition(0, 0));
  const sumAsFixed = (positions) => {
    const points = map$2(positions, toFixed);
    return sum(points);
  };
  const sumAsAbsolute = (positions) => {
    const points = map$2(positions, toAbsolute);
    return sum(points);
  };
  const screen = adt$7.screen;
  const absolute$1 = adt$7.absolute;
  const getOffset = (component, origin, anchorInfo) => {
    const win2 = defaultView(anchorInfo.root).dom;
    const hasSameOwner = (frame) => {
      const frameOwner = owner$4(frame);
      const compOwner = owner$4(component.element);
      return eq(frameOwner, compOwner);
    };
    return Optional.from(win2.frameElement).map(SugarElement.fromDom).filter(hasSameOwner).map(absolute$3);
  };
  const getRootPoint = (component, origin, anchorInfo) => {
    const doc = owner$4(component.element);
    const outerScroll = get$b(doc);
    const offset2 = getOffset(component, origin, anchorInfo).getOr(outerScroll);
    return absolute$1(offset2, outerScroll.left, outerScroll.top);
  };
  const getBox = (left2, top2, width2, height2) => {
    const point2 = screen(SugarPosition(left2, top2));
    return Optional.some(pointed(point2, width2, height2));
  };
  const calcNewAnchor = (optBox, rootPoint, anchorInfo, origin, elem) => optBox.map((box2) => {
    const points = [
      rootPoint,
      box2.point
    ];
    const topLeft = cata$1(origin, () => sumAsAbsolute(points), () => sumAsAbsolute(points), () => sumAsFixed(points));
    const anchorBox = rect(topLeft.left, topLeft.top, box2.width, box2.height);
    const layoutsLtr = anchorInfo.showAbove ? aboveOrBelow() : belowOrAbove();
    const layoutsRtl = anchorInfo.showAbove ? aboveOrBelowRtl() : belowOrAboveRtl();
    const layouts2 = get$5(elem, anchorInfo, layoutsLtr, layoutsRtl, layoutsLtr, layoutsRtl, Optional.none());
    return nu$4({
      anchorBox,
      bubble: anchorInfo.bubble.getOr(fallback()),
      overrides: anchorInfo.overrides,
      layouts: layouts2,
      placer: Optional.none()
    });
  });
  const placement$2 = (component, anchorInfo, origin) => {
    const rootPoint = getRootPoint(component, origin, anchorInfo);
    return anchorInfo.node.filter(inBody).bind((target) => {
      const rect2 = target.dom.getBoundingClientRect();
      const nodeBox = getBox(rect2.left, rect2.top, rect2.width, rect2.height);
      const elem = anchorInfo.node.getOr(component.element);
      return calcNewAnchor(nodeBox, rootPoint, anchorInfo, origin, elem);
    });
  };
  var NodeAnchor = [
    required$1("node"),
    required$1("root"),
    option$3("bubble"),
    schema$y(),
    defaulted("overrides", {}),
    defaulted("showAbove", false),
    output$1("placement", placement$2)
  ];
  const zeroWidth = "\uFEFF";
  const nbsp = "\xA0";
  const create$2 = (start, soffset, finish, foffset) => ({
    start,
    soffset,
    finish,
    foffset
  });
  const SimRange = { create: create$2 };
  const adt$6 = Adt.generate([
    { before: ["element"] },
    {
      on: [
        "element",
        "offset"
      ]
    },
    { after: ["element"] }
  ]);
  const cata = (subject, onBefore, onOn, onAfter) => subject.fold(onBefore, onOn, onAfter);
  const getStart$1 = (situ) => situ.fold(identity, identity, identity);
  const before = adt$6.before;
  const on$1 = adt$6.on;
  const after$1 = adt$6.after;
  const Situ = {
    before,
    on: on$1,
    after: after$1,
    cata,
    getStart: getStart$1
  };
  const adt$5 = Adt.generate([
    { domRange: ["rng"] },
    {
      relative: [
        "startSitu",
        "finishSitu"
      ]
    },
    {
      exact: [
        "start",
        "soffset",
        "finish",
        "foffset"
      ]
    }
  ]);
  const exactFromRange = (simRange) => adt$5.exact(simRange.start, simRange.soffset, simRange.finish, simRange.foffset);
  const getStart = (selection) => selection.match({
    domRange: (rng) => SugarElement.fromDom(rng.startContainer),
    relative: (startSitu, _finishSitu) => Situ.getStart(startSitu),
    exact: (start, _soffset, _finish, _foffset) => start
  });
  const domRange = adt$5.domRange;
  const relative = adt$5.relative;
  const exact = adt$5.exact;
  const getWin = (selection) => {
    const start = getStart(selection);
    return defaultView(start);
  };
  const range$1 = SimRange.create;
  const SimSelection = {
    domRange,
    relative,
    exact,
    exactFromRange,
    getWin,
    range: range$1
  };
  const setStart = (rng, situ) => {
    situ.fold((e) => {
      rng.setStartBefore(e.dom);
    }, (e, o) => {
      rng.setStart(e.dom, o);
    }, (e) => {
      rng.setStartAfter(e.dom);
    });
  };
  const setFinish = (rng, situ) => {
    situ.fold((e) => {
      rng.setEndBefore(e.dom);
    }, (e, o) => {
      rng.setEnd(e.dom, o);
    }, (e) => {
      rng.setEndAfter(e.dom);
    });
  };
  const relativeToNative = (win2, startSitu, finishSitu) => {
    const range2 = win2.document.createRange();
    setStart(range2, startSitu);
    setFinish(range2, finishSitu);
    return range2;
  };
  const exactToNative = (win2, start, soffset, finish, foffset) => {
    const rng = win2.document.createRange();
    rng.setStart(start.dom, soffset);
    rng.setEnd(finish.dom, foffset);
    return rng;
  };
  const toRect = (rect2) => ({
    left: rect2.left,
    top: rect2.top,
    right: rect2.right,
    bottom: rect2.bottom,
    width: rect2.width,
    height: rect2.height
  });
  const getFirstRect$1 = (rng) => {
    const rects = rng.getClientRects();
    const rect2 = rects.length > 0 ? rects[0] : rng.getBoundingClientRect();
    return rect2.width > 0 || rect2.height > 0 ? Optional.some(rect2).map(toRect) : Optional.none();
  };
  const getBounds$2 = (rng) => {
    const rect2 = rng.getBoundingClientRect();
    return rect2.width > 0 || rect2.height > 0 ? Optional.some(rect2).map(toRect) : Optional.none();
  };
  const adt$4 = Adt.generate([
    {
      ltr: [
        "start",
        "soffset",
        "finish",
        "foffset"
      ]
    },
    {
      rtl: [
        "start",
        "soffset",
        "finish",
        "foffset"
      ]
    }
  ]);
  const fromRange = (win2, type2, range2) => type2(SugarElement.fromDom(range2.startContainer), range2.startOffset, SugarElement.fromDom(range2.endContainer), range2.endOffset);
  const getRanges = (win2, selection) => selection.match({
    domRange: (rng) => {
      return {
        ltr: constant$1(rng),
        rtl: Optional.none
      };
    },
    relative: (startSitu, finishSitu) => {
      return {
        ltr: cached(() => relativeToNative(win2, startSitu, finishSitu)),
        rtl: cached(() => Optional.some(relativeToNative(win2, finishSitu, startSitu)))
      };
    },
    exact: (start, soffset, finish, foffset) => {
      return {
        ltr: cached(() => exactToNative(win2, start, soffset, finish, foffset)),
        rtl: cached(() => Optional.some(exactToNative(win2, finish, foffset, start, soffset)))
      };
    }
  });
  const doDiagnose = (win2, ranges) => {
    const rng = ranges.ltr();
    if (rng.collapsed) {
      const reversed = ranges.rtl().filter((rev) => rev.collapsed === false);
      return reversed.map((rev) => adt$4.rtl(SugarElement.fromDom(rev.endContainer), rev.endOffset, SugarElement.fromDom(rev.startContainer), rev.startOffset)).getOrThunk(() => fromRange(win2, adt$4.ltr, rng));
    } else {
      return fromRange(win2, adt$4.ltr, rng);
    }
  };
  const diagnose = (win2, selection) => {
    const ranges = getRanges(win2, selection);
    return doDiagnose(win2, ranges);
  };
  const asLtrRange = (win2, selection) => {
    const diagnosis = diagnose(win2, selection);
    return diagnosis.match({
      ltr: (start, soffset, finish, foffset) => {
        const rng = win2.document.createRange();
        rng.setStart(start.dom, soffset);
        rng.setEnd(finish.dom, foffset);
        return rng;
      },
      rtl: (start, soffset, finish, foffset) => {
        const rng = win2.document.createRange();
        rng.setStart(finish.dom, foffset);
        rng.setEnd(start.dom, soffset);
        return rng;
      }
    });
  };
  adt$4.ltr;
  adt$4.rtl;
  const descendants = (scope, selector) => all$3(selector, scope);
  const makeRange = (start, soffset, finish, foffset) => {
    const doc = owner$4(start);
    const rng = doc.dom.createRange();
    rng.setStart(start.dom, soffset);
    rng.setEnd(finish.dom, foffset);
    return rng;
  };
  const after = (start, soffset, finish, foffset) => {
    const r2 = makeRange(start, soffset, finish, foffset);
    const same = eq(start, finish) && soffset === foffset;
    return r2.collapsed && !same;
  };
  const getNativeSelection = (win2) => Optional.from(win2.getSelection());
  const readRange = (selection) => {
    if (selection.rangeCount > 0) {
      const firstRng = selection.getRangeAt(0);
      const lastRng = selection.getRangeAt(selection.rangeCount - 1);
      return Optional.some(SimRange.create(SugarElement.fromDom(firstRng.startContainer), firstRng.startOffset, SugarElement.fromDom(lastRng.endContainer), lastRng.endOffset));
    } else {
      return Optional.none();
    }
  };
  const doGetExact = (selection) => {
    if (selection.anchorNode === null || selection.focusNode === null) {
      return readRange(selection);
    } else {
      const anchor2 = SugarElement.fromDom(selection.anchorNode);
      const focus2 = SugarElement.fromDom(selection.focusNode);
      return after(anchor2, selection.anchorOffset, focus2, selection.focusOffset) ? Optional.some(SimRange.create(anchor2, selection.anchorOffset, focus2, selection.focusOffset)) : readRange(selection);
    }
  };
  const getExact = (win2) => getNativeSelection(win2).filter((sel) => sel.rangeCount > 0).bind(doGetExact);
  const getFirstRect = (win2, selection) => {
    const rng = asLtrRange(win2, selection);
    return getFirstRect$1(rng);
  };
  const getBounds$1 = (win2, selection) => {
    const rng = asLtrRange(win2, selection);
    return getBounds$2(rng);
  };
  const NodeValue = (is2, name2) => {
    const get2 = (element2) => {
      if (!is2(element2)) {
        throw new Error("Can only get " + name2 + " value of a " + name2 + " node");
      }
      return getOption(element2).getOr("");
    };
    const getOption = (element2) => is2(element2) ? Optional.from(element2.dom.nodeValue) : Optional.none();
    const set2 = (element2, value2) => {
      if (!is2(element2)) {
        throw new Error("Can only set raw " + name2 + " value of a " + name2 + " node");
      }
      element2.dom.nodeValue = value2;
    };
    return {
      get: get2,
      getOption,
      set: set2
    };
  };
  const api = NodeValue(isText, "text");
  const get$4 = (element2) => api.get(element2);
  const point = (element2, offset2) => ({
    element: element2,
    offset: offset2
  });
  const descendOnce$1 = (element2, offset2) => {
    const children$1 = children(element2);
    if (children$1.length === 0) {
      return point(element2, offset2);
    } else if (offset2 < children$1.length) {
      return point(children$1[offset2], 0);
    } else {
      const last2 = children$1[children$1.length - 1];
      const len = isText(last2) ? get$4(last2).length : children(last2).length;
      return point(last2, len);
    }
  };
  const descendOnce = (element2, offset2) => isText(element2) ? point(element2, offset2) : descendOnce$1(element2, offset2);
  const getAnchorSelection = (win2, anchorInfo) => {
    const getSelection = anchorInfo.getSelection.getOrThunk(() => () => getExact(win2));
    return getSelection().map((sel) => {
      const modStart = descendOnce(sel.start, sel.soffset);
      const modFinish = descendOnce(sel.finish, sel.foffset);
      return SimSelection.range(modStart.element, modStart.offset, modFinish.element, modFinish.offset);
    });
  };
  const placement$1 = (component, anchorInfo, origin) => {
    const win2 = defaultView(anchorInfo.root).dom;
    const rootPoint = getRootPoint(component, origin, anchorInfo);
    const selectionBox = getAnchorSelection(win2, anchorInfo).bind((sel) => {
      const optRect = getBounds$1(win2, SimSelection.exactFromRange(sel)).orThunk(() => {
        const x = SugarElement.fromText(zeroWidth);
        before$1(sel.start, x);
        const rect2 = getFirstRect(win2, SimSelection.exact(x, 0, x, 1));
        remove$5(x);
        return rect2;
      });
      return optRect.bind((rawRect) => getBox(rawRect.left, rawRect.top, rawRect.width, rawRect.height));
    });
    const targetElement = getAnchorSelection(win2, anchorInfo).bind((sel) => isElement$1(sel.start) ? Optional.some(sel.start) : parentElement(sel.start));
    const elem = targetElement.getOr(component.element);
    return calcNewAnchor(selectionBox, rootPoint, anchorInfo, origin, elem);
  };
  var SelectionAnchor = [
    option$3("getSelection"),
    required$1("root"),
    option$3("bubble"),
    schema$y(),
    defaulted("overrides", {}),
    defaulted("showAbove", false),
    output$1("placement", placement$1)
  ];
  const labelPrefix$1 = "link-layout";
  const eastX = (anchor2) => anchor2.x + anchor2.width;
  const westX = (anchor2, element2) => anchor2.x - element2.width;
  const northY$1 = (anchor2, element2) => anchor2.y - element2.height + anchor2.height;
  const southY$1 = (anchor2) => anchor2.y;
  const southeast$1 = (anchor2, element2, bubbles) => nu$6(eastX(anchor2), southY$1(anchor2), bubbles.southeast(), southeast$3(), "southeast", boundsRestriction(anchor2, {
    left: 0,
    top: 2
  }), labelPrefix$1);
  const southwest$1 = (anchor2, element2, bubbles) => nu$6(westX(anchor2, element2), southY$1(anchor2), bubbles.southwest(), southwest$3(), "southwest", boundsRestriction(anchor2, {
    right: 1,
    top: 2
  }), labelPrefix$1);
  const northeast$1 = (anchor2, element2, bubbles) => nu$6(eastX(anchor2), northY$1(anchor2, element2), bubbles.northeast(), northeast$3(), "northeast", boundsRestriction(anchor2, {
    left: 0,
    bottom: 3
  }), labelPrefix$1);
  const northwest$1 = (anchor2, element2, bubbles) => nu$6(westX(anchor2, element2), northY$1(anchor2, element2), bubbles.northwest(), northwest$3(), "northwest", boundsRestriction(anchor2, {
    right: 1,
    bottom: 3
  }), labelPrefix$1);
  const all = () => [
    southeast$1,
    southwest$1,
    northeast$1,
    northwest$1
  ];
  const allRtl = () => [
    southwest$1,
    southeast$1,
    northwest$1,
    northeast$1
  ];
  const placement = (component, submenuInfo, origin) => {
    const anchorBox = toBox(origin, submenuInfo.item.element);
    const layouts2 = get$5(component.element, submenuInfo, all(), allRtl(), all(), allRtl(), Optional.none());
    return Optional.some(nu$4({
      anchorBox,
      bubble: fallback(),
      overrides: submenuInfo.overrides,
      layouts: layouts2,
      placer: Optional.none()
    }));
  };
  var SubmenuAnchor = [
    required$1("item"),
    schema$y(),
    defaulted("overrides", {}),
    output$1("placement", placement)
  ];
  var AnchorSchema = choose$1("type", {
    selection: SelectionAnchor,
    node: NodeAnchor,
    hotspot: HotspotAnchor,
    submenu: SubmenuAnchor,
    makeshift: MakeshiftAnchor
  });
  const TransitionSchema = [
    requiredArrayOf("classes", string),
    defaultedStringEnum("mode", "all", [
      "all",
      "layout",
      "placement"
    ])
  ];
  const PositionSchema = [
    defaulted("useFixed", never),
    option$3("getBounds")
  ];
  const PlacementSchema = [
    requiredOf("anchor", AnchorSchema),
    optionObjOf("transition", TransitionSchema)
  ];
  const getFixedOrigin = () => {
    const html = document.documentElement;
    return fixed$1(0, 0, html.clientWidth, html.clientHeight);
  };
  const getRelativeOrigin = (component) => {
    const position2 = absolute$3(component.element);
    const bounds2 = component.element.dom.getBoundingClientRect();
    return relative$1(position2.left, position2.top, bounds2.width, bounds2.height);
  };
  const place = (component, origin, anchoring, getBounds2, placee, lastPlace, transition) => {
    const anchor2 = box(anchoring.anchorBox, origin);
    return simple(anchor2, placee.element, anchoring.bubble, anchoring.layouts, lastPlace, getBounds2, anchoring.overrides, transition);
  };
  const position$1 = (component, posConfig, posState, placee, placementSpec) => {
    positionWithin(component, posConfig, posState, placee, placementSpec, Optional.none());
  };
  const positionWithin = (component, posConfig, posState, placee, placementSpec, boxElement) => {
    const boundsBox = boxElement.map(box$1);
    return positionWithinBounds(component, posConfig, posState, placee, placementSpec, boundsBox);
  };
  const positionWithinBounds = (component, posConfig, posState, placee, placementSpec, bounds2) => {
    const placeeDetail = asRawOrDie$1("placement.info", objOf(PlacementSchema), placementSpec);
    const anchorage = placeeDetail.anchor;
    const element2 = placee.element;
    const placeeState = posState.get(placee.uid);
    preserve$1(() => {
      set$8(element2, "position", "fixed");
      const oldVisibility = getRaw(element2, "visibility");
      set$8(element2, "visibility", "hidden");
      const origin = posConfig.useFixed() ? getFixedOrigin() : getRelativeOrigin(component);
      const placer = anchorage.placement;
      const getBounds2 = bounds2.map(constant$1).or(posConfig.getBounds);
      placer(component, anchorage, origin).each((anchoring) => {
        const doPlace = anchoring.placer.getOr(place);
        const newState = doPlace(component, origin, anchoring, getBounds2, placee, placeeState, placeeDetail.transition);
        posState.set(placee.uid, newState);
      });
      oldVisibility.fold(() => {
        remove$6(element2, "visibility");
      }, (vis) => {
        set$8(element2, "visibility", vis);
      });
      if (getRaw(element2, "left").isNone() && getRaw(element2, "top").isNone() && getRaw(element2, "right").isNone() && getRaw(element2, "bottom").isNone() && is$1(getRaw(element2, "position"), "fixed")) {
        remove$6(element2, "position");
      }
    }, element2);
  };
  const getMode = (component, pConfig, _pState) => pConfig.useFixed() ? "fixed" : "absolute";
  const reset$1 = (component, pConfig, posState, placee) => {
    const element2 = placee.element;
    each$1([
      "position",
      "left",
      "right",
      "top",
      "bottom"
    ], (prop) => remove$6(element2, prop));
    reset$2(element2);
    posState.clear(placee.uid);
  };
  var PositionApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    position: position$1,
    positionWithin,
    positionWithinBounds,
    getMode,
    reset: reset$1
  });
  const init$g = () => {
    let state = {};
    const set2 = (id, data) => {
      state[id] = data;
    };
    const get2 = (id) => get$g(state, id);
    const clear2 = (id) => {
      if (isNonNullable(id)) {
        delete state[id];
      } else {
        state = {};
      }
    };
    return nu$8({
      readState: () => state,
      clear: clear2,
      set: set2,
      get: get2
    });
  };
  var PositioningState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$g
  });
  const Positioning = create$3({
    fields: PositionSchema,
    name: "positioning",
    active: ActivePosition,
    apis: PositionApis,
    state: PositioningState
  });
  const isConnected = (comp) => comp.getSystem().isConnected();
  const fireDetaching = (component) => {
    emit(component, detachedFromDom());
    const children2 = component.components();
    each$1(children2, fireDetaching);
  };
  const fireAttaching = (component) => {
    const children2 = component.components();
    each$1(children2, fireAttaching);
    emit(component, attachedToDom());
  };
  const virtualAttach = (parent2, child2) => {
    parent2.getSystem().addToWorld(child2);
    if (inBody(parent2.element)) {
      fireAttaching(child2);
    }
  };
  const virtualDetach = (comp) => {
    fireDetaching(comp);
    comp.getSystem().removeFromWorld(comp);
  };
  const attach$1 = (parent2, child2) => {
    append$2(parent2.element, child2.element);
  };
  const detachChildren$1 = (component) => {
    each$1(component.components(), (childComp) => remove$5(childComp.element));
    empty(component.element);
    component.syncComponents();
  };
  const replaceChildren = (component, newSpecs, buildNewChildren) => {
    const subs2 = component.components();
    detachChildren$1(component);
    const newChildren = buildNewChildren(newSpecs);
    const deleted = difference(subs2, newChildren);
    each$1(deleted, (comp) => {
      fireDetaching(comp);
      component.getSystem().removeFromWorld(comp);
    });
    each$1(newChildren, (childComp) => {
      if (!isConnected(childComp)) {
        component.getSystem().addToWorld(childComp);
        attach$1(component, childComp);
        if (inBody(component.element)) {
          fireAttaching(childComp);
        }
      } else {
        attach$1(component, childComp);
      }
    });
    component.syncComponents();
  };
  const virtualReplaceChildren = (component, newSpecs, buildNewChildren) => {
    const subs2 = component.components();
    const existingComps = bind$3(newSpecs, (spec) => getPremade(spec).toArray());
    each$1(subs2, (childComp) => {
      if (!contains$2(existingComps, childComp)) {
        virtualDetach(childComp);
      }
    });
    const newChildren = buildNewChildren(newSpecs);
    const deleted = difference(subs2, newChildren);
    each$1(deleted, (deletedComp) => {
      if (isConnected(deletedComp)) {
        virtualDetach(deletedComp);
      }
    });
    each$1(newChildren, (childComp) => {
      if (!isConnected(childComp)) {
        virtualAttach(component, childComp);
      }
    });
    component.syncComponents();
  };
  const attach = (parent2, child2) => {
    attachWith(parent2, child2, append$2);
  };
  const attachWith = (parent2, child2, insertion) => {
    parent2.getSystem().addToWorld(child2);
    insertion(parent2.element, child2.element);
    if (inBody(parent2.element)) {
      fireAttaching(child2);
    }
    parent2.syncComponents();
  };
  const doDetach = (component) => {
    fireDetaching(component);
    remove$5(component.element);
    component.getSystem().removeFromWorld(component);
  };
  const detach = (component) => {
    const parent$1 = parent(component.element).bind((p) => component.getSystem().getByDom(p).toOptional());
    doDetach(component);
    parent$1.each((p) => {
      p.syncComponents();
    });
  };
  const detachChildren = (component) => {
    const subs2 = component.components();
    each$1(subs2, doDetach);
    empty(component.element);
    component.syncComponents();
  };
  const attachSystem = (element2, guiSystem) => {
    attachSystemWith(element2, guiSystem, append$2);
  };
  const attachSystemAfter = (element2, guiSystem) => {
    attachSystemWith(element2, guiSystem, after$2);
  };
  const attachSystemWith = (element2, guiSystem, inserter) => {
    inserter(element2, guiSystem.element);
    const children$1 = children(guiSystem.element);
    each$1(children$1, (child2) => {
      guiSystem.getByDom(child2).each(fireAttaching);
    });
  };
  const detachSystem = (guiSystem) => {
    const children$1 = children(guiSystem.element);
    each$1(children$1, (child2) => {
      guiSystem.getByDom(child2).each(fireDetaching);
    });
    remove$5(guiSystem.element);
  };
  const rebuild = (sandbox, sConfig, sState, data) => {
    sState.get().each((_data) => {
      detachChildren(sandbox);
    });
    const point2 = sConfig.getAttachPoint(sandbox);
    attach(point2, sandbox);
    const built = sandbox.getSystem().build(data);
    attach(sandbox, built);
    sState.set(built);
    return built;
  };
  const open$1 = (sandbox, sConfig, sState, data) => {
    const newState = rebuild(sandbox, sConfig, sState, data);
    sConfig.onOpen(sandbox, newState);
    return newState;
  };
  const setContent = (sandbox, sConfig, sState, data) => sState.get().map(() => rebuild(sandbox, sConfig, sState, data));
  const openWhileCloaked = (sandbox, sConfig, sState, data, transaction) => {
    cloak(sandbox, sConfig);
    open$1(sandbox, sConfig, sState, data);
    transaction();
    decloak(sandbox, sConfig);
  };
  const close$1 = (sandbox, sConfig, sState) => {
    sState.get().each((data) => {
      detachChildren(sandbox);
      detach(sandbox);
      sConfig.onClose(sandbox, data);
      sState.clear();
    });
  };
  const isOpen$1 = (_sandbox, _sConfig, sState) => sState.isOpen();
  const isPartOf = (sandbox, sConfig, sState, queryElem) => isOpen$1(sandbox, sConfig, sState) && sState.get().exists((data) => sConfig.isPartOf(sandbox, data, queryElem));
  const getState$2 = (_sandbox, _sConfig, sState) => sState.get();
  const store = (sandbox, cssKey, attr, newValue) => {
    getRaw(sandbox.element, cssKey).fold(() => {
      remove$7(sandbox.element, attr);
    }, (v) => {
      set$9(sandbox.element, attr, v);
    });
    set$8(sandbox.element, cssKey, newValue);
  };
  const restore = (sandbox, cssKey, attr) => {
    getOpt(sandbox.element, attr).fold(() => remove$6(sandbox.element, cssKey), (oldValue) => set$8(sandbox.element, cssKey, oldValue));
  };
  const cloak = (sandbox, sConfig, _sState) => {
    const sink = sConfig.getAttachPoint(sandbox);
    set$8(sandbox.element, "position", Positioning.getMode(sink));
    store(sandbox, "visibility", sConfig.cloakVisibilityAttr, "hidden");
  };
  const hasPosition = (element2) => exists([
    "top",
    "left",
    "right",
    "bottom"
  ], (pos) => getRaw(element2, pos).isSome());
  const decloak = (sandbox, sConfig, _sState) => {
    if (!hasPosition(sandbox.element)) {
      remove$6(sandbox.element, "position");
    }
    restore(sandbox, "visibility", sConfig.cloakVisibilityAttr);
  };
  var SandboxApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    cloak,
    decloak,
    open: open$1,
    openWhileCloaked,
    close: close$1,
    isOpen: isOpen$1,
    isPartOf,
    getState: getState$2,
    setContent
  });
  const events$g = (sandboxConfig, sandboxState) => derive$2([run$1(sandboxClose(), (sandbox, _simulatedEvent) => {
    close$1(sandbox, sandboxConfig, sandboxState);
  })]);
  var ActiveSandbox = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$g
  });
  var SandboxSchema = [
    onHandler("onOpen"),
    onHandler("onClose"),
    required$1("isPartOf"),
    required$1("getAttachPoint"),
    defaulted("cloakVisibilityAttr", "data-precloak-visibility")
  ];
  const init$f = () => {
    const contents2 = value$2();
    const readState = constant$1("not-implemented");
    return nu$8({
      readState,
      isOpen: contents2.isSet,
      clear: contents2.clear,
      set: contents2.set,
      get: contents2.get
    });
  };
  var SandboxState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$f
  });
  const Sandboxing = create$3({
    fields: SandboxSchema,
    name: "sandboxing",
    active: ActiveSandbox,
    apis: SandboxApis,
    state: SandboxState
  });
  const dismissPopups = constant$1("dismiss.popups");
  const repositionPopups = constant$1("reposition.popups");
  const mouseReleased = constant$1("mouse.released");
  const schema$x = objOfOnly([
    defaulted("isExtraPart", never),
    optionObjOf("fireEventInstead", [defaulted("event", dismissRequested())])
  ]);
  const receivingChannel$1 = (rawSpec) => {
    const detail = asRawOrDie$1("Dismissal", schema$x, rawSpec);
    return {
      [dismissPopups()]: {
        schema: objOfOnly([required$1("target")]),
        onReceive: (sandbox, data) => {
          if (Sandboxing.isOpen(sandbox)) {
            const isPart = Sandboxing.isPartOf(sandbox, data.target) || detail.isExtraPart(sandbox, data.target);
            if (!isPart) {
              detail.fireEventInstead.fold(() => Sandboxing.close(sandbox), (fe) => emit(sandbox, fe.event));
            }
          }
        }
      }
    };
  };
  const schema$w = objOfOnly([
    optionObjOf("fireEventInstead", [defaulted("event", repositionRequested())]),
    requiredFunction("doReposition")
  ]);
  const receivingChannel = (rawSpec) => {
    const detail = asRawOrDie$1("Reposition", schema$w, rawSpec);
    return {
      [repositionPopups()]: {
        onReceive: (sandbox) => {
          if (Sandboxing.isOpen(sandbox)) {
            detail.fireEventInstead.fold(() => detail.doReposition(sandbox), (fe) => emit(sandbox, fe.event));
          }
        }
      }
    };
  };
  const onLoad$5 = (component, repConfig, repState) => {
    repConfig.store.manager.onLoad(component, repConfig, repState);
  };
  const onUnload$2 = (component, repConfig, repState) => {
    repConfig.store.manager.onUnload(component, repConfig, repState);
  };
  const setValue$3 = (component, repConfig, repState, data) => {
    repConfig.store.manager.setValue(component, repConfig, repState, data);
  };
  const getValue$3 = (component, repConfig, repState) => repConfig.store.manager.getValue(component, repConfig, repState);
  const getState$1 = (component, repConfig, repState) => repState;
  var RepresentApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    onLoad: onLoad$5,
    onUnload: onUnload$2,
    setValue: setValue$3,
    getValue: getValue$3,
    getState: getState$1
  });
  const events$f = (repConfig, repState) => {
    const es = repConfig.resetOnDom ? [
      runOnAttached((comp, _se) => {
        onLoad$5(comp, repConfig, repState);
      }),
      runOnDetached((comp, _se) => {
        onUnload$2(comp, repConfig, repState);
      })
    ] : [loadEvent(repConfig, repState, onLoad$5)];
    return derive$2(es);
  };
  var ActiveRepresenting = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$f
  });
  const memory$1 = () => {
    const data = Cell(null);
    const readState = () => ({
      mode: "memory",
      value: data.get()
    });
    const isNotSet = () => data.get() === null;
    const clear2 = () => {
      data.set(null);
    };
    return nu$8({
      set: data.set,
      get: data.get,
      isNotSet,
      clear: clear2,
      readState
    });
  };
  const manual = () => {
    const readState = noop;
    return nu$8({ readState });
  };
  const dataset = () => {
    const dataByValue = Cell({});
    const dataByText = Cell({});
    const readState = () => ({
      mode: "dataset",
      dataByValue: dataByValue.get(),
      dataByText: dataByText.get()
    });
    const clear2 = () => {
      dataByValue.set({});
      dataByText.set({});
    };
    const lookup2 = (itemString) => get$g(dataByValue.get(), itemString).orThunk(() => get$g(dataByText.get(), itemString));
    const update = (items) => {
      const currentDataByValue = dataByValue.get();
      const currentDataByText = dataByText.get();
      const newDataByValue = {};
      const newDataByText = {};
      each$1(items, (item2) => {
        newDataByValue[item2.value] = item2;
        get$g(item2, "meta").each((meta) => {
          get$g(meta, "text").each((text2) => {
            newDataByText[text2] = item2;
          });
        });
      });
      dataByValue.set({
        ...currentDataByValue,
        ...newDataByValue
      });
      dataByText.set({
        ...currentDataByText,
        ...newDataByText
      });
    };
    return nu$8({
      readState,
      lookup: lookup2,
      update,
      clear: clear2
    });
  };
  const init$e = (spec) => spec.store.manager.state(spec);
  var RepresentState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    memory: memory$1,
    dataset,
    manual,
    init: init$e
  });
  const setValue$2 = (component, repConfig, repState, data) => {
    const store2 = repConfig.store;
    repState.update([data]);
    store2.setValue(component, data);
    repConfig.onSetValue(component, data);
  };
  const getValue$2 = (component, repConfig, repState) => {
    const store2 = repConfig.store;
    const key = store2.getDataKey(component);
    return repState.lookup(key).getOrThunk(() => store2.getFallbackEntry(key));
  };
  const onLoad$4 = (component, repConfig, repState) => {
    const store2 = repConfig.store;
    store2.initialValue.each((data) => {
      setValue$2(component, repConfig, repState, data);
    });
  };
  const onUnload$1 = (component, repConfig, repState) => {
    repState.clear();
  };
  var DatasetStore = [
    option$3("initialValue"),
    required$1("getFallbackEntry"),
    required$1("getDataKey"),
    required$1("setValue"),
    output$1("manager", {
      setValue: setValue$2,
      getValue: getValue$2,
      onLoad: onLoad$4,
      onUnload: onUnload$1,
      state: dataset
    })
  ];
  const getValue$1 = (component, repConfig, _repState) => repConfig.store.getValue(component);
  const setValue$1 = (component, repConfig, _repState, data) => {
    repConfig.store.setValue(component, data);
    repConfig.onSetValue(component, data);
  };
  const onLoad$3 = (component, repConfig, _repState) => {
    repConfig.store.initialValue.each((data) => {
      repConfig.store.setValue(component, data);
    });
  };
  var ManualStore = [
    required$1("getValue"),
    defaulted("setValue", noop),
    option$3("initialValue"),
    output$1("manager", {
      setValue: setValue$1,
      getValue: getValue$1,
      onLoad: onLoad$3,
      onUnload: noop,
      state: NoState.init
    })
  ];
  const setValue = (component, repConfig, repState, data) => {
    repState.set(data);
    repConfig.onSetValue(component, data);
  };
  const getValue = (component, repConfig, repState) => repState.get();
  const onLoad$2 = (component, repConfig, repState) => {
    repConfig.store.initialValue.each((initVal) => {
      if (repState.isNotSet()) {
        repState.set(initVal);
      }
    });
  };
  const onUnload = (component, repConfig, repState) => {
    repState.clear();
  };
  var MemoryStore = [
    option$3("initialValue"),
    output$1("manager", {
      setValue,
      getValue,
      onLoad: onLoad$2,
      onUnload,
      state: memory$1
    })
  ];
  var RepresentSchema = [
    defaultedOf("store", { mode: "memory" }, choose$1("mode", {
      memory: MemoryStore,
      manual: ManualStore,
      dataset: DatasetStore
    })),
    onHandler("onSetValue"),
    defaulted("resetOnDom", false)
  ];
  const Representing = create$3({
    fields: RepresentSchema,
    name: "representing",
    active: ActiveRepresenting,
    apis: RepresentApis,
    extra: {
      setValueFrom: (component, source) => {
        const value2 = Representing.getValue(source);
        Representing.setValue(component, value2);
      }
    },
    state: RepresentState
  });
  const field = (name2, forbidden) => defaultedObjOf(name2, {}, map$2(forbidden, (f) => forbid(f.name(), "Cannot configure " + f.name() + " for " + name2)).concat([customField("dump", identity)]));
  const get$3 = (data) => data.dump;
  const augment = (data, original2) => ({
    ...derive$1(original2),
    ...data.dump
  });
  const SketchBehaviours = {
    field,
    augment,
    get: get$3
  };
  const _placeholder = "placeholder";
  const adt$3 = Adt.generate([
    {
      single: [
        "required",
        "valueThunk"
      ]
    },
    {
      multiple: [
        "required",
        "valueThunks"
      ]
    }
  ]);
  const isSubstituted = (spec) => has$2(spec, "uiType");
  const subPlaceholder = (owner2, detail, compSpec, placeholders) => {
    if (owner2.exists((o) => o !== compSpec.owner)) {
      return adt$3.single(true, constant$1(compSpec));
    }
    return get$g(placeholders, compSpec.name).fold(() => {
      throw new Error("Unknown placeholder component: " + compSpec.name + "\nKnown: [" + keys(placeholders) + "]\nNamespace: " + owner2.getOr("none") + "\nSpec: " + JSON.stringify(compSpec, null, 2));
    }, (newSpec) => newSpec.replace());
  };
  const scan = (owner2, detail, compSpec, placeholders) => {
    if (isSubstituted(compSpec) && compSpec.uiType === _placeholder) {
      return subPlaceholder(owner2, detail, compSpec, placeholders);
    } else {
      return adt$3.single(false, constant$1(compSpec));
    }
  };
  const substitute = (owner2, detail, compSpec, placeholders) => {
    const base2 = scan(owner2, detail, compSpec, placeholders);
    return base2.fold((req, valueThunk2) => {
      const value2 = isSubstituted(compSpec) ? valueThunk2(detail, compSpec.config, compSpec.validated) : valueThunk2(detail);
      const childSpecs = get$g(value2, "components").getOr([]);
      const substituted = bind$3(childSpecs, (c) => substitute(owner2, detail, c, placeholders));
      return [{
        ...value2,
        components: substituted
      }];
    }, (req, valuesThunk) => {
      if (isSubstituted(compSpec)) {
        const values2 = valuesThunk(detail, compSpec.config, compSpec.validated);
        const preprocessor = compSpec.validated.preprocess.getOr(identity);
        return preprocessor(values2);
      } else {
        return valuesThunk(detail);
      }
    });
  };
  const substituteAll = (owner2, detail, components2, placeholders) => bind$3(components2, (c) => substitute(owner2, detail, c, placeholders));
  const oneReplace = (label2, replacements) => {
    let called = false;
    const used = () => called;
    const replace2 = () => {
      if (called) {
        throw new Error("Trying to use the same placeholder more than once: " + label2);
      }
      called = true;
      return replacements;
    };
    const required2 = () => replacements.fold((req, _) => req, (req, _) => req);
    return {
      name: constant$1(label2),
      required: required2,
      used,
      replace: replace2
    };
  };
  const substitutePlaces = (owner2, detail, components2, placeholders) => {
    const ps = map$1(placeholders, (ph, name2) => oneReplace(name2, ph));
    const outcome = substituteAll(owner2, detail, components2, ps);
    each(ps, (p) => {
      if (p.used() === false && p.required()) {
        throw new Error("Placeholder: " + p.name() + " was not found in components list\nNamespace: " + owner2.getOr("none") + "\nComponents: " + JSON.stringify(detail.components, null, 2));
      }
    });
    return outcome;
  };
  const single$2 = adt$3.single;
  const multiple = adt$3.multiple;
  const placeholder = constant$1(_placeholder);
  const adt$2 = Adt.generate([
    { required: ["data"] },
    { external: ["data"] },
    { optional: ["data"] },
    { group: ["data"] }
  ]);
  const fFactory = defaulted("factory", { sketch: identity });
  const fSchema = defaulted("schema", []);
  const fName = required$1("name");
  const fPname = field$1("pname", "pname", defaultedThunk((typeSpec) => "<alloy." + generate$6(typeSpec.name) + ">"), anyValue());
  const fGroupSchema = customField("schema", () => [option$3("preprocess")]);
  const fDefaults = defaulted("defaults", constant$1({}));
  const fOverrides = defaulted("overrides", constant$1({}));
  const requiredSpec = objOf([
    fFactory,
    fSchema,
    fName,
    fPname,
    fDefaults,
    fOverrides
  ]);
  const externalSpec = objOf([
    fFactory,
    fSchema,
    fName,
    fDefaults,
    fOverrides
  ]);
  const optionalSpec = objOf([
    fFactory,
    fSchema,
    fName,
    fPname,
    fDefaults,
    fOverrides
  ]);
  const groupSpec = objOf([
    fFactory,
    fGroupSchema,
    fName,
    required$1("unit"),
    fPname,
    fDefaults,
    fOverrides
  ]);
  const asNamedPart = (part2) => {
    return part2.fold(Optional.some, Optional.none, Optional.some, Optional.some);
  };
  const name$2 = (part2) => {
    const get2 = (data) => data.name;
    return part2.fold(get2, get2, get2, get2);
  };
  const asCommon = (part2) => {
    return part2.fold(identity, identity, identity, identity);
  };
  const convert = (adtConstructor, partSchema) => (spec) => {
    const data = asRawOrDie$1("Converting part type", partSchema, spec);
    return adtConstructor(data);
  };
  const required = convert(adt$2.required, requiredSpec);
  const external = convert(adt$2.external, externalSpec);
  const optional = convert(adt$2.optional, optionalSpec);
  const group = convert(adt$2.group, groupSpec);
  const original = constant$1("entirety");
  var PartType = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    required,
    external,
    optional,
    group,
    asNamedPart,
    name: name$2,
    asCommon,
    original
  });
  const combine = (detail, data, partSpec, partValidated) => deepMerge(data.defaults(detail, partSpec, partValidated), partSpec, { uid: detail.partUids[data.name] }, data.overrides(detail, partSpec, partValidated));
  const subs = (owner2, detail, parts2) => {
    const internals = {};
    const externals = {};
    each$1(parts2, (part2) => {
      part2.fold((data) => {
        internals[data.pname] = single$2(true, (detail2, partSpec, partValidated) => data.factory.sketch(combine(detail2, data, partSpec, partValidated)));
      }, (data) => {
        const partSpec = detail.parts[data.name];
        externals[data.name] = constant$1(data.factory.sketch(combine(detail, data, partSpec[original()]), partSpec));
      }, (data) => {
        internals[data.pname] = single$2(false, (detail2, partSpec, partValidated) => data.factory.sketch(combine(detail2, data, partSpec, partValidated)));
      }, (data) => {
        internals[data.pname] = multiple(true, (detail2, _partSpec, _partValidated) => {
          const units2 = detail2[data.name];
          return map$2(units2, (u) => data.factory.sketch(deepMerge(data.defaults(detail2, u, _partValidated), u, data.overrides(detail2, u))));
        });
      });
    });
    return {
      internals: constant$1(internals),
      externals: constant$1(externals)
    };
  };
  const generate$3 = (owner2, parts2) => {
    const r2 = {};
    each$1(parts2, (part2) => {
      asNamedPart(part2).each((np) => {
        const g = doGenerateOne(owner2, np.pname);
        r2[np.name] = (config2) => {
          const validated = asRawOrDie$1("Part: " + np.name + " in " + owner2, objOf(np.schema), config2);
          return {
            ...g,
            config: config2,
            validated
          };
        };
      });
    });
    return r2;
  };
  const doGenerateOne = (owner2, pname) => ({
    uiType: placeholder(),
    owner: owner2,
    name: pname
  });
  const generateOne$1 = (owner2, pname, config2) => ({
    uiType: placeholder(),
    owner: owner2,
    name: pname,
    config: config2,
    validated: {}
  });
  const schemas = (parts2) => bind$3(parts2, (part2) => part2.fold(Optional.none, Optional.some, Optional.none, Optional.none).map((data) => requiredObjOf(data.name, data.schema.concat([snapshot(original())]))).toArray());
  const names = (parts2) => map$2(parts2, name$2);
  const substitutes = (owner2, detail, parts2) => subs(owner2, detail, parts2);
  const components$1 = (owner2, detail, internals) => substitutePlaces(Optional.some(owner2), detail, detail.components, internals);
  const getPart = (component, detail, partKey) => {
    const uid = detail.partUids[partKey];
    return component.getSystem().getByUid(uid).toOptional();
  };
  const getPartOrDie = (component, detail, partKey) => getPart(component, detail, partKey).getOrDie("Could not find part: " + partKey);
  const getParts = (component, detail, partKeys) => {
    const r2 = {};
    const uids2 = detail.partUids;
    const system = component.getSystem();
    each$1(partKeys, (pk) => {
      r2[pk] = constant$1(system.getByUid(uids2[pk]));
    });
    return r2;
  };
  const getAllParts = (component, detail) => {
    const system = component.getSystem();
    return map$1(detail.partUids, (pUid, _k) => constant$1(system.getByUid(pUid)));
  };
  const getAllPartNames = (detail) => keys(detail.partUids);
  const getPartsOrDie = (component, detail, partKeys) => {
    const r2 = {};
    const uids2 = detail.partUids;
    const system = component.getSystem();
    each$1(partKeys, (pk) => {
      r2[pk] = constant$1(system.getByUid(uids2[pk]).getOrDie());
    });
    return r2;
  };
  const defaultUids = (baseUid, partTypes) => {
    const partNames = names(partTypes);
    return wrapAll(map$2(partNames, (pn) => ({
      key: pn,
      value: baseUid + "-" + pn
    })));
  };
  const defaultUidsSchema = (partTypes) => field$1("partUids", "partUids", mergeWithThunk((spec) => defaultUids(spec.uid, partTypes)), anyValue());
  var AlloyParts = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    generate: generate$3,
    generateOne: generateOne$1,
    schemas,
    names,
    substitutes,
    components: components$1,
    defaultUids,
    defaultUidsSchema,
    getAllParts,
    getAllPartNames,
    getPart,
    getPartOrDie,
    getParts,
    getPartsOrDie
  });
  const base = (partSchemas, partUidsSchemas) => {
    const ps = partSchemas.length > 0 ? [requiredObjOf("parts", partSchemas)] : [];
    return ps.concat([
      required$1("uid"),
      defaulted("dom", {}),
      defaulted("components", []),
      snapshot("originalSpec"),
      defaulted("debug.sketcher", {})
    ]).concat(partUidsSchemas);
  };
  const asRawOrDie = (label2, schema2, spec, partSchemas, partUidsSchemas) => {
    const baseS = base(partSchemas, partUidsSchemas);
    return asRawOrDie$1(label2 + " [SpecSchema]", objOfOnly(baseS.concat(schema2)), spec);
  };
  const single$1 = (owner2, schema2, factory2, spec) => {
    const specWithUid = supplyUid(spec);
    const detail = asRawOrDie(owner2, schema2, specWithUid, [], []);
    return factory2(detail, specWithUid);
  };
  const composite$1 = (owner2, schema2, partTypes, factory2, spec) => {
    const specWithUid = supplyUid(spec);
    const partSchemas = schemas(partTypes);
    const partUidsSchema = defaultUidsSchema(partTypes);
    const detail = asRawOrDie(owner2, schema2, specWithUid, partSchemas, [partUidsSchema]);
    const subs2 = substitutes(owner2, detail, partTypes);
    const components2 = components$1(owner2, detail, subs2.internals());
    return factory2(detail, components2, specWithUid, subs2.externals());
  };
  const hasUid = (spec) => has$2(spec, "uid");
  const supplyUid = (spec) => {
    return hasUid(spec) ? spec : {
      ...spec,
      uid: generate$5("uid")
    };
  };
  const isSketchSpec = (spec) => {
    return spec.uid !== void 0;
  };
  const singleSchema = objOfOnly([
    required$1("name"),
    required$1("factory"),
    required$1("configFields"),
    defaulted("apis", {}),
    defaulted("extraApis", {})
  ]);
  const compositeSchema = objOfOnly([
    required$1("name"),
    required$1("factory"),
    required$1("configFields"),
    required$1("partFields"),
    defaulted("apis", {}),
    defaulted("extraApis", {})
  ]);
  const single = (rawConfig) => {
    const config2 = asRawOrDie$1("Sketcher for " + rawConfig.name, singleSchema, rawConfig);
    const sketch2 = (spec) => single$1(config2.name, config2.configFields, config2.factory, spec);
    const apis = map$1(config2.apis, makeApi);
    const extraApis = map$1(config2.extraApis, (f, k) => markAsExtraApi(f, k));
    return {
      name: config2.name,
      configFields: config2.configFields,
      sketch: sketch2,
      ...apis,
      ...extraApis
    };
  };
  const composite = (rawConfig) => {
    const config2 = asRawOrDie$1("Sketcher for " + rawConfig.name, compositeSchema, rawConfig);
    const sketch2 = (spec) => composite$1(config2.name, config2.configFields, config2.partFields, config2.factory, spec);
    const parts2 = generate$3(config2.name, config2.partFields);
    const apis = map$1(config2.apis, makeApi);
    const extraApis = map$1(config2.extraApis, (f, k) => markAsExtraApi(f, k));
    return {
      name: config2.name,
      partFields: config2.partFields,
      configFields: config2.configFields,
      sketch: sketch2,
      parts: parts2,
      ...apis,
      ...extraApis
    };
  };
  const inside = (target) => isTag("input")(target) && get$f(target, "type") !== "radio" || isTag("textarea")(target);
  const getCurrent = (component, composeConfig, _composeState) => composeConfig.find(component);
  var ComposeApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    getCurrent
  });
  const ComposeSchema = [required$1("find")];
  const Composing = create$3({
    fields: ComposeSchema,
    name: "composing",
    apis: ComposeApis
  });
  const nativeDisabled = [
    "input",
    "button",
    "textarea",
    "select"
  ];
  const onLoad$1 = (component, disableConfig, disableState) => {
    const f = disableConfig.disabled() ? disable : enable;
    f(component, disableConfig);
  };
  const hasNative = (component, config2) => config2.useNative === true && contains$2(nativeDisabled, name$3(component.element));
  const nativeIsDisabled = (component) => has$1(component.element, "disabled");
  const nativeDisable = (component) => {
    set$9(component.element, "disabled", "disabled");
  };
  const nativeEnable = (component) => {
    remove$7(component.element, "disabled");
  };
  const ariaIsDisabled = (component) => get$f(component.element, "aria-disabled") === "true";
  const ariaDisable = (component) => {
    set$9(component.element, "aria-disabled", "true");
  };
  const ariaEnable = (component) => {
    set$9(component.element, "aria-disabled", "false");
  };
  const disable = (component, disableConfig, _disableState) => {
    disableConfig.disableClass.each((disableClass) => {
      add$2(component.element, disableClass);
    });
    const f = hasNative(component, disableConfig) ? nativeDisable : ariaDisable;
    f(component);
    disableConfig.onDisabled(component);
  };
  const enable = (component, disableConfig, _disableState) => {
    disableConfig.disableClass.each((disableClass) => {
      remove$2(component.element, disableClass);
    });
    const f = hasNative(component, disableConfig) ? nativeEnable : ariaEnable;
    f(component);
    disableConfig.onEnabled(component);
  };
  const isDisabled = (component, disableConfig) => hasNative(component, disableConfig) ? nativeIsDisabled(component) : ariaIsDisabled(component);
  const set$4 = (component, disableConfig, disableState, disabled) => {
    const f = disabled ? disable : enable;
    f(component, disableConfig);
  };
  var DisableApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    enable,
    disable,
    isDisabled,
    onLoad: onLoad$1,
    set: set$4
  });
  const exhibit$5 = (base2, disableConfig) => nu$7({ classes: disableConfig.disabled() ? disableConfig.disableClass.toArray() : [] });
  const events$e = (disableConfig, disableState) => derive$2([
    abort(execute$5(), (component, _simulatedEvent) => isDisabled(component, disableConfig)),
    loadEvent(disableConfig, disableState, onLoad$1)
  ]);
  var ActiveDisable = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    exhibit: exhibit$5,
    events: events$e
  });
  var DisableSchema = [
    defaultedFunction("disabled", never),
    defaulted("useNative", true),
    option$3("disableClass"),
    onHandler("onDisabled"),
    onHandler("onEnabled")
  ];
  const Disabling = create$3({
    fields: DisableSchema,
    name: "disabling",
    active: ActiveDisable,
    apis: DisableApis
  });
  const dehighlightAllExcept = (component, hConfig, hState, skip) => {
    const highlighted = descendants(component.element, "." + hConfig.highlightClass);
    each$1(highlighted, (h) => {
      const shouldSkip = exists(skip, (skipComp) => eq(skipComp.element, h));
      if (!shouldSkip) {
        remove$2(h, hConfig.highlightClass);
        component.getSystem().getByDom(h).each((target) => {
          hConfig.onDehighlight(component, target);
          emit(target, dehighlight$1());
        });
      }
    });
  };
  const dehighlightAll = (component, hConfig, hState) => dehighlightAllExcept(component, hConfig, hState, []);
  const dehighlight = (component, hConfig, hState, target) => {
    if (isHighlighted(component, hConfig, hState, target)) {
      remove$2(target.element, hConfig.highlightClass);
      hConfig.onDehighlight(component, target);
      emit(target, dehighlight$1());
    }
  };
  const highlight = (component, hConfig, hState, target) => {
    dehighlightAllExcept(component, hConfig, hState, [target]);
    if (!isHighlighted(component, hConfig, hState, target)) {
      add$2(target.element, hConfig.highlightClass);
      hConfig.onHighlight(component, target);
      emit(target, highlight$1());
    }
  };
  const highlightFirst = (component, hConfig, hState) => {
    getFirst(component, hConfig).each((firstComp) => {
      highlight(component, hConfig, hState, firstComp);
    });
  };
  const highlightLast = (component, hConfig, hState) => {
    getLast(component, hConfig).each((lastComp) => {
      highlight(component, hConfig, hState, lastComp);
    });
  };
  const highlightAt = (component, hConfig, hState, index) => {
    getByIndex(component, hConfig, hState, index).fold((err) => {
      throw err;
    }, (firstComp) => {
      highlight(component, hConfig, hState, firstComp);
    });
  };
  const highlightBy = (component, hConfig, hState, predicate) => {
    const candidates = getCandidates(component, hConfig);
    const targetComp = find$5(candidates, predicate);
    targetComp.each((c) => {
      highlight(component, hConfig, hState, c);
    });
  };
  const isHighlighted = (component, hConfig, hState, queryTarget) => has(queryTarget.element, hConfig.highlightClass);
  const getHighlighted = (component, hConfig, _hState) => descendant(component.element, "." + hConfig.highlightClass).bind((e) => component.getSystem().getByDom(e).toOptional());
  const getByIndex = (component, hConfig, hState, index) => {
    const items = descendants(component.element, "." + hConfig.itemClass);
    return Optional.from(items[index]).fold(() => Result.error(new Error("No element found with index " + index)), component.getSystem().getByDom);
  };
  const getFirst = (component, hConfig, _hState) => descendant(component.element, "." + hConfig.itemClass).bind((e) => component.getSystem().getByDom(e).toOptional());
  const getLast = (component, hConfig, _hState) => {
    const items = descendants(component.element, "." + hConfig.itemClass);
    const last2 = items.length > 0 ? Optional.some(items[items.length - 1]) : Optional.none();
    return last2.bind((c) => component.getSystem().getByDom(c).toOptional());
  };
  const getDelta$2 = (component, hConfig, hState, delta) => {
    const items = descendants(component.element, "." + hConfig.itemClass);
    const current = findIndex$1(items, (item2) => has(item2, hConfig.highlightClass));
    return current.bind((selected) => {
      const dest = cycleBy(selected, delta, 0, items.length - 1);
      return component.getSystem().getByDom(items[dest]).toOptional();
    });
  };
  const getPrevious = (component, hConfig, hState) => getDelta$2(component, hConfig, hState, -1);
  const getNext = (component, hConfig, hState) => getDelta$2(component, hConfig, hState, 1);
  const getCandidates = (component, hConfig, _hState) => {
    const items = descendants(component.element, "." + hConfig.itemClass);
    return cat(map$2(items, (i2) => component.getSystem().getByDom(i2).toOptional()));
  };
  var HighlightApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    dehighlightAll,
    dehighlight,
    highlight,
    highlightFirst,
    highlightLast,
    highlightAt,
    highlightBy,
    isHighlighted,
    getHighlighted,
    getFirst,
    getLast,
    getPrevious,
    getNext,
    getCandidates
  });
  var HighlightSchema = [
    required$1("highlightClass"),
    required$1("itemClass"),
    onHandler("onHighlight"),
    onHandler("onDehighlight")
  ];
  const Highlighting = create$3({
    fields: HighlightSchema,
    name: "highlighting",
    apis: HighlightApis
  });
  const BACKSPACE = [8];
  const TAB = [9];
  const ENTER = [13];
  const ESCAPE = [27];
  const SPACE = [32];
  const LEFT = [37];
  const UP = [38];
  const RIGHT = [39];
  const DOWN = [40];
  const cyclePrev = (values2, index, predicate) => {
    const before2 = reverse(values2.slice(0, index));
    const after2 = reverse(values2.slice(index + 1));
    return find$5(before2.concat(after2), predicate);
  };
  const tryPrev = (values2, index, predicate) => {
    const before2 = reverse(values2.slice(0, index));
    return find$5(before2, predicate);
  };
  const cycleNext = (values2, index, predicate) => {
    const before2 = values2.slice(0, index);
    const after2 = values2.slice(index + 1);
    return find$5(after2.concat(before2), predicate);
  };
  const tryNext = (values2, index, predicate) => {
    const after2 = values2.slice(index + 1);
    return find$5(after2, predicate);
  };
  const inSet = (keys2) => (event2) => {
    const raw = event2.raw;
    return contains$2(keys2, raw.which);
  };
  const and = (preds) => (event2) => forall(preds, (pred) => pred(event2));
  const isShift = (event2) => {
    const raw = event2.raw;
    return raw.shiftKey === true;
  };
  const isControl = (event2) => {
    const raw = event2.raw;
    return raw.ctrlKey === true;
  };
  const isNotShift = not(isShift);
  const rule = (matches, action) => ({
    matches,
    classification: action
  });
  const choose = (transitions, event2) => {
    const transition = find$5(transitions, (t2) => t2.matches(event2));
    return transition.map((t2) => t2.classification);
  };
  const reportFocusShifting = (component, prevFocus, newFocus) => {
    const noChange = prevFocus.exists((p) => newFocus.exists((n) => eq(n, p)));
    if (!noChange) {
      emitWith(component, focusShifted(), {
        prevFocus,
        newFocus
      });
    }
  };
  const dom$2 = () => {
    const get2 = (component) => search(component.element);
    const set2 = (component, focusee) => {
      const prevFocus = get2(component);
      component.getSystem().triggerFocus(focusee, component.element);
      const newFocus = get2(component);
      reportFocusShifting(component, prevFocus, newFocus);
    };
    return {
      get: get2,
      set: set2
    };
  };
  const highlights = () => {
    const get2 = (component) => Highlighting.getHighlighted(component).map((item2) => item2.element);
    const set2 = (component, element2) => {
      const prevFocus = get2(component);
      component.getSystem().getByDom(element2).fold(noop, (item2) => {
        Highlighting.highlight(component, item2);
      });
      const newFocus = get2(component);
      reportFocusShifting(component, prevFocus, newFocus);
    };
    return {
      get: get2,
      set: set2
    };
  };
  var FocusInsideModes;
  (function(FocusInsideModes2) {
    FocusInsideModes2["OnFocusMode"] = "onFocus";
    FocusInsideModes2["OnEnterOrSpaceMode"] = "onEnterOrSpace";
    FocusInsideModes2["OnApiMode"] = "onApi";
  })(FocusInsideModes || (FocusInsideModes = {}));
  const typical = (infoSchema, stateInit, getKeydownRules2, getKeyupRules2, optFocusIn) => {
    const schema2 = () => infoSchema.concat([
      defaulted("focusManager", dom$2()),
      defaultedOf("focusInside", "onFocus", valueOf((val) => contains$2([
        "onFocus",
        "onEnterOrSpace",
        "onApi"
      ], val) ? Result.value(val) : Result.error("Invalid value for focusInside"))),
      output$1("handler", me),
      output$1("state", stateInit),
      output$1("sendFocusIn", optFocusIn)
    ]);
    const processKey = (component, simulatedEvent, getRules, keyingConfig, keyingState) => {
      const rules = getRules(component, simulatedEvent, keyingConfig, keyingState);
      return choose(rules, simulatedEvent.event).bind((rule2) => rule2(component, simulatedEvent, keyingConfig, keyingState));
    };
    const toEvents2 = (keyingConfig, keyingState) => {
      const onFocusHandler = keyingConfig.focusInside !== FocusInsideModes.OnFocusMode ? Optional.none() : optFocusIn(keyingConfig).map((focusIn2) => run$1(focus$4(), (component, simulatedEvent) => {
        focusIn2(component, keyingConfig, keyingState);
        simulatedEvent.stop();
      }));
      const tryGoInsideComponent = (component, simulatedEvent) => {
        const isEnterOrSpace = inSet(SPACE.concat(ENTER))(simulatedEvent.event);
        if (keyingConfig.focusInside === FocusInsideModes.OnEnterOrSpaceMode && isEnterOrSpace && isSource(component, simulatedEvent)) {
          optFocusIn(keyingConfig).each((focusIn2) => {
            focusIn2(component, keyingConfig, keyingState);
            simulatedEvent.stop();
          });
        }
      };
      const keyboardEvents = [
        run$1(keydown(), (component, simulatedEvent) => {
          processKey(component, simulatedEvent, getKeydownRules2, keyingConfig, keyingState).fold(() => {
            tryGoInsideComponent(component, simulatedEvent);
          }, (_) => {
            simulatedEvent.stop();
          });
        }),
        run$1(keyup(), (component, simulatedEvent) => {
          processKey(component, simulatedEvent, getKeyupRules2, keyingConfig, keyingState).each((_) => {
            simulatedEvent.stop();
          });
        })
      ];
      return derive$2(onFocusHandler.toArray().concat(keyboardEvents));
    };
    const me = {
      schema: schema2,
      processKey,
      toEvents: toEvents2
    };
    return me;
  };
  const create$1 = (cyclicField) => {
    const schema2 = [
      option$3("onEscape"),
      option$3("onEnter"),
      defaulted("selector", '[data-alloy-tabstop="true"]:not(:disabled)'),
      defaulted("firstTabstop", 0),
      defaulted("useTabstopAt", always),
      option$3("visibilitySelector")
    ].concat([cyclicField]);
    const isVisible2 = (tabbingConfig, element2) => {
      const target = tabbingConfig.visibilitySelector.bind((sel) => closest$1(element2, sel)).getOr(element2);
      return get$d(target) > 0;
    };
    const findInitial = (component, tabbingConfig) => {
      const tabstops = descendants(component.element, tabbingConfig.selector);
      const visibles = filter$2(tabstops, (elem) => isVisible2(tabbingConfig, elem));
      return Optional.from(visibles[tabbingConfig.firstTabstop]);
    };
    const findCurrent2 = (component, tabbingConfig) => tabbingConfig.focusManager.get(component).bind((elem) => closest$1(elem, tabbingConfig.selector));
    const isTabstop = (tabbingConfig, element2) => isVisible2(tabbingConfig, element2) && tabbingConfig.useTabstopAt(element2);
    const focusIn2 = (component, tabbingConfig, _tabbingState) => {
      findInitial(component, tabbingConfig).each((target) => {
        tabbingConfig.focusManager.set(component, target);
      });
    };
    const goFromTabstop = (component, tabstops, stopIndex, tabbingConfig, cycle) => cycle(tabstops, stopIndex, (elem) => isTabstop(tabbingConfig, elem)).fold(() => tabbingConfig.cyclic ? Optional.some(true) : Optional.none(), (target) => {
      tabbingConfig.focusManager.set(component, target);
      return Optional.some(true);
    });
    const go2 = (component, _simulatedEvent, tabbingConfig, cycle) => {
      const tabstops = descendants(component.element, tabbingConfig.selector);
      return findCurrent2(component, tabbingConfig).bind((tabstop) => {
        const optStopIndex = findIndex$1(tabstops, curry(eq, tabstop));
        return optStopIndex.bind((stopIndex) => goFromTabstop(component, tabstops, stopIndex, tabbingConfig, cycle));
      });
    };
    const goBackwards = (component, simulatedEvent, tabbingConfig) => {
      const navigate = tabbingConfig.cyclic ? cyclePrev : tryPrev;
      return go2(component, simulatedEvent, tabbingConfig, navigate);
    };
    const goForwards = (component, simulatedEvent, tabbingConfig) => {
      const navigate = tabbingConfig.cyclic ? cycleNext : tryNext;
      return go2(component, simulatedEvent, tabbingConfig, navigate);
    };
    const execute2 = (component, simulatedEvent, tabbingConfig) => tabbingConfig.onEnter.bind((f) => f(component, simulatedEvent));
    const exit = (component, simulatedEvent, tabbingConfig) => tabbingConfig.onEscape.bind((f) => f(component, simulatedEvent));
    const getKeydownRules2 = constant$1([
      rule(and([
        isShift,
        inSet(TAB)
      ]), goBackwards),
      rule(inSet(TAB), goForwards),
      rule(and([
        isNotShift,
        inSet(ENTER)
      ]), execute2)
    ]);
    const getKeyupRules2 = constant$1([rule(inSet(ESCAPE), exit)]);
    return typical(schema2, NoState.init, getKeydownRules2, getKeyupRules2, () => Optional.some(focusIn2));
  };
  var AcyclicType = create$1(customField("cyclic", never));
  var CyclicType = create$1(customField("cyclic", always));
  const doDefaultExecute = (component, _simulatedEvent, focused) => {
    dispatch(component, focused, execute$5());
    return Optional.some(true);
  };
  const defaultExecute = (component, simulatedEvent, focused) => {
    const isComplex = inside(focused) && inSet(SPACE)(simulatedEvent.event);
    return isComplex ? Optional.none() : doDefaultExecute(component, simulatedEvent, focused);
  };
  const stopEventForFirefox = (_component, _simulatedEvent) => Optional.some(true);
  const schema$v = [
    defaulted("execute", defaultExecute),
    defaulted("useSpace", false),
    defaulted("useEnter", true),
    defaulted("useControlEnter", false),
    defaulted("useDown", false)
  ];
  const execute$4 = (component, simulatedEvent, executeConfig) => executeConfig.execute(component, simulatedEvent, component.element);
  const getKeydownRules$5 = (component, _simulatedEvent, executeConfig, _executeState) => {
    const spaceExec = executeConfig.useSpace && !inside(component.element) ? SPACE : [];
    const enterExec = executeConfig.useEnter ? ENTER : [];
    const downExec = executeConfig.useDown ? DOWN : [];
    const execKeys = spaceExec.concat(enterExec).concat(downExec);
    return [rule(inSet(execKeys), execute$4)].concat(executeConfig.useControlEnter ? [rule(and([
      isControl,
      inSet(ENTER)
    ]), execute$4)] : []);
  };
  const getKeyupRules$5 = (component, _simulatedEvent, executeConfig, _executeState) => executeConfig.useSpace && !inside(component.element) ? [rule(inSet(SPACE), stopEventForFirefox)] : [];
  var ExecutionType = typical(schema$v, NoState.init, getKeydownRules$5, getKeyupRules$5, () => Optional.none());
  const flatgrid$1 = () => {
    const dimensions = value$2();
    const setGridSize = (numRows, numColumns) => {
      dimensions.set({
        numRows,
        numColumns
      });
    };
    const getNumRows = () => dimensions.get().map((d) => d.numRows);
    const getNumColumns = () => dimensions.get().map((d) => d.numColumns);
    return nu$8({
      readState: () => dimensions.get().map((d) => ({
        numRows: String(d.numRows),
        numColumns: String(d.numColumns)
      })).getOr({
        numRows: "?",
        numColumns: "?"
      }),
      setGridSize,
      getNumRows,
      getNumColumns
    });
  };
  const init$d = (spec) => spec.state(spec);
  var KeyingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    flatgrid: flatgrid$1,
    init: init$d
  });
  const useH = (movement) => (component, simulatedEvent, config2, state) => {
    const move2 = movement(component.element);
    return use(move2, component, simulatedEvent, config2, state);
  };
  const west$1 = (moveLeft2, moveRight2) => {
    const movement = onDirection(moveLeft2, moveRight2);
    return useH(movement);
  };
  const east$1 = (moveLeft2, moveRight2) => {
    const movement = onDirection(moveRight2, moveLeft2);
    return useH(movement);
  };
  const useV = (move2) => (component, simulatedEvent, config2, state) => use(move2, component, simulatedEvent, config2, state);
  const use = (move2, component, simulatedEvent, config2, state) => {
    const outcome = config2.focusManager.get(component).bind((focused) => move2(component.element, focused, config2, state));
    return outcome.map((newFocus) => {
      config2.focusManager.set(component, newFocus);
      return true;
    });
  };
  const north$1 = useV;
  const south$1 = useV;
  const move$1 = useV;
  const isHidden$1 = (dom2) => dom2.offsetWidth <= 0 && dom2.offsetHeight <= 0;
  const isVisible = (element2) => !isHidden$1(element2.dom);
  const locate = (candidates, predicate) => findIndex$1(candidates, predicate).map((index) => ({
    index,
    candidates
  }));
  const locateVisible = (container, current, selector) => {
    const predicate = (x) => eq(x, current);
    const candidates = descendants(container, selector);
    const visible = filter$2(candidates, isVisible);
    return locate(visible, predicate);
  };
  const findIndex = (elements, target) => findIndex$1(elements, (elem) => eq(target, elem));
  const withGrid = (values2, index, numCols, f) => {
    const oldRow = Math.floor(index / numCols);
    const oldColumn = index % numCols;
    return f(oldRow, oldColumn).bind((address) => {
      const newIndex = address.row * numCols + address.column;
      return newIndex >= 0 && newIndex < values2.length ? Optional.some(values2[newIndex]) : Optional.none();
    });
  };
  const cycleHorizontal$1 = (values2, index, numRows, numCols, delta) => withGrid(values2, index, numCols, (oldRow, oldColumn) => {
    const onLastRow = oldRow === numRows - 1;
    const colsInRow = onLastRow ? values2.length - oldRow * numCols : numCols;
    const newColumn = cycleBy(oldColumn, delta, 0, colsInRow - 1);
    return Optional.some({
      row: oldRow,
      column: newColumn
    });
  });
  const cycleVertical$1 = (values2, index, numRows, numCols, delta) => withGrid(values2, index, numCols, (oldRow, oldColumn) => {
    const newRow = cycleBy(oldRow, delta, 0, numRows - 1);
    const onLastRow = newRow === numRows - 1;
    const colsInRow = onLastRow ? values2.length - newRow * numCols : numCols;
    const newCol = clamp(oldColumn, 0, colsInRow - 1);
    return Optional.some({
      row: newRow,
      column: newCol
    });
  });
  const cycleRight$1 = (values2, index, numRows, numCols) => cycleHorizontal$1(values2, index, numRows, numCols, 1);
  const cycleLeft$1 = (values2, index, numRows, numCols) => cycleHorizontal$1(values2, index, numRows, numCols, -1);
  const cycleUp$1 = (values2, index, numRows, numCols) => cycleVertical$1(values2, index, numRows, numCols, -1);
  const cycleDown$1 = (values2, index, numRows, numCols) => cycleVertical$1(values2, index, numRows, numCols, 1);
  const schema$u = [
    required$1("selector"),
    defaulted("execute", defaultExecute),
    onKeyboardHandler("onEscape"),
    defaulted("captureTab", false),
    initSize()
  ];
  const focusIn$3 = (component, gridConfig, _gridState) => {
    descendant(component.element, gridConfig.selector).each((first2) => {
      gridConfig.focusManager.set(component, first2);
    });
  };
  const findCurrent$1 = (component, gridConfig) => gridConfig.focusManager.get(component).bind((elem) => closest$1(elem, gridConfig.selector));
  const execute$3 = (component, simulatedEvent, gridConfig, _gridState) => findCurrent$1(component, gridConfig).bind((focused) => gridConfig.execute(component, simulatedEvent, focused));
  const doMove$2 = (cycle) => (element2, focused, gridConfig, gridState) => locateVisible(element2, focused, gridConfig.selector).bind((identified) => cycle(identified.candidates, identified.index, gridState.getNumRows().getOr(gridConfig.initSize.numRows), gridState.getNumColumns().getOr(gridConfig.initSize.numColumns)));
  const handleTab = (_component, _simulatedEvent, gridConfig) => gridConfig.captureTab ? Optional.some(true) : Optional.none();
  const doEscape$1 = (component, simulatedEvent, gridConfig) => gridConfig.onEscape(component, simulatedEvent);
  const moveLeft$3 = doMove$2(cycleLeft$1);
  const moveRight$3 = doMove$2(cycleRight$1);
  const moveNorth$1 = doMove$2(cycleUp$1);
  const moveSouth$1 = doMove$2(cycleDown$1);
  const getKeydownRules$4 = constant$1([
    rule(inSet(LEFT), west$1(moveLeft$3, moveRight$3)),
    rule(inSet(RIGHT), east$1(moveLeft$3, moveRight$3)),
    rule(inSet(UP), north$1(moveNorth$1)),
    rule(inSet(DOWN), south$1(moveSouth$1)),
    rule(and([
      isShift,
      inSet(TAB)
    ]), handleTab),
    rule(and([
      isNotShift,
      inSet(TAB)
    ]), handleTab),
    rule(inSet(SPACE.concat(ENTER)), execute$3)
  ]);
  const getKeyupRules$4 = constant$1([
    rule(inSet(ESCAPE), doEscape$1),
    rule(inSet(SPACE), stopEventForFirefox)
  ]);
  var FlatgridType = typical(schema$u, flatgrid$1, getKeydownRules$4, getKeyupRules$4, () => Optional.some(focusIn$3));
  const horizontal = (container, selector, current, delta) => {
    const isDisabledButton = (candidate) => name$3(candidate) === "button" && get$f(candidate, "disabled") === "disabled";
    const tryCycle = (initial, index, candidates) => {
      const newIndex = cycleBy(index, delta, 0, candidates.length - 1);
      if (newIndex === initial) {
        return Optional.none();
      } else {
        return isDisabledButton(candidates[newIndex]) ? tryCycle(initial, newIndex, candidates) : Optional.from(candidates[newIndex]);
      }
    };
    return locateVisible(container, current, selector).bind((identified) => {
      const index = identified.index;
      const candidates = identified.candidates;
      return tryCycle(index, index, candidates);
    });
  };
  const schema$t = [
    required$1("selector"),
    defaulted("getInitial", Optional.none),
    defaulted("execute", defaultExecute),
    onKeyboardHandler("onEscape"),
    defaulted("executeOnMove", false),
    defaulted("allowVertical", true)
  ];
  const findCurrent = (component, flowConfig) => flowConfig.focusManager.get(component).bind((elem) => closest$1(elem, flowConfig.selector));
  const execute$2 = (component, simulatedEvent, flowConfig) => findCurrent(component, flowConfig).bind((focused) => flowConfig.execute(component, simulatedEvent, focused));
  const focusIn$2 = (component, flowConfig, _state) => {
    flowConfig.getInitial(component).orThunk(() => descendant(component.element, flowConfig.selector)).each((first2) => {
      flowConfig.focusManager.set(component, first2);
    });
  };
  const moveLeft$2 = (element2, focused, info) => horizontal(element2, info.selector, focused, -1);
  const moveRight$2 = (element2, focused, info) => horizontal(element2, info.selector, focused, 1);
  const doMove$1 = (movement) => (component, simulatedEvent, flowConfig, flowState) => movement(component, simulatedEvent, flowConfig, flowState).bind(() => flowConfig.executeOnMove ? execute$2(component, simulatedEvent, flowConfig) : Optional.some(true));
  const doEscape = (component, simulatedEvent, flowConfig) => flowConfig.onEscape(component, simulatedEvent);
  const getKeydownRules$3 = (_component, _se, flowConfig, _flowState) => {
    const westMovers = LEFT.concat(flowConfig.allowVertical ? UP : []);
    const eastMovers = RIGHT.concat(flowConfig.allowVertical ? DOWN : []);
    return [
      rule(inSet(westMovers), doMove$1(west$1(moveLeft$2, moveRight$2))),
      rule(inSet(eastMovers), doMove$1(east$1(moveLeft$2, moveRight$2))),
      rule(inSet(ENTER), execute$2),
      rule(inSet(SPACE), execute$2)
    ];
  };
  const getKeyupRules$3 = constant$1([
    rule(inSet(SPACE), stopEventForFirefox),
    rule(inSet(ESCAPE), doEscape)
  ]);
  var FlowType = typical(schema$t, NoState.init, getKeydownRules$3, getKeyupRules$3, () => Optional.some(focusIn$2));
  const toCell = (matrix2, rowIndex, columnIndex) => Optional.from(matrix2[rowIndex]).bind((row) => Optional.from(row[columnIndex]).map((cell) => ({
    rowIndex,
    columnIndex,
    cell
  })));
  const cycleHorizontal = (matrix2, rowIndex, startCol, deltaCol) => {
    const row = matrix2[rowIndex];
    const colsInRow = row.length;
    const newColIndex = cycleBy(startCol, deltaCol, 0, colsInRow - 1);
    return toCell(matrix2, rowIndex, newColIndex);
  };
  const cycleVertical = (matrix2, colIndex, startRow, deltaRow) => {
    const nextRowIndex = cycleBy(startRow, deltaRow, 0, matrix2.length - 1);
    const colsInNextRow = matrix2[nextRowIndex].length;
    const nextColIndex = clamp(colIndex, 0, colsInNextRow - 1);
    return toCell(matrix2, nextRowIndex, nextColIndex);
  };
  const moveHorizontal = (matrix2, rowIndex, startCol, deltaCol) => {
    const row = matrix2[rowIndex];
    const colsInRow = row.length;
    const newColIndex = clamp(startCol + deltaCol, 0, colsInRow - 1);
    return toCell(matrix2, rowIndex, newColIndex);
  };
  const moveVertical = (matrix2, colIndex, startRow, deltaRow) => {
    const nextRowIndex = clamp(startRow + deltaRow, 0, matrix2.length - 1);
    const colsInNextRow = matrix2[nextRowIndex].length;
    const nextColIndex = clamp(colIndex, 0, colsInNextRow - 1);
    return toCell(matrix2, nextRowIndex, nextColIndex);
  };
  const cycleRight = (matrix2, startRow, startCol) => cycleHorizontal(matrix2, startRow, startCol, 1);
  const cycleLeft = (matrix2, startRow, startCol) => cycleHorizontal(matrix2, startRow, startCol, -1);
  const cycleUp = (matrix2, startRow, startCol) => cycleVertical(matrix2, startCol, startRow, -1);
  const cycleDown = (matrix2, startRow, startCol) => cycleVertical(matrix2, startCol, startRow, 1);
  const moveLeft$1 = (matrix2, startRow, startCol) => moveHorizontal(matrix2, startRow, startCol, -1);
  const moveRight$1 = (matrix2, startRow, startCol) => moveHorizontal(matrix2, startRow, startCol, 1);
  const moveUp$1 = (matrix2, startRow, startCol) => moveVertical(matrix2, startCol, startRow, -1);
  const moveDown$1 = (matrix2, startRow, startCol) => moveVertical(matrix2, startCol, startRow, 1);
  const schema$s = [
    requiredObjOf("selectors", [
      required$1("row"),
      required$1("cell")
    ]),
    defaulted("cycles", true),
    defaulted("previousSelector", Optional.none),
    defaulted("execute", defaultExecute)
  ];
  const focusIn$1 = (component, matrixConfig, _state) => {
    const focused = matrixConfig.previousSelector(component).orThunk(() => {
      const selectors = matrixConfig.selectors;
      return descendant(component.element, selectors.cell);
    });
    focused.each((cell) => {
      matrixConfig.focusManager.set(component, cell);
    });
  };
  const execute$1 = (component, simulatedEvent, matrixConfig) => search(component.element).bind((focused) => matrixConfig.execute(component, simulatedEvent, focused));
  const toMatrix = (rows, matrixConfig) => map$2(rows, (row) => descendants(row, matrixConfig.selectors.cell));
  const doMove = (ifCycle, ifMove) => (element2, focused, matrixConfig) => {
    const move2 = matrixConfig.cycles ? ifCycle : ifMove;
    return closest$1(focused, matrixConfig.selectors.row).bind((inRow) => {
      const cellsInRow = descendants(inRow, matrixConfig.selectors.cell);
      return findIndex(cellsInRow, focused).bind((colIndex) => {
        const allRows = descendants(element2, matrixConfig.selectors.row);
        return findIndex(allRows, inRow).bind((rowIndex) => {
          const matrix2 = toMatrix(allRows, matrixConfig);
          return move2(matrix2, rowIndex, colIndex).map((next) => next.cell);
        });
      });
    });
  };
  const moveLeft = doMove(cycleLeft, moveLeft$1);
  const moveRight = doMove(cycleRight, moveRight$1);
  const moveNorth = doMove(cycleUp, moveUp$1);
  const moveSouth = doMove(cycleDown, moveDown$1);
  const getKeydownRules$2 = constant$1([
    rule(inSet(LEFT), west$1(moveLeft, moveRight)),
    rule(inSet(RIGHT), east$1(moveLeft, moveRight)),
    rule(inSet(UP), north$1(moveNorth)),
    rule(inSet(DOWN), south$1(moveSouth)),
    rule(inSet(SPACE.concat(ENTER)), execute$1)
  ]);
  const getKeyupRules$2 = constant$1([rule(inSet(SPACE), stopEventForFirefox)]);
  var MatrixType = typical(schema$s, NoState.init, getKeydownRules$2, getKeyupRules$2, () => Optional.some(focusIn$1));
  const schema$r = [
    required$1("selector"),
    defaulted("execute", defaultExecute),
    defaulted("moveOnTab", false)
  ];
  const execute = (component, simulatedEvent, menuConfig) => menuConfig.focusManager.get(component).bind((focused) => menuConfig.execute(component, simulatedEvent, focused));
  const focusIn = (component, menuConfig, _state) => {
    descendant(component.element, menuConfig.selector).each((first2) => {
      menuConfig.focusManager.set(component, first2);
    });
  };
  const moveUp = (element2, focused, info) => horizontal(element2, info.selector, focused, -1);
  const moveDown = (element2, focused, info) => horizontal(element2, info.selector, focused, 1);
  const fireShiftTab = (component, simulatedEvent, menuConfig, menuState) => menuConfig.moveOnTab ? move$1(moveUp)(component, simulatedEvent, menuConfig, menuState) : Optional.none();
  const fireTab = (component, simulatedEvent, menuConfig, menuState) => menuConfig.moveOnTab ? move$1(moveDown)(component, simulatedEvent, menuConfig, menuState) : Optional.none();
  const getKeydownRules$1 = constant$1([
    rule(inSet(UP), move$1(moveUp)),
    rule(inSet(DOWN), move$1(moveDown)),
    rule(and([
      isShift,
      inSet(TAB)
    ]), fireShiftTab),
    rule(and([
      isNotShift,
      inSet(TAB)
    ]), fireTab),
    rule(inSet(ENTER), execute),
    rule(inSet(SPACE), execute)
  ]);
  const getKeyupRules$1 = constant$1([rule(inSet(SPACE), stopEventForFirefox)]);
  var MenuType = typical(schema$r, NoState.init, getKeydownRules$1, getKeyupRules$1, () => Optional.some(focusIn));
  const schema$q = [
    onKeyboardHandler("onSpace"),
    onKeyboardHandler("onEnter"),
    onKeyboardHandler("onShiftEnter"),
    onKeyboardHandler("onLeft"),
    onKeyboardHandler("onRight"),
    onKeyboardHandler("onTab"),
    onKeyboardHandler("onShiftTab"),
    onKeyboardHandler("onUp"),
    onKeyboardHandler("onDown"),
    onKeyboardHandler("onEscape"),
    defaulted("stopSpaceKeyup", false),
    option$3("focusIn")
  ];
  const getKeydownRules = (component, simulatedEvent, specialInfo) => [
    rule(inSet(SPACE), specialInfo.onSpace),
    rule(and([
      isNotShift,
      inSet(ENTER)
    ]), specialInfo.onEnter),
    rule(and([
      isShift,
      inSet(ENTER)
    ]), specialInfo.onShiftEnter),
    rule(and([
      isShift,
      inSet(TAB)
    ]), specialInfo.onShiftTab),
    rule(and([
      isNotShift,
      inSet(TAB)
    ]), specialInfo.onTab),
    rule(inSet(UP), specialInfo.onUp),
    rule(inSet(DOWN), specialInfo.onDown),
    rule(inSet(LEFT), specialInfo.onLeft),
    rule(inSet(RIGHT), specialInfo.onRight),
    rule(inSet(SPACE), specialInfo.onSpace)
  ];
  const getKeyupRules = (component, simulatedEvent, specialInfo) => [
    ...specialInfo.stopSpaceKeyup ? [rule(inSet(SPACE), stopEventForFirefox)] : [],
    rule(inSet(ESCAPE), specialInfo.onEscape)
  ];
  var SpecialType = typical(schema$q, NoState.init, getKeydownRules, getKeyupRules, (specialInfo) => specialInfo.focusIn);
  const acyclic = AcyclicType.schema();
  const cyclic = CyclicType.schema();
  const flow = FlowType.schema();
  const flatgrid = FlatgridType.schema();
  const matrix = MatrixType.schema();
  const execution = ExecutionType.schema();
  const menu = MenuType.schema();
  const special = SpecialType.schema();
  var KeyboardBranches = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    acyclic,
    cyclic,
    flow,
    flatgrid,
    matrix,
    execution,
    menu,
    special
  });
  const isFlatgridState = (keyState) => hasNonNullableKey(keyState, "setGridSize");
  const Keying = createModes({
    branchKey: "mode",
    branches: KeyboardBranches,
    name: "keying",
    active: {
      events: (keyingConfig, keyingState) => {
        const handler = keyingConfig.handler;
        return handler.toEvents(keyingConfig, keyingState);
      }
    },
    apis: {
      focusIn: (component, keyConfig, keyState) => {
        keyConfig.sendFocusIn(keyConfig).fold(() => {
          component.getSystem().triggerFocus(component.element, component.element);
        }, (sendFocusIn) => {
          sendFocusIn(component, keyConfig, keyState);
        });
      },
      setGridSize: (component, keyConfig, keyState, numRows, numColumns) => {
        if (!isFlatgridState(keyState)) {
          console.error("Layout does not support setGridSize");
        } else {
          keyState.setGridSize(numRows, numColumns);
        }
      }
    },
    state: KeyingState
  });
  const withoutReuse = (parent2, data) => {
    preserve$1(() => {
      replaceChildren(parent2, data, () => map$2(data, parent2.getSystem().build));
    }, parent2.element);
  };
  const withReuse = (parent2, data) => {
    preserve$1(() => {
      virtualReplaceChildren(parent2, data, () => {
        return patchSpecChildren(parent2.element, data, parent2.getSystem().buildOrPatch);
      });
    }, parent2.element);
  };
  const virtualReplace = (component, replacee, replaceeIndex, childSpec) => {
    virtualDetach(replacee);
    const child2 = patchSpecChild(component.element, replaceeIndex, childSpec, component.getSystem().buildOrPatch);
    virtualAttach(component, child2);
    component.syncComponents();
  };
  const insert = (component, insertion, childSpec) => {
    const child2 = component.getSystem().build(childSpec);
    attachWith(component, child2, insertion);
  };
  const replace = (component, replacee, replaceeIndex, childSpec) => {
    detach(replacee);
    insert(component, (p, c) => appendAt(p, c, replaceeIndex), childSpec);
  };
  const set$3 = (component, replaceConfig, replaceState, data) => {
    const replacer = replaceConfig.reuseDom ? withReuse : withoutReuse;
    return replacer(component, data);
  };
  const append = (component, replaceConfig, replaceState, appendee) => {
    insert(component, append$2, appendee);
  };
  const prepend = (component, replaceConfig, replaceState, prependee) => {
    insert(component, prepend$1, prependee);
  };
  const remove = (component, replaceConfig, replaceState, removee) => {
    const children2 = contents(component);
    const foundChild = find$5(children2, (child2) => eq(removee.element, child2.element));
    foundChild.each(detach);
  };
  const contents = (component, _replaceConfig) => component.components();
  const replaceAt = (component, replaceConfig, replaceState, replaceeIndex, replacer) => {
    const children2 = contents(component);
    return Optional.from(children2[replaceeIndex]).map((replacee) => {
      replacer.fold(() => detach(replacee), (r2) => {
        const replacer2 = replaceConfig.reuseDom ? virtualReplace : replace;
        replacer2(component, replacee, replaceeIndex, r2);
      });
      return replacee;
    });
  };
  const replaceBy = (component, replaceConfig, replaceState, replaceePred, replacer) => {
    const children2 = contents(component);
    return findIndex$1(children2, replaceePred).bind((replaceeIndex) => replaceAt(component, replaceConfig, replaceState, replaceeIndex, replacer));
  };
  var ReplaceApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    append,
    prepend,
    remove,
    replaceAt,
    replaceBy,
    set: set$3,
    contents
  });
  const Replacing = create$3({
    fields: [defaultedBoolean("reuseDom", true)],
    name: "replacing",
    apis: ReplaceApis
  });
  const events$d = (name2, eventHandlers) => {
    const events2 = derive$2(eventHandlers);
    return create$3({
      fields: [required$1("enabled")],
      name: name2,
      active: { events: constant$1(events2) }
    });
  };
  const config = (name2, eventHandlers) => {
    const me = events$d(name2, eventHandlers);
    return {
      key: name2,
      value: {
        config: {},
        me,
        configAsRaw: constant$1({}),
        initialConfig: {},
        state: NoState
      }
    };
  };
  const focus$2 = (component, focusConfig) => {
    if (!focusConfig.ignore) {
      focus$3(component.element);
      focusConfig.onFocus(component);
    }
  };
  const blur = (component, focusConfig) => {
    if (!focusConfig.ignore) {
      blur$1(component.element);
    }
  };
  const isFocused = (component) => hasFocus(component.element);
  var FocusApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    focus: focus$2,
    blur,
    isFocused
  });
  const exhibit$4 = (base2, focusConfig) => {
    const mod = focusConfig.ignore ? {} : { attributes: { tabindex: "-1" } };
    return nu$7(mod);
  };
  const events$c = (focusConfig) => derive$2([run$1(focus$4(), (component, simulatedEvent) => {
    focus$2(component, focusConfig);
    simulatedEvent.stop();
  })].concat(focusConfig.stopMousedown ? [run$1(mousedown(), (_, simulatedEvent) => {
    simulatedEvent.event.prevent();
  })] : []));
  var ActiveFocus = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    exhibit: exhibit$4,
    events: events$c
  });
  var FocusSchema = [
    onHandler("onFocus"),
    defaulted("stopMousedown", false),
    defaulted("ignore", false)
  ];
  const Focusing = create$3({
    fields: FocusSchema,
    name: "focusing",
    active: ActiveFocus,
    apis: FocusApis
  });
  const SetupBehaviourCellState = (initialState) => {
    const init2 = () => {
      const cell = Cell(initialState);
      const get2 = () => cell.get();
      const set2 = (newState) => cell.set(newState);
      const clear2 = () => cell.set(initialState);
      const readState = () => cell.get();
      return {
        get: get2,
        set: set2,
        clear: clear2,
        readState
      };
    };
    return { init: init2 };
  };
  const updateAriaState = (component, toggleConfig, toggleState) => {
    const ariaInfo = toggleConfig.aria;
    ariaInfo.update(component, ariaInfo, toggleState.get());
  };
  const updateClass = (component, toggleConfig, toggleState) => {
    toggleConfig.toggleClass.each((toggleClass) => {
      if (toggleState.get()) {
        add$2(component.element, toggleClass);
      } else {
        remove$2(component.element, toggleClass);
      }
    });
  };
  const set$2 = (component, toggleConfig, toggleState, state) => {
    const initialState = toggleState.get();
    toggleState.set(state);
    updateClass(component, toggleConfig, toggleState);
    updateAriaState(component, toggleConfig, toggleState);
    if (initialState !== state) {
      toggleConfig.onToggled(component, state);
    }
  };
  const toggle$2 = (component, toggleConfig, toggleState) => {
    set$2(component, toggleConfig, toggleState, !toggleState.get());
  };
  const on = (component, toggleConfig, toggleState) => {
    set$2(component, toggleConfig, toggleState, true);
  };
  const off = (component, toggleConfig, toggleState) => {
    set$2(component, toggleConfig, toggleState, false);
  };
  const isOn = (component, toggleConfig, toggleState) => toggleState.get();
  const onLoad = (component, toggleConfig, toggleState) => {
    set$2(component, toggleConfig, toggleState, toggleConfig.selected);
  };
  var ToggleApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    onLoad,
    toggle: toggle$2,
    isOn,
    on,
    off,
    set: set$2
  });
  const exhibit$3 = () => nu$7({});
  const events$b = (toggleConfig, toggleState) => {
    const execute2 = executeEvent(toggleConfig, toggleState, toggle$2);
    const load = loadEvent(toggleConfig, toggleState, onLoad);
    return derive$2(flatten([
      toggleConfig.toggleOnExecute ? [execute2] : [],
      [load]
    ]));
  };
  var ActiveToggle = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    exhibit: exhibit$3,
    events: events$b
  });
  const updatePressed = (component, ariaInfo, status) => {
    set$9(component.element, "aria-pressed", status);
    if (ariaInfo.syncWithExpanded) {
      updateExpanded(component, ariaInfo, status);
    }
  };
  const updateSelected = (component, ariaInfo, status) => {
    set$9(component.element, "aria-selected", status);
  };
  const updateChecked = (component, ariaInfo, status) => {
    set$9(component.element, "aria-checked", status);
  };
  const updateExpanded = (component, ariaInfo, status) => {
    set$9(component.element, "aria-expanded", status);
  };
  var ToggleSchema = [
    defaulted("selected", false),
    option$3("toggleClass"),
    defaulted("toggleOnExecute", true),
    onHandler("onToggled"),
    defaultedOf("aria", { mode: "none" }, choose$1("mode", {
      pressed: [
        defaulted("syncWithExpanded", false),
        output$1("update", updatePressed)
      ],
      checked: [output$1("update", updateChecked)],
      expanded: [output$1("update", updateExpanded)],
      selected: [output$1("update", updateSelected)],
      none: [output$1("update", noop)]
    }))
  ];
  const Toggling = create$3({
    fields: ToggleSchema,
    name: "toggling",
    active: ActiveToggle,
    apis: ToggleApis,
    state: SetupBehaviourCellState(false)
  });
  const pointerEvents = () => {
    const onClick = (component, simulatedEvent) => {
      simulatedEvent.stop();
      emitExecute(component);
    };
    return [
      run$1(click(), onClick),
      run$1(tap(), onClick),
      cutter(touchstart()),
      cutter(mousedown())
    ];
  };
  const events$a = (optAction) => {
    const executeHandler = (action) => runOnExecute$1((component, simulatedEvent) => {
      action(component);
      simulatedEvent.stop();
    });
    return derive$2(flatten([
      optAction.map(executeHandler).toArray(),
      pointerEvents()
    ]));
  };
  const hoverEvent = "alloy.item-hover";
  const focusEvent = "alloy.item-focus";
  const toggledEvent = "alloy.item-toggled";
  const onHover = (item2) => {
    if (search(item2.element).isNone() || Focusing.isFocused(item2)) {
      if (!Focusing.isFocused(item2)) {
        Focusing.focus(item2);
      }
      emitWith(item2, hoverEvent, { item: item2 });
    }
  };
  const onFocus$1 = (item2) => {
    emitWith(item2, focusEvent, { item: item2 });
  };
  const onToggled = (item2, state) => {
    emitWith(item2, toggledEvent, {
      item: item2,
      state
    });
  };
  const hover = constant$1(hoverEvent);
  const focus$1 = constant$1(focusEvent);
  const toggled = constant$1(toggledEvent);
  const getItemRole = (detail) => detail.toggling.map((toggling) => toggling.exclusive ? "menuitemradio" : "menuitemcheckbox").getOr("menuitem");
  const getTogglingSpec = (tConfig) => ({
    aria: { mode: "checked" },
    ...filter$1(tConfig, (_value, name2) => name2 !== "exclusive"),
    onToggled: (component, state) => {
      if (isFunction(tConfig.onToggled)) {
        tConfig.onToggled(component, state);
      }
      onToggled(component, state);
    }
  });
  const builder$2 = (detail) => ({
    dom: detail.dom,
    domModification: {
      ...detail.domModification,
      attributes: {
        "role": getItemRole(detail),
        ...detail.domModification.attributes,
        "aria-haspopup": detail.hasSubmenu,
        ...detail.hasSubmenu ? { "aria-expanded": false } : {}
      }
    },
    behaviours: SketchBehaviours.augment(detail.itemBehaviours, [
      detail.toggling.fold(Toggling.revoke, (tConfig) => Toggling.config(getTogglingSpec(tConfig))),
      Focusing.config({
        ignore: detail.ignoreFocus,
        stopMousedown: detail.ignoreFocus,
        onFocus: (component) => {
          onFocus$1(component);
        }
      }),
      Keying.config({ mode: "execution" }),
      Representing.config({
        store: {
          mode: "memory",
          initialValue: detail.data
        }
      }),
      config("item-type-events", [
        ...pointerEvents(),
        run$1(mouseover(), onHover),
        run$1(focusItem(), Focusing.focus)
      ])
    ]),
    components: detail.components,
    eventOrder: detail.eventOrder
  });
  const schema$p = [
    required$1("data"),
    required$1("components"),
    required$1("dom"),
    defaulted("hasSubmenu", false),
    option$3("toggling"),
    SketchBehaviours.field("itemBehaviours", [
      Toggling,
      Focusing,
      Keying,
      Representing
    ]),
    defaulted("ignoreFocus", false),
    defaulted("domModification", {}),
    output$1("builder", builder$2),
    defaulted("eventOrder", {})
  ];
  const builder$1 = (detail) => ({
    dom: detail.dom,
    components: detail.components,
    events: derive$2([stopper(focusItem())])
  });
  const schema$o = [
    required$1("dom"),
    required$1("components"),
    output$1("builder", builder$1)
  ];
  const owner$2 = constant$1("item-widget");
  const parts$h = constant$1([required({
    name: "widget",
    overrides: (detail) => {
      return {
        behaviours: derive$1([Representing.config({
          store: {
            mode: "manual",
            getValue: (_component) => {
              return detail.data;
            },
            setValue: noop
          }
        })])
      };
    }
  })]);
  const builder = (detail) => {
    const subs2 = substitutes(owner$2(), detail, parts$h());
    const components2 = components$1(owner$2(), detail, subs2.internals());
    const focusWidget = (component) => getPart(component, detail, "widget").map((widget) => {
      Keying.focusIn(widget);
      return widget;
    });
    const onHorizontalArrow = (component, simulatedEvent) => inside(simulatedEvent.event.target) ? Optional.none() : (() => {
      if (detail.autofocus) {
        simulatedEvent.setSource(component.element);
        return Optional.none();
      } else {
        return Optional.none();
      }
    })();
    return {
      dom: detail.dom,
      components: components2,
      domModification: detail.domModification,
      events: derive$2([
        runOnExecute$1((component, simulatedEvent) => {
          focusWidget(component).each((_widget) => {
            simulatedEvent.stop();
          });
        }),
        run$1(mouseover(), onHover),
        run$1(focusItem(), (component, _simulatedEvent) => {
          if (detail.autofocus) {
            focusWidget(component);
          } else {
            Focusing.focus(component);
          }
        })
      ]),
      behaviours: SketchBehaviours.augment(detail.widgetBehaviours, [
        Representing.config({
          store: {
            mode: "memory",
            initialValue: detail.data
          }
        }),
        Focusing.config({
          ignore: detail.ignoreFocus,
          onFocus: (component) => {
            onFocus$1(component);
          }
        }),
        Keying.config({
          mode: "special",
          focusIn: detail.autofocus ? (component) => {
            focusWidget(component);
          } : revoke(),
          onLeft: onHorizontalArrow,
          onRight: onHorizontalArrow,
          onEscape: (component, simulatedEvent) => {
            if (!Focusing.isFocused(component) && !detail.autofocus) {
              Focusing.focus(component);
              return Optional.some(true);
            } else if (detail.autofocus) {
              simulatedEvent.setSource(component.element);
              return Optional.none();
            } else {
              return Optional.none();
            }
          }
        })
      ])
    };
  };
  const schema$n = [
    required$1("uid"),
    required$1("data"),
    required$1("components"),
    required$1("dom"),
    defaulted("autofocus", false),
    defaulted("ignoreFocus", false),
    SketchBehaviours.field("widgetBehaviours", [
      Representing,
      Focusing,
      Keying
    ]),
    defaulted("domModification", {}),
    defaultUidsSchema(parts$h()),
    output$1("builder", builder)
  ];
  const itemSchema$2 = choose$1("type", {
    widget: schema$n,
    item: schema$p,
    separator: schema$o
  });
  const configureGrid = (detail, movementInfo) => ({
    mode: "flatgrid",
    selector: "." + detail.markers.item,
    initSize: {
      numColumns: movementInfo.initSize.numColumns,
      numRows: movementInfo.initSize.numRows
    },
    focusManager: detail.focusManager
  });
  const configureMatrix = (detail, movementInfo) => ({
    mode: "matrix",
    selectors: {
      row: movementInfo.rowSelector,
      cell: "." + detail.markers.item
    },
    focusManager: detail.focusManager
  });
  const configureMenu = (detail, movementInfo) => ({
    mode: "menu",
    selector: "." + detail.markers.item,
    moveOnTab: movementInfo.moveOnTab,
    focusManager: detail.focusManager
  });
  const parts$g = constant$1([group({
    factory: {
      sketch: (spec) => {
        const itemInfo = asRawOrDie$1("menu.spec item", itemSchema$2, spec);
        return itemInfo.builder(itemInfo);
      }
    },
    name: "items",
    unit: "item",
    defaults: (detail, u) => {
      return has$2(u, "uid") ? u : {
        ...u,
        uid: generate$5("item")
      };
    },
    overrides: (detail, u) => {
      return {
        type: u.type,
        ignoreFocus: detail.fakeFocus,
        domModification: { classes: [detail.markers.item] }
      };
    }
  })]);
  const schema$m = constant$1([
    required$1("value"),
    required$1("items"),
    required$1("dom"),
    required$1("components"),
    defaulted("eventOrder", {}),
    field("menuBehaviours", [
      Highlighting,
      Representing,
      Composing,
      Keying
    ]),
    defaultedOf("movement", {
      mode: "menu",
      moveOnTab: true
    }, choose$1("mode", {
      grid: [
        initSize(),
        output$1("config", configureGrid)
      ],
      matrix: [
        output$1("config", configureMatrix),
        required$1("rowSelector")
      ],
      menu: [
        defaulted("moveOnTab", true),
        output$1("config", configureMenu)
      ]
    })),
    itemMarkers(),
    defaulted("fakeFocus", false),
    defaulted("focusManager", dom$2()),
    onHandler("onHighlight"),
    onHandler("onDehighlight")
  ]);
  const focus = constant$1("alloy.menu-focus");
  const deselectOtherRadioItems = (menu2, item2) => {
    const checkedRadioItems = descendants(menu2.element, '[role="menuitemradio"][aria-checked="true"]');
    each$1(checkedRadioItems, (ele) => {
      if (!eq(ele, item2.element)) {
        menu2.getSystem().getByDom(ele).each((c) => {
          Toggling.off(c);
        });
      }
    });
  };
  const make$7 = (detail, components2, _spec, _externals) => ({
    uid: detail.uid,
    dom: detail.dom,
    markers: detail.markers,
    behaviours: augment(detail.menuBehaviours, [
      Highlighting.config({
        highlightClass: detail.markers.selectedItem,
        itemClass: detail.markers.item,
        onHighlight: detail.onHighlight,
        onDehighlight: detail.onDehighlight
      }),
      Representing.config({
        store: {
          mode: "memory",
          initialValue: detail.value
        }
      }),
      Composing.config({ find: Optional.some }),
      Keying.config(detail.movement.config(detail, detail.movement))
    ]),
    events: derive$2([
      run$1(focus$1(), (menu2, simulatedEvent) => {
        const event2 = simulatedEvent.event;
        menu2.getSystem().getByDom(event2.target).each((item2) => {
          Highlighting.highlight(menu2, item2);
          simulatedEvent.stop();
          emitWith(menu2, focus(), {
            menu: menu2,
            item: item2
          });
        });
      }),
      run$1(hover(), (menu2, simulatedEvent) => {
        const item2 = simulatedEvent.event.item;
        Highlighting.highlight(menu2, item2);
      }),
      run$1(toggled(), (menu2, simulatedEvent) => {
        const { item: item2, state } = simulatedEvent.event;
        if (state && get$f(item2.element, "role") === "menuitemradio") {
          deselectOtherRadioItems(menu2, item2);
        }
      })
    ]),
    components: components2,
    eventOrder: detail.eventOrder,
    domModification: { attributes: { role: "menu" } }
  });
  const Menu = composite({
    name: "Menu",
    configFields: schema$m(),
    partFields: parts$g(),
    factory: make$7
  });
  const transpose$1 = (obj) => tupleMap(obj, (v, k) => ({
    k: v,
    v: k
  }));
  const trace = (items, byItem, byMenu, finish) => get$g(byMenu, finish).bind((triggerItem) => get$g(items, triggerItem).bind((triggerMenu) => {
    const rest = trace(items, byItem, byMenu, triggerMenu);
    return Optional.some([triggerMenu].concat(rest));
  })).getOr([]);
  const generate$2 = (menus, expansions) => {
    const items = {};
    each(menus, (menuItems, menu2) => {
      each$1(menuItems, (item2) => {
        items[item2] = menu2;
      });
    });
    const byItem = expansions;
    const byMenu = transpose$1(expansions);
    const menuPaths = map$1(byMenu, (_triggerItem, submenu) => [submenu].concat(trace(items, byItem, byMenu, submenu)));
    return map$1(items, (menu2) => get$g(menuPaths, menu2).getOr([menu2]));
  };
  const init$c = () => {
    const expansions = Cell({});
    const menus = Cell({});
    const paths = Cell({});
    const primary2 = value$2();
    const directory = Cell({});
    const clear2 = () => {
      expansions.set({});
      menus.set({});
      paths.set({});
      primary2.clear();
    };
    const isClear = () => primary2.get().isNone();
    const setMenuBuilt = (menuName, built) => {
      menus.set({
        ...menus.get(),
        [menuName]: {
          type: "prepared",
          menu: built
        }
      });
    };
    const setContents = (sPrimary, sMenus, sExpansions, dir) => {
      primary2.set(sPrimary);
      expansions.set(sExpansions);
      menus.set(sMenus);
      directory.set(dir);
      const sPaths = generate$2(dir, sExpansions);
      paths.set(sPaths);
    };
    const getTriggeringItem = (menuValue) => find$4(expansions.get(), (v, _k) => v === menuValue);
    const getTriggerData = (menuValue, getItemByValue, path2) => getPreparedMenu(menuValue).bind((menu2) => getTriggeringItem(menuValue).bind((triggeringItemValue) => getItemByValue(triggeringItemValue).map((triggeredItem) => ({
      triggeredMenu: menu2,
      triggeringItem: triggeredItem,
      triggeringPath: path2
    }))));
    const getTriggeringPath = (itemValue, getItemByValue) => {
      const extraPath = filter$2(lookupItem(itemValue).toArray(), (menuValue) => getPreparedMenu(menuValue).isSome());
      return get$g(paths.get(), itemValue).bind((path2) => {
        const revPath = reverse(extraPath.concat(path2));
        const triggers = bind$3(revPath, (menuValue, menuIndex) => getTriggerData(menuValue, getItemByValue, revPath.slice(0, menuIndex + 1)).fold(() => is$1(primary2.get(), menuValue) ? [] : [Optional.none()], (data) => [Optional.some(data)]));
        return sequence(triggers);
      });
    };
    const expand2 = (itemValue) => get$g(expansions.get(), itemValue).map((menu2) => {
      const current = get$g(paths.get(), itemValue).getOr([]);
      return [menu2].concat(current);
    });
    const collapse = (itemValue) => get$g(paths.get(), itemValue).bind((path2) => path2.length > 1 ? Optional.some(path2.slice(1)) : Optional.none());
    const refresh2 = (itemValue) => get$g(paths.get(), itemValue);
    const getPreparedMenu = (menuValue) => lookupMenu(menuValue).bind(extractPreparedMenu);
    const lookupMenu = (menuValue) => get$g(menus.get(), menuValue);
    const lookupItem = (itemValue) => get$g(expansions.get(), itemValue);
    const otherMenus = (path2) => {
      const menuValues = directory.get();
      return difference(keys(menuValues), path2);
    };
    const getPrimary = () => primary2.get().bind(getPreparedMenu);
    const getMenus2 = () => menus.get();
    return {
      setMenuBuilt,
      setContents,
      expand: expand2,
      refresh: refresh2,
      collapse,
      lookupMenu,
      lookupItem,
      otherMenus,
      getPrimary,
      getMenus: getMenus2,
      clear: clear2,
      isClear,
      getTriggeringPath
    };
  };
  const extractPreparedMenu = (prep) => prep.type === "prepared" ? Optional.some(prep.menu) : Optional.none();
  const LayeredState = {
    init: init$c,
    extractPreparedMenu
  };
  const onMenuItemHighlightedEvent = generate$6("tiered-menu-item-highlight");
  const onMenuItemDehighlightedEvent = generate$6("tiered-menu-item-dehighlight");
  var HighlightOnOpen;
  (function(HighlightOnOpen2) {
    HighlightOnOpen2[HighlightOnOpen2["HighlightMenuAndItem"] = 0] = "HighlightMenuAndItem";
    HighlightOnOpen2[HighlightOnOpen2["HighlightJustMenu"] = 1] = "HighlightJustMenu";
    HighlightOnOpen2[HighlightOnOpen2["HighlightNone"] = 2] = "HighlightNone";
  })(HighlightOnOpen || (HighlightOnOpen = {}));
  const make$6 = (detail, _rawUiSpec) => {
    const submenuParentItems = value$2();
    const buildMenus = (container, primaryName, menus) => map$1(menus, (spec, name2) => {
      const makeSketch = () => Menu.sketch({
        ...spec,
        value: name2,
        markers: detail.markers,
        fakeFocus: detail.fakeFocus,
        onHighlight: (menuComp, itemComp) => {
          const highlightData = {
            menuComp,
            itemComp
          };
          emitWith(menuComp, onMenuItemHighlightedEvent, highlightData);
        },
        onDehighlight: (menuComp, itemComp) => {
          const dehighlightData = {
            menuComp,
            itemComp
          };
          emitWith(menuComp, onMenuItemDehighlightedEvent, dehighlightData);
        },
        focusManager: detail.fakeFocus ? highlights() : dom$2()
      });
      return name2 === primaryName ? {
        type: "prepared",
        menu: container.getSystem().build(makeSketch())
      } : {
        type: "notbuilt",
        nbMenu: makeSketch
      };
    });
    const layeredState = LayeredState.init();
    const setup2 = (container) => {
      const componentMap = buildMenus(container, detail.data.primary, detail.data.menus);
      const directory = toDirectory();
      layeredState.setContents(detail.data.primary, componentMap, detail.data.expansions, directory);
      return layeredState.getPrimary();
    };
    const getItemValue = (item2) => Representing.getValue(item2).value;
    const getItemByValue = (_container, menus, itemValue) => findMap(menus, (menu2) => {
      if (!menu2.getSystem().isConnected()) {
        return Optional.none();
      }
      const candidates = Highlighting.getCandidates(menu2);
      return find$5(candidates, (c) => getItemValue(c) === itemValue);
    });
    const toDirectory = (_container) => map$1(detail.data.menus, (data, _menuName) => bind$3(data.items, (item2) => item2.type === "separator" ? [] : [item2.data.value]));
    const setActiveMenu = Highlighting.highlight;
    const setActiveMenuAndItem = (container, menu2) => {
      setActiveMenu(container, menu2);
      Highlighting.getHighlighted(menu2).orThunk(() => Highlighting.getFirst(menu2)).each((item2) => {
        if (detail.fakeFocus) {
          Highlighting.highlight(menu2, item2);
        } else {
          dispatch(container, item2.element, focusItem());
        }
      });
    };
    const getMenus2 = (state, menuValues) => cat(map$2(menuValues, (mv) => state.lookupMenu(mv).bind((prep) => prep.type === "prepared" ? Optional.some(prep.menu) : Optional.none())));
    const closeOthers = (container, state, path2) => {
      const others = getMenus2(state, state.otherMenus(path2));
      each$1(others, (o) => {
        remove$1(o.element, [detail.markers.backgroundMenu]);
        if (!detail.stayInDom) {
          Replacing.remove(container, o);
        }
      });
    };
    const getSubmenuParents = (container) => submenuParentItems.get().getOrThunk(() => {
      const r2 = {};
      const items = descendants(container.element, `.${detail.markers.item}`);
      const parentItems = filter$2(items, (i2) => get$f(i2, "aria-haspopup") === "true");
      each$1(parentItems, (i2) => {
        container.getSystem().getByDom(i2).each((itemComp) => {
          const key = getItemValue(itemComp);
          r2[key] = itemComp;
        });
      });
      submenuParentItems.set(r2);
      return r2;
    });
    const updateAriaExpansions = (container, path2) => {
      const parentItems = getSubmenuParents(container);
      each(parentItems, (v, k) => {
        const expanded = contains$2(path2, k);
        set$9(v.element, "aria-expanded", expanded);
      });
    };
    const updateMenuPath = (container, state, path2) => Optional.from(path2[0]).bind((latestMenuName) => state.lookupMenu(latestMenuName).bind((menuPrep) => {
      if (menuPrep.type === "notbuilt") {
        return Optional.none();
      } else {
        const activeMenu = menuPrep.menu;
        const rest = getMenus2(state, path2.slice(1));
        each$1(rest, (r2) => {
          add$2(r2.element, detail.markers.backgroundMenu);
        });
        if (!inBody(activeMenu.element)) {
          Replacing.append(container, premade(activeMenu));
        }
        remove$1(activeMenu.element, [detail.markers.backgroundMenu]);
        setActiveMenuAndItem(container, activeMenu);
        closeOthers(container, state, path2);
        return Optional.some(activeMenu);
      }
    }));
    let ExpandHighlightDecision;
    (function(ExpandHighlightDecision2) {
      ExpandHighlightDecision2[ExpandHighlightDecision2["HighlightSubmenu"] = 0] = "HighlightSubmenu";
      ExpandHighlightDecision2[ExpandHighlightDecision2["HighlightParent"] = 1] = "HighlightParent";
    })(ExpandHighlightDecision || (ExpandHighlightDecision = {}));
    const buildIfRequired = (container, menuName, menuPrep) => {
      if (menuPrep.type === "notbuilt") {
        const menu2 = container.getSystem().build(menuPrep.nbMenu());
        layeredState.setMenuBuilt(menuName, menu2);
        return menu2;
      } else {
        return menuPrep.menu;
      }
    };
    const expandRight = (container, item2, decision = ExpandHighlightDecision.HighlightSubmenu) => {
      if (item2.hasConfigured(Disabling) && Disabling.isDisabled(item2)) {
        return Optional.some(item2);
      } else {
        const value2 = getItemValue(item2);
        return layeredState.expand(value2).bind((path2) => {
          updateAriaExpansions(container, path2);
          return Optional.from(path2[0]).bind((menuName) => layeredState.lookupMenu(menuName).bind((activeMenuPrep) => {
            const activeMenu = buildIfRequired(container, menuName, activeMenuPrep);
            if (!inBody(activeMenu.element)) {
              Replacing.append(container, premade(activeMenu));
            }
            detail.onOpenSubmenu(container, item2, activeMenu, reverse(path2));
            if (decision === ExpandHighlightDecision.HighlightSubmenu) {
              Highlighting.highlightFirst(activeMenu);
              return updateMenuPath(container, layeredState, path2);
            } else {
              Highlighting.dehighlightAll(activeMenu);
              return Optional.some(item2);
            }
          }));
        });
      }
    };
    const collapseLeft = (container, item2) => {
      const value2 = getItemValue(item2);
      return layeredState.collapse(value2).bind((path2) => {
        updateAriaExpansions(container, path2);
        return updateMenuPath(container, layeredState, path2).map((activeMenu) => {
          detail.onCollapseMenu(container, item2, activeMenu);
          return activeMenu;
        });
      });
    };
    const updateView = (container, item2) => {
      const value2 = getItemValue(item2);
      return layeredState.refresh(value2).bind((path2) => {
        updateAriaExpansions(container, path2);
        return updateMenuPath(container, layeredState, path2);
      });
    };
    const onRight2 = (container, item2) => inside(item2.element) ? Optional.none() : expandRight(container, item2, ExpandHighlightDecision.HighlightSubmenu);
    const onLeft2 = (container, item2) => inside(item2.element) ? Optional.none() : collapseLeft(container, item2);
    const onEscape = (container, item2) => collapseLeft(container, item2).orThunk(() => detail.onEscape(container, item2).map(() => container));
    const keyOnItem = (f) => (container, simulatedEvent) => {
      return closest$1(simulatedEvent.getSource(), `.${detail.markers.item}`).bind((target) => container.getSystem().getByDom(target).toOptional().bind((item2) => f(container, item2).map(always)));
    };
    const events2 = derive$2([
      run$1(focus(), (tmenu, simulatedEvent) => {
        const item2 = simulatedEvent.event.item;
        layeredState.lookupItem(getItemValue(item2)).each(() => {
          const menu2 = simulatedEvent.event.menu;
          Highlighting.highlight(tmenu, menu2);
          const value2 = getItemValue(simulatedEvent.event.item);
          layeredState.refresh(value2).each((path2) => closeOthers(tmenu, layeredState, path2));
        });
      }),
      runOnExecute$1((component, simulatedEvent) => {
        const target = simulatedEvent.event.target;
        component.getSystem().getByDom(target).each((item2) => {
          const itemValue = getItemValue(item2);
          if (itemValue.indexOf("collapse-item") === 0) {
            collapseLeft(component, item2);
          }
          expandRight(component, item2, ExpandHighlightDecision.HighlightSubmenu).fold(() => {
            detail.onExecute(component, item2);
          }, noop);
        });
      }),
      runOnAttached((container, _simulatedEvent) => {
        setup2(container).each((primary2) => {
          Replacing.append(container, premade(primary2));
          detail.onOpenMenu(container, primary2);
          if (detail.highlightOnOpen === HighlightOnOpen.HighlightMenuAndItem) {
            setActiveMenuAndItem(container, primary2);
          } else if (detail.highlightOnOpen === HighlightOnOpen.HighlightJustMenu) {
            setActiveMenu(container, primary2);
          }
        });
      }),
      run$1(onMenuItemHighlightedEvent, (tmenuComp, se) => {
        detail.onHighlightItem(tmenuComp, se.event.menuComp, se.event.itemComp);
      }),
      run$1(onMenuItemDehighlightedEvent, (tmenuComp, se) => {
        detail.onDehighlightItem(tmenuComp, se.event.menuComp, se.event.itemComp);
      }),
      ...detail.navigateOnHover ? [run$1(hover(), (tmenu, simulatedEvent) => {
        const item2 = simulatedEvent.event.item;
        updateView(tmenu, item2);
        expandRight(tmenu, item2, ExpandHighlightDecision.HighlightParent);
        detail.onHover(tmenu, item2);
      })] : []
    ]);
    const getActiveItem = (container) => Highlighting.getHighlighted(container).bind(Highlighting.getHighlighted);
    const collapseMenuApi = (container) => {
      getActiveItem(container).each((currentItem) => {
        collapseLeft(container, currentItem);
      });
    };
    const highlightPrimary = (container) => {
      layeredState.getPrimary().each((primary2) => {
        setActiveMenuAndItem(container, primary2);
      });
    };
    const extractMenuFromContainer = (container) => Optional.from(container.components()[0]).filter((comp) => get$f(comp.element, "role") === "menu");
    const repositionMenus2 = (container) => {
      const maybeActivePrimary = layeredState.getPrimary().bind((primary2) => getActiveItem(container).bind((currentItem) => {
        const itemValue = getItemValue(currentItem);
        const allMenus = values(layeredState.getMenus());
        const preparedMenus = cat(map$2(allMenus, LayeredState.extractPreparedMenu));
        return layeredState.getTriggeringPath(itemValue, (v) => getItemByValue(container, preparedMenus, v));
      }).map((triggeringPath) => ({
        primary: primary2,
        triggeringPath
      })));
      maybeActivePrimary.fold(() => {
        extractMenuFromContainer(container).each((primaryMenu) => {
          detail.onRepositionMenu(container, primaryMenu, []);
        });
      }, ({ primary: primary2, triggeringPath }) => {
        detail.onRepositionMenu(container, primary2, triggeringPath);
      });
    };
    const apis = {
      collapseMenu: collapseMenuApi,
      highlightPrimary,
      repositionMenus: repositionMenus2
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      markers: detail.markers,
      behaviours: augment(detail.tmenuBehaviours, [
        Keying.config({
          mode: "special",
          onRight: keyOnItem(onRight2),
          onLeft: keyOnItem(onLeft2),
          onEscape: keyOnItem(onEscape),
          focusIn: (container, _keyInfo) => {
            layeredState.getPrimary().each((primary2) => {
              dispatch(container, primary2.element, focusItem());
            });
          }
        }),
        Highlighting.config({
          highlightClass: detail.markers.selectedMenu,
          itemClass: detail.markers.menu
        }),
        Composing.config({
          find: (container) => {
            return Highlighting.getHighlighted(container);
          }
        }),
        Replacing.config({})
      ]),
      eventOrder: detail.eventOrder,
      apis,
      events: events2
    };
  };
  const collapseItem$1 = constant$1("collapse-item");
  const tieredData = (primary2, menus, expansions) => ({
    primary: primary2,
    menus,
    expansions
  });
  const singleData = (name2, menu2) => ({
    primary: name2,
    menus: wrap$1(name2, menu2),
    expansions: {}
  });
  const collapseItem = (text2) => ({
    value: generate$6(collapseItem$1()),
    meta: { text: text2 }
  });
  const tieredMenu = single({
    name: "TieredMenu",
    configFields: [
      onStrictKeyboardHandler("onExecute"),
      onStrictKeyboardHandler("onEscape"),
      onStrictHandler("onOpenMenu"),
      onStrictHandler("onOpenSubmenu"),
      onHandler("onRepositionMenu"),
      onHandler("onCollapseMenu"),
      defaulted("highlightOnOpen", HighlightOnOpen.HighlightMenuAndItem),
      requiredObjOf("data", [
        required$1("primary"),
        required$1("menus"),
        required$1("expansions")
      ]),
      defaulted("fakeFocus", false),
      onHandler("onHighlightItem"),
      onHandler("onDehighlightItem"),
      onHandler("onHover"),
      tieredMenuMarkers(),
      required$1("dom"),
      defaulted("navigateOnHover", true),
      defaulted("stayInDom", false),
      field("tmenuBehaviours", [
        Keying,
        Highlighting,
        Composing,
        Replacing
      ]),
      defaulted("eventOrder", {})
    ],
    apis: {
      collapseMenu: (apis, tmenu) => {
        apis.collapseMenu(tmenu);
      },
      highlightPrimary: (apis, tmenu) => {
        apis.highlightPrimary(tmenu);
      },
      repositionMenus: (apis, tmenu) => {
        apis.repositionMenus(tmenu);
      }
    },
    factory: make$6,
    extraApis: {
      tieredData,
      singleData,
      collapseItem
    }
  });
  const makeMenu = (detail, menuSandbox, placementSpec, menuSpec, getBounds2) => {
    const lazySink = () => detail.lazySink(menuSandbox);
    const layouts2 = menuSpec.type === "horizontal" ? {
      layouts: {
        onLtr: () => belowOrAbove(),
        onRtl: () => belowOrAboveRtl()
      }
    } : {};
    const isFirstTierSubmenu = (triggeringPaths) => triggeringPaths.length === 2;
    const getSubmenuLayouts = (triggeringPaths) => isFirstTierSubmenu(triggeringPaths) ? layouts2 : {};
    return tieredMenu.sketch({
      dom: { tag: "div" },
      data: menuSpec.data,
      markers: menuSpec.menu.markers,
      highlightOnOpen: menuSpec.menu.highlightOnOpen,
      fakeFocus: menuSpec.menu.fakeFocus,
      onEscape: () => {
        Sandboxing.close(menuSandbox);
        detail.onEscape.map((handler) => handler(menuSandbox));
        return Optional.some(true);
      },
      onExecute: () => {
        return Optional.some(true);
      },
      onOpenMenu: (tmenu, menu2) => {
        Positioning.positionWithinBounds(lazySink().getOrDie(), menu2, placementSpec, getBounds2());
      },
      onOpenSubmenu: (tmenu, item2, submenu, triggeringPaths) => {
        const sink = lazySink().getOrDie();
        Positioning.position(sink, submenu, {
          anchor: {
            type: "submenu",
            item: item2,
            ...getSubmenuLayouts(triggeringPaths)
          }
        });
      },
      onRepositionMenu: (tmenu, primaryMenu, submenuTriggers) => {
        const sink = lazySink().getOrDie();
        Positioning.positionWithinBounds(sink, primaryMenu, placementSpec, getBounds2());
        each$1(submenuTriggers, (st) => {
          const submenuLayouts = getSubmenuLayouts(st.triggeringPath);
          Positioning.position(sink, st.triggeredMenu, {
            anchor: {
              type: "submenu",
              item: st.triggeringItem,
              ...submenuLayouts
            }
          });
        });
      }
    });
  };
  const factory$m = (detail, spec) => {
    const isPartOfRelated = (sandbox, queryElem) => {
      const related = detail.getRelated(sandbox);
      return related.exists((rel) => isPartOf$1(rel, queryElem));
    };
    const setContent2 = (sandbox, thing) => {
      Sandboxing.setContent(sandbox, thing);
    };
    const showAt = (sandbox, thing, placementSpec) => {
      showWithin(sandbox, thing, placementSpec, Optional.none());
    };
    const showWithin = (sandbox, thing, placementSpec, boxElement) => {
      showWithinBounds(sandbox, thing, placementSpec, () => boxElement.map((elem) => box$1(elem)));
    };
    const showWithinBounds = (sandbox, thing, placementSpec, getBounds2) => {
      const sink = detail.lazySink(sandbox).getOrDie();
      Sandboxing.openWhileCloaked(sandbox, thing, () => Positioning.positionWithinBounds(sink, sandbox, placementSpec, getBounds2()));
      Representing.setValue(sandbox, Optional.some({
        mode: "position",
        config: placementSpec,
        getBounds: getBounds2
      }));
    };
    const showMenuAt = (sandbox, placementSpec, menuSpec) => {
      showMenuWithinBounds(sandbox, placementSpec, menuSpec, Optional.none);
    };
    const showMenuWithinBounds = (sandbox, placementSpec, menuSpec, getBounds2) => {
      const menu2 = makeMenu(detail, sandbox, placementSpec, menuSpec, getBounds2);
      Sandboxing.open(sandbox, menu2);
      Representing.setValue(sandbox, Optional.some({
        mode: "menu",
        menu: menu2
      }));
    };
    const hide = (sandbox) => {
      if (Sandboxing.isOpen(sandbox)) {
        Representing.setValue(sandbox, Optional.none());
        Sandboxing.close(sandbox);
      }
    };
    const getContent = (sandbox) => Sandboxing.getState(sandbox);
    const reposition2 = (sandbox) => {
      if (Sandboxing.isOpen(sandbox)) {
        Representing.getValue(sandbox).each((state) => {
          switch (state.mode) {
            case "menu":
              Sandboxing.getState(sandbox).each(tieredMenu.repositionMenus);
              break;
            case "position":
              const sink = detail.lazySink(sandbox).getOrDie();
              Positioning.positionWithinBounds(sink, sandbox, state.config, state.getBounds());
              break;
          }
        });
      }
    };
    const apis = {
      setContent: setContent2,
      showAt,
      showWithin,
      showWithinBounds,
      showMenuAt,
      showMenuWithinBounds,
      hide,
      getContent,
      reposition: reposition2,
      isOpen: Sandboxing.isOpen
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      behaviours: augment(detail.inlineBehaviours, [
        Sandboxing.config({
          isPartOf: (sandbox, data, queryElem) => {
            return isPartOf$1(data, queryElem) || isPartOfRelated(sandbox, queryElem);
          },
          getAttachPoint: (sandbox) => {
            return detail.lazySink(sandbox).getOrDie();
          },
          onOpen: (sandbox) => {
            detail.onShow(sandbox);
          },
          onClose: (sandbox) => {
            detail.onHide(sandbox);
          }
        }),
        Representing.config({
          store: {
            mode: "memory",
            initialValue: Optional.none()
          }
        }),
        Receiving.config({
          channels: {
            ...receivingChannel$1({
              isExtraPart: spec.isExtraPart,
              ...detail.fireDismissalEventInstead.map((fe) => ({ fireEventInstead: { event: fe.event } })).getOr({})
            }),
            ...receivingChannel({
              ...detail.fireRepositionEventInstead.map((fe) => ({ fireEventInstead: { event: fe.event } })).getOr({}),
              doReposition: reposition2
            })
          }
        })
      ]),
      eventOrder: detail.eventOrder,
      apis
    };
  };
  const InlineView = single({
    name: "InlineView",
    configFields: [
      required$1("lazySink"),
      onHandler("onShow"),
      onHandler("onHide"),
      optionFunction("onEscape"),
      field("inlineBehaviours", [
        Sandboxing,
        Representing,
        Receiving
      ]),
      optionObjOf("fireDismissalEventInstead", [defaulted("event", dismissRequested())]),
      optionObjOf("fireRepositionEventInstead", [defaulted("event", repositionRequested())]),
      defaulted("getRelated", Optional.none),
      defaulted("isExtraPart", never),
      defaulted("eventOrder", Optional.none)
    ],
    factory: factory$m,
    apis: {
      showAt: (apis, component, anchor2, thing) => {
        apis.showAt(component, anchor2, thing);
      },
      showWithin: (apis, component, anchor2, thing, boxElement) => {
        apis.showWithin(component, anchor2, thing, boxElement);
      },
      showWithinBounds: (apis, component, anchor2, thing, bounds2) => {
        apis.showWithinBounds(component, anchor2, thing, bounds2);
      },
      showMenuAt: (apis, component, anchor2, menuSpec) => {
        apis.showMenuAt(component, anchor2, menuSpec);
      },
      showMenuWithinBounds: (apis, component, anchor2, menuSpec, bounds2) => {
        apis.showMenuWithinBounds(component, anchor2, menuSpec, bounds2);
      },
      hide: (apis, component) => {
        apis.hide(component);
      },
      isOpen: (apis, component) => apis.isOpen(component),
      getContent: (apis, component) => apis.getContent(component),
      setContent: (apis, component, thing) => {
        apis.setContent(component, thing);
      },
      reposition: (apis, component) => {
        apis.reposition(component);
      }
    }
  });
  var global$9 = tinymce.util.Tools.resolve("tinymce.util.Delay");
  const factory$l = (detail) => {
    const events2 = events$a(detail.action);
    const tag = detail.dom.tag;
    const lookupAttr = (attr) => get$g(detail.dom, "attributes").bind((attrs) => get$g(attrs, attr));
    const getModAttributes = () => {
      if (tag === "button") {
        const type2 = lookupAttr("type").getOr("button");
        const roleAttrs = lookupAttr("role").map((role) => ({ role })).getOr({});
        return {
          type: type2,
          ...roleAttrs
        };
      } else {
        const role = lookupAttr("role").getOr("button");
        return { role };
      }
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: detail.components,
      events: events2,
      behaviours: SketchBehaviours.augment(detail.buttonBehaviours, [
        Focusing.config({}),
        Keying.config({
          mode: "execution",
          useSpace: true,
          useEnter: true
        })
      ]),
      domModification: { attributes: getModAttributes() },
      eventOrder: detail.eventOrder
    };
  };
  const Button = single({
    name: "Button",
    factory: factory$l,
    configFields: [
      defaulted("uid", void 0),
      required$1("dom"),
      defaulted("components", []),
      SketchBehaviours.field("buttonBehaviours", [
        Focusing,
        Keying
      ]),
      option$3("action"),
      option$3("role"),
      defaulted("eventOrder", {})
    ]
  });
  const record = (spec) => {
    const uid = isSketchSpec(spec) && hasNonNullableKey(spec, "uid") ? spec.uid : generate$5("memento");
    const get2 = (anyInSystem) => anyInSystem.getSystem().getByUid(uid).getOrDie();
    const getOpt2 = (anyInSystem) => anyInSystem.getSystem().getByUid(uid).toOptional();
    const asSpec = () => ({
      ...spec,
      uid
    });
    return {
      get: get2,
      getOpt: getOpt2,
      asSpec
    };
  };
  var global$8 = tinymce.util.Tools.resolve("tinymce.util.I18n");
  const rtlTransform = {
    "indent": true,
    "outdent": true,
    "table-insert-column-after": true,
    "table-insert-column-before": true,
    "paste-column-after": true,
    "paste-column-before": true,
    "unordered-list": true,
    "list-bull-circle": true,
    "list-bull-default": true,
    "list-bull-square": true
  };
  const defaultIconName = "temporary-placeholder";
  const defaultIcon = (icons) => () => get$g(icons, defaultIconName).getOr("!not found!");
  const getIconName = (name2, icons) => {
    const lcName = name2.toLowerCase();
    if (global$8.isRtl()) {
      const rtlName = ensureTrailing(lcName, "-rtl");
      return has$2(icons, rtlName) ? rtlName : lcName;
    } else {
      return lcName;
    }
  };
  const lookupIcon = (name2, icons) => get$g(icons, getIconName(name2, icons));
  const get$2 = (name2, iconProvider) => {
    const icons = iconProvider();
    return lookupIcon(name2, icons).getOrThunk(defaultIcon(icons));
  };
  const getOr = (name2, iconProvider, fallbackIcon) => {
    const icons = iconProvider();
    return lookupIcon(name2, icons).or(fallbackIcon).getOrThunk(defaultIcon(icons));
  };
  const needsRtlTransform = (iconName) => global$8.isRtl() ? has$2(rtlTransform, iconName) : false;
  const addFocusableBehaviour = () => config("add-focusable", [runOnAttached((comp) => {
    child(comp.element, "svg").each((svg) => set$9(svg, "focusable", "false"));
  })]);
  const renderIcon$2 = (spec, iconName, icons, fallbackIcon) => {
    var _a, _b;
    const rtlIconClasses = needsRtlTransform(iconName) ? ["tox-icon--flip"] : [];
    const iconHtml = get$g(icons, getIconName(iconName, icons)).or(fallbackIcon).getOrThunk(defaultIcon(icons));
    return {
      dom: {
        tag: spec.tag,
        attributes: (_a = spec.attributes) !== null && _a !== void 0 ? _a : {},
        classes: spec.classes.concat(rtlIconClasses),
        innerHtml: iconHtml
      },
      behaviours: derive$1([
        ...(_b = spec.behaviours) !== null && _b !== void 0 ? _b : [],
        addFocusableBehaviour()
      ])
    };
  };
  const render$3 = (iconName, spec, iconProvider, fallbackIcon = Optional.none()) => renderIcon$2(spec, iconName, iconProvider(), fallbackIcon);
  const renderFirst = (iconNames, spec, iconProvider) => {
    const icons = iconProvider();
    const iconName = find$5(iconNames, (name2) => has$2(icons, getIconName(name2, icons)));
    return renderIcon$2(spec, iconName.getOr(defaultIconName), icons, Optional.none());
  };
  const notificationIconMap = {
    success: "checkmark",
    error: "warning",
    err: "error",
    warning: "warning",
    warn: "warning",
    info: "info"
  };
  const factory$k = (detail) => {
    const memBannerText = record({
      dom: {
        tag: "p",
        innerHtml: detail.translationProvider(detail.text)
      },
      behaviours: derive$1([Replacing.config({})])
    });
    const renderPercentBar = (percent) => ({
      dom: {
        tag: "div",
        classes: ["tox-bar"],
        styles: { width: `${percent}%` }
      }
    });
    const renderPercentText = (percent) => ({
      dom: {
        tag: "div",
        classes: ["tox-text"],
        innerHtml: `${percent}%`
      }
    });
    const memBannerProgress = record({
      dom: {
        tag: "div",
        classes: detail.progress ? [
          "tox-progress-bar",
          "tox-progress-indicator"
        ] : ["tox-progress-bar"]
      },
      components: [
        {
          dom: {
            tag: "div",
            classes: ["tox-bar-container"]
          },
          components: [renderPercentBar(0)]
        },
        renderPercentText(0)
      ],
      behaviours: derive$1([Replacing.config({})])
    });
    const updateProgress = (comp, percent) => {
      if (comp.getSystem().isConnected()) {
        memBannerProgress.getOpt(comp).each((progress) => {
          Replacing.set(progress, [
            {
              dom: {
                tag: "div",
                classes: ["tox-bar-container"]
              },
              components: [renderPercentBar(percent)]
            },
            renderPercentText(percent)
          ]);
        });
      }
    };
    const updateText = (comp, text2) => {
      if (comp.getSystem().isConnected()) {
        const banner = memBannerText.get(comp);
        Replacing.set(banner, [text$1(text2)]);
      }
    };
    const apis = {
      updateProgress,
      updateText
    };
    const iconChoices = flatten([
      detail.icon.toArray(),
      detail.level.toArray(),
      detail.level.bind((level) => Optional.from(notificationIconMap[level])).toArray()
    ]);
    const memButton = record(Button.sketch({
      dom: {
        tag: "button",
        classes: [
          "tox-notification__dismiss",
          "tox-button",
          "tox-button--naked",
          "tox-button--icon"
        ]
      },
      components: [render$3("close", {
        tag: "div",
        classes: ["tox-icon"],
        attributes: { "aria-label": detail.translationProvider("Close") }
      }, detail.iconProvider)],
      action: (comp) => {
        detail.onAction(comp);
      }
    }));
    const notificationIconSpec = renderFirst(iconChoices, {
      tag: "div",
      classes: ["tox-notification__icon"]
    }, detail.iconProvider);
    const notificationBodySpec = {
      dom: {
        tag: "div",
        classes: ["tox-notification__body"]
      },
      components: [memBannerText.asSpec()],
      behaviours: derive$1([Replacing.config({})])
    };
    const components2 = [
      notificationIconSpec,
      notificationBodySpec
    ];
    return {
      uid: detail.uid,
      dom: {
        tag: "div",
        attributes: { role: "alert" },
        classes: detail.level.map((level) => [
          "tox-notification",
          "tox-notification--in",
          `tox-notification--${level}`
        ]).getOr([
          "tox-notification",
          "tox-notification--in"
        ])
      },
      behaviours: derive$1([
        Focusing.config({}),
        config("notification-events", [run$1(focusin(), (comp) => {
          memButton.getOpt(comp).each(Focusing.focus);
        })])
      ]),
      components: components2.concat(detail.progress ? [memBannerProgress.asSpec()] : []).concat(!detail.closeButton ? [] : [memButton.asSpec()]),
      apis
    };
  };
  const Notification = single({
    name: "Notification",
    factory: factory$k,
    configFields: [
      option$3("level"),
      required$1("progress"),
      option$3("icon"),
      required$1("onAction"),
      required$1("text"),
      required$1("iconProvider"),
      required$1("translationProvider"),
      defaultedBoolean("closeButton", true)
    ],
    apis: {
      updateProgress: (apis, comp, percent) => {
        apis.updateProgress(comp, percent);
      },
      updateText: (apis, comp, text2) => {
        apis.updateText(comp, text2);
      }
    }
  });
  var NotificationManagerImpl = (editor, extras, uiMothership) => {
    const sharedBackstage = extras.backstage.shared;
    const getBounds2 = () => {
      const contentArea = box$1(SugarElement.fromDom(editor.getContentAreaContainer()));
      const win$1 = win();
      const x = clamp(win$1.x, contentArea.x, contentArea.right);
      const y = clamp(win$1.y, contentArea.y, contentArea.bottom);
      const right2 = Math.max(contentArea.right, win$1.right);
      const bottom2 = Math.max(contentArea.bottom, win$1.bottom);
      return Optional.some(bounds(x, y, right2 - x, bottom2 - y));
    };
    const open2 = (settings, closeCallback) => {
      const close3 = () => {
        closeCallback();
        InlineView.hide(notificationWrapper);
      };
      const notification = build$1(Notification.sketch({
        text: settings.text,
        level: contains$2([
          "success",
          "error",
          "warning",
          "warn",
          "info"
        ], settings.type) ? settings.type : void 0,
        progress: settings.progressBar === true,
        icon: settings.icon,
        closeButton: settings.closeButton,
        onAction: close3,
        iconProvider: sharedBackstage.providers.icons,
        translationProvider: sharedBackstage.providers.translate
      }));
      const notificationWrapper = build$1(InlineView.sketch({
        dom: {
          tag: "div",
          classes: ["tox-notifications-container"]
        },
        lazySink: sharedBackstage.getSink,
        fireDismissalEventInstead: {},
        ...sharedBackstage.header.isPositionedAtTop() ? {} : { fireRepositionEventInstead: {} }
      }));
      uiMothership.add(notificationWrapper);
      if (isNumber(settings.timeout) && settings.timeout > 0) {
        global$9.setEditorTimeout(editor, () => {
          close3();
        }, settings.timeout);
      }
      const reposition2 = () => {
        const notificationSpec = premade(notification);
        const anchorOverrides2 = { maxHeightFunction: expandable$1() };
        const allNotifications = editor.notificationManager.getNotifications();
        if (allNotifications[0] === thisNotification) {
          const anchor2 = {
            ...sharedBackstage.anchors.banner(),
            overrides: anchorOverrides2
          };
          InlineView.showWithinBounds(notificationWrapper, notificationSpec, { anchor: anchor2 }, getBounds2);
        } else {
          indexOf(allNotifications, thisNotification).each((idx) => {
            const previousNotification = allNotifications[idx - 1].getEl();
            const nodeAnchor = {
              type: "node",
              root: body(),
              node: Optional.some(SugarElement.fromDom(previousNotification)),
              overrides: anchorOverrides2,
              layouts: {
                onRtl: () => [south$2],
                onLtr: () => [south$2]
              }
            };
            InlineView.showWithinBounds(notificationWrapper, notificationSpec, { anchor: nodeAnchor }, getBounds2);
          });
        }
      };
      const thisNotification = {
        close: close3,
        reposition: reposition2,
        text: (nuText) => {
          Notification.updateText(notification, nuText);
        },
        settings,
        getEl: () => notification.element.dom,
        progressBar: {
          value: (percent) => {
            Notification.updateProgress(notification, percent);
          }
        }
      };
      return thisNotification;
    };
    const close2 = (notification) => {
      notification.close();
    };
    const getArgs = (notification) => {
      return notification.settings;
    };
    return {
      open: open2,
      close: close2,
      getArgs
    };
  };
  var global$7 = tinymce.util.Tools.resolve("tinymce.dom.DOMUtils");
  var global$6 = tinymce.util.Tools.resolve("tinymce.EditorManager");
  var global$5 = tinymce.util.Tools.resolve("tinymce.Env");
  var ToolbarMode$1;
  (function(ToolbarMode2) {
    ToolbarMode2["default"] = "wrap";
    ToolbarMode2["floating"] = "floating";
    ToolbarMode2["sliding"] = "sliding";
    ToolbarMode2["scrolling"] = "scrolling";
  })(ToolbarMode$1 || (ToolbarMode$1 = {}));
  var ToolbarLocation$1;
  (function(ToolbarLocation2) {
    ToolbarLocation2["auto"] = "auto";
    ToolbarLocation2["top"] = "top";
    ToolbarLocation2["bottom"] = "bottom";
  })(ToolbarLocation$1 || (ToolbarLocation$1 = {}));
  const option$2 = (name2) => (editor) => editor.options.get(name2);
  const wrapOptional = (fn) => (editor) => Optional.from(fn(editor));
  const register$e = (editor) => {
    const isPhone = global$5.deviceType.isPhone();
    const isMobile = global$5.deviceType.isTablet() || isPhone;
    const registerOption = editor.options.register;
    const stringOrFalseProcessor = (value2) => isString(value2) || value2 === false;
    const stringOrNumberProcessor = (value2) => isString(value2) || isNumber(value2);
    registerOption("skin", {
      processor: (value2) => isString(value2) || value2 === false,
      default: "oxide"
    });
    registerOption("skin_url", { processor: "string" });
    registerOption("height", {
      processor: stringOrNumberProcessor,
      default: Math.max(editor.getElement().offsetHeight, 400)
    });
    registerOption("width", {
      processor: stringOrNumberProcessor,
      default: global$7.DOM.getStyle(editor.getElement(), "width")
    });
    registerOption("min_height", {
      processor: "number",
      default: 100
    });
    registerOption("min_width", { processor: "number" });
    registerOption("max_height", { processor: "number" });
    registerOption("max_width", { processor: "number" });
    registerOption("style_formats", { processor: "object[]" });
    registerOption("style_formats_merge", {
      processor: "boolean",
      default: false
    });
    registerOption("style_formats_autohide", {
      processor: "boolean",
      default: false
    });
    registerOption("line_height_formats", {
      processor: "string",
      default: "1 1.1 1.2 1.3 1.4 1.5 2"
    });
    registerOption("font_family_formats", {
      processor: "string",
      default: "Andale Mono=andale mono,monospace;Arial=arial,helvetica,sans-serif;Arial Black=arial black,sans-serif;Book Antiqua=book antiqua,palatino,serif;Comic Sans MS=comic sans ms,sans-serif;Courier New=courier new,courier,monospace;Georgia=georgia,palatino,serif;Helvetica=helvetica,arial,sans-serif;Impact=impact,sans-serif;Symbol=symbol;Tahoma=tahoma,arial,helvetica,sans-serif;Terminal=terminal,monaco,monospace;Times New Roman=times new roman,times,serif;Trebuchet MS=trebuchet ms,geneva,sans-serif;Verdana=verdana,geneva,sans-serif;Webdings=webdings;Wingdings=wingdings,zapf dingbats"
    });
    registerOption("font_size_formats", {
      processor: "string",
      default: "8pt 10pt 12pt 14pt 18pt 24pt 36pt"
    });
    registerOption("block_formats", {
      processor: "string",
      default: "Paragraph=p;Heading 1=h1;Heading 2=h2;Heading 3=h3;Heading 4=h4;Heading 5=h5;Heading 6=h6;Preformatted=pre"
    });
    registerOption("content_langs", { processor: "object[]" });
    registerOption("removed_menuitems", {
      processor: "string",
      default: ""
    });
    registerOption("menubar", {
      processor: (value2) => isString(value2) || isBoolean(value2),
      default: !isPhone
    });
    registerOption("menu", {
      processor: "object",
      default: {}
    });
    registerOption("toolbar", {
      processor: (value2) => {
        if (isBoolean(value2) || isString(value2) || isArray(value2)) {
          return {
            value: value2,
            valid: true
          };
        } else {
          return {
            valid: false,
            message: "Must be a boolean, string or array."
          };
        }
      },
      default: true
    });
    range$2(9, (num) => {
      registerOption("toolbar" + (num + 1), { processor: "string" });
    });
    registerOption("toolbar_mode", {
      processor: "string",
      default: isMobile ? "scrolling" : "floating"
    });
    registerOption("toolbar_groups", {
      processor: "object",
      default: {}
    });
    registerOption("toolbar_location", {
      processor: "string",
      default: ToolbarLocation$1.auto
    });
    registerOption("toolbar_persist", {
      processor: "boolean",
      default: false
    });
    registerOption("toolbar_sticky", {
      processor: "boolean",
      default: editor.inline
    });
    registerOption("toolbar_sticky_offset", {
      processor: "number",
      default: 0
    });
    registerOption("fixed_toolbar_container", {
      processor: "string",
      default: ""
    });
    registerOption("fixed_toolbar_container_target", { processor: "object" });
    registerOption("file_picker_callback", { processor: "function" });
    registerOption("file_picker_validator_handler", { processor: "function" });
    registerOption("file_picker_types", { processor: "string" });
    registerOption("typeahead_urls", {
      processor: "boolean",
      default: true
    });
    registerOption("anchor_top", {
      processor: stringOrFalseProcessor,
      default: "#top"
    });
    registerOption("anchor_bottom", {
      processor: stringOrFalseProcessor,
      default: "#bottom"
    });
    registerOption("draggable_modal", {
      processor: "boolean",
      default: false
    });
    registerOption("statusbar", {
      processor: "boolean",
      default: true
    });
    registerOption("elementpath", {
      processor: "boolean",
      default: true
    });
    registerOption("branding", {
      processor: "boolean",
      default: true
    });
    registerOption("promotion", {
      processor: "boolean",
      default: true
    });
    registerOption("resize", {
      processor: (value2) => value2 === "both" || isBoolean(value2),
      default: !global$5.deviceType.isTouch()
    });
    registerOption("sidebar_show", { processor: "string" });
  };
  const isReadOnly = option$2("readonly");
  const getHeightOption = option$2("height");
  const getWidthOption = option$2("width");
  const getMinWidthOption = wrapOptional(option$2("min_width"));
  const getMinHeightOption = wrapOptional(option$2("min_height"));
  const getMaxWidthOption = wrapOptional(option$2("max_width"));
  const getMaxHeightOption = wrapOptional(option$2("max_height"));
  const getUserStyleFormats = wrapOptional(option$2("style_formats"));
  const shouldMergeStyleFormats = option$2("style_formats_merge");
  const shouldAutoHideStyleFormats = option$2("style_formats_autohide");
  const getContentLanguages = option$2("content_langs");
  const getRemovedMenuItems = option$2("removed_menuitems");
  const getToolbarMode = option$2("toolbar_mode");
  const getToolbarGroups = option$2("toolbar_groups");
  const getToolbarLocation = option$2("toolbar_location");
  const fixedContainerSelector = option$2("fixed_toolbar_container");
  const fixedToolbarContainerTarget = option$2("fixed_toolbar_container_target");
  const isToolbarPersist = option$2("toolbar_persist");
  const getStickyToolbarOffset = option$2("toolbar_sticky_offset");
  const getMenubar = option$2("menubar");
  const getToolbar = option$2("toolbar");
  const getFilePickerCallback = option$2("file_picker_callback");
  const getFilePickerValidatorHandler = option$2("file_picker_validator_handler");
  const getFilePickerTypes = option$2("file_picker_types");
  const useTypeaheadUrls = option$2("typeahead_urls");
  const getAnchorTop = option$2("anchor_top");
  const getAnchorBottom = option$2("anchor_bottom");
  const isDraggableModal$1 = option$2("draggable_modal");
  const useStatusBar = option$2("statusbar");
  const useElementPath = option$2("elementpath");
  const useBranding = option$2("branding");
  const getResize = option$2("resize");
  const getPasteAsText = option$2("paste_as_text");
  const getSidebarShow = option$2("sidebar_show");
  const promotionEnabled = option$2("promotion");
  const isSkinDisabled = (editor) => editor.options.get("skin") === false;
  const isMenubarEnabled = (editor) => editor.options.get("menubar") !== false;
  const getSkinUrl = (editor) => {
    const skinUrl = editor.options.get("skin_url");
    if (isSkinDisabled(editor)) {
      return skinUrl;
    } else {
      if (skinUrl) {
        return editor.documentBaseURI.toAbsolute(skinUrl);
      } else {
        const skin = editor.options.get("skin");
        return global$6.baseURL + "/skins/ui/" + skin;
      }
    }
  };
  const getLineHeightFormats = (editor) => editor.options.get("line_height_formats").split(" ");
  const isToolbarEnabled = (editor) => {
    const toolbar = getToolbar(editor);
    const isToolbarString = isString(toolbar);
    const isToolbarObjectArray = isArray(toolbar) && toolbar.length > 0;
    return !isMultipleToolbars(editor) && (isToolbarObjectArray || isToolbarString || toolbar === true);
  };
  const getMultipleToolbarsOption = (editor) => {
    const toolbars = range$2(9, (num) => editor.options.get("toolbar" + (num + 1)));
    const toolbarArray = filter$2(toolbars, isString);
    return someIf(toolbarArray.length > 0, toolbarArray);
  };
  const isMultipleToolbars = (editor) => getMultipleToolbarsOption(editor).fold(() => {
    const toolbar = getToolbar(editor);
    return isArrayOf(toolbar, isString) && toolbar.length > 0;
  }, always);
  const isToolbarLocationBottom = (editor) => getToolbarLocation(editor) === ToolbarLocation$1.bottom;
  const fixedContainerTarget = (editor) => {
    var _a;
    if (!editor.inline) {
      return Optional.none();
    }
    const selector = (_a = fixedContainerSelector(editor)) !== null && _a !== void 0 ? _a : "";
    if (selector.length > 0) {
      return descendant(body(), selector);
    }
    const element2 = fixedToolbarContainerTarget(editor);
    if (isNonNullable(element2)) {
      return Optional.some(SugarElement.fromDom(element2));
    }
    return Optional.none();
  };
  const useFixedContainer = (editor) => editor.inline && fixedContainerTarget(editor).isSome();
  const getUiContainer = (editor) => {
    const fixedContainer = fixedContainerTarget(editor);
    return fixedContainer.getOrThunk(() => getContentContainer(getRootNode(SugarElement.fromDom(editor.getElement()))));
  };
  const isDistractionFree = (editor) => editor.inline && !isMenubarEnabled(editor) && !isToolbarEnabled(editor) && !isMultipleToolbars(editor);
  const isStickyToolbar = (editor) => {
    const isStickyToolbar2 = editor.options.get("toolbar_sticky");
    return (isStickyToolbar2 || editor.inline) && !useFixedContainer(editor) && !isDistractionFree(editor);
  };
  const getMenus = (editor) => {
    const menu2 = editor.options.get("menu");
    return map$1(menu2, (menu3) => ({
      ...menu3,
      items: menu3.items
    }));
  };
  var Options = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    get ToolbarMode() {
      return ToolbarMode$1;
    },
    get ToolbarLocation() {
      return ToolbarLocation$1;
    },
    register: register$e,
    getSkinUrl,
    isReadOnly,
    isSkinDisabled,
    getHeightOption,
    getWidthOption,
    getMinWidthOption,
    getMinHeightOption,
    getMaxWidthOption,
    getMaxHeightOption,
    getUserStyleFormats,
    shouldMergeStyleFormats,
    shouldAutoHideStyleFormats,
    getLineHeightFormats,
    getContentLanguages,
    getRemovedMenuItems,
    isMenubarEnabled,
    isMultipleToolbars,
    isToolbarEnabled,
    isToolbarPersist,
    getMultipleToolbarsOption,
    getUiContainer,
    useFixedContainer,
    getToolbarMode,
    isDraggableModal: isDraggableModal$1,
    isDistractionFree,
    isStickyToolbar,
    getStickyToolbarOffset,
    getToolbarLocation,
    isToolbarLocationBottom,
    getToolbarGroups,
    getMenus,
    getMenubar,
    getToolbar,
    getFilePickerCallback,
    getFilePickerTypes,
    useTypeaheadUrls,
    getAnchorTop,
    getAnchorBottom,
    getFilePickerValidatorHandler,
    useStatusBar,
    useElementPath,
    promotionEnabled,
    useBranding,
    getResize,
    getPasteAsText,
    getSidebarShow
  });
  const autocompleteSelector = "[data-mce-autocompleter]";
  const detect = (elm) => closest$1(elm, autocompleteSelector);
  const findIn = (elm) => descendant(elm, autocompleteSelector);
  const setup$e = (api2, editor) => {
    const redirectKeyToItem = (item2, e) => {
      emitWith(item2, keydown(), { raw: e });
    };
    const getItem = () => api2.getMenu().bind(Highlighting.getHighlighted);
    editor.on("keydown", (e) => {
      const keyCode = e.which;
      if (!api2.isActive()) {
        return;
      }
      if (api2.isMenuOpen()) {
        if (keyCode === 13) {
          getItem().each(emitExecute);
          e.preventDefault();
        } else if (keyCode === 40) {
          getItem().fold(() => {
            api2.getMenu().each(Highlighting.highlightFirst);
          }, (item2) => {
            redirectKeyToItem(item2, e);
          });
          e.preventDefault();
          e.stopImmediatePropagation();
        } else if (keyCode === 37 || keyCode === 38 || keyCode === 39) {
          getItem().each((item2) => {
            redirectKeyToItem(item2, e);
            e.preventDefault();
            e.stopImmediatePropagation();
          });
        }
      } else {
        if (keyCode === 13 || keyCode === 38 || keyCode === 40) {
          api2.cancelIfNecessary();
        }
      }
    });
    editor.on("NodeChange", (e) => {
      if (api2.isActive() && !api2.isProcessingAction() && detect(SugarElement.fromDom(e.element)).isNone()) {
        api2.cancelIfNecessary();
      }
    });
  };
  const AutocompleterEditorEvents = { setup: setup$e };
  var ItemResponse;
  (function(ItemResponse2) {
    ItemResponse2[ItemResponse2["CLOSE_ON_EXECUTE"] = 0] = "CLOSE_ON_EXECUTE";
    ItemResponse2[ItemResponse2["BUBBLE_TO_SANDBOX"] = 1] = "BUBBLE_TO_SANDBOX";
  })(ItemResponse || (ItemResponse = {}));
  var ItemResponse$1 = ItemResponse;
  const navClass = "tox-menu-nav__js";
  const selectableClass = "tox-collection__item";
  const colorClass = "tox-swatch";
  const presetClasses = {
    normal: navClass,
    color: colorClass
  };
  const tickedClass = "tox-collection__item--enabled";
  const groupHeadingClass = "tox-collection__group-heading";
  const iconClass = "tox-collection__item-icon";
  const textClass = "tox-collection__item-label";
  const accessoryClass = "tox-collection__item-accessory";
  const caretClass = "tox-collection__item-caret";
  const checkmarkClass = "tox-collection__item-checkmark";
  const activeClass = "tox-collection__item--active";
  const containerClass = "tox-collection__item-container";
  const containerColumnClass = "tox-collection__item-container--column";
  const containerRowClass = "tox-collection__item-container--row";
  const containerAlignRightClass = "tox-collection__item-container--align-right";
  const containerAlignLeftClass = "tox-collection__item-container--align-left";
  const containerValignTopClass = "tox-collection__item-container--valign-top";
  const containerValignMiddleClass = "tox-collection__item-container--valign-middle";
  const containerValignBottomClass = "tox-collection__item-container--valign-bottom";
  const classForPreset = (presets) => get$g(presetClasses, presets).getOr(navClass);
  const forMenu = (presets) => {
    if (presets === "color") {
      return "tox-swatches";
    } else {
      return "tox-menu";
    }
  };
  const classes = (presets) => ({
    backgroundMenu: "tox-background-menu",
    selectedMenu: "tox-selected-menu",
    selectedItem: "tox-collection__item--active",
    hasIcons: "tox-menu--has-icons",
    menu: forMenu(presets),
    tieredMenu: "tox-tiered-menu"
  });
  const markers = (presets) => {
    const menuClasses = classes(presets);
    return {
      backgroundMenu: menuClasses.backgroundMenu,
      selectedMenu: menuClasses.selectedMenu,
      menu: menuClasses.menu,
      selectedItem: menuClasses.selectedItem,
      item: classForPreset(presets)
    };
  };
  const dom$1 = (hasIcons, columns, presets) => {
    const menuClasses = classes(presets);
    return {
      tag: "div",
      classes: flatten([
        [
          menuClasses.menu,
          `tox-menu-${columns}-column`
        ],
        hasIcons ? [menuClasses.hasIcons] : []
      ])
    };
  };
  const components = [Menu.parts.items({})];
  const part = (hasIcons, columns, presets) => {
    const menuClasses = classes(presets);
    const d = {
      tag: "div",
      classes: flatten([[menuClasses.tieredMenu]])
    };
    return {
      dom: d,
      markers: markers(presets)
    };
  };
  const schema$l = constant$1([
    option$3("data"),
    defaulted("inputAttributes", {}),
    defaulted("inputStyles", {}),
    defaulted("tag", "input"),
    defaulted("inputClasses", []),
    onHandler("onSetValue"),
    defaulted("styles", {}),
    defaulted("eventOrder", {}),
    field("inputBehaviours", [
      Representing,
      Focusing
    ]),
    defaulted("selectOnFocus", true)
  ]);
  const focusBehaviours = (detail) => derive$1([Focusing.config({
    onFocus: !detail.selectOnFocus ? noop : (component) => {
      const input2 = component.element;
      const value2 = get$6(input2);
      input2.dom.setSelectionRange(0, value2.length);
    }
  })]);
  const behaviours = (detail) => ({
    ...focusBehaviours(detail),
    ...augment(detail.inputBehaviours, [Representing.config({
      store: {
        mode: "manual",
        ...detail.data.map((data) => ({ initialValue: data })).getOr({}),
        getValue: (input2) => {
          return get$6(input2.element);
        },
        setValue: (input2, data) => {
          const current = get$6(input2.element);
          if (current !== data) {
            set$5(input2.element, data);
          }
        }
      },
      onSetValue: detail.onSetValue
    })])
  });
  const dom = (detail) => ({
    tag: detail.tag,
    attributes: {
      type: "text",
      ...detail.inputAttributes
    },
    styles: detail.inputStyles,
    classes: detail.inputClasses
  });
  const factory$j = (detail, _spec) => ({
    uid: detail.uid,
    dom: dom(detail),
    components: [],
    behaviours: behaviours(detail),
    eventOrder: detail.eventOrder
  });
  const Input = single({
    name: "Input",
    configFields: schema$l(),
    factory: factory$j
  });
  const refetchTriggerEvent = generate$6("refetch-trigger-event");
  const redirectMenuItemInteractionEvent = generate$6("redirect-menu-item-interaction");
  const menuSearcherClass = "tox-menu__searcher";
  const findWithinSandbox = (sandboxComp) => {
    return descendant(sandboxComp.element, `.${menuSearcherClass}`).bind((inputElem) => sandboxComp.getSystem().getByDom(inputElem).toOptional());
  };
  const findWithinMenu = findWithinSandbox;
  const restoreState = (inputComp, searcherState) => {
    Representing.setValue(inputComp, searcherState.fetchPattern);
    inputComp.element.dom.selectionStart = searcherState.selectionStart;
    inputComp.element.dom.selectionEnd = searcherState.selectionEnd;
  };
  const saveState = (inputComp) => {
    const fetchPattern = Representing.getValue(inputComp);
    const selectionStart = inputComp.element.dom.selectionStart;
    const selectionEnd = inputComp.element.dom.selectionEnd;
    return {
      fetchPattern,
      selectionStart,
      selectionEnd
    };
  };
  const setActiveDescendant = (inputComp, active2) => {
    getOpt(active2.element, "id").each((id) => set$9(inputComp.element, "aria-activedescendant", id));
  };
  const renderMenuSearcher = (spec) => {
    const handleByBrowser = (comp, se) => {
      se.cut();
      return Optional.none();
    };
    const handleByHighlightedItem = (comp, se) => {
      const eventData = {
        interactionEvent: se.event,
        eventType: se.event.raw.type
      };
      emitWith(comp, redirectMenuItemInteractionEvent, eventData);
      return Optional.some(true);
    };
    const customSearcherEventsName = "searcher-events";
    return {
      dom: {
        tag: "div",
        classes: [selectableClass]
      },
      components: [Input.sketch({
        inputClasses: [
          menuSearcherClass,
          "tox-textfield"
        ],
        inputAttributes: {
          ...spec.placeholder.map((placeholder2) => ({ placeholder: spec.i18n(placeholder2) })).getOr({}),
          "type": "search",
          "aria-autocomplete": "list"
        },
        inputBehaviours: derive$1([
          config(customSearcherEventsName, [
            run$1(input(), (inputComp) => {
              emit(inputComp, refetchTriggerEvent);
            }),
            run$1(keydown(), (inputComp, se) => {
              if (se.event.raw.key === "Escape") {
                se.stop();
              }
            })
          ]),
          Keying.config({
            mode: "special",
            onLeft: handleByBrowser,
            onRight: handleByBrowser,
            onSpace: handleByBrowser,
            onEnter: handleByHighlightedItem,
            onEscape: handleByHighlightedItem,
            onUp: handleByHighlightedItem,
            onDown: handleByHighlightedItem
          })
        ]),
        eventOrder: {
          keydown: [
            customSearcherEventsName,
            Keying.name()
          ]
        }
      })]
    };
  };
  const searchResultsClass = "tox-collection--results__js";
  const augmentWithAria = (item2) => {
    var _a;
    if (item2.dom) {
      return {
        ...item2,
        dom: {
          ...item2.dom,
          attributes: {
            ...(_a = item2.dom.attributes) !== null && _a !== void 0 ? _a : {},
            "id": generate$6("aria-item-search-result-id"),
            "aria-selected": "false"
          }
        }
      };
    } else {
      return item2;
    }
  };
  const chunk = (rowDom, numColumns) => (items) => {
    const chunks = chunk$1(items, numColumns);
    return map$2(chunks, (c) => ({
      dom: rowDom,
      components: c
    }));
  };
  const forSwatch = (columns) => ({
    dom: {
      tag: "div",
      classes: [
        "tox-menu",
        "tox-swatches-menu"
      ]
    },
    components: [{
      dom: {
        tag: "div",
        classes: ["tox-swatches"]
      },
      components: [Menu.parts.items({
        preprocess: columns !== "auto" ? chunk({
          tag: "div",
          classes: ["tox-swatches__row"]
        }, columns) : identity
      })]
    }]
  });
  const forToolbar = (columns) => ({
    dom: {
      tag: "div",
      classes: [
        "tox-menu",
        "tox-collection",
        "tox-collection--toolbar",
        "tox-collection--toolbar-lg"
      ]
    },
    components: [Menu.parts.items({
      preprocess: chunk({
        tag: "div",
        classes: ["tox-collection__group"]
      }, columns)
    })]
  });
  const preprocessCollection = (items, isSeparator2) => {
    const allSplits = [];
    let currentSplit = [];
    each$1(items, (item2, i2) => {
      if (isSeparator2(item2, i2)) {
        if (currentSplit.length > 0) {
          allSplits.push(currentSplit);
        }
        currentSplit = [];
        if (has$2(item2.dom, "innerHtml") || item2.components && item2.components.length > 0) {
          currentSplit.push(item2);
        }
      } else {
        currentSplit.push(item2);
      }
    });
    if (currentSplit.length > 0) {
      allSplits.push(currentSplit);
    }
    return map$2(allSplits, (s) => ({
      dom: {
        tag: "div",
        classes: ["tox-collection__group"]
      },
      components: s
    }));
  };
  const insertItemsPlaceholder = (columns, initItems, onItem) => {
    return Menu.parts.items({
      preprocess: (rawItems) => {
        const enrichedItems = map$2(rawItems, onItem);
        if (columns !== "auto" && columns > 1) {
          return chunk({
            tag: "div",
            classes: ["tox-collection__group"]
          }, columns)(enrichedItems);
        } else {
          return preprocessCollection(enrichedItems, (_item, i2) => initItems[i2].type === "separator");
        }
      }
    });
  };
  const forCollection = (columns, initItems, _hasIcons = true) => ({
    dom: {
      tag: "div",
      classes: [
        "tox-menu",
        "tox-collection"
      ].concat(columns === 1 ? ["tox-collection--list"] : ["tox-collection--grid"])
    },
    components: [insertItemsPlaceholder(columns, initItems, identity)]
  });
  const forCollectionWithSearchResults = (columns, initItems, _hasIcons = true) => {
    const ariaControlsSearchResults = generate$6("aria-controls-search-results");
    return {
      dom: {
        tag: "div",
        classes: [
          "tox-menu",
          "tox-collection",
          searchResultsClass
        ].concat(columns === 1 ? ["tox-collection--list"] : ["tox-collection--grid"]),
        attributes: { id: ariaControlsSearchResults }
      },
      components: [insertItemsPlaceholder(columns, initItems, augmentWithAria)]
    };
  };
  const forCollectionWithSearchField = (columns, initItems, searchField) => {
    const ariaControlsSearchResults = generate$6("aria-controls-search-results");
    return {
      dom: {
        tag: "div",
        classes: [
          "tox-menu",
          "tox-collection"
        ].concat(columns === 1 ? ["tox-collection--list"] : ["tox-collection--grid"])
      },
      components: [
        renderMenuSearcher({
          i18n: global$8.translate,
          placeholder: searchField.placeholder
        }),
        {
          dom: {
            tag: "div",
            classes: [
              ...columns === 1 ? ["tox-collection--list"] : ["tox-collection--grid"],
              searchResultsClass
            ],
            attributes: { id: ariaControlsSearchResults }
          },
          components: [insertItemsPlaceholder(columns, initItems, augmentWithAria)]
        }
      ]
    };
  };
  const forHorizontalCollection = (initItems, _hasIcons = true) => ({
    dom: {
      tag: "div",
      classes: [
        "tox-collection",
        "tox-collection--horizontal"
      ]
    },
    components: [Menu.parts.items({ preprocess: (items) => preprocessCollection(items, (_item, i2) => initItems[i2].type === "separator") })]
  });
  const menuHasIcons = (xs) => exists(xs, (item2) => "icon" in item2 && item2.icon !== void 0);
  const handleError = (error2) => {
    console.error(formatError(error2));
    console.log(error2);
    return Optional.none();
  };
  const createHorizontalPartialMenuWithAlloyItems = (value2, _hasIcons, items, _columns, _menuLayout) => {
    const structure = forHorizontalCollection(items);
    return {
      value: value2,
      dom: structure.dom,
      components: structure.components,
      items
    };
  };
  const createPartialMenuWithAlloyItems = (value2, hasIcons, items, columns, menuLayout) => {
    const getNormalStructure = () => {
      if (menuLayout.menuType !== "searchable") {
        return forCollection(columns, items);
      } else {
        return menuLayout.searchMode.searchMode === "search-with-field" ? forCollectionWithSearchField(columns, items, menuLayout.searchMode) : forCollectionWithSearchResults(columns, items);
      }
    };
    if (menuLayout.menuType === "color") {
      const structure = forSwatch(columns);
      return {
        value: value2,
        dom: structure.dom,
        components: structure.components,
        items
      };
    } else if (menuLayout.menuType === "normal" && columns === "auto") {
      const structure = forCollection(columns, items);
      return {
        value: value2,
        dom: structure.dom,
        components: structure.components,
        items
      };
    } else if (menuLayout.menuType === "normal" || menuLayout.menuType === "searchable") {
      const structure = getNormalStructure();
      return {
        value: value2,
        dom: structure.dom,
        components: structure.components,
        items
      };
    } else if (menuLayout.menuType === "listpreview" && columns !== "auto") {
      const structure = forToolbar(columns);
      return {
        value: value2,
        dom: structure.dom,
        components: structure.components,
        items
      };
    } else {
      return {
        value: value2,
        dom: dom$1(hasIcons, columns, menuLayout.menuType),
        components,
        items
      };
    }
  };
  const type = requiredString("type");
  const name$1 = requiredString("name");
  const label = requiredString("label");
  const text = requiredString("text");
  const title = requiredString("title");
  const icon = requiredString("icon");
  const value$1 = requiredString("value");
  const fetch$1 = requiredFunction("fetch");
  const getSubmenuItems = requiredFunction("getSubmenuItems");
  const onAction = requiredFunction("onAction");
  const onItemAction = requiredFunction("onItemAction");
  const onSetup = defaultedFunction("onSetup", () => noop);
  const optionalName = optionString("name");
  const optionalText = optionString("text");
  const optionalIcon = optionString("icon");
  const optionalTooltip = optionString("tooltip");
  const optionalLabel = optionString("label");
  const optionalShortcut = optionString("shortcut");
  const optionalSelect = optionFunction("select");
  const active = defaultedBoolean("active", false);
  const borderless = defaultedBoolean("borderless", false);
  const enabled = defaultedBoolean("enabled", true);
  const primary = defaultedBoolean("primary", false);
  const defaultedColumns = (num) => defaulted("columns", num);
  const defaultedMeta = defaulted("meta", {});
  const defaultedOnAction = defaultedFunction("onAction", noop);
  const defaultedType = (type2) => defaultedString("type", type2);
  const generatedName = (namePrefix) => field$1("name", "name", defaultedThunk(() => generate$6(`${namePrefix}-name`)), string);
  const generatedValue = (valuePrefix) => field$1("value", "value", defaultedThunk(() => generate$6(`${valuePrefix}-value`)), anyValue());
  const separatorMenuItemSchema = objOf([
    type,
    optionalText
  ]);
  const createSeparatorMenuItem = (spec) => asRaw("separatormenuitem", separatorMenuItemSchema, spec);
  const autocompleterItemSchema = objOf([
    defaultedType("autocompleteitem"),
    active,
    enabled,
    defaultedMeta,
    value$1,
    optionalText,
    optionalIcon
  ]);
  const createSeparatorItem = (spec) => asRaw("Autocompleter.Separator", separatorMenuItemSchema, spec);
  const createAutocompleterItem = (spec) => asRaw("Autocompleter.Item", autocompleterItemSchema, spec);
  const baseToolbarButtonFields = [
    enabled,
    optionalTooltip,
    optionalIcon,
    optionalText,
    onSetup
  ];
  const toolbarButtonSchema = objOf([
    type,
    onAction
  ].concat(baseToolbarButtonFields));
  const createToolbarButton = (spec) => asRaw("toolbarbutton", toolbarButtonSchema, spec);
  const baseToolbarToggleButtonFields = [active].concat(baseToolbarButtonFields);
  const toggleButtonSchema = objOf(baseToolbarToggleButtonFields.concat([
    type,
    onAction
  ]));
  const createToggleButton = (spec) => asRaw("ToggleButton", toggleButtonSchema, spec);
  const contextBarFields = [
    defaultedFunction("predicate", never),
    defaultedStringEnum("scope", "node", [
      "node",
      "editor"
    ]),
    defaultedStringEnum("position", "selection", [
      "node",
      "selection",
      "line"
    ])
  ];
  const contextButtonFields = baseToolbarButtonFields.concat([
    defaultedType("contextformbutton"),
    primary,
    onAction,
    customField("original", identity)
  ]);
  const contextToggleButtonFields = baseToolbarToggleButtonFields.concat([
    defaultedType("contextformbutton"),
    primary,
    onAction,
    customField("original", identity)
  ]);
  const launchButtonFields = baseToolbarButtonFields.concat([defaultedType("contextformbutton")]);
  const launchToggleButtonFields = baseToolbarToggleButtonFields.concat([defaultedType("contextformtogglebutton")]);
  const toggleOrNormal = choose$1("type", {
    contextformbutton: contextButtonFields,
    contextformtogglebutton: contextToggleButtonFields
  });
  const contextFormSchema = objOf([
    defaultedType("contextform"),
    defaultedFunction("initValue", constant$1("")),
    optionalLabel,
    requiredArrayOf("commands", toggleOrNormal),
    optionOf("launch", choose$1("type", {
      contextformbutton: launchButtonFields,
      contextformtogglebutton: launchToggleButtonFields
    }))
  ].concat(contextBarFields));
  const createContextForm = (spec) => asRaw("ContextForm", contextFormSchema, spec);
  const contextToolbarSchema = objOf([
    defaultedType("contexttoolbar"),
    requiredString("items")
  ].concat(contextBarFields));
  const createContextToolbar = (spec) => asRaw("ContextToolbar", contextToolbarSchema, spec);
  const cardImageFields = [
    type,
    requiredString("src"),
    optionString("alt"),
    defaultedArrayOf("classes", [], string)
  ];
  const cardImageSchema = objOf(cardImageFields);
  const cardTextFields = [
    type,
    text,
    optionalName,
    defaultedArrayOf("classes", ["tox-collection__item-label"], string)
  ];
  const cardTextSchema = objOf(cardTextFields);
  const itemSchema$1 = valueThunk(() => choose$2("type", {
    cardimage: cardImageSchema,
    cardtext: cardTextSchema,
    cardcontainer: cardContainerSchema
  }));
  const cardContainerSchema = objOf([
    type,
    defaultedString("direction", "horizontal"),
    defaultedString("align", "left"),
    defaultedString("valign", "middle"),
    requiredArrayOf("items", itemSchema$1)
  ]);
  const commonMenuItemFields = [
    enabled,
    optionalText,
    optionalShortcut,
    generatedValue("menuitem"),
    defaultedMeta
  ];
  const cardMenuItemSchema = objOf([
    type,
    optionalLabel,
    requiredArrayOf("items", itemSchema$1),
    onSetup,
    defaultedOnAction
  ].concat(commonMenuItemFields));
  const createCardMenuItem = (spec) => asRaw("cardmenuitem", cardMenuItemSchema, spec);
  const choiceMenuItemSchema = objOf([
    type,
    active,
    optionalIcon
  ].concat(commonMenuItemFields));
  const createChoiceMenuItem = (spec) => asRaw("choicemenuitem", choiceMenuItemSchema, spec);
  const baseFields = [
    type,
    requiredString("fancytype"),
    defaultedOnAction
  ];
  const insertTableFields = [defaulted("initData", {})].concat(baseFields);
  const colorSwatchFields = [defaultedObjOf("initData", {}, [
    defaultedBoolean("allowCustomColors", true),
    optionArrayOf("colors", anyValue())
  ])].concat(baseFields);
  const fancyMenuItemSchema = choose$1("fancytype", {
    inserttable: insertTableFields,
    colorswatch: colorSwatchFields
  });
  const createFancyMenuItem = (spec) => asRaw("fancymenuitem", fancyMenuItemSchema, spec);
  const menuItemSchema = objOf([
    type,
    onSetup,
    defaultedOnAction,
    optionalIcon
  ].concat(commonMenuItemFields));
  const createMenuItem = (spec) => asRaw("menuitem", menuItemSchema, spec);
  const nestedMenuItemSchema = objOf([
    type,
    getSubmenuItems,
    onSetup,
    optionalIcon
  ].concat(commonMenuItemFields));
  const createNestedMenuItem = (spec) => asRaw("nestedmenuitem", nestedMenuItemSchema, spec);
  const toggleMenuItemSchema = objOf([
    type,
    optionalIcon,
    active,
    onSetup,
    onAction
  ].concat(commonMenuItemFields));
  const createToggleMenuItem = (spec) => asRaw("togglemenuitem", toggleMenuItemSchema, spec);
  const detectSize = (comp, margin, selectorClass) => {
    const descendants$1 = descendants(comp.element, "." + selectorClass);
    if (descendants$1.length > 0) {
      const columnLength = findIndex$1(descendants$1, (c) => {
        const thisTop = c.dom.getBoundingClientRect().top;
        const cTop = descendants$1[0].dom.getBoundingClientRect().top;
        return Math.abs(thisTop - cTop) > margin;
      }).getOr(descendants$1.length);
      return Optional.some({
        numColumns: columnLength,
        numRows: Math.ceil(descendants$1.length / columnLength)
      });
    } else {
      return Optional.none();
    }
  };
  const namedEvents = (name2, handlers2) => derive$1([config(name2, handlers2)]);
  const unnamedEvents = (handlers2) => namedEvents(generate$6("unnamed-events"), handlers2);
  const SimpleBehaviours = {
    namedEvents,
    unnamedEvents
  };
  const ExclusivityChannel = generate$6("tooltip.exclusive");
  const ShowTooltipEvent = generate$6("tooltip.show");
  const HideTooltipEvent = generate$6("tooltip.hide");
  const hideAllExclusive = (component, _tConfig, _tState) => {
    component.getSystem().broadcastOn([ExclusivityChannel], {});
  };
  const setComponents = (component, tConfig, tState, specs) => {
    tState.getTooltip().each((tooltip) => {
      if (tooltip.getSystem().isConnected()) {
        Replacing.set(tooltip, specs);
      }
    });
  };
  var TooltippingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    hideAllExclusive,
    setComponents
  });
  const events$9 = (tooltipConfig, state) => {
    const hide = (comp) => {
      state.getTooltip().each((p) => {
        detach(p);
        tooltipConfig.onHide(comp, p);
        state.clearTooltip();
      });
      state.clearTimer();
    };
    const show2 = (comp) => {
      if (!state.isShowing()) {
        hideAllExclusive(comp);
        const sink = tooltipConfig.lazySink(comp).getOrDie();
        const popup = comp.getSystem().build({
          dom: tooltipConfig.tooltipDom,
          components: tooltipConfig.tooltipComponents,
          events: derive$2(tooltipConfig.mode === "normal" ? [
            run$1(mouseover(), (_) => {
              emit(comp, ShowTooltipEvent);
            }),
            run$1(mouseout(), (_) => {
              emit(comp, HideTooltipEvent);
            })
          ] : []),
          behaviours: derive$1([Replacing.config({})])
        });
        state.setTooltip(popup);
        attach(sink, popup);
        tooltipConfig.onShow(comp, popup);
        Positioning.position(sink, popup, { anchor: tooltipConfig.anchor(comp) });
      }
    };
    return derive$2(flatten([
      [
        run$1(ShowTooltipEvent, (comp) => {
          state.resetTimer(() => {
            show2(comp);
          }, tooltipConfig.delay);
        }),
        run$1(HideTooltipEvent, (comp) => {
          state.resetTimer(() => {
            hide(comp);
          }, tooltipConfig.delay);
        }),
        run$1(receive(), (comp, message) => {
          const receivingData = message;
          if (!receivingData.universal) {
            if (contains$2(receivingData.channels, ExclusivityChannel)) {
              hide(comp);
            }
          }
        }),
        runOnDetached((comp) => {
          hide(comp);
        })
      ],
      tooltipConfig.mode === "normal" ? [
        run$1(focusin(), (comp) => {
          emit(comp, ShowTooltipEvent);
        }),
        run$1(postBlur(), (comp) => {
          emit(comp, HideTooltipEvent);
        }),
        run$1(mouseover(), (comp) => {
          emit(comp, ShowTooltipEvent);
        }),
        run$1(mouseout(), (comp) => {
          emit(comp, HideTooltipEvent);
        })
      ] : [
        run$1(highlight$1(), (comp, _se) => {
          emit(comp, ShowTooltipEvent);
        }),
        run$1(dehighlight$1(), (comp) => {
          emit(comp, HideTooltipEvent);
        })
      ]
    ]));
  };
  var ActiveTooltipping = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$9
  });
  var TooltippingSchema = [
    required$1("lazySink"),
    required$1("tooltipDom"),
    defaulted("exclusive", true),
    defaulted("tooltipComponents", []),
    defaulted("delay", 300),
    defaultedStringEnum("mode", "normal", [
      "normal",
      "follow-highlight"
    ]),
    defaulted("anchor", (comp) => ({
      type: "hotspot",
      hotspot: comp,
      layouts: {
        onLtr: constant$1([
          south$2,
          north$2,
          southeast$2,
          northeast$2,
          southwest$2,
          northwest$2
        ]),
        onRtl: constant$1([
          south$2,
          north$2,
          southeast$2,
          northeast$2,
          southwest$2,
          northwest$2
        ])
      }
    })),
    onHandler("onHide"),
    onHandler("onShow")
  ];
  const init$b = () => {
    const timer = value$2();
    const popup = value$2();
    const clearTimer = () => {
      timer.on(clearTimeout);
    };
    const resetTimer = (f, delay) => {
      clearTimer();
      timer.set(setTimeout(f, delay));
    };
    const readState = constant$1("not-implemented");
    return nu$8({
      getTooltip: popup.get,
      isShowing: popup.isSet,
      setTooltip: popup.set,
      clearTooltip: popup.clear,
      clearTimer,
      resetTimer,
      readState
    });
  };
  var TooltippingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$b
  });
  const Tooltipping = create$3({
    fields: TooltippingSchema,
    name: "tooltipping",
    active: ActiveTooltipping,
    state: TooltippingState,
    apis: TooltippingApis
  });
  const escape2 = (text2) => text2.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const ReadOnlyChannel = "silver.readonly";
  const ReadOnlyDataSchema = objOf([requiredBoolean("readonly")]);
  const broadcastReadonly = (uiComponents, readonly) => {
    const outerContainer = uiComponents.outerContainer;
    const target = outerContainer.element;
    if (readonly) {
      uiComponents.mothership.broadcastOn([dismissPopups()], { target });
      uiComponents.uiMothership.broadcastOn([dismissPopups()], { target });
    }
    uiComponents.mothership.broadcastOn([ReadOnlyChannel], { readonly });
    uiComponents.uiMothership.broadcastOn([ReadOnlyChannel], { readonly });
  };
  const setupReadonlyModeSwitch = (editor, uiComponents) => {
    editor.on("init", () => {
      if (editor.mode.isReadOnly()) {
        broadcastReadonly(uiComponents, true);
      }
    });
    editor.on("SwitchMode", () => broadcastReadonly(uiComponents, editor.mode.isReadOnly()));
    if (isReadOnly(editor)) {
      editor.mode.set("readonly");
    }
  };
  const receivingConfig = () => Receiving.config({
    channels: {
      [ReadOnlyChannel]: {
        schema: ReadOnlyDataSchema,
        onReceive: (comp, data) => {
          Disabling.set(comp, data.readonly);
        }
      }
    }
  });
  const item = (disabled) => Disabling.config({
    disabled,
    disableClass: "tox-collection__item--state-disabled"
  });
  const button = (disabled) => Disabling.config({ disabled });
  const splitButton = (disabled) => Disabling.config({
    disabled,
    disableClass: "tox-tbtn--disabled"
  });
  const toolbarButton = (disabled) => Disabling.config({
    disabled,
    disableClass: "tox-tbtn--disabled",
    useNative: false
  });
  const DisablingConfigs = {
    item,
    button,
    splitButton,
    toolbarButton
  };
  const runWithApi = (info, comp) => {
    const api2 = info.getApi(comp);
    return (f) => {
      f(api2);
    };
  };
  const onControlAttached = (info, editorOffCell) => runOnAttached((comp) => {
    const run2 = runWithApi(info, comp);
    run2((api2) => {
      const onDestroy = info.onSetup(api2);
      if (isFunction(onDestroy)) {
        editorOffCell.set(onDestroy);
      }
    });
  });
  const onControlDetached = (getApi2, editorOffCell) => runOnDetached((comp) => runWithApi(getApi2, comp)(editorOffCell.get()));
  const onMenuItemExecute = (info, itemResponse) => runOnExecute$1((comp, simulatedEvent) => {
    runWithApi(info, comp)(info.onAction);
    if (!info.triggersSubmenu && itemResponse === ItemResponse$1.CLOSE_ON_EXECUTE) {
      if (comp.getSystem().isConnected()) {
        emit(comp, sandboxClose());
      }
      simulatedEvent.stop();
    }
  });
  const menuItemEventOrder = {
    [execute$5()]: [
      "disabling",
      "alloy.base.behaviour",
      "toggling",
      "item-events"
    ]
  };
  const componentRenderPipeline = cat;
  const renderCommonItem = (spec, structure, itemResponse, providersBackstage) => {
    const editorOffCell = Cell(noop);
    return {
      type: "item",
      dom: structure.dom,
      components: componentRenderPipeline(structure.optComponents),
      data: spec.data,
      eventOrder: menuItemEventOrder,
      hasSubmenu: spec.triggersSubmenu,
      itemBehaviours: derive$1([
        config("item-events", [
          onMenuItemExecute(spec, itemResponse),
          onControlAttached(spec, editorOffCell),
          onControlDetached(spec, editorOffCell)
        ]),
        DisablingConfigs.item(() => !spec.enabled || providersBackstage.isDisabled()),
        receivingConfig(),
        Replacing.config({})
      ].concat(spec.itemBehaviours))
    };
  };
  const buildData = (source) => ({
    value: source.value,
    meta: {
      text: source.text.getOr(""),
      ...source.meta
    }
  });
  const convertText = (source) => {
    const isMac = global$5.os.isMacOS() || global$5.os.isiOS();
    const mac = {
      alt: "\u2325",
      ctrl: "\u2303",
      shift: "\u21E7",
      meta: "\u2318",
      access: "\u2303\u2325"
    };
    const other = {
      meta: "Ctrl",
      access: "Shift+Alt"
    };
    const replace2 = isMac ? mac : other;
    const shortcut = source.split("+");
    const updated = map$2(shortcut, (segment) => {
      const search2 = segment.toLowerCase().trim();
      return has$2(replace2, search2) ? replace2[search2] : segment;
    });
    return isMac ? updated.join("") : updated.join("+");
  };
  const renderIcon$1 = (name2, icons, classes2 = [iconClass]) => render$3(name2, {
    tag: "div",
    classes: classes2
  }, icons);
  const renderText = (text2) => ({
    dom: {
      tag: "div",
      classes: [textClass]
    },
    components: [text$1(global$8.translate(text2))]
  });
  const renderHtml = (html, classes2) => ({
    dom: {
      tag: "div",
      classes: classes2,
      innerHtml: html
    }
  });
  const renderStyledText = (style, text2) => ({
    dom: {
      tag: "div",
      classes: [textClass]
    },
    components: [{
      dom: {
        tag: style.tag,
        styles: style.styles
      },
      components: [text$1(global$8.translate(text2))]
    }]
  });
  const renderShortcut = (shortcut) => ({
    dom: {
      tag: "div",
      classes: [accessoryClass]
    },
    components: [text$1(convertText(shortcut))]
  });
  const renderCheckmark = (icons) => renderIcon$1("checkmark", icons, [checkmarkClass]);
  const renderSubmenuCaret = (icons) => renderIcon$1("chevron-right", icons, [caretClass]);
  const renderDownwardsCaret = (icons) => renderIcon$1("chevron-down", icons, [caretClass]);
  const renderContainer = (container, components2) => {
    const directionClass = container.direction === "vertical" ? containerColumnClass : containerRowClass;
    const alignClass = container.align === "left" ? containerAlignLeftClass : containerAlignRightClass;
    const getValignClass = () => {
      switch (container.valign) {
        case "top":
          return containerValignTopClass;
        case "middle":
          return containerValignMiddleClass;
        case "bottom":
          return containerValignBottomClass;
      }
    };
    return {
      dom: {
        tag: "div",
        classes: [
          containerClass,
          directionClass,
          alignClass,
          getValignClass()
        ]
      },
      components: components2
    };
  };
  const renderImage = (src, classes2, alt) => ({
    dom: {
      tag: "img",
      classes: classes2,
      attributes: {
        src,
        alt: alt.getOr("")
      }
    }
  });
  const renderColorStructure = (item2, providerBackstage, fallbackIcon) => {
    const colorPickerCommand = "custom";
    const removeColorCommand = "remove";
    const itemText = item2.ariaLabel;
    const itemValue = item2.value;
    const iconSvg = item2.iconContent.map((name2) => getOr(name2, providerBackstage.icons, fallbackIcon));
    const getDom = () => {
      const common = colorClass;
      const icon2 = iconSvg.getOr("");
      const attributes = itemText.map((text2) => ({ title: providerBackstage.translate(text2) })).getOr({});
      const baseDom = {
        tag: "div",
        attributes,
        classes: [common]
      };
      if (itemValue === colorPickerCommand) {
        return {
          ...baseDom,
          tag: "button",
          classes: [
            ...baseDom.classes,
            "tox-swatches__picker-btn"
          ],
          innerHtml: icon2
        };
      } else if (itemValue === removeColorCommand) {
        return {
          ...baseDom,
          classes: [
            ...baseDom.classes,
            "tox-swatch--remove"
          ],
          innerHtml: icon2
        };
      } else if (isNonNullable(itemValue)) {
        return {
          ...baseDom,
          attributes: {
            ...baseDom.attributes,
            "data-mce-color": itemValue
          },
          styles: { "background-color": itemValue }
        };
      } else {
        return baseDom;
      }
    };
    return {
      dom: getDom(),
      optComponents: []
    };
  };
  const renderItemDomStructure = (ariaLabel) => {
    const domTitle = ariaLabel.map((label2) => ({ attributes: { title: global$8.translate(label2) } })).getOr({});
    return {
      tag: "div",
      classes: [
        navClass,
        selectableClass
      ],
      ...domTitle
    };
  };
  const renderNormalItemStructure = (info, providersBackstage, renderIcons, fallbackIcon) => {
    const iconSpec = {
      tag: "div",
      classes: [iconClass]
    };
    const renderIcon2 = (iconName) => render$3(iconName, iconSpec, providersBackstage.icons, fallbackIcon);
    const renderEmptyIcon = () => Optional.some({ dom: iconSpec });
    const leftIcon = renderIcons ? info.iconContent.map(renderIcon2).orThunk(renderEmptyIcon) : Optional.none();
    const checkmark = info.checkMark;
    const textRender = Optional.from(info.meta).fold(() => renderText, (meta) => has$2(meta, "style") ? curry(renderStyledText, meta.style) : renderText);
    const content = info.htmlContent.fold(() => info.textContent.map(textRender), (html) => Optional.some(renderHtml(html, [textClass])));
    const menuItem = {
      dom: renderItemDomStructure(info.ariaLabel),
      optComponents: [
        leftIcon,
        content,
        info.shortcutContent.map(renderShortcut),
        checkmark,
        info.caret
      ]
    };
    return menuItem;
  };
  const renderItemStructure = (info, providersBackstage, renderIcons, fallbackIcon = Optional.none()) => {
    if (info.presets === "color") {
      return renderColorStructure(info, providersBackstage, fallbackIcon);
    } else {
      return renderNormalItemStructure(info, providersBackstage, renderIcons, fallbackIcon);
    }
  };
  const tooltipBehaviour = (meta, sharedBackstage) => get$g(meta, "tooltipWorker").map((tooltipWorker) => [Tooltipping.config({
    lazySink: sharedBackstage.getSink,
    tooltipDom: {
      tag: "div",
      classes: ["tox-tooltip-worker-container"]
    },
    tooltipComponents: [],
    anchor: (comp) => ({
      type: "submenu",
      item: comp,
      overrides: { maxHeightFunction: expandable$1 }
    }),
    mode: "follow-highlight",
    onShow: (component, _tooltip) => {
      tooltipWorker((elm) => {
        Tooltipping.setComponents(component, [external$1({ element: SugarElement.fromDom(elm) })]);
      });
    }
  })]).getOr([]);
  const encodeText = (text2) => global$7.DOM.encode(text2);
  const replaceText = (text2, matchText) => {
    const translated = global$8.translate(text2);
    const encoded = encodeText(translated);
    if (matchText.length > 0) {
      const escapedMatchRegex = new RegExp(escape2(matchText), "gi");
      return encoded.replace(escapedMatchRegex, (match) => `<span class="tox-autocompleter-highlight">${match}</span>`);
    } else {
      return encoded;
    }
  };
  const renderAutocompleteItem = (spec, matchText, useText, presets, onItemValueHandler, itemResponse, sharedBackstage, renderIcons = true) => {
    const structure = renderItemStructure({
      presets,
      textContent: Optional.none(),
      htmlContent: useText ? spec.text.map((text2) => replaceText(text2, matchText)) : Optional.none(),
      ariaLabel: spec.text,
      iconContent: spec.icon,
      shortcutContent: Optional.none(),
      checkMark: Optional.none(),
      caret: Optional.none(),
      value: spec.value
    }, sharedBackstage.providers, renderIcons, spec.icon);
    return renderCommonItem({
      data: buildData(spec),
      enabled: spec.enabled,
      getApi: constant$1({}),
      onAction: (_api) => onItemValueHandler(spec.value, spec.meta),
      onSetup: constant$1(noop),
      triggersSubmenu: false,
      itemBehaviours: tooltipBehaviour(spec.meta, sharedBackstage)
    }, structure, itemResponse, sharedBackstage.providers);
  };
  const render$2 = (items, extras) => map$2(items, (item2) => {
    switch (item2.type) {
      case "cardcontainer":
        return renderContainer(item2, render$2(item2.items, extras));
      case "cardimage":
        return renderImage(item2.src, item2.classes, item2.alt);
      case "cardtext":
        const shouldHighlight = item2.name.exists((name2) => contains$2(extras.cardText.highlightOn, name2));
        const matchText = shouldHighlight ? Optional.from(extras.cardText.matchText).getOr("") : "";
        return renderHtml(replaceText(item2.text, matchText), item2.classes);
    }
  });
  const renderCardMenuItem = (spec, itemResponse, sharedBackstage, extras) => {
    const getApi2 = (component) => ({
      isEnabled: () => !Disabling.isDisabled(component),
      setEnabled: (state) => {
        Disabling.set(component, !state);
        each$1(descendants(component.element, "*"), (elm) => {
          component.getSystem().getByDom(elm).each((comp) => {
            if (comp.hasConfigured(Disabling)) {
              Disabling.set(comp, !state);
            }
          });
        });
      }
    });
    const structure = {
      dom: renderItemDomStructure(spec.label),
      optComponents: [Optional.some({
        dom: {
          tag: "div",
          classes: [
            containerClass,
            containerRowClass
          ]
        },
        components: render$2(spec.items, extras)
      })]
    };
    return renderCommonItem({
      data: buildData({
        text: Optional.none(),
        ...spec
      }),
      enabled: spec.enabled,
      getApi: getApi2,
      onAction: spec.onAction,
      onSetup: spec.onSetup,
      triggersSubmenu: false,
      itemBehaviours: Optional.from(extras.itemBehaviours).getOr([])
    }, structure, itemResponse, sharedBackstage.providers);
  };
  const renderChoiceItem = (spec, useText, presets, onItemValueHandler, isSelected, itemResponse, providersBackstage, renderIcons = true) => {
    const getApi2 = (component) => ({
      setActive: (state) => {
        Toggling.set(component, state);
      },
      isActive: () => Toggling.isOn(component),
      isEnabled: () => !Disabling.isDisabled(component),
      setEnabled: (state) => Disabling.set(component, !state)
    });
    const structure = renderItemStructure({
      presets,
      textContent: useText ? spec.text : Optional.none(),
      htmlContent: Optional.none(),
      ariaLabel: spec.text,
      iconContent: spec.icon,
      shortcutContent: useText ? spec.shortcut : Optional.none(),
      checkMark: useText ? Optional.some(renderCheckmark(providersBackstage.icons)) : Optional.none(),
      caret: Optional.none(),
      value: spec.value
    }, providersBackstage, renderIcons);
    return deepMerge(renderCommonItem({
      data: buildData(spec),
      enabled: spec.enabled,
      getApi: getApi2,
      onAction: (_api) => onItemValueHandler(spec.value),
      onSetup: (api2) => {
        api2.setActive(isSelected);
        return noop;
      },
      triggersSubmenu: false,
      itemBehaviours: []
    }, structure, itemResponse, providersBackstage), {
      toggling: {
        toggleClass: tickedClass,
        toggleOnExecute: false,
        selected: spec.active,
        exclusive: true
      }
    });
  };
  const parts$f = generate$3(owner$2(), parts$h());
  const hexColour = (value2) => ({ value: value2 });
  const shorthandRegex = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
  const longformRegex = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i;
  const isHexString = (hex) => shorthandRegex.test(hex) || longformRegex.test(hex);
  const normalizeHex = (hex) => removeLeading(hex, "#").toUpperCase();
  const fromString$1 = (hex) => isHexString(hex) ? Optional.some({ value: normalizeHex(hex) }) : Optional.none();
  const getLongForm = (hex) => {
    const hexString = hex.value.replace(shorthandRegex, (m, r2, g, b2) => r2 + r2 + g + g + b2 + b2);
    return { value: hexString };
  };
  const extractValues = (hex) => {
    const longForm = getLongForm(hex);
    const splitForm = longformRegex.exec(longForm.value);
    return splitForm === null ? [
      "FFFFFF",
      "FF",
      "FF",
      "FF"
    ] : splitForm;
  };
  const toHex = (component) => {
    const hex = component.toString(16);
    return (hex.length === 1 ? "0" + hex : hex).toUpperCase();
  };
  const fromRgba = (rgbaColour2) => {
    const value2 = toHex(rgbaColour2.red) + toHex(rgbaColour2.green) + toHex(rgbaColour2.blue);
    return hexColour(value2);
  };
  const min = Math.min;
  const max = Math.max;
  const round$1 = Math.round;
  const rgbRegex = /^\s*rgb\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*\)\s*$/i;
  const rgbaRegex = /^\s*rgba\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d?(?:\.\d+)?)\s*\)\s*$/i;
  const rgbaColour = (red2, green, blue, alpha) => ({
    red: red2,
    green,
    blue,
    alpha
  });
  const isRgbaComponent = (value2) => {
    const num = parseInt(value2, 10);
    return num.toString() === value2 && num >= 0 && num <= 255;
  };
  const fromHsv = (hsv) => {
    let r2;
    let g;
    let b2;
    const hue = (hsv.hue || 0) % 360;
    let saturation = hsv.saturation / 100;
    let brightness = hsv.value / 100;
    saturation = max(0, min(saturation, 1));
    brightness = max(0, min(brightness, 1));
    if (saturation === 0) {
      r2 = g = b2 = round$1(255 * brightness);
      return rgbaColour(r2, g, b2, 1);
    }
    const side = hue / 60;
    const chroma = brightness * saturation;
    const x = chroma * (1 - Math.abs(side % 2 - 1));
    const match = brightness - chroma;
    switch (Math.floor(side)) {
      case 0:
        r2 = chroma;
        g = x;
        b2 = 0;
        break;
      case 1:
        r2 = x;
        g = chroma;
        b2 = 0;
        break;
      case 2:
        r2 = 0;
        g = chroma;
        b2 = x;
        break;
      case 3:
        r2 = 0;
        g = x;
        b2 = chroma;
        break;
      case 4:
        r2 = x;
        g = 0;
        b2 = chroma;
        break;
      case 5:
        r2 = chroma;
        g = 0;
        b2 = x;
        break;
      default:
        r2 = g = b2 = 0;
    }
    r2 = round$1(255 * (r2 + match));
    g = round$1(255 * (g + match));
    b2 = round$1(255 * (b2 + match));
    return rgbaColour(r2, g, b2, 1);
  };
  const fromHex = (hexColour2) => {
    const result = extractValues(hexColour2);
    const red2 = parseInt(result[1], 16);
    const green = parseInt(result[2], 16);
    const blue = parseInt(result[3], 16);
    return rgbaColour(red2, green, blue, 1);
  };
  const fromStringValues = (red2, green, blue, alpha) => {
    const r2 = parseInt(red2, 10);
    const g = parseInt(green, 10);
    const b2 = parseInt(blue, 10);
    const a = parseFloat(alpha);
    return rgbaColour(r2, g, b2, a);
  };
  const fromString = (rgbaString) => {
    if (rgbaString === "transparent") {
      return Optional.some(rgbaColour(0, 0, 0, 0));
    }
    const rgbMatch = rgbRegex.exec(rgbaString);
    if (rgbMatch !== null) {
      return Optional.some(fromStringValues(rgbMatch[1], rgbMatch[2], rgbMatch[3], "1"));
    }
    const rgbaMatch = rgbaRegex.exec(rgbaString);
    if (rgbaMatch !== null) {
      return Optional.some(fromStringValues(rgbaMatch[1], rgbaMatch[2], rgbaMatch[3], rgbaMatch[4]));
    }
    return Optional.none();
  };
  const toString = (rgba) => `rgba(${rgba.red},${rgba.green},${rgba.blue},${rgba.alpha})`;
  const red = rgbaColour(255, 0, 0, 1);
  const fireSkinLoaded$1 = (editor) => {
    editor.dispatch("SkinLoaded");
  };
  const fireSkinLoadError$1 = (editor, error2) => {
    editor.dispatch("SkinLoadError", error2);
  };
  const fireResizeEditor = (editor) => {
    editor.dispatch("ResizeEditor");
  };
  const fireResizeContent = (editor, e) => {
    editor.dispatch("ResizeContent", e);
  };
  const fireScrollContent = (editor, e) => {
    editor.dispatch("ScrollContent", e);
  };
  const fireTextColorChange = (editor, data) => {
    editor.dispatch("TextColorChange", data);
  };
  const fireAfterProgressState = (editor, state) => {
    editor.dispatch("AfterProgressState", { state });
  };
  const fireResolveName = (editor, node) => editor.dispatch("ResolveName", {
    name: node.nodeName.toLowerCase(),
    target: node
  });
  const hsvColour = (hue, saturation, value2) => ({
    hue,
    saturation,
    value: value2
  });
  const fromRgb = (rgbaColour2) => {
    let h = 0;
    let s = 0;
    let v = 0;
    const r2 = rgbaColour2.red / 255;
    const g = rgbaColour2.green / 255;
    const b2 = rgbaColour2.blue / 255;
    const minRGB = Math.min(r2, Math.min(g, b2));
    const maxRGB = Math.max(r2, Math.max(g, b2));
    if (minRGB === maxRGB) {
      v = minRGB;
      return hsvColour(0, 0, v * 100);
    }
    const d = r2 === minRGB ? g - b2 : b2 === minRGB ? r2 - g : b2 - r2;
    h = r2 === minRGB ? 3 : b2 === minRGB ? 1 : 5;
    h = 60 * (h - d / (maxRGB - minRGB));
    s = (maxRGB - minRGB) / maxRGB;
    v = maxRGB;
    return hsvColour(Math.round(h), Math.round(s * 100), Math.round(v * 100));
  };
  const hexToHsv = (hex) => fromRgb(fromHex(hex));
  const hsvToHex = (hsv) => fromRgba(fromHsv(hsv));
  const anyToHex = (color) => fromString$1(color).orThunk(() => fromString(color).map(fromRgba)).getOrThunk(() => {
    const canvas = document.createElement("canvas");
    canvas.height = 1;
    canvas.width = 1;
    const canvasContext = canvas.getContext("2d");
    canvasContext.clearRect(0, 0, canvas.width, canvas.height);
    canvasContext.fillStyle = "#FFFFFF";
    canvasContext.fillStyle = color;
    canvasContext.fillRect(0, 0, 1, 1);
    const rgba = canvasContext.getImageData(0, 0, 1, 1).data;
    const r2 = rgba[0];
    const g = rgba[1];
    const b2 = rgba[2];
    const a = rgba[3];
    return fromRgba(rgbaColour(r2, g, b2, a));
  });
  var global$4 = tinymce.util.Tools.resolve("tinymce.util.LocalStorage");
  const storageName = "tinymce-custom-colors";
  const ColorCache = (max2 = 10) => {
    const storageString = global$4.getItem(storageName);
    const localstorage = isString(storageString) ? JSON.parse(storageString) : [];
    const prune = (list) => {
      const diff = max2 - list.length;
      return diff < 0 ? list.slice(0, max2) : list;
    };
    const cache = prune(localstorage);
    const add2 = (key) => {
      indexOf(cache, key).each(remove2);
      cache.unshift(key);
      if (cache.length > max2) {
        cache.pop();
      }
      global$4.setItem(storageName, JSON.stringify(cache));
    };
    const remove2 = (idx) => {
      cache.splice(idx, 1);
    };
    const state = () => cache.slice(0);
    return {
      add: add2,
      state
    };
  };
  const colorCache = ColorCache(10);
  const calcCols = (colors) => Math.max(5, Math.ceil(Math.sqrt(colors)));
  const mapColors = (colorMap) => {
    const colors = [];
    for (let i2 = 0; i2 < colorMap.length; i2 += 2) {
      colors.push({
        text: colorMap[i2 + 1],
        value: "#" + anyToHex(colorMap[i2]).value,
        type: "choiceitem"
      });
    }
    return colors;
  };
  const option$1 = (name2) => (editor) => editor.options.get(name2);
  const register$d = (editor) => {
    const registerOption = editor.options.register;
    registerOption("color_map", {
      processor: (value2) => {
        if (isArrayOf(value2, isString)) {
          return {
            value: mapColors(value2),
            valid: true
          };
        } else {
          return {
            valid: false,
            message: "Must be an array of strings."
          };
        }
      },
      default: [
        "#BFEDD2",
        "Light Green",
        "#FBEEB8",
        "Light Yellow",
        "#F8CAC6",
        "Light Red",
        "#ECCAFA",
        "Light Purple",
        "#C2E0F4",
        "Light Blue",
        "#2DC26B",
        "Green",
        "#F1C40F",
        "Yellow",
        "#E03E2D",
        "Red",
        "#B96AD9",
        "Purple",
        "#3598DB",
        "Blue",
        "#169179",
        "Dark Turquoise",
        "#E67E23",
        "Orange",
        "#BA372A",
        "Dark Red",
        "#843FA1",
        "Dark Purple",
        "#236FA1",
        "Dark Blue",
        "#ECF0F1",
        "Light Gray",
        "#CED4D9",
        "Medium Gray",
        "#95A5A6",
        "Gray",
        "#7E8C8D",
        "Dark Gray",
        "#34495E",
        "Navy Blue",
        "#000000",
        "Black",
        "#ffffff",
        "White"
      ]
    });
    registerOption("color_cols", {
      processor: "number",
      default: calcCols(getColors$2(editor).length)
    });
    registerOption("custom_colors", {
      processor: "boolean",
      default: true
    });
  };
  const getColorCols$1 = option$1("color_cols");
  const hasCustomColors$1 = option$1("custom_colors");
  const getColors$2 = option$1("color_map");
  const getCurrentColors = () => map$2(colorCache.state(), (color) => ({
    type: "choiceitem",
    text: color,
    value: color
  }));
  const addColor = (color) => {
    colorCache.add(color);
  };
  const fallbackColor = "#000000";
  const hasStyleApi = (node) => isNonNullable(node.style);
  const getCurrentColor = (editor, format) => {
    let color;
    editor.dom.getParents(editor.selection.getStart(), (elm) => {
      const value2 = hasStyleApi(elm) ? elm.style[format === "forecolor" ? "color" : "backgroundColor"] : null;
      if (value2) {
        color = color ? color : value2;
      }
    });
    return Optional.from(color);
  };
  const applyFormat = (editor, format, value2) => {
    editor.undoManager.transact(() => {
      editor.focus();
      editor.formatter.apply(format, { value: value2 });
      editor.nodeChanged();
    });
  };
  const removeFormat = (editor, format) => {
    editor.undoManager.transact(() => {
      editor.focus();
      editor.formatter.remove(format, { value: null }, void 0, true);
      editor.nodeChanged();
    });
  };
  const registerCommands = (editor) => {
    editor.addCommand("mceApplyTextcolor", (format, value2) => {
      applyFormat(editor, format, value2);
    });
    editor.addCommand("mceRemoveTextcolor", (format) => {
      removeFormat(editor, format);
    });
  };
  const getAdditionalColors = (hasCustom) => {
    const type2 = "choiceitem";
    const remove2 = {
      type: type2,
      text: "Remove color",
      icon: "color-swatch-remove-color",
      value: "remove"
    };
    const custom2 = {
      type: type2,
      text: "Custom color",
      icon: "color-picker",
      value: "custom"
    };
    return hasCustom ? [
      remove2,
      custom2
    ] : [remove2];
  };
  const applyColor = (editor, format, value2, onChoice) => {
    if (value2 === "custom") {
      const dialog = colorPickerDialog(editor);
      dialog((colorOpt) => {
        colorOpt.each((color) => {
          addColor(color);
          editor.execCommand("mceApplyTextcolor", format, color);
          onChoice(color);
        });
      }, fallbackColor);
    } else if (value2 === "remove") {
      onChoice("");
      editor.execCommand("mceRemoveTextcolor", format);
    } else {
      onChoice(value2);
      editor.execCommand("mceApplyTextcolor", format, value2);
    }
  };
  const getColors$1 = (colors, hasCustom) => colors.concat(getCurrentColors().concat(getAdditionalColors(hasCustom)));
  const getFetch$1 = (colors, hasCustom) => (callback) => {
    callback(getColors$1(colors, hasCustom));
  };
  const setIconColor = (splitButtonApi, name2, newColor) => {
    const id = name2 === "forecolor" ? "tox-icon-text-color__color" : "tox-icon-highlight-bg-color__color";
    splitButtonApi.setIconFill(id, newColor);
  };
  const registerTextColorButton = (editor, name2, format, tooltip, lastColor) => {
    editor.ui.registry.addSplitButton(name2, {
      tooltip,
      presets: "color",
      icon: name2 === "forecolor" ? "text-color" : "highlight-bg-color",
      select: (value2) => {
        const optCurrentRgb = getCurrentColor(editor, format);
        return optCurrentRgb.bind((currentRgb) => fromString(currentRgb).map((rgba) => {
          const currentHex = fromRgba(rgba).value;
          return contains$1(value2.toLowerCase(), currentHex);
        })).getOr(false);
      },
      columns: getColorCols$1(editor),
      fetch: getFetch$1(getColors$2(editor), hasCustomColors$1(editor)),
      onAction: (_splitButtonApi) => {
        applyColor(editor, format, lastColor.get(), noop);
      },
      onItemAction: (_splitButtonApi, value2) => {
        applyColor(editor, format, value2, (newColor) => {
          lastColor.set(newColor);
          fireTextColorChange(editor, {
            name: name2,
            color: newColor
          });
        });
      },
      onSetup: (splitButtonApi) => {
        setIconColor(splitButtonApi, name2, lastColor.get());
        const handler = (e) => {
          if (e.name === name2) {
            setIconColor(splitButtonApi, e.name, e.color);
          }
        };
        editor.on("TextColorChange", handler);
        return () => {
          editor.off("TextColorChange", handler);
        };
      }
    });
  };
  const registerTextColorMenuItem = (editor, name2, format, text2) => {
    editor.ui.registry.addNestedMenuItem(name2, {
      text: text2,
      icon: name2 === "forecolor" ? "text-color" : "highlight-bg-color",
      getSubmenuItems: () => [{
        type: "fancymenuitem",
        fancytype: "colorswatch",
        onAction: (data) => {
          applyColor(editor, format, data.value, noop);
        }
      }]
    });
  };
  const colorPickerDialog = (editor) => (callback, value2) => {
    let isValid = false;
    const onSubmit = (api2) => {
      const data = api2.getData();
      const hex = data.colorpicker;
      if (isValid) {
        callback(Optional.from(hex));
        api2.close();
      } else {
        editor.windowManager.alert(editor.translate([
          "Invalid hex color code: {0}",
          hex
        ]));
      }
    };
    const onAction2 = (_api, details) => {
      if (details.name === "hex-valid") {
        isValid = details.value;
      }
    };
    const initialData = { colorpicker: value2 };
    editor.windowManager.open({
      title: "Color Picker",
      size: "normal",
      body: {
        type: "panel",
        items: [{
          type: "colorpicker",
          name: "colorpicker",
          label: "Color"
        }]
      },
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData,
      onAction: onAction2,
      onSubmit,
      onClose: noop,
      onCancel: () => {
        callback(Optional.none());
      }
    });
  };
  const register$c = (editor) => {
    registerCommands(editor);
    const lastForeColor = Cell(fallbackColor);
    const lastBackColor = Cell(fallbackColor);
    registerTextColorButton(editor, "forecolor", "forecolor", "Text color", lastForeColor);
    registerTextColorButton(editor, "backcolor", "hilitecolor", "Background color", lastBackColor);
    registerTextColorMenuItem(editor, "forecolor", "forecolor", "Text color");
    registerTextColorMenuItem(editor, "backcolor", "hilitecolor", "Background color");
  };
  const createPartialChoiceMenu = (value2, items, onItemValueHandler, columns, presets, itemResponse, select2, providersBackstage) => {
    const hasIcons = menuHasIcons(items);
    const presetItemTypes = presets !== "color" ? "normal" : "color";
    const alloyItems = createChoiceItems(items, onItemValueHandler, columns, presetItemTypes, itemResponse, select2, providersBackstage);
    const menuLayout = { menuType: presets };
    return createPartialMenuWithAlloyItems(value2, hasIcons, alloyItems, columns, menuLayout);
  };
  const createChoiceItems = (items, onItemValueHandler, columns, itemPresets, itemResponse, select2, providersBackstage) => cat(map$2(items, (item2) => {
    if (item2.type === "choiceitem") {
      return createChoiceMenuItem(item2).fold(handleError, (d) => Optional.some(renderChoiceItem(d, columns === 1, itemPresets, onItemValueHandler, select2(d.value), itemResponse, providersBackstage, menuHasIcons(items))));
    } else {
      return Optional.none();
    }
  }));
  const deriveMenuMovement = (columns, presets) => {
    const menuMarkers = markers(presets);
    if (columns === 1) {
      return {
        mode: "menu",
        moveOnTab: true
      };
    } else if (columns === "auto") {
      return {
        mode: "grid",
        selector: "." + menuMarkers.item,
        initSize: {
          numColumns: 1,
          numRows: 1
        }
      };
    } else {
      const rowClass = presets === "color" ? "tox-swatches__row" : "tox-collection__group";
      return {
        mode: "matrix",
        rowSelector: "." + rowClass
      };
    }
  };
  const deriveCollectionMovement = (columns, presets) => {
    if (columns === 1) {
      return {
        mode: "menu",
        moveOnTab: false,
        selector: ".tox-collection__item"
      };
    } else if (columns === "auto") {
      return {
        mode: "flatgrid",
        selector: ".tox-collection__item",
        initSize: {
          numColumns: 1,
          numRows: 1
        }
      };
    } else {
      return {
        mode: "matrix",
        selectors: {
          row: presets === "color" ? ".tox-swatches__row" : ".tox-collection__group",
          cell: presets === "color" ? `.${colorClass}` : `.${selectableClass}`
        }
      };
    }
  };
  const renderColorSwatchItem = (spec, backstage) => {
    const items = getColorItems(spec, backstage);
    const columns = backstage.colorinput.getColorCols();
    const presets = "color";
    const menuSpec = createPartialChoiceMenu(generate$6("menu-value"), items, (value2) => {
      spec.onAction({ value: value2 });
    }, columns, presets, ItemResponse$1.CLOSE_ON_EXECUTE, never, backstage.shared.providers);
    const widgetSpec = {
      ...menuSpec,
      markers: markers(presets),
      movement: deriveMenuMovement(columns, presets)
    };
    return {
      type: "widget",
      data: { value: generate$6("widget-id") },
      dom: {
        tag: "div",
        classes: ["tox-fancymenuitem"]
      },
      autofocus: true,
      components: [parts$f.widget(Menu.sketch(widgetSpec))]
    };
  };
  const getColorItems = (spec, backstage) => {
    const useCustomColors = spec.initData.allowCustomColors && backstage.colorinput.hasCustomColors();
    return spec.initData.colors.fold(() => getColors$1(backstage.colorinput.getColors(), useCustomColors), (colors) => colors.concat(getAdditionalColors(useCustomColors)));
  };
  const cellOverEvent = generate$6("cell-over");
  const cellExecuteEvent = generate$6("cell-execute");
  const makeCell = (row, col, labelId) => {
    const emitCellOver = (c) => emitWith(c, cellOverEvent, {
      row,
      col
    });
    const emitExecute2 = (c) => emitWith(c, cellExecuteEvent, {
      row,
      col
    });
    const onClick = (c, se) => {
      se.stop();
      emitExecute2(c);
    };
    return build$1({
      dom: {
        tag: "div",
        attributes: {
          role: "button",
          ["aria-labelledby"]: labelId
        }
      },
      behaviours: derive$1([
        config("insert-table-picker-cell", [
          run$1(mouseover(), Focusing.focus),
          run$1(execute$5(), emitExecute2),
          run$1(click(), onClick),
          run$1(tap(), onClick)
        ]),
        Toggling.config({
          toggleClass: "tox-insert-table-picker__selected",
          toggleOnExecute: false
        }),
        Focusing.config({ onFocus: emitCellOver })
      ])
    });
  };
  const makeCells = (labelId, numRows, numCols) => {
    const cells = [];
    for (let i2 = 0; i2 < numRows; i2++) {
      const row = [];
      for (let j = 0; j < numCols; j++) {
        row.push(makeCell(i2, j, labelId));
      }
      cells.push(row);
    }
    return cells;
  };
  const selectCells = (cells, selectedRow, selectedColumn, numRows, numColumns) => {
    for (let i2 = 0; i2 < numRows; i2++) {
      for (let j = 0; j < numColumns; j++) {
        Toggling.set(cells[i2][j], i2 <= selectedRow && j <= selectedColumn);
      }
    }
  };
  const makeComponents = (cells) => bind$3(cells, (cellRow) => map$2(cellRow, premade));
  const makeLabelText = (row, col) => text$1(`${col}x${row}`);
  const renderInsertTableMenuItem = (spec) => {
    const numRows = 10;
    const numColumns = 10;
    const sizeLabelId = generate$6("size-label");
    const cells = makeCells(sizeLabelId, numRows, numColumns);
    const emptyLabelText = makeLabelText(0, 0);
    const memLabel = record({
      dom: {
        tag: "span",
        classes: ["tox-insert-table-picker__label"],
        attributes: { id: sizeLabelId }
      },
      components: [emptyLabelText],
      behaviours: derive$1([Replacing.config({})])
    });
    return {
      type: "widget",
      data: { value: generate$6("widget-id") },
      dom: {
        tag: "div",
        classes: ["tox-fancymenuitem"]
      },
      autofocus: true,
      components: [parts$f.widget({
        dom: {
          tag: "div",
          classes: ["tox-insert-table-picker"]
        },
        components: makeComponents(cells).concat(memLabel.asSpec()),
        behaviours: derive$1([
          config("insert-table-picker", [
            runOnAttached((c) => {
              Replacing.set(memLabel.get(c), [emptyLabelText]);
            }),
            runWithTarget(cellOverEvent, (c, t2, e) => {
              const { row, col } = e.event;
              selectCells(cells, row, col, numRows, numColumns);
              Replacing.set(memLabel.get(c), [makeLabelText(row + 1, col + 1)]);
            }),
            runWithTarget(cellExecuteEvent, (c, _, e) => {
              const { row, col } = e.event;
              spec.onAction({
                numRows: row + 1,
                numColumns: col + 1
              });
              emit(c, sandboxClose());
            })
          ]),
          Keying.config({
            initSize: {
              numRows,
              numColumns
            },
            mode: "flatgrid",
            selector: '[role="button"]'
          })
        ])
      })]
    };
  };
  const fancyMenuItems = {
    inserttable: renderInsertTableMenuItem,
    colorswatch: renderColorSwatchItem
  };
  const renderFancyMenuItem = (spec, backstage) => get$g(fancyMenuItems, spec.fancytype).map((render2) => render2(spec, backstage));
  const renderNestedItem = (spec, itemResponse, providersBackstage, renderIcons = true, downwardsCaret = false) => {
    const caret = downwardsCaret ? renderDownwardsCaret(providersBackstage.icons) : renderSubmenuCaret(providersBackstage.icons);
    const getApi2 = (component) => ({
      isEnabled: () => !Disabling.isDisabled(component),
      setEnabled: (state) => Disabling.set(component, !state)
    });
    const structure = renderItemStructure({
      presets: "normal",
      iconContent: spec.icon,
      textContent: spec.text,
      htmlContent: Optional.none(),
      ariaLabel: spec.text,
      caret: Optional.some(caret),
      checkMark: Optional.none(),
      shortcutContent: spec.shortcut
    }, providersBackstage, renderIcons);
    return renderCommonItem({
      data: buildData(spec),
      getApi: getApi2,
      enabled: spec.enabled,
      onAction: noop,
      onSetup: spec.onSetup,
      triggersSubmenu: true,
      itemBehaviours: []
    }, structure, itemResponse, providersBackstage);
  };
  const renderNormalItem = (spec, itemResponse, providersBackstage, renderIcons = true) => {
    const getApi2 = (component) => ({
      isEnabled: () => !Disabling.isDisabled(component),
      setEnabled: (state) => Disabling.set(component, !state)
    });
    const structure = renderItemStructure({
      presets: "normal",
      iconContent: spec.icon,
      textContent: spec.text,
      htmlContent: Optional.none(),
      ariaLabel: spec.text,
      caret: Optional.none(),
      checkMark: Optional.none(),
      shortcutContent: spec.shortcut
    }, providersBackstage, renderIcons);
    return renderCommonItem({
      data: buildData(spec),
      getApi: getApi2,
      enabled: spec.enabled,
      onAction: spec.onAction,
      onSetup: spec.onSetup,
      triggersSubmenu: false,
      itemBehaviours: []
    }, structure, itemResponse, providersBackstage);
  };
  const renderSeparatorItem = (spec) => ({
    type: "separator",
    dom: {
      tag: "div",
      classes: [
        selectableClass,
        groupHeadingClass
      ]
    },
    components: spec.text.map(text$1).toArray()
  });
  const renderToggleMenuItem = (spec, itemResponse, providersBackstage, renderIcons = true) => {
    const getApi2 = (component) => ({
      setActive: (state) => {
        Toggling.set(component, state);
      },
      isActive: () => Toggling.isOn(component),
      isEnabled: () => !Disabling.isDisabled(component),
      setEnabled: (state) => Disabling.set(component, !state)
    });
    const structure = renderItemStructure({
      iconContent: spec.icon,
      textContent: spec.text,
      htmlContent: Optional.none(),
      ariaLabel: spec.text,
      checkMark: Optional.some(renderCheckmark(providersBackstage.icons)),
      caret: Optional.none(),
      shortcutContent: spec.shortcut,
      presets: "normal",
      meta: spec.meta
    }, providersBackstage, renderIcons);
    return deepMerge(renderCommonItem({
      data: buildData(spec),
      enabled: spec.enabled,
      getApi: getApi2,
      onAction: spec.onAction,
      onSetup: spec.onSetup,
      triggersSubmenu: false,
      itemBehaviours: []
    }, structure, itemResponse, providersBackstage), {
      toggling: {
        toggleClass: tickedClass,
        toggleOnExecute: false,
        selected: spec.active
      }
    });
  };
  const autocomplete = renderAutocompleteItem;
  const separator$3 = renderSeparatorItem;
  const normal = renderNormalItem;
  const nested = renderNestedItem;
  const toggle$1 = renderToggleMenuItem;
  const fancy = renderFancyMenuItem;
  const card = renderCardMenuItem;
  const getCoupled = (component, coupleConfig, coupleState, name2) => coupleState.getOrCreate(component, coupleConfig, name2);
  const getExistingCoupled = (component, coupleConfig, coupleState, name2) => coupleState.getExisting(component, coupleConfig, name2);
  var CouplingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    getCoupled,
    getExistingCoupled
  });
  var CouplingSchema = [requiredOf("others", setOf(Result.value, anyValue()))];
  const init$a = () => {
    const coupled = {};
    const lookupCoupled = (coupleConfig, coupledName) => {
      const available = keys(coupleConfig.others);
      if (available.length === 0) {
        throw new Error("Cannot find any known coupled components");
      } else {
        return get$g(coupled, coupledName);
      }
    };
    const getOrCreate = (component, coupleConfig, name2) => {
      return lookupCoupled(coupleConfig, name2).getOrThunk(() => {
        const builder2 = get$g(coupleConfig.others, name2).getOrDie("No information found for coupled component: " + name2);
        const spec = builder2(component);
        const built = component.getSystem().build(spec);
        coupled[name2] = built;
        return built;
      });
    };
    const getExisting = (component, coupleConfig, name2) => {
      return lookupCoupled(coupleConfig, name2).orThunk(() => {
        get$g(coupleConfig.others, name2).getOrDie("No information found for coupled component: " + name2);
        return Optional.none();
      });
    };
    const readState = constant$1({});
    return nu$8({
      readState,
      getExisting,
      getOrCreate
    });
  };
  var CouplingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$a
  });
  const Coupling = create$3({
    fields: CouplingSchema,
    name: "coupling",
    apis: CouplingApis,
    state: CouplingState
  });
  const nu$3 = (baseFn) => {
    let data = Optional.none();
    let callbacks = [];
    const map2 = (f) => nu$3((nCallback) => {
      get2((data2) => {
        nCallback(f(data2));
      });
    });
    const get2 = (nCallback) => {
      if (isReady()) {
        call(nCallback);
      } else {
        callbacks.push(nCallback);
      }
    };
    const set2 = (x) => {
      if (!isReady()) {
        data = Optional.some(x);
        run2(callbacks);
        callbacks = [];
      }
    };
    const isReady = () => data.isSome();
    const run2 = (cbs) => {
      each$1(cbs, call);
    };
    const call = (cb) => {
      data.each((x) => {
        setTimeout(() => {
          cb(x);
        }, 0);
      });
    };
    baseFn(set2);
    return {
      get: get2,
      map: map2,
      isReady
    };
  };
  const pure$1 = (a) => nu$3((callback) => {
    callback(a);
  });
  const LazyValue = {
    nu: nu$3,
    pure: pure$1
  };
  const errorReporter = (err) => {
    setTimeout(() => {
      throw err;
    }, 0);
  };
  const make$5 = (run2) => {
    const get2 = (callback) => {
      run2().then(callback, errorReporter);
    };
    const map2 = (fab) => {
      return make$5(() => run2().then(fab));
    };
    const bind2 = (aFutureB) => {
      return make$5(() => run2().then((v) => aFutureB(v).toPromise()));
    };
    const anonBind = (futureB) => {
      return make$5(() => run2().then(() => futureB.toPromise()));
    };
    const toLazy = () => {
      return LazyValue.nu(get2);
    };
    const toCached = () => {
      let cache = null;
      return make$5(() => {
        if (cache === null) {
          cache = run2();
        }
        return cache;
      });
    };
    const toPromise = run2;
    return {
      map: map2,
      bind: bind2,
      anonBind,
      toLazy,
      toCached,
      toPromise,
      get: get2
    };
  };
  const nu$2 = (baseFn) => {
    return make$5(() => new Promise(baseFn));
  };
  const pure = (a) => {
    return make$5(() => Promise.resolve(a));
  };
  const Future = {
    nu: nu$2,
    pure
  };
  const suffix = constant$1("sink");
  const partType$1 = constant$1(optional({
    name: suffix(),
    overrides: constant$1({
      dom: { tag: "div" },
      behaviours: derive$1([Positioning.config({ useFixed: always })]),
      events: derive$2([
        cutter(keydown()),
        cutter(mousedown()),
        cutter(click())
      ])
    })
  }));
  const getAnchor = (detail, component) => {
    const hotspot = detail.getHotspot(component).getOr(component);
    const type2 = "hotspot";
    const overrides = detail.getAnchorOverrides();
    return detail.layouts.fold(() => ({
      type: type2,
      hotspot,
      overrides
    }), (layouts2) => ({
      type: type2,
      hotspot,
      overrides,
      layouts: layouts2
    }));
  };
  const fetch2 = (detail, mapFetch, component) => {
    const fetcher = detail.fetch;
    return fetcher(component).map(mapFetch);
  };
  const openF = (detail, mapFetch, anchor2, component, sandbox, externals, highlightOnOpen) => {
    const futureData = fetch2(detail, mapFetch, component);
    const getLazySink = getSink(component, detail);
    return futureData.map((tdata) => tdata.bind((data) => Optional.from(tieredMenu.sketch({
      ...externals.menu(),
      uid: generate$5(""),
      data,
      highlightOnOpen,
      onOpenMenu: (tmenu, menu2) => {
        const sink = getLazySink().getOrDie();
        Positioning.position(sink, menu2, { anchor: anchor2 });
        Sandboxing.decloak(sandbox);
      },
      onOpenSubmenu: (tmenu, item2, submenu) => {
        const sink = getLazySink().getOrDie();
        Positioning.position(sink, submenu, {
          anchor: {
            type: "submenu",
            item: item2
          }
        });
        Sandboxing.decloak(sandbox);
      },
      onRepositionMenu: (tmenu, primaryMenu, submenuTriggers) => {
        const sink = getLazySink().getOrDie();
        Positioning.position(sink, primaryMenu, { anchor: anchor2 });
        each$1(submenuTriggers, (st) => {
          Positioning.position(sink, st.triggeredMenu, {
            anchor: {
              type: "submenu",
              item: st.triggeringItem
            }
          });
        });
      },
      onEscape: () => {
        Focusing.focus(component);
        Sandboxing.close(sandbox);
        return Optional.some(true);
      }
    }))));
  };
  const open = (detail, mapFetch, hotspot, sandbox, externals, onOpenSync, highlightOnOpen) => {
    const anchor2 = getAnchor(detail, hotspot);
    const processed = openF(detail, mapFetch, anchor2, hotspot, sandbox, externals, highlightOnOpen);
    return processed.map((tdata) => {
      tdata.fold(() => {
        if (Sandboxing.isOpen(sandbox)) {
          Sandboxing.close(sandbox);
        }
      }, (data) => {
        Sandboxing.cloak(sandbox);
        Sandboxing.open(sandbox, data);
        onOpenSync(sandbox);
      });
      return sandbox;
    });
  };
  const close = (detail, mapFetch, component, sandbox, _externals, _onOpenSync, _highlightOnOpen) => {
    Sandboxing.close(sandbox);
    return Future.pure(sandbox);
  };
  const togglePopup = (detail, mapFetch, hotspot, externals, onOpenSync, highlightOnOpen) => {
    const sandbox = Coupling.getCoupled(hotspot, "sandbox");
    const showing = Sandboxing.isOpen(sandbox);
    const action = showing ? close : open;
    return action(detail, mapFetch, hotspot, sandbox, externals, onOpenSync, highlightOnOpen);
  };
  const matchWidth = (hotspot, container, useMinWidth) => {
    const menu2 = Composing.getCurrent(container).getOr(container);
    const buttonWidth = get$c(hotspot.element);
    if (useMinWidth) {
      set$8(menu2.element, "min-width", buttonWidth + "px");
    } else {
      set$7(menu2.element, buttonWidth);
    }
  };
  const getSink = (anyInSystem, sinkDetail) => anyInSystem.getSystem().getByUid(sinkDetail.uid + "-" + suffix()).map((internalSink) => () => Result.value(internalSink)).getOrThunk(() => sinkDetail.lazySink.fold(() => () => Result.error(new Error("No internal sink is specified, nor could an external sink be found")), (lazySinkFn) => () => lazySinkFn(anyInSystem)));
  const doRepositionMenus = (sandbox) => {
    Sandboxing.getState(sandbox).each((tmenu) => {
      tieredMenu.repositionMenus(tmenu);
    });
  };
  const makeSandbox$1 = (detail, hotspot, extras) => {
    const ariaControls = manager();
    const onOpen = (component, menu2) => {
      const anchor2 = getAnchor(detail, hotspot);
      ariaControls.link(hotspot.element);
      if (detail.matchWidth) {
        matchWidth(anchor2.hotspot, menu2, detail.useMinWidth);
      }
      detail.onOpen(anchor2, component, menu2);
      if (extras !== void 0 && extras.onOpen !== void 0) {
        extras.onOpen(component, menu2);
      }
    };
    const onClose = (component, menu2) => {
      ariaControls.unlink(hotspot.element);
      if (extras !== void 0 && extras.onClose !== void 0) {
        extras.onClose(component, menu2);
      }
    };
    const lazySink = getSink(hotspot, detail);
    return {
      dom: {
        tag: "div",
        classes: detail.sandboxClasses,
        attributes: {
          id: ariaControls.id,
          role: "listbox"
        }
      },
      behaviours: SketchBehaviours.augment(detail.sandboxBehaviours, [
        Representing.config({
          store: {
            mode: "memory",
            initialValue: hotspot
          }
        }),
        Sandboxing.config({
          onOpen,
          onClose,
          isPartOf: (container, data, queryElem) => {
            return isPartOf$1(data, queryElem) || isPartOf$1(hotspot, queryElem);
          },
          getAttachPoint: () => {
            return lazySink().getOrDie();
          }
        }),
        Composing.config({
          find: (sandbox) => {
            return Sandboxing.getState(sandbox).bind((menu2) => Composing.getCurrent(menu2));
          }
        }),
        Receiving.config({
          channels: {
            ...receivingChannel$1({ isExtraPart: never }),
            ...receivingChannel({ doReposition: doRepositionMenus })
          }
        })
      ])
    };
  };
  const repositionMenus = (comp) => {
    const sandbox = Coupling.getCoupled(comp, "sandbox");
    doRepositionMenus(sandbox);
  };
  const sandboxFields = () => [
    defaulted("sandboxClasses", []),
    SketchBehaviours.field("sandboxBehaviours", [
      Composing,
      Receiving,
      Sandboxing,
      Representing
    ])
  ];
  const schema$k = constant$1([
    required$1("dom"),
    required$1("fetch"),
    onHandler("onOpen"),
    onKeyboardHandler("onExecute"),
    defaulted("getHotspot", Optional.some),
    defaulted("getAnchorOverrides", constant$1({})),
    schema$y(),
    field("dropdownBehaviours", [
      Toggling,
      Coupling,
      Keying,
      Focusing
    ]),
    required$1("toggleClass"),
    defaulted("eventOrder", {}),
    option$3("lazySink"),
    defaulted("matchWidth", false),
    defaulted("useMinWidth", false),
    option$3("role")
  ].concat(sandboxFields()));
  const parts$e = constant$1([
    external({
      schema: [
        tieredMenuMarkers(),
        defaulted("fakeFocus", false)
      ],
      name: "menu",
      defaults: (detail) => {
        return { onExecute: detail.onExecute };
      }
    }),
    partType$1()
  ]);
  const factory$i = (detail, components2, _spec, externals) => {
    const lookupAttr = (attr) => get$g(detail.dom, "attributes").bind((attrs) => get$g(attrs, attr));
    const switchToMenu = (sandbox) => {
      Sandboxing.getState(sandbox).each((tmenu) => {
        tieredMenu.highlightPrimary(tmenu);
      });
    };
    const togglePopup$1 = (dropdownComp, onOpenSync, highlightOnOpen) => {
      return togglePopup(detail, identity, dropdownComp, externals, onOpenSync, highlightOnOpen);
    };
    const action = (component) => {
      const onOpenSync = switchToMenu;
      togglePopup$1(component, onOpenSync, HighlightOnOpen.HighlightMenuAndItem).get(noop);
    };
    const apis = {
      expand: (comp) => {
        if (!Toggling.isOn(comp)) {
          togglePopup$1(comp, noop, HighlightOnOpen.HighlightNone).get(noop);
        }
      },
      open: (comp) => {
        if (!Toggling.isOn(comp)) {
          togglePopup$1(comp, noop, HighlightOnOpen.HighlightMenuAndItem).get(noop);
        }
      },
      refetch: (comp) => {
        const optSandbox = Coupling.getExistingCoupled(comp, "sandbox");
        return optSandbox.fold(() => {
          return togglePopup$1(comp, noop, HighlightOnOpen.HighlightMenuAndItem).map(noop);
        }, (sandboxComp) => {
          return open(detail, identity, comp, sandboxComp, externals, noop, HighlightOnOpen.HighlightMenuAndItem).map(noop);
        });
      },
      isOpen: Toggling.isOn,
      close: (comp) => {
        if (Toggling.isOn(comp)) {
          togglePopup$1(comp, noop, HighlightOnOpen.HighlightMenuAndItem).get(noop);
        }
      },
      repositionMenus: (comp) => {
        if (Toggling.isOn(comp)) {
          repositionMenus(comp);
        }
      }
    };
    const triggerExecute = (comp, _se) => {
      emitExecute(comp);
      return Optional.some(true);
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: augment(detail.dropdownBehaviours, [
        Toggling.config({
          toggleClass: detail.toggleClass,
          aria: { mode: "expanded" }
        }),
        Coupling.config({
          others: {
            sandbox: (hotspot) => {
              return makeSandbox$1(detail, hotspot, {
                onOpen: () => Toggling.on(hotspot),
                onClose: () => Toggling.off(hotspot)
              });
            }
          }
        }),
        Keying.config({
          mode: "special",
          onSpace: triggerExecute,
          onEnter: triggerExecute,
          onDown: (comp, _se) => {
            if (Dropdown.isOpen(comp)) {
              const sandbox = Coupling.getCoupled(comp, "sandbox");
              switchToMenu(sandbox);
            } else {
              Dropdown.open(comp);
            }
            return Optional.some(true);
          },
          onEscape: (comp, _se) => {
            if (Dropdown.isOpen(comp)) {
              Dropdown.close(comp);
              return Optional.some(true);
            } else {
              return Optional.none();
            }
          }
        }),
        Focusing.config({})
      ]),
      events: events$a(Optional.some(action)),
      eventOrder: {
        ...detail.eventOrder,
        [execute$5()]: [
          "disabling",
          "toggling",
          "alloy.base.behaviour"
        ]
      },
      apis,
      domModification: {
        attributes: {
          "aria-haspopup": "true",
          ...detail.role.fold(() => ({}), (role) => ({ role })),
          ...detail.dom.tag === "button" ? { type: lookupAttr("type").getOr("button") } : {}
        }
      }
    };
  };
  const Dropdown = composite({
    name: "Dropdown",
    configFields: schema$k(),
    partFields: parts$e(),
    factory: factory$i,
    apis: {
      open: (apis, comp) => apis.open(comp),
      refetch: (apis, comp) => apis.refetch(comp),
      expand: (apis, comp) => apis.expand(comp),
      close: (apis, comp) => apis.close(comp),
      isOpen: (apis, comp) => apis.isOpen(comp),
      repositionMenus: (apis, comp) => apis.repositionMenus(comp)
    }
  });
  const identifyMenuLayout = (searchMode) => {
    switch (searchMode.searchMode) {
      case "no-search": {
        return { menuType: "normal" };
      }
      default: {
        return {
          menuType: "searchable",
          searchMode
        };
      }
    }
  };
  const handleRefetchTrigger = (originalSandboxComp) => {
    const dropdown = Representing.getValue(originalSandboxComp);
    const optSearcherState = findWithinSandbox(originalSandboxComp).map(saveState);
    Dropdown.refetch(dropdown).get(() => {
      const newSandboxComp = Coupling.getCoupled(dropdown, "sandbox");
      optSearcherState.each((searcherState) => findWithinSandbox(newSandboxComp).each((inputComp) => restoreState(inputComp, searcherState)));
    });
  };
  const handleRedirectToMenuItem = (sandboxComp, se) => {
    getActiveMenuItemFrom(sandboxComp).each((activeItem) => {
      retargetAndDispatchWith(sandboxComp, activeItem.element, se.event.eventType, se.event.interactionEvent);
    });
  };
  const getActiveMenuItemFrom = (sandboxComp) => {
    return Sandboxing.getState(sandboxComp).bind(Highlighting.getHighlighted).bind(Highlighting.getHighlighted);
  };
  const getSearchResults = (activeMenuComp) => {
    return has(activeMenuComp.element, searchResultsClass) ? Optional.some(activeMenuComp.element) : descendant(activeMenuComp.element, "." + searchResultsClass);
  };
  const updateAriaOnHighlight = (tmenuComp, menuComp, itemComp) => {
    findWithinMenu(tmenuComp).each((inputComp) => {
      setActiveDescendant(inputComp, itemComp);
      const optActiveResults = getSearchResults(menuComp);
      optActiveResults.each((resultsElem) => {
        getOpt(resultsElem, "id").each((controlledId) => set$9(inputComp.element, "aria-controls", controlledId));
      });
    });
    set$9(itemComp.element, "aria-selected", "true");
  };
  const updateAriaOnDehighlight = (tmenuComp, menuComp, itemComp) => {
    set$9(itemComp.element, "aria-selected", "false");
  };
  const focusSearchField = (tmenuComp) => {
    findWithinMenu(tmenuComp).each((searcherComp) => Focusing.focus(searcherComp));
  };
  const getSearchPattern = (dropdownComp) => {
    const optSandboxComp = Coupling.getExistingCoupled(dropdownComp, "sandbox");
    return optSandboxComp.bind(findWithinSandbox).map(saveState).map((state) => state.fetchPattern).getOr("");
  };
  var FocusMode;
  (function(FocusMode2) {
    FocusMode2[FocusMode2["ContentFocus"] = 0] = "ContentFocus";
    FocusMode2[FocusMode2["UiFocus"] = 1] = "UiFocus";
  })(FocusMode || (FocusMode = {}));
  const createMenuItemFromBridge = (item2, itemResponse, backstage, menuHasIcons2, isHorizontalMenu) => {
    const providersBackstage = backstage.shared.providers;
    const parseForHorizontalMenu = (menuitem) => !isHorizontalMenu ? menuitem : {
      ...menuitem,
      shortcut: Optional.none(),
      icon: menuitem.text.isSome() ? Optional.none() : menuitem.icon
    };
    switch (item2.type) {
      case "menuitem":
        return createMenuItem(item2).fold(handleError, (d) => Optional.some(normal(parseForHorizontalMenu(d), itemResponse, providersBackstage, menuHasIcons2)));
      case "nestedmenuitem":
        return createNestedMenuItem(item2).fold(handleError, (d) => Optional.some(nested(parseForHorizontalMenu(d), itemResponse, providersBackstage, menuHasIcons2, isHorizontalMenu)));
      case "togglemenuitem":
        return createToggleMenuItem(item2).fold(handleError, (d) => Optional.some(toggle$1(parseForHorizontalMenu(d), itemResponse, providersBackstage, menuHasIcons2)));
      case "separator":
        return createSeparatorMenuItem(item2).fold(handleError, (d) => Optional.some(separator$3(d)));
      case "fancymenuitem":
        return createFancyMenuItem(item2).fold(handleError, (d) => fancy(d, backstage));
      default: {
        console.error("Unknown item in general menu", item2);
        return Optional.none();
      }
    }
  };
  const createAutocompleteItems = (items, matchText, onItemValueHandler, columns, itemResponse, sharedBackstage, highlightOn) => {
    const renderText2 = columns === 1;
    const renderIcons = !renderText2 || menuHasIcons(items);
    return cat(map$2(items, (item2) => {
      switch (item2.type) {
        case "separator":
          return createSeparatorItem(item2).fold(handleError, (d) => Optional.some(separator$3(d)));
        case "cardmenuitem":
          return createCardMenuItem(item2).fold(handleError, (d) => Optional.some(card({
            ...d,
            onAction: (api2) => {
              d.onAction(api2);
              onItemValueHandler(d.value, d.meta);
            }
          }, itemResponse, sharedBackstage, {
            itemBehaviours: tooltipBehaviour(d.meta, sharedBackstage),
            cardText: {
              matchText,
              highlightOn
            }
          })));
        case "autocompleteitem":
        default:
          return createAutocompleterItem(item2).fold(handleError, (d) => Optional.some(autocomplete(d, matchText, renderText2, "normal", onItemValueHandler, itemResponse, sharedBackstage, renderIcons)));
      }
    }));
  };
  const createPartialMenu = (value2, items, itemResponse, backstage, isHorizontalMenu, searchMode) => {
    const hasIcons = menuHasIcons(items);
    const alloyItems = cat(map$2(items, (item2) => {
      const itemHasIcon = (i2) => isHorizontalMenu ? !has$2(i2, "text") : hasIcons;
      const createItem = (i2) => createMenuItemFromBridge(i2, itemResponse, backstage, itemHasIcon(i2), isHorizontalMenu);
      if (item2.type === "nestedmenuitem" && item2.getSubmenuItems().length <= 0) {
        return createItem({
          ...item2,
          enabled: false
        });
      } else {
        return createItem(item2);
      }
    }));
    const menuLayout = identifyMenuLayout(searchMode);
    const createPartial = isHorizontalMenu ? createHorizontalPartialMenuWithAlloyItems : createPartialMenuWithAlloyItems;
    return createPartial(value2, hasIcons, alloyItems, 1, menuLayout);
  };
  const createTieredDataFrom = (partialMenu) => tieredMenu.singleData(partialMenu.value, partialMenu);
  const createInlineMenuFrom = (partialMenu, columns, focusMode, presets) => {
    const movement = deriveMenuMovement(columns, presets);
    const menuMarkers = markers(presets);
    return {
      data: createTieredDataFrom({
        ...partialMenu,
        movement,
        menuBehaviours: SimpleBehaviours.unnamedEvents(columns !== "auto" ? [] : [runOnAttached((comp, _se) => {
          detectSize(comp, 4, menuMarkers.item).each(({ numColumns, numRows }) => {
            Keying.setGridSize(comp, numRows, numColumns);
          });
        })])
      }),
      menu: {
        markers: markers(presets),
        fakeFocus: focusMode === FocusMode.ContentFocus
      }
    };
  };
  const getAutocompleterRange = (dom2, initRange) => {
    return detect(SugarElement.fromDom(initRange.startContainer)).map((elm) => {
      const range2 = dom2.createRng();
      range2.selectNode(elm.dom);
      return range2;
    });
  };
  const register$b = (editor, sharedBackstage) => {
    const processingAction = Cell(false);
    const activeState = Cell(false);
    const autocompleter = build$1(InlineView.sketch({
      dom: {
        tag: "div",
        classes: ["tox-autocompleter"]
      },
      components: [],
      fireDismissalEventInstead: {},
      inlineBehaviours: derive$1([config("dismissAutocompleter", [run$1(dismissRequested(), () => cancelIfNecessary())])]),
      lazySink: sharedBackstage.getSink
    }));
    const isMenuOpen = () => InlineView.isOpen(autocompleter);
    const isActive = activeState.get;
    const hideIfNecessary = () => {
      if (isMenuOpen()) {
        InlineView.hide(autocompleter);
      }
    };
    const getMenu = () => InlineView.getContent(autocompleter).bind((tmenu) => {
      return get$h(tmenu.components(), 0);
    });
    const cancelIfNecessary = () => editor.execCommand("mceAutocompleterClose");
    const getCombinedItems = (matches) => {
      const columns = findMap(matches, (m) => Optional.from(m.columns)).getOr(1);
      return bind$3(matches, (match) => {
        const choices = match.items;
        return createAutocompleteItems(choices, match.matchText, (itemValue, itemMeta) => {
          const nr = editor.selection.getRng();
          getAutocompleterRange(editor.dom, nr).each((range2) => {
            const autocompleterApi = {
              hide: () => cancelIfNecessary(),
              reload: (fetchOptions) => {
                hideIfNecessary();
                editor.execCommand("mceAutocompleterReload", false, { fetchOptions });
              }
            };
            processingAction.set(true);
            match.onAction(autocompleterApi, range2, itemValue, itemMeta);
            processingAction.set(false);
          });
        }, columns, ItemResponse$1.BUBBLE_TO_SANDBOX, sharedBackstage, match.highlightOn);
      });
    };
    const display = (lookupData, items) => {
      findIn(SugarElement.fromDom(editor.getBody())).each((element2) => {
        const columns = findMap(lookupData, (ld) => Optional.from(ld.columns)).getOr(1);
        InlineView.showMenuAt(autocompleter, {
          anchor: {
            type: "node",
            root: SugarElement.fromDom(editor.getBody()),
            node: Optional.from(element2)
          }
        }, createInlineMenuFrom(createPartialMenuWithAlloyItems("autocompleter-value", true, items, columns, { menuType: "normal" }), columns, FocusMode.ContentFocus, "normal"));
      });
      getMenu().each(Highlighting.highlightFirst);
    };
    const updateDisplay = (lookupData) => {
      const combinedItems = getCombinedItems(lookupData);
      if (combinedItems.length > 0) {
        display(lookupData, combinedItems);
      } else {
        hideIfNecessary();
      }
    };
    editor.on("AutocompleterStart", ({ lookupData }) => {
      activeState.set(true);
      processingAction.set(false);
      updateDisplay(lookupData);
    });
    editor.on("AutocompleterUpdate", ({ lookupData }) => updateDisplay(lookupData));
    editor.on("AutocompleterEnd", () => {
      hideIfNecessary();
      activeState.set(false);
      processingAction.set(false);
    });
    const autocompleterUiApi = {
      cancelIfNecessary,
      isMenuOpen,
      isActive,
      isProcessingAction: processingAction.get,
      getMenu
    };
    AutocompleterEditorEvents.setup(autocompleterUiApi, editor);
  };
  const Autocompleter = { register: register$b };
  const closest = (scope, selector, isRoot) => closest$1(scope, selector, isRoot).isSome();
  const DelayedFunction = (fun, delay) => {
    let ref = null;
    const schedule = (...args) => {
      ref = setTimeout(() => {
        fun.apply(null, args);
        ref = null;
      }, delay);
    };
    const cancel = () => {
      if (ref !== null) {
        clearTimeout(ref);
        ref = null;
      }
    };
    return {
      cancel,
      schedule
    };
  };
  const SIGNIFICANT_MOVE = 5;
  const LONGPRESS_DELAY = 400;
  const getTouch = (event2) => {
    const raw = event2.raw;
    if (raw.touches === void 0 || raw.touches.length !== 1) {
      return Optional.none();
    }
    return Optional.some(raw.touches[0]);
  };
  const isFarEnough = (touch2, data) => {
    const distX = Math.abs(touch2.clientX - data.x);
    const distY = Math.abs(touch2.clientY - data.y);
    return distX > SIGNIFICANT_MOVE || distY > SIGNIFICANT_MOVE;
  };
  const monitor = (settings) => {
    const startData = value$2();
    const longpressFired = Cell(false);
    const longpress$1 = DelayedFunction((event2) => {
      settings.triggerEvent(longpress(), event2);
      longpressFired.set(true);
    }, LONGPRESS_DELAY);
    const handleTouchstart = (event2) => {
      getTouch(event2).each((touch2) => {
        longpress$1.cancel();
        const data = {
          x: touch2.clientX,
          y: touch2.clientY,
          target: event2.target
        };
        longpress$1.schedule(event2);
        longpressFired.set(false);
        startData.set(data);
      });
      return Optional.none();
    };
    const handleTouchmove = (event2) => {
      longpress$1.cancel();
      getTouch(event2).each((touch2) => {
        startData.on((data) => {
          if (isFarEnough(touch2, data)) {
            startData.clear();
          }
        });
      });
      return Optional.none();
    };
    const handleTouchend = (event2) => {
      longpress$1.cancel();
      const isSame = (data) => eq(data.target, event2.target);
      return startData.get().filter(isSame).map((_data) => {
        if (longpressFired.get()) {
          event2.prevent();
          return false;
        } else {
          return settings.triggerEvent(tap(), event2);
        }
      });
    };
    const handlers2 = wrapAll([
      {
        key: touchstart(),
        value: handleTouchstart
      },
      {
        key: touchmove(),
        value: handleTouchmove
      },
      {
        key: touchend(),
        value: handleTouchend
      }
    ]);
    const fireIfReady = (event2, type2) => get$g(handlers2, type2).bind((handler) => handler(event2));
    return { fireIfReady };
  };
  const isDangerous = (event2) => {
    const keyEv = event2.raw;
    return keyEv.which === BACKSPACE[0] && !contains$2([
      "input",
      "textarea"
    ], name$3(event2.target)) && !closest(event2.target, '[contenteditable="true"]');
  };
  const setup$d = (container, rawSettings) => {
    const settings = {
      stopBackspace: true,
      ...rawSettings
    };
    const pointerEvents2 = [
      "touchstart",
      "touchmove",
      "touchend",
      "touchcancel",
      "gesturestart",
      "mousedown",
      "mouseup",
      "mouseover",
      "mousemove",
      "mouseout",
      "click"
    ];
    const tapEvent = monitor(settings);
    const simpleEvents = map$2(pointerEvents2.concat([
      "selectstart",
      "input",
      "contextmenu",
      "change",
      "transitionend",
      "transitioncancel",
      "drag",
      "dragstart",
      "dragend",
      "dragenter",
      "dragleave",
      "dragover",
      "drop",
      "keyup"
    ]), (type2) => bind(container, type2, (event2) => {
      tapEvent.fireIfReady(event2, type2).each((tapStopped) => {
        if (tapStopped) {
          event2.kill();
        }
      });
      const stopped = settings.triggerEvent(type2, event2);
      if (stopped) {
        event2.kill();
      }
    }));
    const pasteTimeout = value$2();
    const onPaste = bind(container, "paste", (event2) => {
      tapEvent.fireIfReady(event2, "paste").each((tapStopped) => {
        if (tapStopped) {
          event2.kill();
        }
      });
      const stopped = settings.triggerEvent("paste", event2);
      if (stopped) {
        event2.kill();
      }
      pasteTimeout.set(setTimeout(() => {
        settings.triggerEvent(postPaste(), event2);
      }, 0));
    });
    const onKeydown = bind(container, "keydown", (event2) => {
      const stopped = settings.triggerEvent("keydown", event2);
      if (stopped) {
        event2.kill();
      } else if (settings.stopBackspace && isDangerous(event2)) {
        event2.prevent();
      }
    });
    const onFocusIn = bind(container, "focusin", (event2) => {
      const stopped = settings.triggerEvent("focusin", event2);
      if (stopped) {
        event2.kill();
      }
    });
    const focusoutTimeout = value$2();
    const onFocusOut = bind(container, "focusout", (event2) => {
      const stopped = settings.triggerEvent("focusout", event2);
      if (stopped) {
        event2.kill();
      }
      focusoutTimeout.set(setTimeout(() => {
        settings.triggerEvent(postBlur(), event2);
      }, 0));
    });
    const unbind2 = () => {
      each$1(simpleEvents, (e) => {
        e.unbind();
      });
      onKeydown.unbind();
      onFocusIn.unbind();
      onFocusOut.unbind();
      onPaste.unbind();
      pasteTimeout.on(clearTimeout);
      focusoutTimeout.on(clearTimeout);
    };
    return { unbind: unbind2 };
  };
  const derive = (rawEvent, rawTarget) => {
    const source = get$g(rawEvent, "target").getOr(rawTarget);
    return Cell(source);
  };
  const fromSource = (event2, source) => {
    const stopper2 = Cell(false);
    const cutter2 = Cell(false);
    const stop2 = () => {
      stopper2.set(true);
    };
    const cut = () => {
      cutter2.set(true);
    };
    return {
      stop: stop2,
      cut,
      isStopped: stopper2.get,
      isCut: cutter2.get,
      event: event2,
      setSource: source.set,
      getSource: source.get
    };
  };
  const fromExternal = (event2) => {
    const stopper2 = Cell(false);
    const stop2 = () => {
      stopper2.set(true);
    };
    return {
      stop: stop2,
      cut: noop,
      isStopped: stopper2.get,
      isCut: never,
      event: event2,
      setSource: die("Cannot set source of a broadcasted event"),
      getSource: die("Cannot get source of a broadcasted event")
    };
  };
  const adt$1 = Adt.generate([
    { stopped: [] },
    { resume: ["element"] },
    { complete: [] }
  ]);
  const doTriggerHandler = (lookup2, eventType, rawEvent, target, source, logger) => {
    const handler = lookup2(eventType, target);
    const simulatedEvent = fromSource(rawEvent, source);
    return handler.fold(() => {
      logger.logEventNoHandlers(eventType, target);
      return adt$1.complete();
    }, (handlerInfo) => {
      const descHandler = handlerInfo.descHandler;
      const eventHandler2 = getCurried(descHandler);
      eventHandler2(simulatedEvent);
      if (simulatedEvent.isStopped()) {
        logger.logEventStopped(eventType, handlerInfo.element, descHandler.purpose);
        return adt$1.stopped();
      } else if (simulatedEvent.isCut()) {
        logger.logEventCut(eventType, handlerInfo.element, descHandler.purpose);
        return adt$1.complete();
      } else {
        return parent(handlerInfo.element).fold(() => {
          logger.logNoParent(eventType, handlerInfo.element, descHandler.purpose);
          return adt$1.complete();
        }, (parent2) => {
          logger.logEventResponse(eventType, handlerInfo.element, descHandler.purpose);
          return adt$1.resume(parent2);
        });
      }
    });
  };
  const doTriggerOnUntilStopped = (lookup2, eventType, rawEvent, rawTarget, source, logger) => doTriggerHandler(lookup2, eventType, rawEvent, rawTarget, source, logger).fold(always, (parent2) => doTriggerOnUntilStopped(lookup2, eventType, rawEvent, parent2, source, logger), never);
  const triggerHandler = (lookup2, eventType, rawEvent, target, logger) => {
    const source = derive(rawEvent, target);
    return doTriggerHandler(lookup2, eventType, rawEvent, target, source, logger);
  };
  const broadcast = (listeners, rawEvent, _logger) => {
    const simulatedEvent = fromExternal(rawEvent);
    each$1(listeners, (listener) => {
      const descHandler = listener.descHandler;
      const handler = getCurried(descHandler);
      handler(simulatedEvent);
    });
    return simulatedEvent.isStopped();
  };
  const triggerUntilStopped = (lookup2, eventType, rawEvent, logger) => triggerOnUntilStopped(lookup2, eventType, rawEvent, rawEvent.target, logger);
  const triggerOnUntilStopped = (lookup2, eventType, rawEvent, rawTarget, logger) => {
    const source = derive(rawEvent, rawTarget);
    return doTriggerOnUntilStopped(lookup2, eventType, rawEvent, rawTarget, source, logger);
  };
  const eventHandler = (element2, descHandler) => ({
    element: element2,
    descHandler
  });
  const broadcastHandler = (id, handler) => ({
    id,
    descHandler: handler
  });
  const EventRegistry = () => {
    const registry = {};
    const registerId = (extraArgs, id, events2) => {
      each(events2, (v, k) => {
        const handlers2 = registry[k] !== void 0 ? registry[k] : {};
        handlers2[id] = curryArgs(v, extraArgs);
        registry[k] = handlers2;
      });
    };
    const findHandler = (handlers2, elem) => read$1(elem).bind((id) => get$g(handlers2, id)).map((descHandler) => eventHandler(elem, descHandler));
    const filterByType = (type2) => get$g(registry, type2).map((handlers2) => mapToArray(handlers2, (f, id) => broadcastHandler(id, f))).getOr([]);
    const find2 = (isAboveRoot, type2, target) => get$g(registry, type2).bind((handlers2) => closest$4(target, (elem) => findHandler(handlers2, elem), isAboveRoot));
    const unregisterId = (id) => {
      each(registry, (handlersById, _eventName) => {
        if (has$2(handlersById, id)) {
          delete handlersById[id];
        }
      });
    };
    return {
      registerId,
      unregisterId,
      filterByType,
      find: find2
    };
  };
  const Registry = () => {
    const events2 = EventRegistry();
    const components2 = {};
    const readOrTag = (component) => {
      const elem = component.element;
      return read$1(elem).getOrThunk(() => write("uid-", component.element));
    };
    const failOnDuplicate = (component, tagId) => {
      const conflict = components2[tagId];
      if (conflict === component) {
        unregister(component);
      } else {
        throw new Error('The tagId "' + tagId + '" is already used by: ' + element(conflict.element) + "\nCannot use it for: " + element(component.element) + "\nThe conflicting element is" + (inBody(conflict.element) ? " " : " not ") + "already in the DOM");
      }
    };
    const register2 = (component) => {
      const tagId = readOrTag(component);
      if (hasNonNullableKey(components2, tagId)) {
        failOnDuplicate(component, tagId);
      }
      const extraArgs = [component];
      events2.registerId(extraArgs, tagId, component.events);
      components2[tagId] = component;
    };
    const unregister = (component) => {
      read$1(component.element).each((tagId) => {
        delete components2[tagId];
        events2.unregisterId(tagId);
      });
    };
    const filter2 = (type2) => events2.filterByType(type2);
    const find2 = (isAboveRoot, type2, target) => events2.find(isAboveRoot, type2, target);
    const getById = (id) => get$g(components2, id);
    return {
      find: find2,
      filter: filter2,
      register: register2,
      unregister,
      getById
    };
  };
  const factory$h = (detail) => {
    const { attributes, ...domWithoutAttributes } = detail.dom;
    return {
      uid: detail.uid,
      dom: {
        tag: "div",
        attributes: {
          role: "presentation",
          ...attributes
        },
        ...domWithoutAttributes
      },
      components: detail.components,
      behaviours: get$3(detail.containerBehaviours),
      events: detail.events,
      domModification: detail.domModification,
      eventOrder: detail.eventOrder
    };
  };
  const Container = single({
    name: "Container",
    factory: factory$h,
    configFields: [
      defaulted("components", []),
      field("containerBehaviours", []),
      defaulted("events", {}),
      defaulted("domModification", {}),
      defaulted("eventOrder", {})
    ]
  });
  const takeover = (root) => {
    const isAboveRoot = (el) => parent(root.element).fold(always, (parent2) => eq(el, parent2));
    const registry = Registry();
    const lookup2 = (eventName, target) => registry.find(isAboveRoot, eventName, target);
    const domEvents = setup$d(root.element, {
      triggerEvent: (eventName, event2) => {
        return monitorEvent(eventName, event2.target, (logger) => triggerUntilStopped(lookup2, eventName, event2, logger));
      }
    });
    const systemApi = {
      debugInfo: constant$1("real"),
      triggerEvent: (eventName, target, data) => {
        monitorEvent(eventName, target, (logger) => triggerOnUntilStopped(lookup2, eventName, data, target, logger));
      },
      triggerFocus: (target, originator) => {
        read$1(target).fold(() => {
          focus$3(target);
        }, (_alloyId) => {
          monitorEvent(focus$4(), target, (logger) => {
            triggerHandler(lookup2, focus$4(), {
              originator,
              kill: noop,
              prevent: noop,
              target
            }, target, logger);
            return false;
          });
        });
      },
      triggerEscape: (comp, simulatedEvent) => {
        systemApi.triggerEvent("keydown", comp.element, simulatedEvent.event);
      },
      getByUid: (uid) => {
        return getByUid(uid);
      },
      getByDom: (elem) => {
        return getByDom(elem);
      },
      build: build$1,
      buildOrPatch,
      addToGui: (c) => {
        add2(c);
      },
      removeFromGui: (c) => {
        remove2(c);
      },
      addToWorld: (c) => {
        addToWorld(c);
      },
      removeFromWorld: (c) => {
        removeFromWorld(c);
      },
      broadcast: (message) => {
        broadcast$1(message);
      },
      broadcastOn: (channels, message) => {
        broadcastOn(channels, message);
      },
      broadcastEvent: (eventName, event2) => {
        broadcastEvent(eventName, event2);
      },
      isConnected: always
    };
    const addToWorld = (component) => {
      component.connect(systemApi);
      if (!isText(component.element)) {
        registry.register(component);
        each$1(component.components(), addToWorld);
        systemApi.triggerEvent(systemInit(), component.element, { target: component.element });
      }
    };
    const removeFromWorld = (component) => {
      if (!isText(component.element)) {
        each$1(component.components(), removeFromWorld);
        registry.unregister(component);
      }
      component.disconnect();
    };
    const add2 = (component) => {
      attach(root, component);
    };
    const remove2 = (component) => {
      detach(component);
    };
    const destroy = () => {
      domEvents.unbind();
      remove$5(root.element);
    };
    const broadcastData = (data) => {
      const receivers = registry.filter(receive());
      each$1(receivers, (receiver) => {
        const descHandler = receiver.descHandler;
        const handler = getCurried(descHandler);
        handler(data);
      });
    };
    const broadcast$1 = (message) => {
      broadcastData({
        universal: true,
        data: message
      });
    };
    const broadcastOn = (channels, message) => {
      broadcastData({
        universal: false,
        channels,
        data: message
      });
    };
    const broadcastEvent = (eventName, event2) => {
      const listeners = registry.filter(eventName);
      return broadcast(listeners, event2);
    };
    const getByUid = (uid) => registry.getById(uid).fold(() => Result.error(new Error('Could not find component with uid: "' + uid + '" in system.')), Result.value);
    const getByDom = (elem) => {
      const uid = read$1(elem).getOr("not found");
      return getByUid(uid);
    };
    addToWorld(root);
    return {
      root,
      element: root.element,
      destroy,
      add: add2,
      remove: remove2,
      getByUid,
      getByDom,
      addToWorld,
      removeFromWorld,
      broadcast: broadcast$1,
      broadcastOn,
      broadcastEvent
    };
  };
  const renderBar = (spec, backstage) => ({
    dom: {
      tag: "div",
      classes: [
        "tox-bar",
        "tox-form__controls-h-stack"
      ]
    },
    components: map$2(spec.items, backstage.interpreter)
  });
  const schema$j = constant$1([
    defaulted("prefix", "form-field"),
    field("fieldBehaviours", [
      Composing,
      Representing
    ])
  ]);
  const parts$d = constant$1([
    optional({
      schema: [required$1("dom")],
      name: "label"
    }),
    optional({
      factory: {
        sketch: (spec) => {
          return {
            uid: spec.uid,
            dom: {
              tag: "span",
              styles: { display: "none" },
              attributes: { "aria-hidden": "true" },
              innerHtml: spec.text
            }
          };
        }
      },
      schema: [required$1("text")],
      name: "aria-descriptor"
    }),
    required({
      factory: {
        sketch: (spec) => {
          const excludeFactory = exclude(spec, ["factory"]);
          return spec.factory.sketch(excludeFactory);
        }
      },
      schema: [required$1("factory")],
      name: "field"
    })
  ]);
  const factory$g = (detail, components2, _spec, _externals) => {
    const behaviours2 = augment(detail.fieldBehaviours, [
      Composing.config({
        find: (container) => {
          return getPart(container, detail, "field");
        }
      }),
      Representing.config({
        store: {
          mode: "manual",
          getValue: (field2) => {
            return Composing.getCurrent(field2).bind(Representing.getValue);
          },
          setValue: (field2, value2) => {
            Composing.getCurrent(field2).each((current) => {
              Representing.setValue(current, value2);
            });
          }
        }
      })
    ]);
    const events2 = derive$2([runOnAttached((component, _simulatedEvent) => {
      const ps = getParts(component, detail, [
        "label",
        "field",
        "aria-descriptor"
      ]);
      ps.field().each((field2) => {
        const id = generate$6(detail.prefix);
        ps.label().each((label2) => {
          set$9(label2.element, "for", id);
          set$9(field2.element, "id", id);
        });
        ps["aria-descriptor"]().each((descriptor) => {
          const descriptorId = generate$6(detail.prefix);
          set$9(descriptor.element, "id", descriptorId);
          set$9(field2.element, "aria-describedby", descriptorId);
        });
      });
    })]);
    const apis = {
      getField: (container) => getPart(container, detail, "field"),
      getLabel: (container) => getPart(container, detail, "label")
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: behaviours2,
      events: events2,
      apis
    };
  };
  const FormField = composite({
    name: "FormField",
    configFields: schema$j(),
    partFields: parts$d(),
    factory: factory$g,
    apis: {
      getField: (apis, comp) => apis.getField(comp),
      getLabel: (apis, comp) => apis.getLabel(comp)
    }
  });
  const exhibit$2 = (base2, tabConfig) => nu$7({
    attributes: wrapAll([{
      key: tabConfig.tabAttr,
      value: "true"
    }])
  });
  var ActiveTabstopping = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    exhibit: exhibit$2
  });
  var TabstopSchema = [defaulted("tabAttr", "data-alloy-tabstop")];
  const Tabstopping = create$3({
    fields: TabstopSchema,
    name: "tabstopping",
    active: ActiveTabstopping
  });
  var global$3 = tinymce.util.Tools.resolve("tinymce.html.Entities");
  const renderFormFieldWith = (pLabel, pField, extraClasses, extraBehaviours) => {
    const spec = renderFormFieldSpecWith(pLabel, pField, extraClasses, extraBehaviours);
    return FormField.sketch(spec);
  };
  const renderFormField = (pLabel, pField) => renderFormFieldWith(pLabel, pField, [], []);
  const renderFormFieldSpecWith = (pLabel, pField, extraClasses, extraBehaviours) => ({
    dom: renderFormFieldDomWith(extraClasses),
    components: pLabel.toArray().concat([pField]),
    fieldBehaviours: derive$1(extraBehaviours)
  });
  const renderFormFieldDom = () => renderFormFieldDomWith([]);
  const renderFormFieldDomWith = (extraClasses) => ({
    tag: "div",
    classes: ["tox-form__group"].concat(extraClasses)
  });
  const renderLabel$2 = (label2, providersBackstage) => FormField.parts.label({
    dom: {
      tag: "label",
      classes: ["tox-label"]
    },
    components: [text$1(providersBackstage.translate(label2))]
  });
  const formChangeEvent = generate$6("form-component-change");
  const formCloseEvent = generate$6("form-close");
  const formCancelEvent = generate$6("form-cancel");
  const formActionEvent = generate$6("form-action");
  const formSubmitEvent = generate$6("form-submit");
  const formBlockEvent = generate$6("form-block");
  const formUnblockEvent = generate$6("form-unblock");
  const formTabChangeEvent = generate$6("form-tabchange");
  const formResizeEvent = generate$6("form-resize");
  const renderCollection = (spec, providersBackstage, initialData) => {
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const runOnItem = (f) => (comp, se) => {
      closest$1(se.event.target, "[data-collection-item-value]").each((target) => {
        f(comp, se, target, get$f(target, "data-collection-item-value"));
      });
    };
    const setContents = (comp, items) => {
      const htmlLines = map$2(items, (item2) => {
        const itemText = global$8.translate(item2.text);
        const textContent = spec.columns === 1 ? `<div class="tox-collection__item-label">${itemText}</div>` : "";
        const iconContent = `<div class="tox-collection__item-icon">${item2.icon}</div>`;
        const mapItemName = {
          "_": " ",
          " - ": " ",
          "-": " "
        };
        const ariaLabel = itemText.replace(/\_| \- |\-/g, (match) => mapItemName[match]);
        const disabledClass = providersBackstage.isDisabled() ? " tox-collection__item--state-disabled" : "";
        return `<div class="tox-collection__item${disabledClass}" tabindex="-1" data-collection-item-value="${global$3.encodeAllRaw(item2.value)}" title="${ariaLabel}" aria-label="${ariaLabel}">${iconContent}${textContent}</div>`;
      });
      const chunks = spec.columns !== "auto" && spec.columns > 1 ? chunk$1(htmlLines, spec.columns) : [htmlLines];
      const html = map$2(chunks, (ch) => `<div class="tox-collection__group">${ch.join("")}</div>`);
      set$6(comp.element, html.join(""));
    };
    const onClick = runOnItem((comp, se, tgt, itemValue) => {
      se.stop();
      if (!providersBackstage.isDisabled()) {
        emitWith(comp, formActionEvent, {
          name: spec.name,
          value: itemValue
        });
      }
    });
    const collectionEvents = [
      run$1(mouseover(), runOnItem((comp, se, tgt) => {
        focus$3(tgt);
      })),
      run$1(click(), onClick),
      run$1(tap(), onClick),
      run$1(focusin(), runOnItem((comp, se, tgt) => {
        descendant(comp.element, "." + activeClass).each((currentActive) => {
          remove$2(currentActive, activeClass);
        });
        add$2(tgt, activeClass);
      })),
      run$1(focusout(), runOnItem((comp) => {
        descendant(comp.element, "." + activeClass).each((currentActive) => {
          remove$2(currentActive, activeClass);
        });
      })),
      runOnExecute$1(runOnItem((comp, se, tgt, itemValue) => {
        emitWith(comp, formActionEvent, {
          name: spec.name,
          value: itemValue
        });
      }))
    ];
    const iterCollectionItems = (comp, applyAttributes) => map$2(descendants(comp.element, ".tox-collection__item"), applyAttributes);
    const pField = FormField.parts.field({
      dom: {
        tag: "div",
        classes: ["tox-collection"].concat(spec.columns !== 1 ? ["tox-collection--grid"] : ["tox-collection--list"])
      },
      components: [],
      factory: { sketch: identity },
      behaviours: derive$1([
        Disabling.config({
          disabled: providersBackstage.isDisabled,
          onDisabled: (comp) => {
            iterCollectionItems(comp, (childElm) => {
              add$2(childElm, "tox-collection__item--state-disabled");
              set$9(childElm, "aria-disabled", true);
            });
          },
          onEnabled: (comp) => {
            iterCollectionItems(comp, (childElm) => {
              remove$2(childElm, "tox-collection__item--state-disabled");
              remove$7(childElm, "aria-disabled");
            });
          }
        }),
        receivingConfig(),
        Replacing.config({}),
        Representing.config({
          store: {
            mode: "memory",
            initialValue: initialData.getOr([])
          },
          onSetValue: (comp, items) => {
            setContents(comp, items);
            if (spec.columns === "auto") {
              detectSize(comp, 5, "tox-collection__item").each(({ numRows, numColumns }) => {
                Keying.setGridSize(comp, numRows, numColumns);
              });
            }
            emit(comp, formResizeEvent);
          }
        }),
        Tabstopping.config({}),
        Keying.config(deriveCollectionMovement(spec.columns, "normal")),
        config("collection-events", collectionEvents)
      ]),
      eventOrder: {
        [execute$5()]: [
          "disabling",
          "alloy.base.behaviour",
          "collection-events"
        ]
      }
    });
    const extraClasses = ["tox-form__group--collection"];
    return renderFormFieldWith(pLabel, pField, extraClasses, []);
  };
  const ariaElements = [
    "input",
    "textarea"
  ];
  const isAriaElement = (elem) => {
    const name2 = name$3(elem);
    return contains$2(ariaElements, name2);
  };
  const markValid = (component, invalidConfig) => {
    const elem = invalidConfig.getRoot(component).getOr(component.element);
    remove$2(elem, invalidConfig.invalidClass);
    invalidConfig.notify.each((notifyInfo) => {
      if (isAriaElement(component.element)) {
        set$9(component.element, "aria-invalid", false);
      }
      notifyInfo.getContainer(component).each((container) => {
        set$6(container, notifyInfo.validHtml);
      });
      notifyInfo.onValid(component);
    });
  };
  const markInvalid = (component, invalidConfig, invalidState, text2) => {
    const elem = invalidConfig.getRoot(component).getOr(component.element);
    add$2(elem, invalidConfig.invalidClass);
    invalidConfig.notify.each((notifyInfo) => {
      if (isAriaElement(component.element)) {
        set$9(component.element, "aria-invalid", true);
      }
      notifyInfo.getContainer(component).each((container) => {
        set$6(container, text2);
      });
      notifyInfo.onInvalid(component, text2);
    });
  };
  const query = (component, invalidConfig, _invalidState) => invalidConfig.validator.fold(() => Future.pure(Result.value(true)), (validatorInfo) => validatorInfo.validate(component));
  const run = (component, invalidConfig, invalidState) => {
    invalidConfig.notify.each((notifyInfo) => {
      notifyInfo.onValidate(component);
    });
    return query(component, invalidConfig).map((valid) => {
      if (component.getSystem().isConnected()) {
        return valid.fold((err) => {
          markInvalid(component, invalidConfig, invalidState, err);
          return Result.error(err);
        }, (v) => {
          markValid(component, invalidConfig);
          return Result.value(v);
        });
      } else {
        return Result.error("No longer in system");
      }
    });
  };
  const isInvalid = (component, invalidConfig) => {
    const elem = invalidConfig.getRoot(component).getOr(component.element);
    return has(elem, invalidConfig.invalidClass);
  };
  var InvalidateApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    markValid,
    markInvalid,
    query,
    run,
    isInvalid
  });
  const events$8 = (invalidConfig, invalidState) => invalidConfig.validator.map((validatorInfo) => derive$2([run$1(validatorInfo.onEvent, (component) => {
    run(component, invalidConfig, invalidState).get(identity);
  })].concat(validatorInfo.validateOnLoad ? [runOnAttached((component) => {
    run(component, invalidConfig, invalidState).get(noop);
  })] : []))).getOr({});
  var ActiveInvalidate = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$8
  });
  var InvalidateSchema = [
    required$1("invalidClass"),
    defaulted("getRoot", Optional.none),
    optionObjOf("notify", [
      defaulted("aria", "alert"),
      defaulted("getContainer", Optional.none),
      defaulted("validHtml", ""),
      onHandler("onValid"),
      onHandler("onInvalid"),
      onHandler("onValidate")
    ]),
    optionObjOf("validator", [
      required$1("validate"),
      defaulted("onEvent", "input"),
      defaulted("validateOnLoad", true)
    ])
  ];
  const Invalidating = create$3({
    fields: InvalidateSchema,
    name: "invalidating",
    active: ActiveInvalidate,
    apis: InvalidateApis,
    extra: {
      validation: (validator) => {
        return (component) => {
          const v = Representing.getValue(component);
          return Future.pure(validator(v));
        };
      }
    }
  });
  const exhibit$1 = () => nu$7({
    styles: {
      "-webkit-user-select": "none",
      "user-select": "none",
      "-ms-user-select": "none",
      "-moz-user-select": "-moz-none"
    },
    attributes: { unselectable: "on" }
  });
  const events$7 = () => derive$2([abort(selectstart(), always)]);
  var ActiveUnselecting = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$7,
    exhibit: exhibit$1
  });
  const Unselecting = create$3({
    fields: [],
    name: "unselecting",
    active: ActiveUnselecting
  });
  const renderPanelButton = (spec, sharedBackstage) => Dropdown.sketch({
    dom: spec.dom,
    components: spec.components,
    toggleClass: "mce-active",
    dropdownBehaviours: derive$1([
      DisablingConfigs.button(sharedBackstage.providers.isDisabled),
      receivingConfig(),
      Unselecting.config({}),
      Tabstopping.config({})
    ]),
    layouts: spec.layouts,
    sandboxClasses: ["tox-dialog__popups"],
    lazySink: sharedBackstage.getSink,
    fetch: (comp) => Future.nu((callback) => spec.fetch(callback)).map((items) => Optional.from(createTieredDataFrom(deepMerge(createPartialChoiceMenu(generate$6("menu-value"), items, (value2) => {
      spec.onItemAction(comp, value2);
    }, spec.columns, spec.presets, ItemResponse$1.CLOSE_ON_EXECUTE, never, sharedBackstage.providers), { movement: deriveMenuMovement(spec.columns, spec.presets) })))),
    parts: { menu: part(false, 1, spec.presets) }
  });
  const colorInputChangeEvent = generate$6("color-input-change");
  const colorSwatchChangeEvent = generate$6("color-swatch-change");
  const colorPickerCancelEvent = generate$6("color-picker-cancel");
  const renderColorInput = (spec, sharedBackstage, colorInputBackstage, initialData) => {
    const pField = FormField.parts.field({
      factory: Input,
      inputClasses: ["tox-textfield"],
      data: initialData,
      onSetValue: (c) => Invalidating.run(c).get(noop),
      inputBehaviours: derive$1([
        Disabling.config({ disabled: sharedBackstage.providers.isDisabled }),
        receivingConfig(),
        Tabstopping.config({}),
        Invalidating.config({
          invalidClass: "tox-textbox-field-invalid",
          getRoot: (comp) => parentElement(comp.element),
          notify: {
            onValid: (comp) => {
              const val = Representing.getValue(comp);
              emitWith(comp, colorInputChangeEvent, { color: val });
            }
          },
          validator: {
            validateOnLoad: false,
            validate: (input2) => {
              const inputValue = Representing.getValue(input2);
              if (inputValue.length === 0) {
                return Future.pure(Result.value(true));
              } else {
                const span = SugarElement.fromTag("span");
                set$8(span, "background-color", inputValue);
                const res = getRaw(span, "background-color").fold(() => Result.error("blah"), (_) => Result.value(inputValue));
                return Future.pure(res);
              }
            }
          }
        })
      ]),
      selectOnFocus: false
    });
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, sharedBackstage.providers));
    const emitSwatchChange = (colorBit, value2) => {
      emitWith(colorBit, colorSwatchChangeEvent, { value: value2 });
    };
    const onItemAction2 = (comp, value2) => {
      memColorButton.getOpt(comp).each((colorBit) => {
        if (value2 === "custom") {
          colorInputBackstage.colorPicker((valueOpt) => {
            valueOpt.fold(() => emit(colorBit, colorPickerCancelEvent), (value3) => {
              emitSwatchChange(colorBit, value3);
              addColor(value3);
            });
          }, "#ffffff");
        } else if (value2 === "remove") {
          emitSwatchChange(colorBit, "");
        } else {
          emitSwatchChange(colorBit, value2);
        }
      });
    };
    const memColorButton = record(renderPanelButton({
      dom: {
        tag: "span",
        attributes: { "aria-label": sharedBackstage.providers.translate("Color swatch") }
      },
      layouts: {
        onRtl: () => [
          southwest$2,
          southeast$2,
          south$2
        ],
        onLtr: () => [
          southeast$2,
          southwest$2,
          south$2
        ]
      },
      components: [],
      fetch: getFetch$1(colorInputBackstage.getColors(), colorInputBackstage.hasCustomColors()),
      columns: colorInputBackstage.getColorCols(),
      presets: "color",
      onItemAction: onItemAction2
    }, sharedBackstage));
    return FormField.sketch({
      dom: {
        tag: "div",
        classes: ["tox-form__group"]
      },
      components: pLabel.toArray().concat([{
        dom: {
          tag: "div",
          classes: ["tox-color-input"]
        },
        components: [
          pField,
          memColorButton.asSpec()
        ]
      }]),
      fieldBehaviours: derive$1([config("form-field-events", [
        run$1(colorInputChangeEvent, (comp, se) => {
          memColorButton.getOpt(comp).each((colorButton) => {
            set$8(colorButton.element, "background-color", se.event.color);
          });
          emitWith(comp, formChangeEvent, { name: spec.name });
        }),
        run$1(colorSwatchChangeEvent, (comp, se) => {
          FormField.getField(comp).each((field2) => {
            Representing.setValue(field2, se.event.value);
            Composing.getCurrent(comp).each(Focusing.focus);
          });
        }),
        run$1(colorPickerCancelEvent, (comp, _se) => {
          FormField.getField(comp).each((_field) => {
            Composing.getCurrent(comp).each(Focusing.focus);
          });
        })
      ])])
    });
  };
  const labelPart = optional({
    schema: [required$1("dom")],
    name: "label"
  });
  const edgePart = (name2) => optional({
    name: "" + name2 + "-edge",
    overrides: (detail) => {
      const action = detail.model.manager.edgeActions[name2];
      return action.fold(() => ({}), (a) => ({
        events: derive$2([
          runActionExtra(touchstart(), (comp, se, d) => a(comp, d), [detail]),
          runActionExtra(mousedown(), (comp, se, d) => a(comp, d), [detail]),
          runActionExtra(mousemove(), (comp, se, det) => {
            if (det.mouseIsDown.get()) {
              a(comp, det);
            }
          }, [detail])
        ])
      }));
    }
  });
  const tlEdgePart = edgePart("top-left");
  const tedgePart = edgePart("top");
  const trEdgePart = edgePart("top-right");
  const redgePart = edgePart("right");
  const brEdgePart = edgePart("bottom-right");
  const bedgePart = edgePart("bottom");
  const blEdgePart = edgePart("bottom-left");
  const ledgePart = edgePart("left");
  const thumbPart = required({
    name: "thumb",
    defaults: constant$1({ dom: { styles: { position: "absolute" } } }),
    overrides: (detail) => {
      return {
        events: derive$2([
          redirectToPart(touchstart(), detail, "spectrum"),
          redirectToPart(touchmove(), detail, "spectrum"),
          redirectToPart(touchend(), detail, "spectrum"),
          redirectToPart(mousedown(), detail, "spectrum"),
          redirectToPart(mousemove(), detail, "spectrum"),
          redirectToPart(mouseup(), detail, "spectrum")
        ])
      };
    }
  });
  const spectrumPart = required({
    schema: [customField("mouseIsDown", () => Cell(false))],
    name: "spectrum",
    overrides: (detail) => {
      const modelDetail = detail.model;
      const model = modelDetail.manager;
      const setValueFrom2 = (component, simulatedEvent) => model.getValueFromEvent(simulatedEvent).map((value2) => model.setValueFrom(component, detail, value2));
      return {
        behaviours: derive$1([
          Keying.config({
            mode: "special",
            onLeft: (spectrum) => model.onLeft(spectrum, detail),
            onRight: (spectrum) => model.onRight(spectrum, detail),
            onUp: (spectrum) => model.onUp(spectrum, detail),
            onDown: (spectrum) => model.onDown(spectrum, detail)
          }),
          Focusing.config({})
        ]),
        events: derive$2([
          run$1(touchstart(), setValueFrom2),
          run$1(touchmove(), setValueFrom2),
          run$1(mousedown(), setValueFrom2),
          run$1(mousemove(), (spectrum, se) => {
            if (detail.mouseIsDown.get()) {
              setValueFrom2(spectrum, se);
            }
          })
        ])
      };
    }
  });
  var SliderParts = [
    labelPart,
    ledgePart,
    redgePart,
    tedgePart,
    bedgePart,
    tlEdgePart,
    trEdgePart,
    blEdgePart,
    brEdgePart,
    thumbPart,
    spectrumPart
  ];
  const _sliderChangeEvent = "slider.change.value";
  const sliderChangeEvent = constant$1(_sliderChangeEvent);
  const isTouchEvent$2 = (evt) => evt.type.indexOf("touch") !== -1;
  const getEventSource = (simulatedEvent) => {
    const evt = simulatedEvent.event.raw;
    if (isTouchEvent$2(evt)) {
      const touchEvent = evt;
      return touchEvent.touches !== void 0 && touchEvent.touches.length === 1 ? Optional.some(touchEvent.touches[0]).map((t2) => SugarPosition(t2.clientX, t2.clientY)) : Optional.none();
    } else {
      const mouseEvent = evt;
      return mouseEvent.clientX !== void 0 ? Optional.some(mouseEvent).map((me) => SugarPosition(me.clientX, me.clientY)) : Optional.none();
    }
  };
  const t = "top", r = "right", b = "bottom", l = "left";
  const minX = (detail) => detail.model.minX;
  const minY = (detail) => detail.model.minY;
  const min1X = (detail) => detail.model.minX - 1;
  const min1Y = (detail) => detail.model.minY - 1;
  const maxX = (detail) => detail.model.maxX;
  const maxY = (detail) => detail.model.maxY;
  const max1X = (detail) => detail.model.maxX + 1;
  const max1Y = (detail) => detail.model.maxY + 1;
  const range = (detail, max2, min2) => max2(detail) - min2(detail);
  const xRange = (detail) => range(detail, maxX, minX);
  const yRange = (detail) => range(detail, maxY, minY);
  const halfX = (detail) => xRange(detail) / 2;
  const halfY = (detail) => yRange(detail) / 2;
  const step = (detail) => detail.stepSize;
  const snap = (detail) => detail.snapToGrid;
  const snapStart = (detail) => detail.snapStart;
  const rounded = (detail) => detail.rounded;
  const hasEdge = (detail, edgeName) => detail[edgeName + "-edge"] !== void 0;
  const hasLEdge = (detail) => hasEdge(detail, l);
  const hasREdge = (detail) => hasEdge(detail, r);
  const hasTEdge = (detail) => hasEdge(detail, t);
  const hasBEdge = (detail) => hasEdge(detail, b);
  const currentValue = (detail) => detail.model.value.get();
  const xyValue = (x, y) => ({
    x,
    y
  });
  const fireSliderChange$3 = (component, value2) => {
    emitWith(component, sliderChangeEvent(), { value: value2 });
  };
  const setToTLEdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(min1X(detail), min1Y(detail)));
  };
  const setToTEdge = (edge2, detail) => {
    fireSliderChange$3(edge2, min1Y(detail));
  };
  const setToTEdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(halfX(detail), min1Y(detail)));
  };
  const setToTREdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(max1X(detail), min1Y(detail)));
  };
  const setToREdge = (edge2, detail) => {
    fireSliderChange$3(edge2, max1X(detail));
  };
  const setToREdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(max1X(detail), halfY(detail)));
  };
  const setToBREdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(max1X(detail), max1Y(detail)));
  };
  const setToBEdge = (edge2, detail) => {
    fireSliderChange$3(edge2, max1Y(detail));
  };
  const setToBEdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(halfX(detail), max1Y(detail)));
  };
  const setToBLEdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(min1X(detail), max1Y(detail)));
  };
  const setToLEdge = (edge2, detail) => {
    fireSliderChange$3(edge2, min1X(detail));
  };
  const setToLEdgeXY = (edge2, detail) => {
    fireSliderChange$3(edge2, xyValue(min1X(detail), halfY(detail)));
  };
  const reduceBy = (value2, min2, max2, step2) => {
    if (value2 < min2) {
      return value2;
    } else if (value2 > max2) {
      return max2;
    } else if (value2 === min2) {
      return min2 - 1;
    } else {
      return Math.max(min2, value2 - step2);
    }
  };
  const increaseBy = (value2, min2, max2, step2) => {
    if (value2 > max2) {
      return value2;
    } else if (value2 < min2) {
      return min2;
    } else if (value2 === max2) {
      return max2 + 1;
    } else {
      return Math.min(max2, value2 + step2);
    }
  };
  const capValue = (value2, min2, max2) => Math.max(min2, Math.min(max2, value2));
  const snapValueOf = (value2, min2, max2, step2, snapStart2) => snapStart2.fold(() => {
    const initValue = value2 - min2;
    const extraValue = Math.round(initValue / step2) * step2;
    return capValue(min2 + extraValue, min2 - 1, max2 + 1);
  }, (start) => {
    const remainder = (value2 - start) % step2;
    const adjustment = Math.round(remainder / step2);
    const rawSteps = Math.floor((value2 - start) / step2);
    const maxSteps = Math.floor((max2 - start) / step2);
    const numSteps = Math.min(maxSteps, rawSteps + adjustment);
    const r2 = start + numSteps * step2;
    return Math.max(start, r2);
  });
  const findOffsetOf = (value2, min2, max2) => Math.min(max2, Math.max(value2, min2)) - min2;
  const findValueOf = (args) => {
    const { min: min2, max: max2, range: range2, value: value2, step: step2, snap: snap2, snapStart: snapStart2, rounded: rounded2, hasMinEdge, hasMaxEdge, minBound, maxBound, screenRange } = args;
    const capMin = hasMinEdge ? min2 - 1 : min2;
    const capMax = hasMaxEdge ? max2 + 1 : max2;
    if (value2 < minBound) {
      return capMin;
    } else if (value2 > maxBound) {
      return capMax;
    } else {
      const offset2 = findOffsetOf(value2, minBound, maxBound);
      const newValue = capValue(offset2 / screenRange * range2 + min2, capMin, capMax);
      if (snap2 && newValue >= min2 && newValue <= max2) {
        return snapValueOf(newValue, min2, max2, step2, snapStart2);
      } else if (rounded2) {
        return Math.round(newValue);
      } else {
        return newValue;
      }
    }
  };
  const findOffsetOfValue$2 = (args) => {
    const { min: min2, max: max2, range: range2, value: value2, hasMinEdge, hasMaxEdge, maxBound, maxOffset, centerMinEdge, centerMaxEdge } = args;
    if (value2 < min2) {
      return hasMinEdge ? 0 : centerMinEdge;
    } else if (value2 > max2) {
      return hasMaxEdge ? maxBound : centerMaxEdge;
    } else {
      return (value2 - min2) / range2 * maxOffset;
    }
  };
  const top = "top", right = "right", bottom = "bottom", left = "left", width = "width", height = "height";
  const getBounds = (component) => component.element.dom.getBoundingClientRect();
  const getBoundsProperty = (bounds2, property) => bounds2[property];
  const getMinXBounds = (component) => {
    const bounds2 = getBounds(component);
    return getBoundsProperty(bounds2, left);
  };
  const getMaxXBounds = (component) => {
    const bounds2 = getBounds(component);
    return getBoundsProperty(bounds2, right);
  };
  const getMinYBounds = (component) => {
    const bounds2 = getBounds(component);
    return getBoundsProperty(bounds2, top);
  };
  const getMaxYBounds = (component) => {
    const bounds2 = getBounds(component);
    return getBoundsProperty(bounds2, bottom);
  };
  const getXScreenRange = (component) => {
    const bounds2 = getBounds(component);
    return getBoundsProperty(bounds2, width);
  };
  const getYScreenRange = (component) => {
    const bounds2 = getBounds(component);
    return getBoundsProperty(bounds2, height);
  };
  const getCenterOffsetOf = (componentMinEdge, componentMaxEdge, spectrumMinEdge) => (componentMinEdge + componentMaxEdge) / 2 - spectrumMinEdge;
  const getXCenterOffSetOf = (component, spectrum) => {
    const componentBounds = getBounds(component);
    const spectrumBounds = getBounds(spectrum);
    const componentMinEdge = getBoundsProperty(componentBounds, left);
    const componentMaxEdge = getBoundsProperty(componentBounds, right);
    const spectrumMinEdge = getBoundsProperty(spectrumBounds, left);
    return getCenterOffsetOf(componentMinEdge, componentMaxEdge, spectrumMinEdge);
  };
  const getYCenterOffSetOf = (component, spectrum) => {
    const componentBounds = getBounds(component);
    const spectrumBounds = getBounds(spectrum);
    const componentMinEdge = getBoundsProperty(componentBounds, top);
    const componentMaxEdge = getBoundsProperty(componentBounds, bottom);
    const spectrumMinEdge = getBoundsProperty(spectrumBounds, top);
    return getCenterOffsetOf(componentMinEdge, componentMaxEdge, spectrumMinEdge);
  };
  const fireSliderChange$2 = (spectrum, value2) => {
    emitWith(spectrum, sliderChangeEvent(), { value: value2 });
  };
  const findValueOfOffset$1 = (spectrum, detail, left2) => {
    const args = {
      min: minX(detail),
      max: maxX(detail),
      range: xRange(detail),
      value: left2,
      step: step(detail),
      snap: snap(detail),
      snapStart: snapStart(detail),
      rounded: rounded(detail),
      hasMinEdge: hasLEdge(detail),
      hasMaxEdge: hasREdge(detail),
      minBound: getMinXBounds(spectrum),
      maxBound: getMaxXBounds(spectrum),
      screenRange: getXScreenRange(spectrum)
    };
    return findValueOf(args);
  };
  const setValueFrom$2 = (spectrum, detail, value2) => {
    const xValue = findValueOfOffset$1(spectrum, detail, value2);
    const sliderVal = xValue;
    fireSliderChange$2(spectrum, sliderVal);
    return xValue;
  };
  const setToMin$2 = (spectrum, detail) => {
    const min2 = minX(detail);
    fireSliderChange$2(spectrum, min2);
  };
  const setToMax$2 = (spectrum, detail) => {
    const max2 = maxX(detail);
    fireSliderChange$2(spectrum, max2);
  };
  const moveBy$2 = (direction, spectrum, detail) => {
    const f = direction > 0 ? increaseBy : reduceBy;
    const xValue = f(currentValue(detail), minX(detail), maxX(detail), step(detail));
    fireSliderChange$2(spectrum, xValue);
    return Optional.some(xValue);
  };
  const handleMovement$2 = (direction) => (spectrum, detail) => moveBy$2(direction, spectrum, detail).map(always);
  const getValueFromEvent$2 = (simulatedEvent) => {
    const pos = getEventSource(simulatedEvent);
    return pos.map((p) => p.left);
  };
  const findOffsetOfValue$1 = (spectrum, detail, value2, minEdge, maxEdge) => {
    const minOffset = 0;
    const maxOffset = getXScreenRange(spectrum);
    const centerMinEdge = minEdge.bind((edge2) => Optional.some(getXCenterOffSetOf(edge2, spectrum))).getOr(minOffset);
    const centerMaxEdge = maxEdge.bind((edge2) => Optional.some(getXCenterOffSetOf(edge2, spectrum))).getOr(maxOffset);
    const args = {
      min: minX(detail),
      max: maxX(detail),
      range: xRange(detail),
      value: value2,
      hasMinEdge: hasLEdge(detail),
      hasMaxEdge: hasREdge(detail),
      minBound: getMinXBounds(spectrum),
      minOffset,
      maxBound: getMaxXBounds(spectrum),
      maxOffset,
      centerMinEdge,
      centerMaxEdge
    };
    return findOffsetOfValue$2(args);
  };
  const findPositionOfValue$1 = (slider, spectrum, value2, minEdge, maxEdge, detail) => {
    const offset2 = findOffsetOfValue$1(spectrum, detail, value2, minEdge, maxEdge);
    return getMinXBounds(spectrum) - getMinXBounds(slider) + offset2;
  };
  const setPositionFromValue$2 = (slider, thumb, detail, edges) => {
    const value2 = currentValue(detail);
    const pos = findPositionOfValue$1(slider, edges.getSpectrum(slider), value2, edges.getLeftEdge(slider), edges.getRightEdge(slider), detail);
    const thumbRadius = get$c(thumb.element) / 2;
    set$8(thumb.element, "left", pos - thumbRadius + "px");
  };
  const onLeft$2 = handleMovement$2(-1);
  const onRight$2 = handleMovement$2(1);
  const onUp$2 = Optional.none;
  const onDown$2 = Optional.none;
  const edgeActions$2 = {
    "top-left": Optional.none(),
    "top": Optional.none(),
    "top-right": Optional.none(),
    "right": Optional.some(setToREdge),
    "bottom-right": Optional.none(),
    "bottom": Optional.none(),
    "bottom-left": Optional.none(),
    "left": Optional.some(setToLEdge)
  };
  var HorizontalModel = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    setValueFrom: setValueFrom$2,
    setToMin: setToMin$2,
    setToMax: setToMax$2,
    findValueOfOffset: findValueOfOffset$1,
    getValueFromEvent: getValueFromEvent$2,
    findPositionOfValue: findPositionOfValue$1,
    setPositionFromValue: setPositionFromValue$2,
    onLeft: onLeft$2,
    onRight: onRight$2,
    onUp: onUp$2,
    onDown: onDown$2,
    edgeActions: edgeActions$2
  });
  const fireSliderChange$1 = (spectrum, value2) => {
    emitWith(spectrum, sliderChangeEvent(), { value: value2 });
  };
  const findValueOfOffset = (spectrum, detail, top2) => {
    const args = {
      min: minY(detail),
      max: maxY(detail),
      range: yRange(detail),
      value: top2,
      step: step(detail),
      snap: snap(detail),
      snapStart: snapStart(detail),
      rounded: rounded(detail),
      hasMinEdge: hasTEdge(detail),
      hasMaxEdge: hasBEdge(detail),
      minBound: getMinYBounds(spectrum),
      maxBound: getMaxYBounds(spectrum),
      screenRange: getYScreenRange(spectrum)
    };
    return findValueOf(args);
  };
  const setValueFrom$1 = (spectrum, detail, value2) => {
    const yValue = findValueOfOffset(spectrum, detail, value2);
    const sliderVal = yValue;
    fireSliderChange$1(spectrum, sliderVal);
    return yValue;
  };
  const setToMin$1 = (spectrum, detail) => {
    const min2 = minY(detail);
    fireSliderChange$1(spectrum, min2);
  };
  const setToMax$1 = (spectrum, detail) => {
    const max2 = maxY(detail);
    fireSliderChange$1(spectrum, max2);
  };
  const moveBy$1 = (direction, spectrum, detail) => {
    const f = direction > 0 ? increaseBy : reduceBy;
    const yValue = f(currentValue(detail), minY(detail), maxY(detail), step(detail));
    fireSliderChange$1(spectrum, yValue);
    return Optional.some(yValue);
  };
  const handleMovement$1 = (direction) => (spectrum, detail) => moveBy$1(direction, spectrum, detail).map(always);
  const getValueFromEvent$1 = (simulatedEvent) => {
    const pos = getEventSource(simulatedEvent);
    return pos.map((p) => {
      return p.top;
    });
  };
  const findOffsetOfValue = (spectrum, detail, value2, minEdge, maxEdge) => {
    const minOffset = 0;
    const maxOffset = getYScreenRange(spectrum);
    const centerMinEdge = minEdge.bind((edge2) => Optional.some(getYCenterOffSetOf(edge2, spectrum))).getOr(minOffset);
    const centerMaxEdge = maxEdge.bind((edge2) => Optional.some(getYCenterOffSetOf(edge2, spectrum))).getOr(maxOffset);
    const args = {
      min: minY(detail),
      max: maxY(detail),
      range: yRange(detail),
      value: value2,
      hasMinEdge: hasTEdge(detail),
      hasMaxEdge: hasBEdge(detail),
      minBound: getMinYBounds(spectrum),
      minOffset,
      maxBound: getMaxYBounds(spectrum),
      maxOffset,
      centerMinEdge,
      centerMaxEdge
    };
    return findOffsetOfValue$2(args);
  };
  const findPositionOfValue = (slider, spectrum, value2, minEdge, maxEdge, detail) => {
    const offset2 = findOffsetOfValue(spectrum, detail, value2, minEdge, maxEdge);
    return getMinYBounds(spectrum) - getMinYBounds(slider) + offset2;
  };
  const setPositionFromValue$1 = (slider, thumb, detail, edges) => {
    const value2 = currentValue(detail);
    const pos = findPositionOfValue(slider, edges.getSpectrum(slider), value2, edges.getTopEdge(slider), edges.getBottomEdge(slider), detail);
    const thumbRadius = get$d(thumb.element) / 2;
    set$8(thumb.element, "top", pos - thumbRadius + "px");
  };
  const onLeft$1 = Optional.none;
  const onRight$1 = Optional.none;
  const onUp$1 = handleMovement$1(-1);
  const onDown$1 = handleMovement$1(1);
  const edgeActions$1 = {
    "top-left": Optional.none(),
    "top": Optional.some(setToTEdge),
    "top-right": Optional.none(),
    "right": Optional.none(),
    "bottom-right": Optional.none(),
    "bottom": Optional.some(setToBEdge),
    "bottom-left": Optional.none(),
    "left": Optional.none()
  };
  var VerticalModel = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    setValueFrom: setValueFrom$1,
    setToMin: setToMin$1,
    setToMax: setToMax$1,
    findValueOfOffset,
    getValueFromEvent: getValueFromEvent$1,
    findPositionOfValue,
    setPositionFromValue: setPositionFromValue$1,
    onLeft: onLeft$1,
    onRight: onRight$1,
    onUp: onUp$1,
    onDown: onDown$1,
    edgeActions: edgeActions$1
  });
  const fireSliderChange = (spectrum, value2) => {
    emitWith(spectrum, sliderChangeEvent(), { value: value2 });
  };
  const sliderValue = (x, y) => ({
    x,
    y
  });
  const setValueFrom = (spectrum, detail, value2) => {
    const xValue = findValueOfOffset$1(spectrum, detail, value2.left);
    const yValue = findValueOfOffset(spectrum, detail, value2.top);
    const val = sliderValue(xValue, yValue);
    fireSliderChange(spectrum, val);
    return val;
  };
  const moveBy = (direction, isVerticalMovement, spectrum, detail) => {
    const f = direction > 0 ? increaseBy : reduceBy;
    const xValue = isVerticalMovement ? currentValue(detail).x : f(currentValue(detail).x, minX(detail), maxX(detail), step(detail));
    const yValue = !isVerticalMovement ? currentValue(detail).y : f(currentValue(detail).y, minY(detail), maxY(detail), step(detail));
    fireSliderChange(spectrum, sliderValue(xValue, yValue));
    return Optional.some(xValue);
  };
  const handleMovement = (direction, isVerticalMovement) => (spectrum, detail) => moveBy(direction, isVerticalMovement, spectrum, detail).map(always);
  const setToMin = (spectrum, detail) => {
    const mX = minX(detail);
    const mY = minY(detail);
    fireSliderChange(spectrum, sliderValue(mX, mY));
  };
  const setToMax = (spectrum, detail) => {
    const mX = maxX(detail);
    const mY = maxY(detail);
    fireSliderChange(spectrum, sliderValue(mX, mY));
  };
  const getValueFromEvent = (simulatedEvent) => getEventSource(simulatedEvent);
  const setPositionFromValue = (slider, thumb, detail, edges) => {
    const value2 = currentValue(detail);
    const xPos = findPositionOfValue$1(slider, edges.getSpectrum(slider), value2.x, edges.getLeftEdge(slider), edges.getRightEdge(slider), detail);
    const yPos = findPositionOfValue(slider, edges.getSpectrum(slider), value2.y, edges.getTopEdge(slider), edges.getBottomEdge(slider), detail);
    const thumbXRadius = get$c(thumb.element) / 2;
    const thumbYRadius = get$d(thumb.element) / 2;
    set$8(thumb.element, "left", xPos - thumbXRadius + "px");
    set$8(thumb.element, "top", yPos - thumbYRadius + "px");
  };
  const onLeft = handleMovement(-1, false);
  const onRight = handleMovement(1, false);
  const onUp = handleMovement(-1, true);
  const onDown = handleMovement(1, true);
  const edgeActions = {
    "top-left": Optional.some(setToTLEdgeXY),
    "top": Optional.some(setToTEdgeXY),
    "top-right": Optional.some(setToTREdgeXY),
    "right": Optional.some(setToREdgeXY),
    "bottom-right": Optional.some(setToBREdgeXY),
    "bottom": Optional.some(setToBEdgeXY),
    "bottom-left": Optional.some(setToBLEdgeXY),
    "left": Optional.some(setToLEdgeXY)
  };
  var TwoDModel = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    setValueFrom,
    setToMin,
    setToMax,
    getValueFromEvent,
    setPositionFromValue,
    onLeft,
    onRight,
    onUp,
    onDown,
    edgeActions
  });
  const SliderSchema = [
    defaulted("stepSize", 1),
    defaulted("onChange", noop),
    defaulted("onChoose", noop),
    defaulted("onInit", noop),
    defaulted("onDragStart", noop),
    defaulted("onDragEnd", noop),
    defaulted("snapToGrid", false),
    defaulted("rounded", true),
    option$3("snapStart"),
    requiredOf("model", choose$1("mode", {
      x: [
        defaulted("minX", 0),
        defaulted("maxX", 100),
        customField("value", (spec) => Cell(spec.mode.minX)),
        required$1("getInitialValue"),
        output$1("manager", HorizontalModel)
      ],
      y: [
        defaulted("minY", 0),
        defaulted("maxY", 100),
        customField("value", (spec) => Cell(spec.mode.minY)),
        required$1("getInitialValue"),
        output$1("manager", VerticalModel)
      ],
      xy: [
        defaulted("minX", 0),
        defaulted("maxX", 100),
        defaulted("minY", 0),
        defaulted("maxY", 100),
        customField("value", (spec) => Cell({
          x: spec.mode.minX,
          y: spec.mode.minY
        })),
        required$1("getInitialValue"),
        output$1("manager", TwoDModel)
      ]
    })),
    field("sliderBehaviours", [
      Keying,
      Representing
    ]),
    customField("mouseIsDown", () => Cell(false))
  ];
  const sketch$2 = (detail, components2, _spec, _externals) => {
    const getThumb = (component) => getPartOrDie(component, detail, "thumb");
    const getSpectrum = (component) => getPartOrDie(component, detail, "spectrum");
    const getLeftEdge = (component) => getPart(component, detail, "left-edge");
    const getRightEdge = (component) => getPart(component, detail, "right-edge");
    const getTopEdge = (component) => getPart(component, detail, "top-edge");
    const getBottomEdge = (component) => getPart(component, detail, "bottom-edge");
    const modelDetail = detail.model;
    const model = modelDetail.manager;
    const refresh2 = (slider, thumb) => {
      model.setPositionFromValue(slider, thumb, detail, {
        getLeftEdge,
        getRightEdge,
        getTopEdge,
        getBottomEdge,
        getSpectrum
      });
    };
    const setValue2 = (slider, newValue) => {
      modelDetail.value.set(newValue);
      const thumb = getThumb(slider);
      refresh2(slider, thumb);
    };
    const changeValue = (slider, newValue) => {
      setValue2(slider, newValue);
      const thumb = getThumb(slider);
      detail.onChange(slider, thumb, newValue);
      return Optional.some(true);
    };
    const resetToMin = (slider) => {
      model.setToMin(slider, detail);
    };
    const resetToMax = (slider) => {
      model.setToMax(slider, detail);
    };
    const choose2 = (slider) => {
      const fireOnChoose = () => {
        getPart(slider, detail, "thumb").each((thumb) => {
          const value2 = modelDetail.value.get();
          detail.onChoose(slider, thumb, value2);
        });
      };
      const wasDown = detail.mouseIsDown.get();
      detail.mouseIsDown.set(false);
      if (wasDown) {
        fireOnChoose();
      }
    };
    const onDragStart = (slider, simulatedEvent) => {
      simulatedEvent.stop();
      detail.mouseIsDown.set(true);
      detail.onDragStart(slider, getThumb(slider));
    };
    const onDragEnd = (slider, simulatedEvent) => {
      simulatedEvent.stop();
      detail.onDragEnd(slider, getThumb(slider));
      choose2(slider);
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: augment(detail.sliderBehaviours, [
        Keying.config({
          mode: "special",
          focusIn: (slider) => {
            return getPart(slider, detail, "spectrum").map(Keying.focusIn).map(always);
          }
        }),
        Representing.config({
          store: {
            mode: "manual",
            getValue: (_) => {
              return modelDetail.value.get();
            },
            setValue: setValue2
          }
        }),
        Receiving.config({ channels: { [mouseReleased()]: { onReceive: choose2 } } })
      ]),
      events: derive$2([
        run$1(sliderChangeEvent(), (slider, simulatedEvent) => {
          changeValue(slider, simulatedEvent.event.value);
        }),
        runOnAttached((slider, _simulatedEvent) => {
          const getInitial = modelDetail.getInitialValue();
          modelDetail.value.set(getInitial);
          const thumb = getThumb(slider);
          refresh2(slider, thumb);
          const spectrum = getSpectrum(slider);
          detail.onInit(slider, thumb, spectrum, modelDetail.value.get());
        }),
        run$1(touchstart(), onDragStart),
        run$1(touchend(), onDragEnd),
        run$1(mousedown(), onDragStart),
        run$1(mouseup(), onDragEnd)
      ]),
      apis: {
        resetToMin,
        resetToMax,
        setValue: setValue2,
        refresh: refresh2
      },
      domModification: { styles: { position: "relative" } }
    };
  };
  const Slider = composite({
    name: "Slider",
    configFields: SliderSchema,
    partFields: SliderParts,
    factory: sketch$2,
    apis: {
      setValue: (apis, slider, value2) => {
        apis.setValue(slider, value2);
      },
      resetToMin: (apis, slider) => {
        apis.resetToMin(slider);
      },
      resetToMax: (apis, slider) => {
        apis.resetToMax(slider);
      },
      refresh: (apis, slider) => {
        apis.refresh(slider);
      }
    }
  });
  const fieldsUpdate = generate$6("rgb-hex-update");
  const sliderUpdate = generate$6("slider-update");
  const paletteUpdate = generate$6("palette-update");
  const sliderFactory = (translate2, getClass) => {
    const spectrum = Slider.parts.spectrum({
      dom: {
        tag: "div",
        classes: [getClass("hue-slider-spectrum")],
        attributes: { role: "presentation" }
      }
    });
    const thumb = Slider.parts.thumb({
      dom: {
        tag: "div",
        classes: [getClass("hue-slider-thumb")],
        attributes: { role: "presentation" }
      }
    });
    return Slider.sketch({
      dom: {
        tag: "div",
        classes: [getClass("hue-slider")],
        attributes: { role: "presentation" }
      },
      rounded: false,
      model: {
        mode: "y",
        getInitialValue: constant$1(0)
      },
      components: [
        spectrum,
        thumb
      ],
      sliderBehaviours: derive$1([Focusing.config({})]),
      onChange: (slider, _thumb, value2) => {
        emitWith(slider, sliderUpdate, { value: value2 });
      }
    });
  };
  const owner$1 = "form";
  const schema$i = [field("formBehaviours", [Representing])];
  const getPartName$1 = (name2) => "<alloy.field." + name2 + ">";
  const sketch$1 = (fSpec) => {
    const parts2 = (() => {
      const record2 = [];
      const field2 = (name2, config2) => {
        record2.push(name2);
        return generateOne$1(owner$1, getPartName$1(name2), config2);
      };
      return {
        field: field2,
        record: constant$1(record2)
      };
    })();
    const spec = fSpec(parts2);
    const partNames = parts2.record();
    const fieldParts = map$2(partNames, (n) => required({
      name: n,
      pname: getPartName$1(n)
    }));
    return composite$1(owner$1, schema$i, fieldParts, make$4, spec);
  };
  const toResult = (o, e) => o.fold(() => Result.error(e), Result.value);
  const make$4 = (detail, components2) => ({
    uid: detail.uid,
    dom: detail.dom,
    components: components2,
    behaviours: augment(detail.formBehaviours, [Representing.config({
      store: {
        mode: "manual",
        getValue: (form) => {
          const resPs = getAllParts(form, detail);
          return map$1(resPs, (resPThunk, pName) => resPThunk().bind((v) => {
            const opt = Composing.getCurrent(v);
            return toResult(opt, new Error(`Cannot find a current component to extract the value from for form part '${pName}': ` + element(v.element)));
          }).map(Representing.getValue));
        },
        setValue: (form, values2) => {
          each(values2, (newValue, key) => {
            getPart(form, detail, key).each((wrapper) => {
              Composing.getCurrent(wrapper).each((field2) => {
                Representing.setValue(field2, newValue);
              });
            });
          });
        }
      }
    })]),
    apis: {
      getField: (form, key) => {
        return getPart(form, detail, key).bind(Composing.getCurrent);
      }
    }
  });
  const Form = {
    getField: makeApi((apis, component, key) => apis.getField(component, key)),
    sketch: sketch$1
  };
  const validInput = generate$6("valid-input");
  const invalidInput = generate$6("invalid-input");
  const validatingInput = generate$6("validating-input");
  const translatePrefix = "colorcustom.rgb.";
  const rgbFormFactory = (translate2, getClass, onValidHexx, onInvalidHexx) => {
    const invalidation = (label2, isValid) => Invalidating.config({
      invalidClass: getClass("invalid"),
      notify: {
        onValidate: (comp) => {
          emitWith(comp, validatingInput, { type: label2 });
        },
        onValid: (comp) => {
          emitWith(comp, validInput, {
            type: label2,
            value: Representing.getValue(comp)
          });
        },
        onInvalid: (comp) => {
          emitWith(comp, invalidInput, {
            type: label2,
            value: Representing.getValue(comp)
          });
        }
      },
      validator: {
        validate: (comp) => {
          const value2 = Representing.getValue(comp);
          const res = isValid(value2) ? Result.value(true) : Result.error(translate2("aria.input.invalid"));
          return Future.pure(res);
        },
        validateOnLoad: false
      }
    });
    const renderTextField2 = (isValid, name2, label2, description, data) => {
      const helptext = translate2(translatePrefix + "range");
      const pLabel = FormField.parts.label({
        dom: {
          tag: "label",
          attributes: { "aria-label": description }
        },
        components: [text$1(label2)]
      });
      const pField = FormField.parts.field({
        data,
        factory: Input,
        inputAttributes: {
          type: "text",
          ...name2 === "hex" ? { "aria-live": "polite" } : {}
        },
        inputClasses: [getClass("textfield")],
        inputBehaviours: derive$1([
          invalidation(name2, isValid),
          Tabstopping.config({})
        ]),
        onSetValue: (input2) => {
          if (Invalidating.isInvalid(input2)) {
            const run2 = Invalidating.run(input2);
            run2.get(noop);
          }
        }
      });
      const comps = [
        pLabel,
        pField
      ];
      const concats = name2 !== "hex" ? [FormField.parts["aria-descriptor"]({ text: helptext })] : [];
      const components2 = comps.concat(concats);
      return {
        dom: {
          tag: "div",
          attributes: { role: "presentation" }
        },
        components: components2
      };
    };
    const copyRgbToHex = (form, rgba) => {
      const hex = fromRgba(rgba);
      Form.getField(form, "hex").each((hexField) => {
        if (!Focusing.isFocused(hexField)) {
          Representing.setValue(form, { hex: hex.value });
        }
      });
      return hex;
    };
    const copyRgbToForm = (form, rgb) => {
      const red2 = rgb.red;
      const green = rgb.green;
      const blue = rgb.blue;
      Representing.setValue(form, {
        red: red2,
        green,
        blue
      });
    };
    const memPreview = record({
      dom: {
        tag: "div",
        classes: [getClass("rgba-preview")],
        styles: { "background-color": "white" },
        attributes: { role: "presentation" }
      }
    });
    const updatePreview = (anyInSystem, hex) => {
      memPreview.getOpt(anyInSystem).each((preview) => {
        set$8(preview.element, "background-color", "#" + hex.value);
      });
    };
    const factory2 = () => {
      const state = {
        red: Cell(Optional.some(255)),
        green: Cell(Optional.some(255)),
        blue: Cell(Optional.some(255)),
        hex: Cell(Optional.some("ffffff"))
      };
      const copyHexToRgb = (form, hex) => {
        const rgb = fromHex(hex);
        copyRgbToForm(form, rgb);
        setValueRgb(rgb);
      };
      const get2 = (prop) => state[prop].get();
      const set2 = (prop, value2) => {
        state[prop].set(value2);
      };
      const getValueRgb = () => get2("red").bind((red2) => get2("green").bind((green) => get2("blue").map((blue) => rgbaColour(red2, green, blue, 1))));
      const setValueRgb = (rgb) => {
        const red2 = rgb.red;
        const green = rgb.green;
        const blue = rgb.blue;
        set2("red", Optional.some(red2));
        set2("green", Optional.some(green));
        set2("blue", Optional.some(blue));
      };
      const onInvalidInput = (form, simulatedEvent) => {
        const data = simulatedEvent.event;
        if (data.type !== "hex") {
          set2(data.type, Optional.none());
        } else {
          onInvalidHexx(form);
        }
      };
      const onValidHex = (form, value2) => {
        onValidHexx(form);
        const hex = hexColour(value2);
        set2("hex", Optional.some(value2));
        const rgb = fromHex(hex);
        copyRgbToForm(form, rgb);
        setValueRgb(rgb);
        emitWith(form, fieldsUpdate, { hex });
        updatePreview(form, hex);
      };
      const onValidRgb = (form, prop, value2) => {
        const val = parseInt(value2, 10);
        set2(prop, Optional.some(val));
        getValueRgb().each((rgb) => {
          const hex = copyRgbToHex(form, rgb);
          emitWith(form, fieldsUpdate, { hex });
          updatePreview(form, hex);
        });
      };
      const isHexInputEvent = (data) => data.type === "hex";
      const onValidInput = (form, simulatedEvent) => {
        const data = simulatedEvent.event;
        if (isHexInputEvent(data)) {
          onValidHex(form, data.value);
        } else {
          onValidRgb(form, data.type, data.value);
        }
      };
      const formPartStrings = (key) => ({
        label: translate2(translatePrefix + key + ".label"),
        description: translate2(translatePrefix + key + ".description")
      });
      const redStrings = formPartStrings("red");
      const greenStrings = formPartStrings("green");
      const blueStrings = formPartStrings("blue");
      const hexStrings = formPartStrings("hex");
      return deepMerge(Form.sketch((parts2) => ({
        dom: {
          tag: "form",
          classes: [getClass("rgb-form")],
          attributes: { "aria-label": translate2("aria.color.picker") }
        },
        components: [
          parts2.field("red", FormField.sketch(renderTextField2(isRgbaComponent, "red", redStrings.label, redStrings.description, 255))),
          parts2.field("green", FormField.sketch(renderTextField2(isRgbaComponent, "green", greenStrings.label, greenStrings.description, 255))),
          parts2.field("blue", FormField.sketch(renderTextField2(isRgbaComponent, "blue", blueStrings.label, blueStrings.description, 255))),
          parts2.field("hex", FormField.sketch(renderTextField2(isHexString, "hex", hexStrings.label, hexStrings.description, "ffffff"))),
          memPreview.asSpec()
        ],
        formBehaviours: derive$1([
          Invalidating.config({ invalidClass: getClass("form-invalid") }),
          config("rgb-form-events", [
            run$1(validInput, onValidInput),
            run$1(invalidInput, onInvalidInput),
            run$1(validatingInput, onInvalidInput)
          ])
        ])
      })), {
        apis: {
          updateHex: (form, hex) => {
            Representing.setValue(form, { hex: hex.value });
            copyHexToRgb(form, hex);
            updatePreview(form, hex);
          }
        }
      });
    };
    const rgbFormSketcher = single({
      factory: factory2,
      name: "RgbForm",
      configFields: [],
      apis: {
        updateHex: (apis, form, hex) => {
          apis.updateHex(form, hex);
        }
      },
      extraApis: {}
    });
    return rgbFormSketcher;
  };
  const paletteFactory = (_translate, getClass) => {
    const spectrumPart2 = Slider.parts.spectrum({
      dom: {
        tag: "canvas",
        attributes: { role: "presentation" },
        classes: [getClass("sv-palette-spectrum")]
      }
    });
    const thumbPart2 = Slider.parts.thumb({
      dom: {
        tag: "div",
        attributes: { role: "presentation" },
        classes: [getClass("sv-palette-thumb")],
        innerHtml: `<div class=${getClass("sv-palette-inner-thumb")} role="presentation"></div>`
      }
    });
    const setColour = (canvas, rgba) => {
      const { width: width2, height: height2 } = canvas;
      const ctx = canvas.getContext("2d");
      if (ctx === null) {
        return;
      }
      ctx.fillStyle = rgba;
      ctx.fillRect(0, 0, width2, height2);
      const grdWhite = ctx.createLinearGradient(0, 0, width2, 0);
      grdWhite.addColorStop(0, "rgba(255,255,255,1)");
      grdWhite.addColorStop(1, "rgba(255,255,255,0)");
      ctx.fillStyle = grdWhite;
      ctx.fillRect(0, 0, width2, height2);
      const grdBlack = ctx.createLinearGradient(0, 0, 0, height2);
      grdBlack.addColorStop(0, "rgba(0,0,0,0)");
      grdBlack.addColorStop(1, "rgba(0,0,0,1)");
      ctx.fillStyle = grdBlack;
      ctx.fillRect(0, 0, width2, height2);
    };
    const setPaletteHue = (slider, hue) => {
      const canvas = slider.components()[0].element.dom;
      const hsv = hsvColour(hue, 100, 100);
      const rgba = fromHsv(hsv);
      setColour(canvas, toString(rgba));
    };
    const setPaletteThumb = (slider, hex) => {
      const hsv = fromRgb(fromHex(hex));
      Slider.setValue(slider, {
        x: hsv.saturation,
        y: 100 - hsv.value
      });
    };
    const factory2 = (_detail) => {
      const getInitialValue = constant$1({
        x: 0,
        y: 0
      });
      const onChange = (slider, _thumb, value2) => {
        emitWith(slider, paletteUpdate, { value: value2 });
      };
      const onInit = (_slider, _thumb, spectrum, _value) => {
        setColour(spectrum.element.dom, toString(red));
      };
      const sliderBehaviours = derive$1([
        Composing.config({ find: Optional.some }),
        Focusing.config({})
      ]);
      return Slider.sketch({
        dom: {
          tag: "div",
          attributes: { role: "presentation" },
          classes: [getClass("sv-palette")]
        },
        model: {
          mode: "xy",
          getInitialValue
        },
        rounded: false,
        components: [
          spectrumPart2,
          thumbPart2
        ],
        onChange,
        onInit,
        sliderBehaviours
      });
    };
    const saturationBrightnessPaletteSketcher = single({
      factory: factory2,
      name: "SaturationBrightnessPalette",
      configFields: [],
      apis: {
        setHue: (_apis, slider, hue) => {
          setPaletteHue(slider, hue);
        },
        setThumb: (_apis, slider, hex) => {
          setPaletteThumb(slider, hex);
        }
      },
      extraApis: {}
    });
    return saturationBrightnessPaletteSketcher;
  };
  const makeFactory = (translate2, getClass) => {
    const factory2 = (detail) => {
      const rgbForm = rgbFormFactory(translate2, getClass, detail.onValidHex, detail.onInvalidHex);
      const sbPalette = paletteFactory(translate2, getClass);
      const hueSliderToDegrees = (hue) => (100 - hue) / 100 * 360;
      const hueDegreesToSlider = (hue) => 100 - hue / 360 * 100;
      const state = {
        paletteRgba: Cell(red),
        paletteHue: Cell(0)
      };
      const memSlider = record(sliderFactory(translate2, getClass));
      const memPalette = record(sbPalette.sketch({}));
      const memRgb = record(rgbForm.sketch({}));
      const updatePalette = (anyInSystem, _hex, hue) => {
        memPalette.getOpt(anyInSystem).each((palette) => {
          sbPalette.setHue(palette, hue);
        });
      };
      const updateFields = (anyInSystem, hex) => {
        memRgb.getOpt(anyInSystem).each((form) => {
          rgbForm.updateHex(form, hex);
        });
      };
      const updateSlider = (anyInSystem, _hex, hue) => {
        memSlider.getOpt(anyInSystem).each((slider) => {
          Slider.setValue(slider, hueDegreesToSlider(hue));
        });
      };
      const updatePaletteThumb = (anyInSystem, hex) => {
        memPalette.getOpt(anyInSystem).each((palette) => {
          sbPalette.setThumb(palette, hex);
        });
      };
      const updateState = (hex, hue) => {
        const rgba = fromHex(hex);
        state.paletteRgba.set(rgba);
        state.paletteHue.set(hue);
      };
      const runUpdates = (anyInSystem, hex, hue, updates) => {
        updateState(hex, hue);
        each$1(updates, (update) => {
          update(anyInSystem, hex, hue);
        });
      };
      const onPaletteUpdate = () => {
        const updates = [updateFields];
        return (form, simulatedEvent) => {
          const value2 = simulatedEvent.event.value;
          const oldHue = state.paletteHue.get();
          const newHsv = hsvColour(oldHue, value2.x, 100 - value2.y);
          const newHex = hsvToHex(newHsv);
          runUpdates(form, newHex, oldHue, updates);
        };
      };
      const onSliderUpdate = () => {
        const updates = [
          updatePalette,
          updateFields
        ];
        return (form, simulatedEvent) => {
          const hue = hueSliderToDegrees(simulatedEvent.event.value);
          const oldRgb = state.paletteRgba.get();
          const oldHsv = fromRgb(oldRgb);
          const newHsv = hsvColour(hue, oldHsv.saturation, oldHsv.value);
          const newHex = hsvToHex(newHsv);
          runUpdates(form, newHex, hue, updates);
        };
      };
      const onFieldsUpdate = () => {
        const updates = [
          updatePalette,
          updateSlider,
          updatePaletteThumb
        ];
        return (form, simulatedEvent) => {
          const hex = simulatedEvent.event.hex;
          const hsv = hexToHsv(hex);
          runUpdates(form, hex, hsv.hue, updates);
        };
      };
      return {
        uid: detail.uid,
        dom: detail.dom,
        components: [
          memPalette.asSpec(),
          memSlider.asSpec(),
          memRgb.asSpec()
        ],
        behaviours: derive$1([
          config("colour-picker-events", [
            run$1(fieldsUpdate, onFieldsUpdate()),
            run$1(paletteUpdate, onPaletteUpdate()),
            run$1(sliderUpdate, onSliderUpdate())
          ]),
          Composing.config({ find: (comp) => memRgb.getOpt(comp) }),
          Keying.config({ mode: "acyclic" })
        ])
      };
    };
    const colourPickerSketcher = single({
      name: "ColourPicker",
      configFields: [
        required$1("dom"),
        defaulted("onValidHex", noop),
        defaulted("onInvalidHex", noop)
      ],
      factory: factory2
    });
    return colourPickerSketcher;
  };
  const self2 = () => Composing.config({ find: Optional.some });
  const memento$1 = (mem) => Composing.config({ find: mem.getOpt });
  const childAt = (index) => Composing.config({ find: (comp) => child$2(comp.element, index).bind((element2) => comp.getSystem().getByDom(element2).toOptional()) });
  const ComposingConfigs = {
    self: self2,
    memento: memento$1,
    childAt
  };
  const processors = objOf([
    defaulted("preprocess", identity),
    defaulted("postprocess", identity)
  ]);
  const memento = (mem, rawProcessors) => {
    const ps = asRawOrDie$1("RepresentingConfigs.memento processors", processors, rawProcessors);
    return Representing.config({
      store: {
        mode: "manual",
        getValue: (comp) => {
          const other = mem.get(comp);
          const rawValue = Representing.getValue(other);
          return ps.postprocess(rawValue);
        },
        setValue: (comp, rawValue) => {
          const newValue = ps.preprocess(rawValue);
          const other = mem.get(comp);
          Representing.setValue(other, newValue);
        }
      }
    });
  };
  const withComp = (optInitialValue, getter, setter) => Representing.config({
    store: {
      mode: "manual",
      ...optInitialValue.map((initialValue) => ({ initialValue })).getOr({}),
      getValue: getter,
      setValue: setter
    }
  });
  const withElement = (initialValue, getter, setter) => withComp(initialValue, (c) => getter(c.element), (c, v) => setter(c.element, v));
  const domValue = (optInitialValue) => withElement(optInitialValue, get$6, set$5);
  const domHtml = (optInitialValue) => withElement(optInitialValue, get$9, set$6);
  const memory = (initialValue) => Representing.config({
    store: {
      mode: "memory",
      initialValue
    }
  });
  const RepresentingConfigs = {
    memento,
    withElement,
    withComp,
    domValue,
    domHtml,
    memory
  };
  const english = {
    "colorcustom.rgb.red.label": "R",
    "colorcustom.rgb.red.description": "Red component",
    "colorcustom.rgb.green.label": "G",
    "colorcustom.rgb.green.description": "Green component",
    "colorcustom.rgb.blue.label": "B",
    "colorcustom.rgb.blue.description": "Blue component",
    "colorcustom.rgb.hex.label": "#",
    "colorcustom.rgb.hex.description": "Hex color code",
    "colorcustom.rgb.range": "Range 0 to 255",
    "aria.color.picker": "Color Picker",
    "aria.input.invalid": "Invalid input"
  };
  const translate$1 = (providerBackstage) => (key) => {
    return providerBackstage.translate(english[key]);
  };
  const renderColorPicker = (_spec, providerBackstage, initialData) => {
    const getClass = (key) => "tox-" + key;
    const colourPickerFactory = makeFactory(translate$1(providerBackstage), getClass);
    const onValidHex = (form) => {
      emitWith(form, formActionEvent, {
        name: "hex-valid",
        value: true
      });
    };
    const onInvalidHex = (form) => {
      emitWith(form, formActionEvent, {
        name: "hex-valid",
        value: false
      });
    };
    const memPicker = record(colourPickerFactory.sketch({
      dom: {
        tag: "div",
        classes: [getClass("color-picker-container")],
        attributes: { role: "presentation" }
      },
      onValidHex,
      onInvalidHex
    }));
    return {
      dom: { tag: "div" },
      components: [memPicker.asSpec()],
      behaviours: derive$1([
        RepresentingConfigs.withComp(initialData, (comp) => {
          const picker = memPicker.get(comp);
          const optRgbForm = Composing.getCurrent(picker);
          const optHex = optRgbForm.bind((rgbForm) => {
            const formValues = Representing.getValue(rgbForm);
            return formValues.hex;
          });
          return optHex.map((hex) => "#" + hex).getOr("");
        }, (comp, newValue) => {
          const pattern2 = /^#([a-fA-F0-9]{3}(?:[a-fA-F0-9]{3})?)/;
          const valOpt = Optional.from(pattern2.exec(newValue)).bind((matches) => get$h(matches, 1));
          const picker = memPicker.get(comp);
          const optRgbForm = Composing.getCurrent(picker);
          optRgbForm.fold(() => {
            console.log("Can not find form");
          }, (rgbForm) => {
            Representing.setValue(rgbForm, { hex: valOpt.getOr("") });
            Form.getField(rgbForm, "hex").each((hexField) => {
              emit(hexField, input());
            });
          });
        }),
        ComposingConfigs.self()
      ])
    };
  };
  var global$2 = tinymce.util.Tools.resolve("tinymce.Resource");
  const isOldCustomEditor = (spec) => has$2(spec, "init");
  const renderCustomEditor = (spec) => {
    const editorApi = value$2();
    const memReplaced = record({ dom: { tag: spec.tag } });
    const initialValue = value$2();
    return {
      dom: {
        tag: "div",
        classes: ["tox-custom-editor"]
      },
      behaviours: derive$1([
        config("custom-editor-events", [runOnAttached((component) => {
          memReplaced.getOpt(component).each((ta) => {
            (isOldCustomEditor(spec) ? spec.init(ta.element.dom) : global$2.load(spec.scriptId, spec.scriptUrl).then((init2) => init2(ta.element.dom, spec.settings))).then((ea) => {
              initialValue.on((cvalue) => {
                ea.setValue(cvalue);
              });
              initialValue.clear();
              editorApi.set(ea);
            });
          });
        })]),
        RepresentingConfigs.withComp(Optional.none(), () => editorApi.get().fold(() => initialValue.get().getOr(""), (ed) => ed.getValue()), (component, value2) => {
          editorApi.get().fold(() => initialValue.set(value2), (ed) => ed.setValue(value2));
        }),
        ComposingConfigs.self()
      ]),
      components: [memReplaced.asSpec()]
    };
  };
  var global$1 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const filterByExtension = (files, providersBackstage) => {
    const allowedImageFileTypes = global$1.explode(providersBackstage.getOption("images_file_types"));
    const isFileInAllowedTypes = (file) => exists(allowedImageFileTypes, (type2) => endsWith(file.name.toLowerCase(), `.${type2.toLowerCase()}`));
    return filter$2(from(files), isFileInAllowedTypes);
  };
  const renderDropZone = (spec, providersBackstage, initialData) => {
    const stopper2 = (_, se) => {
      se.stop();
    };
    const sequence2 = (actions) => (comp, se) => {
      each$1(actions, (a) => {
        a(comp, se);
      });
    };
    const onDrop = (comp, se) => {
      var _a;
      if (!Disabling.isDisabled(comp)) {
        const transferEvent = se.event.raw;
        handleFiles(comp, (_a = transferEvent.dataTransfer) === null || _a === void 0 ? void 0 : _a.files);
      }
    };
    const onSelect = (component, simulatedEvent) => {
      const input2 = simulatedEvent.event.raw.target;
      handleFiles(component, input2.files);
    };
    const handleFiles = (component, files) => {
      if (files) {
        Representing.setValue(component, filterByExtension(files, providersBackstage));
        emitWith(component, formChangeEvent, { name: spec.name });
      }
    };
    const memInput = record({
      dom: {
        tag: "input",
        attributes: {
          type: "file",
          accept: "image/*"
        },
        styles: { display: "none" }
      },
      behaviours: derive$1([config("input-file-events", [
        cutter(click()),
        cutter(tap())
      ])])
    });
    const renderField = (s) => ({
      uid: s.uid,
      dom: {
        tag: "div",
        classes: ["tox-dropzone-container"]
      },
      behaviours: derive$1([
        RepresentingConfigs.memory(initialData.getOr([])),
        ComposingConfigs.self(),
        Disabling.config({}),
        Toggling.config({
          toggleClass: "dragenter",
          toggleOnExecute: false
        }),
        config("dropzone-events", [
          run$1("dragenter", sequence2([
            stopper2,
            Toggling.toggle
          ])),
          run$1("dragleave", sequence2([
            stopper2,
            Toggling.toggle
          ])),
          run$1("dragover", stopper2),
          run$1("drop", sequence2([
            stopper2,
            onDrop
          ])),
          run$1(change(), onSelect)
        ])
      ]),
      components: [{
        dom: {
          tag: "div",
          classes: ["tox-dropzone"],
          styles: {}
        },
        components: [
          {
            dom: { tag: "p" },
            components: [text$1(providersBackstage.translate("Drop an image here"))]
          },
          Button.sketch({
            dom: {
              tag: "button",
              styles: { position: "relative" },
              classes: [
                "tox-button",
                "tox-button--secondary"
              ]
            },
            components: [
              text$1(providersBackstage.translate("Browse for an image")),
              memInput.asSpec()
            ],
            action: (comp) => {
              const inputComp = memInput.get(comp);
              inputComp.element.dom.click();
            },
            buttonBehaviours: derive$1([
              Tabstopping.config({}),
              DisablingConfigs.button(providersBackstage.isDisabled),
              receivingConfig()
            ])
          })
        ]
      }]
    });
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const pField = FormField.parts.field({ factory: { sketch: renderField } });
    return renderFormFieldWith(pLabel, pField, ["tox-form__group--stretched"], []);
  };
  const renderGrid = (spec, backstage) => ({
    dom: {
      tag: "div",
      classes: [
        "tox-form__grid",
        `tox-form__grid--${spec.columns}col`
      ]
    },
    components: map$2(spec.items, backstage.interpreter)
  });
  const beforeObject = generate$6("alloy-fake-before-tabstop");
  const afterObject = generate$6("alloy-fake-after-tabstop");
  const craftWithClasses = (classes2) => {
    return {
      dom: {
        tag: "div",
        styles: {
          width: "1px",
          height: "1px",
          outline: "none"
        },
        attributes: { tabindex: "0" },
        classes: classes2
      },
      behaviours: derive$1([
        Focusing.config({ ignore: true }),
        Tabstopping.config({})
      ])
    };
  };
  const craft = (spec) => {
    return {
      dom: {
        tag: "div",
        classes: ["tox-navobj"]
      },
      components: [
        craftWithClasses([beforeObject]),
        spec,
        craftWithClasses([afterObject])
      ],
      behaviours: derive$1([ComposingConfigs.childAt(1)])
    };
  };
  const triggerTab = (placeholder2, shiftKey) => {
    emitWith(placeholder2, keydown(), {
      raw: {
        which: 9,
        shiftKey
      }
    });
  };
  const onFocus = (container, targetComp) => {
    const target = targetComp.element;
    if (has(target, beforeObject)) {
      triggerTab(container, true);
    } else if (has(target, afterObject)) {
      triggerTab(container, false);
    }
  };
  const isPseudoStop = (element2) => {
    return closest(element2, [
      "." + beforeObject,
      "." + afterObject
    ].join(","), never);
  };
  const getDynamicSource = (initialData) => {
    const cachedValue = Cell(initialData.getOr(""));
    return {
      getValue: (_frameComponent) => cachedValue.get(),
      setValue: (frameComponent, html) => {
        if (cachedValue.get() !== html) {
          set$9(frameComponent.element, "srcdoc", html);
        }
        cachedValue.set(html);
      }
    };
  };
  const renderIFrame = (spec, providersBackstage, initialData) => {
    const isSandbox = spec.sandboxed;
    const isTransparent = spec.transparent;
    const baseClass = "tox-dialog__iframe";
    const attributes = {
      ...spec.label.map((title2) => ({ title: title2 })).getOr({}),
      ...initialData.map((html) => ({ srcdoc: html })).getOr({}),
      ...isSandbox ? { sandbox: "allow-scripts allow-same-origin" } : {}
    };
    const sourcing = getDynamicSource(initialData);
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const factory2 = (newSpec) => craft({
      uid: newSpec.uid,
      dom: {
        tag: "iframe",
        attributes,
        classes: isTransparent ? [baseClass] : [
          baseClass,
          `${baseClass}--opaque`
        ]
      },
      behaviours: derive$1([
        Tabstopping.config({}),
        Focusing.config({}),
        RepresentingConfigs.withComp(initialData, sourcing.getValue, sourcing.setValue)
      ])
    });
    const pField = FormField.parts.field({ factory: { sketch: factory2 } });
    return renderFormFieldWith(pLabel, pField, ["tox-form__group--stretched"], []);
  };
  const image = (image2) => new Promise((resolve2, reject) => {
    const loaded = () => {
      destroy();
      resolve2(image2);
    };
    const listeners = [
      bind(image2, "load", loaded),
      bind(image2, "error", () => {
        destroy();
        reject("Unable to load data from image: " + image2.dom.src);
      })
    ];
    const destroy = () => each$1(listeners, (l2) => l2.unbind());
    if (image2.dom.complete) {
      loaded();
    }
  });
  const calculateImagePosition = (panelWidth, panelHeight, imageWidth, imageHeight, zoom) => {
    const width2 = imageWidth * zoom;
    const height2 = imageHeight * zoom;
    const left2 = Math.max(0, panelWidth / 2 - width2 / 2);
    const top2 = Math.max(0, panelHeight / 2 - height2 / 2);
    return {
      left: left2.toString() + "px",
      top: top2.toString() + "px",
      width: width2.toString() + "px",
      height: height2.toString() + "px"
    };
  };
  const zoomToFit = (panel, width2, height2) => {
    const panelW = get$c(panel);
    const panelH = get$d(panel);
    return Math.min(panelW / width2, panelH / height2, 1);
  };
  const renderImagePreview = (spec, initialData) => {
    const cachedData = Cell(initialData.getOr({ url: "" }));
    const memImage = record({
      dom: {
        tag: "img",
        classes: ["tox-imagepreview__image"],
        attributes: initialData.map((data) => ({ src: data.url })).getOr({})
      }
    });
    const memContainer = record({
      dom: {
        tag: "div",
        classes: ["tox-imagepreview__container"],
        attributes: { role: "presentation" }
      },
      components: [memImage.asSpec()]
    });
    const setValue2 = (frameComponent, data) => {
      const translatedData = { url: data.url };
      data.zoom.each((z) => translatedData.zoom = z);
      data.cachedWidth.each((z) => translatedData.cachedWidth = z);
      data.cachedHeight.each((z) => translatedData.cachedHeight = z);
      cachedData.set(translatedData);
      const applyFramePositioning = () => {
        const { cachedWidth, cachedHeight, zoom } = translatedData;
        if (!isUndefined(cachedWidth) && !isUndefined(cachedHeight)) {
          if (isUndefined(zoom)) {
            const z = zoomToFit(frameComponent.element, cachedWidth, cachedHeight);
            translatedData.zoom = z;
          }
          const position2 = calculateImagePosition(get$c(frameComponent.element), get$d(frameComponent.element), cachedWidth, cachedHeight, translatedData.zoom);
          memContainer.getOpt(frameComponent).each((container) => {
            setAll(container.element, position2);
          });
        }
      };
      memImage.getOpt(frameComponent).each((imageComponent) => {
        const img = imageComponent.element;
        if (data.url !== get$f(img, "src")) {
          set$9(img, "src", data.url);
          remove$2(frameComponent.element, "tox-imagepreview__loaded");
        }
        applyFramePositioning();
        image(img).then((img2) => {
          if (frameComponent.getSystem().isConnected()) {
            add$2(frameComponent.element, "tox-imagepreview__loaded");
            translatedData.cachedWidth = img2.dom.naturalWidth;
            translatedData.cachedHeight = img2.dom.naturalHeight;
            applyFramePositioning();
          }
        });
      });
    };
    const styles = {};
    spec.height.each((h) => styles.height = h);
    const fakeValidatedData = initialData.map((d) => ({
      url: d.url,
      zoom: Optional.from(d.zoom),
      cachedWidth: Optional.from(d.cachedWidth),
      cachedHeight: Optional.from(d.cachedHeight)
    }));
    return {
      dom: {
        tag: "div",
        classes: ["tox-imagepreview"],
        styles,
        attributes: { role: "presentation" }
      },
      components: [memContainer.asSpec()],
      behaviours: derive$1([
        ComposingConfigs.self(),
        RepresentingConfigs.withComp(fakeValidatedData, () => cachedData.get(), setValue2)
      ])
    };
  };
  const renderLabel$1 = (spec, backstageShared) => {
    const label2 = {
      dom: {
        tag: "label",
        classes: ["tox-label"]
      },
      components: [text$1(backstageShared.providers.translate(spec.label))]
    };
    const comps = map$2(spec.items, backstageShared.interpreter);
    return {
      dom: {
        tag: "div",
        classes: ["tox-form__group"]
      },
      components: [
        label2,
        ...comps
      ],
      behaviours: derive$1([
        ComposingConfigs.self(),
        Replacing.config({}),
        RepresentingConfigs.domHtml(Optional.none()),
        Keying.config({ mode: "acyclic" })
      ])
    };
  };
  const internalToolbarButtonExecute = generate$6("toolbar.button.execute");
  const onToolbarButtonExecute = (info) => runOnExecute$1((comp, _simulatedEvent) => {
    runWithApi(info, comp)((itemApi) => {
      emitWith(comp, internalToolbarButtonExecute, { buttonApi: itemApi });
      info.onAction(itemApi);
    });
  });
  const toolbarButtonEventOrder = {
    [execute$5()]: [
      "disabling",
      "alloy.base.behaviour",
      "toggling",
      "toolbar-button-events"
    ]
  };
  const renderIcon = (iconName, iconsProvider, behaviours2) => render$3(iconName, {
    tag: "span",
    classes: [
      "tox-icon",
      "tox-tbtn__icon-wrap"
    ],
    behaviours: behaviours2
  }, iconsProvider);
  const renderIconFromPack = (iconName, iconsProvider) => renderIcon(iconName, iconsProvider, []);
  const renderReplaceableIconFromPack = (iconName, iconsProvider) => renderIcon(iconName, iconsProvider, [Replacing.config({})]);
  const renderLabel = (text2, prefix2, providersBackstage) => ({
    dom: {
      tag: "span",
      classes: [`${prefix2}__select-label`]
    },
    components: [text$1(providersBackstage.translate(text2))],
    behaviours: derive$1([Replacing.config({})])
  });
  const updateMenuText = generate$6("update-menu-text");
  const updateMenuIcon = generate$6("update-menu-icon");
  const renderCommonDropdown = (spec, prefix2, sharedBackstage) => {
    const editorOffCell = Cell(noop);
    const optMemDisplayText = spec.text.map((text2) => record(renderLabel(text2, prefix2, sharedBackstage.providers)));
    const optMemDisplayIcon = spec.icon.map((iconName) => record(renderReplaceableIconFromPack(iconName, sharedBackstage.providers.icons)));
    const onLeftOrRightInMenu = (comp, se) => {
      const dropdown = Representing.getValue(comp);
      Focusing.focus(dropdown);
      emitWith(dropdown, "keydown", { raw: se.event.raw });
      Dropdown.close(dropdown);
      return Optional.some(true);
    };
    const role = spec.role.fold(() => ({}), (role2) => ({ role: role2 }));
    const tooltipAttributes = spec.tooltip.fold(() => ({}), (tooltip) => {
      const translatedTooltip = sharedBackstage.providers.translate(tooltip);
      return {
        "title": translatedTooltip,
        "aria-label": translatedTooltip
      };
    });
    const iconSpec = render$3("chevron-down", {
      tag: "div",
      classes: [`${prefix2}__select-chevron`]
    }, sharedBackstage.providers.icons);
    const memDropdown = record(Dropdown.sketch({
      ...spec.uid ? { uid: spec.uid } : {},
      ...role,
      dom: {
        tag: "button",
        classes: [
          prefix2,
          `${prefix2}--select`
        ].concat(map$2(spec.classes, (c) => `${prefix2}--${c}`)),
        attributes: { ...tooltipAttributes }
      },
      components: componentRenderPipeline([
        optMemDisplayIcon.map((mem) => mem.asSpec()),
        optMemDisplayText.map((mem) => mem.asSpec()),
        Optional.some(iconSpec)
      ]),
      matchWidth: true,
      useMinWidth: true,
      onOpen: (anchor2, dropdownComp, tmenuComp) => {
        if (spec.searchable) {
          focusSearchField(tmenuComp);
        }
      },
      dropdownBehaviours: derive$1([
        ...spec.dropdownBehaviours,
        DisablingConfigs.button(() => spec.disabled || sharedBackstage.providers.isDisabled()),
        receivingConfig(),
        Unselecting.config({}),
        Replacing.config({}),
        config("dropdown-events", [
          onControlAttached(spec, editorOffCell),
          onControlDetached(spec, editorOffCell)
        ]),
        config("menubutton-update-display-text", [
          run$1(updateMenuText, (comp, se) => {
            optMemDisplayText.bind((mem) => mem.getOpt(comp)).each((displayText) => {
              Replacing.set(displayText, [text$1(sharedBackstage.providers.translate(se.event.text))]);
            });
          }),
          run$1(updateMenuIcon, (comp, se) => {
            optMemDisplayIcon.bind((mem) => mem.getOpt(comp)).each((displayIcon) => {
              Replacing.set(displayIcon, [renderReplaceableIconFromPack(se.event.icon, sharedBackstage.providers.icons)]);
            });
          })
        ])
      ]),
      eventOrder: deepMerge(toolbarButtonEventOrder, {
        mousedown: [
          "focusing",
          "alloy.base.behaviour",
          "item-type-events",
          "normal-dropdown-events"
        ]
      }),
      sandboxBehaviours: derive$1([
        Keying.config({
          mode: "special",
          onLeft: onLeftOrRightInMenu,
          onRight: onLeftOrRightInMenu
        }),
        config("dropdown-sandbox-events", [
          run$1(refetchTriggerEvent, (originalSandboxComp, se) => {
            handleRefetchTrigger(originalSandboxComp);
            se.stop();
          }),
          run$1(redirectMenuItemInteractionEvent, (sandboxComp, se) => {
            handleRedirectToMenuItem(sandboxComp, se);
            se.stop();
          })
        ])
      ]),
      lazySink: sharedBackstage.getSink,
      toggleClass: `${prefix2}--active`,
      parts: {
        menu: {
          ...part(false, spec.columns, spec.presets),
          fakeFocus: spec.searchable,
          onHighlightItem: updateAriaOnHighlight,
          onCollapseMenu: (tmenuComp, itemCompCausingCollapse, nowActiveMenuComp) => {
            Highlighting.getHighlighted(nowActiveMenuComp).each((itemComp) => {
              updateAriaOnHighlight(tmenuComp, nowActiveMenuComp, itemComp);
            });
          },
          onDehighlightItem: updateAriaOnDehighlight
        }
      },
      fetch: (comp) => Future.nu(curry(spec.fetch, comp))
    }));
    return memDropdown.asSpec();
  };
  const isMenuItemReference = (item2) => isString(item2);
  const isSeparator$2 = (item2) => item2.type === "separator";
  const isExpandingMenuItem = (item2) => has$2(item2, "getSubmenuItems");
  const separator$2 = { type: "separator" };
  const unwrapReferences = (items, menuItems) => {
    const realItems = foldl(items, (acc, item2) => {
      if (isMenuItemReference(item2)) {
        if (item2 === "") {
          return acc;
        } else if (item2 === "|") {
          return acc.length > 0 && !isSeparator$2(acc[acc.length - 1]) ? acc.concat([separator$2]) : acc;
        } else if (has$2(menuItems, item2.toLowerCase())) {
          return acc.concat([menuItems[item2.toLowerCase()]]);
        } else {
          return acc;
        }
      } else {
        return acc.concat([item2]);
      }
    }, []);
    if (realItems.length > 0 && isSeparator$2(realItems[realItems.length - 1])) {
      realItems.pop();
    }
    return realItems;
  };
  const getFromExpandingItem = (item2, menuItems) => {
    const submenuItems = item2.getSubmenuItems();
    const rest = expand(submenuItems, menuItems);
    const newMenus = deepMerge(rest.menus, { [item2.value]: rest.items });
    const newExpansions = deepMerge(rest.expansions, { [item2.value]: item2.value });
    return {
      item: item2,
      menus: newMenus,
      expansions: newExpansions
    };
  };
  const generateValueIfRequired = (item2) => {
    const itemValue = get$g(item2, "value").getOrThunk(() => generate$6("generated-menu-item"));
    return deepMerge({ value: itemValue }, item2);
  };
  const expand = (items, menuItems) => {
    const realItems = unwrapReferences(isString(items) ? items.split(" ") : items, menuItems);
    return foldr(realItems, (acc, item2) => {
      if (isExpandingMenuItem(item2)) {
        const itemWithValue = generateValueIfRequired(item2);
        const newData = getFromExpandingItem(itemWithValue, menuItems);
        return {
          menus: deepMerge(acc.menus, newData.menus),
          items: [
            newData.item,
            ...acc.items
          ],
          expansions: deepMerge(acc.expansions, newData.expansions)
        };
      } else {
        return {
          ...acc,
          items: [
            item2,
            ...acc.items
          ]
        };
      }
    }, {
      menus: {},
      expansions: {},
      items: []
    });
  };
  const getSearchModeForField = (settings) => {
    return settings.search.fold(() => ({ searchMode: "no-search" }), (searchSettings) => ({
      searchMode: "search-with-field",
      placeholder: searchSettings.placeholder
    }));
  };
  const getSearchModeForResults = (settings) => {
    return settings.search.fold(() => ({ searchMode: "no-search" }), (_) => ({ searchMode: "search-with-results" }));
  };
  const build = (items, itemResponse, backstage, settings) => {
    const primary2 = generate$6("primary-menu");
    const data = expand(items, backstage.shared.providers.menuItems());
    if (data.items.length === 0) {
      return Optional.none();
    }
    const mainMenuSearchMode = getSearchModeForField(settings);
    const mainMenu = createPartialMenu(primary2, data.items, itemResponse, backstage, settings.isHorizontalMenu, mainMenuSearchMode);
    const submenuSearchMode = getSearchModeForResults(settings);
    const submenus = map$1(data.menus, (menuItems, menuName) => createPartialMenu(menuName, menuItems, itemResponse, backstage, false, submenuSearchMode));
    const menus = deepMerge(submenus, wrap$1(primary2, mainMenu));
    return Optional.from(tieredMenu.tieredData(primary2, menus, data.expansions));
  };
  const isSingleListItem = (item2) => !has$2(item2, "items");
  const dataAttribute = "data-value";
  const fetchItems = (dropdownComp, name2, items, selectedValue) => map$2(items, (item2) => {
    if (!isSingleListItem(item2)) {
      return {
        type: "nestedmenuitem",
        text: item2.text,
        getSubmenuItems: () => fetchItems(dropdownComp, name2, item2.items, selectedValue)
      };
    } else {
      return {
        type: "togglemenuitem",
        text: item2.text,
        value: item2.value,
        active: item2.value === selectedValue,
        onAction: () => {
          Representing.setValue(dropdownComp, item2.value);
          emitWith(dropdownComp, formChangeEvent, { name: name2 });
          Focusing.focus(dropdownComp);
        }
      };
    }
  });
  const findItemByValue = (items, value2) => findMap(items, (item2) => {
    if (!isSingleListItem(item2)) {
      return findItemByValue(item2.items, value2);
    } else {
      return someIf(item2.value === value2, item2);
    }
  });
  const renderListBox = (spec, backstage, initialData) => {
    const providersBackstage = backstage.shared.providers;
    const initialItem = initialData.bind((value2) => findItemByValue(spec.items, value2)).orThunk(() => head(spec.items).filter(isSingleListItem));
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const pField = FormField.parts.field({
      dom: {},
      factory: {
        sketch: (sketchSpec) => renderCommonDropdown({
          uid: sketchSpec.uid,
          text: initialItem.map((item2) => item2.text),
          icon: Optional.none(),
          tooltip: spec.label,
          role: Optional.none(),
          fetch: (comp, callback) => {
            const items = fetchItems(comp, spec.name, spec.items, Representing.getValue(comp));
            callback(build(items, ItemResponse$1.CLOSE_ON_EXECUTE, backstage, {
              isHorizontalMenu: false,
              search: Optional.none()
            }));
          },
          onSetup: constant$1(noop),
          getApi: constant$1({}),
          columns: 1,
          presets: "normal",
          classes: [],
          dropdownBehaviours: [
            Tabstopping.config({}),
            RepresentingConfigs.withComp(initialItem.map((item2) => item2.value), (comp) => get$f(comp.element, dataAttribute), (comp, data) => {
              findItemByValue(spec.items, data).each((item2) => {
                set$9(comp.element, dataAttribute, item2.value);
                emitWith(comp, updateMenuText, { text: item2.text });
              });
            })
          ]
        }, "tox-listbox", backstage.shared)
      }
    });
    const listBoxWrap = {
      dom: {
        tag: "div",
        classes: ["tox-listboxfield"]
      },
      components: [pField]
    };
    return FormField.sketch({
      dom: {
        tag: "div",
        classes: ["tox-form__group"]
      },
      components: flatten([
        pLabel.toArray(),
        [listBoxWrap]
      ]),
      fieldBehaviours: derive$1([Disabling.config({
        disabled: constant$1(!spec.enabled),
        onDisabled: (comp) => {
          FormField.getField(comp).each(Disabling.disable);
        },
        onEnabled: (comp) => {
          FormField.getField(comp).each(Disabling.enable);
        }
      })])
    });
  };
  const renderPanel = (spec, backstage) => ({
    dom: {
      tag: "div",
      classes: spec.classes
    },
    components: map$2(spec.items, backstage.shared.interpreter)
  });
  const factory$f = (detail, _spec) => {
    const options = map$2(detail.options, (option2) => ({
      dom: {
        tag: "option",
        value: option2.value,
        innerHtml: option2.text
      }
    }));
    const initialValues = detail.data.map((v) => wrap$1("initialValue", v)).getOr({});
    return {
      uid: detail.uid,
      dom: {
        tag: "select",
        classes: detail.selectClasses,
        attributes: detail.selectAttributes
      },
      components: options,
      behaviours: augment(detail.selectBehaviours, [
        Focusing.config({}),
        Representing.config({
          store: {
            mode: "manual",
            getValue: (select2) => {
              return get$6(select2.element);
            },
            setValue: (select2, newValue) => {
              const found = find$5(detail.options, (opt) => opt.value === newValue);
              if (found.isSome()) {
                set$5(select2.element, newValue);
              }
            },
            ...initialValues
          }
        })
      ])
    };
  };
  const HtmlSelect = single({
    name: "HtmlSelect",
    configFields: [
      required$1("options"),
      field("selectBehaviours", [
        Focusing,
        Representing
      ]),
      defaulted("selectClasses", []),
      defaulted("selectAttributes", {}),
      option$3("data")
    ],
    factory: factory$f
  });
  const renderSelectBox = (spec, providersBackstage, initialData) => {
    const translatedOptions = map$2(spec.items, (item2) => ({
      text: providersBackstage.translate(item2.text),
      value: item2.value
    }));
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const pField = FormField.parts.field({
      dom: {},
      ...initialData.map((data) => ({ data })).getOr({}),
      selectAttributes: { size: spec.size },
      options: translatedOptions,
      factory: HtmlSelect,
      selectBehaviours: derive$1([
        Disabling.config({ disabled: () => !spec.enabled || providersBackstage.isDisabled() }),
        Tabstopping.config({}),
        config("selectbox-change", [run$1(change(), (component, _) => {
          emitWith(component, formChangeEvent, { name: spec.name });
        })])
      ])
    });
    const chevron = spec.size > 1 ? Optional.none() : Optional.some(render$3("chevron-down", {
      tag: "div",
      classes: ["tox-selectfield__icon-js"]
    }, providersBackstage.icons));
    const selectWrap = {
      dom: {
        tag: "div",
        classes: ["tox-selectfield"]
      },
      components: flatten([
        [pField],
        chevron.toArray()
      ])
    };
    return FormField.sketch({
      dom: {
        tag: "div",
        classes: ["tox-form__group"]
      },
      components: flatten([
        pLabel.toArray(),
        [selectWrap]
      ]),
      fieldBehaviours: derive$1([
        Disabling.config({
          disabled: () => !spec.enabled || providersBackstage.isDisabled(),
          onDisabled: (comp) => {
            FormField.getField(comp).each(Disabling.disable);
          },
          onEnabled: (comp) => {
            FormField.getField(comp).each(Disabling.enable);
          }
        }),
        receivingConfig()
      ])
    });
  };
  const schema$h = constant$1([
    defaulted("field1Name", "field1"),
    defaulted("field2Name", "field2"),
    onStrictHandler("onLockedChange"),
    markers$1(["lockClass"]),
    defaulted("locked", false),
    SketchBehaviours.field("coupledFieldBehaviours", [
      Composing,
      Representing
    ])
  ]);
  const getField = (comp, detail, partName) => getPart(comp, detail, partName).bind(Composing.getCurrent);
  const coupledPart = (selfName, otherName) => required({
    factory: FormField,
    name: selfName,
    overrides: (detail) => {
      return {
        fieldBehaviours: derive$1([config("coupled-input-behaviour", [run$1(input(), (me) => {
          getField(me, detail, otherName).each((other) => {
            getPart(me, detail, "lock").each((lock) => {
              if (Toggling.isOn(lock)) {
                detail.onLockedChange(me, other, lock);
              }
            });
          });
        })])])
      };
    }
  });
  const parts$c = constant$1([
    coupledPart("field1", "field2"),
    coupledPart("field2", "field1"),
    required({
      factory: Button,
      schema: [required$1("dom")],
      name: "lock",
      overrides: (detail) => {
        return {
          buttonBehaviours: derive$1([Toggling.config({
            selected: detail.locked,
            toggleClass: detail.markers.lockClass,
            aria: { mode: "pressed" }
          })])
        };
      }
    })
  ]);
  const factory$e = (detail, components2, _spec, _externals) => ({
    uid: detail.uid,
    dom: detail.dom,
    components: components2,
    behaviours: SketchBehaviours.augment(detail.coupledFieldBehaviours, [
      Composing.config({ find: Optional.some }),
      Representing.config({
        store: {
          mode: "manual",
          getValue: (comp) => {
            const parts2 = getPartsOrDie(comp, detail, [
              "field1",
              "field2"
            ]);
            return {
              [detail.field1Name]: Representing.getValue(parts2.field1()),
              [detail.field2Name]: Representing.getValue(parts2.field2())
            };
          },
          setValue: (comp, value2) => {
            const parts2 = getPartsOrDie(comp, detail, [
              "field1",
              "field2"
            ]);
            if (hasNonNullableKey(value2, detail.field1Name)) {
              Representing.setValue(parts2.field1(), value2[detail.field1Name]);
            }
            if (hasNonNullableKey(value2, detail.field2Name)) {
              Representing.setValue(parts2.field2(), value2[detail.field2Name]);
            }
          }
        }
      })
    ]),
    apis: {
      getField1: (component) => getPart(component, detail, "field1"),
      getField2: (component) => getPart(component, detail, "field2"),
      getLock: (component) => getPart(component, detail, "lock")
    }
  });
  const FormCoupledInputs = composite({
    name: "FormCoupledInputs",
    configFields: schema$h(),
    partFields: parts$c(),
    factory: factory$e,
    apis: {
      getField1: (apis, component) => apis.getField1(component),
      getField2: (apis, component) => apis.getField2(component),
      getLock: (apis, component) => apis.getLock(component)
    }
  });
  const formatSize = (size) => {
    const unitDec = {
      "": 0,
      "px": 0,
      "pt": 1,
      "mm": 1,
      "pc": 2,
      "ex": 2,
      "em": 2,
      "ch": 2,
      "rem": 2,
      "cm": 3,
      "in": 4,
      "%": 4
    };
    const maxDecimal = (unit) => unit in unitDec ? unitDec[unit] : 1;
    let numText = size.value.toFixed(maxDecimal(size.unit));
    if (numText.indexOf(".") !== -1) {
      numText = numText.replace(/\.?0*$/, "");
    }
    return numText + size.unit;
  };
  const parseSize = (sizeText) => {
    const numPattern = /^\s*(\d+(?:\.\d+)?)\s*(|cm|mm|in|px|pt|pc|em|ex|ch|rem|vw|vh|vmin|vmax|%)\s*$/;
    const match = numPattern.exec(sizeText);
    if (match !== null) {
      const value2 = parseFloat(match[1]);
      const unit = match[2];
      return Result.value({
        value: value2,
        unit
      });
    } else {
      return Result.error(sizeText);
    }
  };
  const convertUnit = (size, unit) => {
    const inInch = {
      "": 96,
      "px": 96,
      "pt": 72,
      "cm": 2.54,
      "pc": 12,
      "mm": 25.4,
      "in": 1
    };
    const supported2 = (u) => has$2(inInch, u);
    if (size.unit === unit) {
      return Optional.some(size.value);
    } else if (supported2(size.unit) && supported2(unit)) {
      if (inInch[size.unit] === inInch[unit]) {
        return Optional.some(size.value);
      } else {
        return Optional.some(size.value / inInch[size.unit] * inInch[unit]);
      }
    } else {
      return Optional.none();
    }
  };
  const noSizeConversion = (_input) => Optional.none();
  const ratioSizeConversion = (scale, unit) => (size) => convertUnit(size, unit).map((value2) => ({
    value: value2 * scale,
    unit
  }));
  const makeRatioConverter = (currentFieldText, otherFieldText) => {
    const cValue = parseSize(currentFieldText).toOptional();
    const oValue = parseSize(otherFieldText).toOptional();
    return lift2(cValue, oValue, (cSize, oSize) => convertUnit(cSize, oSize.unit).map((val) => oSize.value / val).map((r2) => ratioSizeConversion(r2, oSize.unit)).getOr(noSizeConversion)).getOr(noSizeConversion);
  };
  const renderSizeInput = (spec, providersBackstage) => {
    let converter = noSizeConversion;
    const ratioEvent = generate$6("ratio-event");
    const makeIcon = (iconName) => render$3(iconName, {
      tag: "span",
      classes: [
        "tox-icon",
        "tox-lock-icon__" + iconName
      ]
    }, providersBackstage.icons);
    const pLock = FormCoupledInputs.parts.lock({
      dom: {
        tag: "button",
        classes: [
          "tox-lock",
          "tox-button",
          "tox-button--naked",
          "tox-button--icon"
        ],
        attributes: { title: providersBackstage.translate(spec.label.getOr("Constrain proportions")) }
      },
      components: [
        makeIcon("lock"),
        makeIcon("unlock")
      ],
      buttonBehaviours: derive$1([
        Disabling.config({ disabled: () => !spec.enabled || providersBackstage.isDisabled() }),
        receivingConfig(),
        Tabstopping.config({})
      ])
    });
    const formGroup = (components2) => ({
      dom: {
        tag: "div",
        classes: ["tox-form__group"]
      },
      components: components2
    });
    const getFieldPart = (isField1) => FormField.parts.field({
      factory: Input,
      inputClasses: ["tox-textfield"],
      inputBehaviours: derive$1([
        Disabling.config({ disabled: () => !spec.enabled || providersBackstage.isDisabled() }),
        receivingConfig(),
        Tabstopping.config({}),
        config("size-input-events", [
          run$1(focusin(), (component, _simulatedEvent) => {
            emitWith(component, ratioEvent, { isField1 });
          }),
          run$1(change(), (component, _simulatedEvent) => {
            emitWith(component, formChangeEvent, { name: spec.name });
          })
        ])
      ]),
      selectOnFocus: false
    });
    const getLabel = (label2) => ({
      dom: {
        tag: "label",
        classes: ["tox-label"]
      },
      components: [text$1(providersBackstage.translate(label2))]
    });
    const widthField = FormCoupledInputs.parts.field1(formGroup([
      FormField.parts.label(getLabel("Width")),
      getFieldPart(true)
    ]));
    const heightField = FormCoupledInputs.parts.field2(formGroup([
      FormField.parts.label(getLabel("Height")),
      getFieldPart(false)
    ]));
    return FormCoupledInputs.sketch({
      dom: {
        tag: "div",
        classes: ["tox-form__group"]
      },
      components: [{
        dom: {
          tag: "div",
          classes: ["tox-form__controls-h-stack"]
        },
        components: [
          widthField,
          heightField,
          formGroup([
            getLabel(nbsp),
            pLock
          ])
        ]
      }],
      field1Name: "width",
      field2Name: "height",
      locked: true,
      markers: { lockClass: "tox-locked" },
      onLockedChange: (current, other, _lock) => {
        parseSize(Representing.getValue(current)).each((size) => {
          converter(size).each((newSize) => {
            Representing.setValue(other, formatSize(newSize));
          });
        });
      },
      coupledFieldBehaviours: derive$1([
        Disabling.config({
          disabled: () => !spec.enabled || providersBackstage.isDisabled(),
          onDisabled: (comp) => {
            FormCoupledInputs.getField1(comp).bind(FormField.getField).each(Disabling.disable);
            FormCoupledInputs.getField2(comp).bind(FormField.getField).each(Disabling.disable);
            FormCoupledInputs.getLock(comp).each(Disabling.disable);
          },
          onEnabled: (comp) => {
            FormCoupledInputs.getField1(comp).bind(FormField.getField).each(Disabling.enable);
            FormCoupledInputs.getField2(comp).bind(FormField.getField).each(Disabling.enable);
            FormCoupledInputs.getLock(comp).each(Disabling.enable);
          }
        }),
        receivingConfig(),
        config("size-input-events2", [run$1(ratioEvent, (component, simulatedEvent) => {
          const isField1 = simulatedEvent.event.isField1;
          const optCurrent = isField1 ? FormCoupledInputs.getField1(component) : FormCoupledInputs.getField2(component);
          const optOther = isField1 ? FormCoupledInputs.getField2(component) : FormCoupledInputs.getField1(component);
          const value1 = optCurrent.map(Representing.getValue).getOr("");
          const value2 = optOther.map(Representing.getValue).getOr("");
          converter = makeRatioConverter(value1, value2);
        })])
      ])
    });
  };
  const renderSlider = (spec, providerBackstage, initialData) => {
    const labelPart2 = Slider.parts.label({
      dom: {
        tag: "label",
        classes: ["tox-label"]
      },
      components: [text$1(providerBackstage.translate(spec.label))]
    });
    const spectrum = Slider.parts.spectrum({
      dom: {
        tag: "div",
        classes: ["tox-slider__rail"],
        attributes: { role: "presentation" }
      }
    });
    const thumb = Slider.parts.thumb({
      dom: {
        tag: "div",
        classes: ["tox-slider__handle"],
        attributes: { role: "presentation" }
      }
    });
    return Slider.sketch({
      dom: {
        tag: "div",
        classes: ["tox-slider"],
        attributes: { role: "presentation" }
      },
      model: {
        mode: "x",
        minX: spec.min,
        maxX: spec.max,
        getInitialValue: constant$1(initialData.getOrThunk(() => (Math.abs(spec.max) - Math.abs(spec.min)) / 2))
      },
      components: [
        labelPart2,
        spectrum,
        thumb
      ],
      sliderBehaviours: derive$1([
        ComposingConfigs.self(),
        Focusing.config({})
      ]),
      onChoose: (component, thumb2, value2) => {
        emitWith(component, formChangeEvent, {
          name: spec.name,
          value: value2
        });
      }
    });
  };
  const renderTable = (spec, providersBackstage) => {
    const renderTh = (text2) => ({
      dom: {
        tag: "th",
        innerHtml: providersBackstage.translate(text2)
      }
    });
    const renderHeader2 = (header) => ({
      dom: { tag: "thead" },
      components: [{
        dom: { tag: "tr" },
        components: map$2(header, renderTh)
      }]
    });
    const renderTd = (text2) => ({
      dom: {
        tag: "td",
        innerHtml: providersBackstage.translate(text2)
      }
    });
    const renderTr = (row) => ({
      dom: { tag: "tr" },
      components: map$2(row, renderTd)
    });
    const renderRows = (rows) => ({
      dom: { tag: "tbody" },
      components: map$2(rows, renderTr)
    });
    return {
      dom: {
        tag: "table",
        classes: ["tox-dialog__table"]
      },
      components: [
        renderHeader2(spec.header),
        renderRows(spec.cells)
      ],
      behaviours: derive$1([
        Tabstopping.config({}),
        Focusing.config({})
      ])
    };
  };
  const renderTextField = (spec, providersBackstage) => {
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const baseInputBehaviours = [
      Disabling.config({ disabled: () => spec.disabled || providersBackstage.isDisabled() }),
      receivingConfig(),
      Keying.config({
        mode: "execution",
        useEnter: spec.multiline !== true,
        useControlEnter: spec.multiline === true,
        execute: (comp) => {
          emit(comp, formSubmitEvent);
          return Optional.some(true);
        }
      }),
      config("textfield-change", [
        run$1(input(), (component, _) => {
          emitWith(component, formChangeEvent, { name: spec.name });
        }),
        run$1(postPaste(), (component, _) => {
          emitWith(component, formChangeEvent, { name: spec.name });
        })
      ]),
      Tabstopping.config({})
    ];
    const validatingBehaviours = spec.validation.map((vl) => Invalidating.config({
      getRoot: (input2) => {
        return parentElement(input2.element);
      },
      invalidClass: "tox-invalid",
      validator: {
        validate: (input2) => {
          const v = Representing.getValue(input2);
          const result = vl.validator(v);
          return Future.pure(result === true ? Result.value(v) : Result.error(result));
        },
        validateOnLoad: vl.validateOnLoad
      }
    })).toArray();
    const placeholder2 = spec.placeholder.fold(constant$1({}), (p) => ({ placeholder: providersBackstage.translate(p) }));
    const inputMode = spec.inputMode.fold(constant$1({}), (mode) => ({ inputmode: mode }));
    const inputAttributes = {
      ...placeholder2,
      ...inputMode
    };
    const pField = FormField.parts.field({
      tag: spec.multiline === true ? "textarea" : "input",
      ...spec.data.map((data) => ({ data })).getOr({}),
      inputAttributes,
      inputClasses: [spec.classname],
      inputBehaviours: derive$1(flatten([
        baseInputBehaviours,
        validatingBehaviours
      ])),
      selectOnFocus: false,
      factory: Input
    });
    const extraClasses = spec.flex ? ["tox-form__group--stretched"] : [];
    const extraClasses2 = extraClasses.concat(spec.maximized ? ["tox-form-group--maximize"] : []);
    const extraBehaviours = [
      Disabling.config({
        disabled: () => spec.disabled || providersBackstage.isDisabled(),
        onDisabled: (comp) => {
          FormField.getField(comp).each(Disabling.disable);
        },
        onEnabled: (comp) => {
          FormField.getField(comp).each(Disabling.enable);
        }
      }),
      receivingConfig()
    ];
    return renderFormFieldWith(pLabel, pField, extraClasses2, extraBehaviours);
  };
  const renderInput = (spec, providersBackstage, initialData) => renderTextField({
    name: spec.name,
    multiline: false,
    label: spec.label,
    inputMode: spec.inputMode,
    placeholder: spec.placeholder,
    flex: false,
    disabled: !spec.enabled,
    classname: "tox-textfield",
    validation: Optional.none(),
    maximized: spec.maximized,
    data: initialData
  }, providersBackstage);
  const renderTextarea = (spec, providersBackstage, initialData) => renderTextField({
    name: spec.name,
    multiline: true,
    label: spec.label,
    inputMode: Optional.none(),
    placeholder: spec.placeholder,
    flex: true,
    disabled: !spec.enabled,
    classname: "tox-textarea",
    validation: Optional.none(),
    maximized: spec.maximized,
    data: initialData
  }, providersBackstage);
  const events$6 = (streamConfig, streamState) => {
    const streams = streamConfig.stream.streams;
    const processor = streams.setup(streamConfig, streamState);
    return derive$2([
      run$1(streamConfig.event, processor),
      runOnDetached(() => streamState.cancel())
    ].concat(streamConfig.cancelEvent.map((e) => [run$1(e, () => streamState.cancel())]).getOr([])));
  };
  var ActiveStreaming = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$6
  });
  const first = (fn, rate) => {
    let timer = null;
    const cancel = () => {
      if (!isNull(timer)) {
        clearTimeout(timer);
        timer = null;
      }
    };
    const throttle2 = (...args) => {
      if (isNull(timer)) {
        timer = setTimeout(() => {
          timer = null;
          fn.apply(null, args);
        }, rate);
      }
    };
    return {
      cancel,
      throttle: throttle2
    };
  };
  const last = (fn, rate) => {
    let timer = null;
    const cancel = () => {
      if (!isNull(timer)) {
        clearTimeout(timer);
        timer = null;
      }
    };
    const throttle2 = (...args) => {
      cancel();
      timer = setTimeout(() => {
        timer = null;
        fn.apply(null, args);
      }, rate);
    };
    return {
      cancel,
      throttle: throttle2
    };
  };
  const throttle = (_config) => {
    const state = Cell(null);
    const readState = () => ({ timer: state.get() !== null ? "set" : "unset" });
    const setTimer = (t2) => {
      state.set(t2);
    };
    const cancel = () => {
      const t2 = state.get();
      if (t2 !== null) {
        t2.cancel();
      }
    };
    return nu$8({
      readState,
      setTimer,
      cancel
    });
  };
  const init$9 = (spec) => spec.stream.streams.state(spec);
  var StreamingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    throttle,
    init: init$9
  });
  const setup$c = (streamInfo, streamState) => {
    const sInfo = streamInfo.stream;
    const throttler = last(streamInfo.onStream, sInfo.delay);
    streamState.setTimer(throttler);
    return (component, simulatedEvent) => {
      throttler.throttle(component, simulatedEvent);
      if (sInfo.stopEvent) {
        simulatedEvent.stop();
      }
    };
  };
  var StreamingSchema = [
    requiredOf("stream", choose$1("mode", {
      throttle: [
        required$1("delay"),
        defaulted("stopEvent", true),
        output$1("streams", {
          setup: setup$c,
          state: throttle
        })
      ]
    })),
    defaulted("event", "input"),
    option$3("cancelEvent"),
    onStrictHandler("onStream")
  ];
  const Streaming = create$3({
    fields: StreamingSchema,
    name: "streaming",
    active: ActiveStreaming,
    state: StreamingState
  });
  const setValueFromItem = (model, input2, item2) => {
    const itemData = Representing.getValue(item2);
    Representing.setValue(input2, itemData);
    setCursorAtEnd(input2);
  };
  const setSelectionOn = (input2, f) => {
    const el = input2.element;
    const value2 = get$6(el);
    const node = el.dom;
    if (get$f(el, "type") !== "number") {
      f(node, value2);
    }
  };
  const setCursorAtEnd = (input2) => {
    setSelectionOn(input2, (node, value2) => node.setSelectionRange(value2.length, value2.length));
  };
  const setSelectionToEnd = (input2, startOffset) => {
    setSelectionOn(input2, (node, value2) => node.setSelectionRange(startOffset, value2.length));
  };
  const attemptSelectOver = (model, input2, item2) => {
    if (!model.selectsOver) {
      return Optional.none();
    } else {
      const currentValue2 = Representing.getValue(input2);
      const inputDisplay = model.getDisplayText(currentValue2);
      const itemValue = Representing.getValue(item2);
      const itemDisplay = model.getDisplayText(itemValue);
      return itemDisplay.indexOf(inputDisplay) === 0 ? Optional.some(() => {
        setValueFromItem(model, input2, item2);
        setSelectionToEnd(input2, inputDisplay.length);
      }) : Optional.none();
    }
  };
  const itemExecute = constant$1("alloy.typeahead.itemexecute");
  const make$3 = (detail, components2, spec, externals) => {
    const navigateList = (comp, simulatedEvent, highlighter) => {
      detail.previewing.set(false);
      const sandbox = Coupling.getCoupled(comp, "sandbox");
      if (Sandboxing.isOpen(sandbox)) {
        Composing.getCurrent(sandbox).each((menu2) => {
          Highlighting.getHighlighted(menu2).fold(() => {
            highlighter(menu2);
          }, () => {
            dispatchEvent(sandbox, menu2.element, "keydown", simulatedEvent);
          });
        });
      } else {
        const onOpenSync = (sandbox2) => {
          Composing.getCurrent(sandbox2).each(highlighter);
        };
        open(detail, mapFetch(comp), comp, sandbox, externals, onOpenSync, HighlightOnOpen.HighlightMenuAndItem).get(noop);
      }
    };
    const focusBehaviours$1 = focusBehaviours(detail);
    const mapFetch = (comp) => (tdata) => tdata.map((data) => {
      const menus = values(data.menus);
      const items = bind$3(menus, (menu2) => filter$2(menu2.items, (item2) => item2.type === "item"));
      const repState = Representing.getState(comp);
      repState.update(map$2(items, (item2) => item2.data));
      return data;
    });
    const getActiveMenu = (sandboxComp) => Composing.getCurrent(sandboxComp);
    const typeaheadCustomEvents = "typeaheadevents";
    const behaviours2 = [
      Focusing.config({}),
      Representing.config({
        onSetValue: detail.onSetValue,
        store: {
          mode: "dataset",
          getDataKey: (comp) => get$6(comp.element),
          getFallbackEntry: (itemString) => ({
            value: itemString,
            meta: {}
          }),
          setValue: (comp, data) => {
            set$5(comp.element, detail.model.getDisplayText(data));
          },
          ...detail.initialData.map((d) => wrap$1("initialValue", d)).getOr({})
        }
      }),
      Streaming.config({
        stream: {
          mode: "throttle",
          delay: detail.responseTime,
          stopEvent: false
        },
        onStream: (component, _simulatedEvent) => {
          const sandbox = Coupling.getCoupled(component, "sandbox");
          const focusInInput = Focusing.isFocused(component);
          if (focusInInput) {
            if (get$6(component.element).length >= detail.minChars) {
              const previousValue = getActiveMenu(sandbox).bind((activeMenu) => Highlighting.getHighlighted(activeMenu).map(Representing.getValue));
              detail.previewing.set(true);
              const onOpenSync = (_sandbox) => {
                getActiveMenu(sandbox).each((activeMenu) => {
                  previousValue.fold(() => {
                    if (detail.model.selectsOver) {
                      Highlighting.highlightFirst(activeMenu);
                    }
                  }, (pv) => {
                    Highlighting.highlightBy(activeMenu, (item2) => {
                      const itemData = Representing.getValue(item2);
                      return itemData.value === pv.value;
                    });
                    Highlighting.getHighlighted(activeMenu).orThunk(() => {
                      Highlighting.highlightFirst(activeMenu);
                      return Optional.none();
                    });
                  });
                });
              };
              open(detail, mapFetch(component), component, sandbox, externals, onOpenSync, HighlightOnOpen.HighlightJustMenu).get(noop);
            }
          }
        },
        cancelEvent: typeaheadCancel()
      }),
      Keying.config({
        mode: "special",
        onDown: (comp, simulatedEvent) => {
          navigateList(comp, simulatedEvent, Highlighting.highlightFirst);
          return Optional.some(true);
        },
        onEscape: (comp) => {
          const sandbox = Coupling.getCoupled(comp, "sandbox");
          if (Sandboxing.isOpen(sandbox)) {
            Sandboxing.close(sandbox);
            return Optional.some(true);
          }
          return Optional.none();
        },
        onUp: (comp, simulatedEvent) => {
          navigateList(comp, simulatedEvent, Highlighting.highlightLast);
          return Optional.some(true);
        },
        onEnter: (comp) => {
          const sandbox = Coupling.getCoupled(comp, "sandbox");
          const sandboxIsOpen = Sandboxing.isOpen(sandbox);
          if (sandboxIsOpen && !detail.previewing.get()) {
            return getActiveMenu(sandbox).bind((activeMenu) => Highlighting.getHighlighted(activeMenu)).map((item2) => {
              emitWith(comp, itemExecute(), { item: item2 });
              return true;
            });
          } else {
            const currentValue2 = Representing.getValue(comp);
            emit(comp, typeaheadCancel());
            detail.onExecute(sandbox, comp, currentValue2);
            if (sandboxIsOpen) {
              Sandboxing.close(sandbox);
            }
            return Optional.some(true);
          }
        }
      }),
      Toggling.config({
        toggleClass: detail.markers.openClass,
        aria: { mode: "expanded" }
      }),
      Coupling.config({
        others: {
          sandbox: (hotspot) => {
            return makeSandbox$1(detail, hotspot, {
              onOpen: () => Toggling.on(hotspot),
              onClose: () => Toggling.off(hotspot)
            });
          }
        }
      }),
      config(typeaheadCustomEvents, [
        runOnAttached((typeaheadComp) => {
          detail.lazyTypeaheadComp.set(Optional.some(typeaheadComp));
        }),
        runOnDetached((_typeaheadComp) => {
          detail.lazyTypeaheadComp.set(Optional.none());
        }),
        runOnExecute$1((comp) => {
          const onOpenSync = noop;
          togglePopup(detail, mapFetch(comp), comp, externals, onOpenSync, HighlightOnOpen.HighlightMenuAndItem).get(noop);
        }),
        run$1(itemExecute(), (comp, se) => {
          const sandbox = Coupling.getCoupled(comp, "sandbox");
          setValueFromItem(detail.model, comp, se.event.item);
          emit(comp, typeaheadCancel());
          detail.onItemExecute(comp, sandbox, se.event.item, Representing.getValue(comp));
          Sandboxing.close(sandbox);
          setCursorAtEnd(comp);
        })
      ].concat(detail.dismissOnBlur ? [run$1(postBlur(), (typeahead) => {
        const sandbox = Coupling.getCoupled(typeahead, "sandbox");
        if (search(sandbox.element).isNone()) {
          Sandboxing.close(sandbox);
        }
      })] : []))
    ];
    const eventOrder = {
      [detachedFromDom()]: [
        Representing.name(),
        Streaming.name(),
        typeaheadCustomEvents
      ],
      ...detail.eventOrder
    };
    return {
      uid: detail.uid,
      dom: dom(deepMerge(detail, {
        inputAttributes: {
          "role": "combobox",
          "aria-autocomplete": "list",
          "aria-haspopup": "true"
        }
      })),
      behaviours: {
        ...focusBehaviours$1,
        ...augment(detail.typeaheadBehaviours, behaviours2)
      },
      eventOrder
    };
  };
  const schema$g = constant$1([
    option$3("lazySink"),
    required$1("fetch"),
    defaulted("minChars", 5),
    defaulted("responseTime", 1e3),
    onHandler("onOpen"),
    defaulted("getHotspot", Optional.some),
    defaulted("getAnchorOverrides", constant$1({})),
    defaulted("layouts", Optional.none()),
    defaulted("eventOrder", {}),
    defaultedObjOf("model", {}, [
      defaulted("getDisplayText", (itemData) => itemData.meta !== void 0 && itemData.meta.text !== void 0 ? itemData.meta.text : itemData.value),
      defaulted("selectsOver", true),
      defaulted("populateFromBrowse", true)
    ]),
    onHandler("onSetValue"),
    onKeyboardHandler("onExecute"),
    onHandler("onItemExecute"),
    defaulted("inputClasses", []),
    defaulted("inputAttributes", {}),
    defaulted("inputStyles", {}),
    defaulted("matchWidth", true),
    defaulted("useMinWidth", false),
    defaulted("dismissOnBlur", true),
    markers$1(["openClass"]),
    option$3("initialData"),
    field("typeaheadBehaviours", [
      Focusing,
      Representing,
      Streaming,
      Keying,
      Toggling,
      Coupling
    ]),
    customField("lazyTypeaheadComp", () => Cell(Optional.none)),
    customField("previewing", () => Cell(true))
  ].concat(schema$l()).concat(sandboxFields()));
  const parts$b = constant$1([external({
    schema: [tieredMenuMarkers()],
    name: "menu",
    overrides: (detail) => {
      return {
        fakeFocus: true,
        onHighlightItem: (_tmenu, menu2, item2) => {
          if (!detail.previewing.get()) {
            detail.lazyTypeaheadComp.get().each((input2) => {
              if (detail.model.populateFromBrowse) {
                setValueFromItem(detail.model, input2, item2);
              }
            });
          } else {
            detail.lazyTypeaheadComp.get().each((input2) => {
              attemptSelectOver(detail.model, input2, item2).fold(() => {
                if (detail.model.selectsOver) {
                  Highlighting.dehighlight(menu2, item2);
                  detail.previewing.set(true);
                } else {
                  detail.previewing.set(false);
                }
              }, (selectOverTextInInput) => {
                selectOverTextInInput();
                detail.previewing.set(false);
              });
            });
          }
        },
        onExecute: (_menu, item2) => {
          return detail.lazyTypeaheadComp.get().map((typeahead) => {
            emitWith(typeahead, itemExecute(), { item: item2 });
            return true;
          });
        },
        onHover: (menu2, item2) => {
          detail.previewing.set(false);
          detail.lazyTypeaheadComp.get().each((input2) => {
            if (detail.model.populateFromBrowse) {
              setValueFromItem(detail.model, input2, item2);
            }
          });
        }
      };
    }
  })]);
  const Typeahead = composite({
    name: "Typeahead",
    configFields: schema$g(),
    partFields: parts$b(),
    factory: make$3
  });
  const wrap = (delegate) => {
    const toCached = () => {
      return wrap(delegate.toCached());
    };
    const bindFuture = (f) => {
      return wrap(delegate.bind((resA) => resA.fold((err) => Future.pure(Result.error(err)), (a) => f(a))));
    };
    const bindResult = (f) => {
      return wrap(delegate.map((resA) => resA.bind(f)));
    };
    const mapResult = (f) => {
      return wrap(delegate.map((resA) => resA.map(f)));
    };
    const mapError2 = (f) => {
      return wrap(delegate.map((resA) => resA.mapError(f)));
    };
    const foldResult = (whenError, whenValue) => {
      return delegate.map((res) => res.fold(whenError, whenValue));
    };
    const withTimeout = (timeout, errorThunk) => {
      return wrap(Future.nu((callback) => {
        let timedOut = false;
        const timer = setTimeout(() => {
          timedOut = true;
          callback(Result.error(errorThunk()));
        }, timeout);
        delegate.get((result) => {
          if (!timedOut) {
            clearTimeout(timer);
            callback(result);
          }
        });
      }));
    };
    return {
      ...delegate,
      toCached,
      bindFuture,
      bindResult,
      mapResult,
      mapError: mapError2,
      foldResult,
      withTimeout
    };
  };
  const nu$1 = (worker) => {
    return wrap(Future.nu(worker));
  };
  const value = (value2) => {
    return wrap(Future.pure(Result.value(value2)));
  };
  const error = (error2) => {
    return wrap(Future.pure(Result.error(error2)));
  };
  const fromResult = (result) => {
    return wrap(Future.pure(result));
  };
  const fromFuture = (future) => {
    return wrap(future.map(Result.value));
  };
  const fromPromise = (promise) => {
    return nu$1((completer) => {
      promise.then((value2) => {
        completer(Result.value(value2));
      }, (error2) => {
        completer(Result.error(error2));
      });
    });
  };
  const FutureResult = {
    nu: nu$1,
    wrap,
    pure: value,
    value,
    error,
    fromResult,
    fromFuture,
    fromPromise
  };
  const getMenuButtonApi = (component) => ({
    isEnabled: () => !Disabling.isDisabled(component),
    setEnabled: (state) => Disabling.set(component, !state),
    setActive: (state) => {
      const elm = component.element;
      if (state) {
        add$2(elm, "tox-tbtn--enabled");
        set$9(elm, "aria-pressed", true);
      } else {
        remove$2(elm, "tox-tbtn--enabled");
        remove$7(elm, "aria-pressed");
      }
    },
    isActive: () => has(component.element, "tox-tbtn--enabled")
  });
  const renderMenuButton = (spec, prefix2, backstage, role) => {
    return renderCommonDropdown({
      text: spec.text,
      icon: spec.icon,
      tooltip: spec.tooltip,
      searchable: spec.search.isSome(),
      role,
      fetch: (dropdownComp, callback) => {
        const fetchContext = { pattern: spec.search.isSome() ? getSearchPattern(dropdownComp) : "" };
        spec.fetch((items) => {
          callback(build(items, ItemResponse$1.CLOSE_ON_EXECUTE, backstage, {
            isHorizontalMenu: false,
            search: spec.search
          }));
        }, fetchContext);
      },
      onSetup: spec.onSetup,
      getApi: getMenuButtonApi,
      columns: 1,
      presets: "normal",
      classes: [],
      dropdownBehaviours: [Tabstopping.config({})]
    }, prefix2, backstage.shared);
  };
  const getFetch = (items, getButton, backstage) => {
    const getMenuItemAction = (item2) => (api2) => {
      const newValue = !api2.isActive();
      api2.setActive(newValue);
      item2.storage.set(newValue);
      backstage.shared.getSink().each((sink) => {
        getButton().getOpt(sink).each((orig) => {
          focus$3(orig.element);
          emitWith(orig, formActionEvent, {
            name: item2.name,
            value: item2.storage.get()
          });
        });
      });
    };
    const getMenuItemSetup = (item2) => (api2) => {
      api2.setActive(item2.storage.get());
    };
    return (success) => {
      success(map$2(items, (item2) => {
        const text2 = item2.text.fold(() => ({}), (text3) => ({ text: text3 }));
        return {
          type: item2.type,
          active: false,
          ...text2,
          onAction: getMenuItemAction(item2),
          onSetup: getMenuItemSetup(item2)
        };
      }));
    };
  };
  const renderCommonSpec = (spec, actionOpt, extraBehaviours = [], dom2, components2, providersBackstage) => {
    const action = actionOpt.fold(() => ({}), (action2) => ({ action: action2 }));
    const common = {
      buttonBehaviours: derive$1([
        DisablingConfigs.button(() => !spec.enabled || providersBackstage.isDisabled()),
        receivingConfig(),
        Tabstopping.config({}),
        config("button press", [
          preventDefault("click"),
          preventDefault("mousedown")
        ])
      ].concat(extraBehaviours)),
      eventOrder: {
        click: [
          "button press",
          "alloy.base.behaviour"
        ],
        mousedown: [
          "button press",
          "alloy.base.behaviour"
        ]
      },
      ...action
    };
    const domFinal = deepMerge(common, { dom: dom2 });
    return deepMerge(domFinal, { components: components2 });
  };
  const renderIconButtonSpec = (spec, action, providersBackstage, extraBehaviours = []) => {
    const tooltipAttributes = spec.tooltip.map((tooltip) => ({
      "aria-label": providersBackstage.translate(tooltip),
      "title": providersBackstage.translate(tooltip)
    })).getOr({});
    const dom2 = {
      tag: "button",
      classes: ["tox-tbtn"],
      attributes: tooltipAttributes
    };
    const icon2 = spec.icon.map((iconName) => renderIconFromPack(iconName, providersBackstage.icons));
    const components2 = componentRenderPipeline([icon2]);
    return renderCommonSpec(spec, action, extraBehaviours, dom2, components2, providersBackstage);
  };
  const calculateClassesFromButtonType = (buttonType) => {
    switch (buttonType) {
      case "primary":
        return ["tox-button"];
      case "toolbar":
        return ["tox-tbtn"];
      case "secondary":
      default:
        return [
          "tox-button",
          "tox-button--secondary"
        ];
    }
  };
  const renderButtonSpec = (spec, action, providersBackstage, extraBehaviours = [], extraClasses = []) => {
    const translatedText = providersBackstage.translate(spec.text);
    const icon2 = spec.icon.map((iconName) => renderIconFromPack(iconName, providersBackstage.icons));
    const components2 = [icon2.getOrThunk(() => text$1(translatedText))];
    const buttonType = spec.buttonType.getOr(!spec.primary && !spec.borderless ? "secondary" : "primary");
    const baseClasses = calculateClassesFromButtonType(buttonType);
    const classes2 = [
      ...baseClasses,
      ...icon2.isSome() ? ["tox-button--icon"] : [],
      ...spec.borderless ? ["tox-button--naked"] : [],
      ...extraClasses
    ];
    const dom2 = {
      tag: "button",
      classes: classes2,
      attributes: { title: translatedText }
    };
    return renderCommonSpec(spec, action, extraBehaviours, dom2, components2, providersBackstage);
  };
  const renderButton = (spec, action, providersBackstage, extraBehaviours = [], extraClasses = []) => {
    const buttonSpec = renderButtonSpec(spec, Optional.some(action), providersBackstage, extraBehaviours, extraClasses);
    return Button.sketch(buttonSpec);
  };
  const getAction = (name2, buttonType) => (comp) => {
    if (buttonType === "custom") {
      emitWith(comp, formActionEvent, {
        name: name2,
        value: {}
      });
    } else if (buttonType === "submit") {
      emit(comp, formSubmitEvent);
    } else if (buttonType === "cancel") {
      emit(comp, formCancelEvent);
    } else {
      console.error("Unknown button type: ", buttonType);
    }
  };
  const isMenuFooterButtonSpec = (spec, buttonType) => buttonType === "menu";
  const isNormalFooterButtonSpec = (spec, buttonType) => buttonType === "custom" || buttonType === "cancel" || buttonType === "submit";
  const renderFooterButton = (spec, buttonType, backstage) => {
    if (isMenuFooterButtonSpec(spec, buttonType)) {
      const getButton = () => memButton;
      const menuButtonSpec = spec;
      const fixedSpec = {
        ...spec,
        type: "menubutton",
        search: Optional.none(),
        onSetup: (api2) => {
          api2.setEnabled(spec.enabled);
          return noop;
        },
        fetch: getFetch(menuButtonSpec.items, getButton, backstage)
      };
      const memButton = record(renderMenuButton(fixedSpec, "tox-tbtn", backstage, Optional.none()));
      return memButton.asSpec();
    } else if (isNormalFooterButtonSpec(spec, buttonType)) {
      const action = getAction(spec.name, buttonType);
      const buttonSpec = {
        ...spec,
        borderless: false
      };
      return renderButton(buttonSpec, action, backstage.shared.providers, []);
    } else {
      console.error("Unknown footer button type: ", buttonType);
      throw new Error("Unknown footer button type");
    }
  };
  const renderDialogButton = (spec, providersBackstage) => {
    const action = getAction(spec.name, "custom");
    return renderFormField(Optional.none(), FormField.parts.field({
      factory: Button,
      ...renderButtonSpec(spec, Optional.some(action), providersBackstage, [
        RepresentingConfigs.memory(""),
        ComposingConfigs.self()
      ])
    }));
  };
  const separator$1 = { type: "separator" };
  const toMenuItem = (target) => ({
    type: "menuitem",
    value: target.url,
    text: target.title,
    meta: { attach: target.attach },
    onAction: noop
  });
  const staticMenuItem = (title2, url) => ({
    type: "menuitem",
    value: url,
    text: title2,
    meta: { attach: void 0 },
    onAction: noop
  });
  const toMenuItems = (targets) => map$2(targets, toMenuItem);
  const filterLinkTargets = (type2, targets) => filter$2(targets, (target) => target.type === type2);
  const filteredTargets = (type2, targets) => toMenuItems(filterLinkTargets(type2, targets));
  const headerTargets = (linkInfo) => filteredTargets("header", linkInfo.targets);
  const anchorTargets = (linkInfo) => filteredTargets("anchor", linkInfo.targets);
  const anchorTargetTop = (linkInfo) => Optional.from(linkInfo.anchorTop).map((url) => staticMenuItem("<top>", url)).toArray();
  const anchorTargetBottom = (linkInfo) => Optional.from(linkInfo.anchorBottom).map((url) => staticMenuItem("<bottom>", url)).toArray();
  const historyTargets = (history) => map$2(history, (url) => staticMenuItem(url, url));
  const joinMenuLists = (items) => {
    return foldl(items, (a, b2) => {
      const bothEmpty = a.length === 0 || b2.length === 0;
      return bothEmpty ? a.concat(b2) : a.concat(separator$1, b2);
    }, []);
  };
  const filterByQuery = (term, menuItems) => {
    const lowerCaseTerm = term.toLowerCase();
    return filter$2(menuItems, (item2) => {
      var _a;
      const text2 = item2.meta !== void 0 && item2.meta.text !== void 0 ? item2.meta.text : item2.text;
      const value2 = (_a = item2.value) !== null && _a !== void 0 ? _a : "";
      return contains$1(text2.toLowerCase(), lowerCaseTerm) || contains$1(value2.toLowerCase(), lowerCaseTerm);
    });
  };
  const getItems = (fileType, input2, urlBackstage) => {
    const urlInputValue = Representing.getValue(input2);
    const term = urlInputValue.meta.text !== void 0 ? urlInputValue.meta.text : urlInputValue.value;
    const info = urlBackstage.getLinkInformation();
    return info.fold(() => [], (linkInfo) => {
      const history = filterByQuery(term, historyTargets(urlBackstage.getHistory(fileType)));
      return fileType === "file" ? joinMenuLists([
        history,
        filterByQuery(term, headerTargets(linkInfo)),
        filterByQuery(term, flatten([
          anchorTargetTop(linkInfo),
          anchorTargets(linkInfo),
          anchorTargetBottom(linkInfo)
        ]))
      ]) : history;
    });
  };
  const errorId = generate$6("aria-invalid");
  const renderUrlInput = (spec, backstage, urlBackstage, initialData) => {
    const providersBackstage = backstage.shared.providers;
    const updateHistory = (component) => {
      const urlEntry = Representing.getValue(component);
      urlBackstage.addToHistory(urlEntry.value, spec.filetype);
    };
    const typeaheadSpec = {
      ...initialData.map((initialData2) => ({ initialData: initialData2 })).getOr({}),
      dismissOnBlur: true,
      inputClasses: ["tox-textfield"],
      sandboxClasses: ["tox-dialog__popups"],
      inputAttributes: {
        "aria-errormessage": errorId,
        "type": "url"
      },
      minChars: 0,
      responseTime: 0,
      fetch: (input2) => {
        const items = getItems(spec.filetype, input2, urlBackstage);
        const tdata = build(items, ItemResponse$1.BUBBLE_TO_SANDBOX, backstage, {
          isHorizontalMenu: false,
          search: Optional.none()
        });
        return Future.pure(tdata);
      },
      getHotspot: (comp) => memUrlBox.getOpt(comp),
      onSetValue: (comp, _newValue) => {
        if (comp.hasConfigured(Invalidating)) {
          Invalidating.run(comp).get(noop);
        }
      },
      typeaheadBehaviours: derive$1([
        ...urlBackstage.getValidationHandler().map((handler) => Invalidating.config({
          getRoot: (comp) => parentElement(comp.element),
          invalidClass: "tox-control-wrap--status-invalid",
          notify: {
            onInvalid: (comp, err) => {
              memInvalidIcon.getOpt(comp).each((invalidComp) => {
                set$9(invalidComp.element, "title", providersBackstage.translate(err));
              });
            }
          },
          validator: {
            validate: (input2) => {
              const urlEntry = Representing.getValue(input2);
              return FutureResult.nu((completer) => {
                handler({
                  type: spec.filetype,
                  url: urlEntry.value
                }, (validation) => {
                  if (validation.status === "invalid") {
                    const err = Result.error(validation.message);
                    completer(err);
                  } else {
                    const val = Result.value(validation.message);
                    completer(val);
                  }
                });
              });
            },
            validateOnLoad: false
          }
        })).toArray(),
        Disabling.config({ disabled: () => !spec.enabled || providersBackstage.isDisabled() }),
        Tabstopping.config({}),
        config("urlinput-events", [
          run$1(input(), (comp) => {
            const currentValue2 = get$6(comp.element);
            const trimmedValue = currentValue2.trim();
            if (trimmedValue !== currentValue2) {
              set$5(comp.element, trimmedValue);
            }
            if (spec.filetype === "file") {
              emitWith(comp, formChangeEvent, { name: spec.name });
            }
          }),
          run$1(change(), (comp) => {
            emitWith(comp, formChangeEvent, { name: spec.name });
            updateHistory(comp);
          }),
          run$1(postPaste(), (comp) => {
            emitWith(comp, formChangeEvent, { name: spec.name });
            updateHistory(comp);
          })
        ])
      ]),
      eventOrder: {
        [input()]: [
          "streaming",
          "urlinput-events",
          "invalidating"
        ]
      },
      model: {
        getDisplayText: (itemData) => itemData.value,
        selectsOver: false,
        populateFromBrowse: false
      },
      markers: { openClass: "tox-textfield--popup-open" },
      lazySink: backstage.shared.getSink,
      parts: { menu: part(false, 1, "normal") },
      onExecute: (_menu, component, _entry) => {
        emitWith(component, formSubmitEvent, {});
      },
      onItemExecute: (typeahead, _sandbox, _item, _value) => {
        updateHistory(typeahead);
        emitWith(typeahead, formChangeEvent, { name: spec.name });
      }
    };
    const pField = FormField.parts.field({
      ...typeaheadSpec,
      factory: Typeahead
    });
    const pLabel = spec.label.map((label2) => renderLabel$2(label2, providersBackstage));
    const makeIcon = (name2, errId, icon2 = name2, label2 = name2) => render$3(icon2, {
      tag: "div",
      classes: [
        "tox-icon",
        "tox-control-wrap__status-icon-" + name2
      ],
      attributes: {
        "title": providersBackstage.translate(label2),
        "aria-live": "polite",
        ...errId.fold(() => ({}), (id) => ({ id }))
      }
    }, providersBackstage.icons);
    const memInvalidIcon = record(makeIcon("invalid", Optional.some(errorId), "warning"));
    const memStatus = record({
      dom: {
        tag: "div",
        classes: ["tox-control-wrap__status-icon-wrap"]
      },
      components: [memInvalidIcon.asSpec()]
    });
    const optUrlPicker = urlBackstage.getUrlPicker(spec.filetype);
    const browseUrlEvent = generate$6("browser.url.event");
    const memUrlBox = record({
      dom: {
        tag: "div",
        classes: ["tox-control-wrap"]
      },
      components: [
        pField,
        memStatus.asSpec()
      ],
      behaviours: derive$1([Disabling.config({ disabled: () => !spec.enabled || providersBackstage.isDisabled() })])
    });
    const memUrlPickerButton = record(renderButton({
      name: spec.name,
      icon: Optional.some("browse"),
      text: spec.label.getOr(""),
      enabled: spec.enabled,
      primary: false,
      buttonType: Optional.none(),
      borderless: true
    }, (component) => emit(component, browseUrlEvent), providersBackstage, [], ["tox-browse-url"]));
    const controlHWrapper = () => ({
      dom: {
        tag: "div",
        classes: ["tox-form__controls-h-stack"]
      },
      components: flatten([
        [memUrlBox.asSpec()],
        optUrlPicker.map(() => memUrlPickerButton.asSpec()).toArray()
      ])
    });
    const openUrlPicker = (comp) => {
      Composing.getCurrent(comp).each((field2) => {
        const componentData = Representing.getValue(field2);
        const urlData = {
          fieldname: spec.name,
          ...componentData
        };
        optUrlPicker.each((picker) => {
          picker(urlData).get((chosenData) => {
            Representing.setValue(field2, chosenData);
            emitWith(comp, formChangeEvent, { name: spec.name });
          });
        });
      });
    };
    return FormField.sketch({
      dom: renderFormFieldDom(),
      components: pLabel.toArray().concat([controlHWrapper()]),
      fieldBehaviours: derive$1([
        Disabling.config({
          disabled: () => !spec.enabled || providersBackstage.isDisabled(),
          onDisabled: (comp) => {
            FormField.getField(comp).each(Disabling.disable);
            memUrlPickerButton.getOpt(comp).each(Disabling.disable);
          },
          onEnabled: (comp) => {
            FormField.getField(comp).each(Disabling.enable);
            memUrlPickerButton.getOpt(comp).each(Disabling.enable);
          }
        }),
        receivingConfig(),
        config("url-input-events", [run$1(browseUrlEvent, openUrlPicker)])
      ])
    });
  };
  const renderAlertBanner = (spec, providersBackstage) => Container.sketch({
    dom: {
      tag: "div",
      attributes: { role: "alert" },
      classes: [
        "tox-notification",
        "tox-notification--in",
        `tox-notification--${spec.level}`
      ]
    },
    components: [
      {
        dom: {
          tag: "div",
          classes: ["tox-notification__icon"]
        },
        components: [Button.sketch({
          dom: {
            tag: "button",
            classes: [
              "tox-button",
              "tox-button--naked",
              "tox-button--icon"
            ],
            innerHtml: get$2(spec.icon, providersBackstage.icons),
            attributes: { title: providersBackstage.translate(spec.iconTooltip) }
          },
          action: (comp) => {
            emitWith(comp, formActionEvent, {
              name: "alert-banner",
              value: spec.url
            });
          },
          buttonBehaviours: derive$1([addFocusableBehaviour()])
        })]
      },
      {
        dom: {
          tag: "div",
          classes: ["tox-notification__body"],
          innerHtml: providersBackstage.translate(spec.text)
        }
      }
    ]
  });
  const set$1 = (element2, status) => {
    element2.dom.checked = status;
  };
  const get$1 = (element2) => element2.dom.checked;
  const renderCheckbox = (spec, providerBackstage, initialData) => {
    const toggleCheckboxHandler = (comp) => {
      comp.element.dom.click();
      return Optional.some(true);
    };
    const pField = FormField.parts.field({
      factory: { sketch: identity },
      dom: {
        tag: "input",
        classes: ["tox-checkbox__input"],
        attributes: { type: "checkbox" }
      },
      behaviours: derive$1([
        ComposingConfigs.self(),
        Disabling.config({ disabled: () => !spec.enabled || providerBackstage.isDisabled() }),
        Tabstopping.config({}),
        Focusing.config({}),
        RepresentingConfigs.withElement(initialData, get$1, set$1),
        Keying.config({
          mode: "special",
          onEnter: toggleCheckboxHandler,
          onSpace: toggleCheckboxHandler,
          stopSpaceKeyup: true
        }),
        config("checkbox-events", [run$1(change(), (component, _) => {
          emitWith(component, formChangeEvent, { name: spec.name });
        })])
      ])
    });
    const pLabel = FormField.parts.label({
      dom: {
        tag: "span",
        classes: ["tox-checkbox__label"]
      },
      components: [text$1(providerBackstage.translate(spec.label))],
      behaviours: derive$1([Unselecting.config({})])
    });
    const makeIcon = (className) => {
      const iconName = className === "checked" ? "selected" : "unselected";
      return render$3(iconName, {
        tag: "span",
        classes: [
          "tox-icon",
          "tox-checkbox-icon__" + className
        ]
      }, providerBackstage.icons);
    };
    const memIcons = record({
      dom: {
        tag: "div",
        classes: ["tox-checkbox__icons"]
      },
      components: [
        makeIcon("checked"),
        makeIcon("unchecked")
      ]
    });
    return FormField.sketch({
      dom: {
        tag: "label",
        classes: ["tox-checkbox"]
      },
      components: [
        pField,
        memIcons.asSpec(),
        pLabel
      ],
      fieldBehaviours: derive$1([
        Disabling.config({
          disabled: () => !spec.enabled || providerBackstage.isDisabled(),
          disableClass: "tox-checkbox--disabled",
          onDisabled: (comp) => {
            FormField.getField(comp).each(Disabling.disable);
          },
          onEnabled: (comp) => {
            FormField.getField(comp).each(Disabling.enable);
          }
        }),
        receivingConfig()
      ])
    });
  };
  const renderHtmlPanel = (spec) => {
    if (spec.presets === "presentation") {
      return Container.sketch({
        dom: {
          tag: "div",
          classes: ["tox-form__group"],
          innerHtml: spec.html
        }
      });
    } else {
      return Container.sketch({
        dom: {
          tag: "div",
          classes: ["tox-form__group"],
          innerHtml: spec.html,
          attributes: { role: "document" }
        },
        containerBehaviours: derive$1([
          Tabstopping.config({}),
          Focusing.config({})
        ])
      });
    }
  };
  const make$2 = (render2) => {
    return (parts2, spec, dialogData, backstage) => get$g(spec, "name").fold(() => render2(spec, backstage, Optional.none()), (fieldName) => parts2.field(fieldName, render2(spec, backstage, get$g(dialogData, fieldName))));
  };
  const makeIframe = (render2) => (parts2, spec, dialogData, backstage) => {
    const iframeSpec = deepMerge(spec, { source: "dynamic" });
    return make$2(render2)(parts2, iframeSpec, dialogData, backstage);
  };
  const factories = {
    bar: make$2((spec, backstage) => renderBar(spec, backstage.shared)),
    collection: make$2((spec, backstage, data) => renderCollection(spec, backstage.shared.providers, data)),
    alertbanner: make$2((spec, backstage) => renderAlertBanner(spec, backstage.shared.providers)),
    input: make$2((spec, backstage, data) => renderInput(spec, backstage.shared.providers, data)),
    textarea: make$2((spec, backstage, data) => renderTextarea(spec, backstage.shared.providers, data)),
    label: make$2((spec, backstage) => renderLabel$1(spec, backstage.shared)),
    iframe: makeIframe((spec, backstage, data) => renderIFrame(spec, backstage.shared.providers, data)),
    button: make$2((spec, backstage) => renderDialogButton(spec, backstage.shared.providers)),
    checkbox: make$2((spec, backstage, data) => renderCheckbox(spec, backstage.shared.providers, data)),
    colorinput: make$2((spec, backstage, data) => renderColorInput(spec, backstage.shared, backstage.colorinput, data)),
    colorpicker: make$2((spec, backstage, data) => renderColorPicker(spec, backstage.shared.providers, data)),
    dropzone: make$2((spec, backstage, data) => renderDropZone(spec, backstage.shared.providers, data)),
    grid: make$2((spec, backstage) => renderGrid(spec, backstage.shared)),
    listbox: make$2((spec, backstage, data) => renderListBox(spec, backstage, data)),
    selectbox: make$2((spec, backstage, data) => renderSelectBox(spec, backstage.shared.providers, data)),
    sizeinput: make$2((spec, backstage) => renderSizeInput(spec, backstage.shared.providers)),
    slider: make$2((spec, backstage, data) => renderSlider(spec, backstage.shared.providers, data)),
    urlinput: make$2((spec, backstage, data) => renderUrlInput(spec, backstage, backstage.urlinput, data)),
    customeditor: make$2(renderCustomEditor),
    htmlpanel: make$2(renderHtmlPanel),
    imagepreview: make$2((spec, _, data) => renderImagePreview(spec, data)),
    table: make$2((spec, backstage) => renderTable(spec, backstage.shared.providers)),
    panel: make$2((spec, backstage) => renderPanel(spec, backstage))
  };
  const noFormParts = {
    field: (_name, spec) => spec,
    record: constant$1([])
  };
  const interpretInForm = (parts2, spec, dialogData, oldBackstage) => {
    const newBackstage = deepMerge(oldBackstage, { shared: { interpreter: (childSpec) => interpretParts(parts2, childSpec, dialogData, newBackstage) } });
    return interpretParts(parts2, spec, dialogData, newBackstage);
  };
  const interpretParts = (parts2, spec, dialogData, backstage) => get$g(factories, spec.type).fold(() => {
    console.error(`Unknown factory type "${spec.type}", defaulting to container: `, spec);
    return spec;
  }, (factory2) => factory2(parts2, spec, dialogData, backstage));
  const interpretWithoutForm = (spec, dialogData, backstage) => interpretParts(noFormParts, spec, dialogData, backstage);
  const labelPrefix = "layout-inset";
  const westEdgeX = (anchor2) => anchor2.x;
  const middleX = (anchor2, element2) => anchor2.x + anchor2.width / 2 - element2.width / 2;
  const eastEdgeX = (anchor2, element2) => anchor2.x + anchor2.width - element2.width;
  const northY = (anchor2) => anchor2.y;
  const southY = (anchor2, element2) => anchor2.y + anchor2.height - element2.height;
  const centreY = (anchor2, element2) => anchor2.y + anchor2.height / 2 - element2.height / 2;
  const southwest = (anchor2, element2, bubbles) => nu$6(eastEdgeX(anchor2, element2), southY(anchor2, element2), bubbles.insetSouthwest(), northwest$3(), "southwest", boundsRestriction(anchor2, {
    right: 0,
    bottom: 3
  }), labelPrefix);
  const southeast = (anchor2, element2, bubbles) => nu$6(westEdgeX(anchor2), southY(anchor2, element2), bubbles.insetSoutheast(), northeast$3(), "southeast", boundsRestriction(anchor2, {
    left: 1,
    bottom: 3
  }), labelPrefix);
  const northwest = (anchor2, element2, bubbles) => nu$6(eastEdgeX(anchor2, element2), northY(anchor2), bubbles.insetNorthwest(), southwest$3(), "northwest", boundsRestriction(anchor2, {
    right: 0,
    top: 2
  }), labelPrefix);
  const northeast = (anchor2, element2, bubbles) => nu$6(westEdgeX(anchor2), northY(anchor2), bubbles.insetNortheast(), southeast$3(), "northeast", boundsRestriction(anchor2, {
    left: 1,
    top: 2
  }), labelPrefix);
  const north = (anchor2, element2, bubbles) => nu$6(middleX(anchor2, element2), northY(anchor2), bubbles.insetNorth(), south$3(), "north", boundsRestriction(anchor2, { top: 2 }), labelPrefix);
  const south = (anchor2, element2, bubbles) => nu$6(middleX(anchor2, element2), southY(anchor2, element2), bubbles.insetSouth(), north$3(), "south", boundsRestriction(anchor2, { bottom: 3 }), labelPrefix);
  const east = (anchor2, element2, bubbles) => nu$6(eastEdgeX(anchor2, element2), centreY(anchor2, element2), bubbles.insetEast(), west$3(), "east", boundsRestriction(anchor2, { right: 0 }), labelPrefix);
  const west = (anchor2, element2, bubbles) => nu$6(westEdgeX(anchor2), centreY(anchor2, element2), bubbles.insetWest(), east$3(), "west", boundsRestriction(anchor2, { left: 1 }), labelPrefix);
  const lookupPreserveLayout = (lastPlacement) => {
    switch (lastPlacement) {
      case "north":
        return north;
      case "northeast":
        return northeast;
      case "northwest":
        return northwest;
      case "south":
        return south;
      case "southeast":
        return southeast;
      case "southwest":
        return southwest;
      case "east":
        return east;
      case "west":
        return west;
    }
  };
  const preserve = (anchor2, element2, bubbles, placee, bounds2) => {
    const layout2 = getPlacement(placee).map(lookupPreserveLayout).getOr(north);
    return layout2(anchor2, element2, bubbles, placee, bounds2);
  };
  const lookupFlippedLayout = (lastPlacement) => {
    switch (lastPlacement) {
      case "north":
        return south;
      case "northeast":
        return southeast;
      case "northwest":
        return southwest;
      case "south":
        return north;
      case "southeast":
        return northeast;
      case "southwest":
        return northwest;
      case "east":
        return west;
      case "west":
        return east;
    }
  };
  const flip = (anchor2, element2, bubbles, placee, bounds2) => {
    const layout2 = getPlacement(placee).map(lookupFlippedLayout).getOr(north);
    return layout2(anchor2, element2, bubbles, placee, bounds2);
  };
  const bubbleAlignments$2 = {
    valignCentre: [],
    alignCentre: [],
    alignLeft: [],
    alignRight: [],
    right: [],
    left: [],
    bottom: [],
    top: []
  };
  const getInlineDialogAnchor = (contentAreaElement, lazyAnchorbar, lazyUseEditableAreaAnchor) => {
    const bubbleSize2 = 12;
    const overrides = { maxHeightFunction: expandable$1() };
    const editableAreaAnchor = () => ({
      type: "node",
      root: getContentContainer(getRootNode(contentAreaElement())),
      node: Optional.from(contentAreaElement()),
      bubble: nu$5(bubbleSize2, bubbleSize2, bubbleAlignments$2),
      layouts: {
        onRtl: () => [northeast],
        onLtr: () => [northwest]
      },
      overrides
    });
    const standardAnchor = () => ({
      type: "hotspot",
      hotspot: lazyAnchorbar(),
      bubble: nu$5(-bubbleSize2, bubbleSize2, bubbleAlignments$2),
      layouts: {
        onRtl: () => [southeast$2],
        onLtr: () => [southwest$2]
      },
      overrides
    });
    return () => lazyUseEditableAreaAnchor() ? editableAreaAnchor() : standardAnchor();
  };
  const getBannerAnchor = (contentAreaElement, lazyAnchorbar, lazyUseEditableAreaAnchor) => {
    const editableAreaAnchor = () => ({
      type: "node",
      root: getContentContainer(getRootNode(contentAreaElement())),
      node: Optional.from(contentAreaElement()),
      layouts: {
        onRtl: () => [north],
        onLtr: () => [north]
      }
    });
    const standardAnchor = () => ({
      type: "hotspot",
      hotspot: lazyAnchorbar(),
      layouts: {
        onRtl: () => [south$2],
        onLtr: () => [south$2]
      }
    });
    return () => lazyUseEditableAreaAnchor() ? editableAreaAnchor() : standardAnchor();
  };
  const getCursorAnchor = (editor, bodyElement) => () => ({
    type: "selection",
    root: bodyElement(),
    getSelection: () => {
      const rng = editor.selection.getRng();
      return Optional.some(SimSelection.range(SugarElement.fromDom(rng.startContainer), rng.startOffset, SugarElement.fromDom(rng.endContainer), rng.endOffset));
    }
  });
  const getNodeAnchor$1 = (bodyElement) => (element2) => ({
    type: "node",
    root: bodyElement(),
    node: element2
  });
  const getAnchors = (editor, lazyAnchorbar, isToolbarTop) => {
    const useFixedToolbarContainer = useFixedContainer(editor);
    const bodyElement = () => SugarElement.fromDom(editor.getBody());
    const contentAreaElement = () => SugarElement.fromDom(editor.getContentAreaContainer());
    const lazyUseEditableAreaAnchor = () => useFixedToolbarContainer || !isToolbarTop();
    return {
      inlineDialog: getInlineDialogAnchor(contentAreaElement, lazyAnchorbar, lazyUseEditableAreaAnchor),
      banner: getBannerAnchor(contentAreaElement, lazyAnchorbar, lazyUseEditableAreaAnchor),
      cursor: getCursorAnchor(editor, bodyElement),
      node: getNodeAnchor$1(bodyElement)
    };
  };
  const colorPicker = (editor) => (callback, value2) => {
    const dialog = colorPickerDialog(editor);
    dialog(callback, value2);
  };
  const hasCustomColors = (editor) => () => hasCustomColors$1(editor);
  const getColors = (editor) => () => getColors$2(editor);
  const getColorCols = (editor) => () => getColorCols$1(editor);
  const ColorInputBackstage = (editor) => ({
    colorPicker: colorPicker(editor),
    hasCustomColors: hasCustomColors(editor),
    getColors: getColors(editor),
    getColorCols: getColorCols(editor)
  });
  const isDraggableModal = (editor) => () => isDraggableModal$1(editor);
  const DialogBackstage = (editor) => ({ isDraggableModal: isDraggableModal(editor) });
  const HeaderBackstage = (editor) => {
    const mode = Cell(isToolbarLocationBottom(editor) ? "bottom" : "top");
    return {
      isPositionedAtTop: () => mode.get() === "top",
      getDockingMode: mode.get,
      setDockingMode: mode.set
    };
  };
  const isNestedFormat = (format) => hasNonNullableKey(format, "items");
  const isFormatReference = (format) => hasNonNullableKey(format, "format");
  const defaultStyleFormats = [
    {
      title: "Headings",
      items: [
        {
          title: "Heading 1",
          format: "h1"
        },
        {
          title: "Heading 2",
          format: "h2"
        },
        {
          title: "Heading 3",
          format: "h3"
        },
        {
          title: "Heading 4",
          format: "h4"
        },
        {
          title: "Heading 5",
          format: "h5"
        },
        {
          title: "Heading 6",
          format: "h6"
        }
      ]
    },
    {
      title: "Inline",
      items: [
        {
          title: "Bold",
          format: "bold"
        },
        {
          title: "Italic",
          format: "italic"
        },
        {
          title: "Underline",
          format: "underline"
        },
        {
          title: "Strikethrough",
          format: "strikethrough"
        },
        {
          title: "Superscript",
          format: "superscript"
        },
        {
          title: "Subscript",
          format: "subscript"
        },
        {
          title: "Code",
          format: "code"
        }
      ]
    },
    {
      title: "Blocks",
      items: [
        {
          title: "Paragraph",
          format: "p"
        },
        {
          title: "Blockquote",
          format: "blockquote"
        },
        {
          title: "Div",
          format: "div"
        },
        {
          title: "Pre",
          format: "pre"
        }
      ]
    },
    {
      title: "Align",
      items: [
        {
          title: "Left",
          format: "alignleft"
        },
        {
          title: "Center",
          format: "aligncenter"
        },
        {
          title: "Right",
          format: "alignright"
        },
        {
          title: "Justify",
          format: "alignjustify"
        }
      ]
    }
  ];
  const isNestedFormats = (format) => has$2(format, "items");
  const isBlockFormat = (format) => has$2(format, "block");
  const isInlineFormat = (format) => has$2(format, "inline");
  const isSelectorFormat = (format) => has$2(format, "selector");
  const mapFormats = (userFormats) => foldl(userFormats, (acc, fmt) => {
    if (isNestedFormats(fmt)) {
      const result = mapFormats(fmt.items);
      return {
        customFormats: acc.customFormats.concat(result.customFormats),
        formats: acc.formats.concat([{
          title: fmt.title,
          items: result.formats
        }])
      };
    } else if (isInlineFormat(fmt) || isBlockFormat(fmt) || isSelectorFormat(fmt)) {
      const formatName = isString(fmt.name) ? fmt.name : fmt.title.toLowerCase();
      const formatNameWithPrefix = `custom-${formatName}`;
      return {
        customFormats: acc.customFormats.concat([{
          name: formatNameWithPrefix,
          format: fmt
        }]),
        formats: acc.formats.concat([{
          title: fmt.title,
          format: formatNameWithPrefix,
          icon: fmt.icon
        }])
      };
    } else {
      return {
        ...acc,
        formats: acc.formats.concat(fmt)
      };
    }
  }, {
    customFormats: [],
    formats: []
  });
  const registerCustomFormats = (editor, userFormats) => {
    const result = mapFormats(userFormats);
    const registerFormats = (customFormats) => {
      each$1(customFormats, (fmt) => {
        if (!editor.formatter.has(fmt.name)) {
          editor.formatter.register(fmt.name, fmt.format);
        }
      });
    };
    if (editor.formatter) {
      registerFormats(result.customFormats);
    } else {
      editor.on("init", () => {
        registerFormats(result.customFormats);
      });
    }
    return result.formats;
  };
  const getStyleFormats = (editor) => getUserStyleFormats(editor).map((userFormats) => {
    const registeredUserFormats = registerCustomFormats(editor, userFormats);
    return shouldMergeStyleFormats(editor) ? defaultStyleFormats.concat(registeredUserFormats) : registeredUserFormats;
  }).getOr(defaultStyleFormats);
  const isSeparator$1 = (format) => {
    const keys$1 = keys(format);
    return keys$1.length === 1 && contains$2(keys$1, "title");
  };
  const processBasic = (item2, isSelectedFor, getPreviewFor) => ({
    ...item2,
    type: "formatter",
    isSelected: isSelectedFor(item2.format),
    getStylePreview: getPreviewFor(item2.format)
  });
  const register$a = (editor, formats, isSelectedFor, getPreviewFor) => {
    const enrichSupported = (item2) => processBasic(item2, isSelectedFor, getPreviewFor);
    const enrichMenu = (item2) => {
      const newItems = doEnrich(item2.items);
      return {
        ...item2,
        type: "submenu",
        getStyleItems: constant$1(newItems)
      };
    };
    const enrichCustom = (item2) => {
      const formatName = isString(item2.name) ? item2.name : generate$6(item2.title);
      const formatNameWithPrefix = `custom-${formatName}`;
      const newItem = {
        ...item2,
        type: "formatter",
        format: formatNameWithPrefix,
        isSelected: isSelectedFor(formatNameWithPrefix),
        getStylePreview: getPreviewFor(formatNameWithPrefix)
      };
      editor.formatter.register(formatName, newItem);
      return newItem;
    };
    const doEnrich = (items) => map$2(items, (item2) => {
      if (isNestedFormat(item2)) {
        return enrichMenu(item2);
      } else if (isFormatReference(item2)) {
        return enrichSupported(item2);
      } else if (isSeparator$1(item2)) {
        return {
          ...item2,
          type: "separator"
        };
      } else {
        return enrichCustom(item2);
      }
    });
    return doEnrich(formats);
  };
  const init$8 = (editor) => {
    const isSelectedFor = (format) => () => editor.formatter.match(format);
    const getPreviewFor = (format) => () => {
      const fmt = editor.formatter.get(format);
      return fmt !== void 0 ? Optional.some({
        tag: fmt.length > 0 ? fmt[0].inline || fmt[0].block || "div" : "div",
        styles: editor.dom.parseStyle(editor.formatter.getCssText(format))
      }) : Optional.none();
    };
    const settingsFormats = Cell([]);
    const eventsFormats = Cell([]);
    const replaceSettings = Cell(false);
    editor.on("PreInit", (_e) => {
      const formats = getStyleFormats(editor);
      const enriched = register$a(editor, formats, isSelectedFor, getPreviewFor);
      settingsFormats.set(enriched);
    });
    editor.on("addStyleModifications", (e) => {
      const modifications = register$a(editor, e.items, isSelectedFor, getPreviewFor);
      eventsFormats.set(modifications);
      replaceSettings.set(e.replace);
    });
    const getData2 = () => {
      const fromSettings = replaceSettings.get() ? [] : settingsFormats.get();
      const fromEvents = eventsFormats.get();
      return fromSettings.concat(fromEvents);
    };
    return { getData: getData2 };
  };
  const isElement = (node) => isNonNullable(node) && node.nodeType === 1;
  const trim = global$1.trim;
  const hasContentEditableState = (value2) => {
    return (node) => {
      if (isElement(node)) {
        if (node.contentEditable === value2) {
          return true;
        }
        if (node.getAttribute("data-mce-contenteditable") === value2) {
          return true;
        }
      }
      return false;
    };
  };
  const isContentEditableTrue = hasContentEditableState("true");
  const isContentEditableFalse = hasContentEditableState("false");
  const create = (type2, title2, url, level, attach2) => ({
    type: type2,
    title: title2,
    url,
    level,
    attach: attach2
  });
  const isChildOfContentEditableTrue = (node) => {
    let tempNode = node;
    while (tempNode = tempNode.parentNode) {
      const value2 = tempNode.contentEditable;
      if (value2 && value2 !== "inherit") {
        return isContentEditableTrue(tempNode);
      }
    }
    return false;
  };
  const select = (selector, root) => {
    return map$2(descendants(SugarElement.fromDom(root), selector), (element2) => {
      return element2.dom;
    });
  };
  const getElementText = (elm) => {
    return elm.innerText || elm.textContent;
  };
  const getOrGenerateId = (elm) => {
    return elm.id ? elm.id : generate$6("h");
  };
  const isAnchor = (elm) => {
    return elm && elm.nodeName === "A" && (elm.id || elm.name) !== void 0;
  };
  const isValidAnchor = (elm) => {
    return isAnchor(elm) && isEditable(elm);
  };
  const isHeader = (elm) => {
    return elm && /^(H[1-6])$/.test(elm.nodeName);
  };
  const isEditable = (elm) => {
    return isChildOfContentEditableTrue(elm) && !isContentEditableFalse(elm);
  };
  const isValidHeader = (elm) => {
    return isHeader(elm) && isEditable(elm);
  };
  const getLevel = (elm) => {
    return isHeader(elm) ? parseInt(elm.nodeName.substr(1), 10) : 0;
  };
  const headerTarget = (elm) => {
    var _a;
    const headerId = getOrGenerateId(elm);
    const attach2 = () => {
      elm.id = headerId;
    };
    return create("header", (_a = getElementText(elm)) !== null && _a !== void 0 ? _a : "", "#" + headerId, getLevel(elm), attach2);
  };
  const anchorTarget = (elm) => {
    const anchorId = elm.id || elm.name;
    const anchorText = getElementText(elm);
    return create("anchor", anchorText ? anchorText : "#" + anchorId, "#" + anchorId, 0, noop);
  };
  const getHeaderTargets = (elms) => {
    return map$2(filter$2(elms, isValidHeader), headerTarget);
  };
  const getAnchorTargets = (elms) => {
    return map$2(filter$2(elms, isValidAnchor), anchorTarget);
  };
  const getTargetElements = (elm) => {
    const elms = select("h1,h2,h3,h4,h5,h6,a:not([href])", elm);
    return elms;
  };
  const hasTitle = (target) => {
    return trim(target.title).length > 0;
  };
  const find = (elm) => {
    const elms = getTargetElements(elm);
    return filter$2(getHeaderTargets(elms).concat(getAnchorTargets(elms)), hasTitle);
  };
  const LinkTargets = { find };
  const STORAGE_KEY = "tinymce-url-history";
  const HISTORY_LENGTH = 5;
  const isHttpUrl = (url) => isString(url) && /^https?/.test(url);
  const isArrayOfUrl = (a) => isArray(a) && a.length <= HISTORY_LENGTH && forall(a, isHttpUrl);
  const isRecordOfUrlArray = (r2) => isObject(r2) && find$4(r2, (value2) => !isArrayOfUrl(value2)).isNone();
  const getAllHistory = () => {
    const unparsedHistory = global$4.getItem(STORAGE_KEY);
    if (unparsedHistory === null) {
      return {};
    }
    let history;
    try {
      history = JSON.parse(unparsedHistory);
    } catch (e) {
      if (e instanceof SyntaxError) {
        console.log("Local storage " + STORAGE_KEY + " was not valid JSON", e);
        return {};
      }
      throw e;
    }
    if (!isRecordOfUrlArray(history)) {
      console.log("Local storage " + STORAGE_KEY + " was not valid format", history);
      return {};
    }
    return history;
  };
  const setAllHistory = (history) => {
    if (!isRecordOfUrlArray(history)) {
      throw new Error("Bad format for history:\n" + JSON.stringify(history));
    }
    global$4.setItem(STORAGE_KEY, JSON.stringify(history));
  };
  const getHistory = (fileType) => {
    const history = getAllHistory();
    return get$g(history, fileType).getOr([]);
  };
  const addToHistory = (url, fileType) => {
    if (!isHttpUrl(url)) {
      return;
    }
    const history = getAllHistory();
    const items = get$g(history, fileType).getOr([]);
    const itemsWithoutUrl = filter$2(items, (item2) => item2 !== url);
    history[fileType] = [url].concat(itemsWithoutUrl).slice(0, HISTORY_LENGTH);
    setAllHistory(history);
  };
  const isTruthy = (value2) => !!value2;
  const makeMap = (value2) => map$1(global$1.makeMap(value2, /[, ]/), isTruthy);
  const getPicker = (editor) => Optional.from(getFilePickerCallback(editor));
  const getPickerTypes = (editor) => {
    const optFileTypes = Optional.from(getFilePickerTypes(editor)).filter(isTruthy).map(makeMap);
    return getPicker(editor).fold(never, (_picker) => optFileTypes.fold(always, (types2) => keys(types2).length > 0 ? types2 : false));
  };
  const getPickerSetting = (editor, filetype) => {
    const pickerTypes = getPickerTypes(editor);
    if (isBoolean(pickerTypes)) {
      return pickerTypes ? getPicker(editor) : Optional.none();
    } else {
      return pickerTypes[filetype] ? getPicker(editor) : Optional.none();
    }
  };
  const getUrlPicker = (editor, filetype) => getPickerSetting(editor, filetype).map((picker) => (entry) => Future.nu((completer) => {
    const handler = (value2, meta2) => {
      if (!isString(value2)) {
        throw new Error("Expected value to be string");
      }
      if (meta2 !== void 0 && !isObject(meta2)) {
        throw new Error("Expected meta to be a object");
      }
      const r2 = {
        value: value2,
        meta: meta2
      };
      completer(r2);
    };
    const meta = {
      filetype,
      fieldname: entry.fieldname,
      ...Optional.from(entry.meta).getOr({})
    };
    picker.call(editor, handler, entry.value, meta);
  }));
  const getTextSetting = (value2) => Optional.from(value2).filter(isString).getOrUndefined();
  const getLinkInformation = (editor) => {
    if (!useTypeaheadUrls(editor)) {
      return Optional.none();
    }
    return Optional.some({
      targets: LinkTargets.find(editor.getBody()),
      anchorTop: getTextSetting(getAnchorTop(editor)),
      anchorBottom: getTextSetting(getAnchorBottom(editor))
    });
  };
  const getValidationHandler = (editor) => Optional.from(getFilePickerValidatorHandler(editor));
  const UrlInputBackstage = (editor) => ({
    getHistory,
    addToHistory,
    getLinkInformation: () => getLinkInformation(editor),
    getValidationHandler: () => getValidationHandler(editor),
    getUrlPicker: (filetype) => getUrlPicker(editor, filetype)
  });
  const init$7 = (lazySink, editor, lazyAnchorbar) => {
    const contextMenuState = Cell(false);
    const toolbar = HeaderBackstage(editor);
    const backstage = {
      shared: {
        providers: {
          icons: () => editor.ui.registry.getAll().icons,
          menuItems: () => editor.ui.registry.getAll().menuItems,
          translate: global$8.translate,
          isDisabled: () => editor.mode.isReadOnly() || !editor.ui.isEnabled(),
          getOption: editor.options.get
        },
        interpreter: (s) => interpretWithoutForm(s, {}, backstage),
        anchors: getAnchors(editor, lazyAnchorbar, toolbar.isPositionedAtTop),
        header: toolbar,
        getSink: lazySink
      },
      urlinput: UrlInputBackstage(editor),
      styles: init$8(editor),
      colorinput: ColorInputBackstage(editor),
      dialog: DialogBackstage(editor),
      isContextMenuOpen: () => contextMenuState.get(),
      setContextMenuState: (state) => contextMenuState.set(state)
    };
    return backstage;
  };
  const setup$b = (editor, mothership, uiMothership) => {
    const broadcastEvent = (name2, evt) => {
      each$1([
        mothership,
        uiMothership
      ], (ship) => {
        ship.broadcastEvent(name2, evt);
      });
    };
    const broadcastOn = (channel, message) => {
      each$1([
        mothership,
        uiMothership
      ], (ship) => {
        ship.broadcastOn([channel], message);
      });
    };
    const fireDismissPopups = (evt) => broadcastOn(dismissPopups(), { target: evt.target });
    const doc = getDocument();
    const onTouchstart = bind(doc, "touchstart", fireDismissPopups);
    const onTouchmove = bind(doc, "touchmove", (evt) => broadcastEvent(documentTouchmove(), evt));
    const onTouchend = bind(doc, "touchend", (evt) => broadcastEvent(documentTouchend(), evt));
    const onMousedown = bind(doc, "mousedown", fireDismissPopups);
    const onMouseup = bind(doc, "mouseup", (evt) => {
      if (evt.raw.button === 0) {
        broadcastOn(mouseReleased(), { target: evt.target });
      }
    });
    const onContentClick = (raw) => broadcastOn(dismissPopups(), { target: SugarElement.fromDom(raw.target) });
    const onContentMouseup = (raw) => {
      if (raw.button === 0) {
        broadcastOn(mouseReleased(), { target: SugarElement.fromDom(raw.target) });
      }
    };
    const onContentMousedown = () => {
      each$1(editor.editorManager.get(), (loopEditor) => {
        if (editor !== loopEditor) {
          loopEditor.dispatch("DismissPopups", { relatedTarget: editor });
        }
      });
    };
    const onWindowScroll = (evt) => broadcastEvent(windowScroll(), fromRawEvent(evt));
    const onWindowResize = (evt) => {
      broadcastOn(repositionPopups(), {});
      broadcastEvent(windowResize(), fromRawEvent(evt));
    };
    const onEditorResize = () => broadcastOn(repositionPopups(), {});
    const onEditorProgress = (evt) => {
      if (evt.state) {
        broadcastOn(dismissPopups(), { target: SugarElement.fromDom(editor.getContainer()) });
      }
    };
    const onDismissPopups = (event2) => {
      broadcastOn(dismissPopups(), { target: SugarElement.fromDom(event2.relatedTarget.getContainer()) });
    };
    editor.on("PostRender", () => {
      editor.on("click", onContentClick);
      editor.on("tap", onContentClick);
      editor.on("mouseup", onContentMouseup);
      editor.on("mousedown", onContentMousedown);
      editor.on("ScrollWindow", onWindowScroll);
      editor.on("ResizeWindow", onWindowResize);
      editor.on("ResizeEditor", onEditorResize);
      editor.on("AfterProgressState", onEditorProgress);
      editor.on("DismissPopups", onDismissPopups);
    });
    editor.on("remove", () => {
      editor.off("click", onContentClick);
      editor.off("tap", onContentClick);
      editor.off("mouseup", onContentMouseup);
      editor.off("mousedown", onContentMousedown);
      editor.off("ScrollWindow", onWindowScroll);
      editor.off("ResizeWindow", onWindowResize);
      editor.off("ResizeEditor", onEditorResize);
      editor.off("AfterProgressState", onEditorProgress);
      editor.off("DismissPopups", onDismissPopups);
      onMousedown.unbind();
      onTouchstart.unbind();
      onTouchmove.unbind();
      onTouchend.unbind();
      onMouseup.unbind();
    });
    editor.on("detach", () => {
      detachSystem(mothership);
      detachSystem(uiMothership);
      mothership.destroy();
      uiMothership.destroy();
    });
  };
  const parts$a = AlloyParts;
  const partType = PartType;
  const schema$f = constant$1([
    defaulted("shell", false),
    required$1("makeItem"),
    defaulted("setupItem", noop),
    SketchBehaviours.field("listBehaviours", [Replacing])
  ]);
  const customListDetail = () => ({ behaviours: derive$1([Replacing.config({})]) });
  const itemsPart = optional({
    name: "items",
    overrides: customListDetail
  });
  const parts$9 = constant$1([itemsPart]);
  const name = constant$1("CustomList");
  const factory$d = (detail, components2, _spec, _external) => {
    const setItems = (list, items) => {
      getListContainer(list).fold(() => {
        console.error("Custom List was defined to not be a shell, but no item container was specified in components");
        throw new Error("Custom List was defined to not be a shell, but no item container was specified in components");
      }, (container) => {
        const itemComps = Replacing.contents(container);
        const numListsRequired = items.length;
        const numListsToAdd = numListsRequired - itemComps.length;
        const itemsToAdd = numListsToAdd > 0 ? range$2(numListsToAdd, () => detail.makeItem()) : [];
        const itemsToRemove = itemComps.slice(numListsRequired);
        each$1(itemsToRemove, (item2) => Replacing.remove(container, item2));
        each$1(itemsToAdd, (item2) => Replacing.append(container, item2));
        const builtLists = Replacing.contents(container);
        each$1(builtLists, (item2, i2) => {
          detail.setupItem(list, item2, items[i2], i2);
        });
      });
    };
    const extra = detail.shell ? {
      behaviours: [Replacing.config({})],
      components: []
    } : {
      behaviours: [],
      components: components2
    };
    const getListContainer = (component) => detail.shell ? Optional.some(component) : getPart(component, detail, "items");
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: extra.components,
      behaviours: augment(detail.listBehaviours, extra.behaviours),
      apis: { setItems }
    };
  };
  const CustomList = composite({
    name: name(),
    configFields: schema$f(),
    partFields: parts$9(),
    factory: factory$d,
    apis: {
      setItems: (apis, list, items) => {
        apis.setItems(list, items);
      }
    }
  });
  const schema$e = constant$1([
    required$1("dom"),
    defaulted("shell", true),
    field("toolbarBehaviours", [Replacing])
  ]);
  const enhanceGroups = () => ({ behaviours: derive$1([Replacing.config({})]) });
  const parts$8 = constant$1([optional({
    name: "groups",
    overrides: enhanceGroups
  })]);
  const factory$c = (detail, components2, _spec, _externals) => {
    const setGroups2 = (toolbar, groups) => {
      getGroupContainer(toolbar).fold(() => {
        console.error("Toolbar was defined to not be a shell, but no groups container was specified in components");
        throw new Error("Toolbar was defined to not be a shell, but no groups container was specified in components");
      }, (container) => {
        Replacing.set(container, groups);
      });
    };
    const getGroupContainer = (component) => detail.shell ? Optional.some(component) : getPart(component, detail, "groups");
    const extra = detail.shell ? {
      behaviours: [Replacing.config({})],
      components: []
    } : {
      behaviours: [],
      components: components2
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: extra.components,
      behaviours: augment(detail.toolbarBehaviours, extra.behaviours),
      apis: { setGroups: setGroups2 },
      domModification: { attributes: { role: "group" } }
    };
  };
  const Toolbar = composite({
    name: "Toolbar",
    configFields: schema$e(),
    partFields: parts$8(),
    factory: factory$c,
    apis: {
      setGroups: (apis, toolbar, groups) => {
        apis.setGroups(toolbar, groups);
      }
    }
  });
  const setup$a = noop;
  const isDocked$2 = never;
  const getBehaviours$1 = constant$1([]);
  var StaticHeader = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    setup: setup$a,
    isDocked: isDocked$2,
    getBehaviours: getBehaviours$1
  });
  const getOffsetParent = (element2) => {
    const isFixed = is$1(getRaw(element2, "position"), "fixed");
    const offsetParent$1 = isFixed ? Optional.none() : offsetParent(element2);
    return offsetParent$1.orThunk(() => {
      const marker = SugarElement.fromTag("span");
      return parent(element2).bind((parent2) => {
        append$2(parent2, marker);
        const offsetParent$12 = offsetParent(marker);
        remove$5(marker);
        return offsetParent$12;
      });
    });
  };
  const getOrigin = (element2) => getOffsetParent(element2).map(absolute$3).getOrThunk(() => SugarPosition(0, 0));
  const morphAdt = Adt.generate([
    { static: [] },
    { absolute: ["positionCss"] },
    { fixed: ["positionCss"] }
  ]);
  const appear = (component, contextualInfo) => {
    const elem = component.element;
    add$2(elem, contextualInfo.transitionClass);
    remove$2(elem, contextualInfo.fadeOutClass);
    add$2(elem, contextualInfo.fadeInClass);
    contextualInfo.onShow(component);
  };
  const disappear = (component, contextualInfo) => {
    const elem = component.element;
    add$2(elem, contextualInfo.transitionClass);
    remove$2(elem, contextualInfo.fadeInClass);
    add$2(elem, contextualInfo.fadeOutClass);
    contextualInfo.onHide(component);
  };
  const isPartiallyVisible = (box2, viewport2) => box2.y < viewport2.bottom && box2.bottom > viewport2.y;
  const isTopCompletelyVisible = (box2, viewport2) => box2.y >= viewport2.y;
  const isBottomCompletelyVisible = (box2, viewport2) => box2.bottom <= viewport2.bottom;
  const isVisibleForModes = (modes, box2, viewport2) => forall(modes, (mode) => {
    switch (mode) {
      case "bottom":
        return isBottomCompletelyVisible(box2, viewport2);
      case "top":
        return isTopCompletelyVisible(box2, viewport2);
    }
  });
  const getPrior = (elem, state) => state.getInitialPos().map((pos) => bounds(pos.bounds.x, pos.bounds.y, get$c(elem), get$d(elem)));
  const storePrior = (elem, box2, state) => {
    state.setInitialPos({
      style: getAllRaw(elem),
      position: get$e(elem, "position") || "static",
      bounds: box2
    });
  };
  const revertToOriginal = (elem, box2, state) => state.getInitialPos().bind((position2) => {
    state.clearInitialPos();
    switch (position2.position) {
      case "static":
        return Optional.some(morphAdt.static());
      case "absolute":
        const offsetBox = getOffsetParent(elem).map(box$1).getOrThunk(() => box$1(body()));
        return Optional.some(morphAdt.absolute(NuPositionCss("absolute", get$g(position2.style, "left").map((_left) => box2.x - offsetBox.x), get$g(position2.style, "top").map((_top) => box2.y - offsetBox.y), get$g(position2.style, "right").map((_right) => offsetBox.right - box2.right), get$g(position2.style, "bottom").map((_bottom) => offsetBox.bottom - box2.bottom))));
      default:
        return Optional.none();
    }
  });
  const morphToOriginal = (elem, viewport2, state) => getPrior(elem, state).filter((box2) => isVisibleForModes(state.getModes(), box2, viewport2)).bind((box2) => revertToOriginal(elem, box2, state));
  const morphToFixed = (elem, viewport2, state) => {
    const box2 = box$1(elem);
    if (!isVisibleForModes(state.getModes(), box2, viewport2)) {
      storePrior(elem, box2, state);
      const winBox = win();
      const left2 = box2.x - winBox.x;
      const top2 = viewport2.y - winBox.y;
      const bottom2 = winBox.bottom - viewport2.bottom;
      const isTop = box2.y <= viewport2.y;
      return Optional.some(morphAdt.fixed(NuPositionCss("fixed", Optional.some(left2), isTop ? Optional.some(top2) : Optional.none(), Optional.none(), !isTop ? Optional.some(bottom2) : Optional.none())));
    } else {
      return Optional.none();
    }
  };
  const getMorph = (component, viewport2, state) => {
    const elem = component.element;
    const isDocked2 = is$1(getRaw(elem, "position"), "fixed");
    return isDocked2 ? morphToOriginal(elem, viewport2, state) : morphToFixed(elem, viewport2, state);
  };
  const getMorphToOriginal = (component, state) => {
    const elem = component.element;
    return getPrior(elem, state).bind((box2) => revertToOriginal(elem, box2, state));
  };
  const morphToStatic = (component, config2, state) => {
    state.setDocked(false);
    each$1([
      "left",
      "right",
      "top",
      "bottom",
      "position"
    ], (prop) => remove$6(component.element, prop));
    config2.onUndocked(component);
  };
  const morphToCoord = (component, config2, state, position2) => {
    const isDocked2 = position2.position === "fixed";
    state.setDocked(isDocked2);
    applyPositionCss(component.element, position2);
    const method = isDocked2 ? config2.onDocked : config2.onUndocked;
    method(component);
  };
  const updateVisibility = (component, config2, state, viewport2, morphToDocked = false) => {
    config2.contextual.each((contextInfo) => {
      contextInfo.lazyContext(component).each((box2) => {
        const isVisible2 = isPartiallyVisible(box2, viewport2);
        if (isVisible2 !== state.isVisible()) {
          state.setVisible(isVisible2);
          if (morphToDocked && !isVisible2) {
            add$1(component.element, [contextInfo.fadeOutClass]);
            contextInfo.onHide(component);
          } else {
            const method = isVisible2 ? appear : disappear;
            method(component, contextInfo);
          }
        }
      });
    });
  };
  const refreshInternal = (component, config2, state) => {
    const viewport2 = config2.lazyViewport(component);
    const isDocked2 = state.isDocked();
    if (isDocked2) {
      updateVisibility(component, config2, state, viewport2);
    }
    getMorph(component, viewport2, state).each((morph) => {
      morph.fold(() => morphToStatic(component, config2, state), (position2) => morphToCoord(component, config2, state, position2), (position2) => {
        updateVisibility(component, config2, state, viewport2, true);
        morphToCoord(component, config2, state, position2);
      });
    });
  };
  const resetInternal = (component, config2, state) => {
    const elem = component.element;
    state.setDocked(false);
    getMorphToOriginal(component, state).each((morph) => {
      morph.fold(() => morphToStatic(component, config2, state), (position2) => morphToCoord(component, config2, state, position2), noop);
    });
    state.setVisible(true);
    config2.contextual.each((contextInfo) => {
      remove$1(elem, [
        contextInfo.fadeInClass,
        contextInfo.fadeOutClass,
        contextInfo.transitionClass
      ]);
      contextInfo.onShow(component);
    });
    refresh$4(component, config2, state);
  };
  const refresh$4 = (component, config2, state) => {
    if (component.getSystem().isConnected()) {
      refreshInternal(component, config2, state);
    }
  };
  const reset = (component, config2, state) => {
    if (state.isDocked()) {
      resetInternal(component, config2, state);
    }
  };
  const isDocked$1 = (component, config2, state) => state.isDocked();
  const setModes = (component, config2, state, modes) => state.setModes(modes);
  const getModes = (component, config2, state) => state.getModes();
  var DockingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    refresh: refresh$4,
    reset,
    isDocked: isDocked$1,
    getModes,
    setModes
  });
  const events$5 = (dockInfo, dockState) => derive$2([
    runOnSource(transitionend(), (component, simulatedEvent) => {
      dockInfo.contextual.each((contextInfo) => {
        if (has(component.element, contextInfo.transitionClass)) {
          remove$1(component.element, [
            contextInfo.transitionClass,
            contextInfo.fadeInClass
          ]);
          const notify = dockState.isVisible() ? contextInfo.onShown : contextInfo.onHidden;
          notify(component);
        }
        simulatedEvent.stop();
      });
    }),
    run$1(windowScroll(), (component, _) => {
      refresh$4(component, dockInfo, dockState);
    }),
    run$1(windowResize(), (component, _) => {
      reset(component, dockInfo, dockState);
    })
  ]);
  var ActiveDocking = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$5
  });
  var DockingSchema = [
    optionObjOf("contextual", [
      requiredString("fadeInClass"),
      requiredString("fadeOutClass"),
      requiredString("transitionClass"),
      requiredFunction("lazyContext"),
      onHandler("onShow"),
      onHandler("onShown"),
      onHandler("onHide"),
      onHandler("onHidden")
    ]),
    defaultedFunction("lazyViewport", win),
    defaultedArrayOf("modes", [
      "top",
      "bottom"
    ], string),
    onHandler("onDocked"),
    onHandler("onUndocked")
  ];
  const init$6 = (spec) => {
    const docked = Cell(false);
    const visible = Cell(true);
    const initialBounds = value$2();
    const modes = Cell(spec.modes);
    const readState = () => `docked:  ${docked.get()}, visible: ${visible.get()}, modes: ${modes.get().join(",")}`;
    return nu$8({
      isDocked: docked.get,
      setDocked: docked.set,
      getInitialPos: initialBounds.get,
      setInitialPos: initialBounds.set,
      clearInitialPos: initialBounds.clear,
      isVisible: visible.get,
      setVisible: visible.set,
      getModes: modes.get,
      setModes: modes.set,
      readState
    });
  };
  var DockingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$6
  });
  const Docking = create$3({
    fields: DockingSchema,
    name: "docking",
    active: ActiveDocking,
    apis: DockingApis,
    state: DockingState
  });
  const toolbarHeightChange = constant$1(generate$6("toolbar-height-change"));
  const visibility = {
    fadeInClass: "tox-editor-dock-fadein",
    fadeOutClass: "tox-editor-dock-fadeout",
    transitionClass: "tox-editor-dock-transition"
  };
  const editorStickyOnClass = "tox-tinymce--toolbar-sticky-on";
  const editorStickyOffClass = "tox-tinymce--toolbar-sticky-off";
  const scrollFromBehindHeader = (e, containerHeader) => {
    const doc = owner$4(containerHeader);
    const win2 = defaultView(containerHeader);
    const viewHeight = win2.dom.innerHeight;
    const scrollPos = get$b(doc);
    const markerElement = SugarElement.fromDom(e.elm);
    const markerPos = absolute$2(markerElement);
    const markerHeight = get$d(markerElement);
    const markerTop = markerPos.y;
    const markerBottom = markerTop + markerHeight;
    const editorHeaderPos = absolute$3(containerHeader);
    const editorHeaderHeight = get$d(containerHeader);
    const editorHeaderTop = editorHeaderPos.top;
    const editorHeaderBottom = editorHeaderTop + editorHeaderHeight;
    const editorHeaderDockedAtTop = Math.abs(editorHeaderTop - scrollPos.top) < 2;
    const editorHeaderDockedAtBottom = Math.abs(editorHeaderBottom - (scrollPos.top + viewHeight)) < 2;
    if (editorHeaderDockedAtTop && markerTop < editorHeaderBottom) {
      to(scrollPos.left, markerTop - editorHeaderHeight, doc);
    } else if (editorHeaderDockedAtBottom && markerBottom > editorHeaderTop) {
      const y = markerTop - viewHeight + markerHeight + editorHeaderHeight;
      to(scrollPos.left, y, doc);
    }
  };
  const isDockedMode = (header, mode) => contains$2(Docking.getModes(header), mode);
  const updateIframeContentFlow = (header) => {
    const getOccupiedHeight = (elm2) => getOuter$2(elm2) + (parseInt(get$e(elm2, "margin-top"), 10) || 0) + (parseInt(get$e(elm2, "margin-bottom"), 10) || 0);
    const elm = header.element;
    parentElement(elm).each((parentElem) => {
      const padding = "padding-" + Docking.getModes(header)[0];
      if (Docking.isDocked(header)) {
        const parentWidth = get$c(parentElem);
        set$8(elm, "width", parentWidth + "px");
        set$8(parentElem, padding, getOccupiedHeight(elm) + "px");
      } else {
        remove$6(elm, "width");
        remove$6(parentElem, padding);
      }
    });
  };
  const updateSinkVisibility = (sinkElem, visible) => {
    if (visible) {
      remove$2(sinkElem, visibility.fadeOutClass);
      add$1(sinkElem, [
        visibility.transitionClass,
        visibility.fadeInClass
      ]);
    } else {
      remove$2(sinkElem, visibility.fadeInClass);
      add$1(sinkElem, [
        visibility.fadeOutClass,
        visibility.transitionClass
      ]);
    }
  };
  const updateEditorClasses = (editor, docked) => {
    const editorContainer = SugarElement.fromDom(editor.getContainer());
    if (docked) {
      add$2(editorContainer, editorStickyOnClass);
      remove$2(editorContainer, editorStickyOffClass);
    } else {
      add$2(editorContainer, editorStickyOffClass);
      remove$2(editorContainer, editorStickyOnClass);
    }
  };
  const restoreFocus = (headerElem, focusedElem) => {
    const ownerDoc = owner$4(focusedElem);
    active$1(ownerDoc).filter((activeElm) => !eq(focusedElem, activeElm)).filter((activeElm) => eq(activeElm, SugarElement.fromDom(ownerDoc.dom.body)) || contains(headerElem, activeElm)).each(() => focus$3(focusedElem));
  };
  const findFocusedElem = (rootElm, lazySink) => search(rootElm).orThunk(() => lazySink().toOptional().bind((sink) => search(sink.element)));
  const setup$9 = (editor, sharedBackstage, lazyHeader) => {
    if (!editor.inline) {
      if (!sharedBackstage.header.isPositionedAtTop()) {
        editor.on("ResizeEditor", () => {
          lazyHeader().each(Docking.reset);
        });
      }
      editor.on("ResizeWindow ResizeEditor", () => {
        lazyHeader().each(updateIframeContentFlow);
      });
      editor.on("SkinLoaded", () => {
        lazyHeader().each((comp) => {
          Docking.isDocked(comp) ? Docking.reset(comp) : Docking.refresh(comp);
        });
      });
      editor.on("FullscreenStateChanged", () => {
        lazyHeader().each(Docking.reset);
      });
    }
    editor.on("AfterScrollIntoView", (e) => {
      lazyHeader().each((header) => {
        Docking.refresh(header);
        const headerElem = header.element;
        if (isVisible(headerElem)) {
          scrollFromBehindHeader(e, headerElem);
        }
      });
    });
    editor.on("PostRender", () => {
      updateEditorClasses(editor, false);
    });
  };
  const isDocked = (lazyHeader) => lazyHeader().map(Docking.isDocked).getOr(false);
  const getIframeBehaviours = () => [Receiving.config({ channels: { [toolbarHeightChange()]: { onReceive: updateIframeContentFlow } } })];
  const getBehaviours = (editor, sharedBackstage) => {
    const focusedElm = value$2();
    const lazySink = sharedBackstage.getSink;
    const runOnSinkElement = (f) => {
      lazySink().each((sink) => f(sink.element));
    };
    const onDockingSwitch = (comp) => {
      if (!editor.inline) {
        updateIframeContentFlow(comp);
      }
      updateEditorClasses(editor, Docking.isDocked(comp));
      comp.getSystem().broadcastOn([repositionPopups()], {});
      lazySink().each((sink) => sink.getSystem().broadcastOn([repositionPopups()], {}));
    };
    const additionalBehaviours = editor.inline ? [] : getIframeBehaviours();
    return [
      Focusing.config({}),
      Docking.config({
        contextual: {
          lazyContext: (comp) => {
            const headerHeight = getOuter$2(comp.element);
            const container = editor.inline ? editor.getContentAreaContainer() : editor.getContainer();
            const box2 = box$1(SugarElement.fromDom(container));
            const boxHeight = box2.height - headerHeight;
            const topBound = box2.y + (isDockedMode(comp, "top") ? 0 : headerHeight);
            return Optional.some(bounds(box2.x, topBound, box2.width, boxHeight));
          },
          onShow: () => {
            runOnSinkElement((elem) => updateSinkVisibility(elem, true));
          },
          onShown: (comp) => {
            runOnSinkElement((elem) => remove$1(elem, [
              visibility.transitionClass,
              visibility.fadeInClass
            ]));
            focusedElm.get().each((elem) => {
              restoreFocus(comp.element, elem);
              focusedElm.clear();
            });
          },
          onHide: (comp) => {
            findFocusedElem(comp.element, lazySink).fold(focusedElm.clear, focusedElm.set);
            runOnSinkElement((elem) => updateSinkVisibility(elem, false));
          },
          onHidden: () => {
            runOnSinkElement((elem) => remove$1(elem, [visibility.transitionClass]));
          },
          ...visibility
        },
        lazyViewport: (comp) => {
          const win$1 = win();
          const offset2 = getStickyToolbarOffset(editor);
          const top2 = win$1.y + (isDockedMode(comp, "top") ? offset2 : 0);
          const height2 = win$1.height - (isDockedMode(comp, "bottom") ? offset2 : 0);
          return bounds(win$1.x, top2, win$1.width, height2);
        },
        modes: [sharedBackstage.header.getDockingMode()],
        onDocked: onDockingSwitch,
        onUndocked: onDockingSwitch
      }),
      ...additionalBehaviours
    ];
  };
  var StickyHeader = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    setup: setup$9,
    isDocked,
    getBehaviours
  });
  const renderHeader = (spec) => {
    const editor = spec.editor;
    const getBehaviours$22 = spec.sticky ? getBehaviours : getBehaviours$1;
    return {
      uid: spec.uid,
      dom: spec.dom,
      components: spec.components,
      behaviours: derive$1(getBehaviours$22(editor, spec.sharedBackstage))
    };
  };
  const groupToolbarButtonSchema = objOf([
    type,
    requiredOf("items", oneOf([
      arrOfObj([
        name$1,
        requiredArrayOf("items", string)
      ]),
      string
    ]))
  ].concat(baseToolbarButtonFields));
  const createGroupToolbarButton = (spec) => asRaw("GroupToolbarButton", groupToolbarButtonSchema, spec);
  const baseMenuButtonFields = [
    optionString("text"),
    optionString("tooltip"),
    optionString("icon"),
    defaultedOf("search", false, oneOf([
      boolean,
      objOf([optionString("placeholder")])
    ], (x) => {
      if (isBoolean(x)) {
        return x ? Optional.some({ placeholder: Optional.none() }) : Optional.none();
      } else {
        return Optional.some(x);
      }
    })),
    requiredFunction("fetch"),
    defaultedFunction("onSetup", () => noop)
  ];
  const MenuButtonSchema = objOf([
    type,
    ...baseMenuButtonFields
  ]);
  const createMenuButton = (spec) => asRaw("menubutton", MenuButtonSchema, spec);
  const splitButtonSchema = objOf([
    type,
    optionalTooltip,
    optionalIcon,
    optionalText,
    optionalSelect,
    fetch$1,
    onSetup,
    defaultedStringEnum("presets", "normal", [
      "normal",
      "color",
      "listpreview"
    ]),
    defaultedColumns(1),
    onAction,
    onItemAction
  ]);
  const createSplitButton = (spec) => asRaw("SplitButton", splitButtonSchema, spec);
  const factory$b = (detail, spec) => {
    const setMenus = (comp, menus) => {
      const newMenus = map$2(menus, (m) => {
        const buttonSpec = {
          type: "menubutton",
          text: m.text,
          fetch: (callback) => {
            callback(m.getItems());
          }
        };
        const internal = createMenuButton(buttonSpec).mapError((errInfo) => formatError(errInfo)).getOrDie();
        return renderMenuButton(internal, "tox-mbtn", spec.backstage, Optional.some("menuitem"));
      });
      Replacing.set(comp, newMenus);
    };
    const apis = {
      focus: Keying.focusIn,
      setMenus
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: [],
      behaviours: derive$1([
        Replacing.config({}),
        config("menubar-events", [
          runOnAttached((component) => {
            detail.onSetup(component);
          }),
          run$1(mouseover(), (comp, se) => {
            descendant(comp.element, ".tox-mbtn--active").each((activeButton) => {
              closest$1(se.event.target, ".tox-mbtn").each((hoveredButton) => {
                if (!eq(activeButton, hoveredButton)) {
                  comp.getSystem().getByDom(activeButton).each((activeComp) => {
                    comp.getSystem().getByDom(hoveredButton).each((hoveredComp) => {
                      Dropdown.expand(hoveredComp);
                      Dropdown.close(activeComp);
                      Focusing.focus(hoveredComp);
                    });
                  });
                }
              });
            });
          }),
          run$1(focusShifted(), (comp, se) => {
            se.event.prevFocus.bind((prev) => comp.getSystem().getByDom(prev).toOptional()).each((prev) => {
              se.event.newFocus.bind((nu2) => comp.getSystem().getByDom(nu2).toOptional()).each((nu2) => {
                if (Dropdown.isOpen(prev)) {
                  Dropdown.expand(nu2);
                  Dropdown.close(prev);
                }
              });
            });
          })
        ]),
        Keying.config({
          mode: "flow",
          selector: ".tox-mbtn",
          onEscape: (comp) => {
            detail.onEscape(comp);
            return Optional.some(true);
          }
        }),
        Tabstopping.config({})
      ]),
      apis,
      domModification: { attributes: { role: "menubar" } }
    };
  };
  var SilverMenubar = single({
    factory: factory$b,
    name: "silver.Menubar",
    configFields: [
      required$1("dom"),
      required$1("uid"),
      required$1("onEscape"),
      required$1("backstage"),
      defaulted("onSetup", noop)
    ],
    apis: {
      focus: (apis, comp) => {
        apis.focus(comp);
      },
      setMenus: (apis, comp, menus) => {
        apis.setMenus(comp, menus);
      }
    }
  });
  const promotionMessage = "\u26A1\uFE0FUpgrade";
  const promotionLink = "https://www.tiny.cloud/tinymce-self-hosted-premium-features/?utm_source=TinyMCE&utm_medium=SPAP&utm_campaign=SPAP&utm_id=editorreferral";
  const renderPromotion = (spec) => {
    return {
      uid: spec.uid,
      dom: spec.dom,
      components: [{
        dom: {
          tag: "a",
          attributes: {
            "href": promotionLink,
            "rel": "noopener",
            "target": "_blank",
            "aria-hidden": "true"
          },
          classes: ["tox-promotion-link"],
          innerHtml: promotionMessage
        }
      }]
    };
  };
  const getAnimationRoot = (component, slideConfig) => slideConfig.getAnimationRoot.fold(() => component.element, (get2) => get2(component));
  const getDimensionProperty = (slideConfig) => slideConfig.dimension.property;
  const getDimension = (slideConfig, elem) => slideConfig.dimension.getDimension(elem);
  const disableTransitions = (component, slideConfig) => {
    const root = getAnimationRoot(component, slideConfig);
    remove$1(root, [
      slideConfig.shrinkingClass,
      slideConfig.growingClass
    ]);
  };
  const setShrunk = (component, slideConfig) => {
    remove$2(component.element, slideConfig.openClass);
    add$2(component.element, slideConfig.closedClass);
    set$8(component.element, getDimensionProperty(slideConfig), "0px");
    reflow(component.element);
  };
  const setGrown = (component, slideConfig) => {
    remove$2(component.element, slideConfig.closedClass);
    add$2(component.element, slideConfig.openClass);
    remove$6(component.element, getDimensionProperty(slideConfig));
  };
  const doImmediateShrink = (component, slideConfig, slideState, _calculatedSize) => {
    slideState.setCollapsed();
    set$8(component.element, getDimensionProperty(slideConfig), getDimension(slideConfig, component.element));
    disableTransitions(component, slideConfig);
    setShrunk(component, slideConfig);
    slideConfig.onStartShrink(component);
    slideConfig.onShrunk(component);
  };
  const doStartShrink = (component, slideConfig, slideState, calculatedSize) => {
    const size = calculatedSize.getOrThunk(() => getDimension(slideConfig, component.element));
    slideState.setCollapsed();
    set$8(component.element, getDimensionProperty(slideConfig), size);
    reflow(component.element);
    const root = getAnimationRoot(component, slideConfig);
    remove$2(root, slideConfig.growingClass);
    add$2(root, slideConfig.shrinkingClass);
    setShrunk(component, slideConfig);
    slideConfig.onStartShrink(component);
  };
  const doStartSmartShrink = (component, slideConfig, slideState) => {
    const size = getDimension(slideConfig, component.element);
    const shrinker = size === "0px" ? doImmediateShrink : doStartShrink;
    shrinker(component, slideConfig, slideState, Optional.some(size));
  };
  const doStartGrow = (component, slideConfig, slideState) => {
    const root = getAnimationRoot(component, slideConfig);
    const wasShrinking = has(root, slideConfig.shrinkingClass);
    const beforeSize = getDimension(slideConfig, component.element);
    setGrown(component, slideConfig);
    const fullSize = getDimension(slideConfig, component.element);
    const startPartialGrow = () => {
      set$8(component.element, getDimensionProperty(slideConfig), beforeSize);
      reflow(component.element);
    };
    const startCompleteGrow = () => {
      setShrunk(component, slideConfig);
    };
    const setStartSize = wasShrinking ? startPartialGrow : startCompleteGrow;
    setStartSize();
    remove$2(root, slideConfig.shrinkingClass);
    add$2(root, slideConfig.growingClass);
    setGrown(component, slideConfig);
    set$8(component.element, getDimensionProperty(slideConfig), fullSize);
    slideState.setExpanded();
    slideConfig.onStartGrow(component);
  };
  const refresh$3 = (component, slideConfig, slideState) => {
    if (slideState.isExpanded()) {
      remove$6(component.element, getDimensionProperty(slideConfig));
      const fullSize = getDimension(slideConfig, component.element);
      set$8(component.element, getDimensionProperty(slideConfig), fullSize);
    }
  };
  const grow = (component, slideConfig, slideState) => {
    if (!slideState.isExpanded()) {
      doStartGrow(component, slideConfig, slideState);
    }
  };
  const shrink = (component, slideConfig, slideState) => {
    if (slideState.isExpanded()) {
      doStartSmartShrink(component, slideConfig, slideState);
    }
  };
  const immediateShrink = (component, slideConfig, slideState) => {
    if (slideState.isExpanded()) {
      doImmediateShrink(component, slideConfig, slideState);
    }
  };
  const hasGrown = (component, slideConfig, slideState) => slideState.isExpanded();
  const hasShrunk = (component, slideConfig, slideState) => slideState.isCollapsed();
  const isGrowing = (component, slideConfig, _slideState) => {
    const root = getAnimationRoot(component, slideConfig);
    return has(root, slideConfig.growingClass) === true;
  };
  const isShrinking = (component, slideConfig, _slideState) => {
    const root = getAnimationRoot(component, slideConfig);
    return has(root, slideConfig.shrinkingClass) === true;
  };
  const isTransitioning = (component, slideConfig, slideState) => isGrowing(component, slideConfig) || isShrinking(component, slideConfig);
  const toggleGrow = (component, slideConfig, slideState) => {
    const f = slideState.isExpanded() ? doStartSmartShrink : doStartGrow;
    f(component, slideConfig, slideState);
  };
  const immediateGrow = (component, slideConfig, slideState) => {
    if (!slideState.isExpanded()) {
      setGrown(component, slideConfig);
      set$8(component.element, getDimensionProperty(slideConfig), getDimension(slideConfig, component.element));
      disableTransitions(component, slideConfig);
      slideState.setExpanded();
      slideConfig.onStartGrow(component);
      slideConfig.onGrown(component);
    }
  };
  var SlidingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    refresh: refresh$3,
    grow,
    shrink,
    immediateShrink,
    hasGrown,
    hasShrunk,
    isGrowing,
    isShrinking,
    isTransitioning,
    toggleGrow,
    disableTransitions,
    immediateGrow
  });
  const exhibit = (base2, slideConfig, _slideState) => {
    const expanded = slideConfig.expanded;
    return expanded ? nu$7({
      classes: [slideConfig.openClass],
      styles: {}
    }) : nu$7({
      classes: [slideConfig.closedClass],
      styles: wrap$1(slideConfig.dimension.property, "0px")
    });
  };
  const events$4 = (slideConfig, slideState) => derive$2([runOnSource(transitionend(), (component, simulatedEvent) => {
    const raw = simulatedEvent.event.raw;
    if (raw.propertyName === slideConfig.dimension.property) {
      disableTransitions(component, slideConfig);
      if (slideState.isExpanded()) {
        remove$6(component.element, slideConfig.dimension.property);
      }
      const notify = slideState.isExpanded() ? slideConfig.onGrown : slideConfig.onShrunk;
      notify(component);
    }
  })]);
  var ActiveSliding = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    exhibit,
    events: events$4
  });
  var SlidingSchema = [
    required$1("closedClass"),
    required$1("openClass"),
    required$1("shrinkingClass"),
    required$1("growingClass"),
    option$3("getAnimationRoot"),
    onHandler("onShrunk"),
    onHandler("onStartShrink"),
    onHandler("onGrown"),
    onHandler("onStartGrow"),
    defaulted("expanded", false),
    requiredOf("dimension", choose$1("property", {
      width: [
        output$1("property", "width"),
        output$1("getDimension", (elem) => get$c(elem) + "px")
      ],
      height: [
        output$1("property", "height"),
        output$1("getDimension", (elem) => get$d(elem) + "px")
      ]
    }))
  ];
  const init$5 = (spec) => {
    const state = Cell(spec.expanded);
    const readState = () => "expanded: " + state.get();
    return nu$8({
      isExpanded: () => state.get() === true,
      isCollapsed: () => state.get() === false,
      setCollapsed: curry(state.set, false),
      setExpanded: curry(state.set, true),
      readState
    });
  };
  var SlidingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$5
  });
  const Sliding = create$3({
    fields: SlidingSchema,
    name: "sliding",
    active: ActiveSliding,
    apis: SlidingApis,
    state: SlidingState
  });
  const owner = "container";
  const schema$d = [field("slotBehaviours", [])];
  const getPartName = (name2) => "<alloy.field." + name2 + ">";
  const sketch = (sSpec) => {
    const parts2 = (() => {
      const record2 = [];
      const slot = (name2, config2) => {
        record2.push(name2);
        return generateOne$1(owner, getPartName(name2), config2);
      };
      return {
        slot,
        record: constant$1(record2)
      };
    })();
    const spec = sSpec(parts2);
    const partNames = parts2.record();
    const fieldParts = map$2(partNames, (n) => required({
      name: n,
      pname: getPartName(n)
    }));
    return composite$1(owner, schema$d, fieldParts, make$1, spec);
  };
  const make$1 = (detail, components2) => {
    const getSlotNames = (_) => getAllPartNames(detail);
    const getSlot = (container, key) => getPart(container, detail, key);
    const onSlot = (f, def) => (container, key) => getPart(container, detail, key).map((slot) => f(slot, key)).getOr(def);
    const onSlots = (f) => (container, keys2) => {
      each$1(keys2, (key) => f(container, key));
    };
    const doShowing = (comp, _key) => get$f(comp.element, "aria-hidden") !== "true";
    const doShow = (comp, key) => {
      if (!doShowing(comp)) {
        const element2 = comp.element;
        remove$6(element2, "display");
        remove$7(element2, "aria-hidden");
        emitWith(comp, slotVisibility(), {
          name: key,
          visible: true
        });
      }
    };
    const doHide = (comp, key) => {
      if (doShowing(comp)) {
        const element2 = comp.element;
        set$8(element2, "display", "none");
        set$9(element2, "aria-hidden", "true");
        emitWith(comp, slotVisibility(), {
          name: key,
          visible: false
        });
      }
    };
    const isShowing = onSlot(doShowing, false);
    const hideSlot = onSlot(doHide);
    const hideSlots = onSlots(hideSlot);
    const hideAllSlots = (container) => hideSlots(container, getSlotNames());
    const showSlot = onSlot(doShow);
    const apis = {
      getSlotNames,
      getSlot,
      isShowing,
      hideSlot,
      hideAllSlots,
      showSlot
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: get$3(detail.slotBehaviours),
      apis
    };
  };
  const slotApis = map$1({
    getSlotNames: (apis, c) => apis.getSlotNames(c),
    getSlot: (apis, c, key) => apis.getSlot(c, key),
    isShowing: (apis, c, key) => apis.isShowing(c, key),
    hideSlot: (apis, c, key) => apis.hideSlot(c, key),
    hideAllSlots: (apis, c) => apis.hideAllSlots(c),
    showSlot: (apis, c, key) => apis.showSlot(c, key)
  }, (value2) => makeApi(value2));
  const SlotContainer = {
    ...slotApis,
    ...{ sketch }
  };
  const sidebarSchema = objOf([
    optionalIcon,
    optionalTooltip,
    defaultedFunction("onShow", noop),
    defaultedFunction("onHide", noop),
    onSetup
  ]);
  const createSidebar = (spec) => asRaw("sidebar", sidebarSchema, spec);
  const setup$8 = (editor) => {
    const { sidebars } = editor.ui.registry.getAll();
    each$1(keys(sidebars), (name2) => {
      const spec = sidebars[name2];
      const isActive = () => is$1(Optional.from(editor.queryCommandValue("ToggleSidebar")), name2);
      editor.ui.registry.addToggleButton(name2, {
        icon: spec.icon,
        tooltip: spec.tooltip,
        onAction: (buttonApi) => {
          editor.execCommand("ToggleSidebar", false, name2);
          buttonApi.setActive(isActive());
        },
        onSetup: (buttonApi) => {
          buttonApi.setActive(isActive());
          const handleToggle = () => buttonApi.setActive(isActive());
          editor.on("ToggleSidebar", handleToggle);
          return () => {
            editor.off("ToggleSidebar", handleToggle);
          };
        }
      });
    });
  };
  const getApi = (comp) => ({ element: () => comp.element.dom });
  const makePanels = (parts2, panelConfigs) => {
    const specs = map$2(keys(panelConfigs), (name2) => {
      const spec = panelConfigs[name2];
      const bridged = getOrDie(createSidebar(spec));
      return {
        name: name2,
        getApi,
        onSetup: bridged.onSetup,
        onShow: bridged.onShow,
        onHide: bridged.onHide
      };
    });
    return map$2(specs, (spec) => {
      const editorOffCell = Cell(noop);
      return parts2.slot(spec.name, {
        dom: {
          tag: "div",
          classes: ["tox-sidebar__pane"]
        },
        behaviours: SimpleBehaviours.unnamedEvents([
          onControlAttached(spec, editorOffCell),
          onControlDetached(spec, editorOffCell),
          run$1(slotVisibility(), (sidepanel, se) => {
            const data = se.event;
            const optSidePanelSpec = find$5(specs, (config2) => config2.name === data.name);
            optSidePanelSpec.each((sidePanelSpec) => {
              const handler = data.visible ? sidePanelSpec.onShow : sidePanelSpec.onHide;
              handler(sidePanelSpec.getApi(sidepanel));
            });
          })
        ])
      });
    });
  };
  const makeSidebar = (panelConfigs) => SlotContainer.sketch((parts2) => ({
    dom: {
      tag: "div",
      classes: ["tox-sidebar__pane-container"]
    },
    components: makePanels(parts2, panelConfigs),
    slotBehaviours: SimpleBehaviours.unnamedEvents([runOnAttached((slotContainer) => SlotContainer.hideAllSlots(slotContainer))])
  }));
  const setSidebar = (sidebar, panelConfigs, showSidebar) => {
    const optSlider = Composing.getCurrent(sidebar);
    optSlider.each((slider) => {
      Replacing.set(slider, [makeSidebar(panelConfigs)]);
      const configKey = showSidebar === null || showSidebar === void 0 ? void 0 : showSidebar.toLowerCase();
      if (isString(configKey) && has$2(panelConfigs, configKey)) {
        Composing.getCurrent(slider).each((slotContainer) => {
          SlotContainer.showSlot(slotContainer, configKey);
          Sliding.immediateGrow(slider);
          remove$6(slider.element, "width");
        });
      }
    });
  };
  const toggleSidebar = (sidebar, name2) => {
    const optSlider = Composing.getCurrent(sidebar);
    optSlider.each((slider) => {
      const optSlotContainer = Composing.getCurrent(slider);
      optSlotContainer.each((slotContainer) => {
        if (Sliding.hasGrown(slider)) {
          if (SlotContainer.isShowing(slotContainer, name2)) {
            Sliding.shrink(slider);
          } else {
            SlotContainer.hideAllSlots(slotContainer);
            SlotContainer.showSlot(slotContainer, name2);
          }
        } else {
          SlotContainer.hideAllSlots(slotContainer);
          SlotContainer.showSlot(slotContainer, name2);
          Sliding.grow(slider);
        }
      });
    });
  };
  const whichSidebar = (sidebar) => {
    const optSlider = Composing.getCurrent(sidebar);
    return optSlider.bind((slider) => {
      const sidebarOpen = Sliding.isGrowing(slider) || Sliding.hasGrown(slider);
      if (sidebarOpen) {
        const optSlotContainer = Composing.getCurrent(slider);
        return optSlotContainer.bind((slotContainer) => find$5(SlotContainer.getSlotNames(slotContainer), (name2) => SlotContainer.isShowing(slotContainer, name2)));
      } else {
        return Optional.none();
      }
    });
  };
  const fixSize = generate$6("FixSizeEvent");
  const autoSize = generate$6("AutoSizeEvent");
  const renderSidebar = (spec) => ({
    uid: spec.uid,
    dom: {
      tag: "div",
      classes: ["tox-sidebar"],
      attributes: { role: "complementary" }
    },
    components: [{
      dom: {
        tag: "div",
        classes: ["tox-sidebar__slider"]
      },
      components: [],
      behaviours: derive$1([
        Tabstopping.config({}),
        Focusing.config({}),
        Sliding.config({
          dimension: { property: "width" },
          closedClass: "tox-sidebar--sliding-closed",
          openClass: "tox-sidebar--sliding-open",
          shrinkingClass: "tox-sidebar--sliding-shrinking",
          growingClass: "tox-sidebar--sliding-growing",
          onShrunk: (slider) => {
            const optSlotContainer = Composing.getCurrent(slider);
            optSlotContainer.each(SlotContainer.hideAllSlots);
            emit(slider, autoSize);
          },
          onGrown: (slider) => {
            emit(slider, autoSize);
          },
          onStartGrow: (slider) => {
            emitWith(slider, fixSize, { width: getRaw(slider.element, "width").getOr("") });
          },
          onStartShrink: (slider) => {
            emitWith(slider, fixSize, { width: get$c(slider.element) + "px" });
          }
        }),
        Replacing.config({}),
        Composing.config({
          find: (comp) => {
            const children2 = Replacing.contents(comp);
            return head(children2);
          }
        })
      ])
    }],
    behaviours: derive$1([
      ComposingConfigs.childAt(0),
      config("sidebar-sliding-events", [
        run$1(fixSize, (comp, se) => {
          set$8(comp.element, "width", se.event.width);
        }),
        run$1(autoSize, (comp, _se) => {
          remove$6(comp.element, "width");
        })
      ])
    ])
  });
  const block = (component, config2, state, getBusySpec2) => {
    set$9(component.element, "aria-busy", true);
    const root = config2.getRoot(component).getOr(component);
    const blockerBehaviours = derive$1([
      Keying.config({
        mode: "special",
        onTab: () => Optional.some(true),
        onShiftTab: () => Optional.some(true)
      }),
      Focusing.config({})
    ]);
    const blockSpec = getBusySpec2(root, blockerBehaviours);
    const blocker = root.getSystem().build(blockSpec);
    Replacing.append(root, premade(blocker));
    if (blocker.hasConfigured(Keying) && config2.focus) {
      Keying.focusIn(blocker);
    }
    if (!state.isBlocked()) {
      config2.onBlock(component);
    }
    state.blockWith(() => Replacing.remove(root, blocker));
  };
  const unblock = (component, config2, state) => {
    remove$7(component.element, "aria-busy");
    if (state.isBlocked()) {
      config2.onUnblock(component);
    }
    state.clear();
  };
  var BlockingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    block,
    unblock
  });
  var BlockingSchema = [
    defaultedFunction("getRoot", Optional.none),
    defaultedBoolean("focus", true),
    onHandler("onBlock"),
    onHandler("onUnblock")
  ];
  const init$4 = () => {
    const blocker = destroyable();
    const blockWith = (destroy) => {
      blocker.set({ destroy });
    };
    return nu$8({
      readState: blocker.isSet,
      blockWith,
      clear: blocker.clear,
      isBlocked: blocker.isSet
    });
  };
  var BlockingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$4
  });
  const Blocking = create$3({
    fields: BlockingSchema,
    name: "blocking",
    apis: BlockingApis,
    state: BlockingState
  });
  const getAttrs = (elem) => {
    const attributes = elem.dom.attributes !== void 0 ? elem.dom.attributes : [];
    return foldl(attributes, (b2, attr) => {
      if (attr.name === "class") {
        return b2;
      } else {
        return {
          ...b2,
          [attr.name]: attr.value
        };
      }
    }, {});
  };
  const getClasses = (elem) => Array.prototype.slice.call(elem.dom.classList, 0);
  const fromHtml = (html) => {
    const elem = SugarElement.fromHtml(html);
    const children$1 = children(elem);
    const attrs = getAttrs(elem);
    const classes2 = getClasses(elem);
    const contents2 = children$1.length === 0 ? {} : { innerHtml: get$9(elem) };
    return {
      tag: name$3(elem),
      classes: classes2,
      attributes: attrs,
      ...contents2
    };
  };
  const getBusySpec$1 = (providerBackstage) => (_root, _behaviours) => ({
    dom: {
      tag: "div",
      attributes: {
        "aria-label": providerBackstage.translate("Loading..."),
        "tabindex": "0"
      },
      classes: ["tox-throbber__busy-spinner"]
    },
    components: [{ dom: fromHtml('<div class="tox-spinner"><div></div><div></div><div></div></div>') }]
  });
  const focusBusyComponent = (throbber) => Composing.getCurrent(throbber).each((comp) => focus$3(comp.element));
  const toggleEditorTabIndex = (editor, state) => {
    const tabIndexAttr = "tabindex";
    const dataTabIndexAttr = `data-mce-${tabIndexAttr}`;
    Optional.from(editor.iframeElement).map(SugarElement.fromDom).each((iframe2) => {
      if (state) {
        getOpt(iframe2, tabIndexAttr).each((tabIndex) => set$9(iframe2, dataTabIndexAttr, tabIndex));
        set$9(iframe2, tabIndexAttr, -1);
      } else {
        remove$7(iframe2, tabIndexAttr);
        getOpt(iframe2, dataTabIndexAttr).each((tabIndex) => {
          set$9(iframe2, tabIndexAttr, tabIndex);
          remove$7(iframe2, dataTabIndexAttr);
        });
      }
    });
  };
  const toggleThrobber = (editor, comp, state, providerBackstage) => {
    const element2 = comp.element;
    toggleEditorTabIndex(editor, state);
    if (state) {
      Blocking.block(comp, getBusySpec$1(providerBackstage));
      remove$6(element2, "display");
      remove$7(element2, "aria-hidden");
      if (editor.hasFocus()) {
        focusBusyComponent(comp);
      }
    } else {
      const throbberFocus = Composing.getCurrent(comp).exists((busyComp) => hasFocus(busyComp.element));
      Blocking.unblock(comp);
      set$8(element2, "display", "none");
      set$9(element2, "aria-hidden", "true");
      if (throbberFocus) {
        editor.focus();
      }
    }
  };
  const renderThrobber = (spec) => ({
    uid: spec.uid,
    dom: {
      tag: "div",
      attributes: { "aria-hidden": "true" },
      classes: ["tox-throbber"],
      styles: { display: "none" }
    },
    behaviours: derive$1([
      Replacing.config({}),
      Blocking.config({ focus: false }),
      Composing.config({ find: (comp) => head(comp.components()) })
    ]),
    components: []
  });
  const isFocusEvent = (event2) => event2.type === "focusin";
  const isPasteBinTarget = (event2) => {
    if (isFocusEvent(event2)) {
      const node = event2.composed ? head(event2.composedPath()) : Optional.from(event2.target);
      return node.map(SugarElement.fromDom).filter(isElement$1).exists((targetElm) => has(targetElm, "mce-pastebin"));
    } else {
      return false;
    }
  };
  const setup$7 = (editor, lazyThrobber, sharedBackstage) => {
    const throbberState = Cell(false);
    const timer = value$2();
    const stealFocus = (e) => {
      if (throbberState.get() && !isPasteBinTarget(e)) {
        e.preventDefault();
        focusBusyComponent(lazyThrobber());
        editor.editorManager.setActive(editor);
      }
    };
    if (!editor.inline) {
      editor.on("PreInit", () => {
        editor.dom.bind(editor.getWin(), "focusin", stealFocus);
        editor.on("BeforeExecCommand", (e) => {
          if (e.command.toLowerCase() === "mcefocus" && e.value !== true) {
            stealFocus(e);
          }
        });
      });
    }
    const toggle2 = (state) => {
      if (state !== throbberState.get()) {
        throbberState.set(state);
        toggleThrobber(editor, lazyThrobber(), state, sharedBackstage.providers);
        fireAfterProgressState(editor, state);
      }
    };
    editor.on("ProgressState", (e) => {
      timer.on(clearTimeout);
      if (isNumber(e.time)) {
        const timerId = global$9.setEditorTimeout(editor, () => toggle2(e.state), e.time);
        timer.set(timerId);
      } else {
        toggle2(e.state);
        timer.clear();
      }
    });
  };
  const generate$1 = (xs, f) => {
    const init2 = {
      len: 0,
      list: []
    };
    const r2 = foldl(xs, (b2, a) => {
      const value2 = f(a, b2.len);
      return value2.fold(constant$1(b2), (v) => ({
        len: v.finish,
        list: b2.list.concat([v])
      }));
    }, init2);
    return r2.list;
  };
  const output = (within, extra, withinWidth) => ({
    within,
    extra,
    withinWidth
  });
  const apportion = (units2, total, len) => {
    const parray = generate$1(units2, (unit, current) => {
      const width2 = len(unit);
      return Optional.some({
        element: unit,
        start: current,
        finish: current + width2,
        width: width2
      });
    });
    const within = filter$2(parray, (unit) => unit.finish <= total);
    const withinWidth = foldr(within, (acc, el) => acc + el.width, 0);
    const extra = parray.slice(within.length);
    return {
      within,
      extra,
      withinWidth
    };
  };
  const toUnit = (parray) => map$2(parray, (unit) => unit.element);
  const fitLast = (within, extra, withinWidth) => {
    const fits = toUnit(within.concat(extra));
    return output(fits, [], withinWidth);
  };
  const overflow = (within, extra, overflower, withinWidth) => {
    const fits = toUnit(within).concat([overflower]);
    return output(fits, toUnit(extra), withinWidth);
  };
  const fitAll = (within, extra, withinWidth) => output(toUnit(within), [], withinWidth);
  const tryFit = (total, units2, len) => {
    const divide = apportion(units2, total, len);
    return divide.extra.length === 0 ? Optional.some(divide) : Optional.none();
  };
  const partition = (total, units2, len, overflower) => {
    const divide = tryFit(total, units2, len).getOrThunk(() => apportion(units2, total - len(overflower), len));
    const within = divide.within;
    const extra = divide.extra;
    const withinWidth = divide.withinWidth;
    if (extra.length === 1 && extra[0].width <= len(overflower)) {
      return fitLast(within, extra, withinWidth);
    } else if (extra.length >= 1) {
      return overflow(within, extra, overflower, withinWidth);
    } else {
      return fitAll(within, extra, withinWidth);
    }
  };
  const setGroups$1 = (toolbar, storedGroups) => {
    const bGroups = map$2(storedGroups, (g) => premade(g));
    Toolbar.setGroups(toolbar, bGroups);
  };
  const findFocusedComp = (comps) => findMap(comps, (comp) => search(comp.element).bind((focusedElm) => comp.getSystem().getByDom(focusedElm).toOptional()));
  const refresh$2 = (toolbar, detail, setOverflow) => {
    const builtGroups = detail.builtGroups.get();
    if (builtGroups.length === 0) {
      return;
    }
    const primary2 = getPartOrDie(toolbar, detail, "primary");
    const overflowGroup = Coupling.getCoupled(toolbar, "overflowGroup");
    set$8(primary2.element, "visibility", "hidden");
    const groups = builtGroups.concat([overflowGroup]);
    const focusedComp = findFocusedComp(groups);
    setOverflow([]);
    setGroups$1(primary2, groups);
    const availableWidth = get$c(primary2.element);
    const overflows = partition(availableWidth, detail.builtGroups.get(), (comp) => get$c(comp.element), overflowGroup);
    if (overflows.extra.length === 0) {
      Replacing.remove(primary2, overflowGroup);
      setOverflow([]);
    } else {
      setGroups$1(primary2, overflows.within);
      setOverflow(overflows.extra);
    }
    remove$6(primary2.element, "visibility");
    reflow(primary2.element);
    focusedComp.each(Focusing.focus);
  };
  const schema$c = constant$1([
    field("splitToolbarBehaviours", [Coupling]),
    customField("builtGroups", () => Cell([]))
  ]);
  const schema$b = constant$1([
    markers$1(["overflowToggledClass"]),
    optionFunction("getOverflowBounds"),
    required$1("lazySink"),
    customField("overflowGroups", () => Cell([]))
  ].concat(schema$c()));
  const parts$7 = constant$1([
    required({
      factory: Toolbar,
      schema: schema$e(),
      name: "primary"
    }),
    external({
      schema: schema$e(),
      name: "overflow"
    }),
    external({ name: "overflow-button" }),
    external({ name: "overflow-group" })
  ]);
  const expandable = constant$1((element2, available) => {
    setMax(element2, Math.floor(available));
  });
  const schema$a = constant$1([
    markers$1(["toggledClass"]),
    required$1("lazySink"),
    requiredFunction("fetch"),
    optionFunction("getBounds"),
    optionObjOf("fireDismissalEventInstead", [defaulted("event", dismissRequested())]),
    schema$y()
  ]);
  const parts$6 = constant$1([
    external({
      name: "button",
      overrides: (detail) => ({
        dom: { attributes: { "aria-haspopup": "true" } },
        buttonBehaviours: derive$1([Toggling.config({
          toggleClass: detail.markers.toggledClass,
          aria: { mode: "expanded" },
          toggleOnExecute: false
        })])
      })
    }),
    external({
      factory: Toolbar,
      schema: schema$e(),
      name: "toolbar",
      overrides: (detail) => {
        return {
          toolbarBehaviours: derive$1([Keying.config({
            mode: "cyclic",
            onEscape: (comp) => {
              getPart(comp, detail, "button").each(Focusing.focus);
              return Optional.none();
            }
          })])
        };
      }
    })
  ]);
  const toggle = (button2, externals) => {
    const toolbarSandbox = Coupling.getCoupled(button2, "toolbarSandbox");
    if (Sandboxing.isOpen(toolbarSandbox)) {
      Sandboxing.close(toolbarSandbox);
    } else {
      Sandboxing.open(toolbarSandbox, externals.toolbar());
    }
  };
  const position = (button2, toolbar, detail, layouts2) => {
    const bounds2 = detail.getBounds.map((bounder) => bounder());
    const sink = detail.lazySink(button2).getOrDie();
    Positioning.positionWithinBounds(sink, toolbar, {
      anchor: {
        type: "hotspot",
        hotspot: button2,
        layouts: layouts2,
        overrides: { maxWidthFunction: expandable() }
      }
    }, bounds2);
  };
  const setGroups = (button2, toolbar, detail, layouts2, groups) => {
    Toolbar.setGroups(toolbar, groups);
    position(button2, toolbar, detail, layouts2);
    Toggling.on(button2);
  };
  const makeSandbox = (button2, spec, detail) => {
    const ariaControls = manager();
    const onOpen = (sandbox, toolbar) => {
      detail.fetch().get((groups) => {
        setGroups(button2, toolbar, detail, spec.layouts, groups);
        ariaControls.link(button2.element);
        Keying.focusIn(toolbar);
      });
    };
    const onClose = () => {
      Toggling.off(button2);
      Focusing.focus(button2);
      ariaControls.unlink(button2.element);
    };
    return {
      dom: {
        tag: "div",
        attributes: { id: ariaControls.id }
      },
      behaviours: derive$1([
        Keying.config({
          mode: "special",
          onEscape: (comp) => {
            Sandboxing.close(comp);
            return Optional.some(true);
          }
        }),
        Sandboxing.config({
          onOpen,
          onClose,
          isPartOf: (container, data, queryElem) => {
            return isPartOf$1(data, queryElem) || isPartOf$1(button2, queryElem);
          },
          getAttachPoint: () => {
            return detail.lazySink(button2).getOrDie();
          }
        }),
        Receiving.config({
          channels: {
            ...receivingChannel$1({
              isExtraPart: never,
              ...detail.fireDismissalEventInstead.map((fe) => ({ fireEventInstead: { event: fe.event } })).getOr({})
            }),
            ...receivingChannel({
              doReposition: () => {
                Sandboxing.getState(Coupling.getCoupled(button2, "toolbarSandbox")).each((toolbar) => {
                  position(button2, toolbar, detail, spec.layouts);
                });
              }
            })
          }
        })
      ])
    };
  };
  const factory$a = (detail, components2, spec, externals) => ({
    ...Button.sketch({
      ...externals.button(),
      action: (button2) => {
        toggle(button2, externals);
      },
      buttonBehaviours: SketchBehaviours.augment({ dump: externals.button().buttonBehaviours }, [Coupling.config({
        others: {
          toolbarSandbox: (button2) => {
            return makeSandbox(button2, spec, detail);
          }
        }
      })])
    }),
    apis: {
      setGroups: (button2, groups) => {
        Sandboxing.getState(Coupling.getCoupled(button2, "toolbarSandbox")).each((toolbar) => {
          setGroups(button2, toolbar, detail, spec.layouts, groups);
        });
      },
      reposition: (button2) => {
        Sandboxing.getState(Coupling.getCoupled(button2, "toolbarSandbox")).each((toolbar) => {
          position(button2, toolbar, detail, spec.layouts);
        });
      },
      toggle: (button2) => {
        toggle(button2, externals);
      },
      getToolbar: (button2) => {
        return Sandboxing.getState(Coupling.getCoupled(button2, "toolbarSandbox"));
      },
      isOpen: (button2) => {
        return Sandboxing.isOpen(Coupling.getCoupled(button2, "toolbarSandbox"));
      }
    }
  });
  const FloatingToolbarButton = composite({
    name: "FloatingToolbarButton",
    factory: factory$a,
    configFields: schema$a(),
    partFields: parts$6(),
    apis: {
      setGroups: (apis, button2, groups) => {
        apis.setGroups(button2, groups);
      },
      reposition: (apis, button2) => {
        apis.reposition(button2);
      },
      toggle: (apis, button2) => {
        apis.toggle(button2);
      },
      getToolbar: (apis, button2) => apis.getToolbar(button2),
      isOpen: (apis, button2) => apis.isOpen(button2)
    }
  });
  const schema$9 = constant$1([
    required$1("items"),
    markers$1(["itemSelector"]),
    field("tgroupBehaviours", [Keying])
  ]);
  const parts$5 = constant$1([group({
    name: "items",
    unit: "item"
  })]);
  const factory$9 = (detail, components2, _spec, _externals) => ({
    uid: detail.uid,
    dom: detail.dom,
    components: components2,
    behaviours: augment(detail.tgroupBehaviours, [Keying.config({
      mode: "flow",
      selector: detail.markers.itemSelector
    })]),
    domModification: { attributes: { role: "toolbar" } }
  });
  const ToolbarGroup = composite({
    name: "ToolbarGroup",
    configFields: schema$9(),
    partFields: parts$5(),
    factory: factory$9
  });
  const buildGroups = (comps) => map$2(comps, (g) => premade(g));
  const refresh$1 = (toolbar, memFloatingToolbarButton, detail) => {
    refresh$2(toolbar, detail, (overflowGroups) => {
      detail.overflowGroups.set(overflowGroups);
      memFloatingToolbarButton.getOpt(toolbar).each((floatingToolbarButton) => {
        FloatingToolbarButton.setGroups(floatingToolbarButton, buildGroups(overflowGroups));
      });
    });
  };
  const factory$8 = (detail, components2, spec, externals) => {
    const memFloatingToolbarButton = record(FloatingToolbarButton.sketch({
      fetch: () => Future.nu((resolve2) => {
        resolve2(buildGroups(detail.overflowGroups.get()));
      }),
      layouts: {
        onLtr: () => [
          southwest$2,
          southeast$2
        ],
        onRtl: () => [
          southeast$2,
          southwest$2
        ],
        onBottomLtr: () => [
          northwest$2,
          northeast$2
        ],
        onBottomRtl: () => [
          northeast$2,
          northwest$2
        ]
      },
      getBounds: spec.getOverflowBounds,
      lazySink: detail.lazySink,
      fireDismissalEventInstead: {},
      markers: { toggledClass: detail.markers.overflowToggledClass },
      parts: {
        button: externals["overflow-button"](),
        toolbar: externals.overflow()
      }
    }));
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: augment(detail.splitToolbarBehaviours, [Coupling.config({
        others: {
          overflowGroup: () => {
            return ToolbarGroup.sketch({
              ...externals["overflow-group"](),
              items: [memFloatingToolbarButton.asSpec()]
            });
          }
        }
      })]),
      apis: {
        setGroups: (toolbar, groups) => {
          detail.builtGroups.set(map$2(groups, toolbar.getSystem().build));
          refresh$1(toolbar, memFloatingToolbarButton, detail);
        },
        refresh: (toolbar) => refresh$1(toolbar, memFloatingToolbarButton, detail),
        toggle: (toolbar) => {
          memFloatingToolbarButton.getOpt(toolbar).each((floatingToolbarButton) => {
            FloatingToolbarButton.toggle(floatingToolbarButton);
          });
        },
        isOpen: (toolbar) => memFloatingToolbarButton.getOpt(toolbar).map(FloatingToolbarButton.isOpen).getOr(false),
        reposition: (toolbar) => {
          memFloatingToolbarButton.getOpt(toolbar).each((floatingToolbarButton) => {
            FloatingToolbarButton.reposition(floatingToolbarButton);
          });
        },
        getOverflow: (toolbar) => memFloatingToolbarButton.getOpt(toolbar).bind(FloatingToolbarButton.getToolbar)
      },
      domModification: { attributes: { role: "group" } }
    };
  };
  const SplitFloatingToolbar = composite({
    name: "SplitFloatingToolbar",
    configFields: schema$b(),
    partFields: parts$7(),
    factory: factory$8,
    apis: {
      setGroups: (apis, toolbar, groups) => {
        apis.setGroups(toolbar, groups);
      },
      refresh: (apis, toolbar) => {
        apis.refresh(toolbar);
      },
      reposition: (apis, toolbar) => {
        apis.reposition(toolbar);
      },
      toggle: (apis, toolbar) => {
        apis.toggle(toolbar);
      },
      isOpen: (apis, toolbar) => apis.isOpen(toolbar),
      getOverflow: (apis, toolbar) => apis.getOverflow(toolbar)
    }
  });
  const schema$8 = constant$1([
    markers$1([
      "closedClass",
      "openClass",
      "shrinkingClass",
      "growingClass",
      "overflowToggledClass"
    ]),
    onHandler("onOpened"),
    onHandler("onClosed")
  ].concat(schema$c()));
  const parts$4 = constant$1([
    required({
      factory: Toolbar,
      schema: schema$e(),
      name: "primary"
    }),
    required({
      factory: Toolbar,
      schema: schema$e(),
      name: "overflow",
      overrides: (detail) => {
        return {
          toolbarBehaviours: derive$1([
            Sliding.config({
              dimension: { property: "height" },
              closedClass: detail.markers.closedClass,
              openClass: detail.markers.openClass,
              shrinkingClass: detail.markers.shrinkingClass,
              growingClass: detail.markers.growingClass,
              onShrunk: (comp) => {
                getPart(comp, detail, "overflow-button").each((button2) => {
                  Toggling.off(button2);
                  Focusing.focus(button2);
                });
                detail.onClosed(comp);
              },
              onGrown: (comp) => {
                Keying.focusIn(comp);
                detail.onOpened(comp);
              },
              onStartGrow: (comp) => {
                getPart(comp, detail, "overflow-button").each(Toggling.on);
              }
            }),
            Keying.config({
              mode: "acyclic",
              onEscape: (comp) => {
                getPart(comp, detail, "overflow-button").each(Focusing.focus);
                return Optional.some(true);
              }
            })
          ])
        };
      }
    }),
    external({
      name: "overflow-button",
      overrides: (detail) => ({
        buttonBehaviours: derive$1([Toggling.config({
          toggleClass: detail.markers.overflowToggledClass,
          aria: { mode: "pressed" },
          toggleOnExecute: false
        })])
      })
    }),
    external({ name: "overflow-group" })
  ]);
  const isOpen = (toolbar, detail) => getPart(toolbar, detail, "overflow").map(Sliding.hasGrown).getOr(false);
  const toggleToolbar = (toolbar, detail) => {
    getPart(toolbar, detail, "overflow-button").bind(() => getPart(toolbar, detail, "overflow")).each((overf) => {
      refresh(toolbar, detail);
      Sliding.toggleGrow(overf);
    });
  };
  const refresh = (toolbar, detail) => {
    getPart(toolbar, detail, "overflow").each((overflow2) => {
      refresh$2(toolbar, detail, (groups) => {
        const builtGroups = map$2(groups, (g) => premade(g));
        Toolbar.setGroups(overflow2, builtGroups);
      });
      getPart(toolbar, detail, "overflow-button").each((button2) => {
        if (Sliding.hasGrown(overflow2)) {
          Toggling.on(button2);
        }
      });
      Sliding.refresh(overflow2);
    });
  };
  const factory$7 = (detail, components2, spec, externals) => {
    const toolbarToggleEvent = "alloy.toolbar.toggle";
    const doSetGroups = (toolbar, groups) => {
      const built = map$2(groups, toolbar.getSystem().build);
      detail.builtGroups.set(built);
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: augment(detail.splitToolbarBehaviours, [
        Coupling.config({
          others: {
            overflowGroup: (toolbar) => {
              return ToolbarGroup.sketch({
                ...externals["overflow-group"](),
                items: [Button.sketch({
                  ...externals["overflow-button"](),
                  action: (_button) => {
                    emit(toolbar, toolbarToggleEvent);
                  }
                })]
              });
            }
          }
        }),
        config("toolbar-toggle-events", [run$1(toolbarToggleEvent, (toolbar) => {
          toggleToolbar(toolbar, detail);
        })])
      ]),
      apis: {
        setGroups: (toolbar, groups) => {
          doSetGroups(toolbar, groups);
          refresh(toolbar, detail);
        },
        refresh: (toolbar) => refresh(toolbar, detail),
        toggle: (toolbar) => toggleToolbar(toolbar, detail),
        isOpen: (toolbar) => isOpen(toolbar, detail)
      },
      domModification: { attributes: { role: "group" } }
    };
  };
  const SplitSlidingToolbar = composite({
    name: "SplitSlidingToolbar",
    configFields: schema$8(),
    partFields: parts$4(),
    factory: factory$7,
    apis: {
      setGroups: (apis, toolbar, groups) => {
        apis.setGroups(toolbar, groups);
      },
      refresh: (apis, toolbar) => {
        apis.refresh(toolbar);
      },
      toggle: (apis, toolbar) => {
        apis.toggle(toolbar);
      },
      isOpen: (apis, toolbar) => apis.isOpen(toolbar)
    }
  });
  const renderToolbarGroupCommon = (toolbarGroup) => {
    const attributes = toolbarGroup.title.fold(() => ({}), (title2) => ({ attributes: { title: title2 } }));
    return {
      dom: {
        tag: "div",
        classes: ["tox-toolbar__group"],
        ...attributes
      },
      components: [ToolbarGroup.parts.items({})],
      items: toolbarGroup.items,
      markers: { itemSelector: "*:not(.tox-split-button) > .tox-tbtn:not([disabled]), .tox-split-button:not([disabled]), .tox-toolbar-nav-js:not([disabled])" },
      tgroupBehaviours: derive$1([
        Tabstopping.config({}),
        Focusing.config({})
      ])
    };
  };
  const renderToolbarGroup = (toolbarGroup) => ToolbarGroup.sketch(renderToolbarGroupCommon(toolbarGroup));
  const getToolbarBehaviours = (toolbarSpec, modeName) => {
    const onAttached = runOnAttached((component) => {
      const groups = map$2(toolbarSpec.initGroups, renderToolbarGroup);
      Toolbar.setGroups(component, groups);
    });
    return derive$1([
      DisablingConfigs.toolbarButton(toolbarSpec.providers.isDisabled),
      receivingConfig(),
      Keying.config({
        mode: modeName,
        onEscape: toolbarSpec.onEscape,
        selector: ".tox-toolbar__group"
      }),
      config("toolbar-events", [onAttached])
    ]);
  };
  const renderMoreToolbarCommon = (toolbarSpec) => {
    const modeName = toolbarSpec.cyclicKeying ? "cyclic" : "acyclic";
    return {
      uid: toolbarSpec.uid,
      dom: {
        tag: "div",
        classes: ["tox-toolbar-overlord"]
      },
      parts: {
        "overflow-group": renderToolbarGroupCommon({
          title: Optional.none(),
          items: []
        }),
        "overflow-button": renderIconButtonSpec({
          name: "more",
          icon: Optional.some("more-drawer"),
          enabled: true,
          tooltip: Optional.some("More..."),
          primary: false,
          buttonType: Optional.none(),
          borderless: false
        }, Optional.none(), toolbarSpec.providers)
      },
      splitToolbarBehaviours: getToolbarBehaviours(toolbarSpec, modeName)
    };
  };
  const renderFloatingMoreToolbar = (toolbarSpec) => {
    const baseSpec = renderMoreToolbarCommon(toolbarSpec);
    const overflowXOffset = 4;
    const primary2 = SplitFloatingToolbar.parts.primary({
      dom: {
        tag: "div",
        classes: ["tox-toolbar__primary"]
      }
    });
    return SplitFloatingToolbar.sketch({
      ...baseSpec,
      lazySink: toolbarSpec.getSink,
      getOverflowBounds: () => {
        const headerElem = toolbarSpec.moreDrawerData.lazyHeader().element;
        const headerBounds = absolute$2(headerElem);
        const docElem = documentElement(headerElem);
        const docBounds = absolute$2(docElem);
        const height2 = Math.max(docElem.dom.scrollHeight, docBounds.height);
        return bounds(headerBounds.x + overflowXOffset, docBounds.y, headerBounds.width - overflowXOffset * 2, height2);
      },
      parts: {
        ...baseSpec.parts,
        overflow: {
          dom: {
            tag: "div",
            classes: ["tox-toolbar__overflow"],
            attributes: toolbarSpec.attributes
          }
        }
      },
      components: [primary2],
      markers: { overflowToggledClass: "tox-tbtn--enabled" }
    });
  };
  const renderSlidingMoreToolbar = (toolbarSpec) => {
    const primary2 = SplitSlidingToolbar.parts.primary({
      dom: {
        tag: "div",
        classes: ["tox-toolbar__primary"]
      }
    });
    const overflow2 = SplitSlidingToolbar.parts.overflow({
      dom: {
        tag: "div",
        classes: ["tox-toolbar__overflow"]
      }
    });
    const baseSpec = renderMoreToolbarCommon(toolbarSpec);
    return SplitSlidingToolbar.sketch({
      ...baseSpec,
      components: [
        primary2,
        overflow2
      ],
      markers: {
        openClass: "tox-toolbar__overflow--open",
        closedClass: "tox-toolbar__overflow--closed",
        growingClass: "tox-toolbar__overflow--growing",
        shrinkingClass: "tox-toolbar__overflow--shrinking",
        overflowToggledClass: "tox-tbtn--enabled"
      },
      onOpened: (comp) => {
        comp.getSystem().broadcastOn([toolbarHeightChange()], { type: "opened" });
      },
      onClosed: (comp) => {
        comp.getSystem().broadcastOn([toolbarHeightChange()], { type: "closed" });
      }
    });
  };
  const renderToolbar = (toolbarSpec) => {
    const modeName = toolbarSpec.cyclicKeying ? "cyclic" : "acyclic";
    return Toolbar.sketch({
      uid: toolbarSpec.uid,
      dom: {
        tag: "div",
        classes: ["tox-toolbar"].concat(toolbarSpec.type === ToolbarMode$1.scrolling ? ["tox-toolbar--scrolling"] : [])
      },
      components: [Toolbar.parts.groups({})],
      toolbarBehaviours: getToolbarBehaviours(toolbarSpec, modeName)
    });
  };
  const factory$6 = (detail, components2, _spec) => {
    const apis = {
      getSocket: (comp) => {
        return parts$a.getPart(comp, detail, "socket");
      },
      setSidebar: (comp, panelConfigs, showSidebar) => {
        parts$a.getPart(comp, detail, "sidebar").each((sidebar) => setSidebar(sidebar, panelConfigs, showSidebar));
      },
      toggleSidebar: (comp, name2) => {
        parts$a.getPart(comp, detail, "sidebar").each((sidebar) => toggleSidebar(sidebar, name2));
      },
      whichSidebar: (comp) => {
        return parts$a.getPart(comp, detail, "sidebar").bind(whichSidebar).getOrNull();
      },
      getHeader: (comp) => {
        return parts$a.getPart(comp, detail, "header");
      },
      getToolbar: (comp) => {
        return parts$a.getPart(comp, detail, "toolbar");
      },
      setToolbar: (comp, groups) => {
        parts$a.getPart(comp, detail, "toolbar").each((toolbar) => {
          const renderedGroups = map$2(groups, renderToolbarGroup);
          toolbar.getApis().setGroups(toolbar, renderedGroups);
        });
      },
      setToolbars: (comp, toolbars) => {
        parts$a.getPart(comp, detail, "multiple-toolbar").each((mToolbar) => {
          const renderedToolbars = map$2(toolbars, (g) => map$2(g, renderToolbarGroup));
          CustomList.setItems(mToolbar, renderedToolbars);
        });
      },
      refreshToolbar: (comp) => {
        const toolbar = parts$a.getPart(comp, detail, "toolbar");
        toolbar.each((toolbar2) => toolbar2.getApis().refresh(toolbar2));
      },
      toggleToolbarDrawer: (comp) => {
        parts$a.getPart(comp, detail, "toolbar").each((toolbar) => {
          mapFrom(toolbar.getApis().toggle, (toggle2) => toggle2(toolbar));
        });
      },
      isToolbarDrawerToggled: (comp) => {
        return parts$a.getPart(comp, detail, "toolbar").bind((toolbar) => Optional.from(toolbar.getApis().isOpen).map((isOpen2) => isOpen2(toolbar))).getOr(false);
      },
      getThrobber: (comp) => {
        return parts$a.getPart(comp, detail, "throbber");
      },
      focusToolbar: (comp) => {
        const optToolbar = parts$a.getPart(comp, detail, "toolbar").orThunk(() => parts$a.getPart(comp, detail, "multiple-toolbar"));
        optToolbar.each((toolbar) => {
          Keying.focusIn(toolbar);
        });
      },
      setMenubar: (comp, menus) => {
        parts$a.getPart(comp, detail, "menubar").each((menubar) => {
          SilverMenubar.setMenus(menubar, menus);
        });
      },
      focusMenubar: (comp) => {
        parts$a.getPart(comp, detail, "menubar").each((menubar) => {
          SilverMenubar.focus(menubar);
        });
      }
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      apis,
      behaviours: detail.behaviours
    };
  };
  const partMenubar = partType.optional({
    factory: SilverMenubar,
    name: "menubar",
    schema: [required$1("backstage")]
  });
  const toolbarFactory = (spec) => {
    if (spec.type === ToolbarMode$1.sliding) {
      return renderSlidingMoreToolbar;
    } else if (spec.type === ToolbarMode$1.floating) {
      return renderFloatingMoreToolbar;
    } else {
      return renderToolbar;
    }
  };
  const partMultipleToolbar = partType.optional({
    factory: {
      sketch: (spec) => CustomList.sketch({
        uid: spec.uid,
        dom: spec.dom,
        listBehaviours: derive$1([Keying.config({
          mode: "acyclic",
          selector: ".tox-toolbar"
        })]),
        makeItem: () => renderToolbar({
          type: spec.type,
          uid: generate$6("multiple-toolbar-item"),
          cyclicKeying: false,
          initGroups: [],
          providers: spec.providers,
          onEscape: () => {
            spec.onEscape();
            return Optional.some(true);
          }
        }),
        setupItem: (_mToolbar, tc, data, _index) => {
          Toolbar.setGroups(tc, data);
        },
        shell: true
      })
    },
    name: "multiple-toolbar",
    schema: [
      required$1("dom"),
      required$1("onEscape")
    ]
  });
  const partToolbar = partType.optional({
    factory: {
      sketch: (spec) => {
        const renderer = toolbarFactory(spec);
        const toolbarSpec = {
          type: spec.type,
          uid: spec.uid,
          onEscape: () => {
            spec.onEscape();
            return Optional.some(true);
          },
          cyclicKeying: false,
          initGroups: [],
          getSink: spec.getSink,
          providers: spec.providers,
          moreDrawerData: {
            lazyToolbar: spec.lazyToolbar,
            lazyMoreButton: spec.lazyMoreButton,
            lazyHeader: spec.lazyHeader
          },
          attributes: spec.attributes
        };
        return renderer(toolbarSpec);
      }
    },
    name: "toolbar",
    schema: [
      required$1("dom"),
      required$1("onEscape"),
      required$1("getSink")
    ]
  });
  const partHeader = partType.optional({
    factory: { sketch: renderHeader },
    name: "header",
    schema: [required$1("dom")]
  });
  const partPromotion = partType.optional({
    factory: { sketch: renderPromotion },
    name: "promotion",
    schema: [required$1("dom")]
  });
  const partSocket = partType.optional({
    name: "socket",
    schema: [required$1("dom")]
  });
  const partSidebar = partType.optional({
    factory: { sketch: renderSidebar },
    name: "sidebar",
    schema: [required$1("dom")]
  });
  const partThrobber = partType.optional({
    factory: { sketch: renderThrobber },
    name: "throbber",
    schema: [required$1("dom")]
  });
  var OuterContainer = composite({
    name: "OuterContainer",
    factory: factory$6,
    configFields: [
      required$1("dom"),
      required$1("behaviours")
    ],
    partFields: [
      partHeader,
      partMenubar,
      partToolbar,
      partMultipleToolbar,
      partSocket,
      partSidebar,
      partPromotion,
      partThrobber
    ],
    apis: {
      getSocket: (apis, comp) => {
        return apis.getSocket(comp);
      },
      setSidebar: (apis, comp, panelConfigs, showSidebar) => {
        apis.setSidebar(comp, panelConfigs, showSidebar);
      },
      toggleSidebar: (apis, comp, name2) => {
        apis.toggleSidebar(comp, name2);
      },
      whichSidebar: (apis, comp) => {
        return apis.whichSidebar(comp);
      },
      getHeader: (apis, comp) => {
        return apis.getHeader(comp);
      },
      getToolbar: (apis, comp) => {
        return apis.getToolbar(comp);
      },
      setToolbar: (apis, comp, groups) => {
        apis.setToolbar(comp, groups);
      },
      setToolbars: (apis, comp, toolbars) => {
        apis.setToolbars(comp, toolbars);
      },
      refreshToolbar: (apis, comp) => {
        return apis.refreshToolbar(comp);
      },
      toggleToolbarDrawer: (apis, comp) => {
        apis.toggleToolbarDrawer(comp);
      },
      isToolbarDrawerToggled: (apis, comp) => {
        return apis.isToolbarDrawerToggled(comp);
      },
      getThrobber: (apis, comp) => {
        return apis.getThrobber(comp);
      },
      setMenubar: (apis, comp, menus) => {
        apis.setMenubar(comp, menus);
      },
      focusMenubar: (apis, comp) => {
        apis.focusMenubar(comp);
      },
      focusToolbar: (apis, comp) => {
        apis.focusToolbar(comp);
      }
    }
  });
  const defaultMenubar = "file edit view insert format tools table help";
  const defaultMenus = {
    file: {
      title: "File",
      items: "newdocument restoredraft | preview | export print | deleteallconversations"
    },
    edit: {
      title: "Edit",
      items: "undo redo | cut copy paste pastetext | selectall | searchreplace"
    },
    view: {
      title: "View",
      items: "code | visualaid visualchars visualblocks | spellchecker | preview fullscreen | showcomments"
    },
    insert: {
      title: "Insert",
      items: "image link media addcomment pageembed template codesample inserttable | charmap emoticons hr | pagebreak nonbreaking anchor tableofcontents footnotes | mergetags | insertdatetime"
    },
    format: {
      title: "Format",
      items: "bold italic underline strikethrough superscript subscript codeformat | styles blocks fontfamily fontsize align lineheight | forecolor backcolor | language | removeformat"
    },
    tools: {
      title: "Tools",
      items: "spellchecker spellcheckerlanguage | autocorrect capitalization | a11ycheck code wordcount"
    },
    table: {
      title: "Table",
      items: "inserttable | cell row column | advtablesort | tableprops deletetable"
    },
    help: {
      title: "Help",
      items: "help"
    }
  };
  const make = (menu2, registry, editor) => {
    const removedMenuItems = getRemovedMenuItems(editor).split(/[ ,]/);
    return {
      text: menu2.title,
      getItems: () => bind$3(menu2.items, (i2) => {
        const itemName = i2.toLowerCase();
        if (itemName.trim().length === 0) {
          return [];
        } else if (exists(removedMenuItems, (removedMenuItem) => removedMenuItem === itemName)) {
          return [];
        } else if (itemName === "separator" || itemName === "|") {
          return [{ type: "separator" }];
        } else if (registry.menuItems[itemName]) {
          return [registry.menuItems[itemName]];
        } else {
          return [];
        }
      })
    };
  };
  const parseItemsString = (items) => {
    return items.split(" ");
  };
  const identifyMenus = (editor, registry) => {
    const rawMenuData = {
      ...defaultMenus,
      ...registry.menus
    };
    const userDefinedMenus = keys(registry.menus).length > 0;
    const menubar = registry.menubar === void 0 || registry.menubar === true ? parseItemsString(defaultMenubar) : parseItemsString(registry.menubar === false ? "" : registry.menubar);
    const validMenus = filter$2(menubar, (menuName) => {
      const isDefaultMenu = has$2(defaultMenus, menuName);
      if (userDefinedMenus) {
        return isDefaultMenu || get$g(registry.menus, menuName).exists((menu2) => has$2(menu2, "items"));
      } else {
        return isDefaultMenu;
      }
    });
    const menus = map$2(validMenus, (menuName) => {
      const menuData = rawMenuData[menuName];
      return make({
        title: menuData.title,
        items: parseItemsString(menuData.items)
      }, registry, editor);
    });
    return filter$2(menus, (menu2) => {
      const isNotSeparator = (item2) => isString(item2) || item2.type !== "separator";
      return menu2.getItems().length > 0 && exists(menu2.getItems(), isNotSeparator);
    });
  };
  const fireSkinLoaded = (editor) => {
    const done = () => {
      editor._skinLoaded = true;
      fireSkinLoaded$1(editor);
    };
    return () => {
      if (editor.initialized) {
        done();
      } else {
        editor.on("init", done);
      }
    };
  };
  const fireSkinLoadError = (editor, err) => () => fireSkinLoadError$1(editor, { message: err });
  const loadStylesheet = (editor, stylesheetUrl, styleSheetLoader) => {
    editor.on("remove", () => styleSheetLoader.unload(stylesheetUrl));
    return styleSheetLoader.load(stylesheetUrl);
  };
  const loadUiSkins = (editor, skinUrl) => {
    const skinUiCss = skinUrl + "/skin.min.css";
    return loadStylesheet(editor, skinUiCss, editor.ui.styleSheetLoader);
  };
  const loadShadowDomUiSkins = (editor, skinUrl) => {
    const isInShadowRoot$1 = isInShadowRoot(SugarElement.fromDom(editor.getElement()));
    if (isInShadowRoot$1) {
      const shadowDomSkinCss = skinUrl + "/skin.shadowdom.min.css";
      return loadStylesheet(editor, shadowDomSkinCss, global$7.DOM.styleSheetLoader);
    } else {
      return Promise.resolve();
    }
  };
  const loadSkin = (isInline, editor) => {
    const skinUrl = getSkinUrl(editor);
    if (skinUrl) {
      editor.contentCSS.push(skinUrl + (isInline ? "/content.inline" : "/content") + ".min.css");
    }
    if (!isSkinDisabled(editor) && isString(skinUrl)) {
      Promise.all([
        loadUiSkins(editor, skinUrl),
        loadShadowDomUiSkins(editor, skinUrl)
      ]).then(fireSkinLoaded(editor), fireSkinLoadError(editor, "Skin could not be loaded"));
    } else {
      fireSkinLoaded(editor)();
    }
  };
  const iframe = curry(loadSkin, false);
  const inline = curry(loadSkin, true);
  const onSetupFormatToggle = (editor, name2) => (api2) => {
    const boundCallback = unbindable();
    const init2 = () => {
      api2.setActive(editor.formatter.match(name2));
      const binding = editor.formatter.formatChanged(name2, api2.setActive);
      boundCallback.set(binding);
    };
    editor.initialized ? init2() : editor.once("init", init2);
    return () => {
      editor.off("init", init2);
      boundCallback.clear();
    };
  };
  const onSetupEvent = (editor, event2, f) => (api2) => {
    const handleEvent = () => f(api2);
    const init2 = () => {
      f(api2);
      editor.on(event2, handleEvent);
    };
    editor.initialized ? init2() : editor.once("init", init2);
    return () => {
      editor.off("init", init2);
      editor.off(event2, handleEvent);
    };
  };
  const onActionToggleFormat$1 = (editor) => (rawItem) => () => {
    editor.undoManager.transact(() => {
      editor.focus();
      editor.execCommand("mceToggleFormat", false, rawItem.format);
    });
  };
  const onActionExecCommand = (editor, command) => () => editor.execCommand(command);
  const generateSelectItems = (_editor, backstage, spec) => {
    const generateItem = (rawItem, response, invalid, value2) => {
      const translatedText = backstage.shared.providers.translate(rawItem.title);
      if (rawItem.type === "separator") {
        return Optional.some({
          type: "separator",
          text: translatedText
        });
      } else if (rawItem.type === "submenu") {
        const items = bind$3(rawItem.getStyleItems(), (si) => validate(si, response, value2));
        if (response === 0 && items.length <= 0) {
          return Optional.none();
        } else {
          return Optional.some({
            type: "nestedmenuitem",
            text: translatedText,
            enabled: items.length > 0,
            getSubmenuItems: () => bind$3(rawItem.getStyleItems(), (si) => validate(si, response, value2))
          });
        }
      } else {
        return Optional.some({
          type: "togglemenuitem",
          text: translatedText,
          icon: rawItem.icon,
          active: rawItem.isSelected(value2),
          enabled: !invalid,
          onAction: spec.onAction(rawItem),
          ...rawItem.getStylePreview().fold(() => ({}), (preview) => ({ meta: { style: preview } }))
        });
      }
    };
    const validate = (item2, response, value2) => {
      const invalid = item2.type === "formatter" && spec.isInvalid(item2);
      if (response === 0) {
        return invalid ? [] : generateItem(item2, response, false, value2).toArray();
      } else {
        return generateItem(item2, response, invalid, value2).toArray();
      }
    };
    const validateItems = (preItems) => {
      const value2 = spec.getCurrentValue();
      const response = spec.shouldHide ? 0 : 1;
      return bind$3(preItems, (item2) => validate(item2, response, value2));
    };
    const getFetch2 = (backstage2, getStyleItems) => (comp, callback) => {
      const preItems = getStyleItems();
      const items = validateItems(preItems);
      const menu2 = build(items, ItemResponse$1.CLOSE_ON_EXECUTE, backstage2, {
        isHorizontalMenu: false,
        search: Optional.none()
      });
      callback(menu2);
    };
    return {
      validateItems,
      getFetch: getFetch2
    };
  };
  const createMenuItems = (editor, backstage, spec) => {
    const dataset2 = spec.dataset;
    const getStyleItems = dataset2.type === "basic" ? () => map$2(dataset2.data, (d) => processBasic(d, spec.isSelectedFor, spec.getPreviewFor)) : dataset2.getData;
    return {
      items: generateSelectItems(editor, backstage, spec),
      getStyleItems
    };
  };
  const createSelectButton = (editor, backstage, spec) => {
    const { items, getStyleItems } = createMenuItems(editor, backstage, spec);
    const getApi2 = (comp) => ({ getComponent: constant$1(comp) });
    const onSetup2 = onSetupEvent(editor, "NodeChange", (api2) => {
      const comp = api2.getComponent();
      spec.updateText(comp);
    });
    return renderCommonDropdown({
      text: spec.icon.isSome() ? Optional.none() : spec.text,
      icon: spec.icon,
      tooltip: Optional.from(spec.tooltip),
      role: Optional.none(),
      fetch: items.getFetch(backstage, getStyleItems),
      onSetup: onSetup2,
      getApi: getApi2,
      columns: 1,
      presets: "normal",
      classes: spec.icon.isSome() ? [] : ["bespoke"],
      dropdownBehaviours: []
    }, "tox-tbtn", backstage.shared);
  };
  const process = (rawFormats) => map$2(rawFormats, (item2) => {
    let title2 = item2, format = item2;
    const values2 = item2.split("=");
    if (values2.length > 1) {
      title2 = values2[0];
      format = values2[1];
    }
    return {
      title: title2,
      format
    };
  });
  const buildBasicStaticDataset = (data) => ({
    type: "basic",
    data
  });
  var Delimiter;
  (function(Delimiter2) {
    Delimiter2[Delimiter2["SemiColon"] = 0] = "SemiColon";
    Delimiter2[Delimiter2["Space"] = 1] = "Space";
  })(Delimiter || (Delimiter = {}));
  const split = (rawFormats, delimiter) => {
    if (delimiter === Delimiter.SemiColon) {
      return rawFormats.replace(/;$/, "").split(";");
    } else {
      return rawFormats.split(" ");
    }
  };
  const buildBasicSettingsDataset = (editor, settingName, delimiter) => {
    const rawFormats = editor.options.get(settingName);
    const data = process(split(rawFormats, delimiter));
    return {
      type: "basic",
      data
    };
  };
  const alignMenuItems = [
    {
      title: "Left",
      icon: "align-left",
      format: "alignleft",
      command: "JustifyLeft"
    },
    {
      title: "Center",
      icon: "align-center",
      format: "aligncenter",
      command: "JustifyCenter"
    },
    {
      title: "Right",
      icon: "align-right",
      format: "alignright",
      command: "JustifyRight"
    },
    {
      title: "Justify",
      icon: "align-justify",
      format: "alignjustify",
      command: "JustifyFull"
    }
  ];
  const getSpec$4 = (editor) => {
    const getMatchingValue = () => find$5(alignMenuItems, (item2) => editor.formatter.match(item2.format));
    const isSelectedFor = (format) => () => editor.formatter.match(format);
    const getPreviewFor = (_format) => Optional.none;
    const updateSelectMenuIcon = (comp) => {
      const match = getMatchingValue();
      const alignment = match.fold(constant$1("left"), (item2) => item2.title.toLowerCase());
      emitWith(comp, updateMenuIcon, { icon: `align-${alignment}` });
    };
    const dataset2 = buildBasicStaticDataset(alignMenuItems);
    const onAction2 = (rawItem) => () => find$5(alignMenuItems, (item2) => item2.format === rawItem.format).each((item2) => editor.execCommand(item2.command));
    return {
      tooltip: "Align",
      text: Optional.none(),
      icon: Optional.some("align-left"),
      isSelectedFor,
      getCurrentValue: Optional.none,
      getPreviewFor,
      onAction: onAction2,
      updateText: updateSelectMenuIcon,
      dataset: dataset2,
      shouldHide: false,
      isInvalid: (item2) => !editor.formatter.canApply(item2.format)
    };
  };
  const createAlignButton = (editor, backstage) => createSelectButton(editor, backstage, getSpec$4(editor));
  const createAlignMenu = (editor, backstage) => {
    const menuItems = createMenuItems(editor, backstage, getSpec$4(editor));
    editor.ui.registry.addNestedMenuItem("align", {
      text: backstage.shared.providers.translate("Align"),
      getSubmenuItems: () => menuItems.items.validateItems(menuItems.getStyleItems())
    });
  };
  const findNearest = (editor, getStyles) => {
    const styles = getStyles();
    const formats = map$2(styles, (style) => style.format);
    return Optional.from(editor.formatter.closest(formats)).bind((fmt) => find$5(styles, (data) => data.format === fmt)).orThunk(() => someIf(editor.formatter.match("p"), {
      title: "Paragraph",
      format: "p"
    }));
  };
  const getSpec$3 = (editor) => {
    const fallbackFormat = "Paragraph";
    const isSelectedFor = (format) => () => editor.formatter.match(format);
    const getPreviewFor = (format) => () => {
      const fmt = editor.formatter.get(format);
      if (fmt) {
        return Optional.some({
          tag: fmt.length > 0 ? fmt[0].inline || fmt[0].block || "div" : "div",
          styles: editor.dom.parseStyle(editor.formatter.getCssText(format))
        });
      } else {
        return Optional.none();
      }
    };
    const updateSelectMenuText = (comp) => {
      const detectedFormat = findNearest(editor, () => dataset2.data);
      const text2 = detectedFormat.fold(constant$1(fallbackFormat), (fmt) => fmt.title);
      emitWith(comp, updateMenuText, { text: text2 });
    };
    const dataset2 = buildBasicSettingsDataset(editor, "block_formats", Delimiter.SemiColon);
    return {
      tooltip: "Blocks",
      text: Optional.some(fallbackFormat),
      icon: Optional.none(),
      isSelectedFor,
      getCurrentValue: Optional.none,
      getPreviewFor,
      onAction: onActionToggleFormat$1(editor),
      updateText: updateSelectMenuText,
      dataset: dataset2,
      shouldHide: false,
      isInvalid: (item2) => !editor.formatter.canApply(item2.format)
    };
  };
  const createBlocksButton = (editor, backstage) => createSelectButton(editor, backstage, getSpec$3(editor));
  const createBlocksMenu = (editor, backstage) => {
    const menuItems = createMenuItems(editor, backstage, getSpec$3(editor));
    editor.ui.registry.addNestedMenuItem("blocks", {
      text: "Blocks",
      getSubmenuItems: () => menuItems.items.validateItems(menuItems.getStyleItems())
    });
  };
  const systemStackFonts = [
    "-apple-system",
    "Segoe UI",
    "Roboto",
    "Helvetica Neue",
    "sans-serif"
  ];
  const splitFonts = (fontFamily) => {
    const fonts = fontFamily.split(/\s*,\s*/);
    return map$2(fonts, (font) => font.replace(/^['"]+|['"]+$/g, ""));
  };
  const isSystemFontStack = (fontFamily) => {
    const matchesSystemStack = () => {
      const fonts = splitFonts(fontFamily.toLowerCase());
      return forall(systemStackFonts, (font) => fonts.indexOf(font.toLowerCase()) > -1);
    };
    return fontFamily.indexOf("-apple-system") === 0 && matchesSystemStack();
  };
  const getSpec$2 = (editor) => {
    const systemFont = "System Font";
    const getMatchingValue = () => {
      const getFirstFont = (fontFamily2) => fontFamily2 ? splitFonts(fontFamily2)[0] : "";
      const fontFamily = editor.queryCommandValue("FontName");
      const items = dataset2.data;
      const font = fontFamily ? fontFamily.toLowerCase() : "";
      const matchOpt = find$5(items, (item2) => {
        const format = item2.format;
        return format.toLowerCase() === font || getFirstFont(format).toLowerCase() === getFirstFont(font).toLowerCase();
      }).orThunk(() => {
        return someIf(isSystemFontStack(font), {
          title: systemFont,
          format: font
        });
      });
      return {
        matchOpt,
        font: fontFamily
      };
    };
    const isSelectedFor = (item2) => (valueOpt) => valueOpt.exists((value2) => value2.format === item2);
    const getCurrentValue = () => {
      const { matchOpt } = getMatchingValue();
      return matchOpt;
    };
    const getPreviewFor = (item2) => () => Optional.some({
      tag: "div",
      styles: item2.indexOf("dings") === -1 ? { "font-family": item2 } : {}
    });
    const onAction2 = (rawItem) => () => {
      editor.undoManager.transact(() => {
        editor.focus();
        editor.execCommand("FontName", false, rawItem.format);
      });
    };
    const updateSelectMenuText = (comp) => {
      const { matchOpt, font } = getMatchingValue();
      const text2 = matchOpt.fold(constant$1(font), (item2) => item2.title);
      emitWith(comp, updateMenuText, { text: text2 });
    };
    const dataset2 = buildBasicSettingsDataset(editor, "font_family_formats", Delimiter.SemiColon);
    return {
      tooltip: "Fonts",
      text: Optional.some(systemFont),
      icon: Optional.none(),
      isSelectedFor,
      getCurrentValue,
      getPreviewFor,
      onAction: onAction2,
      updateText: updateSelectMenuText,
      dataset: dataset2,
      shouldHide: false,
      isInvalid: never
    };
  };
  const createFontFamilyButton = (editor, backstage) => createSelectButton(editor, backstage, getSpec$2(editor));
  const createFontFamilyMenu = (editor, backstage) => {
    const menuItems = createMenuItems(editor, backstage, getSpec$2(editor));
    editor.ui.registry.addNestedMenuItem("fontfamily", {
      text: backstage.shared.providers.translate("Fonts"),
      getSubmenuItems: () => menuItems.items.validateItems(menuItems.getStyleItems())
    });
  };
  const legacyFontSizes = {
    "8pt": "1",
    "10pt": "2",
    "12pt": "3",
    "14pt": "4",
    "18pt": "5",
    "24pt": "6",
    "36pt": "7"
  };
  const keywordFontSizes = {
    "xx-small": "7pt",
    "x-small": "8pt",
    "small": "10pt",
    "medium": "12pt",
    "large": "14pt",
    "x-large": "18pt",
    "xx-large": "24pt"
  };
  const round = (number2, precision) => {
    const factor = Math.pow(10, precision);
    return Math.round(number2 * factor) / factor;
  };
  const toPt = (fontSize, precision) => {
    if (/[0-9.]+px$/.test(fontSize)) {
      return round(parseInt(fontSize, 10) * 72 / 96, precision || 0) + "pt";
    } else {
      return get$g(keywordFontSizes, fontSize).getOr(fontSize);
    }
  };
  const toLegacy = (fontSize) => get$g(legacyFontSizes, fontSize).getOr("");
  const getSpec$1 = (editor) => {
    const getMatchingValue = () => {
      let matchOpt = Optional.none();
      const items = dataset2.data;
      const fontSize = editor.queryCommandValue("FontSize");
      if (fontSize) {
        for (let precision = 3; matchOpt.isNone() && precision >= 0; precision--) {
          const pt = toPt(fontSize, precision);
          const legacy = toLegacy(pt);
          matchOpt = find$5(items, (item2) => item2.format === fontSize || item2.format === pt || item2.format === legacy);
        }
      }
      return {
        matchOpt,
        size: fontSize
      };
    };
    const isSelectedFor = (item2) => (valueOpt) => valueOpt.exists((value2) => value2.format === item2);
    const getCurrentValue = () => {
      const { matchOpt } = getMatchingValue();
      return matchOpt;
    };
    const getPreviewFor = constant$1(Optional.none);
    const onAction2 = (rawItem) => () => {
      editor.undoManager.transact(() => {
        editor.focus();
        editor.execCommand("FontSize", false, rawItem.format);
      });
    };
    const updateSelectMenuText = (comp) => {
      const { matchOpt, size } = getMatchingValue();
      const text2 = matchOpt.fold(constant$1(size), (match) => match.title);
      emitWith(comp, updateMenuText, { text: text2 });
    };
    const dataset2 = buildBasicSettingsDataset(editor, "font_size_formats", Delimiter.Space);
    return {
      tooltip: "Font sizes",
      text: Optional.some("12pt"),
      icon: Optional.none(),
      isSelectedFor,
      getPreviewFor,
      getCurrentValue,
      onAction: onAction2,
      updateText: updateSelectMenuText,
      dataset: dataset2,
      shouldHide: false,
      isInvalid: never
    };
  };
  const createFontSizeButton = (editor, backstage) => createSelectButton(editor, backstage, getSpec$1(editor));
  const createFontSizeMenu = (editor, backstage) => {
    const menuItems = createMenuItems(editor, backstage, getSpec$1(editor));
    editor.ui.registry.addNestedMenuItem("fontsize", {
      text: "Font sizes",
      getSubmenuItems: () => menuItems.items.validateItems(menuItems.getStyleItems())
    });
  };
  const getSpec = (editor, dataset2) => {
    const fallbackFormat = "Paragraph";
    const isSelectedFor = (format) => () => editor.formatter.match(format);
    const getPreviewFor = (format) => () => {
      const fmt = editor.formatter.get(format);
      return fmt !== void 0 ? Optional.some({
        tag: fmt.length > 0 ? fmt[0].inline || fmt[0].block || "div" : "div",
        styles: editor.dom.parseStyle(editor.formatter.getCssText(format))
      }) : Optional.none();
    };
    const updateSelectMenuText = (comp) => {
      const getFormatItems = (fmt) => {
        if (isNestedFormat(fmt)) {
          return bind$3(fmt.items, getFormatItems);
        } else if (isFormatReference(fmt)) {
          return [{
            title: fmt.title,
            format: fmt.format
          }];
        } else {
          return [];
        }
      };
      const flattenedItems = bind$3(getStyleFormats(editor), getFormatItems);
      const detectedFormat = findNearest(editor, constant$1(flattenedItems));
      const text2 = detectedFormat.fold(constant$1(fallbackFormat), (fmt) => fmt.title);
      emitWith(comp, updateMenuText, { text: text2 });
    };
    return {
      tooltip: "Formats",
      text: Optional.some(fallbackFormat),
      icon: Optional.none(),
      isSelectedFor,
      getCurrentValue: Optional.none,
      getPreviewFor,
      onAction: onActionToggleFormat$1(editor),
      updateText: updateSelectMenuText,
      shouldHide: shouldAutoHideStyleFormats(editor),
      isInvalid: (item2) => !editor.formatter.canApply(item2.format),
      dataset: dataset2
    };
  };
  const createStylesButton = (editor, backstage) => {
    const dataset2 = {
      type: "advanced",
      ...backstage.styles
    };
    return createSelectButton(editor, backstage, getSpec(editor, dataset2));
  };
  const createStylesMenu = (editor, backstage) => {
    const dataset2 = {
      type: "advanced",
      ...backstage.styles
    };
    const menuItems = createMenuItems(editor, backstage, getSpec(editor, dataset2));
    editor.ui.registry.addNestedMenuItem("styles", {
      text: "Formats",
      getSubmenuItems: () => menuItems.items.validateItems(menuItems.getStyleItems())
    });
  };
  const events$3 = (reflectingConfig, reflectingState) => {
    const update = (component, data) => {
      reflectingConfig.updateState.each((updateState) => {
        const newState = updateState(component, data);
        reflectingState.set(newState);
      });
      reflectingConfig.renderComponents.each((renderComponents2) => {
        const newComponents = renderComponents2(data, reflectingState.get());
        const replacer = reflectingConfig.reuseDom ? withReuse : withoutReuse;
        replacer(component, newComponents);
      });
    };
    return derive$2([
      run$1(receive(), (component, message) => {
        const receivingData = message;
        if (!receivingData.universal) {
          const channel = reflectingConfig.channel;
          if (contains$2(receivingData.channels, channel)) {
            update(component, receivingData.data);
          }
        }
      }),
      runOnAttached((comp, _se) => {
        reflectingConfig.initialData.each((rawData) => {
          update(comp, rawData);
        });
      })
    ]);
  };
  var ActiveReflecting = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    events: events$3
  });
  const getState = (component, replaceConfig, reflectState) => reflectState;
  var ReflectingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    getState
  });
  var ReflectingSchema = [
    required$1("channel"),
    option$3("renderComponents"),
    option$3("updateState"),
    option$3("initialData"),
    defaultedBoolean("reuseDom", true)
  ];
  const init$3 = () => {
    const cell = Cell(Optional.none());
    const clear2 = () => cell.set(Optional.none());
    const readState = () => cell.get().getOr("none");
    return {
      readState,
      get: cell.get,
      set: cell.set,
      clear: clear2
    };
  };
  var ReflectingState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init: init$3
  });
  const Reflecting = create$3({
    fields: ReflectingSchema,
    name: "reflecting",
    active: ActiveReflecting,
    apis: ReflectingApis,
    state: ReflectingState
  });
  const schema$7 = constant$1([
    required$1("toggleClass"),
    required$1("fetch"),
    onStrictHandler("onExecute"),
    defaulted("getHotspot", Optional.some),
    defaulted("getAnchorOverrides", constant$1({})),
    schema$y(),
    onStrictHandler("onItemExecute"),
    option$3("lazySink"),
    required$1("dom"),
    onHandler("onOpen"),
    field("splitDropdownBehaviours", [
      Coupling,
      Keying,
      Focusing
    ]),
    defaulted("matchWidth", false),
    defaulted("useMinWidth", false),
    defaulted("eventOrder", {}),
    option$3("role")
  ].concat(sandboxFields()));
  const arrowPart = required({
    factory: Button,
    schema: [required$1("dom")],
    name: "arrow",
    defaults: () => {
      return { buttonBehaviours: derive$1([Focusing.revoke()]) };
    },
    overrides: (detail) => {
      return {
        dom: {
          tag: "span",
          attributes: { role: "presentation" }
        },
        action: (arrow) => {
          arrow.getSystem().getByUid(detail.uid).each(emitExecute);
        },
        buttonBehaviours: derive$1([Toggling.config({
          toggleOnExecute: false,
          toggleClass: detail.toggleClass
        })])
      };
    }
  });
  const buttonPart = required({
    factory: Button,
    schema: [required$1("dom")],
    name: "button",
    defaults: () => {
      return { buttonBehaviours: derive$1([Focusing.revoke()]) };
    },
    overrides: (detail) => {
      return {
        dom: {
          tag: "span",
          attributes: { role: "presentation" }
        },
        action: (btn) => {
          btn.getSystem().getByUid(detail.uid).each((splitDropdown) => {
            detail.onExecute(splitDropdown, btn);
          });
        }
      };
    }
  });
  const parts$3 = constant$1([
    arrowPart,
    buttonPart,
    optional({
      factory: {
        sketch: (spec) => {
          return {
            uid: spec.uid,
            dom: {
              tag: "span",
              styles: { display: "none" },
              attributes: { "aria-hidden": "true" },
              innerHtml: spec.text
            }
          };
        }
      },
      schema: [required$1("text")],
      name: "aria-descriptor"
    }),
    external({
      schema: [tieredMenuMarkers()],
      name: "menu",
      defaults: (detail) => {
        return {
          onExecute: (tmenu, item2) => {
            tmenu.getSystem().getByUid(detail.uid).each((splitDropdown) => {
              detail.onItemExecute(splitDropdown, tmenu, item2);
            });
          }
        };
      }
    }),
    partType$1()
  ]);
  const factory$5 = (detail, components2, spec, externals) => {
    const switchToMenu = (sandbox) => {
      Composing.getCurrent(sandbox).each((current) => {
        Highlighting.highlightFirst(current);
        Keying.focusIn(current);
      });
    };
    const action = (component) => {
      const onOpenSync = switchToMenu;
      togglePopup(detail, identity, component, externals, onOpenSync, HighlightOnOpen.HighlightMenuAndItem).get(noop);
    };
    const openMenu = (comp) => {
      action(comp);
      return Optional.some(true);
    };
    const executeOnButton = (comp) => {
      const button2 = getPartOrDie(comp, detail, "button");
      emitExecute(button2);
      return Optional.some(true);
    };
    const buttonEvents = {
      ...derive$2([runOnAttached((component, _simulatedEvent) => {
        const ariaDescriptor = getPart(component, detail, "aria-descriptor");
        ariaDescriptor.each((descriptor) => {
          const descriptorId = generate$6("aria");
          set$9(descriptor.element, "id", descriptorId);
          set$9(component.element, "aria-describedby", descriptorId);
        });
      })]),
      ...events$a(Optional.some(action))
    };
    const apis = {
      repositionMenus: (comp) => {
        if (Toggling.isOn(comp)) {
          repositionMenus(comp);
        }
      }
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      apis,
      eventOrder: {
        ...detail.eventOrder,
        [execute$5()]: [
          "disabling",
          "toggling",
          "alloy.base.behaviour"
        ]
      },
      events: buttonEvents,
      behaviours: augment(detail.splitDropdownBehaviours, [
        Coupling.config({
          others: {
            sandbox: (hotspot) => {
              const arrow = getPartOrDie(hotspot, detail, "arrow");
              const extras = {
                onOpen: () => {
                  Toggling.on(arrow);
                  Toggling.on(hotspot);
                },
                onClose: () => {
                  Toggling.off(arrow);
                  Toggling.off(hotspot);
                }
              };
              return makeSandbox$1(detail, hotspot, extras);
            }
          }
        }),
        Keying.config({
          mode: "special",
          onSpace: executeOnButton,
          onEnter: executeOnButton,
          onDown: openMenu
        }),
        Focusing.config({}),
        Toggling.config({
          toggleOnExecute: false,
          aria: { mode: "expanded" }
        })
      ]),
      domModification: {
        attributes: {
          "role": detail.role.getOr("button"),
          "aria-haspopup": true
        }
      }
    };
  };
  const SplitDropdown = composite({
    name: "SplitDropdown",
    configFields: schema$7(),
    partFields: parts$3(),
    factory: factory$5,
    apis: { repositionMenus: (apis, comp) => apis.repositionMenus(comp) }
  });
  const getButtonApi = (component) => ({
    isEnabled: () => !Disabling.isDisabled(component),
    setEnabled: (state) => Disabling.set(component, !state)
  });
  const getToggleApi = (component) => ({
    setActive: (state) => {
      Toggling.set(component, state);
    },
    isActive: () => Toggling.isOn(component),
    isEnabled: () => !Disabling.isDisabled(component),
    setEnabled: (state) => Disabling.set(component, !state)
  });
  const getTooltipAttributes = (tooltip, providersBackstage) => tooltip.map((tooltip2) => ({
    "aria-label": providersBackstage.translate(tooltip2),
    "title": providersBackstage.translate(tooltip2)
  })).getOr({});
  const focusButtonEvent = generate$6("focus-button");
  const renderCommonStructure = (icon2, text2, tooltip, receiver, behaviours2, providersBackstage) => {
    return {
      dom: {
        tag: "button",
        classes: ["tox-tbtn"].concat(text2.isSome() ? ["tox-tbtn--select"] : []),
        attributes: getTooltipAttributes(tooltip, providersBackstage)
      },
      components: componentRenderPipeline([
        icon2.map((iconName) => renderIconFromPack(iconName, providersBackstage.icons)),
        text2.map((text3) => renderLabel(text3, "tox-tbtn", providersBackstage))
      ]),
      eventOrder: {
        [mousedown()]: [
          "focusing",
          "alloy.base.behaviour",
          "common-button-display-events"
        ]
      },
      buttonBehaviours: derive$1([
        DisablingConfigs.toolbarButton(providersBackstage.isDisabled),
        receivingConfig(),
        config("common-button-display-events", [run$1(mousedown(), (button2, se) => {
          se.event.prevent();
          emit(button2, focusButtonEvent);
        })])
      ].concat(receiver.map((r2) => Reflecting.config({
        channel: r2,
        initialData: {
          icon: icon2,
          text: text2
        },
        renderComponents: (data, _state) => componentRenderPipeline([
          data.icon.map((iconName) => renderIconFromPack(iconName, providersBackstage.icons)),
          data.text.map((text3) => renderLabel(text3, "tox-tbtn", providersBackstage))
        ])
      })).toArray()).concat(behaviours2.getOr([])))
    };
  };
  const renderFloatingToolbarButton = (spec, backstage, identifyButtons2, attributes) => {
    const sharedBackstage = backstage.shared;
    return FloatingToolbarButton.sketch({
      lazySink: sharedBackstage.getSink,
      fetch: () => Future.nu((resolve2) => {
        resolve2(map$2(identifyButtons2(spec.items), renderToolbarGroup));
      }),
      markers: { toggledClass: "tox-tbtn--enabled" },
      parts: {
        button: renderCommonStructure(spec.icon, spec.text, spec.tooltip, Optional.none(), Optional.none(), sharedBackstage.providers),
        toolbar: {
          dom: {
            tag: "div",
            classes: ["tox-toolbar__overflow"],
            attributes
          }
        }
      }
    });
  };
  const renderCommonToolbarButton = (spec, specialisation, providersBackstage) => {
    const editorOffCell = Cell(noop);
    const structure = renderCommonStructure(spec.icon, spec.text, spec.tooltip, Optional.none(), Optional.none(), providersBackstage);
    return Button.sketch({
      dom: structure.dom,
      components: structure.components,
      eventOrder: toolbarButtonEventOrder,
      buttonBehaviours: derive$1([
        config("toolbar-button-events", [
          onToolbarButtonExecute({
            onAction: spec.onAction,
            getApi: specialisation.getApi
          }),
          onControlAttached(specialisation, editorOffCell),
          onControlDetached(specialisation, editorOffCell)
        ]),
        DisablingConfigs.toolbarButton(() => !spec.enabled || providersBackstage.isDisabled()),
        receivingConfig()
      ].concat(specialisation.toolbarButtonBehaviours))
    });
  };
  const renderToolbarButton = (spec, providersBackstage) => renderToolbarButtonWith(spec, providersBackstage, []);
  const renderToolbarButtonWith = (spec, providersBackstage, bonusEvents) => renderCommonToolbarButton(spec, {
    toolbarButtonBehaviours: bonusEvents.length > 0 ? [config("toolbarButtonWith", bonusEvents)] : [],
    getApi: getButtonApi,
    onSetup: spec.onSetup
  }, providersBackstage);
  const renderToolbarToggleButton = (spec, providersBackstage) => renderToolbarToggleButtonWith(spec, providersBackstage, []);
  const renderToolbarToggleButtonWith = (spec, providersBackstage, bonusEvents) => renderCommonToolbarButton(spec, {
    toolbarButtonBehaviours: [
      Replacing.config({}),
      Toggling.config({
        toggleClass: "tox-tbtn--enabled",
        aria: { mode: "pressed" },
        toggleOnExecute: false
      })
    ].concat(bonusEvents.length > 0 ? [config("toolbarToggleButtonWith", bonusEvents)] : []),
    getApi: getToggleApi,
    onSetup: spec.onSetup
  }, providersBackstage);
  const fetchChoices = (getApi2, spec, providersBackstage) => (comp) => Future.nu((callback) => spec.fetch(callback)).map((items) => Optional.from(createTieredDataFrom(deepMerge(createPartialChoiceMenu(generate$6("menu-value"), items, (value2) => {
    spec.onItemAction(getApi2(comp), value2);
  }, spec.columns, spec.presets, ItemResponse$1.CLOSE_ON_EXECUTE, spec.select.getOr(never), providersBackstage), {
    movement: deriveMenuMovement(spec.columns, spec.presets),
    menuBehaviours: SimpleBehaviours.unnamedEvents(spec.columns !== "auto" ? [] : [runOnAttached((comp2, _se) => {
      detectSize(comp2, 4, classForPreset(spec.presets)).each(({ numRows, numColumns }) => {
        Keying.setGridSize(comp2, numRows, numColumns);
      });
    })])
  }))));
  const renderSplitButton = (spec, sharedBackstage) => {
    const displayChannel = generate$6("channel-update-split-dropdown-display");
    const getApi2 = (comp) => ({
      isEnabled: () => !Disabling.isDisabled(comp),
      setEnabled: (state) => Disabling.set(comp, !state),
      setIconFill: (id, value2) => {
        descendant(comp.element, 'svg path[id="' + id + '"], rect[id="' + id + '"]').each((underlinePath) => {
          set$9(underlinePath, "fill", value2);
        });
      },
      setActive: (state) => {
        set$9(comp.element, "aria-pressed", state);
        descendant(comp.element, "span").each((button2) => {
          comp.getSystem().getByDom(button2).each((buttonComp) => Toggling.set(buttonComp, state));
        });
      },
      isActive: () => descendant(comp.element, "span").exists((button2) => comp.getSystem().getByDom(button2).exists(Toggling.isOn))
    });
    const editorOffCell = Cell(noop);
    const specialisation = {
      getApi: getApi2,
      onSetup: spec.onSetup
    };
    return SplitDropdown.sketch({
      dom: {
        tag: "div",
        classes: ["tox-split-button"],
        attributes: {
          "aria-pressed": false,
          ...getTooltipAttributes(spec.tooltip, sharedBackstage.providers)
        }
      },
      onExecute: (button2) => {
        spec.onAction(getApi2(button2));
      },
      onItemExecute: (_a, _b, _c) => {
      },
      splitDropdownBehaviours: derive$1([
        DisablingConfigs.splitButton(sharedBackstage.providers.isDisabled),
        receivingConfig(),
        config("split-dropdown-events", [
          run$1(focusButtonEvent, Focusing.focus),
          onControlAttached(specialisation, editorOffCell),
          onControlDetached(specialisation, editorOffCell)
        ]),
        Unselecting.config({})
      ]),
      eventOrder: {
        [attachedToDom()]: [
          "alloy.base.behaviour",
          "split-dropdown-events"
        ]
      },
      toggleClass: "tox-tbtn--enabled",
      lazySink: sharedBackstage.getSink,
      fetch: fetchChoices(getApi2, spec, sharedBackstage.providers),
      parts: { menu: part(false, spec.columns, spec.presets) },
      components: [
        SplitDropdown.parts.button(renderCommonStructure(spec.icon, spec.text, Optional.none(), Optional.some(displayChannel), Optional.some([Toggling.config({
          toggleClass: "tox-tbtn--enabled",
          toggleOnExecute: false
        })]), sharedBackstage.providers)),
        SplitDropdown.parts.arrow({
          dom: {
            tag: "button",
            classes: [
              "tox-tbtn",
              "tox-split-button__chevron"
            ],
            innerHtml: get$2("chevron-down", sharedBackstage.providers.icons)
          },
          buttonBehaviours: derive$1([
            DisablingConfigs.splitButton(sharedBackstage.providers.isDisabled),
            receivingConfig(),
            addFocusableBehaviour()
          ])
        }),
        SplitDropdown.parts["aria-descriptor"]({ text: sharedBackstage.providers.translate("To open the popup, press Shift+Enter") })
      ]
    });
  };
  const defaultToolbar = [
    {
      name: "history",
      items: [
        "undo",
        "redo"
      ]
    },
    {
      name: "styles",
      items: ["styles"]
    },
    {
      name: "formatting",
      items: [
        "bold",
        "italic"
      ]
    },
    {
      name: "alignment",
      items: [
        "alignleft",
        "aligncenter",
        "alignright",
        "alignjustify"
      ]
    },
    {
      name: "indentation",
      items: [
        "outdent",
        "indent"
      ]
    },
    {
      name: "permanent pen",
      items: ["permanentpen"]
    },
    {
      name: "comments",
      items: ["addcomment"]
    }
  ];
  const renderFromBridge = (bridgeBuilder, render2) => (spec, backstage, editor) => {
    const internal = bridgeBuilder(spec).mapError((errInfo) => formatError(errInfo)).getOrDie();
    return render2(internal, backstage, editor);
  };
  const types = {
    button: renderFromBridge(createToolbarButton, (s, backstage) => renderToolbarButton(s, backstage.shared.providers)),
    togglebutton: renderFromBridge(createToggleButton, (s, backstage) => renderToolbarToggleButton(s, backstage.shared.providers)),
    menubutton: renderFromBridge(createMenuButton, (s, backstage) => renderMenuButton(s, "tox-tbtn", backstage, Optional.none())),
    splitbutton: renderFromBridge(createSplitButton, (s, backstage) => renderSplitButton(s, backstage.shared)),
    grouptoolbarbutton: renderFromBridge(createGroupToolbarButton, (s, backstage, editor) => {
      const buttons = editor.ui.registry.getAll().buttons;
      const identify = (toolbar) => identifyButtons(editor, {
        buttons,
        toolbar,
        allowToolbarGroups: false
      }, backstage, Optional.none());
      const attributes = { [Attribute]: backstage.shared.header.isPositionedAtTop() ? AttributeValue.TopToBottom : AttributeValue.BottomToTop };
      switch (getToolbarMode(editor)) {
        case ToolbarMode$1.floating:
          return renderFloatingToolbarButton(s, backstage, identify, attributes);
        default:
          throw new Error("Toolbar groups are only supported when using floating toolbar mode");
      }
    })
  };
  const extractFrom = (spec, backstage, editor) => get$g(types, spec.type).fold(() => {
    console.error("skipping button defined by", spec);
    return Optional.none();
  }, (render2) => Optional.some(render2(spec, backstage, editor)));
  const bespokeButtons = {
    styles: createStylesButton,
    fontsize: createFontSizeButton,
    fontfamily: createFontFamilyButton,
    blocks: createBlocksButton,
    align: createAlignButton
  };
  const removeUnusedDefaults = (buttons) => {
    const filteredItemGroups = map$2(defaultToolbar, (group2) => {
      const items = filter$2(group2.items, (subItem) => has$2(buttons, subItem) || has$2(bespokeButtons, subItem));
      return {
        name: group2.name,
        items
      };
    });
    return filter$2(filteredItemGroups, (group2) => group2.items.length > 0);
  };
  const convertStringToolbar = (strToolbar) => {
    const groupsStrings = strToolbar.split("|");
    return map$2(groupsStrings, (g) => ({ items: g.trim().split(" ") }));
  };
  const isToolbarGroupSettingArray = (toolbar) => isArrayOf(toolbar, (t2) => has$2(t2, "name") && has$2(t2, "items"));
  const createToolbar = (toolbarConfig) => {
    const toolbar = toolbarConfig.toolbar;
    const buttons = toolbarConfig.buttons;
    if (toolbar === false) {
      return [];
    } else if (toolbar === void 0 || toolbar === true) {
      return removeUnusedDefaults(buttons);
    } else if (isString(toolbar)) {
      return convertStringToolbar(toolbar);
    } else if (isToolbarGroupSettingArray(toolbar)) {
      return toolbar;
    } else {
      console.error("Toolbar type should be string, string[], boolean or ToolbarGroup[]");
      return [];
    }
  };
  const lookupButton = (editor, buttons, toolbarItem, allowToolbarGroups, backstage, prefixes) => get$g(buttons, toolbarItem.toLowerCase()).orThunk(() => prefixes.bind((ps) => findMap(ps, (prefix2) => get$g(buttons, prefix2 + toolbarItem.toLowerCase())))).fold(() => get$g(bespokeButtons, toolbarItem.toLowerCase()).map((r2) => r2(editor, backstage)), (spec) => {
    if (spec.type === "grouptoolbarbutton" && !allowToolbarGroups) {
      console.warn(`Ignoring the '${toolbarItem}' toolbar button. Group toolbar buttons are only supported when using floating toolbar mode and cannot be nested.`);
      return Optional.none();
    } else {
      return extractFrom(spec, backstage, editor);
    }
  });
  const identifyButtons = (editor, toolbarConfig, backstage, prefixes) => {
    const toolbarGroups = createToolbar(toolbarConfig);
    const groups = map$2(toolbarGroups, (group2) => {
      const items = bind$3(group2.items, (toolbarItem) => {
        return toolbarItem.trim().length === 0 ? [] : lookupButton(editor, toolbarConfig.buttons, toolbarItem, toolbarConfig.allowToolbarGroups, backstage, prefixes).toArray();
      });
      return {
        title: Optional.from(editor.translate(group2.name)),
        items
      };
    });
    return filter$2(groups, (group2) => group2.items.length > 0);
  };
  const setToolbar = (editor, uiComponents, rawUiConfig, backstage) => {
    const comp = uiComponents.outerContainer;
    const toolbarConfig = rawUiConfig.toolbar;
    const toolbarButtonsConfig = rawUiConfig.buttons;
    if (isArrayOf(toolbarConfig, isString)) {
      const toolbars = toolbarConfig.map((t2) => {
        const config2 = {
          toolbar: t2,
          buttons: toolbarButtonsConfig,
          allowToolbarGroups: rawUiConfig.allowToolbarGroups
        };
        return identifyButtons(editor, config2, backstage, Optional.none());
      });
      OuterContainer.setToolbars(comp, toolbars);
    } else {
      OuterContainer.setToolbar(comp, identifyButtons(editor, rawUiConfig, backstage, Optional.none()));
    }
  };
  const detection = detect$1();
  const isiOS12 = detection.os.isiOS() && detection.os.version.major <= 12;
  const setupEvents$1 = (editor, uiComponents) => {
    const dom2 = editor.dom;
    let contentWindow = editor.getWin();
    const initialDocEle = editor.getDoc().documentElement;
    const lastWindowDimensions = Cell(SugarPosition(contentWindow.innerWidth, contentWindow.innerHeight));
    const lastDocumentDimensions = Cell(SugarPosition(initialDocEle.offsetWidth, initialDocEle.offsetHeight));
    const resizeWindow = () => {
      const outer = lastWindowDimensions.get();
      if (outer.left !== contentWindow.innerWidth || outer.top !== contentWindow.innerHeight) {
        lastWindowDimensions.set(SugarPosition(contentWindow.innerWidth, contentWindow.innerHeight));
        fireResizeContent(editor);
      }
    };
    const resizeDocument = () => {
      const docEle = editor.getDoc().documentElement;
      const inner = lastDocumentDimensions.get();
      if (inner.left !== docEle.offsetWidth || inner.top !== docEle.offsetHeight) {
        lastDocumentDimensions.set(SugarPosition(docEle.offsetWidth, docEle.offsetHeight));
        fireResizeContent(editor);
      }
    };
    const scroll = (e) => {
      fireScrollContent(editor, e);
    };
    dom2.bind(contentWindow, "resize", resizeWindow);
    dom2.bind(contentWindow, "scroll", scroll);
    const elementLoad = capture(SugarElement.fromDom(editor.getBody()), "load", resizeDocument);
    const mothership = uiComponents.uiMothership.element;
    editor.on("hide", () => {
      set$8(mothership, "display", "none");
    });
    editor.on("show", () => {
      remove$6(mothership, "display");
    });
    editor.on("NodeChange", resizeDocument);
    editor.on("remove", () => {
      elementLoad.unbind();
      dom2.unbind(contentWindow, "resize", resizeWindow);
      dom2.unbind(contentWindow, "scroll", scroll);
      contentWindow = null;
    });
  };
  const render$1 = (editor, uiComponents, rawUiConfig, backstage, args) => {
    const lastToolbarWidth = Cell(0);
    const outerContainer = uiComponents.outerContainer;
    iframe(editor);
    const eTargetNode = SugarElement.fromDom(args.targetNode);
    const uiRoot = getContentContainer(getRootNode(eTargetNode));
    attachSystemAfter(eTargetNode, uiComponents.mothership);
    attachSystem(uiRoot, uiComponents.uiMothership);
    editor.on("PostRender", () => {
      OuterContainer.setSidebar(outerContainer, rawUiConfig.sidebar, getSidebarShow(editor));
      setToolbar(editor, uiComponents, rawUiConfig, backstage);
      lastToolbarWidth.set(editor.getWin().innerWidth);
      OuterContainer.setMenubar(outerContainer, identifyMenus(editor, rawUiConfig));
      setupEvents$1(editor, uiComponents);
    });
    const socket = OuterContainer.getSocket(outerContainer).getOrDie("Could not find expected socket element");
    if (isiOS12) {
      setAll(socket.element, {
        "overflow": "scroll",
        "-webkit-overflow-scrolling": "touch"
      });
      const limit = first(() => {
        editor.dispatch("ScrollContent");
      }, 20);
      const unbinder = bind(socket.element, "scroll", limit.throttle);
      editor.on("remove", unbinder.unbind);
    }
    setupReadonlyModeSwitch(editor, uiComponents);
    editor.addCommand("ToggleSidebar", (_ui, value2) => {
      OuterContainer.toggleSidebar(outerContainer, value2);
      editor.dispatch("ToggleSidebar");
    });
    editor.addQueryValueHandler("ToggleSidebar", () => {
      var _a;
      return (_a = OuterContainer.whichSidebar(outerContainer)) !== null && _a !== void 0 ? _a : "";
    });
    const toolbarMode = getToolbarMode(editor);
    const refreshDrawer = () => {
      OuterContainer.refreshToolbar(uiComponents.outerContainer);
    };
    if (toolbarMode === ToolbarMode$1.sliding || toolbarMode === ToolbarMode$1.floating) {
      editor.on("ResizeWindow ResizeEditor ResizeContent", () => {
        const width2 = editor.getWin().innerWidth;
        if (width2 !== lastToolbarWidth.get()) {
          refreshDrawer();
          lastToolbarWidth.set(width2);
        }
      });
    }
    const api2 = {
      setEnabled: (state) => {
        broadcastReadonly(uiComponents, !state);
      },
      isEnabled: () => !Disabling.isDisabled(outerContainer)
    };
    return {
      iframeContainer: socket.element.dom,
      editorContainer: outerContainer.element.dom,
      api: api2
    };
  };
  var Iframe = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    render: render$1
  });
  const parseToInt = (val) => {
    const re = /^[0-9\.]+(|px)$/i;
    if (re.test("" + val)) {
      return Optional.some(parseInt("" + val, 10));
    }
    return Optional.none();
  };
  const numToPx = (val) => isNumber(val) ? val + "px" : val;
  const calcCappedSize = (size, minSize, maxSize) => {
    const minOverride = minSize.filter((min2) => size < min2);
    const maxOverride = maxSize.filter((max2) => size > max2);
    return minOverride.or(maxOverride).getOr(size);
  };
  const getHeight = (editor) => {
    const baseHeight = getHeightOption(editor);
    const minHeight = getMinHeightOption(editor);
    const maxHeight = getMaxHeightOption(editor);
    return parseToInt(baseHeight).map((height2) => calcCappedSize(height2, minHeight, maxHeight));
  };
  const getHeightWithFallback = (editor) => {
    const height2 = getHeight(editor);
    return height2.getOr(getHeightOption(editor));
  };
  const getWidth = (editor) => {
    const baseWidth = getWidthOption(editor);
    const minWidth = getMinWidthOption(editor);
    const maxWidth = getMaxWidthOption(editor);
    return parseToInt(baseWidth).map((width2) => calcCappedSize(width2, minWidth, maxWidth));
  };
  const getWidthWithFallback = (editor) => {
    const width2 = getWidth(editor);
    return width2.getOr(getWidthOption(editor));
  };
  const { ToolbarLocation, ToolbarMode } = Options;
  const InlineHeader = (editor, targetElm, uiComponents, backstage, floatContainer) => {
    const { uiMothership, outerContainer } = uiComponents;
    const DOM = global$7.DOM;
    const useFixedToolbarContainer = useFixedContainer(editor);
    const isSticky = isStickyToolbar(editor);
    const editorMaxWidthOpt = getMaxWidthOption(editor).or(getWidth(editor));
    const headerBackstage = backstage.shared.header;
    const isPositionedAtTop = headerBackstage.isPositionedAtTop;
    const toolbarMode = getToolbarMode(editor);
    const isSplitToolbar = toolbarMode === ToolbarMode.sliding || toolbarMode === ToolbarMode.floating;
    const visible = Cell(false);
    const isVisible2 = () => visible.get() && !editor.removed;
    const calcToolbarOffset = (toolbar) => isSplitToolbar ? toolbar.fold(constant$1(0), (tbar) => tbar.components().length > 1 ? get$d(tbar.components()[1].element) : 0) : 0;
    const calcMode = (container) => {
      switch (getToolbarLocation(editor)) {
        case ToolbarLocation.auto:
          const toolbar = OuterContainer.getToolbar(outerContainer);
          const offset2 = calcToolbarOffset(toolbar);
          const toolbarHeight = get$d(container.element) - offset2;
          const targetBounds = box$1(targetElm);
          const roomAtTop = targetBounds.y > toolbarHeight;
          if (roomAtTop) {
            return "top";
          } else {
            const doc = documentElement(targetElm);
            const docHeight = Math.max(doc.dom.scrollHeight, get$d(doc));
            const roomAtBottom = targetBounds.bottom < docHeight - toolbarHeight;
            if (roomAtBottom) {
              return "bottom";
            } else {
              const winBounds = win();
              const isRoomAtBottomViewport = winBounds.bottom < targetBounds.bottom - toolbarHeight;
              return isRoomAtBottomViewport ? "bottom" : "top";
            }
          }
        case ToolbarLocation.bottom:
          return "bottom";
        case ToolbarLocation.top:
        default:
          return "top";
      }
    };
    const setupMode = (mode) => {
      floatContainer.on((container) => {
        Docking.setModes(container, [mode]);
        headerBackstage.setDockingMode(mode);
        const verticalDir = isPositionedAtTop() ? AttributeValue.TopToBottom : AttributeValue.BottomToTop;
        set$9(container.element, Attribute, verticalDir);
      });
    };
    const updateChromeWidth = () => {
      floatContainer.on((container) => {
        const maxWidth = editorMaxWidthOpt.getOrThunk(() => {
          const bodyMargin = parseToInt(get$e(body(), "margin-left")).getOr(0);
          return get$c(body()) - absolute$3(targetElm).left + bodyMargin;
        });
        set$8(container.element, "max-width", maxWidth + "px");
      });
    };
    const updateChromePosition = () => {
      floatContainer.on((container) => {
        const toolbar = OuterContainer.getToolbar(outerContainer);
        const offset2 = calcToolbarOffset(toolbar);
        const targetBounds = box$1(targetElm);
        const top2 = isPositionedAtTop() ? Math.max(targetBounds.y - get$d(container.element) + offset2, 0) : targetBounds.bottom;
        setAll(outerContainer.element, {
          position: "absolute",
          top: Math.round(top2) + "px",
          left: Math.round(targetBounds.x) + "px"
        });
      });
    };
    const repositionPopups$1 = () => {
      uiMothership.broadcastOn([repositionPopups()], {});
    };
    const updateChromeUi = (resetDocking = false) => {
      if (!isVisible2()) {
        return;
      }
      if (!useFixedToolbarContainer) {
        updateChromeWidth();
      }
      if (isSplitToolbar) {
        OuterContainer.refreshToolbar(outerContainer);
      }
      if (!useFixedToolbarContainer) {
        updateChromePosition();
      }
      if (isSticky) {
        const action = resetDocking ? Docking.reset : Docking.refresh;
        floatContainer.on(action);
      }
      repositionPopups$1();
    };
    const updateMode = (updateUi = true) => {
      if (useFixedToolbarContainer || !isSticky || !isVisible2()) {
        return;
      }
      floatContainer.on((container) => {
        const currentMode = headerBackstage.getDockingMode();
        const newMode = calcMode(container);
        if (newMode !== currentMode) {
          setupMode(newMode);
          if (updateUi) {
            updateChromeUi(true);
          }
        }
      });
    };
    const show2 = () => {
      visible.set(true);
      set$8(outerContainer.element, "display", "flex");
      DOM.addClass(editor.getBody(), "mce-edit-focus");
      remove$6(uiMothership.element, "display");
      updateMode(false);
      updateChromeUi();
    };
    const hide = () => {
      visible.set(false);
      if (uiComponents.outerContainer) {
        set$8(outerContainer.element, "display", "none");
        DOM.removeClass(editor.getBody(), "mce-edit-focus");
      }
      set$8(uiMothership.element, "display", "none");
    };
    return {
      isVisible: isVisible2,
      isPositionedAtTop,
      show: show2,
      hide,
      update: updateChromeUi,
      updateMode,
      repositionPopups: repositionPopups$1
    };
  };
  const getTargetPosAndBounds = (targetElm, isToolbarTop) => {
    const bounds2 = box$1(targetElm);
    return {
      pos: isToolbarTop ? bounds2.y : bounds2.bottom,
      bounds: bounds2
    };
  };
  const setupEvents = (editor, targetElm, ui, toolbarPersist) => {
    const prevPosAndBounds = Cell(getTargetPosAndBounds(targetElm, ui.isPositionedAtTop()));
    const resizeContent = (e) => {
      const { pos, bounds: bounds2 } = getTargetPosAndBounds(targetElm, ui.isPositionedAtTop());
      const {
        pos: prevPos,
        bounds: prevBounds
      } = prevPosAndBounds.get();
      const hasResized = bounds2.height !== prevBounds.height || bounds2.width !== prevBounds.width;
      prevPosAndBounds.set({
        pos,
        bounds: bounds2
      });
      if (hasResized) {
        fireResizeContent(editor, e);
      }
      if (ui.isVisible()) {
        if (prevPos !== pos) {
          ui.update(true);
        } else if (hasResized) {
          ui.updateMode();
          ui.repositionPopups();
        }
      }
    };
    if (!toolbarPersist) {
      editor.on("activate", ui.show);
      editor.on("deactivate", ui.hide);
    }
    editor.on("SkinLoaded ResizeWindow", () => ui.update(true));
    editor.on("NodeChange keydown", (e) => {
      requestAnimationFrame(() => resizeContent(e));
    });
    editor.on("ScrollWindow", () => ui.updateMode());
    const elementLoad = unbindable();
    elementLoad.set(capture(SugarElement.fromDom(editor.getBody()), "load", (e) => resizeContent(e.raw)));
    editor.on("remove", () => {
      elementLoad.clear();
    });
  };
  const render = (editor, uiComponents, rawUiConfig, backstage, args) => {
    const { mothership, uiMothership, outerContainer } = uiComponents;
    const floatContainer = value$2();
    const targetElm = SugarElement.fromDom(args.targetNode);
    const ui = InlineHeader(editor, targetElm, uiComponents, backstage, floatContainer);
    const toolbarPersist = isToolbarPersist(editor);
    inline(editor);
    const render2 = () => {
      if (floatContainer.isSet()) {
        ui.show();
        return;
      }
      floatContainer.set(OuterContainer.getHeader(outerContainer).getOrDie());
      const uiContainer = getUiContainer(editor);
      attachSystem(uiContainer, mothership);
      attachSystem(uiContainer, uiMothership);
      setToolbar(editor, uiComponents, rawUiConfig, backstage);
      OuterContainer.setMenubar(outerContainer, identifyMenus(editor, rawUiConfig));
      ui.show();
      setupEvents(editor, targetElm, ui, toolbarPersist);
      editor.nodeChanged();
    };
    editor.on("show", render2);
    editor.on("hide", ui.hide);
    if (!toolbarPersist) {
      editor.on("focus", render2);
      editor.on("blur", ui.hide);
    }
    editor.on("init", () => {
      if (editor.hasFocus() || toolbarPersist) {
        render2();
      }
    });
    setupReadonlyModeSwitch(editor, uiComponents);
    const api2 = {
      show: render2,
      hide: ui.hide,
      setEnabled: (state) => {
        broadcastReadonly(uiComponents, !state);
      },
      isEnabled: () => !Disabling.isDisabled(outerContainer)
    };
    return {
      editorContainer: outerContainer.element.dom,
      api: api2
    };
  };
  var Inline = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    render
  });
  const showContextToolbarEvent = "contexttoolbar-show";
  const hideContextToolbarEvent = "contexttoolbar-hide";
  const getFormApi = (input2) => ({
    hide: () => emit(input2, sandboxClose()),
    getValue: () => Representing.getValue(input2)
  });
  const runOnExecute = (memInput, original2) => run$1(internalToolbarButtonExecute, (comp, se) => {
    const input2 = memInput.get(comp);
    const formApi = getFormApi(input2);
    original2.onAction(formApi, se.event.buttonApi);
  });
  const renderContextButton = (memInput, button2, providers) => {
    const { primary: primary2, ...rest } = button2.original;
    const bridged = getOrDie(createToolbarButton({
      ...rest,
      type: "button",
      onAction: noop
    }));
    return renderToolbarButtonWith(bridged, providers, [runOnExecute(memInput, button2)]);
  };
  const renderContextToggleButton = (memInput, button2, providers) => {
    const { primary: primary2, ...rest } = button2.original;
    const bridged = getOrDie(createToggleButton({
      ...rest,
      type: "togglebutton",
      onAction: noop
    }));
    return renderToolbarToggleButtonWith(bridged, providers, [runOnExecute(memInput, button2)]);
  };
  const isToggleButton = (button2) => button2.type === "contextformtogglebutton";
  const generateOne = (memInput, button2, providersBackstage) => {
    if (isToggleButton(button2)) {
      return renderContextToggleButton(memInput, button2, providersBackstage);
    } else {
      return renderContextButton(memInput, button2, providersBackstage);
    }
  };
  const generate = (memInput, buttons, providersBackstage) => {
    const mementos = map$2(buttons, (button2) => record(generateOne(memInput, button2, providersBackstage)));
    const asSpecs = () => map$2(mementos, (mem) => mem.asSpec());
    const findPrimary = (compInSystem) => findMap(buttons, (button2, i2) => {
      if (button2.primary) {
        return Optional.from(mementos[i2]).bind((mem) => mem.getOpt(compInSystem)).filter(not(Disabling.isDisabled));
      } else {
        return Optional.none();
      }
    });
    return {
      asSpecs,
      findPrimary
    };
  };
  const buildInitGroups = (ctx, providers) => {
    const inputAttributes = ctx.label.fold(() => ({}), (label2) => ({ "aria-label": label2 }));
    const memInput = record(Input.sketch({
      inputClasses: [
        "tox-toolbar-textfield",
        "tox-toolbar-nav-js"
      ],
      data: ctx.initValue(),
      inputAttributes,
      selectOnFocus: true,
      inputBehaviours: derive$1([Keying.config({
        mode: "special",
        onEnter: (input2) => commands.findPrimary(input2).map((primary2) => {
          emitExecute(primary2);
          return true;
        }),
        onLeft: (comp, se) => {
          se.cut();
          return Optional.none();
        },
        onRight: (comp, se) => {
          se.cut();
          return Optional.none();
        }
      })])
    }));
    const commands = generate(memInput, ctx.commands, providers);
    return [
      {
        title: Optional.none(),
        items: [memInput.asSpec()]
      },
      {
        title: Optional.none(),
        items: commands.asSpecs()
      }
    ];
  };
  const renderContextForm = (toolbarType, ctx, providers) => renderToolbar({
    type: toolbarType,
    uid: generate$6("context-toolbar"),
    initGroups: buildInitGroups(ctx, providers),
    onEscape: Optional.none,
    cyclicKeying: true,
    providers
  });
  const ContextForm = {
    renderContextForm,
    buildInitGroups
  };
  const isVerticalOverlap = (a, b2, threshold = 0.01) => b2.bottom - a.y >= threshold && a.bottom - b2.y >= threshold;
  const getRangeRect = (rng) => {
    const rect2 = rng.getBoundingClientRect();
    if (rect2.height <= 0 && rect2.width <= 0) {
      const leaf$1 = leaf(SugarElement.fromDom(rng.startContainer), rng.startOffset).element;
      const elm = isText(leaf$1) ? parent(leaf$1) : Optional.some(leaf$1);
      return elm.filter(isElement$1).map((e) => e.dom.getBoundingClientRect()).getOr(rect2);
    } else {
      return rect2;
    }
  };
  const getSelectionBounds = (editor) => {
    const rng = editor.selection.getRng();
    const rect2 = getRangeRect(rng);
    if (editor.inline) {
      const scroll = get$b();
      return bounds(scroll.left + rect2.left, scroll.top + rect2.top, rect2.width, rect2.height);
    } else {
      const bodyPos = absolute$2(SugarElement.fromDom(editor.getBody()));
      return bounds(bodyPos.x + rect2.left, bodyPos.y + rect2.top, rect2.width, rect2.height);
    }
  };
  const getAnchorElementBounds = (editor, lastElement) => lastElement.filter((elem) => inBody(elem) && isHTMLElement(elem)).map(absolute$2).getOrThunk(() => getSelectionBounds(editor));
  const getHorizontalBounds = (contentAreaBox, viewportBounds, margin) => {
    const x = Math.max(contentAreaBox.x + margin, viewportBounds.x);
    const right2 = Math.min(contentAreaBox.right - margin, viewportBounds.right);
    return {
      x,
      width: right2 - x
    };
  };
  const getVerticalBounds = (editor, contentAreaBox, viewportBounds, isToolbarLocationTop, toolbarType, margin) => {
    const container = SugarElement.fromDom(editor.getContainer());
    const header = descendant(container, ".tox-editor-header").getOr(container);
    const headerBox = box$1(header);
    const isToolbarBelowContentArea = headerBox.y >= contentAreaBox.bottom;
    const isToolbarAbove = isToolbarLocationTop && !isToolbarBelowContentArea;
    if (editor.inline && isToolbarAbove) {
      return {
        y: Math.max(headerBox.bottom + margin, viewportBounds.y),
        bottom: viewportBounds.bottom
      };
    }
    if (editor.inline && !isToolbarAbove) {
      return {
        y: viewportBounds.y,
        bottom: Math.min(headerBox.y - margin, viewportBounds.bottom)
      };
    }
    const containerBounds = toolbarType === "line" ? box$1(container) : contentAreaBox;
    if (isToolbarAbove) {
      return {
        y: Math.max(headerBox.bottom + margin, viewportBounds.y),
        bottom: Math.min(containerBounds.bottom - margin, viewportBounds.bottom)
      };
    }
    return {
      y: Math.max(containerBounds.y + margin, viewportBounds.y),
      bottom: Math.min(headerBox.y - margin, viewportBounds.bottom)
    };
  };
  const getContextToolbarBounds = (editor, sharedBackstage, toolbarType, margin = 0) => {
    const viewportBounds = getBounds$3(window);
    const contentAreaBox = box$1(SugarElement.fromDom(editor.getContentAreaContainer()));
    const toolbarOrMenubarEnabled = isMenubarEnabled(editor) || isToolbarEnabled(editor) || isMultipleToolbars(editor);
    const { x, width: width2 } = getHorizontalBounds(contentAreaBox, viewportBounds, margin);
    if (editor.inline && !toolbarOrMenubarEnabled) {
      return bounds(x, viewportBounds.y, width2, viewportBounds.height);
    } else {
      const isToolbarTop = sharedBackstage.header.isPositionedAtTop();
      const { y, bottom: bottom2 } = getVerticalBounds(editor, contentAreaBox, viewportBounds, isToolbarTop, toolbarType, margin);
      return bounds(x, y, width2, bottom2 - y);
    }
  };
  const bubbleSize$1 = 12;
  const bubbleAlignments$1 = {
    valignCentre: [],
    alignCentre: [],
    alignLeft: ["tox-pop--align-left"],
    alignRight: ["tox-pop--align-right"],
    right: ["tox-pop--right"],
    left: ["tox-pop--left"],
    bottom: ["tox-pop--bottom"],
    top: ["tox-pop--top"],
    inset: ["tox-pop--inset"]
  };
  const anchorOverrides = {
    maxHeightFunction: expandable$1(),
    maxWidthFunction: expandable()
  };
  const isEntireElementSelected = (editor, elem) => {
    const rng = editor.selection.getRng();
    const leaf$1 = leaf(SugarElement.fromDom(rng.startContainer), rng.startOffset);
    return rng.startContainer === rng.endContainer && rng.startOffset === rng.endOffset - 1 && eq(leaf$1.element, elem);
  };
  const preservePosition = (elem, position2, f) => {
    const currentPosition = getRaw(elem, "position");
    set$8(elem, "position", position2);
    const result = f(elem);
    currentPosition.each((pos) => set$8(elem, "position", pos));
    return result;
  };
  const shouldUseInsetLayouts = (position2) => position2 === "node";
  const determineInsetLayout = (editor, contextbar, elem, data, bounds2) => {
    const selectionBounds = getSelectionBounds(editor);
    const isSameAnchorElement = data.lastElement().exists((prev) => eq(elem, prev));
    if (isEntireElementSelected(editor, elem)) {
      return isSameAnchorElement ? preserve : north;
    } else if (isSameAnchorElement) {
      return preservePosition(contextbar, data.getMode(), () => {
        const isOverlapping = isVerticalOverlap(selectionBounds, box$1(contextbar));
        return isOverlapping && !data.isReposition() ? flip : preserve;
      });
    } else {
      const yBounds = data.getMode() === "fixed" ? bounds2.y + get$b().top : bounds2.y;
      const contextbarHeight = get$d(contextbar) + bubbleSize$1;
      return yBounds + contextbarHeight <= selectionBounds.y ? north : south;
    }
  };
  const getAnchorSpec$2 = (editor, mobile, data, position2) => {
    const smartInsetLayout = (elem) => (anchor2, element2, bubbles, placee, bounds2) => {
      const layout2 = determineInsetLayout(editor, placee, elem, data, bounds2);
      const newAnchor = {
        ...anchor2,
        y: bounds2.y,
        height: bounds2.height
      };
      return {
        ...layout2(newAnchor, element2, bubbles, placee, bounds2),
        alwaysFit: true
      };
    };
    const getInsetLayouts = (elem) => shouldUseInsetLayouts(position2) ? [smartInsetLayout(elem)] : [];
    const desktopAnchorSpecLayouts = {
      onLtr: (elem) => [
        north$2,
        south$2,
        northeast$2,
        southeast$2,
        northwest$2,
        southwest$2
      ].concat(getInsetLayouts(elem)),
      onRtl: (elem) => [
        north$2,
        south$2,
        northwest$2,
        southwest$2,
        northeast$2,
        southeast$2
      ].concat(getInsetLayouts(elem))
    };
    const mobileAnchorSpecLayouts = {
      onLtr: (elem) => [
        south$2,
        southeast$2,
        southwest$2,
        northeast$2,
        northwest$2,
        north$2
      ].concat(getInsetLayouts(elem)),
      onRtl: (elem) => [
        south$2,
        southwest$2,
        southeast$2,
        northwest$2,
        northeast$2,
        north$2
      ].concat(getInsetLayouts(elem))
    };
    return mobile ? mobileAnchorSpecLayouts : desktopAnchorSpecLayouts;
  };
  const getAnchorLayout = (editor, position2, isTouch2, data) => {
    if (position2 === "line") {
      return {
        bubble: nu$5(bubbleSize$1, 0, bubbleAlignments$1),
        layouts: {
          onLtr: () => [east$2],
          onRtl: () => [west$2]
        },
        overrides: anchorOverrides
      };
    } else {
      return {
        bubble: nu$5(0, bubbleSize$1, bubbleAlignments$1, 1 / bubbleSize$1),
        layouts: getAnchorSpec$2(editor, isTouch2, data, position2),
        overrides: anchorOverrides
      };
    }
  };
  const matchTargetWith = (elem, candidates) => {
    const ctxs = filter$2(candidates, (toolbarApi) => toolbarApi.predicate(elem.dom));
    const { pass, fail } = partition$3(ctxs, (t2) => t2.type === "contexttoolbar");
    return {
      contextToolbars: pass,
      contextForms: fail
    };
  };
  const filterByPositionForStartNode = (toolbars) => {
    if (toolbars.length <= 1) {
      return toolbars;
    } else {
      const doesPositionExist = (value2) => exists(toolbars, (t2) => t2.position === value2);
      const filterToolbarsByPosition = (value2) => filter$2(toolbars, (t2) => t2.position === value2);
      const hasSelectionToolbars = doesPositionExist("selection");
      const hasNodeToolbars = doesPositionExist("node");
      if (hasSelectionToolbars || hasNodeToolbars) {
        if (hasNodeToolbars && hasSelectionToolbars) {
          const nodeToolbars = filterToolbarsByPosition("node");
          const selectionToolbars = map$2(filterToolbarsByPosition("selection"), (t2) => ({
            ...t2,
            position: "node"
          }));
          return nodeToolbars.concat(selectionToolbars);
        } else {
          return hasSelectionToolbars ? filterToolbarsByPosition("selection") : filterToolbarsByPosition("node");
        }
      } else {
        return filterToolbarsByPosition("line");
      }
    }
  };
  const filterByPositionForAncestorNode = (toolbars) => {
    if (toolbars.length <= 1) {
      return toolbars;
    } else {
      const findPosition = (value2) => find$5(toolbars, (t2) => t2.position === value2);
      const basePosition = findPosition("selection").orThunk(() => findPosition("node")).orThunk(() => findPosition("line")).map((t2) => t2.position);
      return basePosition.fold(() => [], (pos) => filter$2(toolbars, (t2) => t2.position === pos));
    }
  };
  const matchStartNode = (elem, nodeCandidates, editorCandidates) => {
    const nodeMatches = matchTargetWith(elem, nodeCandidates);
    if (nodeMatches.contextForms.length > 0) {
      return Optional.some({
        elem,
        toolbars: [nodeMatches.contextForms[0]]
      });
    } else {
      const editorMatches = matchTargetWith(elem, editorCandidates);
      if (editorMatches.contextForms.length > 0) {
        return Optional.some({
          elem,
          toolbars: [editorMatches.contextForms[0]]
        });
      } else if (nodeMatches.contextToolbars.length > 0 || editorMatches.contextToolbars.length > 0) {
        const toolbars = filterByPositionForStartNode(nodeMatches.contextToolbars.concat(editorMatches.contextToolbars));
        return Optional.some({
          elem,
          toolbars
        });
      } else {
        return Optional.none();
      }
    }
  };
  const matchAncestor = (isRoot, startNode, scopes) => {
    if (isRoot(startNode)) {
      return Optional.none();
    } else {
      return ancestor$2(startNode, (ancestorElem) => {
        if (isElement$1(ancestorElem)) {
          const { contextToolbars, contextForms } = matchTargetWith(ancestorElem, scopes.inNodeScope);
          const toolbars = contextForms.length > 0 ? contextForms : filterByPositionForAncestorNode(contextToolbars);
          return toolbars.length > 0 ? Optional.some({
            elem: ancestorElem,
            toolbars
          }) : Optional.none();
        } else {
          return Optional.none();
        }
      }, isRoot);
    }
  };
  const lookup$1 = (scopes, editor) => {
    const rootElem = SugarElement.fromDom(editor.getBody());
    const isRoot = (elem) => eq(elem, rootElem);
    const isOutsideRoot = (startNode2) => !isRoot(startNode2) && !contains(rootElem, startNode2);
    const startNode = SugarElement.fromDom(editor.selection.getNode());
    if (isOutsideRoot(startNode)) {
      return Optional.none();
    }
    return matchStartNode(startNode, scopes.inNodeScope, scopes.inEditorScope).orThunk(() => matchAncestor(isRoot, startNode, scopes));
  };
  const categorise = (contextToolbars, navigate) => {
    const forms = {};
    const inNodeScope = [];
    const inEditorScope = [];
    const formNavigators = {};
    const lookupTable = {};
    const registerForm = (key, toolbarSpec) => {
      const contextForm = getOrDie(createContextForm(toolbarSpec));
      forms[key] = contextForm;
      contextForm.launch.map((launch) => {
        formNavigators["form:" + key] = {
          ...toolbarSpec.launch,
          type: launch.type === "contextformtogglebutton" ? "togglebutton" : "button",
          onAction: () => {
            navigate(contextForm);
          }
        };
      });
      if (contextForm.scope === "editor") {
        inEditorScope.push(contextForm);
      } else {
        inNodeScope.push(contextForm);
      }
      lookupTable[key] = contextForm;
    };
    const registerToolbar = (key, toolbarSpec) => {
      createContextToolbar(toolbarSpec).each((contextToolbar) => {
        if (toolbarSpec.scope === "editor") {
          inEditorScope.push(contextToolbar);
        } else {
          inNodeScope.push(contextToolbar);
        }
        lookupTable[key] = contextToolbar;
      });
    };
    const keys$1 = keys(contextToolbars);
    each$1(keys$1, (key) => {
      const toolbarApi = contextToolbars[key];
      if (toolbarApi.type === "contextform") {
        registerForm(key, toolbarApi);
      } else if (toolbarApi.type === "contexttoolbar") {
        registerToolbar(key, toolbarApi);
      }
    });
    return {
      forms,
      inNodeScope,
      inEditorScope,
      lookupTable,
      formNavigators
    };
  };
  const forwardSlideEvent = generate$6("forward-slide");
  const backSlideEvent = generate$6("backward-slide");
  const changeSlideEvent = generate$6("change-slide-event");
  const resizingClass = "tox-pop--resizing";
  const renderContextToolbar = (spec) => {
    const stack = Cell([]);
    return InlineView.sketch({
      dom: {
        tag: "div",
        classes: ["tox-pop"]
      },
      fireDismissalEventInstead: { event: "doNotDismissYet" },
      onShow: (comp) => {
        stack.set([]);
        InlineView.getContent(comp).each((c) => {
          remove$6(c.element, "visibility");
        });
        remove$2(comp.element, resizingClass);
        remove$6(comp.element, "width");
      },
      inlineBehaviours: derive$1([
        config("context-toolbar-events", [
          runOnSource(transitionend(), (comp, se) => {
            if (se.event.raw.propertyName === "width") {
              remove$2(comp.element, resizingClass);
              remove$6(comp.element, "width");
            }
          }),
          run$1(changeSlideEvent, (comp, se) => {
            const elem = comp.element;
            remove$6(elem, "width");
            const currentWidth = get$c(elem);
            InlineView.setContent(comp, se.event.contents);
            add$2(elem, resizingClass);
            const newWidth = get$c(elem);
            set$8(elem, "width", currentWidth + "px");
            InlineView.getContent(comp).each((newContents) => {
              se.event.focus.bind((f) => {
                focus$3(f);
                return search(elem);
              }).orThunk(() => {
                Keying.focusIn(newContents);
                return active$1(getRootNode(elem));
              });
            });
            setTimeout(() => {
              set$8(comp.element, "width", newWidth + "px");
            }, 0);
          }),
          run$1(forwardSlideEvent, (comp, se) => {
            InlineView.getContent(comp).each((oldContents) => {
              stack.set(stack.get().concat([{
                bar: oldContents,
                focus: active$1(getRootNode(comp.element))
              }]));
            });
            emitWith(comp, changeSlideEvent, {
              contents: se.event.forwardContents,
              focus: Optional.none()
            });
          }),
          run$1(backSlideEvent, (comp, _se) => {
            last$1(stack.get()).each((last2) => {
              stack.set(stack.get().slice(0, stack.get().length - 1));
              emitWith(comp, changeSlideEvent, {
                contents: premade(last2.bar),
                focus: last2.focus
              });
            });
          })
        ]),
        Keying.config({
          mode: "special",
          onEscape: (comp) => last$1(stack.get()).fold(() => spec.onEscape(), (_) => {
            emit(comp, backSlideEvent);
            return Optional.some(true);
          })
        })
      ]),
      lazySink: () => Result.value(spec.sink)
    });
  };
  const transitionClass = "tox-pop--transition";
  const register$9 = (editor, registryContextToolbars, sink, extras) => {
    const backstage = extras.backstage;
    const sharedBackstage = backstage.shared;
    const isTouch2 = detect$1().deviceType.isTouch;
    const lastElement = value$2();
    const lastTrigger = value$2();
    const lastContextPosition = value$2();
    const contextbar = build$1(renderContextToolbar({
      sink,
      onEscape: () => {
        editor.focus();
        return Optional.some(true);
      }
    }));
    const getBounds2 = () => {
      const position2 = lastContextPosition.get().getOr("node");
      const margin = shouldUseInsetLayouts(position2) ? 1 : 0;
      return getContextToolbarBounds(editor, sharedBackstage, position2, margin);
    };
    const canLaunchToolbar = () => {
      return !editor.removed && !(isTouch2() && backstage.isContextMenuOpen());
    };
    const isSameLaunchElement = (elem) => is$1(lift2(elem, lastElement.get(), eq), true);
    const shouldContextToolbarHide = () => {
      if (!canLaunchToolbar()) {
        return true;
      } else {
        const contextToolbarBounds = getBounds2();
        const anchorBounds = is$1(lastContextPosition.get(), "node") ? getAnchorElementBounds(editor, lastElement.get()) : getSelectionBounds(editor);
        return contextToolbarBounds.height <= 0 || !isVerticalOverlap(anchorBounds, contextToolbarBounds);
      }
    };
    const close2 = () => {
      lastElement.clear();
      lastTrigger.clear();
      lastContextPosition.clear();
      InlineView.hide(contextbar);
    };
    const hideOrRepositionIfNecessary = () => {
      if (InlineView.isOpen(contextbar)) {
        const contextBarEle = contextbar.element;
        remove$6(contextBarEle, "display");
        if (shouldContextToolbarHide()) {
          set$8(contextBarEle, "display", "none");
        } else {
          lastTrigger.set(0);
          InlineView.reposition(contextbar);
        }
      }
    };
    const wrapInPopDialog = (toolbarSpec) => ({
      dom: {
        tag: "div",
        classes: ["tox-pop__dialog"]
      },
      components: [toolbarSpec],
      behaviours: derive$1([
        Keying.config({ mode: "acyclic" }),
        config("pop-dialog-wrap-events", [
          runOnAttached((comp) => {
            editor.shortcuts.add("ctrl+F9", "focus statusbar", () => Keying.focusIn(comp));
          }),
          runOnDetached((_comp) => {
            editor.shortcuts.remove("ctrl+F9");
          })
        ])
      ])
    });
    const getScopes = cached(() => categorise(registryContextToolbars, (toolbarApi) => {
      const alloySpec = buildToolbar([toolbarApi]);
      emitWith(contextbar, forwardSlideEvent, { forwardContents: wrapInPopDialog(alloySpec) });
    }));
    const buildContextToolbarGroups = (allButtons, ctx) => identifyButtons(editor, {
      buttons: allButtons,
      toolbar: ctx.items,
      allowToolbarGroups: false
    }, extras.backstage, Optional.some(["form:"]));
    const buildContextFormGroups = (ctx, providers) => ContextForm.buildInitGroups(ctx, providers);
    const buildToolbar = (toolbars) => {
      const { buttons } = editor.ui.registry.getAll();
      const scopes = getScopes();
      const allButtons = {
        ...buttons,
        ...scopes.formNavigators
      };
      const toolbarType = getToolbarMode(editor) === ToolbarMode$1.scrolling ? ToolbarMode$1.scrolling : ToolbarMode$1.default;
      const initGroups = flatten(map$2(toolbars, (ctx) => ctx.type === "contexttoolbar" ? buildContextToolbarGroups(allButtons, ctx) : buildContextFormGroups(ctx, sharedBackstage.providers)));
      return renderToolbar({
        type: toolbarType,
        uid: generate$6("context-toolbar"),
        initGroups,
        onEscape: Optional.none,
        cyclicKeying: true,
        providers: sharedBackstage.providers
      });
    };
    const getAnchor2 = (position2, element2) => {
      const anchorage = position2 === "node" ? sharedBackstage.anchors.node(element2) : sharedBackstage.anchors.cursor();
      const anchorLayout = getAnchorLayout(editor, position2, isTouch2(), {
        lastElement: lastElement.get,
        isReposition: () => is$1(lastTrigger.get(), 0),
        getMode: () => Positioning.getMode(sink)
      });
      return deepMerge(anchorage, anchorLayout);
    };
    const launchContext = (toolbarApi, elem) => {
      launchContextToolbar.cancel();
      if (!canLaunchToolbar()) {
        return;
      }
      const toolbarSpec = buildToolbar(toolbarApi);
      const position2 = toolbarApi[0].position;
      const anchor2 = getAnchor2(position2, elem);
      lastContextPosition.set(position2);
      lastTrigger.set(1);
      const contextBarEle = contextbar.element;
      remove$6(contextBarEle, "display");
      if (!isSameLaunchElement(elem)) {
        remove$2(contextBarEle, transitionClass);
        Positioning.reset(sink, contextbar);
      }
      InlineView.showWithinBounds(contextbar, wrapInPopDialog(toolbarSpec), {
        anchor: anchor2,
        transition: {
          classes: [transitionClass],
          mode: "placement"
        }
      }, () => Optional.some(getBounds2()));
      elem.fold(lastElement.clear, lastElement.set);
      if (shouldContextToolbarHide()) {
        set$8(contextBarEle, "display", "none");
      }
    };
    const launchContextToolbar = last(() => {
      if (!editor.hasFocus() || editor.removed) {
        return;
      }
      if (has(contextbar.element, transitionClass)) {
        launchContextToolbar.throttle();
      } else {
        const scopes = getScopes();
        lookup$1(scopes, editor).fold(close2, (info) => {
          launchContext(info.toolbars, Optional.some(info.elem));
        });
      }
    }, 17);
    editor.on("init", () => {
      editor.on("remove", close2);
      editor.on("ScrollContent ScrollWindow ObjectResized ResizeEditor longpress", hideOrRepositionIfNecessary);
      editor.on("click keyup focus SetContent", launchContextToolbar.throttle);
      editor.on(hideContextToolbarEvent, close2);
      editor.on(showContextToolbarEvent, (e) => {
        const scopes = getScopes();
        get$g(scopes.lookupTable, e.toolbarKey).each((ctx) => {
          launchContext([ctx], someIf(e.target !== editor, e.target));
          InlineView.getContent(contextbar).each(Keying.focusIn);
        });
      });
      editor.on("focusout", (_e) => {
        global$9.setEditorTimeout(editor, () => {
          if (search(sink.element).isNone() && search(contextbar.element).isNone()) {
            close2();
          }
        }, 0);
      });
      editor.on("SwitchMode", () => {
        if (editor.mode.isReadOnly()) {
          close2();
        }
      });
      editor.on("AfterProgressState", (event2) => {
        if (event2.state) {
          close2();
        } else if (editor.hasFocus()) {
          launchContextToolbar.throttle();
        }
      });
      editor.on("NodeChange", (_e) => {
        search(contextbar.element).fold(launchContextToolbar.throttle, noop);
      });
    });
  };
  const register$8 = (editor) => {
    const alignToolbarButtons = [
      {
        name: "alignleft",
        text: "Align left",
        cmd: "JustifyLeft",
        icon: "align-left"
      },
      {
        name: "aligncenter",
        text: "Align center",
        cmd: "JustifyCenter",
        icon: "align-center"
      },
      {
        name: "alignright",
        text: "Align right",
        cmd: "JustifyRight",
        icon: "align-right"
      },
      {
        name: "alignjustify",
        text: "Justify",
        cmd: "JustifyFull",
        icon: "align-justify"
      }
    ];
    each$1(alignToolbarButtons, (item2) => {
      editor.ui.registry.addToggleButton(item2.name, {
        tooltip: item2.text,
        icon: item2.icon,
        onAction: onActionExecCommand(editor, item2.cmd),
        onSetup: onSetupFormatToggle(editor, item2.name)
      });
    });
    editor.ui.registry.addButton("alignnone", {
      tooltip: "No alignment",
      icon: "align-none",
      onAction: onActionExecCommand(editor, "JustifyNone")
    });
  };
  const units = {
    unsupportedLength: [
      "em",
      "ex",
      "cap",
      "ch",
      "ic",
      "rem",
      "lh",
      "rlh",
      "vw",
      "vh",
      "vi",
      "vb",
      "vmin",
      "vmax",
      "cm",
      "mm",
      "Q",
      "in",
      "pc",
      "pt",
      "px"
    ],
    fixed: [
      "px",
      "pt"
    ],
    relative: ["%"],
    empty: [""]
  };
  const pattern = (() => {
    const decimalDigits = "[0-9]+";
    const signedInteger = "[+-]?" + decimalDigits;
    const exponentPart = "[eE]" + signedInteger;
    const dot = "\\.";
    const opt = (input2) => `(?:${input2})?`;
    const unsignedDecimalLiteral = [
      "Infinity",
      decimalDigits + dot + opt(decimalDigits) + opt(exponentPart),
      dot + decimalDigits + opt(exponentPart),
      decimalDigits + opt(exponentPart)
    ].join("|");
    const float = `[+-]?(?:${unsignedDecimalLiteral})`;
    return new RegExp(`^(${float})(.*)$`);
  })();
  const isUnit = (unit, accepted) => exists(accepted, (acc) => exists(units[acc], (check) => unit === check));
  const parse = (input2, accepted) => {
    const match = Optional.from(pattern.exec(input2));
    return match.bind((array) => {
      const value2 = Number(array[1]);
      const unitRaw = array[2];
      if (isUnit(unitRaw, accepted)) {
        return Optional.some({
          value: value2,
          unit: unitRaw
        });
      } else {
        return Optional.none();
      }
    });
  };
  const normalise = (input2, accepted) => parse(input2, accepted).map(({ value: value2, unit }) => value2 + unit);
  const registerController = (editor, spec) => {
    const getMenuItems = () => {
      const options = spec.getOptions(editor);
      const initial = spec.getCurrent(editor).map(spec.hash);
      const current = value$2();
      return map$2(options, (value2) => ({
        type: "togglemenuitem",
        text: spec.display(value2),
        onSetup: (api2) => {
          const setActive = (active2) => {
            if (active2) {
              current.on((oldApi) => oldApi.setActive(false));
              current.set(api2);
            }
            api2.setActive(active2);
          };
          setActive(is$1(initial, spec.hash(value2)));
          const unbindWatcher = spec.watcher(editor, value2, setActive);
          return () => {
            current.clear();
            unbindWatcher();
          };
        },
        onAction: () => spec.setCurrent(editor, value2)
      }));
    };
    editor.ui.registry.addMenuButton(spec.name, {
      tooltip: spec.text,
      icon: spec.icon,
      fetch: (callback) => callback(getMenuItems()),
      onSetup: spec.onToolbarSetup
    });
    editor.ui.registry.addNestedMenuItem(spec.name, {
      type: "nestedmenuitem",
      text: spec.text,
      getSubmenuItems: getMenuItems,
      onSetup: spec.onMenuSetup
    });
  };
  const lineHeightSpec = {
    name: "lineheight",
    text: "Line height",
    icon: "line-height",
    getOptions: getLineHeightFormats,
    hash: (input2) => normalise(input2, [
      "fixed",
      "relative",
      "empty"
    ]).getOr(input2),
    display: identity,
    watcher: (editor, value2, callback) => editor.formatter.formatChanged("lineheight", callback, false, { value: value2 }).unbind,
    getCurrent: (editor) => Optional.from(editor.queryCommandValue("LineHeight")),
    setCurrent: (editor, value2) => editor.execCommand("LineHeight", false, value2)
  };
  const languageSpec = (editor) => {
    const settingsOpt = Optional.from(getContentLanguages(editor));
    return settingsOpt.map((settings) => ({
      name: "language",
      text: "Language",
      icon: "language",
      getOptions: constant$1(settings),
      hash: (input2) => isUndefined(input2.customCode) ? input2.code : `${input2.code}/${input2.customCode}`,
      display: (input2) => input2.title,
      watcher: (editor2, value2, callback) => {
        var _a;
        return editor2.formatter.formatChanged("lang", callback, false, {
          value: value2.code,
          customValue: (_a = value2.customCode) !== null && _a !== void 0 ? _a : null
        }).unbind;
      },
      getCurrent: (editor2) => {
        const node = SugarElement.fromDom(editor2.selection.getNode());
        return closest$4(node, (n) => Optional.some(n).filter(isElement$1).bind((ele) => {
          const codeOpt = getOpt(ele, "lang");
          return codeOpt.map((code) => {
            const customCode = getOpt(ele, "data-mce-lang").getOrUndefined();
            return {
              code,
              customCode,
              title: ""
            };
          });
        }));
      },
      setCurrent: (editor2, lang) => editor2.execCommand("Lang", false, lang),
      onToolbarSetup: (api2) => {
        const unbinder = unbindable();
        api2.setActive(editor.formatter.match("lang", {}, void 0, true));
        unbinder.set(editor.formatter.formatChanged("lang", api2.setActive, true));
        return unbinder.clear;
      }
    }));
  };
  const register$7 = (editor) => {
    registerController(editor, lineHeightSpec);
    languageSpec(editor).each((spec) => registerController(editor, spec));
  };
  const register$6 = (editor, backstage) => {
    createAlignMenu(editor, backstage);
    createFontFamilyMenu(editor, backstage);
    createStylesMenu(editor, backstage);
    createBlocksMenu(editor, backstage);
    createFontSizeMenu(editor, backstage);
  };
  const onSetupOutdentState = (editor) => onSetupEvent(editor, "NodeChange", (api2) => {
    api2.setEnabled(editor.queryCommandState("outdent"));
  });
  const registerButtons$2 = (editor) => {
    editor.ui.registry.addButton("outdent", {
      tooltip: "Decrease indent",
      icon: "outdent",
      onSetup: onSetupOutdentState(editor),
      onAction: onActionExecCommand(editor, "outdent")
    });
    editor.ui.registry.addButton("indent", {
      tooltip: "Increase indent",
      icon: "indent",
      onAction: onActionExecCommand(editor, "indent")
    });
  };
  const register$5 = (editor) => {
    registerButtons$2(editor);
  };
  const makeSetupHandler = (editor, pasteAsText) => (api2) => {
    api2.setActive(pasteAsText.get());
    const pastePlainTextToggleHandler = (e) => {
      pasteAsText.set(e.state);
      api2.setActive(e.state);
    };
    editor.on("PastePlainTextToggle", pastePlainTextToggleHandler);
    return () => editor.off("PastePlainTextToggle", pastePlainTextToggleHandler);
  };
  const register$4 = (editor) => {
    const pasteAsText = Cell(getPasteAsText(editor));
    const onAction2 = () => editor.execCommand("mceTogglePlainTextPaste");
    editor.ui.registry.addToggleButton("pastetext", {
      active: false,
      icon: "paste-text",
      tooltip: "Paste as text",
      onAction: onAction2,
      onSetup: makeSetupHandler(editor, pasteAsText)
    });
    editor.ui.registry.addToggleMenuItem("pastetext", {
      text: "Paste as text",
      icon: "paste-text",
      onAction: onAction2,
      onSetup: makeSetupHandler(editor, pasteAsText)
    });
  };
  const onActionToggleFormat = (editor, fmt) => () => {
    editor.execCommand("mceToggleFormat", false, fmt);
  };
  const registerFormatButtons = (editor) => {
    global$1.each([
      {
        name: "bold",
        text: "Bold",
        icon: "bold"
      },
      {
        name: "italic",
        text: "Italic",
        icon: "italic"
      },
      {
        name: "underline",
        text: "Underline",
        icon: "underline"
      },
      {
        name: "strikethrough",
        text: "Strikethrough",
        icon: "strike-through"
      },
      {
        name: "subscript",
        text: "Subscript",
        icon: "subscript"
      },
      {
        name: "superscript",
        text: "Superscript",
        icon: "superscript"
      }
    ], (btn, _idx) => {
      editor.ui.registry.addToggleButton(btn.name, {
        tooltip: btn.text,
        icon: btn.icon,
        onSetup: onSetupFormatToggle(editor, btn.name),
        onAction: onActionToggleFormat(editor, btn.name)
      });
    });
    for (let i2 = 1; i2 <= 6; i2++) {
      const name2 = "h" + i2;
      editor.ui.registry.addToggleButton(name2, {
        text: name2.toUpperCase(),
        tooltip: "Heading " + i2,
        onSetup: onSetupFormatToggle(editor, name2),
        onAction: onActionToggleFormat(editor, name2)
      });
    }
  };
  const registerCommandButtons = (editor) => {
    global$1.each([
      {
        name: "cut",
        text: "Cut",
        action: "Cut",
        icon: "cut"
      },
      {
        name: "copy",
        text: "Copy",
        action: "Copy",
        icon: "copy"
      },
      {
        name: "paste",
        text: "Paste",
        action: "Paste",
        icon: "paste"
      },
      {
        name: "help",
        text: "Help",
        action: "mceHelp",
        icon: "help"
      },
      {
        name: "selectall",
        text: "Select all",
        action: "SelectAll",
        icon: "select-all"
      },
      {
        name: "newdocument",
        text: "New document",
        action: "mceNewDocument",
        icon: "new-document"
      },
      {
        name: "removeformat",
        text: "Clear formatting",
        action: "RemoveFormat",
        icon: "remove-formatting"
      },
      {
        name: "remove",
        text: "Remove",
        action: "Delete",
        icon: "remove"
      },
      {
        name: "print",
        text: "Print",
        action: "mcePrint",
        icon: "print"
      },
      {
        name: "hr",
        text: "Horizontal line",
        action: "InsertHorizontalRule",
        icon: "horizontal-rule"
      }
    ], (btn) => {
      editor.ui.registry.addButton(btn.name, {
        tooltip: btn.text,
        icon: btn.icon,
        onAction: onActionExecCommand(editor, btn.action)
      });
    });
  };
  const registerCommandToggleButtons = (editor) => {
    global$1.each([{
      name: "blockquote",
      text: "Blockquote",
      action: "mceBlockQuote",
      icon: "quote"
    }], (btn) => {
      editor.ui.registry.addToggleButton(btn.name, {
        tooltip: btn.text,
        icon: btn.icon,
        onAction: onActionExecCommand(editor, btn.action),
        onSetup: onSetupFormatToggle(editor, btn.name)
      });
    });
  };
  const registerButtons$1 = (editor) => {
    registerFormatButtons(editor);
    registerCommandButtons(editor);
    registerCommandToggleButtons(editor);
  };
  const registerMenuItems$2 = (editor) => {
    global$1.each([
      {
        name: "bold",
        text: "Bold",
        action: "Bold",
        icon: "bold",
        shortcut: "Meta+B"
      },
      {
        name: "italic",
        text: "Italic",
        action: "Italic",
        icon: "italic",
        shortcut: "Meta+I"
      },
      {
        name: "underline",
        text: "Underline",
        action: "Underline",
        icon: "underline",
        shortcut: "Meta+U"
      },
      {
        name: "strikethrough",
        text: "Strikethrough",
        action: "Strikethrough",
        icon: "strike-through"
      },
      {
        name: "subscript",
        text: "Subscript",
        action: "Subscript",
        icon: "subscript"
      },
      {
        name: "superscript",
        text: "Superscript",
        action: "Superscript",
        icon: "superscript"
      },
      {
        name: "removeformat",
        text: "Clear formatting",
        action: "RemoveFormat",
        icon: "remove-formatting"
      },
      {
        name: "newdocument",
        text: "New document",
        action: "mceNewDocument",
        icon: "new-document"
      },
      {
        name: "cut",
        text: "Cut",
        action: "Cut",
        icon: "cut",
        shortcut: "Meta+X"
      },
      {
        name: "copy",
        text: "Copy",
        action: "Copy",
        icon: "copy",
        shortcut: "Meta+C"
      },
      {
        name: "paste",
        text: "Paste",
        action: "Paste",
        icon: "paste",
        shortcut: "Meta+V"
      },
      {
        name: "selectall",
        text: "Select all",
        action: "SelectAll",
        icon: "select-all",
        shortcut: "Meta+A"
      },
      {
        name: "print",
        text: "Print...",
        action: "mcePrint",
        icon: "print",
        shortcut: "Meta+P"
      },
      {
        name: "hr",
        text: "Horizontal line",
        action: "InsertHorizontalRule",
        icon: "horizontal-rule"
      }
    ], (menuitem) => {
      editor.ui.registry.addMenuItem(menuitem.name, {
        text: menuitem.text,
        icon: menuitem.icon,
        shortcut: menuitem.shortcut,
        onAction: onActionExecCommand(editor, menuitem.action)
      });
    });
    editor.ui.registry.addMenuItem("codeformat", {
      text: "Code",
      icon: "sourcecode",
      onAction: onActionToggleFormat(editor, "code")
    });
  };
  const register$3 = (editor) => {
    registerButtons$1(editor);
    registerMenuItems$2(editor);
  };
  const onSetupUndoRedoState = (editor, type2) => onSetupEvent(editor, "Undo Redo AddUndo TypingUndo ClearUndos SwitchMode", (api2) => {
    api2.setEnabled(!editor.mode.isReadOnly() && editor.undoManager[type2]());
  });
  const registerMenuItems$1 = (editor) => {
    editor.ui.registry.addMenuItem("undo", {
      text: "Undo",
      icon: "undo",
      shortcut: "Meta+Z",
      onSetup: onSetupUndoRedoState(editor, "hasUndo"),
      onAction: onActionExecCommand(editor, "undo")
    });
    editor.ui.registry.addMenuItem("redo", {
      text: "Redo",
      icon: "redo",
      shortcut: "Meta+Y",
      onSetup: onSetupUndoRedoState(editor, "hasRedo"),
      onAction: onActionExecCommand(editor, "redo")
    });
  };
  const registerButtons = (editor) => {
    editor.ui.registry.addButton("undo", {
      tooltip: "Undo",
      icon: "undo",
      enabled: false,
      onSetup: onSetupUndoRedoState(editor, "hasUndo"),
      onAction: onActionExecCommand(editor, "undo")
    });
    editor.ui.registry.addButton("redo", {
      tooltip: "Redo",
      icon: "redo",
      enabled: false,
      onSetup: onSetupUndoRedoState(editor, "hasRedo"),
      onAction: onActionExecCommand(editor, "redo")
    });
  };
  const register$2 = (editor) => {
    registerMenuItems$1(editor);
    registerButtons(editor);
  };
  const onSetupVisualAidState = (editor) => onSetupEvent(editor, "VisualAid", (api2) => {
    api2.setActive(editor.hasVisual);
  });
  const registerMenuItems = (editor) => {
    editor.ui.registry.addToggleMenuItem("visualaid", {
      text: "Visual aids",
      onSetup: onSetupVisualAidState(editor),
      onAction: onActionExecCommand(editor, "mceToggleVisualAid")
    });
  };
  const registerToolbarButton = (editor) => {
    editor.ui.registry.addButton("visualaid", {
      tooltip: "Visual aids",
      text: "Visual aids",
      onAction: onActionExecCommand(editor, "mceToggleVisualAid")
    });
  };
  const register$1 = (editor) => {
    registerToolbarButton(editor);
    registerMenuItems(editor);
  };
  const setup$6 = (editor, backstage) => {
    register$8(editor);
    register$3(editor);
    register$6(editor, backstage);
    register$2(editor);
    register$c(editor);
    register$1(editor);
    register$5(editor);
    register$7(editor);
    register$4(editor);
  };
  const patchPipeConfig = (config2) => isString(config2) ? config2.split(/[ ,]/) : config2;
  const option = (name2) => (editor) => editor.options.get(name2);
  const register = (editor) => {
    const registerOption = editor.options.register;
    registerOption("contextmenu_avoid_overlap", {
      processor: "string",
      default: ""
    });
    registerOption("contextmenu_never_use_native", {
      processor: "boolean",
      default: false
    });
    registerOption("contextmenu", {
      processor: (value2) => {
        if (value2 === false) {
          return {
            value: [],
            valid: true
          };
        } else if (isString(value2) || isArrayOf(value2, isString)) {
          return {
            value: patchPipeConfig(value2),
            valid: true
          };
        } else {
          return {
            valid: false,
            message: "Must be false or a string."
          };
        }
      },
      default: "link linkchecker image editimage table spellchecker configurepermanentpen"
    });
  };
  const shouldNeverUseNative = option("contextmenu_never_use_native");
  const getAvoidOverlapSelector = option("contextmenu_avoid_overlap");
  const isContextMenuDisabled = (editor) => getContextMenu(editor).length === 0;
  const getContextMenu = (editor) => {
    const contextMenus = editor.ui.registry.getAll().contextMenus;
    const contextMenu = editor.options.get("contextmenu");
    if (editor.options.isSet("contextmenu")) {
      return contextMenu;
    } else {
      return filter$2(contextMenu, (item2) => has$2(contextMenus, item2));
    }
  };
  const nu = (x, y) => ({
    type: "makeshift",
    x,
    y
  });
  const transpose = (pos, dx, dy) => {
    return nu(pos.x + dx, pos.y + dy);
  };
  const isTouchEvent$1 = (e) => e.type === "longpress" || e.type.indexOf("touch") === 0;
  const fromPageXY = (e) => {
    if (isTouchEvent$1(e)) {
      const touch2 = e.touches[0];
      return nu(touch2.pageX, touch2.pageY);
    } else {
      return nu(e.pageX, e.pageY);
    }
  };
  const fromClientXY = (e) => {
    if (isTouchEvent$1(e)) {
      const touch2 = e.touches[0];
      return nu(touch2.clientX, touch2.clientY);
    } else {
      return nu(e.clientX, e.clientY);
    }
  };
  const transposeContentAreaContainer = (element2, pos) => {
    const containerPos = global$7.DOM.getPos(element2);
    return transpose(pos, containerPos.x, containerPos.y);
  };
  const getPointAnchor = (editor, e) => {
    if (e.type === "contextmenu" || e.type === "longpress") {
      if (editor.inline) {
        return fromPageXY(e);
      } else {
        return transposeContentAreaContainer(editor.getContentAreaContainer(), fromClientXY(e));
      }
    } else {
      return getSelectionAnchor(editor);
    }
  };
  const getSelectionAnchor = (editor) => {
    return {
      type: "selection",
      root: SugarElement.fromDom(editor.selection.getNode())
    };
  };
  const getNodeAnchor = (editor) => ({
    type: "node",
    node: Optional.some(SugarElement.fromDom(editor.selection.getNode())),
    root: SugarElement.fromDom(editor.getBody())
  });
  const getAnchorSpec$1 = (editor, e, anchorType) => {
    switch (anchorType) {
      case "node":
        return getNodeAnchor(editor);
      case "point":
        return getPointAnchor(editor, e);
      case "selection":
        return getSelectionAnchor(editor);
    }
  };
  const initAndShow$1 = (editor, e, buildMenu, backstage, contextmenu, anchorType) => {
    const items = buildMenu();
    const anchorSpec = getAnchorSpec$1(editor, e, anchorType);
    build(items, ItemResponse$1.CLOSE_ON_EXECUTE, backstage, {
      isHorizontalMenu: false,
      search: Optional.none()
    }).map((menuData) => {
      e.preventDefault();
      InlineView.showMenuAt(contextmenu, { anchor: anchorSpec }, {
        menu: { markers: markers("normal") },
        data: menuData
      });
    });
  };
  const layouts = {
    onLtr: () => [
      south$2,
      southeast$2,
      southwest$2,
      northeast$2,
      northwest$2,
      north$2,
      north,
      south,
      northeast,
      southeast,
      northwest,
      southwest
    ],
    onRtl: () => [
      south$2,
      southwest$2,
      southeast$2,
      northwest$2,
      northeast$2,
      north$2,
      north,
      south,
      northwest,
      southwest,
      northeast,
      southeast
    ]
  };
  const bubbleSize = 12;
  const bubbleAlignments = {
    valignCentre: [],
    alignCentre: [],
    alignLeft: ["tox-pop--align-left"],
    alignRight: ["tox-pop--align-right"],
    right: ["tox-pop--right"],
    left: ["tox-pop--left"],
    bottom: ["tox-pop--bottom"],
    top: ["tox-pop--top"]
  };
  const isTouchWithinSelection = (editor, e) => {
    const selection = editor.selection;
    if (selection.isCollapsed() || e.touches.length < 1) {
      return false;
    } else {
      const touch2 = e.touches[0];
      const rng = selection.getRng();
      const rngRectOpt = getFirstRect(editor.getWin(), SimSelection.domRange(rng));
      return rngRectOpt.exists((rngRect) => rngRect.left <= touch2.clientX && rngRect.right >= touch2.clientX && rngRect.top <= touch2.clientY && rngRect.bottom >= touch2.clientY);
    }
  };
  const setupiOSOverrides = (editor) => {
    const originalSelection = editor.selection.getRng();
    const selectionReset = () => {
      global$9.setEditorTimeout(editor, () => {
        editor.selection.setRng(originalSelection);
      }, 10);
      unbindEventListeners();
    };
    editor.once("touchend", selectionReset);
    const preventMousedown = (e) => {
      e.preventDefault();
      e.stopImmediatePropagation();
    };
    editor.on("mousedown", preventMousedown, true);
    const clearSelectionReset = () => unbindEventListeners();
    editor.once("longpresscancel", clearSelectionReset);
    const unbindEventListeners = () => {
      editor.off("touchend", selectionReset);
      editor.off("longpresscancel", clearSelectionReset);
      editor.off("mousedown", preventMousedown);
    };
  };
  const getAnchorSpec = (editor, e, anchorType) => {
    const anchorSpec = getAnchorSpec$1(editor, e, anchorType);
    const bubbleYOffset = anchorType === "point" ? bubbleSize : 0;
    return {
      bubble: nu$5(0, bubbleYOffset, bubbleAlignments),
      layouts,
      overrides: {
        maxWidthFunction: expandable(),
        maxHeightFunction: expandable$1()
      },
      ...anchorSpec
    };
  };
  const show = (editor, e, items, backstage, contextmenu, anchorType, highlightImmediately) => {
    const anchorSpec = getAnchorSpec(editor, e, anchorType);
    build(items, ItemResponse$1.CLOSE_ON_EXECUTE, backstage, {
      isHorizontalMenu: true,
      search: Optional.none()
    }).map((menuData) => {
      e.preventDefault();
      const highlightOnOpen = highlightImmediately ? HighlightOnOpen.HighlightMenuAndItem : HighlightOnOpen.HighlightNone;
      InlineView.showMenuWithinBounds(contextmenu, { anchor: anchorSpec }, {
        menu: {
          markers: markers("normal"),
          highlightOnOpen
        },
        data: menuData,
        type: "horizontal"
      }, () => Optional.some(getContextToolbarBounds(editor, backstage.shared, anchorType === "node" ? "node" : "selection")));
      editor.dispatch(hideContextToolbarEvent);
    });
  };
  const initAndShow = (editor, e, buildMenu, backstage, contextmenu, anchorType) => {
    const detection2 = detect$1();
    const isiOS = detection2.os.isiOS();
    const isMacOS = detection2.os.isMacOS();
    const isAndroid = detection2.os.isAndroid();
    const isTouch2 = detection2.deviceType.isTouch();
    const shouldHighlightImmediately = () => !(isAndroid || isiOS || isMacOS && isTouch2);
    const open2 = () => {
      const items = buildMenu();
      show(editor, e, items, backstage, contextmenu, anchorType, shouldHighlightImmediately());
    };
    if ((isMacOS || isiOS) && anchorType !== "node") {
      const openiOS = () => {
        setupiOSOverrides(editor);
        open2();
      };
      if (isTouchWithinSelection(editor, e)) {
        openiOS();
      } else {
        editor.once("selectionchange", openiOS);
        editor.once("touchend", () => editor.off("selectionchange", openiOS));
      }
    } else {
      open2();
    }
  };
  const isSeparator = (item2) => isString(item2) ? item2 === "|" : item2.type === "separator";
  const separator = { type: "separator" };
  const makeContextItem = (item2) => {
    const commonMenuItem = (item3) => ({
      text: item3.text,
      icon: item3.icon,
      enabled: item3.enabled,
      shortcut: item3.shortcut
    });
    if (isString(item2)) {
      return item2;
    } else {
      switch (item2.type) {
        case "separator":
          return separator;
        case "submenu":
          return {
            type: "nestedmenuitem",
            ...commonMenuItem(item2),
            getSubmenuItems: () => {
              const items = item2.getSubmenuItems();
              if (isString(items)) {
                return items;
              } else {
                return map$2(items, makeContextItem);
              }
            }
          };
        default:
          const commonItem = item2;
          return {
            type: "menuitem",
            ...commonMenuItem(commonItem),
            onAction: noarg(commonItem.onAction)
          };
      }
    }
  };
  const addContextMenuGroup = (xs, groupItems) => {
    if (groupItems.length === 0) {
      return xs;
    }
    const lastMenuItem = last$1(xs).filter((item2) => !isSeparator(item2));
    const before2 = lastMenuItem.fold(() => [], (_) => [separator]);
    return xs.concat(before2).concat(groupItems).concat([separator]);
  };
  const generateContextMenu = (contextMenus, menuConfig, selectedElement) => {
    const sections = foldl(menuConfig, (acc, name2) => {
      return get$g(contextMenus, name2.toLowerCase()).map((menu2) => {
        const items = menu2.update(selectedElement);
        if (isString(items)) {
          return addContextMenuGroup(acc, items.split(" "));
        } else if (items.length > 0) {
          const allItems = map$2(items, makeContextItem);
          return addContextMenuGroup(acc, allItems);
        } else {
          return acc;
        }
      }).getOrThunk(() => acc.concat([name2]));
    }, []);
    if (sections.length > 0 && isSeparator(sections[sections.length - 1])) {
      sections.pop();
    }
    return sections;
  };
  const isNativeOverrideKeyEvent = (editor, e) => e.ctrlKey && !shouldNeverUseNative(editor);
  const isTouchEvent = (e) => e.type === "longpress" || has$2(e, "touches");
  const isTriggeredByKeyboard = (editor, e) => !isTouchEvent(e) && (e.button !== 2 || e.target === editor.getBody() && e.pointerType === "");
  const getSelectedElement = (editor, e) => isTriggeredByKeyboard(editor, e) ? editor.selection.getStart(true) : e.target;
  const getAnchorType = (editor, e) => {
    const selector = getAvoidOverlapSelector(editor);
    const anchorType = isTriggeredByKeyboard(editor, e) ? "selection" : "point";
    if (isNotEmpty(selector)) {
      const target = getSelectedElement(editor, e);
      const selectorExists = closest(SugarElement.fromDom(target), selector);
      return selectorExists ? "node" : anchorType;
    } else {
      return anchorType;
    }
  };
  const setup$5 = (editor, lazySink, backstage) => {
    const detection2 = detect$1();
    const isTouch2 = detection2.deviceType.isTouch;
    const contextmenu = build$1(InlineView.sketch({
      dom: { tag: "div" },
      lazySink,
      onEscape: () => editor.focus(),
      onShow: () => backstage.setContextMenuState(true),
      onHide: () => backstage.setContextMenuState(false),
      fireDismissalEventInstead: {},
      inlineBehaviours: derive$1([config("dismissContextMenu", [run$1(dismissRequested(), (comp, _se) => {
        Sandboxing.close(comp);
        editor.focus();
      })])])
    }));
    const hideContextMenu = () => InlineView.hide(contextmenu);
    const showContextMenu = (e) => {
      if (shouldNeverUseNative(editor)) {
        e.preventDefault();
      }
      if (isNativeOverrideKeyEvent(editor, e) || isContextMenuDisabled(editor)) {
        return;
      }
      const anchorType = getAnchorType(editor, e);
      const buildMenu = () => {
        const selectedElement = getSelectedElement(editor, e);
        const registry = editor.ui.registry.getAll();
        const menuConfig = getContextMenu(editor);
        return generateContextMenu(registry.contextMenus, menuConfig, selectedElement);
      };
      const initAndShow$2 = isTouch2() ? initAndShow : initAndShow$1;
      initAndShow$2(editor, e, buildMenu, backstage, contextmenu, anchorType);
    };
    editor.on("init", () => {
      const hideEvents = "ResizeEditor ScrollContent ScrollWindow longpresscancel" + (isTouch2() ? "" : " ResizeWindow");
      editor.on(hideEvents, hideContextMenu);
      editor.on("longpress contextmenu", showContextMenu);
    });
  };
  const adt = Adt.generate([
    {
      offset: [
        "x",
        "y"
      ]
    },
    {
      absolute: [
        "x",
        "y"
      ]
    },
    {
      fixed: [
        "x",
        "y"
      ]
    }
  ]);
  const subtract = (change2) => (point2) => point2.translate(-change2.left, -change2.top);
  const add = (change2) => (point2) => point2.translate(change2.left, change2.top);
  const transform = (changes) => (x, y) => foldl(changes, (rest, f) => f(rest), SugarPosition(x, y));
  const asFixed = (coord, scroll, origin) => coord.fold(transform([
    add(origin),
    subtract(scroll)
  ]), transform([subtract(scroll)]), transform([]));
  const asAbsolute = (coord, scroll, origin) => coord.fold(transform([add(origin)]), transform([]), transform([add(scroll)]));
  const asOffset = (coord, scroll, origin) => coord.fold(transform([]), transform([subtract(origin)]), transform([
    add(scroll),
    subtract(origin)
  ]));
  const withinRange = (coord1, coord2, xRange2, yRange2, scroll, origin) => {
    const a1 = asAbsolute(coord1, scroll, origin);
    const a2 = asAbsolute(coord2, scroll, origin);
    return Math.abs(a1.left - a2.left) <= xRange2 && Math.abs(a1.top - a2.top) <= yRange2;
  };
  const getDeltas = (coord1, coord2, xRange2, yRange2, scroll, origin) => {
    const a1 = asAbsolute(coord1, scroll, origin);
    const a2 = asAbsolute(coord2, scroll, origin);
    const left2 = Math.abs(a1.left - a2.left);
    const top2 = Math.abs(a1.top - a2.top);
    return SugarPosition(left2, top2);
  };
  const toStyles = (coord, scroll, origin) => {
    const stylesOpt = coord.fold((x, y) => ({
      position: Optional.some("absolute"),
      left: Optional.some(x + "px"),
      top: Optional.some(y + "px")
    }), (x, y) => ({
      position: Optional.some("absolute"),
      left: Optional.some(x - origin.left + "px"),
      top: Optional.some(y - origin.top + "px")
    }), (x, y) => ({
      position: Optional.some("fixed"),
      left: Optional.some(x + "px"),
      top: Optional.some(y + "px")
    }));
    return {
      right: Optional.none(),
      bottom: Optional.none(),
      ...stylesOpt
    };
  };
  const translate = (coord, deltaX, deltaY) => coord.fold((x, y) => offset(x + deltaX, y + deltaY), (x, y) => absolute(x + deltaX, y + deltaY), (x, y) => fixed(x + deltaX, y + deltaY));
  const absorb = (partialCoord, originalCoord, scroll, origin) => {
    const absorbOne = (stencil, nu2) => (optX, optY) => {
      const original2 = stencil(originalCoord, scroll, origin);
      return nu2(optX.getOr(original2.left), optY.getOr(original2.top));
    };
    return partialCoord.fold(absorbOne(asOffset, offset), absorbOne(asAbsolute, absolute), absorbOne(asFixed, fixed));
  };
  const offset = adt.offset;
  const absolute = adt.absolute;
  const fixed = adt.fixed;
  const parseAttrToInt = (element2, name2) => {
    const value2 = get$f(element2, name2);
    return isUndefined(value2) ? NaN : parseInt(value2, 10);
  };
  const get = (component, snapsInfo) => {
    const element2 = component.element;
    const x = parseAttrToInt(element2, snapsInfo.leftAttr);
    const y = parseAttrToInt(element2, snapsInfo.topAttr);
    return isNaN(x) || isNaN(y) ? Optional.none() : Optional.some(SugarPosition(x, y));
  };
  const set = (component, snapsInfo, pt) => {
    const element2 = component.element;
    set$9(element2, snapsInfo.leftAttr, pt.left + "px");
    set$9(element2, snapsInfo.topAttr, pt.top + "px");
  };
  const clear = (component, snapsInfo) => {
    const element2 = component.element;
    remove$7(element2, snapsInfo.leftAttr);
    remove$7(element2, snapsInfo.topAttr);
  };
  const getCoords = (component, snapInfo, coord, delta) => get(component, snapInfo).fold(() => coord, (fixed$12) => fixed(fixed$12.left + delta.left, fixed$12.top + delta.top));
  const moveOrSnap = (component, snapInfo, coord, delta, scroll, origin) => {
    const newCoord = getCoords(component, snapInfo, coord, delta);
    const snap2 = snapInfo.mustSnap ? findClosestSnap(component, snapInfo, newCoord, scroll, origin) : findSnap(component, snapInfo, newCoord, scroll, origin);
    const fixedCoord = asFixed(newCoord, scroll, origin);
    set(component, snapInfo, fixedCoord);
    return snap2.fold(() => ({
      coord: fixed(fixedCoord.left, fixedCoord.top),
      extra: Optional.none()
    }), (spanned) => ({
      coord: spanned.output,
      extra: spanned.extra
    }));
  };
  const stopDrag = (component, snapInfo) => {
    clear(component, snapInfo);
  };
  const findMatchingSnap = (snaps, newCoord, scroll, origin) => findMap(snaps, (snap2) => {
    const sensor = snap2.sensor;
    const inRange = withinRange(newCoord, sensor, snap2.range.left, snap2.range.top, scroll, origin);
    return inRange ? Optional.some({
      output: absorb(snap2.output, newCoord, scroll, origin),
      extra: snap2.extra
    }) : Optional.none();
  });
  const findClosestSnap = (component, snapInfo, newCoord, scroll, origin) => {
    const snaps = snapInfo.getSnapPoints(component);
    const matchSnap = findMatchingSnap(snaps, newCoord, scroll, origin);
    return matchSnap.orThunk(() => {
      const bestSnap = foldl(snaps, (acc, snap2) => {
        const sensor = snap2.sensor;
        const deltas = getDeltas(newCoord, sensor, snap2.range.left, snap2.range.top, scroll, origin);
        return acc.deltas.fold(() => ({
          deltas: Optional.some(deltas),
          snap: Optional.some(snap2)
        }), (bestDeltas) => {
          const currAvg = (deltas.left + deltas.top) / 2;
          const bestAvg = (bestDeltas.left + bestDeltas.top) / 2;
          if (currAvg <= bestAvg) {
            return {
              deltas: Optional.some(deltas),
              snap: Optional.some(snap2)
            };
          } else {
            return acc;
          }
        });
      }, {
        deltas: Optional.none(),
        snap: Optional.none()
      });
      return bestSnap.snap.map((snap2) => ({
        output: absorb(snap2.output, newCoord, scroll, origin),
        extra: snap2.extra
      }));
    });
  };
  const findSnap = (component, snapInfo, newCoord, scroll, origin) => {
    const snaps = snapInfo.getSnapPoints(component);
    return findMatchingSnap(snaps, newCoord, scroll, origin);
  };
  const snapTo$1 = (snap2, scroll, origin) => ({
    coord: absorb(snap2.output, snap2.output, scroll, origin),
    extra: snap2.extra
  });
  const snapTo = (component, dragConfig, _state, snap2) => {
    const target = dragConfig.getTarget(component.element);
    if (dragConfig.repositionTarget) {
      const doc = owner$4(component.element);
      const scroll = get$b(doc);
      const origin = getOrigin(target);
      const snapPin = snapTo$1(snap2, scroll, origin);
      const styles = toStyles(snapPin.coord, scroll, origin);
      setOptions(target, styles);
    }
  };
  var DraggingApis = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    snapTo
  });
  const initialAttribute = "data-initial-z-index";
  const resetZIndex = (blocker) => {
    parent(blocker.element).filter(isElement$1).each((root) => {
      getOpt(root, initialAttribute).fold(() => remove$6(root, "z-index"), (zIndex) => set$8(root, "z-index", zIndex));
      remove$7(root, initialAttribute);
    });
  };
  const changeZIndex = (blocker) => {
    parent(blocker.element).filter(isElement$1).each((root) => {
      getRaw(root, "z-index").each((zindex) => {
        set$9(root, initialAttribute, zindex);
      });
      set$8(root, "z-index", get$e(blocker.element, "z-index"));
    });
  };
  const instigate = (anyComponent, blocker) => {
    anyComponent.getSystem().addToGui(blocker);
    changeZIndex(blocker);
  };
  const discard = (blocker) => {
    resetZIndex(blocker);
    blocker.getSystem().removeFromGui(blocker);
  };
  const createComponent = (component, blockerClass, blockerEvents) => component.getSystem().build(Container.sketch({
    dom: {
      styles: {
        "left": "0px",
        "top": "0px",
        "width": "100%",
        "height": "100%",
        "position": "fixed",
        "z-index": "1000000000000000"
      },
      classes: [blockerClass]
    },
    events: blockerEvents
  }));
  var SnapSchema = optionObjOf("snaps", [
    required$1("getSnapPoints"),
    onHandler("onSensor"),
    required$1("leftAttr"),
    required$1("topAttr"),
    defaulted("lazyViewport", win),
    defaulted("mustSnap", false)
  ]);
  const schema$6 = [
    defaulted("useFixed", never),
    required$1("blockerClass"),
    defaulted("getTarget", identity),
    defaulted("onDrag", noop),
    defaulted("repositionTarget", true),
    defaulted("onDrop", noop),
    defaultedFunction("getBounds", win),
    SnapSchema
  ];
  const getCurrentCoord = (target) => lift3(getRaw(target, "left"), getRaw(target, "top"), getRaw(target, "position"), (left2, top2, position2) => {
    const nu2 = position2 === "fixed" ? fixed : offset;
    return nu2(parseInt(left2, 10), parseInt(top2, 10));
  }).getOrThunk(() => {
    const location = absolute$3(target);
    return absolute(location.left, location.top);
  });
  const clampCoords = (component, coords, scroll, origin, startData) => {
    const bounds2 = startData.bounds;
    const absoluteCoord = asAbsolute(coords, scroll, origin);
    const newX = clamp(absoluteCoord.left, bounds2.x, bounds2.x + bounds2.width - startData.width);
    const newY = clamp(absoluteCoord.top, bounds2.y, bounds2.y + bounds2.height - startData.height);
    const newCoords = absolute(newX, newY);
    return coords.fold(() => {
      const offset$1 = asOffset(newCoords, scroll, origin);
      return offset(offset$1.left, offset$1.top);
    }, constant$1(newCoords), () => {
      const fixed$12 = asFixed(newCoords, scroll, origin);
      return fixed(fixed$12.left, fixed$12.top);
    });
  };
  const calcNewCoord = (component, optSnaps, currentCoord, scroll, origin, delta, startData) => {
    const newCoord = optSnaps.fold(() => {
      const translated = translate(currentCoord, delta.left, delta.top);
      const fixedCoord = asFixed(translated, scroll, origin);
      return fixed(fixedCoord.left, fixedCoord.top);
    }, (snapInfo) => {
      const snapping = moveOrSnap(component, snapInfo, currentCoord, delta, scroll, origin);
      snapping.extra.each((extra) => {
        snapInfo.onSensor(component, extra);
      });
      return snapping.coord;
    });
    return clampCoords(component, newCoord, scroll, origin, startData);
  };
  const dragBy = (component, dragConfig, startData, delta) => {
    const target = dragConfig.getTarget(component.element);
    if (dragConfig.repositionTarget) {
      const doc = owner$4(component.element);
      const scroll = get$b(doc);
      const origin = getOrigin(target);
      const currentCoord = getCurrentCoord(target);
      const newCoord = calcNewCoord(component, dragConfig.snaps, currentCoord, scroll, origin, delta, startData);
      const styles = toStyles(newCoord, scroll, origin);
      setOptions(target, styles);
    }
    dragConfig.onDrag(component, target, delta);
  };
  const calcStartData = (dragConfig, comp) => ({
    bounds: dragConfig.getBounds(),
    height: getOuter$2(comp.element),
    width: getOuter$1(comp.element)
  });
  const move = (component, dragConfig, dragState, dragMode, event2) => {
    const delta = dragState.update(dragMode, event2);
    const dragStartData = dragState.getStartData().getOrThunk(() => calcStartData(dragConfig, component));
    delta.each((dlt) => {
      dragBy(component, dragConfig, dragStartData, dlt);
    });
  };
  const stop = (component, blocker, dragConfig, dragState) => {
    blocker.each(discard);
    dragConfig.snaps.each((snapInfo) => {
      stopDrag(component, snapInfo);
    });
    const target = dragConfig.getTarget(component.element);
    dragState.reset();
    dragConfig.onDrop(component, target);
  };
  const handlers = (events2) => (dragConfig, dragState) => {
    const updateStartState = (comp) => {
      dragState.setStartData(calcStartData(dragConfig, comp));
    };
    return derive$2([
      run$1(windowScroll(), (comp) => {
        dragState.getStartData().each(() => updateStartState(comp));
      }),
      ...events2(dragConfig, dragState, updateStartState)
    ]);
  };
  const init$2 = (dragApi) => derive$2([
    run$1(mousedown(), dragApi.forceDrop),
    run$1(mouseup(), dragApi.drop),
    run$1(mousemove(), (comp, simulatedEvent) => {
      dragApi.move(simulatedEvent.event);
    }),
    run$1(mouseout(), dragApi.delayDrop)
  ]);
  const getData$1 = (event2) => Optional.from(SugarPosition(event2.x, event2.y));
  const getDelta$1 = (old, nu2) => SugarPosition(nu2.left - old.left, nu2.top - old.top);
  var MouseData = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    getData: getData$1,
    getDelta: getDelta$1
  });
  const events$2 = (dragConfig, dragState, updateStartState) => [run$1(mousedown(), (component, simulatedEvent) => {
    const raw = simulatedEvent.event.raw;
    if (raw.button !== 0) {
      return;
    }
    simulatedEvent.stop();
    const stop$1 = () => stop(component, Optional.some(blocker), dragConfig, dragState);
    const delayDrop = DelayedFunction(stop$1, 200);
    const dragApi = {
      drop: stop$1,
      delayDrop: delayDrop.schedule,
      forceDrop: stop$1,
      move: (event2) => {
        delayDrop.cancel();
        move(component, dragConfig, dragState, MouseData, event2);
      }
    };
    const blocker = createComponent(component, dragConfig.blockerClass, init$2(dragApi));
    const start = () => {
      updateStartState(component);
      instigate(component, blocker);
    };
    start();
  })];
  const schema$5 = [
    ...schema$6,
    output$1("dragger", { handlers: handlers(events$2) })
  ];
  const init$1 = (dragApi) => derive$2([
    run$1(touchstart(), dragApi.forceDrop),
    run$1(touchend(), dragApi.drop),
    run$1(touchcancel(), dragApi.drop),
    run$1(touchmove(), (comp, simulatedEvent) => {
      dragApi.move(simulatedEvent.event);
    })
  ]);
  const getDataFrom = (touches) => {
    const touch2 = touches[0];
    return Optional.some(SugarPosition(touch2.clientX, touch2.clientY));
  };
  const getData = (event2) => {
    const raw = event2.raw;
    const touches = raw.touches;
    return touches.length === 1 ? getDataFrom(touches) : Optional.none();
  };
  const getDelta = (old, nu2) => SugarPosition(nu2.left - old.left, nu2.top - old.top);
  var TouchData = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    getData,
    getDelta
  });
  const events$1 = (dragConfig, dragState, updateStartState) => {
    const blockerSingleton = value$2();
    const stopBlocking = (component) => {
      stop(component, blockerSingleton.get(), dragConfig, dragState);
      blockerSingleton.clear();
    };
    return [
      run$1(touchstart(), (component, simulatedEvent) => {
        simulatedEvent.stop();
        const stop2 = () => stopBlocking(component);
        const dragApi = {
          drop: stop2,
          delayDrop: noop,
          forceDrop: stop2,
          move: (event2) => {
            move(component, dragConfig, dragState, TouchData, event2);
          }
        };
        const blocker = createComponent(component, dragConfig.blockerClass, init$1(dragApi));
        blockerSingleton.set(blocker);
        const start = () => {
          updateStartState(component);
          instigate(component, blocker);
        };
        start();
      }),
      run$1(touchmove(), (component, simulatedEvent) => {
        simulatedEvent.stop();
        move(component, dragConfig, dragState, TouchData, simulatedEvent.event);
      }),
      run$1(touchend(), (component, simulatedEvent) => {
        simulatedEvent.stop();
        stopBlocking(component);
      }),
      run$1(touchcancel(), stopBlocking)
    ];
  };
  const schema$4 = [
    ...schema$6,
    output$1("dragger", { handlers: handlers(events$1) })
  ];
  const events = (dragConfig, dragState, updateStartState) => [
    ...events$2(dragConfig, dragState, updateStartState),
    ...events$1(dragConfig, dragState, updateStartState)
  ];
  const schema$3 = [
    ...schema$6,
    output$1("dragger", { handlers: handlers(events) })
  ];
  const mouse = schema$5;
  const touch = schema$4;
  const mouseOrTouch = schema$3;
  var DraggingBranches = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    mouse,
    touch,
    mouseOrTouch
  });
  const init = () => {
    let previous = Optional.none();
    let startData = Optional.none();
    const reset2 = () => {
      previous = Optional.none();
      startData = Optional.none();
    };
    const calculateDelta = (mode, nu2) => {
      const result = previous.map((old) => mode.getDelta(old, nu2));
      previous = Optional.some(nu2);
      return result;
    };
    const update = (mode, dragEvent) => mode.getData(dragEvent).bind((nuData) => calculateDelta(mode, nuData));
    const setStartData = (data) => {
      startData = Optional.some(data);
    };
    const getStartData = () => startData;
    const readState = constant$1({});
    return nu$8({
      readState,
      reset: reset2,
      update,
      getStartData,
      setStartData
    });
  };
  var DragState = /* @__PURE__ */ Object.freeze({
    __proto__: null,
    init
  });
  const Dragging = createModes({
    branchKey: "mode",
    branches: DraggingBranches,
    name: "dragging",
    active: {
      events: (dragConfig, dragState) => {
        const dragger = dragConfig.dragger;
        return dragger.handlers(dragConfig, dragState);
      }
    },
    extra: {
      snap: (sConfig) => ({
        sensor: sConfig.sensor,
        range: sConfig.range,
        output: sConfig.output,
        extra: Optional.from(sConfig.extra)
      })
    },
    state: DragState,
    apis: DraggingApis
  });
  const snapWidth = 40;
  const snapOffset = snapWidth / 2;
  const calcSnap = (selectorOpt, td, x, y, width2, height2) => selectorOpt.fold(() => Dragging.snap({
    sensor: absolute(x - snapOffset, y - snapOffset),
    range: SugarPosition(width2, height2),
    output: absolute(Optional.some(x), Optional.some(y)),
    extra: { td }
  }), (selectorHandle) => {
    const sensorLeft = x - snapOffset;
    const sensorTop = y - snapOffset;
    const sensorWidth = snapWidth;
    const sensorHeight = snapWidth;
    const rect2 = selectorHandle.element.dom.getBoundingClientRect();
    return Dragging.snap({
      sensor: absolute(sensorLeft, sensorTop),
      range: SugarPosition(sensorWidth, sensorHeight),
      output: absolute(Optional.some(x - rect2.width / 2), Optional.some(y - rect2.height / 2)),
      extra: { td }
    });
  });
  const getSnapsConfig = (getSnapPoints, cell, onChange) => {
    const isSameCell = (cellOpt, td) => cellOpt.exists((currentTd) => eq(currentTd, td));
    return {
      getSnapPoints,
      leftAttr: "data-drag-left",
      topAttr: "data-drag-top",
      onSensor: (component, extra) => {
        const td = extra.td;
        if (!isSameCell(cell.get(), td)) {
          cell.set(td);
          onChange(td);
        }
      },
      mustSnap: true
    };
  };
  const createSelector = (snaps) => record(Button.sketch({
    dom: {
      tag: "div",
      classes: ["tox-selector"]
    },
    buttonBehaviours: derive$1([
      Dragging.config({
        mode: "mouseOrTouch",
        blockerClass: "blocker",
        snaps
      }),
      Unselecting.config({})
    ]),
    eventOrder: {
      mousedown: [
        "dragging",
        "alloy.base.behaviour"
      ],
      touchstart: [
        "dragging",
        "alloy.base.behaviour"
      ]
    }
  }));
  const setup$4 = (editor, sink) => {
    const tlTds = Cell([]);
    const brTds = Cell([]);
    const isVisible2 = Cell(false);
    const startCell = value$2();
    const finishCell = value$2();
    const getTopLeftSnap = (td) => {
      const box2 = absolute$2(td);
      return calcSnap(memTopLeft.getOpt(sink), td, box2.x, box2.y, box2.width, box2.height);
    };
    const getTopLeftSnaps = () => map$2(tlTds.get(), (td) => getTopLeftSnap(td));
    const getBottomRightSnap = (td) => {
      const box2 = absolute$2(td);
      return calcSnap(memBottomRight.getOpt(sink), td, box2.right, box2.bottom, box2.width, box2.height);
    };
    const getBottomRightSnaps = () => map$2(brTds.get(), (td) => getBottomRightSnap(td));
    const topLeftSnaps = getSnapsConfig(getTopLeftSnaps, startCell, (start) => {
      finishCell.get().each((finish) => {
        editor.dispatch("TableSelectorChange", {
          start,
          finish
        });
      });
    });
    const bottomRightSnaps = getSnapsConfig(getBottomRightSnaps, finishCell, (finish) => {
      startCell.get().each((start) => {
        editor.dispatch("TableSelectorChange", {
          start,
          finish
        });
      });
    });
    const memTopLeft = createSelector(topLeftSnaps);
    const memBottomRight = createSelector(bottomRightSnaps);
    const topLeft = build$1(memTopLeft.asSpec());
    const bottomRight = build$1(memBottomRight.asSpec());
    const showOrHideHandle = (selector, cell, isAbove, isBelow) => {
      const cellRect = cell.dom.getBoundingClientRect();
      remove$6(selector.element, "display");
      const viewportHeight = defaultView(SugarElement.fromDom(editor.getBody())).dom.innerHeight;
      const aboveViewport = isAbove(cellRect);
      const belowViewport = isBelow(cellRect, viewportHeight);
      if (aboveViewport || belowViewport) {
        set$8(selector.element, "display", "none");
      }
    };
    const snapTo2 = (selector, cell, getSnapConfig, pos) => {
      const snap2 = getSnapConfig(cell);
      Dragging.snapTo(selector, snap2);
      const isAbove = (rect2) => rect2[pos] < 0;
      const isBelow = (rect2, viewportHeight) => rect2[pos] > viewportHeight;
      showOrHideHandle(selector, cell, isAbove, isBelow);
    };
    const snapTopLeft = (cell) => snapTo2(topLeft, cell, getTopLeftSnap, "top");
    const snapLastTopLeft = () => startCell.get().each(snapTopLeft);
    const snapBottomRight = (cell) => snapTo2(bottomRight, cell, getBottomRightSnap, "bottom");
    const snapLastBottomRight = () => finishCell.get().each(snapBottomRight);
    if (detect$1().deviceType.isTouch()) {
      editor.on("TableSelectionChange", (e) => {
        if (!isVisible2.get()) {
          attach(sink, topLeft);
          attach(sink, bottomRight);
          isVisible2.set(true);
        }
        startCell.set(e.start);
        finishCell.set(e.finish);
        e.otherCells.each((otherCells) => {
          tlTds.set(otherCells.upOrLeftCells);
          brTds.set(otherCells.downOrRightCells);
          snapTopLeft(e.start);
          snapBottomRight(e.finish);
        });
      });
      editor.on("ResizeEditor ResizeWindow ScrollContent", () => {
        snapLastTopLeft();
        snapLastBottomRight();
      });
      editor.on("TableSelectionClear", () => {
        if (isVisible2.get()) {
          detach(topLeft);
          detach(bottomRight);
          isVisible2.set(false);
        }
        startCell.clear();
        finishCell.clear();
      });
    }
  };
  var Logo = '<svg width="50px" height="16px" viewBox="0 0 50 16" xmlns="http://www.w3.org/2000/svg">\n  <path fill-rule="evenodd" clip-rule="evenodd" d="M10.143 0c2.608.015 5.186 2.178 5.186 5.331 0 0 .077 3.812-.084 4.87-.361 2.41-2.164 4.074-4.65 4.496-1.453.284-2.523.49-3.212.623-.373.071-.634.122-.785.152-.184.038-.997.145-1.35.145-2.732 0-5.21-2.04-5.248-5.33 0 0 0-3.514.03-4.442.093-2.4 1.758-4.342 4.926-4.963 0 0 3.875-.752 4.036-.782.368-.07.775-.1 1.15-.1Zm1.826 2.8L5.83 3.989v2.393l-2.455.475v5.968l6.137-1.189V9.243l2.456-.476V2.8ZM5.83 6.382l3.682-.713v3.574l-3.682.713V6.382Zm27.173-1.64-.084-1.066h-2.226v9.132h2.456V7.743c-.008-1.151.998-2.064 2.149-2.072 1.15-.008 1.987.92 1.995 2.072v5.065h2.455V7.359c-.015-2.18-1.657-3.929-3.837-3.913a3.993 3.993 0 0 0-2.908 1.296Zm-6.3-4.266L29.16 0v2.387l-2.456.475V.476Zm0 3.2v9.132h2.456V3.676h-2.456Zm18.179 11.787L49.11 3.676H46.58l-1.612 4.527-.46 1.382-.384-1.382-1.611-4.527H39.98l3.3 9.132L42.15 16l2.732-.537ZM22.867 9.738c0 .752.568 1.075.921 1.075.353 0 .668-.047.998-.154l.537 1.765c-.23.154-.92.537-2.225.537-1.305 0-2.655-.997-2.686-2.686a136.877 136.877 0 0 1 0-4.374H18.8V3.676h1.612v-1.98l2.455-.476v2.456h2.302V5.9h-2.302v3.837Z"/>\n</svg>\n';
  const isHidden = (elm) => elm.nodeName === "BR" || !!elm.getAttribute("data-mce-bogus") || elm.getAttribute("data-mce-type") === "bookmark";
  const renderElementPath = (editor, settings, providersBackstage) => {
    var _a;
    const delimiter = (_a = settings.delimiter) !== null && _a !== void 0 ? _a : "\u203A";
    const renderElement = (name2, element2, index) => Button.sketch({
      dom: {
        tag: "div",
        classes: ["tox-statusbar__path-item"],
        attributes: {
          "data-index": index,
          "aria-level": index + 1
        }
      },
      components: [text$1(name2)],
      action: (_btn) => {
        editor.focus();
        editor.selection.select(element2);
        editor.nodeChanged();
      },
      buttonBehaviours: derive$1([
        DisablingConfigs.button(providersBackstage.isDisabled),
        receivingConfig()
      ])
    });
    const renderDivider = () => ({
      dom: {
        tag: "div",
        classes: ["tox-statusbar__path-divider"],
        attributes: { "aria-hidden": true }
      },
      components: [text$1(` ${delimiter} `)]
    });
    const renderPathData = (data) => foldl(data, (acc, path2, index) => {
      const element2 = renderElement(path2.name, path2.element, index);
      if (index === 0) {
        return acc.concat([element2]);
      } else {
        return acc.concat([
          renderDivider(),
          element2
        ]);
      }
    }, []);
    const updatePath = (parents) => {
      const newPath = [];
      let i2 = parents.length;
      while (i2-- > 0) {
        const parent2 = parents[i2];
        if (parent2.nodeType === 1 && !isHidden(parent2)) {
          const args = fireResolveName(editor, parent2);
          if (!args.isDefaultPrevented()) {
            newPath.push({
              name: args.name,
              element: parent2
            });
          }
          if (args.isPropagationStopped()) {
            break;
          }
        }
      }
      return newPath;
    };
    return {
      dom: {
        tag: "div",
        classes: ["tox-statusbar__path"],
        attributes: { role: "navigation" }
      },
      behaviours: derive$1([
        Keying.config({
          mode: "flow",
          selector: "div[role=button]"
        }),
        Disabling.config({ disabled: providersBackstage.isDisabled }),
        receivingConfig(),
        Tabstopping.config({}),
        Replacing.config({}),
        config("elementPathEvents", [runOnAttached((comp, _e) => {
          editor.shortcuts.add("alt+F11", "focus statusbar elementpath", () => Keying.focusIn(comp));
          editor.on("NodeChange", (e) => {
            const newPath = updatePath(e.parents);
            const newChildren = newPath.length > 0 ? renderPathData(newPath) : [];
            Replacing.set(comp, newChildren);
          });
        })])
      ]),
      components: []
    };
  };
  var ResizeTypes;
  (function(ResizeTypes2) {
    ResizeTypes2[ResizeTypes2["None"] = 0] = "None";
    ResizeTypes2[ResizeTypes2["Both"] = 1] = "Both";
    ResizeTypes2[ResizeTypes2["Vertical"] = 2] = "Vertical";
  })(ResizeTypes || (ResizeTypes = {}));
  const getDimensions = (editor, deltas, resizeType, originalHeight, originalWidth) => {
    const dimensions = { height: calcCappedSize(originalHeight + deltas.top, getMinHeightOption(editor), getMaxHeightOption(editor)) };
    if (resizeType === ResizeTypes.Both) {
      dimensions.width = calcCappedSize(originalWidth + deltas.left, getMinWidthOption(editor), getMaxWidthOption(editor));
    }
    return dimensions;
  };
  const resize = (editor, deltas, resizeType) => {
    const container = SugarElement.fromDom(editor.getContainer());
    const dimensions = getDimensions(editor, deltas, resizeType, get$d(container), get$c(container));
    each(dimensions, (val, dim) => {
      if (isNumber(val)) {
        set$8(container, dim, numToPx(val));
      }
    });
    fireResizeEditor(editor);
  };
  const getResizeType = (editor) => {
    const resize2 = getResize(editor);
    if (resize2 === false) {
      return ResizeTypes.None;
    } else if (resize2 === "both") {
      return ResizeTypes.Both;
    } else {
      return ResizeTypes.Vertical;
    }
  };
  const keyboardHandler = (editor, resizeType, x, y) => {
    const scale = 20;
    const delta = SugarPosition(x * scale, y * scale);
    resize(editor, delta, resizeType);
    return Optional.some(true);
  };
  const renderResizeHandler = (editor, providersBackstage) => {
    const resizeType = getResizeType(editor);
    if (resizeType === ResizeTypes.None) {
      return Optional.none();
    }
    return Optional.some(render$3("resize-handle", {
      tag: "div",
      classes: ["tox-statusbar__resize-handle"],
      attributes: { title: providersBackstage.translate("Resize") },
      behaviours: [
        Dragging.config({
          mode: "mouse",
          repositionTarget: false,
          onDrag: (_comp, _target, delta) => resize(editor, delta, resizeType),
          blockerClass: "tox-blocker"
        }),
        Keying.config({
          mode: "special",
          onLeft: () => keyboardHandler(editor, resizeType, -1, 0),
          onRight: () => keyboardHandler(editor, resizeType, 1, 0),
          onUp: () => keyboardHandler(editor, resizeType, 0, -1),
          onDown: () => keyboardHandler(editor, resizeType, 0, 1)
        }),
        Tabstopping.config({}),
        Focusing.config({})
      ]
    }, providersBackstage.icons));
  };
  const renderWordCount = (editor, providersBackstage) => {
    const replaceCountText = (comp, count, mode) => Replacing.set(comp, [text$1(providersBackstage.translate([
      "{0} " + mode,
      count[mode]
    ]))]);
    return Button.sketch({
      dom: {
        tag: "button",
        classes: ["tox-statusbar__wordcount"]
      },
      components: [],
      buttonBehaviours: derive$1([
        DisablingConfigs.button(providersBackstage.isDisabled),
        receivingConfig(),
        Tabstopping.config({}),
        Replacing.config({}),
        Representing.config({
          store: {
            mode: "memory",
            initialValue: {
              mode: "words",
              count: {
                words: 0,
                characters: 0
              }
            }
          }
        }),
        config("wordcount-events", [
          runOnExecute$1((comp) => {
            const currentVal = Representing.getValue(comp);
            const newMode = currentVal.mode === "words" ? "characters" : "words";
            Representing.setValue(comp, {
              mode: newMode,
              count: currentVal.count
            });
            replaceCountText(comp, currentVal.count, newMode);
          }),
          runOnAttached((comp) => {
            editor.on("wordCountUpdate", (e) => {
              const { mode } = Representing.getValue(comp);
              Representing.setValue(comp, {
                mode,
                count: e.wordCount
              });
              replaceCountText(comp, e.wordCount, mode);
            });
          })
        ])
      ]),
      eventOrder: {
        [execute$5()]: [
          "disabling",
          "alloy.base.behaviour",
          "wordcount-events"
        ]
      }
    });
  };
  const renderStatusbar = (editor, providersBackstage) => {
    const renderBranding = () => {
      return {
        dom: {
          tag: "span",
          classes: ["tox-statusbar__branding"]
        },
        components: [{
          dom: {
            tag: "a",
            attributes: {
              "href": "https://www.tiny.cloud/powered-by-tiny?utm_campaign=editor_referral&utm_medium=poweredby&utm_source=tinymce&utm_content=v6",
              "rel": "noopener",
              "target": "_blank",
              "aria-label": global$8.translate([
                "Powered by {0}",
                "Tiny"
              ])
            },
            innerHtml: Logo.trim()
          },
          behaviours: derive$1([Focusing.config({})])
        }]
      };
    };
    const getTextComponents = () => {
      const components2 = [];
      if (useElementPath(editor)) {
        components2.push(renderElementPath(editor, {}, providersBackstage));
      }
      if (editor.hasPlugin("wordcount")) {
        components2.push(renderWordCount(editor, providersBackstage));
      }
      if (useBranding(editor)) {
        components2.push(renderBranding());
      }
      if (components2.length > 0) {
        return [{
          dom: {
            tag: "div",
            classes: ["tox-statusbar__text-container"]
          },
          components: components2
        }];
      }
      return [];
    };
    const getComponents = () => {
      const components2 = getTextComponents();
      const resizeHandler = renderResizeHandler(editor, providersBackstage);
      return components2.concat(resizeHandler.toArray());
    };
    return {
      dom: {
        tag: "div",
        classes: ["tox-statusbar"]
      },
      components: getComponents()
    };
  };
  const getLazyMothership = (singleton2) => singleton2.get().getOrDie("UI has not been rendered");
  const setup$3 = (editor) => {
    const isInline = editor.inline;
    const mode = isInline ? Inline : Iframe;
    const header = isStickyToolbar(editor) ? StickyHeader : StaticHeader;
    const lazySink = value$2();
    const lazyOuterContainer = value$2();
    const lazyMothership = value$2();
    const lazyUiMothership = value$2();
    const platform2 = detect$1();
    const isTouch2 = platform2.deviceType.isTouch();
    const touchPlatformClass = "tox-platform-touch";
    const deviceClasses = isTouch2 ? [touchPlatformClass] : [];
    const isToolbarBottom = isToolbarLocationBottom(editor);
    const toolbarMode = getToolbarMode(editor);
    const memAnchorBar = record({
      dom: {
        tag: "div",
        classes: ["tox-anchorbar"]
      }
    });
    const lazyHeader = () => lazyOuterContainer.get().bind(OuterContainer.getHeader);
    const lazySinkResult = () => Result.fromOption(lazySink.get(), "UI has not been rendered");
    const lazyAnchorBar = () => lazyOuterContainer.get().bind((container) => memAnchorBar.getOpt(container)).getOrDie("Could not find a anchor bar element");
    const lazyToolbar = () => lazyOuterContainer.get().bind((container) => OuterContainer.getToolbar(container)).getOrDie("Could not find more toolbar element");
    const lazyThrobber = () => lazyOuterContainer.get().bind((container) => OuterContainer.getThrobber(container)).getOrDie("Could not find throbber element");
    const backstage = init$7(lazySinkResult, editor, lazyAnchorBar);
    const makeHeaderPart = () => {
      const verticalDirAttributes = { attributes: { [Attribute]: isToolbarBottom ? AttributeValue.BottomToTop : AttributeValue.TopToBottom } };
      const partMenubar2 = OuterContainer.parts.menubar({
        dom: {
          tag: "div",
          classes: ["tox-menubar"]
        },
        backstage,
        onEscape: () => {
          editor.focus();
        }
      });
      const partToolbar2 = OuterContainer.parts.toolbar({
        dom: {
          tag: "div",
          classes: ["tox-toolbar"]
        },
        getSink: lazySinkResult,
        providers: backstage.shared.providers,
        onEscape: () => {
          editor.focus();
        },
        type: toolbarMode,
        lazyToolbar,
        lazyHeader: () => lazyHeader().getOrDie("Could not find header element"),
        ...verticalDirAttributes
      });
      const partMultipleToolbar2 = OuterContainer.parts["multiple-toolbar"]({
        dom: {
          tag: "div",
          classes: ["tox-toolbar-overlord"]
        },
        providers: backstage.shared.providers,
        onEscape: () => {
          editor.focus();
        },
        type: toolbarMode
      });
      const hasMultipleToolbar = isMultipleToolbars(editor);
      const hasToolbar = isToolbarEnabled(editor);
      const hasMenubar = isMenubarEnabled(editor);
      const shouldHavePromotion = promotionEnabled(editor);
      const partPromotion2 = makePromotion();
      const getPartToolbar = () => {
        if (hasMultipleToolbar) {
          return [partMultipleToolbar2];
        } else if (hasToolbar) {
          return [partToolbar2];
        } else {
          return [];
        }
      };
      const menubarCollection = shouldHavePromotion ? [
        partPromotion2,
        partMenubar2
      ] : [partMenubar2];
      return OuterContainer.parts.header({
        dom: {
          tag: "div",
          classes: ["tox-editor-header"],
          ...verticalDirAttributes
        },
        components: flatten([
          hasMenubar ? menubarCollection : [],
          getPartToolbar(),
          useFixedContainer(editor) ? [] : [memAnchorBar.asSpec()]
        ]),
        sticky: isStickyToolbar(editor),
        editor,
        sharedBackstage: backstage.shared
      });
    };
    const makePromotion = () => {
      return OuterContainer.parts.promotion({
        dom: {
          tag: "div",
          classes: ["tox-promotion"]
        }
      });
    };
    const makeSidebarDefinition = () => {
      const partSocket2 = OuterContainer.parts.socket({
        dom: {
          tag: "div",
          classes: ["tox-edit-area"]
        }
      });
      const partSidebar2 = OuterContainer.parts.sidebar({
        dom: {
          tag: "div",
          classes: ["tox-sidebar"]
        }
      });
      return {
        dom: {
          tag: "div",
          classes: ["tox-sidebar-wrap"]
        },
        components: [
          partSocket2,
          partSidebar2
        ]
      };
    };
    const renderSink = () => {
      const uiContainer = getUiContainer(editor);
      const isGridUiContainer = eq(body(), uiContainer) && get$e(uiContainer, "display") === "grid";
      const sinkSpec = {
        dom: {
          tag: "div",
          classes: [
            "tox",
            "tox-silver-sink",
            "tox-tinymce-aux"
          ].concat(deviceClasses),
          attributes: { ...global$8.isRtl() ? { dir: "rtl" } : {} }
        },
        behaviours: derive$1([Positioning.config({ useFixed: () => header.isDocked(lazyHeader) })])
      };
      const reactiveWidthSpec = {
        dom: { styles: { width: document.body.clientWidth + "px" } },
        events: derive$2([run$1(windowResize(), (comp) => {
          set$8(comp.element, "width", document.body.clientWidth + "px");
        })])
      };
      const sink = build$1(deepMerge(sinkSpec, isGridUiContainer ? reactiveWidthSpec : {}));
      const uiMothership = takeover(sink);
      lazySink.set(sink);
      lazyUiMothership.set(uiMothership);
      return {
        sink,
        uiMothership
      };
    };
    const renderContainer2 = () => {
      const partHeader2 = makeHeaderPart();
      const sidebarContainer = makeSidebarDefinition();
      const partThrobber2 = OuterContainer.parts.throbber({
        dom: {
          tag: "div",
          classes: ["tox-throbber"]
        },
        backstage
      });
      const statusbar = useStatusBar(editor) && !isInline ? Optional.some(renderStatusbar(editor, backstage.shared.providers)) : Optional.none();
      const editorComponents = flatten([
        isToolbarBottom ? [] : [partHeader2],
        isInline ? [] : [sidebarContainer],
        isToolbarBottom ? [partHeader2] : []
      ]);
      const editorContainer = {
        dom: {
          tag: "div",
          classes: ["tox-editor-container"]
        },
        components: editorComponents
      };
      const containerComponents = flatten([
        [editorContainer],
        isInline ? [] : statusbar.toArray(),
        [partThrobber2]
      ]);
      const isHidden2 = isDistractionFree(editor);
      const attributes = {
        role: "application",
        ...global$8.isRtl() ? { dir: "rtl" } : {},
        ...isHidden2 ? { "aria-hidden": "true" } : {}
      };
      const outerContainer = build$1(OuterContainer.sketch({
        dom: {
          tag: "div",
          classes: [
            "tox",
            "tox-tinymce"
          ].concat(isInline ? ["tox-tinymce-inline"] : []).concat(isToolbarBottom ? ["tox-tinymce--toolbar-bottom"] : []).concat(deviceClasses),
          styles: {
            visibility: "hidden",
            ...isHidden2 ? {
              opacity: "0",
              border: "0"
            } : {}
          },
          attributes
        },
        components: containerComponents,
        behaviours: derive$1([
          receivingConfig(),
          Disabling.config({ disableClass: "tox-tinymce--disabled" }),
          Keying.config({
            mode: "cyclic",
            selector: ".tox-menubar, .tox-toolbar, .tox-toolbar__primary, .tox-toolbar__overflow--open, .tox-sidebar__overflow--open, .tox-statusbar__path, .tox-statusbar__wordcount, .tox-statusbar__branding a, .tox-statusbar__resize-handle"
          })
        ])
      }));
      const mothership = takeover(outerContainer);
      lazyOuterContainer.set(outerContainer);
      lazyMothership.set(mothership);
      return {
        mothership,
        outerContainer
      };
    };
    const setEditorSize = (outerContainer) => {
      const parsedHeight = numToPx(getHeightWithFallback(editor));
      const parsedWidth = numToPx(getWidthWithFallback(editor));
      if (!editor.inline) {
        if (isValidValue("div", "width", parsedWidth)) {
          set$8(outerContainer.element, "width", parsedWidth);
        }
        if (isValidValue("div", "height", parsedHeight)) {
          set$8(outerContainer.element, "height", parsedHeight);
        } else {
          set$8(outerContainer.element, "height", "400px");
        }
      }
      return parsedHeight;
    };
    const setupShortcutsAndCommands = (outerContainer) => {
      editor.addShortcut("alt+F9", "focus menubar", () => {
        OuterContainer.focusMenubar(outerContainer);
      });
      editor.addShortcut("alt+F10", "focus toolbar", () => {
        OuterContainer.focusToolbar(outerContainer);
      });
      editor.addCommand("ToggleToolbarDrawer", () => {
        OuterContainer.toggleToolbarDrawer(outerContainer);
      });
      editor.addQueryStateHandler("ToggleToolbarDrawer", () => OuterContainer.isToolbarDrawerToggled(outerContainer));
    };
    const renderUI = () => {
      const { mothership, outerContainer } = renderContainer2();
      const { uiMothership, sink } = renderSink();
      map$1(getToolbarGroups(editor), (toolbarGroupButtonConfig, name2) => {
        editor.ui.registry.addGroupToolbarButton(name2, toolbarGroupButtonConfig);
      });
      const { buttons, menuItems, contextToolbars, sidebars } = editor.ui.registry.getAll();
      const toolbarOpt = getMultipleToolbarsOption(editor);
      const rawUiConfig = {
        menuItems,
        menus: getMenus(editor),
        menubar: getMenubar(editor),
        toolbar: toolbarOpt.getOrThunk(() => getToolbar(editor)),
        allowToolbarGroups: toolbarMode === ToolbarMode$1.floating,
        buttons,
        sidebar: sidebars
      };
      setupShortcutsAndCommands(outerContainer);
      setup$b(editor, mothership, uiMothership);
      header.setup(editor, backstage.shared, lazyHeader);
      setup$6(editor, backstage);
      setup$5(editor, lazySinkResult, backstage);
      setup$8(editor);
      setup$7(editor, lazyThrobber, backstage.shared);
      register$9(editor, contextToolbars, sink, { backstage });
      setup$4(editor, sink);
      const elm = editor.getElement();
      const height2 = setEditorSize(outerContainer);
      const uiComponents = {
        mothership,
        uiMothership,
        outerContainer,
        sink
      };
      const args = {
        targetNode: elm,
        height: height2
      };
      return mode.render(editor, uiComponents, rawUiConfig, backstage, args);
    };
    const getMothership = () => getLazyMothership(lazyMothership);
    const getUiMothership = () => getLazyMothership(lazyUiMothership);
    return {
      getMothership,
      getUiMothership,
      backstage,
      renderUI
    };
  };
  const describedBy = (describedElement, describeElement) => {
    const describeId = Optional.from(get$f(describedElement, "id")).fold(() => {
      const id = generate$6("dialog-describe");
      set$9(describeElement, "id", id);
      return id;
    }, identity);
    set$9(describedElement, "aria-describedby", describeId);
  };
  const labelledBy = (labelledElement, labelElement) => {
    const labelId = getOpt(labelledElement, "id").fold(() => {
      const id = generate$6("dialog-label");
      set$9(labelElement, "id", id);
      return id;
    }, identity);
    set$9(labelledElement, "aria-labelledby", labelId);
  };
  const schema$2 = constant$1([
    required$1("lazySink"),
    option$3("dragBlockClass"),
    defaultedFunction("getBounds", win),
    defaulted("useTabstopAt", always),
    defaulted("eventOrder", {}),
    field("modalBehaviours", [Keying]),
    onKeyboardHandler("onExecute"),
    onStrictKeyboardHandler("onEscape")
  ]);
  const basic = { sketch: identity };
  const parts$2 = constant$1([
    optional({
      name: "draghandle",
      overrides: (detail, spec) => {
        return {
          behaviours: derive$1([Dragging.config({
            mode: "mouse",
            getTarget: (handle2) => {
              return ancestor(handle2, '[role="dialog"]').getOr(handle2);
            },
            blockerClass: detail.dragBlockClass.getOrDie(new Error("The drag blocker class was not specified for a dialog with a drag handle: \n" + JSON.stringify(spec, null, 2)).message),
            getBounds: detail.getDragBounds
          })])
        };
      }
    }),
    required({
      schema: [required$1("dom")],
      name: "title"
    }),
    required({
      factory: basic,
      schema: [required$1("dom")],
      name: "close"
    }),
    required({
      factory: basic,
      schema: [required$1("dom")],
      name: "body"
    }),
    optional({
      factory: basic,
      schema: [required$1("dom")],
      name: "footer"
    }),
    external({
      factory: {
        sketch: (spec, detail) => ({
          ...spec,
          dom: detail.dom,
          components: detail.components
        })
      },
      schema: [
        defaulted("dom", {
          tag: "div",
          styles: {
            position: "fixed",
            left: "0px",
            top: "0px",
            right: "0px",
            bottom: "0px"
          }
        }),
        defaulted("components", [])
      ],
      name: "blocker"
    })
  ]);
  const factory$4 = (detail, components2, spec, externals) => {
    const dialogComp = value$2();
    const showDialog = (dialog) => {
      dialogComp.set(dialog);
      const sink = detail.lazySink(dialog).getOrDie();
      const externalBlocker = externals.blocker();
      const blocker = sink.getSystem().build({
        ...externalBlocker,
        components: externalBlocker.components.concat([premade(dialog)]),
        behaviours: derive$1([
          Focusing.config({}),
          config("dialog-blocker-events", [runOnSource(focusin(), () => {
            Keying.focusIn(dialog);
          })])
        ])
      });
      attach(sink, blocker);
      Keying.focusIn(dialog);
    };
    const hideDialog = (dialog) => {
      dialogComp.clear();
      parent(dialog.element).each((blockerDom) => {
        dialog.getSystem().getByDom(blockerDom).each((blocker) => {
          detach(blocker);
        });
      });
    };
    const getDialogBody = (dialog) => getPartOrDie(dialog, detail, "body");
    const getDialogFooter = (dialog) => getPartOrDie(dialog, detail, "footer");
    const setBusy = (dialog, getBusySpec2) => {
      Blocking.block(dialog, getBusySpec2);
    };
    const setIdle = (dialog) => {
      Blocking.unblock(dialog);
    };
    const modalEventsId = generate$6("modal-events");
    const eventOrder = {
      ...detail.eventOrder,
      [attachedToDom()]: [modalEventsId].concat(detail.eventOrder["alloy.system.attached"] || [])
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      apis: {
        show: showDialog,
        hide: hideDialog,
        getBody: getDialogBody,
        getFooter: getDialogFooter,
        setIdle,
        setBusy
      },
      eventOrder,
      domModification: {
        attributes: {
          "role": "dialog",
          "aria-modal": "true"
        }
      },
      behaviours: augment(detail.modalBehaviours, [
        Replacing.config({}),
        Keying.config({
          mode: "cyclic",
          onEnter: detail.onExecute,
          onEscape: detail.onEscape,
          useTabstopAt: detail.useTabstopAt
        }),
        Blocking.config({ getRoot: dialogComp.get }),
        config(modalEventsId, [runOnAttached((c) => {
          labelledBy(c.element, getPartOrDie(c, detail, "title").element);
          describedBy(c.element, getPartOrDie(c, detail, "body").element);
        })])
      ])
    };
  };
  const ModalDialog = composite({
    name: "ModalDialog",
    configFields: schema$2(),
    partFields: parts$2(),
    factory: factory$4,
    apis: {
      show: (apis, dialog) => {
        apis.show(dialog);
      },
      hide: (apis, dialog) => {
        apis.hide(dialog);
      },
      getBody: (apis, dialog) => apis.getBody(dialog),
      getFooter: (apis, dialog) => apis.getFooter(dialog),
      setBusy: (apis, dialog, getBusySpec2) => {
        apis.setBusy(dialog, getBusySpec2);
      },
      setIdle: (apis, dialog) => {
        apis.setIdle(dialog);
      }
    }
  });
  const dialogToggleMenuItemSchema = objOf([
    type,
    name$1
  ].concat(commonMenuItemFields));
  const dialogToggleMenuItemDataProcessor = boolean;
  const baseFooterButtonFields = [
    generatedName("button"),
    optionalIcon,
    defaultedStringEnum("align", "end", [
      "start",
      "end"
    ]),
    primary,
    enabled,
    optionStringEnum("buttonType", [
      "primary",
      "secondary"
    ])
  ];
  const dialogFooterButtonFields = [
    ...baseFooterButtonFields,
    text
  ];
  const normalFooterButtonFields = [
    requiredStringEnum("type", [
      "submit",
      "cancel",
      "custom"
    ]),
    ...dialogFooterButtonFields
  ];
  const menuFooterButtonFields = [
    requiredStringEnum("type", ["menu"]),
    optionalText,
    optionalTooltip,
    optionalIcon,
    requiredArrayOf("items", dialogToggleMenuItemSchema),
    ...baseFooterButtonFields
  ];
  const dialogFooterButtonSchema = choose$1("type", {
    submit: normalFooterButtonFields,
    cancel: normalFooterButtonFields,
    custom: normalFooterButtonFields,
    menu: menuFooterButtonFields
  });
  const alertBannerFields = [
    type,
    text,
    requiredStringEnum("level", [
      "info",
      "warn",
      "error",
      "success"
    ]),
    icon,
    defaulted("url", "")
  ];
  const alertBannerSchema = objOf(alertBannerFields);
  const createBarFields = (itemsField) => [
    type,
    itemsField
  ];
  const buttonFields = [
    type,
    text,
    enabled,
    generatedName("button"),
    optionalIcon,
    borderless,
    optionStringEnum("buttonType", [
      "primary",
      "secondary",
      "toolbar"
    ]),
    primary
  ];
  const buttonSchema = objOf(buttonFields);
  const formComponentFields = [
    type,
    name$1
  ];
  const formComponentWithLabelFields = formComponentFields.concat([optionalLabel]);
  const checkboxFields = formComponentFields.concat([
    label,
    enabled
  ]);
  const checkboxSchema = objOf(checkboxFields);
  const checkboxDataProcessor = boolean;
  const collectionFields = formComponentWithLabelFields.concat([defaultedColumns("auto")]);
  const collectionSchema = objOf(collectionFields);
  const collectionDataProcessor = arrOfObj([
    value$1,
    text,
    icon
  ]);
  const colorInputFields = formComponentWithLabelFields;
  const colorInputSchema = objOf(colorInputFields);
  const colorInputDataProcessor = string;
  const colorPickerFields = formComponentWithLabelFields;
  const colorPickerSchema = objOf(colorPickerFields);
  const colorPickerDataProcessor = string;
  const customEditorFields = formComponentFields.concat([
    defaultedString("tag", "textarea"),
    requiredString("scriptId"),
    requiredString("scriptUrl"),
    defaultedPostMsg("settings", void 0)
  ]);
  const customEditorFieldsOld = formComponentFields.concat([
    defaultedString("tag", "textarea"),
    requiredFunction("init")
  ]);
  const customEditorSchema = valueOf((v) => asRaw("customeditor.old", objOfOnly(customEditorFieldsOld), v).orThunk(() => asRaw("customeditor.new", objOfOnly(customEditorFields), v)));
  const customEditorDataProcessor = string;
  const dropZoneFields = formComponentWithLabelFields;
  const dropZoneSchema = objOf(dropZoneFields);
  const dropZoneDataProcessor = arrOfVal();
  const createGridFields = (itemsField) => [
    type,
    requiredNumber("columns"),
    itemsField
  ];
  const htmlPanelFields = [
    type,
    requiredString("html"),
    defaultedStringEnum("presets", "presentation", [
      "presentation",
      "document"
    ])
  ];
  const htmlPanelSchema = objOf(htmlPanelFields);
  const iframeFields = formComponentWithLabelFields.concat([
    defaultedBoolean("sandboxed", true),
    defaultedBoolean("transparent", true)
  ]);
  const iframeSchema = objOf(iframeFields);
  const iframeDataProcessor = string;
  const imagePreviewSchema = objOf(formComponentFields.concat([optionString("height")]));
  const imagePreviewDataProcessor = objOf([
    requiredString("url"),
    optionNumber("zoom"),
    optionNumber("cachedWidth"),
    optionNumber("cachedHeight")
  ]);
  const inputFields = formComponentWithLabelFields.concat([
    optionString("inputMode"),
    optionString("placeholder"),
    defaultedBoolean("maximized", false),
    enabled
  ]);
  const inputSchema = objOf(inputFields);
  const inputDataProcessor = string;
  const createLabelFields = (itemsField) => [
    type,
    label,
    itemsField
  ];
  const listBoxSingleItemFields = [
    text,
    value$1
  ];
  const listBoxNestedItemFields = [
    text,
    requiredArrayOf("items", thunkOf("items", () => listBoxItemSchema))
  ];
  const listBoxItemSchema = oneOf([
    objOf(listBoxSingleItemFields),
    objOf(listBoxNestedItemFields)
  ]);
  const listBoxFields = formComponentWithLabelFields.concat([
    requiredArrayOf("items", listBoxItemSchema),
    enabled
  ]);
  const listBoxSchema = objOf(listBoxFields);
  const listBoxDataProcessor = string;
  const selectBoxFields = formComponentWithLabelFields.concat([
    requiredArrayOfObj("items", [
      text,
      value$1
    ]),
    defaultedNumber("size", 1),
    enabled
  ]);
  const selectBoxSchema = objOf(selectBoxFields);
  const selectBoxDataProcessor = string;
  const sizeInputFields = formComponentWithLabelFields.concat([
    defaultedBoolean("constrain", true),
    enabled
  ]);
  const sizeInputSchema = objOf(sizeInputFields);
  const sizeInputDataProcessor = objOf([
    requiredString("width"),
    requiredString("height")
  ]);
  const sliderFields = formComponentFields.concat([
    label,
    defaultedNumber("min", 0),
    defaultedNumber("max", 0)
  ]);
  const sliderSchema = objOf(sliderFields);
  const sliderInputDataProcessor = number;
  const tableFields = [
    type,
    requiredArrayOf("header", string),
    requiredArrayOf("cells", arrOf(string))
  ];
  const tableSchema = objOf(tableFields);
  const textAreaFields = formComponentWithLabelFields.concat([
    optionString("placeholder"),
    defaultedBoolean("maximized", false),
    enabled
  ]);
  const textAreaSchema = objOf(textAreaFields);
  const textAreaDataProcessor = string;
  const urlInputFields = formComponentWithLabelFields.concat([
    defaultedStringEnum("filetype", "file", [
      "image",
      "media",
      "file"
    ]),
    enabled
  ]);
  const urlInputSchema = objOf(urlInputFields);
  const urlInputDataProcessor = objOf([
    value$1,
    defaultedMeta
  ]);
  const createItemsField = (name2) => field$1("items", "items", required$2(), arrOf(valueOf((v) => asRaw(`Checking item of ${name2}`, itemSchema, v).fold((sErr) => Result.error(formatError(sErr)), (passValue) => Result.value(passValue)))));
  const itemSchema = valueThunk(() => choose$2("type", {
    alertbanner: alertBannerSchema,
    bar: objOf(createBarFields(createItemsField("bar"))),
    button: buttonSchema,
    checkbox: checkboxSchema,
    colorinput: colorInputSchema,
    colorpicker: colorPickerSchema,
    dropzone: dropZoneSchema,
    grid: objOf(createGridFields(createItemsField("grid"))),
    iframe: iframeSchema,
    input: inputSchema,
    listbox: listBoxSchema,
    selectbox: selectBoxSchema,
    sizeinput: sizeInputSchema,
    slider: sliderSchema,
    textarea: textAreaSchema,
    urlinput: urlInputSchema,
    customeditor: customEditorSchema,
    htmlpanel: htmlPanelSchema,
    imagepreview: imagePreviewSchema,
    collection: collectionSchema,
    label: objOf(createLabelFields(createItemsField("label"))),
    table: tableSchema,
    panel: panelSchema
  }));
  const panelFields = [
    type,
    defaulted("classes", []),
    requiredArrayOf("items", itemSchema)
  ];
  const panelSchema = objOf(panelFields);
  const tabFields = [
    generatedName("tab"),
    title,
    requiredArrayOf("items", itemSchema)
  ];
  const tabPanelFields = [
    type,
    requiredArrayOfObj("tabs", tabFields)
  ];
  const tabPanelSchema = objOf(tabPanelFields);
  const dialogButtonFields = dialogFooterButtonFields;
  const dialogButtonSchema = dialogFooterButtonSchema;
  const dialogSchema = objOf([
    requiredString("title"),
    requiredOf("body", choose$2("type", {
      panel: panelSchema,
      tabpanel: tabPanelSchema
    })),
    defaultedString("size", "normal"),
    requiredArrayOf("buttons", dialogButtonSchema),
    defaulted("initialData", {}),
    defaultedFunction("onAction", noop),
    defaultedFunction("onChange", noop),
    defaultedFunction("onSubmit", noop),
    defaultedFunction("onClose", noop),
    defaultedFunction("onCancel", noop),
    defaultedFunction("onTabChange", noop)
  ]);
  const createDialog = (spec) => asRaw("dialog", dialogSchema, spec);
  const urlDialogButtonSchema = objOf([
    requiredStringEnum("type", [
      "cancel",
      "custom"
    ]),
    ...dialogButtonFields
  ]);
  const urlDialogSchema = objOf([
    requiredString("title"),
    requiredString("url"),
    optionNumber("height"),
    optionNumber("width"),
    optionArrayOf("buttons", urlDialogButtonSchema),
    defaultedFunction("onAction", noop),
    defaultedFunction("onCancel", noop),
    defaultedFunction("onClose", noop),
    defaultedFunction("onMessage", noop)
  ]);
  const createUrlDialog = (spec) => asRaw("dialog", urlDialogSchema, spec);
  const getAllObjects = (obj) => {
    if (isObject(obj)) {
      return [obj].concat(bind$3(values(obj), getAllObjects));
    } else if (isArray(obj)) {
      return bind$3(obj, getAllObjects);
    } else {
      return [];
    }
  };
  const isNamedItem = (obj) => isString(obj.type) && isString(obj.name);
  const dataProcessors = {
    checkbox: checkboxDataProcessor,
    colorinput: colorInputDataProcessor,
    colorpicker: colorPickerDataProcessor,
    dropzone: dropZoneDataProcessor,
    input: inputDataProcessor,
    iframe: iframeDataProcessor,
    imagepreview: imagePreviewDataProcessor,
    selectbox: selectBoxDataProcessor,
    sizeinput: sizeInputDataProcessor,
    slider: sliderInputDataProcessor,
    listbox: listBoxDataProcessor,
    size: sizeInputDataProcessor,
    textarea: textAreaDataProcessor,
    urlinput: urlInputDataProcessor,
    customeditor: customEditorDataProcessor,
    collection: collectionDataProcessor,
    togglemenuitem: dialogToggleMenuItemDataProcessor
  };
  const getDataProcessor = (item2) => Optional.from(dataProcessors[item2.type]);
  const getNamedItems = (structure) => filter$2(getAllObjects(structure), isNamedItem);
  const createDataValidator = (structure) => {
    const namedItems = getNamedItems(structure);
    const fields = bind$3(namedItems, (item2) => getDataProcessor(item2).fold(() => [], (schema2) => [requiredOf(item2.name, schema2)]));
    return objOf(fields);
  };
  const extract = (structure) => {
    var _a;
    const internalDialog = getOrDie(createDialog(structure));
    const dataValidator = createDataValidator(structure);
    const initialData = (_a = structure.initialData) !== null && _a !== void 0 ? _a : {};
    return {
      internalDialog,
      dataValidator,
      initialData
    };
  };
  const DialogManager = {
    open: (factory2, structure) => {
      const extraction = extract(structure);
      return factory2(extraction.internalDialog, extraction.initialData, extraction.dataValidator);
    },
    openUrl: (factory2, structure) => {
      const internalDialog = getOrDie(createUrlDialog(structure));
      return factory2(internalDialog);
    },
    redial: (structure) => extract(structure)
  };
  const toValidValues = (values2) => {
    const errors = [];
    const result = {};
    each(values2, (value2, name2) => {
      value2.fold(() => {
        errors.push(name2);
      }, (v) => {
        result[name2] = v;
      });
    });
    return errors.length > 0 ? Result.error(errors) : Result.value(result);
  };
  const renderBodyPanel = (spec, dialogData, backstage) => {
    const memForm = record(Form.sketch((parts2) => ({
      dom: {
        tag: "div",
        classes: ["tox-form"].concat(spec.classes)
      },
      components: map$2(spec.items, (item2) => interpretInForm(parts2, item2, dialogData, backstage))
    })));
    return {
      dom: {
        tag: "div",
        classes: ["tox-dialog__body"]
      },
      components: [{
        dom: {
          tag: "div",
          classes: ["tox-dialog__body-content"]
        },
        components: [memForm.asSpec()]
      }],
      behaviours: derive$1([
        Keying.config({
          mode: "acyclic",
          useTabstopAt: not(isPseudoStop)
        }),
        ComposingConfigs.memento(memForm),
        RepresentingConfigs.memento(memForm, {
          postprocess: (formValue) => toValidValues(formValue).fold((err) => {
            console.error(err);
            return {};
          }, identity)
        })
      ])
    };
  };
  const factory$3 = (detail, _spec) => ({
    uid: detail.uid,
    dom: detail.dom,
    components: detail.components,
    events: events$a(detail.action),
    behaviours: augment(detail.tabButtonBehaviours, [
      Focusing.config({}),
      Keying.config({
        mode: "execution",
        useSpace: true,
        useEnter: true
      }),
      Representing.config({
        store: {
          mode: "memory",
          initialValue: detail.value
        }
      })
    ]),
    domModification: detail.domModification
  });
  const TabButton = single({
    name: "TabButton",
    configFields: [
      defaulted("uid", void 0),
      required$1("value"),
      field$1("dom", "dom", mergeWithThunk(() => ({
        attributes: {
          "role": "tab",
          "id": generate$6("aria"),
          "aria-selected": "false"
        }
      })), anyValue()),
      option$3("action"),
      defaulted("domModification", {}),
      field("tabButtonBehaviours", [
        Focusing,
        Keying,
        Representing
      ]),
      required$1("view")
    ],
    factory: factory$3
  });
  const schema$1 = constant$1([
    required$1("tabs"),
    required$1("dom"),
    defaulted("clickToDismiss", false),
    field("tabbarBehaviours", [
      Highlighting,
      Keying
    ]),
    markers$1([
      "tabClass",
      "selectedClass"
    ])
  ]);
  const tabsPart = group({
    factory: TabButton,
    name: "tabs",
    unit: "tab",
    overrides: (barDetail) => {
      const dismissTab$1 = (tabbar, button2) => {
        Highlighting.dehighlight(tabbar, button2);
        emitWith(tabbar, dismissTab(), {
          tabbar,
          button: button2
        });
      };
      const changeTab$1 = (tabbar, button2) => {
        Highlighting.highlight(tabbar, button2);
        emitWith(tabbar, changeTab(), {
          tabbar,
          button: button2
        });
      };
      return {
        action: (button2) => {
          const tabbar = button2.getSystem().getByUid(barDetail.uid).getOrDie();
          const activeButton = Highlighting.isHighlighted(tabbar, button2);
          const response = (() => {
            if (activeButton && barDetail.clickToDismiss) {
              return dismissTab$1;
            } else if (!activeButton) {
              return changeTab$1;
            } else {
              return noop;
            }
          })();
          response(tabbar, button2);
        },
        domModification: { classes: [barDetail.markers.tabClass] }
      };
    }
  });
  const parts$1 = constant$1([tabsPart]);
  const factory$2 = (detail, components2, _spec, _externals) => ({
    "uid": detail.uid,
    "dom": detail.dom,
    components: components2,
    "debug.sketcher": "Tabbar",
    "domModification": { attributes: { role: "tablist" } },
    "behaviours": augment(detail.tabbarBehaviours, [
      Highlighting.config({
        highlightClass: detail.markers.selectedClass,
        itemClass: detail.markers.tabClass,
        onHighlight: (tabbar, tab) => {
          set$9(tab.element, "aria-selected", "true");
        },
        onDehighlight: (tabbar, tab) => {
          set$9(tab.element, "aria-selected", "false");
        }
      }),
      Keying.config({
        mode: "flow",
        getInitial: (tabbar) => {
          return Highlighting.getHighlighted(tabbar).map((tab) => tab.element);
        },
        selector: "." + detail.markers.tabClass,
        executeOnMove: true
      })
    ])
  });
  const Tabbar = composite({
    name: "Tabbar",
    configFields: schema$1(),
    partFields: parts$1(),
    factory: factory$2
  });
  const factory$1 = (detail, _spec) => ({
    uid: detail.uid,
    dom: detail.dom,
    behaviours: augment(detail.tabviewBehaviours, [Replacing.config({})]),
    domModification: { attributes: { role: "tabpanel" } }
  });
  const Tabview = single({
    name: "Tabview",
    configFields: [field("tabviewBehaviours", [Replacing])],
    factory: factory$1
  });
  const schema = constant$1([
    defaulted("selectFirst", true),
    onHandler("onChangeTab"),
    onHandler("onDismissTab"),
    defaulted("tabs", []),
    field("tabSectionBehaviours", [])
  ]);
  const barPart = required({
    factory: Tabbar,
    schema: [
      required$1("dom"),
      requiredObjOf("markers", [
        required$1("tabClass"),
        required$1("selectedClass")
      ])
    ],
    name: "tabbar",
    defaults: (detail) => {
      return { tabs: detail.tabs };
    }
  });
  const viewPart = required({
    factory: Tabview,
    name: "tabview"
  });
  const parts = constant$1([
    barPart,
    viewPart
  ]);
  const factory = (detail, components2, _spec, _externals) => {
    const changeTab$1 = (button2) => {
      const tabValue = Representing.getValue(button2);
      getPart(button2, detail, "tabview").each((tabview) => {
        const tabWithValue = find$5(detail.tabs, (t2) => t2.value === tabValue);
        tabWithValue.each((tabData) => {
          const panel = tabData.view();
          getOpt(button2.element, "id").each((id) => {
            set$9(tabview.element, "aria-labelledby", id);
          });
          Replacing.set(tabview, panel);
          detail.onChangeTab(tabview, button2, panel);
        });
      });
    };
    const changeTabBy = (section, byPred) => {
      getPart(section, detail, "tabbar").each((tabbar) => {
        byPred(tabbar).each(emitExecute);
      });
    };
    return {
      uid: detail.uid,
      dom: detail.dom,
      components: components2,
      behaviours: get$3(detail.tabSectionBehaviours),
      events: derive$2(flatten([
        detail.selectFirst ? [runOnAttached((section, _simulatedEvent) => {
          changeTabBy(section, Highlighting.getFirst);
        })] : [],
        [
          run$1(changeTab(), (section, simulatedEvent) => {
            const button2 = simulatedEvent.event.button;
            changeTab$1(button2);
          }),
          run$1(dismissTab(), (section, simulatedEvent) => {
            const button2 = simulatedEvent.event.button;
            detail.onDismissTab(section, button2);
          })
        ]
      ])),
      apis: {
        getViewItems: (section) => {
          return getPart(section, detail, "tabview").map((tabview) => Replacing.contents(tabview)).getOr([]);
        },
        showTab: (section, tabKey) => {
          const getTabIfNotActive = (tabbar) => {
            const candidates = Highlighting.getCandidates(tabbar);
            const optTab = find$5(candidates, (c) => Representing.getValue(c) === tabKey);
            return optTab.filter((tab) => !Highlighting.isHighlighted(tabbar, tab));
          };
          changeTabBy(section, getTabIfNotActive);
        }
      }
    };
  };
  const TabSection = composite({
    name: "TabSection",
    configFields: schema(),
    partFields: parts(),
    factory,
    apis: {
      getViewItems: (apis, component) => apis.getViewItems(component),
      showTab: (apis, component, tabKey) => {
        apis.showTab(component, tabKey);
      }
    }
  });
  const measureHeights = (allTabs, tabview, tabviewComp) => map$2(allTabs, (_tab, i2) => {
    Replacing.set(tabviewComp, allTabs[i2].view());
    const rect2 = tabview.dom.getBoundingClientRect();
    Replacing.set(tabviewComp, []);
    return rect2.height;
  });
  const getMaxHeight = (heights) => head(sort(heights, (a, b2) => {
    if (a > b2) {
      return -1;
    } else if (a < b2) {
      return 1;
    } else {
      return 0;
    }
  }));
  const getMaxTabviewHeight = (dialog, tabview, tablist) => {
    const documentElement$1 = documentElement(dialog).dom;
    const rootElm = ancestor(dialog, ".tox-dialog-wrap").getOr(dialog);
    const isFixed = get$e(rootElm, "position") === "fixed";
    let maxHeight;
    if (isFixed) {
      maxHeight = Math.max(documentElement$1.clientHeight, window.innerHeight);
    } else {
      maxHeight = Math.max(documentElement$1.offsetHeight, documentElement$1.scrollHeight);
    }
    const tabviewHeight = get$d(tabview);
    const isTabListBeside = tabview.dom.offsetLeft >= tablist.dom.offsetLeft + get$c(tablist);
    const currentTabHeight = isTabListBeside ? Math.max(get$d(tablist), tabviewHeight) : tabviewHeight;
    const dialogTopMargin = parseInt(get$e(dialog, "margin-top"), 10) || 0;
    const dialogBottomMargin = parseInt(get$e(dialog, "margin-bottom"), 10) || 0;
    const dialogHeight = get$d(dialog) + dialogTopMargin + dialogBottomMargin;
    const chromeHeight = dialogHeight - currentTabHeight;
    return maxHeight - chromeHeight;
  };
  const showTab = (allTabs, comp) => {
    head(allTabs).each((tab) => TabSection.showTab(comp, tab.value));
  };
  const setTabviewHeight = (tabview, height2) => {
    set$8(tabview, "height", height2 + "px");
    set$8(tabview, "flex-basis", height2 + "px");
  };
  const updateTabviewHeight = (dialogBody, tabview, maxTabHeight) => {
    ancestor(dialogBody, '[role="dialog"]').each((dialog) => {
      descendant(dialog, '[role="tablist"]').each((tablist) => {
        maxTabHeight.get().map((height2) => {
          set$8(tabview, "height", "0");
          set$8(tabview, "flex-basis", "0");
          return Math.min(height2, getMaxTabviewHeight(dialog, tabview, tablist));
        }).each((height2) => {
          setTabviewHeight(tabview, height2);
        });
      });
    });
  };
  const getTabview = (dialog) => descendant(dialog, '[role="tabpanel"]');
  const smartMode = (allTabs) => {
    const maxTabHeight = value$2();
    const extraEvents = [
      runOnAttached((comp) => {
        const dialog = comp.element;
        getTabview(dialog).each((tabview) => {
          set$8(tabview, "visibility", "hidden");
          comp.getSystem().getByDom(tabview).toOptional().each((tabviewComp) => {
            const heights = measureHeights(allTabs, tabview, tabviewComp);
            const maxTabHeightOpt = getMaxHeight(heights);
            maxTabHeightOpt.fold(maxTabHeight.clear, maxTabHeight.set);
          });
          updateTabviewHeight(dialog, tabview, maxTabHeight);
          remove$6(tabview, "visibility");
          showTab(allTabs, comp);
          requestAnimationFrame(() => {
            updateTabviewHeight(dialog, tabview, maxTabHeight);
          });
        });
      }),
      run$1(windowResize(), (comp) => {
        const dialog = comp.element;
        getTabview(dialog).each((tabview) => {
          updateTabviewHeight(dialog, tabview, maxTabHeight);
        });
      }),
      run$1(formResizeEvent, (comp, _se) => {
        const dialog = comp.element;
        getTabview(dialog).each((tabview) => {
          const oldFocus = active$1(getRootNode(tabview));
          set$8(tabview, "visibility", "hidden");
          const oldHeight = getRaw(tabview, "height").map((h) => parseInt(h, 10));
          remove$6(tabview, "height");
          remove$6(tabview, "flex-basis");
          const newHeight = tabview.dom.getBoundingClientRect().height;
          const hasGrown2 = oldHeight.forall((h) => newHeight > h);
          if (hasGrown2) {
            maxTabHeight.set(newHeight);
            updateTabviewHeight(dialog, tabview, maxTabHeight);
          } else {
            oldHeight.each((h) => {
              setTabviewHeight(tabview, h);
            });
          }
          remove$6(tabview, "visibility");
          oldFocus.each(focus$3);
        });
      })
    ];
    const selectFirst = false;
    return {
      extraEvents,
      selectFirst
    };
  };
  const SendDataToSectionChannel = "send-data-to-section";
  const SendDataToViewChannel = "send-data-to-view";
  const renderTabPanel = (spec, dialogData, backstage) => {
    const storedValue = Cell({});
    const updateDataWithForm = (form) => {
      const formData = Representing.getValue(form);
      const validData = toValidValues(formData).getOr({});
      const currentData = storedValue.get();
      const newData = deepMerge(currentData, validData);
      storedValue.set(newData);
    };
    const setDataOnForm = (form) => {
      const tabData = storedValue.get();
      Representing.setValue(form, tabData);
    };
    const oldTab = Cell(null);
    const allTabs = map$2(spec.tabs, (tab) => {
      return {
        value: tab.name,
        dom: {
          tag: "div",
          classes: ["tox-dialog__body-nav-item"]
        },
        components: [text$1(backstage.shared.providers.translate(tab.title))],
        view: () => {
          return [Form.sketch((parts2) => ({
            dom: {
              tag: "div",
              classes: ["tox-form"]
            },
            components: map$2(tab.items, (item2) => interpretInForm(parts2, item2, dialogData, backstage)),
            formBehaviours: derive$1([
              Keying.config({
                mode: "acyclic",
                useTabstopAt: not(isPseudoStop)
              }),
              config("TabView.form.events", [
                runOnAttached(setDataOnForm),
                runOnDetached(updateDataWithForm)
              ]),
              Receiving.config({
                channels: wrapAll([
                  {
                    key: SendDataToSectionChannel,
                    value: { onReceive: updateDataWithForm }
                  },
                  {
                    key: SendDataToViewChannel,
                    value: { onReceive: setDataOnForm }
                  }
                ])
              })
            ])
          }))];
        }
      };
    });
    const tabMode = smartMode(allTabs);
    return TabSection.sketch({
      dom: {
        tag: "div",
        classes: ["tox-dialog__body"]
      },
      onChangeTab: (section, button2, _viewItems) => {
        const name2 = Representing.getValue(button2);
        emitWith(section, formTabChangeEvent, {
          name: name2,
          oldName: oldTab.get()
        });
        oldTab.set(name2);
      },
      tabs: allTabs,
      components: [
        TabSection.parts.tabbar({
          dom: {
            tag: "div",
            classes: ["tox-dialog__body-nav"]
          },
          components: [Tabbar.parts.tabs({})],
          markers: {
            tabClass: "tox-tab",
            selectedClass: "tox-dialog__body-nav-item--active"
          },
          tabbarBehaviours: derive$1([Tabstopping.config({})])
        }),
        TabSection.parts.tabview({
          dom: {
            tag: "div",
            classes: ["tox-dialog__body-content"]
          }
        })
      ],
      selectFirst: tabMode.selectFirst,
      tabSectionBehaviours: derive$1([
        config("tabpanel", tabMode.extraEvents),
        Keying.config({ mode: "acyclic" }),
        Composing.config({ find: (comp) => head(TabSection.getViewItems(comp)) }),
        RepresentingConfigs.withComp(Optional.none(), (tsection) => {
          tsection.getSystem().broadcastOn([SendDataToSectionChannel], {});
          return storedValue.get();
        }, (tsection, value2) => {
          storedValue.set(value2);
          tsection.getSystem().broadcastOn([SendDataToViewChannel], {});
        })
      ])
    });
  };
  const dialogChannel = generate$6("update-dialog");
  const titleChannel = generate$6("update-title");
  const bodyChannel = generate$6("update-body");
  const footerChannel = generate$6("update-footer");
  const bodySendMessageChannel = generate$6("body-send-message");
  const renderBody = (spec, dialogId, contentId, backstage, ariaAttrs) => {
    const renderComponents2 = (incoming) => {
      const body2 = incoming.body;
      switch (body2.type) {
        case "tabpanel": {
          return [renderTabPanel(body2, incoming.initialData, backstage)];
        }
        default: {
          return [renderBodyPanel(body2, incoming.initialData, backstage)];
        }
      }
    };
    const updateState = (_comp, incoming) => Optional.some({ isTabPanel: () => incoming.body.type === "tabpanel" });
    const ariaAttributes = { "aria-live": "polite" };
    return {
      dom: {
        tag: "div",
        classes: ["tox-dialog__content-js"],
        attributes: {
          ...contentId.map((x) => ({ id: x })).getOr({}),
          ...ariaAttrs ? ariaAttributes : {}
        }
      },
      components: [],
      behaviours: derive$1([
        ComposingConfigs.childAt(0),
        Reflecting.config({
          channel: `${bodyChannel}-${dialogId}`,
          updateState,
          renderComponents: renderComponents2,
          initialData: spec
        })
      ])
    };
  };
  const renderInlineBody = (spec, dialogId, contentId, backstage, ariaAttrs) => renderBody(spec, dialogId, Optional.some(contentId), backstage, ariaAttrs);
  const renderModalBody = (spec, dialogId, backstage) => {
    const bodySpec = renderBody(spec, dialogId, Optional.none(), backstage, false);
    return ModalDialog.parts.body(bodySpec);
  };
  const renderIframeBody = (spec) => {
    const bodySpec = {
      dom: {
        tag: "div",
        classes: ["tox-dialog__content-js"]
      },
      components: [{
        dom: {
          tag: "div",
          classes: ["tox-dialog__body-iframe"]
        },
        components: [craft({
          dom: {
            tag: "iframe",
            attributes: { src: spec.url }
          },
          behaviours: derive$1([
            Tabstopping.config({}),
            Focusing.config({})
          ])
        })]
      }],
      behaviours: derive$1([Keying.config({
        mode: "acyclic",
        useTabstopAt: not(isPseudoStop)
      })])
    };
    return ModalDialog.parts.body(bodySpec);
  };
  const isTouch = global$5.deviceType.isTouch();
  const hiddenHeader = (title2, close2) => ({
    dom: {
      tag: "div",
      styles: { display: "none" },
      classes: ["tox-dialog__header"]
    },
    components: [
      title2,
      close2
    ]
  });
  const pClose = (onClose, providersBackstage) => ModalDialog.parts.close(Button.sketch({
    dom: {
      tag: "button",
      classes: [
        "tox-button",
        "tox-button--icon",
        "tox-button--naked"
      ],
      attributes: {
        "type": "button",
        "aria-label": providersBackstage.translate("Close")
      }
    },
    action: onClose,
    buttonBehaviours: derive$1([Tabstopping.config({})])
  }));
  const pUntitled = () => ModalDialog.parts.title({
    dom: {
      tag: "div",
      classes: ["tox-dialog__title"],
      innerHtml: "",
      styles: { display: "none" }
    }
  });
  const pBodyMessage = (message, providersBackstage) => ModalDialog.parts.body({
    dom: {
      tag: "div",
      classes: ["tox-dialog__body"]
    },
    components: [{
      dom: {
        tag: "div",
        classes: ["tox-dialog__body-content"]
      },
      components: [{ dom: fromHtml(`<p>${providersBackstage.translate(message)}</p>`) }]
    }]
  });
  const pFooter = (buttons) => ModalDialog.parts.footer({
    dom: {
      tag: "div",
      classes: ["tox-dialog__footer"]
    },
    components: buttons
  });
  const pFooterGroup = (startButtons, endButtons) => [
    Container.sketch({
      dom: {
        tag: "div",
        classes: ["tox-dialog__footer-start"]
      },
      components: startButtons
    }),
    Container.sketch({
      dom: {
        tag: "div",
        classes: ["tox-dialog__footer-end"]
      },
      components: endButtons
    })
  ];
  const renderDialog$1 = (spec) => {
    const dialogClass = "tox-dialog";
    const blockerClass = dialogClass + "-wrap";
    const blockerBackdropClass = blockerClass + "__backdrop";
    const scrollLockClass = dialogClass + "__disable-scroll";
    return ModalDialog.sketch({
      lazySink: spec.lazySink,
      onEscape: (comp) => {
        spec.onEscape(comp);
        return Optional.some(true);
      },
      useTabstopAt: (elem) => !isPseudoStop(elem),
      dom: {
        tag: "div",
        classes: [dialogClass].concat(spec.extraClasses),
        styles: {
          position: "relative",
          ...spec.extraStyles
        }
      },
      components: [
        spec.header,
        spec.body,
        ...spec.footer.toArray()
      ],
      parts: {
        blocker: {
          dom: fromHtml(`<div class="${blockerClass}"></div>`),
          components: [{
            dom: {
              tag: "div",
              classes: isTouch ? [
                blockerBackdropClass,
                blockerBackdropClass + "--opaque"
              ] : [blockerBackdropClass]
            }
          }]
        }
      },
      dragBlockClass: blockerClass,
      modalBehaviours: derive$1([
        Focusing.config({}),
        config("dialog-events", spec.dialogEvents.concat([runOnSource(focusin(), (comp, _se) => {
          Keying.focusIn(comp);
        })])),
        config("scroll-lock", [
          runOnAttached(() => {
            add$2(body(), scrollLockClass);
          }),
          runOnDetached(() => {
            remove$2(body(), scrollLockClass);
          })
        ]),
        ...spec.extraBehaviours
      ]),
      eventOrder: {
        [execute$5()]: ["dialog-events"],
        [attachedToDom()]: [
          "scroll-lock",
          "dialog-events",
          "alloy.base.behaviour"
        ],
        [detachedFromDom()]: [
          "alloy.base.behaviour",
          "dialog-events",
          "scroll-lock"
        ],
        ...spec.eventOrder
      }
    });
  };
  const renderClose = (providersBackstage) => Button.sketch({
    dom: {
      tag: "button",
      classes: [
        "tox-button",
        "tox-button--icon",
        "tox-button--naked"
      ],
      attributes: {
        "type": "button",
        "aria-label": providersBackstage.translate("Close"),
        "title": providersBackstage.translate("Close")
      }
    },
    components: [render$3("close", {
      tag: "div",
      classes: ["tox-icon"]
    }, providersBackstage.icons)],
    action: (comp) => {
      emit(comp, formCancelEvent);
    }
  });
  const renderTitle = (spec, dialogId, titleId, providersBackstage) => {
    const renderComponents2 = (data) => [text$1(providersBackstage.translate(data.title))];
    return {
      dom: {
        tag: "div",
        classes: ["tox-dialog__title"],
        attributes: { ...titleId.map((x) => ({ id: x })).getOr({}) }
      },
      components: [],
      behaviours: derive$1([Reflecting.config({
        channel: `${titleChannel}-${dialogId}`,
        initialData: spec,
        renderComponents: renderComponents2
      })])
    };
  };
  const renderDragHandle = () => ({ dom: fromHtml('<div class="tox-dialog__draghandle"></div>') });
  const renderInlineHeader = (spec, dialogId, titleId, providersBackstage) => Container.sketch({
    dom: fromHtml('<div class="tox-dialog__header"></div>'),
    components: [
      renderTitle(spec, dialogId, Optional.some(titleId), providersBackstage),
      renderDragHandle(),
      renderClose(providersBackstage)
    ],
    containerBehaviours: derive$1([Dragging.config({
      mode: "mouse",
      blockerClass: "blocker",
      getTarget: (handle2) => {
        return closest$1(handle2, '[role="dialog"]').getOrDie();
      },
      snaps: {
        getSnapPoints: () => [],
        leftAttr: "data-drag-left",
        topAttr: "data-drag-top"
      }
    })])
  });
  const renderModalHeader = (spec, dialogId, providersBackstage) => {
    const pTitle = ModalDialog.parts.title(renderTitle(spec, dialogId, Optional.none(), providersBackstage));
    const pHandle = ModalDialog.parts.draghandle(renderDragHandle());
    const pClose2 = ModalDialog.parts.close(renderClose(providersBackstage));
    const components2 = [pTitle].concat(spec.draggable ? [pHandle] : []).concat([pClose2]);
    return Container.sketch({
      dom: fromHtml('<div class="tox-dialog__header"></div>'),
      components: components2
    });
  };
  const getHeader = (title2, dialogId, backstage) => renderModalHeader({
    title: backstage.shared.providers.translate(title2),
    draggable: backstage.dialog.isDraggableModal()
  }, dialogId, backstage.shared.providers);
  const getBusySpec = (message, bs, providers) => ({
    dom: {
      tag: "div",
      classes: ["tox-dialog__busy-spinner"],
      attributes: { "aria-label": providers.translate(message) },
      styles: {
        left: "0px",
        right: "0px",
        bottom: "0px",
        top: "0px",
        position: "absolute"
      }
    },
    behaviours: bs,
    components: [{ dom: fromHtml('<div class="tox-spinner"><div></div><div></div><div></div></div>') }]
  });
  const getEventExtras = (lazyDialog, providers, extra) => ({
    onClose: () => extra.closeWindow(),
    onBlock: (blockEvent) => {
      ModalDialog.setBusy(lazyDialog(), (_comp, bs) => getBusySpec(blockEvent.message, bs, providers));
    },
    onUnblock: () => {
      ModalDialog.setIdle(lazyDialog());
    }
  });
  const renderModalDialog = (spec, initialData, dialogEvents, backstage) => {
    const updateState = (_comp, incoming) => Optional.some(incoming);
    return build$1(renderDialog$1({
      ...spec,
      lazySink: backstage.shared.getSink,
      extraBehaviours: [
        Reflecting.config({
          channel: `${dialogChannel}-${spec.id}`,
          updateState,
          initialData
        }),
        RepresentingConfigs.memory({}),
        ...spec.extraBehaviours
      ],
      onEscape: (comp) => {
        emit(comp, formCancelEvent);
      },
      dialogEvents,
      eventOrder: {
        [receive()]: [
          Reflecting.name(),
          Receiving.name()
        ],
        [attachedToDom()]: [
          "scroll-lock",
          Reflecting.name(),
          "messages",
          "dialog-events",
          "alloy.base.behaviour"
        ],
        [detachedFromDom()]: [
          "alloy.base.behaviour",
          "dialog-events",
          "messages",
          Reflecting.name(),
          "scroll-lock"
        ]
      }
    }));
  };
  const mapMenuButtons = (buttons) => {
    const mapItems = (button2) => {
      const items = map$2(button2.items, (item2) => {
        const cell = Cell(false);
        return {
          ...item2,
          storage: cell
        };
      });
      return {
        ...button2,
        items
      };
    };
    return map$2(buttons, (button2) => {
      return button2.type === "menu" ? mapItems(button2) : button2;
    });
  };
  const extractCellsToObject = (buttons) => foldl(buttons, (acc, button2) => {
    if (button2.type === "menu") {
      const menuButton = button2;
      return foldl(menuButton.items, (innerAcc, item2) => {
        innerAcc[item2.name] = item2.storage;
        return innerAcc;
      }, acc);
    }
    return acc;
  }, {});
  const initCommonEvents = (fireApiEvent, extras) => [
    runWithTarget(focusin(), onFocus),
    fireApiEvent(formCloseEvent, (_api, spec) => {
      extras.onClose();
      spec.onClose();
    }),
    fireApiEvent(formCancelEvent, (api2, spec, _event, self3) => {
      spec.onCancel(api2);
      emit(self3, formCloseEvent);
    }),
    run$1(formUnblockEvent, (_c, _se) => extras.onUnblock()),
    run$1(formBlockEvent, (_c, se) => extras.onBlock(se.event))
  ];
  const initUrlDialog = (getInstanceApi, extras) => {
    const fireApiEvent = (eventName, f) => run$1(eventName, (c, se) => {
      withSpec(c, (spec, _c) => {
        f(getInstanceApi(), spec, se.event, c);
      });
    });
    const withSpec = (c, f) => {
      Reflecting.getState(c).get().each((currentDialog) => {
        f(currentDialog, c);
      });
    };
    return [
      ...initCommonEvents(fireApiEvent, extras),
      fireApiEvent(formActionEvent, (api2, spec, event2) => {
        spec.onAction(api2, { name: event2.name });
      })
    ];
  };
  const initDialog = (getInstanceApi, extras, getSink2) => {
    const fireApiEvent = (eventName, f) => run$1(eventName, (c, se) => {
      withSpec(c, (spec, _c) => {
        f(getInstanceApi(), spec, se.event, c);
      });
    });
    const withSpec = (c, f) => {
      Reflecting.getState(c).get().each((currentDialogInit) => {
        f(currentDialogInit.internalDialog, c);
      });
    };
    return [
      ...initCommonEvents(fireApiEvent, extras),
      fireApiEvent(formSubmitEvent, (api2, spec) => spec.onSubmit(api2)),
      fireApiEvent(formChangeEvent, (api2, spec, event2) => {
        spec.onChange(api2, { name: event2.name });
      }),
      fireApiEvent(formActionEvent, (api2, spec, event2, component) => {
        const focusIn2 = () => Keying.focusIn(component);
        const isDisabled2 = (focused) => has$1(focused, "disabled") || getOpt(focused, "aria-disabled").exists((val) => val === "true");
        const rootNode = getRootNode(component.element);
        const current = active$1(rootNode);
        spec.onAction(api2, {
          name: event2.name,
          value: event2.value
        });
        active$1(rootNode).fold(focusIn2, (focused) => {
          if (isDisabled2(focused)) {
            focusIn2();
          } else if (current.exists((cur) => contains(focused, cur) && isDisabled2(cur))) {
            focusIn2();
          } else {
            getSink2().toOptional().filter((sink) => !contains(sink.element, focused)).each(focusIn2);
          }
        });
      }),
      fireApiEvent(formTabChangeEvent, (api2, spec, event2) => {
        spec.onTabChange(api2, {
          newTabName: event2.name,
          oldTabName: event2.oldName
        });
      }),
      runOnDetached((component) => {
        const api2 = getInstanceApi();
        Representing.setValue(component, api2.getData());
      })
    ];
  };
  const SilverDialogEvents = {
    initUrlDialog,
    initDialog
  };
  const makeButton = (button2, backstage) => renderFooterButton(button2, button2.type, backstage);
  const lookup = (compInSystem, footerButtons, buttonName) => find$5(footerButtons, (button2) => button2.name === buttonName).bind((memButton) => memButton.memento.getOpt(compInSystem));
  const renderComponents = (_data, state) => {
    const footerButtons = state.map((s) => s.footerButtons).getOr([]);
    const buttonGroups = partition$3(footerButtons, (button2) => button2.align === "start");
    const makeGroup = (edge2, buttons) => Container.sketch({
      dom: {
        tag: "div",
        classes: [`tox-dialog__footer-${edge2}`]
      },
      components: map$2(buttons, (button2) => button2.memento.asSpec())
    });
    const startButtons = makeGroup("start", buttonGroups.pass);
    const endButtons = makeGroup("end", buttonGroups.fail);
    return [
      startButtons,
      endButtons
    ];
  };
  const renderFooter = (initSpec, dialogId, backstage) => {
    const updateState = (comp, data) => {
      const footerButtons = map$2(data.buttons, (button2) => {
        const memButton = record(makeButton(button2, backstage));
        return {
          name: button2.name,
          align: button2.align,
          memento: memButton
        };
      });
      const lookupByName = (buttonName) => lookup(comp, footerButtons, buttonName);
      return Optional.some({
        lookupByName,
        footerButtons
      });
    };
    return {
      dom: fromHtml('<div class="tox-dialog__footer"></div>'),
      components: [],
      behaviours: derive$1([Reflecting.config({
        channel: `${footerChannel}-${dialogId}`,
        initialData: initSpec,
        updateState,
        renderComponents
      })])
    };
  };
  const renderInlineFooter = (initSpec, dialogId, backstage) => renderFooter(initSpec, dialogId, backstage);
  const renderModalFooter = (initSpec, dialogId, backstage) => ModalDialog.parts.footer(renderFooter(initSpec, dialogId, backstage));
  const getCompByName = (access, name2) => {
    const root = access.getRoot();
    if (root.getSystem().isConnected()) {
      const form = Composing.getCurrent(access.getFormWrapper()).getOr(access.getFormWrapper());
      return Form.getField(form, name2).orThunk(() => {
        const footer = access.getFooter();
        const footerState = Reflecting.getState(footer).get();
        return footerState.bind((f) => f.lookupByName(name2));
      });
    } else {
      return Optional.none();
    }
  };
  const validateData$1 = (access, data) => {
    const root = access.getRoot();
    return Reflecting.getState(root).get().map((dialogState) => getOrDie(asRaw("data", dialogState.dataValidator, data))).getOr(data);
  };
  const getDialogApi = (access, doRedial, menuItemStates) => {
    const withRoot = (f) => {
      const root = access.getRoot();
      if (root.getSystem().isConnected()) {
        f(root);
      }
    };
    const getData2 = () => {
      const root = access.getRoot();
      const valueComp = root.getSystem().isConnected() ? access.getFormWrapper() : root;
      const representedValues = Representing.getValue(valueComp);
      const menuItemCurrentState = map$1(menuItemStates, (cell) => cell.get());
      return {
        ...representedValues,
        ...menuItemCurrentState
      };
    };
    const setData = (newData) => {
      withRoot((_) => {
        const prevData = instanceApi.getData();
        const mergedData = deepMerge(prevData, newData);
        const newInternalData = validateData$1(access, mergedData);
        const form = access.getFormWrapper();
        Representing.setValue(form, newInternalData);
        each(menuItemStates, (v, k) => {
          if (has$2(mergedData, k)) {
            v.set(mergedData[k]);
          }
        });
      });
    };
    const setEnabled = (name2, state) => {
      getCompByName(access, name2).each(state ? Disabling.enable : Disabling.disable);
    };
    const focus2 = (name2) => {
      getCompByName(access, name2).each(Focusing.focus);
    };
    const block2 = (message) => {
      if (!isString(message)) {
        throw new Error("The dialogInstanceAPI.block function should be passed a blocking message of type string as an argument");
      }
      withRoot((root) => {
        emitWith(root, formBlockEvent, { message });
      });
    };
    const unblock2 = () => {
      withRoot((root) => {
        emit(root, formUnblockEvent);
      });
    };
    const showTab2 = (name2) => {
      withRoot((_) => {
        const body2 = access.getBody();
        const bodyState = Reflecting.getState(body2);
        if (bodyState.get().exists((b2) => b2.isTabPanel())) {
          Composing.getCurrent(body2).each((tabSection) => {
            TabSection.showTab(tabSection, name2);
          });
        }
      });
    };
    const redial = (d) => {
      withRoot((root) => {
        const id = access.getId();
        const dialogInit = doRedial(d);
        root.getSystem().broadcastOn([`${dialogChannel}-${id}`], dialogInit);
        root.getSystem().broadcastOn([`${titleChannel}-${id}`], dialogInit.internalDialog);
        root.getSystem().broadcastOn([`${bodyChannel}-${id}`], dialogInit.internalDialog);
        root.getSystem().broadcastOn([`${footerChannel}-${id}`], dialogInit.internalDialog);
        instanceApi.setData(dialogInit.initialData);
      });
    };
    const close2 = () => {
      withRoot((root) => {
        emit(root, formCloseEvent);
      });
    };
    const instanceApi = {
      getData: getData2,
      setData,
      setEnabled,
      focus: focus2,
      block: block2,
      unblock: unblock2,
      showTab: showTab2,
      redial,
      close: close2
    };
    return instanceApi;
  };
  const getDialogSizeClasses = (size) => {
    switch (size) {
      case "large":
        return ["tox-dialog--width-lg"];
      case "medium":
        return ["tox-dialog--width-md"];
      default:
        return [];
    }
  };
  const renderDialog = (dialogInit, extra, backstage) => {
    const dialogId = generate$6("dialog");
    const internalDialog = dialogInit.internalDialog;
    const header = getHeader(internalDialog.title, dialogId, backstage);
    const body2 = renderModalBody({
      body: internalDialog.body,
      initialData: internalDialog.initialData
    }, dialogId, backstage);
    const storedMenuButtons = mapMenuButtons(internalDialog.buttons);
    const objOfCells = extractCellsToObject(storedMenuButtons);
    const footer = renderModalFooter({ buttons: storedMenuButtons }, dialogId, backstage);
    const dialogEvents = SilverDialogEvents.initDialog(() => instanceApi, getEventExtras(() => dialog, backstage.shared.providers, extra), backstage.shared.getSink);
    const dialogSize = getDialogSizeClasses(internalDialog.size);
    const spec = {
      id: dialogId,
      header,
      body: body2,
      footer: Optional.some(footer),
      extraClasses: dialogSize,
      extraBehaviours: [],
      extraStyles: {}
    };
    const dialog = renderModalDialog(spec, dialogInit, dialogEvents, backstage);
    const modalAccess = (() => {
      const getForm = () => {
        const outerForm = ModalDialog.getBody(dialog);
        return Composing.getCurrent(outerForm).getOr(outerForm);
      };
      return {
        getId: constant$1(dialogId),
        getRoot: constant$1(dialog),
        getBody: () => ModalDialog.getBody(dialog),
        getFooter: () => ModalDialog.getFooter(dialog),
        getFormWrapper: getForm
      };
    })();
    const instanceApi = getDialogApi(modalAccess, extra.redial, objOfCells);
    return {
      dialog,
      instanceApi
    };
  };
  const renderInlineDialog = (dialogInit, extra, backstage, ariaAttrs) => {
    const dialogId = generate$6("dialog");
    const dialogLabelId = generate$6("dialog-label");
    const dialogContentId = generate$6("dialog-content");
    const internalDialog = dialogInit.internalDialog;
    const updateState = (_comp, incoming) => Optional.some(incoming);
    const memHeader = record(renderInlineHeader({
      title: internalDialog.title,
      draggable: true
    }, dialogId, dialogLabelId, backstage.shared.providers));
    const memBody = record(renderInlineBody({
      body: internalDialog.body,
      initialData: internalDialog.initialData
    }, dialogId, dialogContentId, backstage, ariaAttrs));
    const storagedMenuButtons = mapMenuButtons(internalDialog.buttons);
    const objOfCells = extractCellsToObject(storagedMenuButtons);
    const memFooter = record(renderInlineFooter({ buttons: storagedMenuButtons }, dialogId, backstage));
    const dialogEvents = SilverDialogEvents.initDialog(() => instanceApi, {
      onBlock: (event2) => {
        Blocking.block(dialog, (_comp, bs) => getBusySpec(event2.message, bs, backstage.shared.providers));
      },
      onUnblock: () => {
        Blocking.unblock(dialog);
      },
      onClose: () => extra.closeWindow()
    }, backstage.shared.getSink);
    const dialog = build$1({
      dom: {
        tag: "div",
        classes: [
          "tox-dialog",
          "tox-dialog-inline"
        ],
        attributes: {
          role: "dialog",
          ["aria-labelledby"]: dialogLabelId,
          ["aria-describedby"]: dialogContentId
        }
      },
      eventOrder: {
        [receive()]: [
          Reflecting.name(),
          Receiving.name()
        ],
        [execute$5()]: ["execute-on-form"],
        [attachedToDom()]: [
          "reflecting",
          "execute-on-form"
        ]
      },
      behaviours: derive$1([
        Keying.config({
          mode: "cyclic",
          onEscape: (c) => {
            emit(c, formCloseEvent);
            return Optional.some(true);
          },
          useTabstopAt: (elem) => !isPseudoStop(elem) && (name$3(elem) !== "button" || get$f(elem, "disabled") !== "disabled")
        }),
        Reflecting.config({
          channel: `${dialogChannel}-${dialogId}`,
          updateState,
          initialData: dialogInit
        }),
        Focusing.config({}),
        config("execute-on-form", dialogEvents.concat([runOnSource(focusin(), (comp, _se) => {
          Keying.focusIn(comp);
        })])),
        Blocking.config({ getRoot: () => Optional.some(dialog) }),
        Replacing.config({}),
        RepresentingConfigs.memory({})
      ]),
      components: [
        memHeader.asSpec(),
        memBody.asSpec(),
        memFooter.asSpec()
      ]
    });
    const instanceApi = getDialogApi({
      getId: constant$1(dialogId),
      getRoot: constant$1(dialog),
      getFooter: () => memFooter.get(dialog),
      getBody: () => memBody.get(dialog),
      getFormWrapper: () => {
        const body2 = memBody.get(dialog);
        return Composing.getCurrent(body2).getOr(body2);
      }
    }, extra.redial, objOfCells);
    return {
      dialog,
      instanceApi
    };
  };
  var global2 = tinymce.util.Tools.resolve("tinymce.util.URI");
  const getUrlDialogApi = (root) => {
    const withRoot = (f) => {
      if (root.getSystem().isConnected()) {
        f(root);
      }
    };
    const block2 = (message) => {
      if (!isString(message)) {
        throw new Error("The urlDialogInstanceAPI.block function should be passed a blocking message of type string as an argument");
      }
      withRoot((root2) => {
        emitWith(root2, formBlockEvent, { message });
      });
    };
    const unblock2 = () => {
      withRoot((root2) => {
        emit(root2, formUnblockEvent);
      });
    };
    const close2 = () => {
      withRoot((root2) => {
        emit(root2, formCloseEvent);
      });
    };
    const sendMessage = (data) => {
      withRoot((root2) => {
        root2.getSystem().broadcastOn([bodySendMessageChannel], data);
      });
    };
    return {
      block: block2,
      unblock: unblock2,
      close: close2,
      sendMessage
    };
  };
  const SUPPORTED_MESSAGE_ACTIONS = [
    "insertContent",
    "setContent",
    "execCommand",
    "close",
    "block",
    "unblock"
  ];
  const isSupportedMessage = (data) => isObject(data) && SUPPORTED_MESSAGE_ACTIONS.indexOf(data.mceAction) !== -1;
  const isCustomMessage = (data) => !isSupportedMessage(data) && isObject(data) && has$2(data, "mceAction");
  const handleMessage = (editor, api2, data) => {
    switch (data.mceAction) {
      case "insertContent":
        editor.insertContent(data.content);
        break;
      case "setContent":
        editor.setContent(data.content);
        break;
      case "execCommand":
        const ui = isBoolean(data.ui) ? data.ui : false;
        editor.execCommand(data.cmd, ui, data.value);
        break;
      case "close":
        api2.close();
        break;
      case "block":
        api2.block(data.message);
        break;
      case "unblock":
        api2.unblock();
        break;
    }
  };
  const renderUrlDialog = (internalDialog, extra, editor, backstage) => {
    const dialogId = generate$6("dialog");
    const header = getHeader(internalDialog.title, dialogId, backstage);
    const body2 = renderIframeBody(internalDialog);
    const footer = internalDialog.buttons.bind((buttons) => {
      if (buttons.length === 0) {
        return Optional.none();
      } else {
        return Optional.some(renderModalFooter({ buttons }, dialogId, backstage));
      }
    });
    const dialogEvents = SilverDialogEvents.initUrlDialog(() => instanceApi, getEventExtras(() => dialog, backstage.shared.providers, extra));
    const styles = {
      ...internalDialog.height.fold(() => ({}), (height2) => ({
        "height": height2 + "px",
        "max-height": height2 + "px"
      })),
      ...internalDialog.width.fold(() => ({}), (width2) => ({
        "width": width2 + "px",
        "max-width": width2 + "px"
      }))
    };
    const classes2 = internalDialog.width.isNone() && internalDialog.height.isNone() ? ["tox-dialog--width-lg"] : [];
    const iframeUri = new global2(internalDialog.url, { base_uri: new global2(window.location.href) });
    const iframeDomain = `${iframeUri.protocol}://${iframeUri.host}${iframeUri.port ? ":" + iframeUri.port : ""}`;
    const messageHandlerUnbinder = unbindable();
    const extraBehaviours = [
      config("messages", [
        runOnAttached(() => {
          const unbind2 = bind(SugarElement.fromDom(window), "message", (e) => {
            if (iframeUri.isSameOrigin(new global2(e.raw.origin))) {
              const data = e.raw.data;
              if (isSupportedMessage(data)) {
                handleMessage(editor, instanceApi, data);
              } else if (isCustomMessage(data)) {
                internalDialog.onMessage(instanceApi, data);
              }
            }
          });
          messageHandlerUnbinder.set(unbind2);
        }),
        runOnDetached(messageHandlerUnbinder.clear)
      ]),
      Receiving.config({
        channels: {
          [bodySendMessageChannel]: {
            onReceive: (comp, data) => {
              descendant(comp.element, "iframe").each((iframeEle) => {
                const iframeWin = iframeEle.dom.contentWindow;
                if (isNonNullable(iframeWin)) {
                  iframeWin.postMessage(data, iframeDomain);
                }
              });
            }
          }
        }
      })
    ];
    const spec = {
      id: dialogId,
      header,
      body: body2,
      footer,
      extraClasses: classes2,
      extraBehaviours,
      extraStyles: styles
    };
    const dialog = renderModalDialog(spec, internalDialog, dialogEvents, backstage);
    const instanceApi = getUrlDialogApi(dialog);
    return {
      dialog,
      instanceApi
    };
  };
  const setup$2 = (backstage) => {
    const sharedBackstage = backstage.shared;
    const open2 = (message, callback) => {
      const closeDialog = () => {
        ModalDialog.hide(alertDialog);
        callback();
      };
      const memFooterClose = record(renderFooterButton({
        name: "close-alert",
        text: "OK",
        primary: true,
        buttonType: Optional.some("primary"),
        align: "end",
        enabled: true,
        icon: Optional.none()
      }, "cancel", backstage));
      const titleSpec = pUntitled();
      const closeSpec = pClose(closeDialog, sharedBackstage.providers);
      const alertDialog = build$1(renderDialog$1({
        lazySink: () => sharedBackstage.getSink(),
        header: hiddenHeader(titleSpec, closeSpec),
        body: pBodyMessage(message, sharedBackstage.providers),
        footer: Optional.some(pFooter(pFooterGroup([], [memFooterClose.asSpec()]))),
        onEscape: closeDialog,
        extraClasses: ["tox-alert-dialog"],
        extraBehaviours: [],
        extraStyles: {},
        dialogEvents: [run$1(formCancelEvent, closeDialog)],
        eventOrder: {}
      }));
      ModalDialog.show(alertDialog);
      const footerCloseButton = memFooterClose.get(alertDialog);
      Focusing.focus(footerCloseButton);
    };
    return { open: open2 };
  };
  const setup$1 = (backstage) => {
    const sharedBackstage = backstage.shared;
    const open2 = (message, callback) => {
      const closeDialog = (state) => {
        ModalDialog.hide(confirmDialog);
        callback(state);
      };
      const memFooterYes = record(renderFooterButton({
        name: "yes",
        text: "Yes",
        primary: true,
        buttonType: Optional.some("primary"),
        align: "end",
        enabled: true,
        icon: Optional.none()
      }, "submit", backstage));
      const footerNo = renderFooterButton({
        name: "no",
        text: "No",
        primary: false,
        buttonType: Optional.some("secondary"),
        align: "end",
        enabled: true,
        icon: Optional.none()
      }, "cancel", backstage);
      const titleSpec = pUntitled();
      const closeSpec = pClose(() => closeDialog(false), sharedBackstage.providers);
      const confirmDialog = build$1(renderDialog$1({
        lazySink: () => sharedBackstage.getSink(),
        header: hiddenHeader(titleSpec, closeSpec),
        body: pBodyMessage(message, sharedBackstage.providers),
        footer: Optional.some(pFooter(pFooterGroup([], [
          footerNo,
          memFooterYes.asSpec()
        ]))),
        onEscape: () => closeDialog(false),
        extraClasses: ["tox-confirm-dialog"],
        extraBehaviours: [],
        extraStyles: {},
        dialogEvents: [
          run$1(formCancelEvent, () => closeDialog(false)),
          run$1(formSubmitEvent, () => closeDialog(true))
        ],
        eventOrder: {}
      }));
      ModalDialog.show(confirmDialog);
      const footerYesButton = memFooterYes.get(confirmDialog);
      Focusing.focus(footerYesButton);
    };
    return { open: open2 };
  };
  const validateData = (data, validator) => getOrDie(asRaw("data", validator, data));
  const isAlertOrConfirmDialog = (target) => closest(target, ".tox-alert-dialog") || closest(target, ".tox-confirm-dialog");
  const inlineAdditionalBehaviours = (editor, isStickyToolbar2, isToolbarLocationTop) => {
    if (isStickyToolbar2 && isToolbarLocationTop) {
      return [];
    } else {
      return [Docking.config({
        contextual: {
          lazyContext: () => Optional.some(box$1(SugarElement.fromDom(editor.getContentAreaContainer()))),
          fadeInClass: "tox-dialog-dock-fadein",
          fadeOutClass: "tox-dialog-dock-fadeout",
          transitionClass: "tox-dialog-dock-transition"
        },
        modes: ["top"]
      })];
    }
  };
  const setup = (extras) => {
    const backstage = extras.backstage;
    const editor = extras.editor;
    const isStickyToolbar$1 = isStickyToolbar(editor);
    const alertDialog = setup$2(backstage);
    const confirmDialog = setup$1(backstage);
    const open2 = (config2, params, closeWindow) => {
      if (params !== void 0 && params.inline === "toolbar") {
        return openInlineDialog(config2, backstage.shared.anchors.inlineDialog(), closeWindow, params.ariaAttrs);
      } else if (params !== void 0 && params.inline === "cursor") {
        return openInlineDialog(config2, backstage.shared.anchors.cursor(), closeWindow, params.ariaAttrs);
      } else {
        return openModalDialog(config2, closeWindow);
      }
    };
    const openUrl = (config2, closeWindow) => openModalUrlDialog(config2, closeWindow);
    const openModalUrlDialog = (config2, closeWindow) => {
      const factory2 = (contents2) => {
        const dialog = renderUrlDialog(contents2, {
          closeWindow: () => {
            ModalDialog.hide(dialog.dialog);
            closeWindow(dialog.instanceApi);
          }
        }, editor, backstage);
        ModalDialog.show(dialog.dialog);
        return dialog.instanceApi;
      };
      return DialogManager.openUrl(factory2, config2);
    };
    const openModalDialog = (config2, closeWindow) => {
      const factory2 = (contents2, internalInitialData, dataValidator) => {
        const initialData = internalInitialData;
        const dialogInit = {
          dataValidator,
          initialData,
          internalDialog: contents2
        };
        const dialog = renderDialog(dialogInit, {
          redial: DialogManager.redial,
          closeWindow: () => {
            ModalDialog.hide(dialog.dialog);
            closeWindow(dialog.instanceApi);
          }
        }, backstage);
        ModalDialog.show(dialog.dialog);
        dialog.instanceApi.setData(initialData);
        return dialog.instanceApi;
      };
      return DialogManager.open(factory2, config2);
    };
    const openInlineDialog = (config$1, anchor2, closeWindow, ariaAttrs = false) => {
      const factory2 = (contents2, internalInitialData, dataValidator) => {
        const initialData = validateData(internalInitialData, dataValidator);
        const inlineDialog = value$2();
        const isToolbarLocationTop = backstage.shared.header.isPositionedAtTop();
        const dialogInit = {
          dataValidator,
          initialData,
          internalDialog: contents2
        };
        const refreshDocking = () => inlineDialog.on((dialog) => {
          InlineView.reposition(dialog);
          Docking.refresh(dialog);
        });
        const dialogUi = renderInlineDialog(dialogInit, {
          redial: DialogManager.redial,
          closeWindow: () => {
            inlineDialog.on(InlineView.hide);
            editor.off("ResizeEditor", refreshDocking);
            inlineDialog.clear();
            closeWindow(dialogUi.instanceApi);
          }
        }, backstage, ariaAttrs);
        const inlineDialogComp = build$1(InlineView.sketch({
          lazySink: backstage.shared.getSink,
          dom: {
            tag: "div",
            classes: []
          },
          fireDismissalEventInstead: {},
          ...isToolbarLocationTop ? {} : { fireRepositionEventInstead: {} },
          inlineBehaviours: derive$1([
            config("window-manager-inline-events", [run$1(dismissRequested(), (_comp, _se) => {
              emit(dialogUi.dialog, formCancelEvent);
            })]),
            ...inlineAdditionalBehaviours(editor, isStickyToolbar$1, isToolbarLocationTop)
          ]),
          isExtraPart: (_comp, target) => isAlertOrConfirmDialog(target)
        }));
        inlineDialog.set(inlineDialogComp);
        InlineView.showWithin(inlineDialogComp, premade(dialogUi.dialog), { anchor: anchor2 }, Optional.some(body()));
        if (!isStickyToolbar$1 || !isToolbarLocationTop) {
          Docking.refresh(inlineDialogComp);
          editor.on("ResizeEditor", refreshDocking);
        }
        dialogUi.instanceApi.setData(initialData);
        Keying.focusIn(dialogUi.dialog);
        return dialogUi.instanceApi;
      };
      return DialogManager.open(factory2, config$1);
    };
    const confirm = (message, callback) => {
      confirmDialog.open(message, callback);
    };
    const alert2 = (message, callback) => {
      alertDialog.open(message, callback);
    };
    const close2 = (instanceApi) => {
      instanceApi.close();
    };
    return {
      open: open2,
      openUrl,
      alert: alert2,
      close: close2,
      confirm
    };
  };
  const registerOptions = (editor) => {
    register$e(editor);
    register$d(editor);
    register(editor);
  };
  var Theme = () => {
    global$a.add("silver", (editor) => {
      registerOptions(editor);
      const { getUiMothership, backstage, renderUI } = setup$3(editor);
      Autocompleter.register(editor, backstage.shared);
      const windowMgr = setup({
        editor,
        backstage
      });
      return {
        renderUI,
        getWindowManagerImpl: constant$1(windowMgr),
        getNotificationManagerImpl: () => NotificationManagerImpl(editor, { backstage }, getUiMothership())
      };
    });
  };
  Theme();
})();
(function() {
  "use strict";
  var global$2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const eq = (t) => (a) => t === a;
  const isNull = eq(null);
  const identity = (x) => {
    return x;
  };
  const zeroWidth = "\uFEFF";
  const removeZwsp$1 = (s) => s.replace(/\uFEFF/g, "");
  const map = (xs, f) => {
    const len = xs.length;
    const r = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r[i2] = f(x, i2);
    }
    return r;
  };
  const punctuationStr = "[!-#%-*,-\\/:;?@\\[-\\]_{}\xA1\xAB\xB7\xBB\xBF;\xB7\u055A-\u055F\u0589\u058A\u05BE\u05C0\u05C3\u05C6\u05F3\u05F4\u0609\u060A\u060C\u060D\u061B\u061E\u061F\u066A-\u066D\u06D4\u0700-\u070D\u07F7-\u07F9\u0830-\u083E\u085E\u0964\u0965\u0970\u0DF4\u0E4F\u0E5A\u0E5B\u0F04-\u0F12\u0F3A-\u0F3D\u0F85\u0FD0-\u0FD4\u0FD9\u0FDA\u104A-\u104F\u10FB\u1361-\u1368\u1400\u166D\u166E\u169B\u169C\u16EB-\u16ED\u1735\u1736\u17D4-\u17D6\u17D8-\u17DA\u1800-\u180A\u1944\u1945\u1A1E\u1A1F\u1AA0-\u1AA6\u1AA8-\u1AAD\u1B5A-\u1B60\u1BFC-\u1BFF\u1C3B-\u1C3F\u1C7E\u1C7F\u1CD3\u2010-\u2027\u2030-\u2043\u2045-\u2051\u2053-\u205E\u207D\u207E\u208D\u208E\u3008\u3009\u2768-\u2775\u27C5\u27C6\u27E6-\u27EF\u2983-\u2998\u29D8-\u29DB\u29FC\u29FD\u2CF9-\u2CFC\u2CFE\u2CFF\u2D70\u2E00-\u2E2E\u2E30\u2E31\u3001-\u3003\u3008-\u3011\u3014-\u301F\u3030\u303D\u30A0\u30FB\uA4FE\uA4FF\uA60D-\uA60F\uA673\uA67E\uA6F2-\uA6F7\uA874-\uA877\uA8CE\uA8CF\uA8F8-\uA8FA\uA92E\uA92F\uA95F\uA9C1-\uA9CD\uA9DE\uA9DF\uAA5C-\uAA5F\uAADE\uAADF\uABEB\uFD3E\uFD3F\uFE10-\uFE19\uFE30-\uFE52\uFE54-\uFE61\uFE63\uFE68\uFE6A\uFE6B\uFF01-\uFF03\uFF05-\uFF0A\uFF0C-\uFF0F\uFF1A\uFF1B\uFF1F\uFF20\uFF3B-\uFF3D\uFF3F\uFF5B\uFF5D\uFF5F-\uFF65]";
  const regExps = {
    aletter: "[A-Za-z\xAA\xB5\xBA\xC0-\xD6\xD8-\xF6\xF8-\u02C1\u02C6-\u02D1\u02E0-\u02E4\u02EC\u02EE\u0370-\u0374\u0376\u0377\u037A-\u037D\u0386\u0388-\u038A\u038C\u038E-\u03A1\u03A3-\u03F5\u03F7-\u0481\u048A-\u0527\u0531-\u0556\u0559\u0561-\u0587\u05D0-\u05EA\u05F0-\u05F3\u0620-\u064A\u066E\u066F\u0671-\u06D3\u06D5\u06E5\u06E6\u06EE\u06EF\u06FA-\u06FC\u06FF\u0710\u0712-\u072F\u074D-\u07A5\u07B1\u07CA-\u07EA\u07F4\u07F5\u07FA\u0800-\u0815\u081A\u0824\u0828\u0840-\u0858\u0904-\u0939\u093D\u0950\u0958-\u0961\u0971-\u0977\u0979-\u097F\u0985-\u098C\u098F\u0990\u0993-\u09A8\u09AA-\u09B0\u09B2\u09B6-\u09B9\u09BD\u09CE\u09DC\u09DD\u09DF-\u09E1\u09F0\u09F1\u0A05-\u0A0A\u0A0F\u0A10\u0A13-\u0A28\u0A2A-\u0A30\u0A32\u0A33\u0A35\u0A36\u0A38\u0A39\u0A59-\u0A5C\u0A5E\u0A72-\u0A74\u0A85-\u0A8D\u0A8F-\u0A91\u0A93-\u0AA8\u0AAA-\u0AB0\u0AB2\u0AB3\u0AB5-\u0AB9\u0ABD\u0AD0\u0AE0\u0AE1\u0B05-\u0B0C\u0B0F\u0B10\u0B13-\u0B28\u0B2A-\u0B30\u0B32\u0B33\u0B35-\u0B39\u0B3D\u0B5C\u0B5D\u0B5F-\u0B61\u0B71\u0B83\u0B85-\u0B8A\u0B8E-\u0B90\u0B92-\u0B95\u0B99\u0B9A\u0B9C\u0B9E\u0B9F\u0BA3\u0BA4\u0BA8-\u0BAA\u0BAE-\u0BB9\u0BD0\u0C05-\u0C0C\u0C0E-\u0C10\u0C12-\u0C28\u0C2A-\u0C33\u0C35-\u0C39\u0C3D\u0C58\u0C59\u0C60\u0C61\u0C85-\u0C8C\u0C8E-\u0C90\u0C92-\u0CA8\u0CAA-\u0CB3\u0CB5-\u0CB9\u0CBD\u0CDE\u0CE0\u0CE1\u0CF1\u0CF2\u0D05-\u0D0C\u0D0E-\u0D10\u0D12-\u0D3A\u0D3D\u0D4E\u0D60\u0D61\u0D7A-\u0D7F\u0D85-\u0D96\u0D9A-\u0DB1\u0DB3-\u0DBB\u0DBD\u0DC0-\u0DC6\u0F00\u0F40-\u0F47\u0F49-\u0F6C\u0F88-\u0F8C\u10A0-\u10C5\u10D0-\u10FA\u10FC\u1100-\u1248\u124A-\u124D\u1250-\u1256\u1258\u125A-\u125D\u1260-\u1288\u128A-\u128D\u1290-\u12B0\u12B2-\u12B5\u12B8-\u12BE\u12C0\u12C2-\u12C5\u12C8-\u12D6\u12D8-\u1310\u1312-\u1315\u1318-\u135A\u1380-\u138F\u13A0-\u13F4\u1401-\u166C\u166F-\u167F\u1681-\u169A\u16A0-\u16EA\u16EE-\u16F0\u1700-\u170C\u170E-\u1711\u1720-\u1731\u1740-\u1751\u1760-\u176C\u176E-\u1770\u1820-\u1877\u1880-\u18A8\u18AA\u18B0-\u18F5\u1900-\u191C\u1A00-\u1A16\u1B05-\u1B33\u1B45-\u1B4B\u1B83-\u1BA0\u1BAE\u1BAF\u1BC0-\u1BE5\u1C00-\u1C23\u1C4D-\u1C4F\u1C5A-\u1C7D\u1CE9-\u1CEC\u1CEE-\u1CF1\u1D00-\u1DBF\u1E00-\u1F15\u1F18-\u1F1D\u1F20-\u1F45\u1F48-\u1F4D\u1F50-\u1F57\u1F59\u1F5B\u1F5D\u1F5F-\u1F7D\u1F80-\u1FB4\u1FB6-\u1FBC\u1FBE\u1FC2-\u1FC4\u1FC6-\u1FCC\u1FD0-\u1FD3\u1FD6-\u1FDB\u1FE0-\u1FEC\u1FF2-\u1FF4\u1FF6-\u1FFC\u2071\u207F\u2090-\u209C\u2102\u2107\u210A-\u2113\u2115\u2119-\u211D\u2124\u2126\u2128\u212A-\u212D\u212F-\u2139\u213C-\u213F\u2145-\u2149\u214E\u2160-\u2188\u24B6-\u24E9\u2C00-\u2C2E\u2C30-\u2C5E\u2C60-\u2CE4\u2CEB-\u2CEE\u2D00-\u2D25\u2D30-\u2D65\u2D6F\u2D80-\u2D96\u2DA0-\u2DA6\u2DA8-\u2DAE\u2DB0-\u2DB6\u2DB8-\u2DBE\u2DC0-\u2DC6\u2DC8-\u2DCE\u2DD0-\u2DD6\u2DD8-\u2DDE\u2E2F\u3005\u303B\u303C\u3105-\u312D\u3131-\u318E\u31A0-\u31BA\uA000-\uA48C\uA4D0-\uA4FD\uA500-\uA60C\uA610-\uA61F\uA62A\uA62B\uA640-\uA66E\uA67F-\uA697\uA6A0-\uA6EF\uA717-\uA71F\uA722-\uA788\uA78B-\uA78E\uA790\uA791\uA7A0-\uA7A9\uA7FA-\uA801\uA803-\uA805\uA807-\uA80A\uA80C-\uA822\uA840-\uA873\uA882-\uA8B3\uA8F2-\uA8F7\uA8FB\uA90A-\uA925\uA930-\uA946\uA960-\uA97C\uA984-\uA9B2\uA9CF\uAA00-\uAA28\uAA40-\uAA42\uAA44-\uAA4B\uAB01-\uAB06\uAB09-\uAB0E\uAB11-\uAB16\uAB20-\uAB26\uAB28-\uAB2E\uABC0-\uABE2\uAC00-\uD7A3\uD7B0-\uD7C6\uD7CB-\uD7FB\uFB00-\uFB06\uFB13-\uFB17\uFB1D\uFB1F-\uFB28\uFB2A-\uFB36\uFB38-\uFB3C\uFB3E\uFB40\uFB41\uFB43\uFB44\uFB46-\uFBB1\uFBD3-\uFD3D\uFD50-\uFD8F\uFD92-\uFDC7\uFDF0-\uFDFB\uFE70-\uFE74\uFE76-\uFEFC\uFF21-\uFF3A\uFF41-\uFF5A\uFFA0-\uFFBE\uFFC2-\uFFC7\uFFCA-\uFFCF\uFFD2-\uFFD7\uFFDA-\uFFDC]",
    midnumlet: `[-'\\.\u2018\u2019\u2024\uFE52\uFF07\uFF0E]`,
    midletter: "[:\xB7\xB7\u05F4\u2027\uFE13\uFE55\uFF1A]",
    midnum: "[\xB1+*/,;;\u0589\u060C\u060D\u066C\u07F8\u2044\uFE10\uFE14\uFE50\uFE54\uFF0C\uFF1B]",
    numeric: "[0-9\u0660-\u0669\u066B\u06F0-\u06F9\u07C0-\u07C9\u0966-\u096F\u09E6-\u09EF\u0A66-\u0A6F\u0AE6-\u0AEF\u0B66-\u0B6F\u0BE6-\u0BEF\u0C66-\u0C6F\u0CE6-\u0CEF\u0D66-\u0D6F\u0E50-\u0E59\u0ED0-\u0ED9\u0F20-\u0F29\u1040-\u1049\u1090-\u1099\u17E0-\u17E9\u1810-\u1819\u1946-\u194F\u19D0-\u19D9\u1A80-\u1A89\u1A90-\u1A99\u1B50-\u1B59\u1BB0-\u1BB9\u1C40-\u1C49\u1C50-\u1C59\uA620-\uA629\uA8D0-\uA8D9\uA900-\uA909\uA9D0-\uA9D9\uAA50-\uAA59\uABF0-\uABF9]",
    cr: "\\r",
    lf: "\\n",
    newline: "[\v\f\x85\u2028\u2029]",
    extend: "[\u0300-\u036F\u0483-\u0489\u0591-\u05BD\u05BF\u05C1\u05C2\u05C4\u05C5\u05C7\u0610-\u061A\u064B-\u065F\u0670\u06D6-\u06DC\u06DF-\u06E4\u06E7\u06E8\u06EA-\u06ED\u0711\u0730-\u074A\u07A6-\u07B0\u07EB-\u07F3\u0816-\u0819\u081B-\u0823\u0825-\u0827\u0829-\u082D\u0859-\u085B\u0900-\u0903\u093A-\u093C\u093E-\u094F\u0951-\u0957\u0962\u0963\u0981-\u0983\u09BC\u09BE-\u09C4\u09C7\u09C8\u09CB-\u09CD\u09D7\u09E2\u09E3\u0A01-\u0A03\u0A3C\u0A3E-\u0A42\u0A47\u0A48\u0A4B-\u0A4D\u0A51\u0A70\u0A71\u0A75\u0A81-\u0A83\u0ABC\u0ABE-\u0AC5\u0AC7-\u0AC9\u0ACB-\u0ACD\u0AE2\u0AE3\u0B01-\u0B03\u0B3C\u0B3E-\u0B44\u0B47\u0B48\u0B4B-\u0B4D\u0B56\u0B57\u0B62\u0B63\u0B82\u0BBE-\u0BC2\u0BC6-\u0BC8\u0BCA-\u0BCD\u0BD7\u0C01-\u0C03\u0C3E-\u0C44\u0C46-\u0C48\u0C4A-\u0C4D\u0C55\u0C56\u0C62\u0C63\u0C82\u0C83\u0CBC\u0CBE-\u0CC4\u0CC6-\u0CC8\u0CCA-\u0CCD\u0CD5\u0CD6\u0CE2\u0CE3\u0D02\u0D03\u0D3E-\u0D44\u0D46-\u0D48\u0D4A-\u0D4D\u0D57\u0D62\u0D63\u0D82\u0D83\u0DCA\u0DCF-\u0DD4\u0DD6\u0DD8-\u0DDF\u0DF2\u0DF3\u0E31\u0E34-\u0E3A\u0E47-\u0E4E\u0EB1\u0EB4-\u0EB9\u0EBB\u0EBC\u0EC8-\u0ECD\u0F18\u0F19\u0F35\u0F37\u0F39\u0F3E\u0F3F\u0F71-\u0F84\u0F86\u0F87\u0F8D-\u0F97\u0F99-\u0FBC\u0FC6\u102B-\u103E\u1056-\u1059\u105E-\u1060\u1062-\u1064\u1067-\u106D\u1071-\u1074\u1082-\u108D\u108F\u109A-\u109D\u135D-\u135F\u1712-\u1714\u1732-\u1734\u1752\u1753\u1772\u1773\u17B6-\u17D3\u17DD\u180B-\u180D\u18A9\u1920-\u192B\u1930-\u193B\u19B0-\u19C0\u19C8\u19C9\u1A17-\u1A1B\u1A55-\u1A5E\u1A60-\u1A7C\u1A7F\u1B00-\u1B04\u1B34-\u1B44\u1B6B-\u1B73\u1B80-\u1B82\u1BA1-\u1BAA\u1BE6-\u1BF3\u1C24-\u1C37\u1CD0-\u1CD2\u1CD4-\u1CE8\u1CED\u1CF2\u1DC0-\u1DE6\u1DFC-\u1DFF\u200C\u200D\u20D0-\u20F0\u2CEF-\u2CF1\u2D7F\u2DE0-\u2DFF\u302A-\u302F\u3099\u309A\uA66F-\uA672\uA67C\uA67D\uA6F0\uA6F1\uA802\uA806\uA80B\uA823-\uA827\uA880\uA881\uA8B4-\uA8C4\uA8E0-\uA8F1\uA926-\uA92D\uA947-\uA953\uA980-\uA983\uA9B3-\uA9C0\uAA29-\uAA36\uAA43\uAA4C\uAA4D\uAA7B\uAAB0\uAAB2-\uAAB4\uAAB7\uAAB8\uAABE\uAABF\uAAC1\uABE3-\uABEA\uABEC\uABED\uFB1E\uFE00-\uFE0F\uFE20-\uFE26\uFF9E\uFF9F]",
    format: "[\xAD\u0600-\u0603\u06DD\u070F\u17B4\u17B5\u200E\u200F\u202A-\u202E\u2060-\u2064\u206A-\u206F\uFEFF\uFFF9-\uFFFB]",
    katakana: "[\u3031-\u3035\u309B\u309C\u30A0-\u30FA\u30FC-\u30FF\u31F0-\u31FF\u32D0-\u32FE\u3300-\u3357\uFF66-\uFF9D]",
    extendnumlet: "[=_\u203F\u2040\u2054\uFE33\uFE34\uFE4D-\uFE4F\uFF3F\u2200-\u22FF<>]",
    punctuation: punctuationStr
  };
  const characterIndices = {
    ALETTER: 0,
    MIDNUMLET: 1,
    MIDLETTER: 2,
    MIDNUM: 3,
    NUMERIC: 4,
    CR: 5,
    LF: 6,
    NEWLINE: 7,
    EXTEND: 8,
    FORMAT: 9,
    KATAKANA: 10,
    EXTENDNUMLET: 11,
    AT: 12,
    OTHER: 13
  };
  const SETS$1 = [
    new RegExp(regExps.aletter),
    new RegExp(regExps.midnumlet),
    new RegExp(regExps.midletter),
    new RegExp(regExps.midnum),
    new RegExp(regExps.numeric),
    new RegExp(regExps.cr),
    new RegExp(regExps.lf),
    new RegExp(regExps.newline),
    new RegExp(regExps.extend),
    new RegExp(regExps.format),
    new RegExp(regExps.katakana),
    new RegExp(regExps.extendnumlet),
    new RegExp("@")
  ];
  const EMPTY_STRING$1 = "";
  const PUNCTUATION$1 = new RegExp("^" + regExps.punctuation + "$");
  const WHITESPACE$1 = /^\s+$/;
  const SETS = SETS$1;
  const OTHER = characterIndices.OTHER;
  const getType = (char) => {
    let type = OTHER;
    const setsLength = SETS.length;
    for (let j = 0; j < setsLength; ++j) {
      const set = SETS[j];
      if (set && set.test(char)) {
        type = j;
        break;
      }
    }
    return type;
  };
  const memoize = (func) => {
    const cache = {};
    return (char) => {
      if (cache[char]) {
        return cache[char];
      } else {
        const result = func(char);
        cache[char] = result;
        return result;
      }
    };
  };
  const classify = (characters) => {
    const memoized = memoize(getType);
    return map(characters, memoized);
  };
  const isWordBoundary = (map2, index) => {
    const type = map2[index];
    const nextType = map2[index + 1];
    if (index < 0 || index > map2.length - 1 && index !== 0) {
      return false;
    }
    if (type === characterIndices.ALETTER && nextType === characterIndices.ALETTER) {
      return false;
    }
    const nextNextType = map2[index + 2];
    if (type === characterIndices.ALETTER && (nextType === characterIndices.MIDLETTER || nextType === characterIndices.MIDNUMLET || nextType === characterIndices.AT) && nextNextType === characterIndices.ALETTER) {
      return false;
    }
    const prevType = map2[index - 1];
    if ((type === characterIndices.MIDLETTER || type === characterIndices.MIDNUMLET || nextType === characterIndices.AT) && nextType === characterIndices.ALETTER && prevType === characterIndices.ALETTER) {
      return false;
    }
    if ((type === characterIndices.NUMERIC || type === characterIndices.ALETTER) && (nextType === characterIndices.NUMERIC || nextType === characterIndices.ALETTER)) {
      return false;
    }
    if ((type === characterIndices.MIDNUM || type === characterIndices.MIDNUMLET) && nextType === characterIndices.NUMERIC && prevType === characterIndices.NUMERIC) {
      return false;
    }
    if (type === characterIndices.NUMERIC && (nextType === characterIndices.MIDNUM || nextType === characterIndices.MIDNUMLET) && nextNextType === characterIndices.NUMERIC) {
      return false;
    }
    if (type === characterIndices.EXTEND || type === characterIndices.FORMAT || prevType === characterIndices.EXTEND || prevType === characterIndices.FORMAT || nextType === characterIndices.EXTEND || nextType === characterIndices.FORMAT) {
      return false;
    }
    if (type === characterIndices.CR && nextType === characterIndices.LF) {
      return false;
    }
    if (type === characterIndices.NEWLINE || type === characterIndices.CR || type === characterIndices.LF) {
      return true;
    }
    if (nextType === characterIndices.NEWLINE || nextType === characterIndices.CR || nextType === characterIndices.LF) {
      return true;
    }
    if (type === characterIndices.KATAKANA && nextType === characterIndices.KATAKANA) {
      return false;
    }
    if (nextType === characterIndices.EXTENDNUMLET && (type === characterIndices.ALETTER || type === characterIndices.NUMERIC || type === characterIndices.KATAKANA || type === characterIndices.EXTENDNUMLET)) {
      return false;
    }
    if (type === characterIndices.EXTENDNUMLET && (nextType === characterIndices.ALETTER || nextType === characterIndices.NUMERIC || nextType === characterIndices.KATAKANA)) {
      return false;
    }
    if (type === characterIndices.AT) {
      return false;
    }
    return true;
  };
  const EMPTY_STRING = EMPTY_STRING$1;
  const WHITESPACE = WHITESPACE$1;
  const PUNCTUATION = PUNCTUATION$1;
  const isProtocol = (str) => str === "http" || str === "https";
  const findWordEnd = (characters, startIndex) => {
    let i2;
    for (i2 = startIndex; i2 < characters.length; i2++) {
      if (WHITESPACE.test(characters[i2])) {
        break;
      }
    }
    return i2;
  };
  const findUrlEnd = (characters, startIndex) => {
    const endIndex = findWordEnd(characters, startIndex + 1);
    const peakedWord = characters.slice(startIndex + 1, endIndex).join(EMPTY_STRING);
    return peakedWord.substr(0, 3) === "://" ? endIndex : startIndex;
  };
  const findWords = (chars, sChars, characterMap, options) => {
    const words = [];
    let word = [];
    for (let i2 = 0; i2 < characterMap.length; ++i2) {
      word.push(chars[i2]);
      if (isWordBoundary(characterMap, i2)) {
        const ch = sChars[i2];
        if ((options.includeWhitespace || !WHITESPACE.test(ch)) && (options.includePunctuation || !PUNCTUATION.test(ch))) {
          const startOfWord = i2 - word.length + 1;
          const endOfWord = i2 + 1;
          const str = sChars.slice(startOfWord, endOfWord).join(EMPTY_STRING);
          if (isProtocol(str)) {
            const endOfUrl = findUrlEnd(sChars, i2);
            const url = chars.slice(endOfWord, endOfUrl);
            Array.prototype.push.apply(word, url);
            i2 = endOfUrl;
          }
          words.push(word);
        }
        word = [];
      }
    }
    return words;
  };
  const getDefaultOptions = () => ({
    includeWhitespace: false,
    includePunctuation: false
  });
  const getWords$1 = (chars, extract, options) => {
    options = {
      ...getDefaultOptions(),
      ...options
    };
    const filteredChars = [];
    const extractedChars = [];
    for (let i2 = 0; i2 < chars.length; i2++) {
      const ch = extract(chars[i2]);
      if (ch !== zeroWidth) {
        filteredChars.push(chars[i2]);
        extractedChars.push(ch);
      }
    }
    const characterMap = classify(extractedChars);
    return findWords(filteredChars, extractedChars, characterMap, options);
  };
  const getWords = getWords$1;
  var global$1 = tinymce.util.Tools.resolve("tinymce.dom.TreeWalker");
  const getText = (node, schema) => {
    const blockElements = schema.getBlockElements();
    const voidElements = schema.getVoidElements();
    const isNewline = (node2) => blockElements[node2.nodeName] || voidElements[node2.nodeName];
    const textBlocks = [];
    let txt = "";
    const treeWalker = new global$1(node, node);
    let tempNode;
    while (tempNode = treeWalker.next()) {
      if (tempNode.nodeType === 3) {
        txt += removeZwsp$1(tempNode.data);
      } else if (isNewline(tempNode) && txt.length) {
        textBlocks.push(txt);
        txt = "";
      }
    }
    if (txt.length) {
      textBlocks.push(txt);
    }
    return textBlocks;
  };
  const removeZwsp = (text) => text.replace(/\u200B/g, "");
  const strLen = (str) => str.replace(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g, "_").length;
  const countWords = (node, schema) => {
    const text = removeZwsp(getText(node, schema).join("\n"));
    return getWords(text.split(""), identity).length;
  };
  const countCharacters = (node, schema) => {
    const text = getText(node, schema).join("");
    return strLen(text);
  };
  const countCharactersWithoutSpaces = (node, schema) => {
    const text = getText(node, schema).join("").replace(/\s/g, "");
    return strLen(text);
  };
  const createBodyCounter = (editor, count) => () => count(editor.getBody(), editor.schema);
  const createSelectionCounter = (editor, count) => () => count(editor.selection.getRng().cloneContents(), editor.schema);
  const createBodyWordCounter = (editor) => createBodyCounter(editor, countWords);
  const get = (editor) => ({
    body: {
      getWordCount: createBodyWordCounter(editor),
      getCharacterCount: createBodyCounter(editor, countCharacters),
      getCharacterCountWithoutSpaces: createBodyCounter(editor, countCharactersWithoutSpaces)
    },
    selection: {
      getWordCount: createSelectionCounter(editor, countWords),
      getCharacterCount: createSelectionCounter(editor, countCharacters),
      getCharacterCountWithoutSpaces: createSelectionCounter(editor, countCharactersWithoutSpaces)
    },
    getCount: createBodyWordCounter(editor)
  });
  const open = (editor, api) => {
    editor.windowManager.open({
      title: "Word Count",
      body: {
        type: "panel",
        items: [{
          type: "table",
          header: [
            "Count",
            "Document",
            "Selection"
          ],
          cells: [
            [
              "Words",
              String(api.body.getWordCount()),
              String(api.selection.getWordCount())
            ],
            [
              "Characters (no spaces)",
              String(api.body.getCharacterCountWithoutSpaces()),
              String(api.selection.getCharacterCountWithoutSpaces())
            ],
            [
              "Characters",
              String(api.body.getCharacterCount()),
              String(api.selection.getCharacterCount())
            ]
          ]
        }]
      },
      buttons: [{
        type: "cancel",
        name: "close",
        text: "Close",
        primary: true
      }]
    });
  };
  const register$1 = (editor, api) => {
    editor.addCommand("mceWordCount", () => open(editor, api));
  };
  const first = (fn, rate) => {
    let timer = null;
    const cancel = () => {
      if (!isNull(timer)) {
        clearTimeout(timer);
        timer = null;
      }
    };
    const throttle = (...args) => {
      if (isNull(timer)) {
        timer = setTimeout(() => {
          timer = null;
          fn.apply(null, args);
        }, rate);
      }
    };
    return {
      cancel,
      throttle
    };
  };
  var global2 = tinymce.util.Tools.resolve("tinymce.util.Delay");
  const fireWordCountUpdate = (editor, api) => {
    editor.dispatch("wordCountUpdate", {
      wordCount: {
        words: api.body.getWordCount(),
        characters: api.body.getCharacterCount(),
        charactersWithoutSpaces: api.body.getCharacterCountWithoutSpaces()
      }
    });
  };
  const updateCount = (editor, api) => {
    fireWordCountUpdate(editor, api);
  };
  const setup = (editor, api, delay) => {
    const debouncedUpdate = first(() => updateCount(editor, api), delay);
    editor.on("init", () => {
      updateCount(editor, api);
      global2.setEditorTimeout(editor, () => {
        editor.on("SetContent BeforeAddUndo Undo Redo ViewUpdate keyup", debouncedUpdate.throttle);
      }, 0);
      editor.on("remove", debouncedUpdate.cancel);
    });
  };
  const register = (editor) => {
    const onAction = () => editor.execCommand("mceWordCount");
    editor.ui.registry.addButton("wordcount", {
      tooltip: "Word count",
      icon: "character-count",
      onAction
    });
    editor.ui.registry.addMenuItem("wordcount", {
      text: "Word count",
      icon: "character-count",
      onAction
    });
  };
  var Plugin = (delay = 300) => {
    global$2.add("wordcount", (editor) => {
      const api = get(editor);
      register$1(editor, api);
      register(editor);
      setup(editor, api, delay);
      return api;
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  const Cell = (initial) => {
    let value = initial;
    const get = () => {
      return value;
    };
    const set = (v) => {
      value = v;
    };
    return {
      get,
      set
    };
  };
  var global2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const fireVisualBlocks = (editor, state) => {
    editor.dispatch("VisualBlocks", { state });
  };
  const toggleVisualBlocks = (editor, pluginUrl, enabledState) => {
    const dom = editor.dom;
    dom.toggleClass(editor.getBody(), "mce-visualblocks");
    enabledState.set(!enabledState.get());
    fireVisualBlocks(editor, enabledState.get());
  };
  const register$2 = (editor, pluginUrl, enabledState) => {
    editor.addCommand("mceVisualBlocks", () => {
      toggleVisualBlocks(editor, pluginUrl, enabledState);
    });
  };
  const option = (name) => (editor) => editor.options.get(name);
  const register$1 = (editor) => {
    const registerOption = editor.options.register;
    registerOption("visualblocks_default_state", {
      processor: "boolean",
      default: false
    });
  };
  const isEnabledByDefault = option("visualblocks_default_state");
  const setup = (editor, pluginUrl, enabledState) => {
    editor.on("PreviewFormats AfterPreviewFormats", (e) => {
      if (enabledState.get()) {
        editor.dom.toggleClass(editor.getBody(), "mce-visualblocks", e.type === "afterpreviewformats");
      }
    });
    editor.on("init", () => {
      if (isEnabledByDefault(editor)) {
        toggleVisualBlocks(editor, pluginUrl, enabledState);
      }
    });
  };
  const toggleActiveState = (editor, enabledState) => (api) => {
    api.setActive(enabledState.get());
    const editorEventCallback = (e) => api.setActive(e.state);
    editor.on("VisualBlocks", editorEventCallback);
    return () => editor.off("VisualBlocks", editorEventCallback);
  };
  const register = (editor, enabledState) => {
    const onAction = () => editor.execCommand("mceVisualBlocks");
    editor.ui.registry.addToggleButton("visualblocks", {
      icon: "visualblocks",
      tooltip: "Show blocks",
      onAction,
      onSetup: toggleActiveState(editor, enabledState)
    });
    editor.ui.registry.addToggleMenuItem("visualblocks", {
      text: "Show blocks",
      icon: "visualblocks",
      onAction,
      onSetup: toggleActiveState(editor, enabledState)
    });
  };
  var Plugin = () => {
    global2.add("visualblocks", (editor, pluginUrl) => {
      register$1(editor);
      const enabledState = Cell(false);
      register$2(editor, pluginUrl, enabledState);
      register(editor, enabledState);
      setup(editor, pluginUrl, enabledState);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  const Cell = (initial) => {
    let value2 = initial;
    const get2 = () => {
      return value2;
    };
    const set2 = (v) => {
      value2 = v;
    };
    return {
      get: get2,
      set: set2
    };
  };
  var global$2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const get$5 = (fullscreenState) => ({ isFullscreen: () => fullscreenState.get() !== null });
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType$1 = (type2) => (value2) => typeOf(value2) === type2;
  const isSimpleType = (type2) => (value2) => typeof value2 === type2;
  const eq$1 = (t) => (a) => t === a;
  const isString = isType$1("string");
  const isArray = isType$1("array");
  const isNull = eq$1(null);
  const isBoolean = isSimpleType("boolean");
  const isUndefined = eq$1(void 0);
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const isNumber = isSimpleType("number");
  const noop = () => {
  };
  const compose = (fa, fb) => {
    return (...args) => {
      return fa(fb.apply(null, args));
    };
  };
  const compose1 = (fbc, fab) => (a) => fbc(fab(a));
  const constant = (value2) => {
    return () => {
      return value2;
    };
  };
  function curry(fn, ...initialArgs) {
    return (...restArgs) => {
      const all2 = initialArgs.concat(restArgs);
      return fn.apply(null, all2);
    };
  }
  const never = constant(false);
  const always = constant(true);
  class Optional {
    constructor(tag, value2) {
      this.tag = tag;
      this.value = value2;
    }
    static some(value2) {
      return new Optional(true, value2);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder2) {
      if (this.tag) {
        return binder2(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value2) {
      return isNonNullable(value2) ? Optional.some(value2) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const singleton = (doRevoke) => {
    const subject = Cell(Optional.none());
    const revoke = () => subject.get().each(doRevoke);
    const clear = () => {
      revoke();
      subject.set(Optional.none());
    };
    const isSet = () => subject.get().isSome();
    const get2 = () => subject.get();
    const set2 = (s) => {
      revoke();
      subject.set(Optional.some(s));
    };
    return {
      clear,
      isSet,
      get: get2,
      set: set2
    };
  };
  const unbindable = () => singleton((s) => s.unbind());
  const value = () => {
    const subject = singleton(noop);
    const on = (f) => subject.get().each(f);
    return {
      ...subject,
      on
    };
  };
  const first = (fn, rate) => {
    let timer = null;
    const cancel = () => {
      if (!isNull(timer)) {
        clearTimeout(timer);
        timer = null;
      }
    };
    const throttle = (...args) => {
      if (isNull(timer)) {
        timer = setTimeout(() => {
          timer = null;
          fn.apply(null, args);
        }, rate);
      }
    };
    return {
      cancel,
      throttle
    };
  };
  const nativePush = Array.prototype.push;
  const map = (xs, f) => {
    const len = xs.length;
    const r2 = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r2[i2] = f(x, i2);
    }
    return r2;
  };
  const each$1 = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const filter$1 = (xs, pred) => {
    const r2 = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        r2.push(x);
      }
    }
    return r2;
  };
  const findUntil = (xs, pred, until) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return Optional.some(x);
      } else if (until(x, i2)) {
        break;
      }
    }
    return Optional.none();
  };
  const find$1 = (xs, pred) => {
    return findUntil(xs, pred, never);
  };
  const flatten = (xs) => {
    const r2 = [];
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      if (!isArray(xs[i2])) {
        throw new Error("Arr.flatten item " + i2 + " was not an array, input: " + xs);
      }
      nativePush.apply(r2, xs[i2]);
    }
    return r2;
  };
  const bind$3 = (xs, f) => flatten(map(xs, f));
  const get$4 = (xs, i2) => i2 >= 0 && i2 < xs.length ? Optional.some(xs[i2]) : Optional.none();
  const head = (xs) => get$4(xs, 0);
  const findMap = (arr, f) => {
    for (let i2 = 0; i2 < arr.length; i2++) {
      const r2 = f(arr[i2], i2);
      if (r2.isSome()) {
        return r2;
      }
    }
    return Optional.none();
  };
  const keys = Object.keys;
  const each = (obj, f) => {
    const props = keys(obj);
    for (let k = 0, len = props.length; k < len; k++) {
      const i2 = props[k];
      const x = obj[i2];
      f(x, i2);
    }
  };
  const contains = (str, substr, start = 0, end) => {
    const idx = str.indexOf(substr, start);
    if (idx !== -1) {
      return isUndefined(end) ? true : idx + substr.length <= end;
    } else {
      return false;
    }
  };
  const isSupported$1 = (dom) => dom.style !== void 0 && isFunction(dom.style.getPropertyValue);
  const fromHtml = (html, scope) => {
    const doc = scope || document;
    const div = doc.createElement("div");
    div.innerHTML = html;
    if (!div.hasChildNodes() || div.childNodes.length > 1) {
      const message = "HTML does not have a single root node";
      console.error(message, html);
      throw new Error(message);
    }
    return fromDom(div.childNodes[0]);
  };
  const fromTag = (tag, scope) => {
    const doc = scope || document;
    const node = doc.createElement(tag);
    return fromDom(node);
  };
  const fromText = (text, scope) => {
    const doc = scope || document;
    const node = doc.createTextNode(text);
    return fromDom(node);
  };
  const fromDom = (node) => {
    if (node === null || node === void 0) {
      throw new Error("Node cannot be null or undefined");
    }
    return { dom: node };
  };
  const fromPoint = (docElm, x, y) => Optional.from(docElm.dom.elementFromPoint(x, y)).map(fromDom);
  const SugarElement = {
    fromHtml,
    fromTag,
    fromText,
    fromDom,
    fromPoint
  };
  typeof window !== "undefined" ? window : Function("return this;")();
  const DOCUMENT = 9;
  const DOCUMENT_FRAGMENT = 11;
  const ELEMENT = 1;
  const TEXT = 3;
  const type = (element) => element.dom.nodeType;
  const isType = (t) => (element) => type(element) === t;
  const isElement = isType(ELEMENT);
  const isText = isType(TEXT);
  const isDocument = isType(DOCUMENT);
  const isDocumentFragment = isType(DOCUMENT_FRAGMENT);
  const is = (element, selector) => {
    const dom = element.dom;
    if (dom.nodeType !== ELEMENT) {
      return false;
    } else {
      const elem = dom;
      if (elem.matches !== void 0) {
        return elem.matches(selector);
      } else if (elem.msMatchesSelector !== void 0) {
        return elem.msMatchesSelector(selector);
      } else if (elem.webkitMatchesSelector !== void 0) {
        return elem.webkitMatchesSelector(selector);
      } else if (elem.mozMatchesSelector !== void 0) {
        return elem.mozMatchesSelector(selector);
      } else {
        throw new Error("Browser lacks native selectors");
      }
    }
  };
  const bypassSelector = (dom) => dom.nodeType !== ELEMENT && dom.nodeType !== DOCUMENT && dom.nodeType !== DOCUMENT_FRAGMENT || dom.childElementCount === 0;
  const all$1 = (selector, scope) => {
    const base = scope === void 0 ? document : scope.dom;
    return bypassSelector(base) ? [] : map(base.querySelectorAll(selector), SugarElement.fromDom);
  };
  const eq = (e1, e2) => e1.dom === e2.dom;
  const owner = (element) => SugarElement.fromDom(element.dom.ownerDocument);
  const documentOrOwner = (dos) => isDocument(dos) ? dos : owner(dos);
  const parent = (element) => Optional.from(element.dom.parentNode).map(SugarElement.fromDom);
  const parents = (element, isRoot) => {
    const stop = isFunction(isRoot) ? isRoot : never;
    let dom = element.dom;
    const ret = [];
    while (dom.parentNode !== null && dom.parentNode !== void 0) {
      const rawParent = dom.parentNode;
      const p = SugarElement.fromDom(rawParent);
      ret.push(p);
      if (stop(p) === true) {
        break;
      } else {
        dom = rawParent;
      }
    }
    return ret;
  };
  const siblings$2 = (element) => {
    const filterSelf = (elements) => filter$1(elements, (x) => !eq(element, x));
    return parent(element).map(children).map(filterSelf).getOr([]);
  };
  const children = (element) => map(element.dom.childNodes, SugarElement.fromDom);
  const isShadowRoot = (dos) => isDocumentFragment(dos) && isNonNullable(dos.dom.host);
  const supported = isFunction(Element.prototype.attachShadow) && isFunction(Node.prototype.getRootNode);
  const isSupported = constant(supported);
  const getRootNode = supported ? (e) => SugarElement.fromDom(e.dom.getRootNode()) : documentOrOwner;
  const getShadowRoot = (e) => {
    const r2 = getRootNode(e);
    return isShadowRoot(r2) ? Optional.some(r2) : Optional.none();
  };
  const getShadowHost = (e) => SugarElement.fromDom(e.dom.host);
  const getOriginalEventTarget = (event2) => {
    if (isSupported() && isNonNullable(event2.target)) {
      const el = SugarElement.fromDom(event2.target);
      if (isElement(el) && isOpenShadowHost(el)) {
        if (event2.composed && event2.composedPath) {
          const composedPath = event2.composedPath();
          if (composedPath) {
            return head(composedPath);
          }
        }
      }
    }
    return Optional.from(event2.target);
  };
  const isOpenShadowHost = (element) => isNonNullable(element.dom.shadowRoot);
  const inBody = (element) => {
    const dom = isText(element) ? element.dom.parentNode : element.dom;
    if (dom === void 0 || dom === null || dom.ownerDocument === null) {
      return false;
    }
    const doc = dom.ownerDocument;
    return getShadowRoot(SugarElement.fromDom(dom)).fold(() => doc.body.contains(dom), compose1(inBody, getShadowHost));
  };
  const getBody = (doc) => {
    const b = doc.dom.body;
    if (b === null || b === void 0) {
      throw new Error("Body is not available yet");
    }
    return SugarElement.fromDom(b);
  };
  const rawSet = (dom, key, value2) => {
    if (isString(value2) || isBoolean(value2) || isNumber(value2)) {
      dom.setAttribute(key, value2 + "");
    } else {
      console.error("Invalid call to Attribute.set. Key ", key, ":: Value ", value2, ":: Element ", dom);
      throw new Error("Attribute value was not simple");
    }
  };
  const set = (element, key, value2) => {
    rawSet(element.dom, key, value2);
  };
  const get$3 = (element, key) => {
    const v = element.dom.getAttribute(key);
    return v === null ? void 0 : v;
  };
  const remove = (element, key) => {
    element.dom.removeAttribute(key);
  };
  const internalSet = (dom, property, value2) => {
    if (!isString(value2)) {
      console.error("Invalid call to CSS.set. Property ", property, ":: Value ", value2, ":: Element ", dom);
      throw new Error("CSS value must be a string: " + value2);
    }
    if (isSupported$1(dom)) {
      dom.style.setProperty(property, value2);
    }
  };
  const setAll = (element, css) => {
    const dom = element.dom;
    each(css, (v, k) => {
      internalSet(dom, k, v);
    });
  };
  const get$2 = (element, property) => {
    const dom = element.dom;
    const styles = window.getComputedStyle(dom);
    const r2 = styles.getPropertyValue(property);
    return r2 === "" && !inBody(element) ? getUnsafeProperty(dom, property) : r2;
  };
  const getUnsafeProperty = (dom, property) => isSupported$1(dom) ? dom.style.getPropertyValue(property) : "";
  const mkEvent = (target, x, y, stop, prevent, kill, raw) => ({
    target,
    x,
    y,
    stop,
    prevent,
    kill,
    raw
  });
  const fromRawEvent = (rawEvent) => {
    const target = SugarElement.fromDom(getOriginalEventTarget(rawEvent).getOr(rawEvent.target));
    const stop = () => rawEvent.stopPropagation();
    const prevent = () => rawEvent.preventDefault();
    const kill = compose(prevent, stop);
    return mkEvent(target, rawEvent.clientX, rawEvent.clientY, stop, prevent, kill, rawEvent);
  };
  const handle = (filter2, handler) => (rawEvent) => {
    if (filter2(rawEvent)) {
      handler(fromRawEvent(rawEvent));
    }
  };
  const binder = (element, event2, filter2, handler, useCapture) => {
    const wrapped = handle(filter2, handler);
    element.dom.addEventListener(event2, wrapped, useCapture);
    return { unbind: curry(unbind, element, event2, wrapped, useCapture) };
  };
  const bind$2 = (element, event2, filter2, handler) => binder(element, event2, filter2, handler, false);
  const unbind = (element, event2, handler, useCapture) => {
    element.dom.removeEventListener(event2, handler, useCapture);
  };
  const filter = always;
  const bind$1 = (element, event2, handler) => bind$2(element, event2, filter, handler);
  const cached = (f) => {
    let called = false;
    let r2;
    return (...args) => {
      if (!called) {
        called = true;
        r2 = f.apply(null, args);
      }
      return r2;
    };
  };
  const DeviceType = (os, browser, userAgent, mediaMatch2) => {
    const isiPad = os.isiOS() && /ipad/i.test(userAgent) === true;
    const isiPhone = os.isiOS() && !isiPad;
    const isMobile = os.isiOS() || os.isAndroid();
    const isTouch = isMobile || mediaMatch2("(pointer:coarse)");
    const isTablet = isiPad || !isiPhone && isMobile && mediaMatch2("(min-device-width:768px)");
    const isPhone = isiPhone || isMobile && !isTablet;
    const iOSwebview = browser.isSafari() && os.isiOS() && /safari/i.test(userAgent) === false;
    const isDesktop = !isPhone && !isTablet && !iOSwebview;
    return {
      isiPad: constant(isiPad),
      isiPhone: constant(isiPhone),
      isTablet: constant(isTablet),
      isPhone: constant(isPhone),
      isTouch: constant(isTouch),
      isAndroid: os.isAndroid,
      isiOS: os.isiOS,
      isWebView: constant(iOSwebview),
      isDesktop: constant(isDesktop)
    };
  };
  const firstMatch = (regexes, s) => {
    for (let i2 = 0; i2 < regexes.length; i2++) {
      const x = regexes[i2];
      if (x.test(s)) {
        return x;
      }
    }
    return void 0;
  };
  const find = (regexes, agent) => {
    const r2 = firstMatch(regexes, agent);
    if (!r2) {
      return {
        major: 0,
        minor: 0
      };
    }
    const group = (i2) => {
      return Number(agent.replace(r2, "$" + i2));
    };
    return nu$2(group(1), group(2));
  };
  const detect$3 = (versionRegexes, agent) => {
    const cleanedAgent = String(agent).toLowerCase();
    if (versionRegexes.length === 0) {
      return unknown$2();
    }
    return find(versionRegexes, cleanedAgent);
  };
  const unknown$2 = () => {
    return nu$2(0, 0);
  };
  const nu$2 = (major, minor) => {
    return {
      major,
      minor
    };
  };
  const Version = {
    nu: nu$2,
    detect: detect$3,
    unknown: unknown$2
  };
  const detectBrowser$1 = (browsers2, userAgentData) => {
    return findMap(userAgentData.brands, (uaBrand) => {
      const lcBrand = uaBrand.brand.toLowerCase();
      return find$1(browsers2, (browser) => {
        var _a;
        return lcBrand === ((_a = browser.brand) === null || _a === void 0 ? void 0 : _a.toLowerCase());
      }).map((info) => ({
        current: info.name,
        version: Version.nu(parseInt(uaBrand.version, 10), 0)
      }));
    });
  };
  const detect$2 = (candidates, userAgent) => {
    const agent = String(userAgent).toLowerCase();
    return find$1(candidates, (candidate) => {
      return candidate.search(agent);
    });
  };
  const detectBrowser = (browsers2, userAgent) => {
    return detect$2(browsers2, userAgent).map((browser) => {
      const version = Version.detect(browser.versionRegexes, userAgent);
      return {
        current: browser.name,
        version
      };
    });
  };
  const detectOs = (oses2, userAgent) => {
    return detect$2(oses2, userAgent).map((os) => {
      const version = Version.detect(os.versionRegexes, userAgent);
      return {
        current: os.name,
        version
      };
    });
  };
  const normalVersionRegex = /.*?version\/\ ?([0-9]+)\.([0-9]+).*/;
  const checkContains = (target) => {
    return (uastring) => {
      return contains(uastring, target);
    };
  };
  const browsers = [
    {
      name: "Edge",
      versionRegexes: [/.*?edge\/ ?([0-9]+)\.([0-9]+)$/],
      search: (uastring) => {
        return contains(uastring, "edge/") && contains(uastring, "chrome") && contains(uastring, "safari") && contains(uastring, "applewebkit");
      }
    },
    {
      name: "Chromium",
      brand: "Chromium",
      versionRegexes: [
        /.*?chrome\/([0-9]+)\.([0-9]+).*/,
        normalVersionRegex
      ],
      search: (uastring) => {
        return contains(uastring, "chrome") && !contains(uastring, "chromeframe");
      }
    },
    {
      name: "IE",
      versionRegexes: [
        /.*?msie\ ?([0-9]+)\.([0-9]+).*/,
        /.*?rv:([0-9]+)\.([0-9]+).*/
      ],
      search: (uastring) => {
        return contains(uastring, "msie") || contains(uastring, "trident");
      }
    },
    {
      name: "Opera",
      versionRegexes: [
        normalVersionRegex,
        /.*?opera\/([0-9]+)\.([0-9]+).*/
      ],
      search: checkContains("opera")
    },
    {
      name: "Firefox",
      versionRegexes: [/.*?firefox\/\ ?([0-9]+)\.([0-9]+).*/],
      search: checkContains("firefox")
    },
    {
      name: "Safari",
      versionRegexes: [
        normalVersionRegex,
        /.*?cpu os ([0-9]+)_([0-9]+).*/
      ],
      search: (uastring) => {
        return (contains(uastring, "safari") || contains(uastring, "mobile/")) && contains(uastring, "applewebkit");
      }
    }
  ];
  const oses = [
    {
      name: "Windows",
      search: checkContains("win"),
      versionRegexes: [/.*?windows\ nt\ ?([0-9]+)\.([0-9]+).*/]
    },
    {
      name: "iOS",
      search: (uastring) => {
        return contains(uastring, "iphone") || contains(uastring, "ipad");
      },
      versionRegexes: [
        /.*?version\/\ ?([0-9]+)\.([0-9]+).*/,
        /.*cpu os ([0-9]+)_([0-9]+).*/,
        /.*cpu iphone os ([0-9]+)_([0-9]+).*/
      ]
    },
    {
      name: "Android",
      search: checkContains("android"),
      versionRegexes: [/.*?android\ ?([0-9]+)\.([0-9]+).*/]
    },
    {
      name: "macOS",
      search: checkContains("mac os x"),
      versionRegexes: [/.*?mac\ os\ x\ ?([0-9]+)_([0-9]+).*/]
    },
    {
      name: "Linux",
      search: checkContains("linux"),
      versionRegexes: []
    },
    {
      name: "Solaris",
      search: checkContains("sunos"),
      versionRegexes: []
    },
    {
      name: "FreeBSD",
      search: checkContains("freebsd"),
      versionRegexes: []
    },
    {
      name: "ChromeOS",
      search: checkContains("cros"),
      versionRegexes: [/.*?chrome\/([0-9]+)\.([0-9]+).*/]
    }
  ];
  const PlatformInfo = {
    browsers: constant(browsers),
    oses: constant(oses)
  };
  const edge = "Edge";
  const chromium = "Chromium";
  const ie = "IE";
  const opera = "Opera";
  const firefox = "Firefox";
  const safari = "Safari";
  const unknown$1 = () => {
    return nu$1({
      current: void 0,
      version: Version.unknown()
    });
  };
  const nu$1 = (info) => {
    const current = info.current;
    const version = info.version;
    const isBrowser = (name) => () => current === name;
    return {
      current,
      version,
      isEdge: isBrowser(edge),
      isChromium: isBrowser(chromium),
      isIE: isBrowser(ie),
      isOpera: isBrowser(opera),
      isFirefox: isBrowser(firefox),
      isSafari: isBrowser(safari)
    };
  };
  const Browser = {
    unknown: unknown$1,
    nu: nu$1,
    edge: constant(edge),
    chromium: constant(chromium),
    ie: constant(ie),
    opera: constant(opera),
    firefox: constant(firefox),
    safari: constant(safari)
  };
  const windows = "Windows";
  const ios = "iOS";
  const android = "Android";
  const linux = "Linux";
  const macos = "macOS";
  const solaris = "Solaris";
  const freebsd = "FreeBSD";
  const chromeos = "ChromeOS";
  const unknown = () => {
    return nu({
      current: void 0,
      version: Version.unknown()
    });
  };
  const nu = (info) => {
    const current = info.current;
    const version = info.version;
    const isOS = (name) => () => current === name;
    return {
      current,
      version,
      isWindows: isOS(windows),
      isiOS: isOS(ios),
      isAndroid: isOS(android),
      isMacOS: isOS(macos),
      isLinux: isOS(linux),
      isSolaris: isOS(solaris),
      isFreeBSD: isOS(freebsd),
      isChromeOS: isOS(chromeos)
    };
  };
  const OperatingSystem = {
    unknown,
    nu,
    windows: constant(windows),
    ios: constant(ios),
    android: constant(android),
    linux: constant(linux),
    macos: constant(macos),
    solaris: constant(solaris),
    freebsd: constant(freebsd),
    chromeos: constant(chromeos)
  };
  const detect$1 = (userAgent, userAgentDataOpt, mediaMatch2) => {
    const browsers2 = PlatformInfo.browsers();
    const oses2 = PlatformInfo.oses();
    const browser = userAgentDataOpt.bind((userAgentData) => detectBrowser$1(browsers2, userAgentData)).orThunk(() => detectBrowser(browsers2, userAgent)).fold(Browser.unknown, Browser.nu);
    const os = detectOs(oses2, userAgent).fold(OperatingSystem.unknown, OperatingSystem.nu);
    const deviceType = DeviceType(os, browser, userAgent, mediaMatch2);
    return {
      browser,
      os,
      deviceType
    };
  };
  const PlatformDetection = { detect: detect$1 };
  const mediaMatch = (query) => window.matchMedia(query).matches;
  let platform = cached(() => PlatformDetection.detect(navigator.userAgent, Optional.from(navigator.userAgentData), mediaMatch));
  const detect = () => platform();
  const r = (left, top) => {
    const translate = (x, y) => r(left + x, top + y);
    return {
      left,
      top,
      translate
    };
  };
  const SugarPosition = r;
  const get$1 = (_DOC) => {
    const doc = _DOC !== void 0 ? _DOC.dom : document;
    const x = doc.body.scrollLeft || doc.documentElement.scrollLeft;
    const y = doc.body.scrollTop || doc.documentElement.scrollTop;
    return SugarPosition(x, y);
  };
  const get = (_win) => {
    const win = _win === void 0 ? window : _win;
    if (detect().browser.isFirefox()) {
      return Optional.none();
    } else {
      return Optional.from(win.visualViewport);
    }
  };
  const bounds = (x, y, width, height) => ({
    x,
    y,
    width,
    height,
    right: x + width,
    bottom: y + height
  });
  const getBounds = (_win) => {
    const win = _win === void 0 ? window : _win;
    const doc = win.document;
    const scroll = get$1(SugarElement.fromDom(doc));
    return get(win).fold(() => {
      const html = win.document.documentElement;
      const width = html.clientWidth;
      const height = html.clientHeight;
      return bounds(scroll.left, scroll.top, width, height);
    }, (visualViewport) => bounds(Math.max(visualViewport.pageLeft, scroll.left), Math.max(visualViewport.pageTop, scroll.top), visualViewport.width, visualViewport.height));
  };
  const bind = (name, callback, _win) => get(_win).map((visualViewport) => {
    const handler = (e) => callback(fromRawEvent(e));
    visualViewport.addEventListener(name, handler);
    return { unbind: () => visualViewport.removeEventListener(name, handler) };
  }).getOrThunk(() => ({ unbind: noop }));
  var global$1 = tinymce.util.Tools.resolve("tinymce.dom.DOMUtils");
  var global2 = tinymce.util.Tools.resolve("tinymce.Env");
  const fireFullscreenStateChanged = (editor, state) => {
    editor.dispatch("FullscreenStateChanged", { state });
    editor.dispatch("ResizeEditor");
  };
  const option = (name) => (editor) => editor.options.get(name);
  const register$2 = (editor) => {
    const registerOption = editor.options.register;
    registerOption("fullscreen_native", {
      processor: "boolean",
      default: false
    });
  };
  const getFullscreenNative = option("fullscreen_native");
  const getFullscreenRoot = (editor) => {
    const elem = SugarElement.fromDom(editor.getElement());
    return getShadowRoot(elem).map(getShadowHost).getOrThunk(() => getBody(owner(elem)));
  };
  const getFullscreenElement = (root) => {
    if (root.fullscreenElement !== void 0) {
      return root.fullscreenElement;
    } else if (root.msFullscreenElement !== void 0) {
      return root.msFullscreenElement;
    } else if (root.webkitFullscreenElement !== void 0) {
      return root.webkitFullscreenElement;
    } else {
      return null;
    }
  };
  const getFullscreenchangeEventName = () => {
    if (document.fullscreenElement !== void 0) {
      return "fullscreenchange";
    } else if (document.msFullscreenElement !== void 0) {
      return "MSFullscreenChange";
    } else if (document.webkitFullscreenElement !== void 0) {
      return "webkitfullscreenchange";
    } else {
      return "fullscreenchange";
    }
  };
  const requestFullscreen = (sugarElem) => {
    const elem = sugarElem.dom;
    if (elem.requestFullscreen) {
      elem.requestFullscreen();
    } else if (elem.msRequestFullscreen) {
      elem.msRequestFullscreen();
    } else if (elem.webkitRequestFullScreen) {
      elem.webkitRequestFullScreen();
    }
  };
  const exitFullscreen = (sugarDoc) => {
    const doc = sugarDoc.dom;
    if (doc.exitFullscreen) {
      doc.exitFullscreen();
    } else if (doc.msExitFullscreen) {
      doc.msExitFullscreen();
    } else if (doc.webkitCancelFullScreen) {
      doc.webkitCancelFullScreen();
    }
  };
  const isFullscreenElement = (elem) => elem.dom === getFullscreenElement(owner(elem).dom);
  const ancestors$1 = (scope, predicate, isRoot) => filter$1(parents(scope, isRoot), predicate);
  const siblings$1 = (scope, predicate) => filter$1(siblings$2(scope), predicate);
  const all = (selector) => all$1(selector);
  const ancestors = (scope, selector, isRoot) => ancestors$1(scope, (e) => is(e, selector), isRoot);
  const siblings = (scope, selector) => siblings$1(scope, (e) => is(e, selector));
  const attr = "data-ephox-mobile-fullscreen-style";
  const siblingStyles = "display:none!important;";
  const ancestorPosition = "position:absolute!important;";
  const ancestorStyles = "top:0!important;left:0!important;margin:0!important;padding:0!important;width:100%!important;height:100%!important;overflow:visible!important;";
  const bgFallback = "background-color:rgb(255,255,255)!important;";
  const isAndroid = global2.os.isAndroid();
  const matchColor = (editorBody) => {
    const color = get$2(editorBody, "background-color");
    return color !== void 0 && color !== "" ? "background-color:" + color + "!important" : bgFallback;
  };
  const clobberStyles = (dom, container, editorBody) => {
    const gatherSiblings = (element) => {
      return siblings(element, "*:not(.tox-silver-sink)");
    };
    const clobber = (clobberStyle) => (element) => {
      const styles = get$3(element, "style");
      const backup = styles === void 0 ? "no-styles" : styles.trim();
      if (backup === clobberStyle) {
        return;
      } else {
        set(element, attr, backup);
        setAll(element, dom.parseStyle(clobberStyle));
      }
    };
    const ancestors$12 = ancestors(container, "*");
    const siblings$12 = bind$3(ancestors$12, gatherSiblings);
    const bgColor = matchColor(editorBody);
    each$1(siblings$12, clobber(siblingStyles));
    each$1(ancestors$12, clobber(ancestorPosition + ancestorStyles + bgColor));
    const containerStyles = isAndroid === true ? "" : ancestorPosition;
    clobber(containerStyles + ancestorStyles + bgColor)(container);
  };
  const restoreStyles = (dom) => {
    const clobberedEls = all("[" + attr + "]");
    each$1(clobberedEls, (element) => {
      const restore = get$3(element, attr);
      if (restore && restore !== "no-styles") {
        setAll(element, dom.parseStyle(restore));
      } else {
        remove(element, "style");
      }
      remove(element, attr);
    });
  };
  const DOM = global$1.DOM;
  const getScrollPos = () => getBounds(window);
  const setScrollPos = (pos) => window.scrollTo(pos.x, pos.y);
  const viewportUpdate = get().fold(() => ({
    bind: noop,
    unbind: noop
  }), (visualViewport) => {
    const editorContainer = value();
    const resizeBinder = unbindable();
    const scrollBinder = unbindable();
    const refreshScroll = () => {
      document.body.scrollTop = 0;
      document.documentElement.scrollTop = 0;
    };
    const refreshVisualViewport = () => {
      window.requestAnimationFrame(() => {
        editorContainer.on((container) => setAll(container, {
          top: visualViewport.offsetTop + "px",
          left: visualViewport.offsetLeft + "px",
          height: visualViewport.height + "px",
          width: visualViewport.width + "px"
        }));
      });
    };
    const update = first(() => {
      refreshScroll();
      refreshVisualViewport();
    }, 50);
    const bind$12 = (element) => {
      editorContainer.set(element);
      update.throttle();
      resizeBinder.set(bind("resize", update.throttle));
      scrollBinder.set(bind("scroll", update.throttle));
    };
    const unbind2 = () => {
      editorContainer.on(() => {
        resizeBinder.clear();
        scrollBinder.clear();
      });
      editorContainer.clear();
    };
    return {
      bind: bind$12,
      unbind: unbind2
    };
  });
  const toggleFullscreen = (editor, fullscreenState) => {
    const body = document.body;
    const documentElement = document.documentElement;
    const editorContainer = editor.getContainer();
    const editorContainerS = SugarElement.fromDom(editorContainer);
    const fullscreenRoot = getFullscreenRoot(editor);
    const fullscreenInfo = fullscreenState.get();
    const editorBody = SugarElement.fromDom(editor.getBody());
    const isTouch = global2.deviceType.isTouch();
    const editorContainerStyle = editorContainer.style;
    const iframe = editor.iframeElement;
    const iframeStyle = iframe === null || iframe === void 0 ? void 0 : iframe.style;
    const handleClasses = (handler) => {
      handler(body, "tox-fullscreen");
      handler(documentElement, "tox-fullscreen");
      handler(editorContainer, "tox-fullscreen");
      getShadowRoot(editorContainerS).map((root) => getShadowHost(root).dom).each((host) => {
        handler(host, "tox-fullscreen");
        handler(host, "tox-shadowhost");
      });
    };
    const cleanup = () => {
      if (isTouch) {
        restoreStyles(editor.dom);
      }
      handleClasses(DOM.removeClass);
      viewportUpdate.unbind();
      Optional.from(fullscreenState.get()).each((info) => info.fullscreenChangeHandler.unbind());
    };
    if (!fullscreenInfo) {
      const fullscreenChangeHandler = bind$1(owner(fullscreenRoot), getFullscreenchangeEventName(), (_evt) => {
        if (getFullscreenNative(editor)) {
          if (!isFullscreenElement(fullscreenRoot) && fullscreenState.get() !== null) {
            toggleFullscreen(editor, fullscreenState);
          }
        }
      });
      const newFullScreenInfo = {
        scrollPos: getScrollPos(),
        containerWidth: editorContainerStyle.width,
        containerHeight: editorContainerStyle.height,
        containerTop: editorContainerStyle.top,
        containerLeft: editorContainerStyle.left,
        iframeWidth: iframeStyle.width,
        iframeHeight: iframeStyle.height,
        fullscreenChangeHandler
      };
      if (isTouch) {
        clobberStyles(editor.dom, editorContainerS, editorBody);
      }
      iframeStyle.width = iframeStyle.height = "100%";
      editorContainerStyle.width = editorContainerStyle.height = "";
      handleClasses(DOM.addClass);
      viewportUpdate.bind(editorContainerS);
      editor.on("remove", cleanup);
      fullscreenState.set(newFullScreenInfo);
      if (getFullscreenNative(editor)) {
        requestFullscreen(fullscreenRoot);
      }
      fireFullscreenStateChanged(editor, true);
    } else {
      fullscreenInfo.fullscreenChangeHandler.unbind();
      if (getFullscreenNative(editor) && isFullscreenElement(fullscreenRoot)) {
        exitFullscreen(owner(fullscreenRoot));
      }
      iframeStyle.width = fullscreenInfo.iframeWidth;
      iframeStyle.height = fullscreenInfo.iframeHeight;
      editorContainerStyle.width = fullscreenInfo.containerWidth;
      editorContainerStyle.height = fullscreenInfo.containerHeight;
      editorContainerStyle.top = fullscreenInfo.containerTop;
      editorContainerStyle.left = fullscreenInfo.containerLeft;
      cleanup();
      setScrollPos(fullscreenInfo.scrollPos);
      fullscreenState.set(null);
      fireFullscreenStateChanged(editor, false);
      editor.off("remove", cleanup);
    }
  };
  const register$1 = (editor, fullscreenState) => {
    editor.addCommand("mceFullScreen", () => {
      toggleFullscreen(editor, fullscreenState);
    });
  };
  const makeSetupHandler = (editor, fullscreenState) => (api) => {
    api.setActive(fullscreenState.get() !== null);
    const editorEventCallback = (e) => api.setActive(e.state);
    editor.on("FullscreenStateChanged", editorEventCallback);
    return () => editor.off("FullscreenStateChanged", editorEventCallback);
  };
  const register = (editor, fullscreenState) => {
    const onAction = () => editor.execCommand("mceFullScreen");
    editor.ui.registry.addToggleMenuItem("fullscreen", {
      text: "Fullscreen",
      icon: "fullscreen",
      shortcut: "Meta+Shift+F",
      onAction,
      onSetup: makeSetupHandler(editor, fullscreenState)
    });
    editor.ui.registry.addToggleButton("fullscreen", {
      tooltip: "Fullscreen",
      icon: "fullscreen",
      onAction,
      onSetup: makeSetupHandler(editor, fullscreenState)
    });
  };
  var Plugin = () => {
    global$2.add("fullscreen", (editor) => {
      const fullscreenState = Cell(null);
      if (editor.inline) {
        return get$5(fullscreenState);
      }
      register$2(editor);
      register$1(editor, fullscreenState);
      register(editor, fullscreenState);
      editor.addShortcut("Meta+Shift+F", "", "mceFullScreen");
      return get$5(fullscreenState);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const setContent = (editor, html) => {
    editor.focus();
    editor.undoManager.transact(() => {
      editor.setContent(html);
    });
    editor.selection.setCursorLocation();
    editor.nodeChanged();
  };
  const getContent = (editor) => {
    return editor.getContent({ source_view: true });
  };
  const open = (editor) => {
    const editorContent = getContent(editor);
    editor.windowManager.open({
      title: "Source Code",
      size: "large",
      body: {
        type: "panel",
        items: [{
          type: "textarea",
          name: "code"
        }]
      },
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData: { code: editorContent },
      onSubmit: (api) => {
        setContent(editor, api.getData().code);
        api.close();
      }
    });
  };
  const register$1 = (editor) => {
    editor.addCommand("mceCodeEditor", () => {
      open(editor);
    });
  };
  const register = (editor) => {
    const onAction = () => editor.execCommand("mceCodeEditor");
    editor.ui.registry.addButton("code", {
      icon: "sourcecode",
      tooltip: "Source code",
      onAction
    });
    editor.ui.registry.addMenuItem("code", {
      icon: "sourcecode",
      text: "Source code",
      onAction
    });
  };
  var Plugin = () => {
    global2.add("code", (editor) => {
      register$1(editor);
      register(editor);
      return {};
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$3 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType$1 = (type2) => (value) => typeOf(value) === type2;
  const isSimpleType = (type2) => (value) => typeof value === type2;
  const eq$1 = (t) => (a) => t === a;
  const isString = isType$1("string");
  const isArray = isType$1("array");
  const isBoolean = isSimpleType("boolean");
  const isUndefined = eq$1(void 0);
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const isNumber = isSimpleType("number");
  const noop = () => {
  };
  const compose1 = (fbc, fab) => (a) => fbc(fab(a));
  const constant = (value) => {
    return () => {
      return value;
    };
  };
  const identity = (x) => {
    return x;
  };
  const tripleEquals = (a, b) => {
    return a === b;
  };
  function curry(fn, ...initialArgs) {
    return (...restArgs) => {
      const all2 = initialArgs.concat(restArgs);
      return fn.apply(null, all2);
    };
  }
  const call = (f) => {
    f();
  };
  const never = constant(false);
  const always = constant(true);
  class Optional {
    constructor(tag, value) {
      this.tag = tag;
      this.value = value;
    }
    static some(value) {
      return new Optional(true, value);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder) {
      if (this.tag) {
        return binder(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value) {
      return isNonNullable(value) ? Optional.some(value) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const keys = Object.keys;
  const hasOwnProperty = Object.hasOwnProperty;
  const each$1 = (obj, f) => {
    const props = keys(obj);
    for (let k = 0, len = props.length; k < len; k++) {
      const i2 = props[k];
      const x = obj[i2];
      f(x, i2);
    }
  };
  const objAcc = (r) => (x, i2) => {
    r[i2] = x;
  };
  const internalFilter = (obj, pred, onTrue, onFalse) => {
    each$1(obj, (x, i2) => {
      (pred(x, i2) ? onTrue : onFalse)(x, i2);
    });
  };
  const filter$1 = (obj, pred) => {
    const t = {};
    internalFilter(obj, pred, objAcc(t), noop);
    return t;
  };
  const mapToArray = (obj, f) => {
    const r = [];
    each$1(obj, (value, name2) => {
      r.push(f(value, name2));
    });
    return r;
  };
  const values = (obj) => {
    return mapToArray(obj, identity);
  };
  const size = (obj) => {
    return keys(obj).length;
  };
  const get$4 = (obj, key2) => {
    return has(obj, key2) ? Optional.from(obj[key2]) : Optional.none();
  };
  const has = (obj, key2) => hasOwnProperty.call(obj, key2);
  const hasNonNullableKey = (obj, key2) => has(obj, key2) && obj[key2] !== void 0 && obj[key2] !== null;
  const nativeIndexOf = Array.prototype.indexOf;
  const nativePush = Array.prototype.push;
  const rawIndexOf = (ts, t) => nativeIndexOf.call(ts, t);
  const contains = (xs, x) => rawIndexOf(xs, x) > -1;
  const exists = (xs, pred) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return true;
      }
    }
    return false;
  };
  const range = (num, f) => {
    const r = [];
    for (let i2 = 0; i2 < num; i2++) {
      r.push(f(i2));
    }
    return r;
  };
  const map = (xs, f) => {
    const len = xs.length;
    const r = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r[i2] = f(x, i2);
    }
    return r;
  };
  const each = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const eachr = (xs, f) => {
    for (let i2 = xs.length - 1; i2 >= 0; i2--) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const partition = (xs, pred) => {
    const pass = [];
    const fail = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      const arr = pred(x, i2) ? pass : fail;
      arr.push(x);
    }
    return {
      pass,
      fail
    };
  };
  const filter = (xs, pred) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        r.push(x);
      }
    }
    return r;
  };
  const foldr = (xs, f, acc) => {
    eachr(xs, (x, i2) => {
      acc = f(acc, x, i2);
    });
    return acc;
  };
  const foldl = (xs, f, acc) => {
    each(xs, (x, i2) => {
      acc = f(acc, x, i2);
    });
    return acc;
  };
  const findUntil = (xs, pred, until) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return Optional.some(x);
      } else if (until(x, i2)) {
        break;
      }
    }
    return Optional.none();
  };
  const find = (xs, pred) => {
    return findUntil(xs, pred, never);
  };
  const flatten$1 = (xs) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      if (!isArray(xs[i2])) {
        throw new Error("Arr.flatten item " + i2 + " was not an array, input: " + xs);
      }
      nativePush.apply(r, xs[i2]);
    }
    return r;
  };
  const bind = (xs, f) => flatten$1(map(xs, f));
  const forall = (xs, pred) => {
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      const x = xs[i2];
      if (pred(x, i2) !== true) {
        return false;
      }
    }
    return true;
  };
  const mapToObject = (xs, f) => {
    const r = {};
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      r[String(x)] = f(x, i2);
    }
    return r;
  };
  const get$3 = (xs, i2) => i2 >= 0 && i2 < xs.length ? Optional.some(xs[i2]) : Optional.none();
  const head = (xs) => get$3(xs, 0);
  const last = (xs) => get$3(xs, xs.length - 1);
  const findMap = (arr, f) => {
    for (let i2 = 0; i2 < arr.length; i2++) {
      const r = f(arr[i2], i2);
      if (r.isSome()) {
        return r;
      }
    }
    return Optional.none();
  };
  const fromHtml = (html, scope) => {
    const doc = scope || document;
    const div = doc.createElement("div");
    div.innerHTML = html;
    if (!div.hasChildNodes() || div.childNodes.length > 1) {
      const message = "HTML does not have a single root node";
      console.error(message, html);
      throw new Error(message);
    }
    return fromDom$1(div.childNodes[0]);
  };
  const fromTag = (tag, scope) => {
    const doc = scope || document;
    const node = doc.createElement(tag);
    return fromDom$1(node);
  };
  const fromText = (text, scope) => {
    const doc = scope || document;
    const node = doc.createTextNode(text);
    return fromDom$1(node);
  };
  const fromDom$1 = (node) => {
    if (node === null || node === void 0) {
      throw new Error("Node cannot be null or undefined");
    }
    return { dom: node };
  };
  const fromPoint = (docElm, x, y) => Optional.from(docElm.dom.elementFromPoint(x, y)).map(fromDom$1);
  const SugarElement = {
    fromHtml,
    fromTag,
    fromText,
    fromDom: fromDom$1,
    fromPoint
  };
  typeof window !== "undefined" ? window : Function("return this;")();
  const COMMENT = 8;
  const DOCUMENT = 9;
  const DOCUMENT_FRAGMENT = 11;
  const ELEMENT = 1;
  const TEXT = 3;
  const name = (element) => {
    const r = element.dom.nodeName;
    return r.toLowerCase();
  };
  const type = (element) => element.dom.nodeType;
  const isType = (t) => (element) => type(element) === t;
  const isComment = (element) => type(element) === COMMENT || name(element) === "#comment";
  const isElement = isType(ELEMENT);
  const isText = isType(TEXT);
  const isDocument = isType(DOCUMENT);
  const isDocumentFragment = isType(DOCUMENT_FRAGMENT);
  const isTag = (tag) => (e) => isElement(e) && name(e) === tag;
  const is$2 = (element, selector) => {
    const dom = element.dom;
    if (dom.nodeType !== ELEMENT) {
      return false;
    } else {
      const elem = dom;
      if (elem.matches !== void 0) {
        return elem.matches(selector);
      } else if (elem.msMatchesSelector !== void 0) {
        return elem.msMatchesSelector(selector);
      } else if (elem.webkitMatchesSelector !== void 0) {
        return elem.webkitMatchesSelector(selector);
      } else if (elem.mozMatchesSelector !== void 0) {
        return elem.mozMatchesSelector(selector);
      } else {
        throw new Error("Browser lacks native selectors");
      }
    }
  };
  const bypassSelector = (dom) => dom.nodeType !== ELEMENT && dom.nodeType !== DOCUMENT && dom.nodeType !== DOCUMENT_FRAGMENT || dom.childElementCount === 0;
  const all$1 = (selector, scope) => {
    const base = scope === void 0 ? document : scope.dom;
    return bypassSelector(base) ? [] : map(base.querySelectorAll(selector), SugarElement.fromDom);
  };
  const one = (selector, scope) => {
    const base = scope === void 0 ? document : scope.dom;
    return bypassSelector(base) ? Optional.none() : Optional.from(base.querySelector(selector)).map(SugarElement.fromDom);
  };
  const eq = (e1, e2) => e1.dom === e2.dom;
  const is$1 = is$2;
  const owner = (element) => SugarElement.fromDom(element.dom.ownerDocument);
  const documentOrOwner = (dos) => isDocument(dos) ? dos : owner(dos);
  const parent = (element) => Optional.from(element.dom.parentNode).map(SugarElement.fromDom);
  const parents = (element, isRoot) => {
    const stop = isFunction(isRoot) ? isRoot : never;
    let dom = element.dom;
    const ret = [];
    while (dom.parentNode !== null && dom.parentNode !== void 0) {
      const rawParent = dom.parentNode;
      const p = SugarElement.fromDom(rawParent);
      ret.push(p);
      if (stop(p) === true) {
        break;
      } else {
        dom = rawParent;
      }
    }
    return ret;
  };
  const prevSibling = (element) => Optional.from(element.dom.previousSibling).map(SugarElement.fromDom);
  const nextSibling = (element) => Optional.from(element.dom.nextSibling).map(SugarElement.fromDom);
  const children$3 = (element) => map(element.dom.childNodes, SugarElement.fromDom);
  const child$3 = (element, index) => {
    const cs = element.dom.childNodes;
    return Optional.from(cs[index]).map(SugarElement.fromDom);
  };
  const firstChild = (element) => child$3(element, 0);
  const isShadowRoot = (dos) => isDocumentFragment(dos) && isNonNullable(dos.dom.host);
  const supported = isFunction(Element.prototype.attachShadow) && isFunction(Node.prototype.getRootNode);
  const getRootNode = supported ? (e) => SugarElement.fromDom(e.dom.getRootNode()) : documentOrOwner;
  const getShadowRoot = (e) => {
    const r = getRootNode(e);
    return isShadowRoot(r) ? Optional.some(r) : Optional.none();
  };
  const getShadowHost = (e) => SugarElement.fromDom(e.dom.host);
  const inBody = (element) => {
    const dom = isText(element) ? element.dom.parentNode : element.dom;
    if (dom === void 0 || dom === null || dom.ownerDocument === null) {
      return false;
    }
    const doc = dom.ownerDocument;
    return getShadowRoot(SugarElement.fromDom(dom)).fold(() => doc.body.contains(dom), compose1(inBody, getShadowHost));
  };
  const children$2 = (scope, predicate) => filter(children$3(scope), predicate);
  const descendants$1 = (scope, predicate) => {
    let result = [];
    each(children$3(scope), (x) => {
      if (predicate(x)) {
        result = result.concat([x]);
      }
      result = result.concat(descendants$1(x, predicate));
    });
    return result;
  };
  const children$1 = (scope, selector) => children$2(scope, (e) => is$2(e, selector));
  const descendants = (scope, selector) => all$1(selector, scope);
  var ClosestOrAncestor = (is2, ancestor2, scope, a, isRoot) => {
    if (is2(scope, a)) {
      return Optional.some(scope);
    } else if (isFunction(isRoot) && isRoot(scope)) {
      return Optional.none();
    } else {
      return ancestor2(scope, a, isRoot);
    }
  };
  const ancestor$1 = (scope, predicate, isRoot) => {
    let element = scope.dom;
    const stop = isFunction(isRoot) ? isRoot : never;
    while (element.parentNode) {
      element = element.parentNode;
      const el = SugarElement.fromDom(element);
      if (predicate(el)) {
        return Optional.some(el);
      } else if (stop(el)) {
        break;
      }
    }
    return Optional.none();
  };
  const child$2 = (scope, predicate) => {
    const pred = (node) => predicate(SugarElement.fromDom(node));
    const result = find(scope.dom.childNodes, pred);
    return result.map(SugarElement.fromDom);
  };
  const ancestor = (scope, selector, isRoot) => ancestor$1(scope, (e) => is$2(e, selector), isRoot);
  const child$1 = (scope, selector) => child$2(scope, (e) => is$2(e, selector));
  const descendant = (scope, selector) => one(selector, scope);
  const closest = (scope, selector, isRoot) => {
    const is2 = (element, selector2) => is$2(element, selector2);
    return ClosestOrAncestor(is2, ancestor, scope, selector, isRoot);
  };
  const rawSet = (dom, key2, value) => {
    if (isString(value) || isBoolean(value) || isNumber(value)) {
      dom.setAttribute(key2, value + "");
    } else {
      console.error("Invalid call to Attribute.set. Key ", key2, ":: Value ", value, ":: Element ", dom);
      throw new Error("Attribute value was not simple");
    }
  };
  const set$2 = (element, key2, value) => {
    rawSet(element.dom, key2, value);
  };
  const setAll = (element, attrs) => {
    const dom = element.dom;
    each$1(attrs, (v, k) => {
      rawSet(dom, k, v);
    });
  };
  const get$2 = (element, key2) => {
    const v = element.dom.getAttribute(key2);
    return v === null ? void 0 : v;
  };
  const getOpt = (element, key2) => Optional.from(get$2(element, key2));
  const remove$2 = (element, key2) => {
    element.dom.removeAttribute(key2);
  };
  const clone = (element) => foldl(element.dom.attributes, (acc, attr) => {
    acc[attr.name] = attr.value;
    return acc;
  }, {});
  const is = (lhs, rhs, comparator = tripleEquals) => lhs.exists((left) => comparator(left, rhs));
  const cat = (arr) => {
    const r = [];
    const push = (x) => {
      r.push(x);
    };
    for (let i2 = 0; i2 < arr.length; i2++) {
      arr[i2].each(push);
    }
    return r;
  };
  const lift2 = (oa, ob, f) => oa.isSome() && ob.isSome() ? Optional.some(f(oa.getOrDie(), ob.getOrDie())) : Optional.none();
  const flatten = (oot) => oot.bind(identity);
  const someIf = (b, a) => b ? Optional.some(a) : Optional.none();
  const removeFromStart = (str, numChars) => {
    return str.substring(numChars);
  };
  const checkRange = (str, substr, start) => substr === "" || str.length >= substr.length && str.substr(start, start + substr.length) === substr;
  const removeLeading = (str, prefix) => {
    return startsWith(str, prefix) ? removeFromStart(str, prefix.length) : str;
  };
  const startsWith = (str, prefix) => {
    return checkRange(str, prefix, 0);
  };
  const blank = (r) => (s) => s.replace(r, "");
  const trim = blank(/^\s+|\s+$/g);
  const isNotEmpty = (s) => s.length > 0;
  const isEmpty = (s) => !isNotEmpty(s);
  const toInt = (value, radix = 10) => {
    const num = parseInt(value, radix);
    return isNaN(num) ? Optional.none() : Optional.some(num);
  };
  const toFloat = (value) => {
    const num = parseFloat(value);
    return isNaN(num) ? Optional.none() : Optional.some(num);
  };
  const isSupported = (dom) => dom.style !== void 0 && isFunction(dom.style.getPropertyValue);
  const internalSet = (dom, property, value) => {
    if (!isString(value)) {
      console.error("Invalid call to CSS.set. Property ", property, ":: Value ", value, ":: Element ", dom);
      throw new Error("CSS value must be a string: " + value);
    }
    if (isSupported(dom)) {
      dom.style.setProperty(property, value);
    }
  };
  const internalRemove = (dom, property) => {
    if (isSupported(dom)) {
      dom.style.removeProperty(property);
    }
  };
  const set$1 = (element, property, value) => {
    const dom = element.dom;
    internalSet(dom, property, value);
  };
  const get$1 = (element, property) => {
    const dom = element.dom;
    const styles = window.getComputedStyle(dom);
    const r = styles.getPropertyValue(property);
    return r === "" && !inBody(element) ? getUnsafeProperty(dom, property) : r;
  };
  const getUnsafeProperty = (dom, property) => isSupported(dom) ? dom.style.getPropertyValue(property) : "";
  const getRaw = (element, property) => {
    const dom = element.dom;
    const raw = getUnsafeProperty(dom, property);
    return Optional.from(raw).filter((r) => r.length > 0);
  };
  const remove$1 = (element, property) => {
    const dom = element.dom;
    internalRemove(dom, property);
    if (is(getOpt(element, "style").map(trim), "")) {
      remove$2(element, "style");
    }
  };
  const getAttrValue = (cell2, name2, fallback = 0) => getOpt(cell2, name2).map((value) => parseInt(value, 10)).getOr(fallback);
  const firstLayer = (scope, selector) => {
    return filterFirstLayer(scope, selector, always);
  };
  const filterFirstLayer = (scope, selector, predicate) => {
    return bind(children$3(scope), (x) => {
      if (is$2(x, selector)) {
        return predicate(x) ? [x] : [];
      } else {
        return filterFirstLayer(x, selector, predicate);
      }
    });
  };
  const validSectionList = [
    "tfoot",
    "thead",
    "tbody",
    "colgroup"
  ];
  const isValidSection = (parentName) => contains(validSectionList, parentName);
  const grid = (rows2, columns2) => ({
    rows: rows2,
    columns: columns2
  });
  const detail = (element, rowspan, colspan) => ({
    element,
    rowspan,
    colspan
  });
  const extended = (element, rowspan, colspan, row, column, isLocked) => ({
    element,
    rowspan,
    colspan,
    row,
    column,
    isLocked
  });
  const rowdetail = (element, cells2, section) => ({
    element,
    cells: cells2,
    section
  });
  const bounds = (startRow, startCol, finishRow, finishCol) => ({
    startRow,
    startCol,
    finishRow,
    finishCol
  });
  const columnext = (element, colspan, column) => ({
    element,
    colspan,
    column
  });
  const colgroup = (element, columns2) => ({
    element,
    columns: columns2
  });
  const lookup = (tags, element, isRoot = never) => {
    if (isRoot(element)) {
      return Optional.none();
    }
    if (contains(tags, name(element))) {
      return Optional.some(element);
    }
    const isRootOrUpperTable = (elm) => is$2(elm, "table") || isRoot(elm);
    return ancestor(element, tags.join(","), isRootOrUpperTable);
  };
  const cell = (element, isRoot) => lookup([
    "td",
    "th"
  ], element, isRoot);
  const cells = (ancestor2) => firstLayer(ancestor2, "th,td");
  const columns = (ancestor2) => {
    if (is$2(ancestor2, "colgroup")) {
      return children$1(ancestor2, "col");
    } else {
      return bind(columnGroups(ancestor2), (columnGroup) => children$1(columnGroup, "col"));
    }
  };
  const table = (element, isRoot) => closest(element, "table", isRoot);
  const rows = (ancestor2) => firstLayer(ancestor2, "tr");
  const columnGroups = (ancestor2) => table(ancestor2).fold(constant([]), (table2) => children$1(table2, "colgroup"));
  const fromRowsOrColGroups = (elems, getSection) => map(elems, (row) => {
    if (name(row) === "colgroup") {
      const cells2 = map(columns(row), (column) => {
        const colspan = getAttrValue(column, "span", 1);
        return detail(column, 1, colspan);
      });
      return rowdetail(row, cells2, "colgroup");
    } else {
      const cells$1 = map(cells(row), (cell2) => {
        const rowspan = getAttrValue(cell2, "rowspan", 1);
        const colspan = getAttrValue(cell2, "colspan", 1);
        return detail(cell2, rowspan, colspan);
      });
      return rowdetail(row, cells$1, getSection(row));
    }
  });
  const getParentSection = (group) => parent(group).map((parent2) => {
    const parentName = name(parent2);
    return isValidSection(parentName) ? parentName : "tbody";
  }).getOr("tbody");
  const fromTable$1 = (table2) => {
    const rows$1 = rows(table2);
    const columnGroups$1 = columnGroups(table2);
    const elems = [
      ...columnGroups$1,
      ...rows$1
    ];
    return fromRowsOrColGroups(elems, getParentSection);
  };
  const LOCKED_COL_ATTR = "data-snooker-locked-cols";
  const getLockedColumnsFromTable = (table2) => getOpt(table2, LOCKED_COL_ATTR).bind((lockedColStr) => Optional.from(lockedColStr.match(/\d+/g))).map((lockedCols) => mapToObject(lockedCols, always));
  const key = (row, column) => {
    return row + "," + column;
  };
  const getAt = (warehouse, row, column) => Optional.from(warehouse.access[key(row, column)]);
  const findItem = (warehouse, item, comparator) => {
    const filtered = filterItems(warehouse, (detail2) => {
      return comparator(item, detail2.element);
    });
    return filtered.length > 0 ? Optional.some(filtered[0]) : Optional.none();
  };
  const filterItems = (warehouse, predicate) => {
    const all2 = bind(warehouse.all, (r) => {
      return r.cells;
    });
    return filter(all2, predicate);
  };
  const generateColumns = (rowData) => {
    const columnsGroup = {};
    let index = 0;
    each(rowData.cells, (column) => {
      const colspan = column.colspan;
      range(colspan, (columnIndex) => {
        const colIndex = index + columnIndex;
        columnsGroup[colIndex] = columnext(column.element, colspan, colIndex);
      });
      index += colspan;
    });
    return columnsGroup;
  };
  const generate$1 = (list) => {
    const access = {};
    const cells2 = [];
    const tableOpt = head(list).map((rowData) => rowData.element).bind(table);
    const lockedColumns = tableOpt.bind(getLockedColumnsFromTable).getOr({});
    let maxRows = 0;
    let maxColumns = 0;
    let rowCount = 0;
    const {
      pass: colgroupRows,
      fail: rows2
    } = partition(list, (rowData) => rowData.section === "colgroup");
    each(rows2, (rowData) => {
      const currentRow = [];
      each(rowData.cells, (rowCell) => {
        let start = 0;
        while (access[key(rowCount, start)] !== void 0) {
          start++;
        }
        const isLocked = hasNonNullableKey(lockedColumns, start.toString());
        const current = extended(rowCell.element, rowCell.rowspan, rowCell.colspan, rowCount, start, isLocked);
        for (let occupiedColumnPosition = 0; occupiedColumnPosition < rowCell.colspan; occupiedColumnPosition++) {
          for (let occupiedRowPosition = 0; occupiedRowPosition < rowCell.rowspan; occupiedRowPosition++) {
            const rowPosition = rowCount + occupiedRowPosition;
            const columnPosition = start + occupiedColumnPosition;
            const newpos = key(rowPosition, columnPosition);
            access[newpos] = current;
            maxColumns = Math.max(maxColumns, columnPosition + 1);
          }
        }
        currentRow.push(current);
      });
      maxRows++;
      cells2.push(rowdetail(rowData.element, currentRow, rowData.section));
      rowCount++;
    });
    const { columns: columns2, colgroups } = last(colgroupRows).map((rowData) => {
      const columns3 = generateColumns(rowData);
      const colgroup$1 = colgroup(rowData.element, values(columns3));
      return {
        colgroups: [colgroup$1],
        columns: columns3
      };
    }).getOrThunk(() => ({
      colgroups: [],
      columns: {}
    }));
    const grid$1 = grid(maxRows, maxColumns);
    return {
      grid: grid$1,
      access,
      all: cells2,
      columns: columns2,
      colgroups
    };
  };
  const fromTable = (table2) => {
    const list = fromTable$1(table2);
    return generate$1(list);
  };
  const justCells = (warehouse) => bind(warehouse.all, (w) => w.cells);
  const justColumns = (warehouse) => values(warehouse.columns);
  const hasColumns = (warehouse) => keys(warehouse.columns).length > 0;
  const getColumnAt = (warehouse, columnIndex) => Optional.from(warehouse.columns[columnIndex]);
  const Warehouse = {
    fromTable,
    generate: generate$1,
    getAt,
    findItem,
    filterItems,
    justCells,
    justColumns,
    hasColumns,
    getColumnAt
  };
  var global$2 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const getTDTHOverallStyle = (dom, elm, name2) => {
    const cells2 = dom.select("td,th", elm);
    let firstChildStyle;
    for (let i2 = 0; i2 < cells2.length; i2++) {
      const currentStyle = dom.getStyle(cells2[i2], name2);
      if (isUndefined(firstChildStyle)) {
        firstChildStyle = currentStyle;
      }
      if (firstChildStyle !== currentStyle) {
        return "";
      }
    }
    return firstChildStyle;
  };
  const setAlign = (editor, elm, name2) => {
    global$2.each("left center right".split(" "), (align) => {
      if (align !== name2) {
        editor.formatter.remove("align" + align, {}, elm);
      }
    });
    if (name2) {
      editor.formatter.apply("align" + name2, {}, elm);
    }
  };
  const setVAlign = (editor, elm, name2) => {
    global$2.each("top middle bottom".split(" "), (align) => {
      if (align !== name2) {
        editor.formatter.remove("valign" + align, {}, elm);
      }
    });
    if (name2) {
      editor.formatter.apply("valign" + name2, {}, elm);
    }
  };
  const fireTableModified = (editor, table2, data) => {
    editor.dispatch("TableModified", {
      ...data,
      table: table2
    });
  };
  const toNumber = (px, fallback) => toFloat(px).getOr(fallback);
  const getProp = (element, name2, fallback) => toNumber(get$1(element, name2), fallback);
  const calcContentBoxSize = (element, size2, upper, lower) => {
    const paddingUpper = getProp(element, `padding-${upper}`, 0);
    const paddingLower = getProp(element, `padding-${lower}`, 0);
    const borderUpper = getProp(element, `border-${upper}-width`, 0);
    const borderLower = getProp(element, `border-${lower}-width`, 0);
    return size2 - paddingUpper - paddingLower - borderUpper - borderLower;
  };
  const getCalculatedWidth = (element, boxSizing) => {
    const dom = element.dom;
    const width = dom.getBoundingClientRect().width || dom.offsetWidth;
    return boxSizing === "border-box" ? width : calcContentBoxSize(element, width, "left", "right");
  };
  const getInnerWidth = (element) => getCalculatedWidth(element, "content-box");
  const getInner = getInnerWidth;
  var global$1 = tinymce.util.Tools.resolve("tinymce.Env");
  const defaultTableToolbar = "tableprops tabledelete | tableinsertrowbefore tableinsertrowafter tabledeleterow | tableinsertcolbefore tableinsertcolafter tabledeletecol";
  const defaultCellBorderWidths = range(5, (i2) => {
    const size2 = `${i2 + 1}px`;
    return {
      title: size2,
      value: size2
    };
  });
  const defaultCellBorderStyles = map([
    "Solid",
    "Dotted",
    "Dashed",
    "Double",
    "Groove",
    "Ridge",
    "Inset",
    "Outset",
    "None",
    "Hidden"
  ], (type2) => {
    return {
      title: type2,
      value: type2.toLowerCase()
    };
  });
  const defaultWidth = "100%";
  const getPixelForcedWidth = (editor) => {
    var _a;
    const dom = editor.dom;
    const parentBlock = (_a = dom.getParent(editor.selection.getStart(), dom.isBlock)) !== null && _a !== void 0 ? _a : editor.getBody();
    return getInner(SugarElement.fromDom(parentBlock)) + "px";
  };
  const determineDefaultStyles = (editor, defaultStyles) => {
    if (isResponsiveForced(editor) || !shouldStyleWithCss(editor)) {
      return defaultStyles;
    } else if (isPixelsForced(editor)) {
      return {
        ...defaultStyles,
        width: getPixelForcedWidth(editor)
      };
    } else {
      return {
        ...defaultStyles,
        width: defaultWidth
      };
    }
  };
  const determineDefaultAttributes = (editor, defaultAttributes) => {
    if (isResponsiveForced(editor) || shouldStyleWithCss(editor)) {
      return defaultAttributes;
    } else if (isPixelsForced(editor)) {
      return {
        ...defaultAttributes,
        width: getPixelForcedWidth(editor)
      };
    } else {
      return {
        ...defaultAttributes,
        width: defaultWidth
      };
    }
  };
  const option = (name2) => (editor) => editor.options.get(name2);
  const register = (editor) => {
    const registerOption = editor.options.register;
    registerOption("table_border_widths", {
      processor: "object[]",
      default: defaultCellBorderWidths
    });
    registerOption("table_border_styles", {
      processor: "object[]",
      default: defaultCellBorderStyles
    });
    registerOption("table_cell_advtab", {
      processor: "boolean",
      default: true
    });
    registerOption("table_row_advtab", {
      processor: "boolean",
      default: true
    });
    registerOption("table_advtab", {
      processor: "boolean",
      default: true
    });
    registerOption("table_appearance_options", {
      processor: "boolean",
      default: true
    });
    registerOption("table_grid", {
      processor: "boolean",
      default: !global$1.deviceType.isTouch()
    });
    registerOption("table_cell_class_list", {
      processor: "object[]",
      default: []
    });
    registerOption("table_row_class_list", {
      processor: "object[]",
      default: []
    });
    registerOption("table_class_list", {
      processor: "object[]",
      default: []
    });
    registerOption("table_toolbar", {
      processor: "string",
      default: defaultTableToolbar
    });
    registerOption("table_background_color_map", {
      processor: "object[]",
      default: []
    });
    registerOption("table_border_color_map", {
      processor: "object[]",
      default: []
    });
  };
  const getTableSizingMode = option("table_sizing_mode");
  const getTableBorderWidths = option("table_border_widths");
  const getTableBorderStyles = option("table_border_styles");
  const hasAdvancedCellTab = option("table_cell_advtab");
  const hasAdvancedRowTab = option("table_row_advtab");
  const hasAdvancedTableTab = option("table_advtab");
  const hasAppearanceOptions = option("table_appearance_options");
  const hasTableGrid = option("table_grid");
  const shouldStyleWithCss = option("table_style_by_css");
  const getCellClassList = option("table_cell_class_list");
  const getRowClassList = option("table_row_class_list");
  const getTableClassList = option("table_class_list");
  const getToolbar = option("table_toolbar");
  const getTableBackgroundColorMap = option("table_background_color_map");
  const getTableBorderColorMap = option("table_border_color_map");
  const isPixelsForced = (editor) => getTableSizingMode(editor) === "fixed";
  const isResponsiveForced = (editor) => getTableSizingMode(editor) === "responsive";
  const getDefaultStyles = (editor) => {
    const options = editor.options;
    const defaultStyles = options.get("table_default_styles");
    return options.isSet("table_default_styles") ? defaultStyles : determineDefaultStyles(editor, defaultStyles);
  };
  const getDefaultAttributes = (editor) => {
    const options = editor.options;
    const defaultAttributes = options.get("table_default_attributes");
    return options.isSet("table_default_attributes") ? defaultAttributes : determineDefaultAttributes(editor, defaultAttributes);
  };
  const getNodeName = (elm) => elm.nodeName.toLowerCase();
  const getBody = (editor) => SugarElement.fromDom(editor.getBody());
  const getIsRoot = (editor) => (element) => eq(element, getBody(editor));
  const removePxSuffix = (size2) => size2 ? size2.replace(/px$/, "") : "";
  const addPxSuffix = (size2) => /^\d+(\.\d+)?$/.test(size2) ? size2 + "px" : size2;
  const getSelectionStart = (editor) => SugarElement.fromDom(editor.selection.getStart());
  const getSelectionEnd = (editor) => SugarElement.fromDom(editor.selection.getEnd());
  const isWithin = (bounds2, detail2) => {
    return detail2.column >= bounds2.startCol && detail2.column + detail2.colspan - 1 <= bounds2.finishCol && detail2.row >= bounds2.startRow && detail2.row + detail2.rowspan - 1 <= bounds2.finishRow;
  };
  const isRectangular = (warehouse, bounds2) => {
    let isRect = true;
    const detailIsWithin = curry(isWithin, bounds2);
    for (let i2 = bounds2.startRow; i2 <= bounds2.finishRow; i2++) {
      for (let j = bounds2.startCol; j <= bounds2.finishCol; j++) {
        isRect = isRect && Warehouse.getAt(warehouse, i2, j).exists(detailIsWithin);
      }
    }
    return isRect ? Optional.some(bounds2) : Optional.none();
  };
  const getBounds = (detailA, detailB) => {
    return bounds(Math.min(detailA.row, detailB.row), Math.min(detailA.column, detailB.column), Math.max(detailA.row + detailA.rowspan - 1, detailB.row + detailB.rowspan - 1), Math.max(detailA.column + detailA.colspan - 1, detailB.column + detailB.colspan - 1));
  };
  const getAnyBox = (warehouse, startCell, finishCell) => {
    const startCoords = Warehouse.findItem(warehouse, startCell, eq);
    const finishCoords = Warehouse.findItem(warehouse, finishCell, eq);
    return startCoords.bind((sc) => {
      return finishCoords.map((fc) => {
        return getBounds(sc, fc);
      });
    });
  };
  const getBox$1 = (warehouse, startCell, finishCell) => {
    return getAnyBox(warehouse, startCell, finishCell).bind((bounds2) => {
      return isRectangular(warehouse, bounds2);
    });
  };
  const getBox = (table2, first, last2) => {
    const warehouse = getWarehouse(table2);
    return getBox$1(warehouse, first, last2);
  };
  const getWarehouse = Warehouse.fromTable;
  const before = (marker, element) => {
    const parent$1 = parent(marker);
    parent$1.each((v) => {
      v.dom.insertBefore(element.dom, marker.dom);
    });
  };
  const after$1 = (marker, element) => {
    const sibling = nextSibling(marker);
    sibling.fold(() => {
      const parent$1 = parent(marker);
      parent$1.each((v) => {
        append$1(v, element);
      });
    }, (v) => {
      before(v, element);
    });
  };
  const prepend = (parent2, element) => {
    const firstChild$1 = firstChild(parent2);
    firstChild$1.fold(() => {
      append$1(parent2, element);
    }, (v) => {
      parent2.dom.insertBefore(element.dom, v.dom);
    });
  };
  const append$1 = (parent2, element) => {
    parent2.dom.appendChild(element.dom);
  };
  const wrap = (element, wrapper) => {
    before(element, wrapper);
    append$1(wrapper, element);
  };
  const after = (marker, elements) => {
    each(elements, (x, i2) => {
      const e = i2 === 0 ? marker : elements[i2 - 1];
      after$1(e, x);
    });
  };
  const append = (parent2, elements) => {
    each(elements, (x) => {
      append$1(parent2, x);
    });
  };
  const remove = (element) => {
    const dom = element.dom;
    if (dom.parentNode !== null) {
      dom.parentNode.removeChild(dom);
    }
  };
  const unwrap = (wrapper) => {
    const children2 = children$3(wrapper);
    if (children2.length > 0) {
      after(wrapper, children2);
    }
    remove(wrapper);
  };
  const NodeValue = (is2, name2) => {
    const get2 = (element) => {
      if (!is2(element)) {
        throw new Error("Can only get " + name2 + " value of a " + name2 + " node");
      }
      return getOption(element).getOr("");
    };
    const getOption = (element) => is2(element) ? Optional.from(element.dom.nodeValue) : Optional.none();
    const set2 = (element, value) => {
      if (!is2(element)) {
        throw new Error("Can only set raw " + name2 + " value of a " + name2 + " node");
      }
      element.dom.nodeValue = value;
    };
    return {
      get: get2,
      getOption,
      set: set2
    };
  };
  const api = NodeValue(isText, "text");
  const get = (element) => api.get(element);
  const set = (element, value) => api.set(element, value);
  var TagBoundaries = [
    "body",
    "p",
    "div",
    "article",
    "aside",
    "figcaption",
    "figure",
    "footer",
    "header",
    "nav",
    "section",
    "ol",
    "ul",
    "li",
    "table",
    "thead",
    "tbody",
    "tfoot",
    "caption",
    "tr",
    "td",
    "th",
    "h1",
    "h2",
    "h3",
    "h4",
    "h5",
    "h6",
    "blockquote",
    "pre",
    "address"
  ];
  var DomUniverse = () => {
    const clone$1 = (element) => {
      return SugarElement.fromDom(element.dom.cloneNode(false));
    };
    const document2 = (element) => documentOrOwner(element).dom;
    const isBoundary = (element) => {
      if (!isElement(element)) {
        return false;
      }
      if (name(element) === "body") {
        return true;
      }
      return contains(TagBoundaries, name(element));
    };
    const isEmptyTag = (element) => {
      if (!isElement(element)) {
        return false;
      }
      return contains([
        "br",
        "img",
        "hr",
        "input"
      ], name(element));
    };
    const isNonEditable = (element) => isElement(element) && get$2(element, "contenteditable") === "false";
    const comparePosition = (element, other) => {
      return element.dom.compareDocumentPosition(other.dom);
    };
    const copyAttributesTo = (source, destination) => {
      const as = clone(source);
      setAll(destination, as);
    };
    const isSpecial = (element) => {
      const tag = name(element);
      return contains([
        "script",
        "noscript",
        "iframe",
        "noframes",
        "noembed",
        "title",
        "style",
        "textarea",
        "xmp"
      ], tag);
    };
    const getLanguage = (element) => isElement(element) ? getOpt(element, "lang") : Optional.none();
    return {
      up: constant({
        selector: ancestor,
        closest,
        predicate: ancestor$1,
        all: parents
      }),
      down: constant({
        selector: descendants,
        predicate: descendants$1
      }),
      styles: constant({
        get: get$1,
        getRaw,
        set: set$1,
        remove: remove$1
      }),
      attrs: constant({
        get: get$2,
        set: set$2,
        remove: remove$2,
        copyTo: copyAttributesTo
      }),
      insert: constant({
        before,
        after: after$1,
        afterAll: after,
        append: append$1,
        appendAll: append,
        prepend,
        wrap
      }),
      remove: constant({
        unwrap,
        remove
      }),
      create: constant({
        nu: SugarElement.fromTag,
        clone: clone$1,
        text: SugarElement.fromText
      }),
      query: constant({
        comparePosition,
        prevSibling,
        nextSibling
      }),
      property: constant({
        children: children$3,
        name,
        parent,
        document: document2,
        isText,
        isComment,
        isElement,
        isSpecial,
        getLanguage,
        getText: get,
        setText: set,
        isBoundary,
        isEmptyTag,
        isNonEditable
      }),
      eq,
      is: is$1
    };
  };
  const all = (universe2, look, elements, f) => {
    const head2 = elements[0];
    const tail = elements.slice(1);
    return f(universe2, look, head2, tail);
  };
  const oneAll = (universe2, look, elements) => {
    return elements.length > 0 ? all(universe2, look, elements, unsafeOne) : Optional.none();
  };
  const unsafeOne = (universe2, look, head2, tail) => {
    const start = look(universe2, head2);
    return foldr(tail, (b, a) => {
      const current = look(universe2, a);
      return commonElement(universe2, b, current);
    }, start);
  };
  const commonElement = (universe2, start, end) => {
    return start.bind((s) => {
      return end.filter(curry(universe2.eq, s));
    });
  };
  const sharedOne$1 = oneAll;
  const universe = DomUniverse();
  const sharedOne = (look, elements) => {
    return sharedOne$1(universe, (_universe, element) => {
      return look(element);
    }, elements);
  };
  const lookupTable = (container) => {
    return ancestor(container, "table");
  };
  const retrieve$1 = (container, selector) => {
    const sels = descendants(container, selector);
    return sels.length > 0 ? Optional.some(sels) : Optional.none();
  };
  const getEdges = (container, firstSelectedSelector, lastSelectedSelector) => {
    return descendant(container, firstSelectedSelector).bind((first) => {
      return descendant(container, lastSelectedSelector).bind((last2) => {
        return sharedOne(lookupTable, [
          first,
          last2
        ]).map((table2) => {
          return {
            first,
            last: last2,
            table: table2
          };
        });
      });
    });
  };
  const retrieve = (container, selector) => {
    return retrieve$1(container, selector);
  };
  const retrieveBox = (container, firstSelectedSelector, lastSelectedSelector) => {
    return getEdges(container, firstSelectedSelector, lastSelectedSelector).bind((edges) => {
      const isRoot = (ancestor2) => {
        return eq(container, ancestor2);
      };
      const sectionSelector = "thead,tfoot,tbody,table";
      const firstAncestor = ancestor(edges.first, sectionSelector, isRoot);
      const lastAncestor = ancestor(edges.last, sectionSelector, isRoot);
      return firstAncestor.bind((fA) => {
        return lastAncestor.bind((lA) => {
          return eq(fA, lA) ? getBox(edges.table, edges.first, edges.last) : Optional.none();
        });
      });
    });
  };
  const fromDom = (nodes) => map(nodes, SugarElement.fromDom);
  const strSelected = "data-mce-selected";
  const strSelectedSelector = "td[" + strSelected + "],th[" + strSelected + "]";
  const strFirstSelected = "data-mce-first-selected";
  const strFirstSelectedSelector = "td[" + strFirstSelected + "],th[" + strFirstSelected + "]";
  const strLastSelected = "data-mce-last-selected";
  const strLastSelectedSelector = "td[" + strLastSelected + "],th[" + strLastSelected + "]";
  const ephemera = {
    selected: strSelected,
    selectedSelector: strSelectedSelector,
    firstSelected: strFirstSelected,
    firstSelectedSelector: strFirstSelectedSelector,
    lastSelected: strLastSelected,
    lastSelectedSelector: strLastSelectedSelector
  };
  const getSelectionCellFallback = (element) => table(element).bind((table2) => retrieve(table2, ephemera.firstSelectedSelector)).fold(constant(element), (cells2) => cells2[0]);
  const getSelectionFromSelector = (selector) => (initCell, isRoot) => {
    const cellName = name(initCell);
    const cell2 = cellName === "col" || cellName === "colgroup" ? getSelectionCellFallback(initCell) : initCell;
    return closest(cell2, selector, isRoot);
  };
  const getSelectionCellOrCaption = getSelectionFromSelector("th,td,caption");
  const getSelectionCell = getSelectionFromSelector("th,td");
  const getCellsFromSelection = (editor) => fromDom(editor.model.table.getSelectedCells());
  const getRowsFromSelection = (selected, selector) => {
    const cellOpt = getSelectionCell(selected);
    const rowsOpt = cellOpt.bind((cell2) => table(cell2)).map((table2) => rows(table2));
    return lift2(cellOpt, rowsOpt, (cell2, rows2) => filter(rows2, (row) => exists(fromDom(row.dom.cells), (rowCell) => get$2(rowCell, selector) === "1" || eq(rowCell, cell2)))).getOr([]);
  };
  const verticalAlignValues = [
    {
      text: "None",
      value: ""
    },
    {
      text: "Top",
      value: "top"
    },
    {
      text: "Middle",
      value: "middle"
    },
    {
      text: "Bottom",
      value: "bottom"
    }
  ];
  const hexColour = (value) => ({ value });
  const shorthandRegex = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
  const longformRegex = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i;
  const isHexString = (hex) => shorthandRegex.test(hex) || longformRegex.test(hex);
  const normalizeHex = (hex) => removeLeading(hex, "#").toUpperCase();
  const fromString$1 = (hex) => isHexString(hex) ? Optional.some({ value: normalizeHex(hex) }) : Optional.none();
  const toHex = (component) => {
    const hex = component.toString(16);
    return (hex.length === 1 ? "0" + hex : hex).toUpperCase();
  };
  const fromRgba = (rgbaColour2) => {
    const value = toHex(rgbaColour2.red) + toHex(rgbaColour2.green) + toHex(rgbaColour2.blue);
    return hexColour(value);
  };
  const rgbRegex = /^\s*rgb\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*\)\s*$/i;
  const rgbaRegex = /^\s*rgba\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d?(?:\.\d+)?)\s*\)\s*$/i;
  const rgbaColour = (red, green, blue, alpha) => ({
    red,
    green,
    blue,
    alpha
  });
  const fromStringValues = (red, green, blue, alpha) => {
    const r = parseInt(red, 10);
    const g = parseInt(green, 10);
    const b = parseInt(blue, 10);
    const a = parseFloat(alpha);
    return rgbaColour(r, g, b, a);
  };
  const fromString = (rgbaString) => {
    if (rgbaString === "transparent") {
      return Optional.some(rgbaColour(0, 0, 0, 0));
    }
    const rgbMatch = rgbRegex.exec(rgbaString);
    if (rgbMatch !== null) {
      return Optional.some(fromStringValues(rgbMatch[1], rgbMatch[2], rgbMatch[3], "1"));
    }
    const rgbaMatch = rgbaRegex.exec(rgbaString);
    if (rgbaMatch !== null) {
      return Optional.some(fromStringValues(rgbaMatch[1], rgbaMatch[2], rgbaMatch[3], rgbaMatch[4]));
    }
    return Optional.none();
  };
  const anyToHex = (color) => fromString$1(color).orThunk(() => fromString(color).map(fromRgba)).getOrThunk(() => {
    const canvas = document.createElement("canvas");
    canvas.height = 1;
    canvas.width = 1;
    const canvasContext = canvas.getContext("2d");
    canvasContext.clearRect(0, 0, canvas.width, canvas.height);
    canvasContext.fillStyle = "#FFFFFF";
    canvasContext.fillStyle = color;
    canvasContext.fillRect(0, 0, 1, 1);
    const rgba = canvasContext.getImageData(0, 0, 1, 1).data;
    const r = rgba[0];
    const g = rgba[1];
    const b = rgba[2];
    const a = rgba[3];
    return fromRgba(rgbaColour(r, g, b, a));
  });
  const rgbaToHexString = (color) => fromString(color).map(fromRgba).map((h) => "#" + h.value).getOr(color);
  const Cell = (initial) => {
    let value = initial;
    const get2 = () => {
      return value;
    };
    const set2 = (v) => {
      value = v;
    };
    return {
      get: get2,
      set: set2
    };
  };
  const singleton = (doRevoke) => {
    const subject = Cell(Optional.none());
    const revoke = () => subject.get().each(doRevoke);
    const clear = () => {
      revoke();
      subject.set(Optional.none());
    };
    const isSet = () => subject.get().isSome();
    const get2 = () => subject.get();
    const set2 = (s) => {
      revoke();
      subject.set(Optional.some(s));
    };
    return {
      clear,
      isSet,
      get: get2,
      set: set2
    };
  };
  const unbindable = () => singleton((s) => s.unbind());
  const onSetupToggle = (editor, formatName, formatValue) => {
    return (api2) => {
      const boundCallback = unbindable();
      const isNone = isEmpty(formatValue);
      const init = () => {
        const selectedCells = getCellsFromSelection(editor);
        const checkNode = (cell2) => editor.formatter.match(formatName, { value: formatValue }, cell2.dom, isNone);
        if (isNone) {
          api2.setActive(!exists(selectedCells, checkNode));
          boundCallback.set(editor.formatter.formatChanged(formatName, (match) => api2.setActive(!match), true));
        } else {
          api2.setActive(forall(selectedCells, checkNode));
          boundCallback.set(editor.formatter.formatChanged(formatName, api2.setActive, false, { value: formatValue }));
        }
      };
      editor.initialized ? init() : editor.on("init", init);
      return boundCallback.clear;
    };
  };
  const isListGroup = (item) => hasNonNullableKey(item, "menu");
  const buildListItems = (items) => map(items, (item) => {
    const text = item.text || item.title || "";
    if (isListGroup(item)) {
      return {
        text,
        items: buildListItems(item.menu)
      };
    } else {
      return {
        text,
        value: item.value
      };
    }
  });
  const buildMenuItems = (editor, items, format, onAction) => map(items, (item) => {
    const text = item.text || item.title;
    if (isListGroup(item)) {
      return {
        type: "nestedmenuitem",
        text,
        getSubmenuItems: () => buildMenuItems(editor, item.menu, format, onAction)
      };
    } else {
      return {
        text,
        type: "togglemenuitem",
        onAction: () => onAction(item.value),
        onSetup: onSetupToggle(editor, format, item.value)
      };
    }
  });
  const applyTableCellStyle = (editor, style) => (value) => {
    editor.execCommand("mceTableApplyCellStyle", false, { [style]: value });
  };
  const filterNoneItem = (list) => bind(list, (item) => {
    if (isListGroup(item)) {
      return [{
        ...item,
        menu: filterNoneItem(item.menu)
      }];
    } else {
      return isNotEmpty(item.value) ? [item] : [];
    }
  });
  const generateMenuItemsCallback = (editor, items, format, onAction) => (callback) => callback(buildMenuItems(editor, items, format, onAction));
  const buildColorMenu = (editor, colorList, style) => {
    const colorMap = map(colorList, (entry) => ({
      text: entry.title,
      value: "#" + anyToHex(entry.value).value,
      type: "choiceitem"
    }));
    return [{
      type: "fancymenuitem",
      fancytype: "colorswatch",
      initData: {
        colors: colorMap.length > 0 ? colorMap : void 0,
        allowCustomColors: false
      },
      onAction: (data) => {
        const value = data.value === "remove" ? "" : data.value;
        editor.execCommand("mceTableApplyCellStyle", false, { [style]: value });
      }
    }];
  };
  const changeRowHeader = (editor) => () => {
    const currentType = editor.queryCommandValue("mceTableRowType");
    const newType = currentType === "header" ? "body" : "header";
    editor.execCommand("mceTableRowType", false, { type: newType });
  };
  const changeColumnHeader = (editor) => () => {
    const currentType = editor.queryCommandValue("mceTableColType");
    const newType = currentType === "th" ? "td" : "th";
    editor.execCommand("mceTableColType", false, { type: newType });
  };
  const getClassList$1 = (editor) => {
    const classes = buildListItems(getCellClassList(editor));
    if (classes.length > 0) {
      return Optional.some({
        name: "class",
        type: "listbox",
        label: "Class",
        items: classes
      });
    }
    return Optional.none();
  };
  const children = [
    {
      name: "width",
      type: "input",
      label: "Width"
    },
    {
      name: "height",
      type: "input",
      label: "Height"
    },
    {
      name: "celltype",
      type: "listbox",
      label: "Cell type",
      items: [
        {
          text: "Cell",
          value: "td"
        },
        {
          text: "Header cell",
          value: "th"
        }
      ]
    },
    {
      name: "scope",
      type: "listbox",
      label: "Scope",
      items: [
        {
          text: "None",
          value: ""
        },
        {
          text: "Row",
          value: "row"
        },
        {
          text: "Column",
          value: "col"
        },
        {
          text: "Row group",
          value: "rowgroup"
        },
        {
          text: "Column group",
          value: "colgroup"
        }
      ]
    },
    {
      name: "halign",
      type: "listbox",
      label: "Horizontal align",
      items: [
        {
          text: "None",
          value: ""
        },
        {
          text: "Left",
          value: "left"
        },
        {
          text: "Center",
          value: "center"
        },
        {
          text: "Right",
          value: "right"
        }
      ]
    },
    {
      name: "valign",
      type: "listbox",
      label: "Vertical align",
      items: verticalAlignValues
    }
  ];
  const getItems$2 = (editor) => children.concat(getClassList$1(editor).toArray());
  const getAdvancedTab = (editor, dialogName) => {
    const emptyBorderStyle = [{
      text: "Select...",
      value: ""
    }];
    const advTabItems = [
      {
        name: "borderstyle",
        type: "listbox",
        label: "Border style",
        items: emptyBorderStyle.concat(buildListItems(getTableBorderStyles(editor)))
      },
      {
        name: "bordercolor",
        type: "colorinput",
        label: "Border color"
      },
      {
        name: "backgroundcolor",
        type: "colorinput",
        label: "Background color"
      }
    ];
    const borderWidth = {
      name: "borderwidth",
      type: "input",
      label: "Border width"
    };
    const items = dialogName === "cell" ? [borderWidth].concat(advTabItems) : advTabItems;
    return {
      title: "Advanced",
      name: "advanced",
      items
    };
  };
  const normal = (editor, element) => {
    const dom = editor.dom;
    const setAttrib = (attr, value) => {
      dom.setAttrib(element, attr, value);
    };
    const setStyle = (prop, value) => {
      dom.setStyle(element, prop, value);
    };
    const setFormat = (formatName, value) => {
      if (value === "") {
        editor.formatter.remove(formatName, { value: null }, element, true);
      } else {
        editor.formatter.apply(formatName, { value }, element);
      }
    };
    return {
      setAttrib,
      setStyle,
      setFormat
    };
  };
  const DomModifier = { normal };
  const isHeaderCell = isTag("th");
  const getRowHeaderType = (isHeaderRow, isHeaderCells) => {
    if (isHeaderRow && isHeaderCells) {
      return "sectionCells";
    } else if (isHeaderRow) {
      return "section";
    } else {
      return "cells";
    }
  };
  const getRowType$1 = (row) => {
    const isHeaderRow = row.section === "thead";
    const isHeaderCells = is(findCommonCellType(row.cells), "th");
    if (row.section === "tfoot") {
      return { type: "footer" };
    } else if (isHeaderRow || isHeaderCells) {
      return {
        type: "header",
        subType: getRowHeaderType(isHeaderRow, isHeaderCells)
      };
    } else {
      return { type: "body" };
    }
  };
  const findCommonCellType = (cells2) => {
    const headerCells = filter(cells2, (cell2) => isHeaderCell(cell2.element));
    if (headerCells.length === 0) {
      return Optional.some("td");
    } else if (headerCells.length === cells2.length) {
      return Optional.some("th");
    } else {
      return Optional.none();
    }
  };
  const findCommonRowType = (rows2) => {
    const rowTypes = map(rows2, (row) => getRowType$1(row).type);
    const hasHeader = contains(rowTypes, "header");
    const hasFooter = contains(rowTypes, "footer");
    if (!hasHeader && !hasFooter) {
      return Optional.some("body");
    } else {
      const hasBody = contains(rowTypes, "body");
      if (hasHeader && !hasBody && !hasFooter) {
        return Optional.some("header");
      } else if (!hasHeader && !hasBody && hasFooter) {
        return Optional.some("footer");
      } else {
        return Optional.none();
      }
    }
  };
  const cached = (f) => {
    let called = false;
    let r;
    return (...args) => {
      if (!called) {
        called = true;
        r = f.apply(null, args);
      }
      return r;
    };
  };
  const findInWarehouse = (warehouse, element) => findMap(warehouse.all, (r) => find(r.cells, (e) => eq(element, e.element)));
  const extractCells = (warehouse, target, predicate) => {
    const details = map(target.selection, (cell$1) => {
      return cell(cell$1).bind((lc) => findInWarehouse(warehouse, lc)).filter(predicate);
    });
    const cells2 = cat(details);
    return someIf(cells2.length > 0, cells2);
  };
  const onMergable = (_warehouse, target) => target.mergable;
  const onUnmergable = (_warehouse, target) => target.unmergable;
  const onCells = (warehouse, target) => extractCells(warehouse, target, always);
  const isUnlockedTableCell = (warehouse, cell2) => findInWarehouse(warehouse, cell2).exists((detail2) => !detail2.isLocked);
  const allUnlocked = (warehouse, cells2) => forall(cells2, (cell2) => isUnlockedTableCell(warehouse, cell2));
  const onUnlockedMergable = (warehouse, target) => onMergable(warehouse, target).filter((mergeable) => allUnlocked(warehouse, mergeable.cells));
  const onUnlockedUnmergable = (warehouse, target) => onUnmergable(warehouse, target).filter((cells2) => allUnlocked(warehouse, cells2));
  const generate = (cases) => {
    if (!isArray(cases)) {
      throw new Error("cases must be an array");
    }
    if (cases.length === 0) {
      throw new Error("there must be at least one case");
    }
    const constructors = [];
    const adt2 = {};
    each(cases, (acase, count) => {
      const keys$1 = keys(acase);
      if (keys$1.length !== 1) {
        throw new Error("one and only one name per case");
      }
      const key2 = keys$1[0];
      const value = acase[key2];
      if (adt2[key2] !== void 0) {
        throw new Error("duplicate key detected:" + key2);
      } else if (key2 === "cata") {
        throw new Error("cannot have a case named cata (sorry)");
      } else if (!isArray(value)) {
        throw new Error("case arguments must be an array");
      }
      constructors.push(key2);
      adt2[key2] = (...args) => {
        const argLength = args.length;
        if (argLength !== value.length) {
          throw new Error("Wrong number of arguments to case " + key2 + ". Expected " + value.length + " (" + value + "), got " + argLength);
        }
        const match = (branches) => {
          const branchKeys = keys(branches);
          if (constructors.length !== branchKeys.length) {
            throw new Error("Wrong number of arguments to match. Expected: " + constructors.join(",") + "\nActual: " + branchKeys.join(","));
          }
          const allReqd = forall(constructors, (reqKey) => {
            return contains(branchKeys, reqKey);
          });
          if (!allReqd) {
            throw new Error("Not all branches were specified when using match. Specified: " + branchKeys.join(", ") + "\nRequired: " + constructors.join(", "));
          }
          return branches[key2].apply(null, args);
        };
        return {
          fold: (...foldArgs) => {
            if (foldArgs.length !== cases.length) {
              throw new Error("Wrong number of arguments to fold. Expected " + cases.length + ", got " + foldArgs.length);
            }
            const target = foldArgs[count];
            return target.apply(null, args);
          },
          match,
          log: (label) => {
            console.log(label, {
              constructors,
              constructor: key2,
              params: args
            });
          }
        };
      };
    });
    return adt2;
  };
  const Adt = { generate };
  const adt = Adt.generate([
    { none: [] },
    { only: ["index"] },
    {
      left: [
        "index",
        "next"
      ]
    },
    {
      middle: [
        "prev",
        "index",
        "next"
      ]
    },
    {
      right: [
        "prev",
        "index"
      ]
    }
  ]);
  ({ ...adt });
  const opGetRowsType = (table2, target) => {
    const house = Warehouse.fromTable(table2);
    const details = onCells(house, target);
    return details.bind((selectedCells) => {
      const lastSelectedCell = selectedCells[selectedCells.length - 1];
      const minRowRange = selectedCells[0].row;
      const maxRowRange = lastSelectedCell.row + lastSelectedCell.rowspan;
      const selectedRows = house.all.slice(minRowRange, maxRowRange);
      return findCommonRowType(selectedRows);
    }).getOr("");
  };
  const getRowsType = opGetRowsType;
  const rgbToHex = (value) => startsWith(value, "rgb") ? rgbaToHexString(value) : value;
  const extractAdvancedStyles = (elm) => {
    const element = SugarElement.fromDom(elm);
    return {
      borderwidth: getRaw(element, "border-width").getOr(""),
      borderstyle: getRaw(element, "border-style").getOr(""),
      bordercolor: getRaw(element, "border-color").map(rgbToHex).getOr(""),
      backgroundcolor: getRaw(element, "background-color").map(rgbToHex).getOr("")
    };
  };
  const getSharedValues = (data) => {
    const baseData = data[0];
    const comparisonData = data.slice(1);
    each(comparisonData, (items) => {
      each(keys(baseData), (key2) => {
        each$1(items, (itemValue, itemKey) => {
          const comparisonValue = baseData[key2];
          if (comparisonValue !== "" && key2 === itemKey) {
            if (comparisonValue !== itemValue) {
              baseData[key2] = "";
            }
          }
        });
      });
    });
    return baseData;
  };
  const getAlignment = (formats, formatName, editor, elm) => find(formats, (name2) => !isUndefined(editor.formatter.matchNode(elm, formatName + name2))).getOr("");
  const getHAlignment = curry(getAlignment, [
    "left",
    "center",
    "right"
  ], "align");
  const getVAlignment = curry(getAlignment, [
    "top",
    "middle",
    "bottom"
  ], "valign");
  const extractDataFromSettings = (editor, hasAdvTableTab) => {
    const style = getDefaultStyles(editor);
    const attrs = getDefaultAttributes(editor);
    const extractAdvancedStyleData = () => ({
      borderstyle: get$4(style, "border-style").getOr(""),
      bordercolor: rgbToHex(get$4(style, "border-color").getOr("")),
      backgroundcolor: rgbToHex(get$4(style, "background-color").getOr(""))
    });
    const defaultData = {
      height: "",
      width: "100%",
      cellspacing: "",
      cellpadding: "",
      caption: false,
      class: "",
      align: "",
      border: ""
    };
    const getBorder = () => {
      const borderWidth = style["border-width"];
      if (shouldStyleWithCss(editor) && borderWidth) {
        return { border: borderWidth };
      }
      return get$4(attrs, "border").fold(() => ({}), (border) => ({ border }));
    };
    const advStyle = hasAdvTableTab ? extractAdvancedStyleData() : {};
    const getCellPaddingCellSpacing = () => {
      const spacing = get$4(style, "border-spacing").or(get$4(attrs, "cellspacing")).fold(() => ({}), (cellspacing) => ({ cellspacing }));
      const padding = get$4(style, "border-padding").or(get$4(attrs, "cellpadding")).fold(() => ({}), (cellpadding) => ({ cellpadding }));
      return {
        ...spacing,
        ...padding
      };
    };
    const data = {
      ...defaultData,
      ...style,
      ...attrs,
      ...advStyle,
      ...getBorder(),
      ...getCellPaddingCellSpacing()
    };
    return data;
  };
  const getRowType = (elm) => table(SugarElement.fromDom(elm)).map((table2) => {
    const target = { selection: fromDom(elm.cells) };
    return getRowsType(table2, target);
  }).getOr("");
  const extractDataFromTableElement = (editor, elm, hasAdvTableTab) => {
    const getBorder = (dom2, elm2) => {
      const optBorderWidth = getRaw(SugarElement.fromDom(elm2), "border-width");
      if (shouldStyleWithCss(editor) && optBorderWidth.isSome()) {
        return optBorderWidth.getOr("");
      }
      return dom2.getAttrib(elm2, "border") || getTDTHOverallStyle(editor.dom, elm2, "border-width") || getTDTHOverallStyle(editor.dom, elm2, "border") || "";
    };
    const dom = editor.dom;
    const cellspacing = shouldStyleWithCss(editor) ? dom.getStyle(elm, "border-spacing") || dom.getAttrib(elm, "cellspacing") : dom.getAttrib(elm, "cellspacing") || dom.getStyle(elm, "border-spacing");
    const cellpadding = shouldStyleWithCss(editor) ? getTDTHOverallStyle(dom, elm, "padding") || dom.getAttrib(elm, "cellpadding") : dom.getAttrib(elm, "cellpadding") || getTDTHOverallStyle(dom, elm, "padding");
    return {
      width: dom.getStyle(elm, "width") || dom.getAttrib(elm, "width"),
      height: dom.getStyle(elm, "height") || dom.getAttrib(elm, "height"),
      cellspacing: cellspacing !== null && cellspacing !== void 0 ? cellspacing : "",
      cellpadding: cellpadding !== null && cellpadding !== void 0 ? cellpadding : "",
      border: getBorder(dom, elm),
      caption: !!dom.select("caption", elm)[0],
      class: dom.getAttrib(elm, "class", ""),
      align: getHAlignment(editor, elm),
      ...hasAdvTableTab ? extractAdvancedStyles(elm) : {}
    };
  };
  const extractDataFromRowElement = (editor, elm, hasAdvancedRowTab2) => {
    const dom = editor.dom;
    return {
      height: dom.getStyle(elm, "height") || dom.getAttrib(elm, "height"),
      class: dom.getAttrib(elm, "class", ""),
      type: getRowType(elm),
      align: getHAlignment(editor, elm),
      ...hasAdvancedRowTab2 ? extractAdvancedStyles(elm) : {}
    };
  };
  const extractDataFromCellElement = (editor, cell2, hasAdvancedCellTab2, column) => {
    const dom = editor.dom;
    const colElm = column.getOr(cell2);
    const getStyle = (element, style) => dom.getStyle(element, style) || dom.getAttrib(element, style);
    return {
      width: getStyle(colElm, "width"),
      height: getStyle(cell2, "height"),
      scope: dom.getAttrib(cell2, "scope"),
      celltype: getNodeName(cell2),
      class: dom.getAttrib(cell2, "class", ""),
      halign: getHAlignment(editor, cell2),
      valign: getVAlignment(editor, cell2),
      ...hasAdvancedCellTab2 ? extractAdvancedStyles(cell2) : {}
    };
  };
  const getSelectedCells = (table2, cells2) => {
    const warehouse = Warehouse.fromTable(table2);
    const allCells = Warehouse.justCells(warehouse);
    const filtered = filter(allCells, (cellA) => exists(cells2, (cellB) => eq(cellA.element, cellB)));
    return map(filtered, (cell2) => ({
      element: cell2.element.dom,
      column: Warehouse.getColumnAt(warehouse, cell2.column).map((col) => col.element.dom)
    }));
  };
  const updateSimpleProps$1 = (modifier, colModifier, data, shouldUpdate) => {
    if (shouldUpdate("scope")) {
      modifier.setAttrib("scope", data.scope);
    }
    if (shouldUpdate("class")) {
      modifier.setAttrib("class", data.class);
    }
    if (shouldUpdate("height")) {
      modifier.setStyle("height", addPxSuffix(data.height));
    }
    if (shouldUpdate("width")) {
      colModifier.setStyle("width", addPxSuffix(data.width));
    }
  };
  const updateAdvancedProps$1 = (modifier, data, shouldUpdate) => {
    if (shouldUpdate("backgroundcolor")) {
      modifier.setFormat("tablecellbackgroundcolor", data.backgroundcolor);
    }
    if (shouldUpdate("bordercolor")) {
      modifier.setFormat("tablecellbordercolor", data.bordercolor);
    }
    if (shouldUpdate("borderstyle")) {
      modifier.setFormat("tablecellborderstyle", data.borderstyle);
    }
    if (shouldUpdate("borderwidth")) {
      modifier.setFormat("tablecellborderwidth", addPxSuffix(data.borderwidth));
    }
  };
  const applyStyleData$1 = (editor, cells2, data, wasChanged) => {
    const isSingleCell = cells2.length === 1;
    each(cells2, (item) => {
      const cellElm = item.element;
      const shouldOverrideCurrentValue = isSingleCell ? always : wasChanged;
      const modifier = DomModifier.normal(editor, cellElm);
      const colModifier = item.column.map((col) => DomModifier.normal(editor, col)).getOr(modifier);
      updateSimpleProps$1(modifier, colModifier, data, shouldOverrideCurrentValue);
      if (hasAdvancedCellTab(editor)) {
        updateAdvancedProps$1(modifier, data, shouldOverrideCurrentValue);
      }
      if (wasChanged("halign")) {
        setAlign(editor, cellElm, data.halign);
      }
      if (wasChanged("valign")) {
        setVAlign(editor, cellElm, data.valign);
      }
    });
  };
  const applyStructureData$1 = (editor, data) => {
    editor.execCommand("mceTableCellType", false, {
      type: data.celltype,
      no_events: true
    });
  };
  const applyCellData = (editor, cells2, oldData, data) => {
    const modifiedData = filter$1(data, (value, key2) => oldData[key2] !== value);
    if (size(modifiedData) > 0 && cells2.length >= 1) {
      table(cells2[0]).each((table2) => {
        const selectedCells = getSelectedCells(table2, cells2);
        const styleModified = size(filter$1(modifiedData, (_value, key2) => key2 !== "scope" && key2 !== "celltype")) > 0;
        const structureModified = has(modifiedData, "celltype");
        if (styleModified || has(modifiedData, "scope")) {
          applyStyleData$1(editor, selectedCells, data, curry(has, modifiedData));
        }
        if (structureModified) {
          applyStructureData$1(editor, data);
        }
        fireTableModified(editor, table2.dom, {
          structure: structureModified,
          style: styleModified
        });
      });
    }
  };
  const onSubmitCellForm = (editor, cells2, oldData, api2) => {
    const data = api2.getData();
    api2.close();
    editor.undoManager.transact(() => {
      applyCellData(editor, cells2, oldData, data);
      editor.focus();
    });
  };
  const getData$1 = (editor, cells2) => {
    const cellsData = table(cells2[0]).map((table2) => map(getSelectedCells(table2, cells2), (item) => extractDataFromCellElement(editor, item.element, hasAdvancedCellTab(editor), item.column)));
    return getSharedValues(cellsData.getOrDie());
  };
  const open$2 = (editor) => {
    const cells2 = getCellsFromSelection(editor);
    if (cells2.length === 0) {
      return;
    }
    const data = getData$1(editor, cells2);
    const dialogTabPanel = {
      type: "tabpanel",
      tabs: [
        {
          title: "General",
          name: "general",
          items: getItems$2(editor)
        },
        getAdvancedTab(editor, "cell")
      ]
    };
    const dialogPanel = {
      type: "panel",
      items: [{
        type: "grid",
        columns: 2,
        items: getItems$2(editor)
      }]
    };
    editor.windowManager.open({
      title: "Cell Properties",
      size: "normal",
      body: hasAdvancedCellTab(editor) ? dialogTabPanel : dialogPanel,
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData: data,
      onSubmit: curry(onSubmitCellForm, editor, cells2, data)
    });
  };
  const getClassList = (editor) => {
    const classes = buildListItems(getRowClassList(editor));
    if (classes.length > 0) {
      return Optional.some({
        name: "class",
        type: "listbox",
        label: "Class",
        items: classes
      });
    }
    return Optional.none();
  };
  const formChildren = [
    {
      type: "listbox",
      name: "type",
      label: "Row type",
      items: [
        {
          text: "Header",
          value: "header"
        },
        {
          text: "Body",
          value: "body"
        },
        {
          text: "Footer",
          value: "footer"
        }
      ]
    },
    {
      type: "listbox",
      name: "align",
      label: "Alignment",
      items: [
        {
          text: "None",
          value: ""
        },
        {
          text: "Left",
          value: "left"
        },
        {
          text: "Center",
          value: "center"
        },
        {
          text: "Right",
          value: "right"
        }
      ]
    },
    {
      label: "Height",
      name: "height",
      type: "input"
    }
  ];
  const getItems$1 = (editor) => formChildren.concat(getClassList(editor).toArray());
  const updateSimpleProps = (modifier, data, shouldUpdate) => {
    if (shouldUpdate("class")) {
      modifier.setAttrib("class", data.class);
    }
    if (shouldUpdate("height")) {
      modifier.setStyle("height", addPxSuffix(data.height));
    }
  };
  const updateAdvancedProps = (modifier, data, shouldUpdate) => {
    if (shouldUpdate("backgroundcolor")) {
      modifier.setStyle("background-color", data.backgroundcolor);
    }
    if (shouldUpdate("bordercolor")) {
      modifier.setStyle("border-color", data.bordercolor);
    }
    if (shouldUpdate("borderstyle")) {
      modifier.setStyle("border-style", data.borderstyle);
    }
  };
  const applyStyleData = (editor, rows2, data, wasChanged) => {
    const isSingleRow = rows2.length === 1;
    const shouldOverrideCurrentValue = isSingleRow ? always : wasChanged;
    each(rows2, (rowElm) => {
      const modifier = DomModifier.normal(editor, rowElm);
      updateSimpleProps(modifier, data, shouldOverrideCurrentValue);
      if (hasAdvancedRowTab(editor)) {
        updateAdvancedProps(modifier, data, shouldOverrideCurrentValue);
      }
      if (wasChanged("align")) {
        setAlign(editor, rowElm, data.align);
      }
    });
  };
  const applyStructureData = (editor, data) => {
    editor.execCommand("mceTableRowType", false, {
      type: data.type,
      no_events: true
    });
  };
  const applyRowData = (editor, rows2, oldData, data) => {
    const modifiedData = filter$1(data, (value, key2) => oldData[key2] !== value);
    if (size(modifiedData) > 0) {
      const typeModified = has(modifiedData, "type");
      const styleModified = typeModified ? size(modifiedData) > 1 : true;
      if (styleModified) {
        applyStyleData(editor, rows2, data, curry(has, modifiedData));
      }
      if (typeModified) {
        applyStructureData(editor, data);
      }
      table(SugarElement.fromDom(rows2[0])).each((table2) => fireTableModified(editor, table2.dom, {
        structure: typeModified,
        style: styleModified
      }));
    }
  };
  const onSubmitRowForm = (editor, rows2, oldData, api2) => {
    const data = api2.getData();
    api2.close();
    editor.undoManager.transact(() => {
      applyRowData(editor, rows2, oldData, data);
      editor.focus();
    });
  };
  const open$1 = (editor) => {
    const rows2 = getRowsFromSelection(getSelectionStart(editor), ephemera.selected);
    if (rows2.length === 0) {
      return;
    }
    const rowsData = map(rows2, (rowElm) => extractDataFromRowElement(editor, rowElm.dom, hasAdvancedRowTab(editor)));
    const data = getSharedValues(rowsData);
    const dialogTabPanel = {
      type: "tabpanel",
      tabs: [
        {
          title: "General",
          name: "general",
          items: getItems$1(editor)
        },
        getAdvancedTab(editor, "row")
      ]
    };
    const dialogPanel = {
      type: "panel",
      items: [{
        type: "grid",
        columns: 2,
        items: getItems$1(editor)
      }]
    };
    editor.windowManager.open({
      title: "Row Properties",
      size: "normal",
      body: hasAdvancedRowTab(editor) ? dialogTabPanel : dialogPanel,
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData: data,
      onSubmit: curry(onSubmitRowForm, editor, map(rows2, (r) => r.dom), data)
    });
  };
  const getItems = (editor, classes, insertNewTable) => {
    const rowColCountItems = !insertNewTable ? [] : [
      {
        type: "input",
        name: "cols",
        label: "Cols",
        inputMode: "numeric"
      },
      {
        type: "input",
        name: "rows",
        label: "Rows",
        inputMode: "numeric"
      }
    ];
    const alwaysItems = [
      {
        type: "input",
        name: "width",
        label: "Width"
      },
      {
        type: "input",
        name: "height",
        label: "Height"
      }
    ];
    const appearanceItems = hasAppearanceOptions(editor) ? [
      {
        type: "input",
        name: "cellspacing",
        label: "Cell spacing",
        inputMode: "numeric"
      },
      {
        type: "input",
        name: "cellpadding",
        label: "Cell padding",
        inputMode: "numeric"
      },
      {
        type: "input",
        name: "border",
        label: "Border width"
      },
      {
        type: "label",
        label: "Caption",
        items: [{
          type: "checkbox",
          name: "caption",
          label: "Show caption"
        }]
      }
    ] : [];
    const alignmentItem = [{
      type: "listbox",
      name: "align",
      label: "Alignment",
      items: [
        {
          text: "None",
          value: ""
        },
        {
          text: "Left",
          value: "left"
        },
        {
          text: "Center",
          value: "center"
        },
        {
          text: "Right",
          value: "right"
        }
      ]
    }];
    const classListItem = classes.length > 0 ? [{
      type: "listbox",
      name: "class",
      label: "Class",
      items: classes
    }] : [];
    return rowColCountItems.concat(alwaysItems).concat(appearanceItems).concat(alignmentItem).concat(classListItem);
  };
  const styleTDTH = (dom, elm, name2, value) => {
    if (elm.tagName === "TD" || elm.tagName === "TH") {
      if (isString(name2) && isNonNullable(value)) {
        dom.setStyle(elm, name2, value);
      } else {
        dom.setStyles(elm, name2);
      }
    } else {
      if (elm.children) {
        for (let i2 = 0; i2 < elm.children.length; i2++) {
          styleTDTH(dom, elm.children[i2], name2, value);
        }
      }
    }
  };
  const applyDataToElement = (editor, tableElm, data) => {
    const dom = editor.dom;
    const attrs = {};
    const styles = {};
    if (!isUndefined(data.class)) {
      attrs.class = data.class;
    }
    styles.height = addPxSuffix(data.height);
    if (shouldStyleWithCss(editor)) {
      styles.width = addPxSuffix(data.width);
    } else if (dom.getAttrib(tableElm, "width")) {
      attrs.width = removePxSuffix(data.width);
    }
    if (shouldStyleWithCss(editor)) {
      styles["border-width"] = addPxSuffix(data.border);
      styles["border-spacing"] = addPxSuffix(data.cellspacing);
    } else {
      attrs.border = data.border;
      attrs.cellpadding = data.cellpadding;
      attrs.cellspacing = data.cellspacing;
    }
    if (shouldStyleWithCss(editor) && tableElm.children) {
      for (let i2 = 0; i2 < tableElm.children.length; i2++) {
        styleTDTH(dom, tableElm.children[i2], {
          "border-width": addPxSuffix(data.border),
          "padding": addPxSuffix(data.cellpadding)
        });
        if (hasAdvancedTableTab(editor)) {
          styleTDTH(dom, tableElm.children[i2], { "border-color": data.bordercolor });
        }
      }
    }
    if (hasAdvancedTableTab(editor)) {
      const advData = data;
      styles["background-color"] = advData.backgroundcolor;
      styles["border-color"] = advData.bordercolor;
      styles["border-style"] = advData.borderstyle;
    }
    attrs.style = dom.serializeStyle({
      ...getDefaultStyles(editor),
      ...styles
    });
    dom.setAttribs(tableElm, {
      ...getDefaultAttributes(editor),
      ...attrs
    });
  };
  const onSubmitTableForm = (editor, tableElm, oldData, api2) => {
    const dom = editor.dom;
    const data = api2.getData();
    const modifiedData = filter$1(data, (value, key2) => oldData[key2] !== value);
    api2.close();
    if (data.class === "") {
      delete data.class;
    }
    editor.undoManager.transact(() => {
      if (!tableElm) {
        const cols = toInt(data.cols).getOr(1);
        const rows2 = toInt(data.rows).getOr(1);
        editor.execCommand("mceInsertTable", false, {
          rows: rows2,
          columns: cols
        });
        tableElm = getSelectionCell(getSelectionStart(editor), getIsRoot(editor)).bind((cell2) => table(cell2, getIsRoot(editor))).map((table2) => table2.dom).getOrDie();
      }
      if (size(modifiedData) > 0) {
        applyDataToElement(editor, tableElm, data);
        const captionElm = dom.select("caption", tableElm)[0];
        if (captionElm && !data.caption || !captionElm && data.caption) {
          editor.execCommand("mceTableToggleCaption");
        }
        setAlign(editor, tableElm, data.align);
      }
      editor.focus();
      editor.addVisual();
      if (size(modifiedData) > 0) {
        const captionModified = has(modifiedData, "caption");
        const styleModified = captionModified ? size(modifiedData) > 1 : true;
        fireTableModified(editor, tableElm, {
          structure: captionModified,
          style: styleModified
        });
      }
    });
  };
  const open = (editor, insertNewTable) => {
    const dom = editor.dom;
    let tableElm;
    let data = extractDataFromSettings(editor, hasAdvancedTableTab(editor));
    if (insertNewTable) {
      data.cols = "1";
      data.rows = "1";
      if (hasAdvancedTableTab(editor)) {
        data.borderstyle = "";
        data.bordercolor = "";
        data.backgroundcolor = "";
      }
    } else {
      tableElm = dom.getParent(editor.selection.getStart(), "table", editor.getBody());
      if (tableElm) {
        data = extractDataFromTableElement(editor, tableElm, hasAdvancedTableTab(editor));
      } else {
        if (hasAdvancedTableTab(editor)) {
          data.borderstyle = "";
          data.bordercolor = "";
          data.backgroundcolor = "";
        }
      }
    }
    const classes = buildListItems(getTableClassList(editor));
    if (classes.length > 0) {
      if (data.class) {
        data.class = data.class.replace(/\s*mce\-item\-table\s*/g, "");
      }
    }
    const generalPanel = {
      type: "grid",
      columns: 2,
      items: getItems(editor, classes, insertNewTable)
    };
    const nonAdvancedForm = () => ({
      type: "panel",
      items: [generalPanel]
    });
    const advancedForm = () => ({
      type: "tabpanel",
      tabs: [
        {
          title: "General",
          name: "general",
          items: [generalPanel]
        },
        getAdvancedTab(editor, "table")
      ]
    });
    const dialogBody = hasAdvancedTableTab(editor) ? advancedForm() : nonAdvancedForm();
    editor.windowManager.open({
      title: "Table Properties",
      size: "normal",
      body: dialogBody,
      onSubmit: curry(onSubmitTableForm, editor, tableElm, data),
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData: data
    });
  };
  const registerCommands = (editor) => {
    each$1({
      mceTableProps: curry(open, editor, false),
      mceTableRowProps: curry(open$1, editor),
      mceTableCellProps: curry(open$2, editor)
    }, (func, name2) => editor.addCommand(name2, () => func()));
    editor.addCommand("mceInsertTableDialog", (_ui) => {
      open(editor, true);
    });
  };
  const child = (scope, selector) => child$1(scope, selector).isSome();
  const selection = identity;
  const unmergable = (selectedCells) => {
    const hasSpan = (elem, type2) => getOpt(elem, type2).exists((span) => parseInt(span, 10) > 1);
    const hasRowOrColSpan = (elem) => hasSpan(elem, "rowspan") || hasSpan(elem, "colspan");
    return selectedCells.length > 0 && forall(selectedCells, hasRowOrColSpan) ? Optional.some(selectedCells) : Optional.none();
  };
  const mergable = (table2, selectedCells, ephemera2) => {
    if (selectedCells.length <= 1) {
      return Optional.none();
    } else {
      return retrieveBox(table2, ephemera2.firstSelectedSelector, ephemera2.lastSelectedSelector).map((bounds2) => ({
        bounds: bounds2,
        cells: selectedCells
      }));
    }
  };
  const noMenu = (cell2) => ({
    element: cell2,
    mergable: Optional.none(),
    unmergable: Optional.none(),
    selection: [cell2]
  });
  const forMenu = (selectedCells, table2, cell2) => ({
    element: cell2,
    mergable: mergable(table2, selectedCells, ephemera),
    unmergable: unmergable(selectedCells),
    selection: selection(selectedCells)
  });
  const getSelectionTargets = (editor) => {
    const targets = Cell(Optional.none());
    const changeHandlers = Cell([]);
    let selectionDetails = Optional.none();
    const isCaption = isTag("caption");
    const isDisabledForSelection = (key2) => selectionDetails.forall((details) => !details[key2]);
    const getStart = () => getSelectionCellOrCaption(getSelectionStart(editor), getIsRoot(editor));
    const getEnd = () => getSelectionCellOrCaption(getSelectionEnd(editor), getIsRoot(editor));
    const findTargets = () => getStart().bind((startCellOrCaption) => flatten(lift2(table(startCellOrCaption), getEnd().bind(table), (startTable, endTable) => {
      if (eq(startTable, endTable)) {
        if (isCaption(startCellOrCaption)) {
          return Optional.some(noMenu(startCellOrCaption));
        } else {
          return Optional.some(forMenu(getCellsFromSelection(editor), startTable, startCellOrCaption));
        }
      }
      return Optional.none();
    })));
    const getExtractedDetails = (targets2) => {
      const tableOpt = table(targets2.element);
      return tableOpt.map((table2) => {
        const warehouse = Warehouse.fromTable(table2);
        const selectedCells = onCells(warehouse, targets2).getOr([]);
        const locked = foldl(selectedCells, (acc, cell2) => {
          if (cell2.isLocked) {
            acc.onAny = true;
            if (cell2.column === 0) {
              acc.onFirst = true;
            } else if (cell2.column + cell2.colspan >= warehouse.grid.columns) {
              acc.onLast = true;
            }
          }
          return acc;
        }, {
          onAny: false,
          onFirst: false,
          onLast: false
        });
        return {
          mergeable: onUnlockedMergable(warehouse, targets2).isSome(),
          unmergeable: onUnlockedUnmergable(warehouse, targets2).isSome(),
          locked
        };
      });
    };
    const resetTargets = () => {
      targets.set(cached(findTargets)());
      selectionDetails = targets.get().bind(getExtractedDetails);
      each(changeHandlers.get(), call);
    };
    const setupHandler = (handler) => {
      handler();
      changeHandlers.set(changeHandlers.get().concat([handler]));
      return () => {
        changeHandlers.set(filter(changeHandlers.get(), (h) => h !== handler));
      };
    };
    const onSetup = (api2, isDisabled) => setupHandler(() => targets.get().fold(() => {
      api2.setEnabled(false);
    }, (targets2) => {
      api2.setEnabled(!isDisabled(targets2));
    }));
    const onSetupWithToggle = (api2, isDisabled, isActive) => setupHandler(() => targets.get().fold(() => {
      api2.setEnabled(false);
      api2.setActive(false);
    }, (targets2) => {
      api2.setEnabled(!isDisabled(targets2));
      api2.setActive(isActive(targets2));
    }));
    const isDisabledFromLocked = (lockedDisable) => selectionDetails.exists((details) => details.locked[lockedDisable]);
    const onSetupTable = (api2) => onSetup(api2, (_) => false);
    const onSetupCellOrRow = (api2) => onSetup(api2, (targets2) => isCaption(targets2.element));
    const onSetupColumn = (lockedDisable) => (api2) => onSetup(api2, (targets2) => isCaption(targets2.element) || isDisabledFromLocked(lockedDisable));
    const onSetupPasteable = (getClipboardData) => (api2) => onSetup(api2, (targets2) => isCaption(targets2.element) || getClipboardData().isNone());
    const onSetupPasteableColumn = (getClipboardData, lockedDisable) => (api2) => onSetup(api2, (targets2) => isCaption(targets2.element) || getClipboardData().isNone() || isDisabledFromLocked(lockedDisable));
    const onSetupMergeable = (api2) => onSetup(api2, (_targets) => isDisabledForSelection("mergeable"));
    const onSetupUnmergeable = (api2) => onSetup(api2, (_targets) => isDisabledForSelection("unmergeable"));
    const onSetupTableWithCaption = (api2) => {
      return onSetupWithToggle(api2, never, (targets2) => {
        const tableOpt = table(targets2.element, getIsRoot(editor));
        return tableOpt.exists((table2) => child(table2, "caption"));
      });
    };
    const onSetupTableHeaders = (command, headerType) => (api2) => {
      return onSetupWithToggle(api2, (targets2) => isCaption(targets2.element), () => editor.queryCommandValue(command) === headerType);
    };
    const onSetupTableRowHeaders = onSetupTableHeaders("mceTableRowType", "header");
    const onSetupTableColumnHeaders = onSetupTableHeaders("mceTableColType", "th");
    editor.on("NodeChange ExecCommand TableSelectorChange", resetTargets);
    return {
      onSetupTable,
      onSetupCellOrRow,
      onSetupColumn,
      onSetupPasteable,
      onSetupPasteableColumn,
      onSetupMergeable,
      onSetupUnmergeable,
      resetTargets,
      onSetupTableWithCaption,
      onSetupTableRowHeaders,
      onSetupTableColumnHeaders,
      targets: targets.get
    };
  };
  var global2 = tinymce.util.Tools.resolve("tinymce.FakeClipboard");
  const tableTypeBase = "x-tinymce/dom-table-";
  const tableTypeRow = tableTypeBase + "rows";
  const tableTypeColumn = tableTypeBase + "columns";
  const getData = (type2) => {
    var _a;
    const items = (_a = global2.read()) !== null && _a !== void 0 ? _a : [];
    return findMap(items, (item) => Optional.from(item.getType(type2)));
  };
  const getRows = () => getData(tableTypeRow);
  const getColumns = () => getData(tableTypeColumn);
  const addButtons = (editor, selectionTargets) => {
    editor.ui.registry.addMenuButton("table", {
      tooltip: "Table",
      icon: "table",
      fetch: (callback) => callback("inserttable | cell row column | advtablesort | tableprops deletetable")
    });
    const cmd = (command) => () => editor.execCommand(command);
    const addButtonIfRegistered = (name2, spec) => {
      if (editor.queryCommandSupported(spec.command)) {
        editor.ui.registry.addButton(name2, {
          ...spec,
          onAction: isFunction(spec.onAction) ? spec.onAction : cmd(spec.command)
        });
      }
    };
    const addToggleButtonIfRegistered = (name2, spec) => {
      if (editor.queryCommandSupported(spec.command)) {
        editor.ui.registry.addToggleButton(name2, {
          ...spec,
          onAction: isFunction(spec.onAction) ? spec.onAction : cmd(spec.command)
        });
      }
    };
    addButtonIfRegistered("tableprops", {
      tooltip: "Table properties",
      command: "mceTableProps",
      icon: "table",
      onSetup: selectionTargets.onSetupTable
    });
    addButtonIfRegistered("tabledelete", {
      tooltip: "Delete table",
      command: "mceTableDelete",
      icon: "table-delete-table",
      onSetup: selectionTargets.onSetupTable
    });
    addButtonIfRegistered("tablecellprops", {
      tooltip: "Cell properties",
      command: "mceTableCellProps",
      icon: "table-cell-properties",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tablemergecells", {
      tooltip: "Merge cells",
      command: "mceTableMergeCells",
      icon: "table-merge-cells",
      onSetup: selectionTargets.onSetupMergeable
    });
    addButtonIfRegistered("tablesplitcells", {
      tooltip: "Split cell",
      command: "mceTableSplitCells",
      icon: "table-split-cells",
      onSetup: selectionTargets.onSetupUnmergeable
    });
    addButtonIfRegistered("tableinsertrowbefore", {
      tooltip: "Insert row before",
      command: "mceTableInsertRowBefore",
      icon: "table-insert-row-above",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tableinsertrowafter", {
      tooltip: "Insert row after",
      command: "mceTableInsertRowAfter",
      icon: "table-insert-row-after",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tabledeleterow", {
      tooltip: "Delete row",
      command: "mceTableDeleteRow",
      icon: "table-delete-row",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tablerowprops", {
      tooltip: "Row properties",
      command: "mceTableRowProps",
      icon: "table-row-properties",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tableinsertcolbefore", {
      tooltip: "Insert column before",
      command: "mceTableInsertColBefore",
      icon: "table-insert-column-before",
      onSetup: selectionTargets.onSetupColumn("onFirst")
    });
    addButtonIfRegistered("tableinsertcolafter", {
      tooltip: "Insert column after",
      command: "mceTableInsertColAfter",
      icon: "table-insert-column-after",
      onSetup: selectionTargets.onSetupColumn("onLast")
    });
    addButtonIfRegistered("tabledeletecol", {
      tooltip: "Delete column",
      command: "mceTableDeleteCol",
      icon: "table-delete-column",
      onSetup: selectionTargets.onSetupColumn("onAny")
    });
    addButtonIfRegistered("tablecutrow", {
      tooltip: "Cut row",
      command: "mceTableCutRow",
      icon: "cut-row",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tablecopyrow", {
      tooltip: "Copy row",
      command: "mceTableCopyRow",
      icon: "duplicate-row",
      onSetup: selectionTargets.onSetupCellOrRow
    });
    addButtonIfRegistered("tablepasterowbefore", {
      tooltip: "Paste row before",
      command: "mceTablePasteRowBefore",
      icon: "paste-row-before",
      onSetup: selectionTargets.onSetupPasteable(getRows)
    });
    addButtonIfRegistered("tablepasterowafter", {
      tooltip: "Paste row after",
      command: "mceTablePasteRowAfter",
      icon: "paste-row-after",
      onSetup: selectionTargets.onSetupPasteable(getRows)
    });
    addButtonIfRegistered("tablecutcol", {
      tooltip: "Cut column",
      command: "mceTableCutCol",
      icon: "cut-column",
      onSetup: selectionTargets.onSetupColumn("onAny")
    });
    addButtonIfRegistered("tablecopycol", {
      tooltip: "Copy column",
      command: "mceTableCopyCol",
      icon: "duplicate-column",
      onSetup: selectionTargets.onSetupColumn("onAny")
    });
    addButtonIfRegistered("tablepastecolbefore", {
      tooltip: "Paste column before",
      command: "mceTablePasteColBefore",
      icon: "paste-column-before",
      onSetup: selectionTargets.onSetupPasteableColumn(getColumns, "onFirst")
    });
    addButtonIfRegistered("tablepastecolafter", {
      tooltip: "Paste column after",
      command: "mceTablePasteColAfter",
      icon: "paste-column-after",
      onSetup: selectionTargets.onSetupPasteableColumn(getColumns, "onLast")
    });
    addButtonIfRegistered("tableinsertdialog", {
      tooltip: "Insert table",
      command: "mceInsertTableDialog",
      icon: "table"
    });
    const tableClassList = filterNoneItem(getTableClassList(editor));
    if (tableClassList.length !== 0 && editor.queryCommandSupported("mceTableToggleClass")) {
      editor.ui.registry.addMenuButton("tableclass", {
        icon: "table-classes",
        tooltip: "Table styles",
        fetch: generateMenuItemsCallback(editor, tableClassList, "tableclass", (value) => editor.execCommand("mceTableToggleClass", false, value)),
        onSetup: selectionTargets.onSetupTable
      });
    }
    const tableCellClassList = filterNoneItem(getCellClassList(editor));
    if (tableCellClassList.length !== 0 && editor.queryCommandSupported("mceTableCellToggleClass")) {
      editor.ui.registry.addMenuButton("tablecellclass", {
        icon: "table-cell-classes",
        tooltip: "Cell styles",
        fetch: generateMenuItemsCallback(editor, tableCellClassList, "tablecellclass", (value) => editor.execCommand("mceTableCellToggleClass", false, value)),
        onSetup: selectionTargets.onSetupCellOrRow
      });
    }
    if (editor.queryCommandSupported("mceTableApplyCellStyle")) {
      editor.ui.registry.addMenuButton("tablecellvalign", {
        icon: "vertical-align",
        tooltip: "Vertical align",
        fetch: generateMenuItemsCallback(editor, verticalAlignValues, "tablecellverticalalign", applyTableCellStyle(editor, "vertical-align")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addMenuButton("tablecellborderwidth", {
        icon: "border-width",
        tooltip: "Border width",
        fetch: generateMenuItemsCallback(editor, getTableBorderWidths(editor), "tablecellborderwidth", applyTableCellStyle(editor, "border-width")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addMenuButton("tablecellborderstyle", {
        icon: "border-style",
        tooltip: "Border style",
        fetch: generateMenuItemsCallback(editor, getTableBorderStyles(editor), "tablecellborderstyle", applyTableCellStyle(editor, "border-style")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addMenuButton("tablecellbackgroundcolor", {
        icon: "cell-background-color",
        tooltip: "Background color",
        fetch: (callback) => callback(buildColorMenu(editor, getTableBackgroundColorMap(editor), "background-color")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addMenuButton("tablecellbordercolor", {
        icon: "cell-border-color",
        tooltip: "Border color",
        fetch: (callback) => callback(buildColorMenu(editor, getTableBorderColorMap(editor), "border-color")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
    }
    addToggleButtonIfRegistered("tablecaption", {
      tooltip: "Table caption",
      icon: "table-caption",
      command: "mceTableToggleCaption",
      onSetup: selectionTargets.onSetupTableWithCaption
    });
    addToggleButtonIfRegistered("tablerowheader", {
      tooltip: "Row header",
      icon: "table-top-header",
      command: "mceTableRowType",
      onAction: changeRowHeader(editor),
      onSetup: selectionTargets.onSetupTableRowHeaders
    });
    addToggleButtonIfRegistered("tablecolheader", {
      tooltip: "Column header",
      icon: "table-left-header",
      command: "mceTableColType",
      onAction: changeColumnHeader(editor),
      onSetup: selectionTargets.onSetupTableColumnHeaders
    });
  };
  const addToolbars = (editor) => {
    const isTable = (table2) => editor.dom.is(table2, "table") && editor.getBody().contains(table2);
    const toolbar = getToolbar(editor);
    if (toolbar.length > 0) {
      editor.ui.registry.addContextToolbar("table", {
        predicate: isTable,
        items: toolbar,
        scope: "node",
        position: "node"
      });
    }
  };
  const addMenuItems = (editor, selectionTargets) => {
    const cmd = (command) => () => editor.execCommand(command);
    const addMenuIfRegistered = (name2, spec) => {
      if (editor.queryCommandSupported(spec.command)) {
        editor.ui.registry.addMenuItem(name2, {
          ...spec,
          onAction: isFunction(spec.onAction) ? spec.onAction : cmd(spec.command)
        });
        return true;
      } else {
        return false;
      }
    };
    const addToggleMenuIfRegistered = (name2, spec) => {
      if (editor.queryCommandSupported(spec.command)) {
        editor.ui.registry.addToggleMenuItem(name2, {
          ...spec,
          onAction: isFunction(spec.onAction) ? spec.onAction : cmd(spec.command)
        });
      }
    };
    const insertTableAction = (data) => {
      editor.execCommand("mceInsertTable", false, {
        rows: data.numRows,
        columns: data.numColumns
      });
    };
    const hasRowMenuItems = [
      addMenuIfRegistered("tableinsertrowbefore", {
        text: "Insert row before",
        icon: "table-insert-row-above",
        command: "mceTableInsertRowBefore",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tableinsertrowafter", {
        text: "Insert row after",
        icon: "table-insert-row-after",
        command: "mceTableInsertRowAfter",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tabledeleterow", {
        text: "Delete row",
        icon: "table-delete-row",
        command: "mceTableDeleteRow",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tablerowprops", {
        text: "Row properties",
        icon: "table-row-properties",
        command: "mceTableRowProps",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tablecutrow", {
        text: "Cut row",
        icon: "cut-row",
        command: "mceTableCutRow",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tablecopyrow", {
        text: "Copy row",
        icon: "duplicate-row",
        command: "mceTableCopyRow",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tablepasterowbefore", {
        text: "Paste row before",
        icon: "paste-row-before",
        command: "mceTablePasteRowBefore",
        onSetup: selectionTargets.onSetupPasteable(getRows)
      }),
      addMenuIfRegistered("tablepasterowafter", {
        text: "Paste row after",
        icon: "paste-row-after",
        command: "mceTablePasteRowAfter",
        onSetup: selectionTargets.onSetupPasteable(getRows)
      })
    ];
    const hasColumnMenuItems = [
      addMenuIfRegistered("tableinsertcolumnbefore", {
        text: "Insert column before",
        icon: "table-insert-column-before",
        command: "mceTableInsertColBefore",
        onSetup: selectionTargets.onSetupColumn("onFirst")
      }),
      addMenuIfRegistered("tableinsertcolumnafter", {
        text: "Insert column after",
        icon: "table-insert-column-after",
        command: "mceTableInsertColAfter",
        onSetup: selectionTargets.onSetupColumn("onLast")
      }),
      addMenuIfRegistered("tabledeletecolumn", {
        text: "Delete column",
        icon: "table-delete-column",
        command: "mceTableDeleteCol",
        onSetup: selectionTargets.onSetupColumn("onAny")
      }),
      addMenuIfRegistered("tablecutcolumn", {
        text: "Cut column",
        icon: "cut-column",
        command: "mceTableCutCol",
        onSetup: selectionTargets.onSetupColumn("onAny")
      }),
      addMenuIfRegistered("tablecopycolumn", {
        text: "Copy column",
        icon: "duplicate-column",
        command: "mceTableCopyCol",
        onSetup: selectionTargets.onSetupColumn("onAny")
      }),
      addMenuIfRegistered("tablepastecolumnbefore", {
        text: "Paste column before",
        icon: "paste-column-before",
        command: "mceTablePasteColBefore",
        onSetup: selectionTargets.onSetupPasteableColumn(getColumns, "onFirst")
      }),
      addMenuIfRegistered("tablepastecolumnafter", {
        text: "Paste column after",
        icon: "paste-column-after",
        command: "mceTablePasteColAfter",
        onSetup: selectionTargets.onSetupPasteableColumn(getColumns, "onLast")
      })
    ];
    const hasCellMenuItems = [
      addMenuIfRegistered("tablecellprops", {
        text: "Cell properties",
        icon: "table-cell-properties",
        command: "mceTableCellProps",
        onSetup: selectionTargets.onSetupCellOrRow
      }),
      addMenuIfRegistered("tablemergecells", {
        text: "Merge cells",
        icon: "table-merge-cells",
        command: "mceTableMergeCells",
        onSetup: selectionTargets.onSetupMergeable
      }),
      addMenuIfRegistered("tablesplitcells", {
        text: "Split cell",
        icon: "table-split-cells",
        command: "mceTableSplitCells",
        onSetup: selectionTargets.onSetupUnmergeable
      })
    ];
    if (!hasTableGrid(editor)) {
      editor.ui.registry.addMenuItem("inserttable", {
        text: "Table",
        icon: "table",
        onAction: cmd("mceInsertTableDialog")
      });
    } else {
      editor.ui.registry.addNestedMenuItem("inserttable", {
        text: "Table",
        icon: "table",
        getSubmenuItems: () => [{
          type: "fancymenuitem",
          fancytype: "inserttable",
          onAction: insertTableAction
        }]
      });
    }
    editor.ui.registry.addMenuItem("inserttabledialog", {
      text: "Insert table",
      icon: "table",
      onAction: cmd("mceInsertTableDialog")
    });
    addMenuIfRegistered("tableprops", {
      text: "Table properties",
      onSetup: selectionTargets.onSetupTable,
      command: "mceTableProps"
    });
    addMenuIfRegistered("deletetable", {
      text: "Delete table",
      icon: "table-delete-table",
      onSetup: selectionTargets.onSetupTable,
      command: "mceTableDelete"
    });
    if (contains(hasRowMenuItems, true)) {
      editor.ui.registry.addNestedMenuItem("row", {
        type: "nestedmenuitem",
        text: "Row",
        getSubmenuItems: constant("tableinsertrowbefore tableinsertrowafter tabledeleterow tablerowprops | tablecutrow tablecopyrow tablepasterowbefore tablepasterowafter")
      });
    }
    if (contains(hasColumnMenuItems, true)) {
      editor.ui.registry.addNestedMenuItem("column", {
        type: "nestedmenuitem",
        text: "Column",
        getSubmenuItems: constant("tableinsertcolumnbefore tableinsertcolumnafter tabledeletecolumn | tablecutcolumn tablecopycolumn tablepastecolumnbefore tablepastecolumnafter")
      });
    }
    if (contains(hasCellMenuItems, true)) {
      editor.ui.registry.addNestedMenuItem("cell", {
        type: "nestedmenuitem",
        text: "Cell",
        getSubmenuItems: constant("tablecellprops tablemergecells tablesplitcells")
      });
    }
    editor.ui.registry.addContextMenu("table", {
      update: () => {
        selectionTargets.resetTargets();
        return selectionTargets.targets().fold(constant(""), (targets) => {
          if (name(targets.element) === "caption") {
            return "tableprops deletetable";
          } else {
            return "cell row column | advtablesort | tableprops deletetable";
          }
        });
      }
    });
    const tableClassList = filterNoneItem(getTableClassList(editor));
    if (tableClassList.length !== 0 && editor.queryCommandSupported("mceTableToggleClass")) {
      editor.ui.registry.addNestedMenuItem("tableclass", {
        icon: "table-classes",
        text: "Table styles",
        getSubmenuItems: () => buildMenuItems(editor, tableClassList, "tableclass", (value) => editor.execCommand("mceTableToggleClass", false, value)),
        onSetup: selectionTargets.onSetupTable
      });
    }
    const tableCellClassList = filterNoneItem(getCellClassList(editor));
    if (tableCellClassList.length !== 0 && editor.queryCommandSupported("mceTableCellToggleClass")) {
      editor.ui.registry.addNestedMenuItem("tablecellclass", {
        icon: "table-cell-classes",
        text: "Cell styles",
        getSubmenuItems: () => buildMenuItems(editor, tableCellClassList, "tablecellclass", (value) => editor.execCommand("mceTableCellToggleClass", false, value)),
        onSetup: selectionTargets.onSetupCellOrRow
      });
    }
    if (editor.queryCommandSupported("mceTableApplyCellStyle")) {
      editor.ui.registry.addNestedMenuItem("tablecellvalign", {
        icon: "vertical-align",
        text: "Vertical align",
        getSubmenuItems: () => buildMenuItems(editor, verticalAlignValues, "tablecellverticalalign", applyTableCellStyle(editor, "vertical-align")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addNestedMenuItem("tablecellborderwidth", {
        icon: "border-width",
        text: "Border width",
        getSubmenuItems: () => buildMenuItems(editor, getTableBorderWidths(editor), "tablecellborderwidth", applyTableCellStyle(editor, "border-width")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addNestedMenuItem("tablecellborderstyle", {
        icon: "border-style",
        text: "Border style",
        getSubmenuItems: () => buildMenuItems(editor, getTableBorderStyles(editor), "tablecellborderstyle", applyTableCellStyle(editor, "border-style")),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addNestedMenuItem("tablecellbackgroundcolor", {
        icon: "cell-background-color",
        text: "Background color",
        getSubmenuItems: () => buildColorMenu(editor, getTableBackgroundColorMap(editor), "background-color"),
        onSetup: selectionTargets.onSetupCellOrRow
      });
      editor.ui.registry.addNestedMenuItem("tablecellbordercolor", {
        icon: "cell-border-color",
        text: "Border color",
        getSubmenuItems: () => buildColorMenu(editor, getTableBorderColorMap(editor), "border-color"),
        onSetup: selectionTargets.onSetupCellOrRow
      });
    }
    addToggleMenuIfRegistered("tablecaption", {
      icon: "table-caption",
      text: "Table caption",
      command: "mceTableToggleCaption",
      onSetup: selectionTargets.onSetupTableWithCaption
    });
    addToggleMenuIfRegistered("tablerowheader", {
      text: "Row header",
      icon: "table-top-header",
      command: "mceTableRowType",
      onAction: changeRowHeader(editor),
      onSetup: selectionTargets.onSetupTableRowHeaders
    });
    addToggleMenuIfRegistered("tablecolheader", {
      text: "Column header",
      icon: "table-left-header",
      command: "mceTableColType",
      onAction: changeColumnHeader(editor),
      onSetup: selectionTargets.onSetupTableRowHeaders
    });
  };
  const Plugin = (editor) => {
    const selectionTargets = getSelectionTargets(editor);
    register(editor);
    registerCommands(editor);
    addMenuItems(editor, selectionTargets);
    addButtons(editor, selectionTargets);
    addToolbars(editor);
  };
  var Plugin$1 = () => {
    global$3.add("table", Plugin);
  };
  Plugin$1();
})();
(function() {
  "use strict";
  var global$5 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType = (type) => (value) => typeOf(value) === type;
  const isSimpleType = (type) => (value) => typeof value === type;
  const eq = (t) => (a) => t === a;
  const isString = isType("string");
  const isObject = isType("object");
  const isArray = isType("array");
  const isNull = eq(null);
  const isBoolean = isSimpleType("boolean");
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const isArrayOf = (value, pred) => {
    if (isArray(value)) {
      for (let i2 = 0, len = value.length; i2 < len; ++i2) {
        if (!pred(value[i2])) {
          return false;
        }
      }
      return true;
    }
    return false;
  };
  const noop = () => {
  };
  const constant = (value) => {
    return () => {
      return value;
    };
  };
  const tripleEquals = (a, b) => {
    return a === b;
  };
  class Optional {
    constructor(tag, value) {
      this.tag = tag;
      this.value = value;
    }
    static some(value) {
      return new Optional(true, value);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder) {
      if (this.tag) {
        return binder(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value) {
      return isNonNullable(value) ? Optional.some(value) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const nativeIndexOf = Array.prototype.indexOf;
  const nativePush = Array.prototype.push;
  const rawIndexOf = (ts, t) => nativeIndexOf.call(ts, t);
  const contains = (xs, x) => rawIndexOf(xs, x) > -1;
  const map = (xs, f) => {
    const len = xs.length;
    const r = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r[i2] = f(x, i2);
    }
    return r;
  };
  const each$1 = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const foldl = (xs, f, acc) => {
    each$1(xs, (x, i2) => {
      acc = f(acc, x, i2);
    });
    return acc;
  };
  const flatten = (xs) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      if (!isArray(xs[i2])) {
        throw new Error("Arr.flatten item " + i2 + " was not an array, input: " + xs);
      }
      nativePush.apply(r, xs[i2]);
    }
    return r;
  };
  const bind = (xs, f) => flatten(map(xs, f));
  const findMap = (arr, f) => {
    for (let i2 = 0; i2 < arr.length; i2++) {
      const r = f(arr[i2], i2);
      if (r.isSome()) {
        return r;
      }
    }
    return Optional.none();
  };
  const is = (lhs, rhs, comparator = tripleEquals) => lhs.exists((left) => comparator(left, rhs));
  const cat = (arr) => {
    const r = [];
    const push = (x) => {
      r.push(x);
    };
    for (let i2 = 0; i2 < arr.length; i2++) {
      arr[i2].each(push);
    }
    return r;
  };
  const someIf = (b, a) => b ? Optional.some(a) : Optional.none();
  const option = (name) => (editor) => editor.options.get(name);
  const register$1 = (editor) => {
    const registerOption = editor.options.register;
    registerOption("link_assume_external_targets", {
      processor: (value) => {
        const valid = isString(value) || isBoolean(value);
        if (valid) {
          if (value === true) {
            return {
              value: 1,
              valid
            };
          } else if (value === "http" || value === "https") {
            return {
              value,
              valid
            };
          } else {
            return {
              value: 0,
              valid
            };
          }
        } else {
          return {
            valid: false,
            message: "Must be a string or a boolean."
          };
        }
      },
      default: false
    });
    registerOption("link_context_toolbar", {
      processor: "boolean",
      default: false
    });
    registerOption("link_list", { processor: (value) => isString(value) || isFunction(value) || isArrayOf(value, isObject) });
    registerOption("link_default_target", { processor: "string" });
    registerOption("link_default_protocol", {
      processor: "string",
      default: "https"
    });
    registerOption("link_target_list", {
      processor: (value) => isBoolean(value) || isArrayOf(value, isObject),
      default: true
    });
    registerOption("link_rel_list", {
      processor: "object[]",
      default: []
    });
    registerOption("link_class_list", {
      processor: "object[]",
      default: []
    });
    registerOption("link_title", {
      processor: "boolean",
      default: true
    });
    registerOption("allow_unsafe_link_target", {
      processor: "boolean",
      default: false
    });
    registerOption("link_quicklink", {
      processor: "boolean",
      default: false
    });
  };
  const assumeExternalTargets = option("link_assume_external_targets");
  const hasContextToolbar = option("link_context_toolbar");
  const getLinkList = option("link_list");
  const getDefaultLinkTarget = option("link_default_target");
  const getDefaultLinkProtocol = option("link_default_protocol");
  const getTargetList = option("link_target_list");
  const getRelList = option("link_rel_list");
  const getLinkClassList = option("link_class_list");
  const shouldShowLinkTitle = option("link_title");
  const allowUnsafeLinkTarget = option("allow_unsafe_link_target");
  const useQuickLink = option("link_quicklink");
  var global$4 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const getValue = (item) => isString(item.value) ? item.value : "";
  const getText = (item) => {
    if (isString(item.text)) {
      return item.text;
    } else if (isString(item.title)) {
      return item.title;
    } else {
      return "";
    }
  };
  const sanitizeList = (list, extractValue) => {
    const out = [];
    global$4.each(list, (item) => {
      const text = getText(item);
      if (item.menu !== void 0) {
        const items = sanitizeList(item.menu, extractValue);
        out.push({
          text,
          items
        });
      } else {
        const value = extractValue(item);
        out.push({
          text,
          value
        });
      }
    });
    return out;
  };
  const sanitizeWith = (extracter = getValue) => (list) => Optional.from(list).map((list2) => sanitizeList(list2, extracter));
  const sanitize = (list) => sanitizeWith(getValue)(list);
  const createUi = (name, label) => (items) => ({
    name,
    type: "listbox",
    label,
    items
  });
  const ListOptions = {
    sanitize,
    sanitizeWith,
    createUi,
    getValue
  };
  const keys = Object.keys;
  const hasOwnProperty = Object.hasOwnProperty;
  const each = (obj, f) => {
    const props = keys(obj);
    for (let k = 0, len = props.length; k < len; k++) {
      const i2 = props[k];
      const x = obj[i2];
      f(x, i2);
    }
  };
  const objAcc = (r) => (x, i2) => {
    r[i2] = x;
  };
  const internalFilter = (obj, pred, onTrue, onFalse) => {
    each(obj, (x, i2) => {
      (pred(x, i2) ? onTrue : onFalse)(x, i2);
    });
  };
  const filter = (obj, pred) => {
    const t = {};
    internalFilter(obj, pred, objAcc(t), noop);
    return t;
  };
  const has = (obj, key) => hasOwnProperty.call(obj, key);
  const hasNonNullableKey = (obj, key) => has(obj, key) && obj[key] !== void 0 && obj[key] !== null;
  var global$3 = tinymce.util.Tools.resolve("tinymce.dom.TreeWalker");
  var global$2 = tinymce.util.Tools.resolve("tinymce.util.URI");
  const isAnchor = (elm) => isNonNullable(elm) && elm.nodeName.toLowerCase() === "a";
  const isLink = (elm) => isAnchor(elm) && !!getHref(elm);
  const collectNodesInRange = (rng, predicate) => {
    if (rng.collapsed) {
      return [];
    } else {
      const contents = rng.cloneContents();
      const firstChild = contents.firstChild;
      const walker = new global$3(firstChild, contents);
      const elements = [];
      let current = firstChild;
      do {
        if (predicate(current)) {
          elements.push(current);
        }
      } while (current = walker.next());
      return elements;
    }
  };
  const hasProtocol = (url) => /^\w+:/i.test(url);
  const getHref = (elm) => {
    var _a, _b;
    return (_b = (_a = elm.getAttribute("data-mce-href")) !== null && _a !== void 0 ? _a : elm.getAttribute("href")) !== null && _b !== void 0 ? _b : "";
  };
  const applyRelTargetRules = (rel, isUnsafe) => {
    const rules = ["noopener"];
    const rels = rel ? rel.split(/\s+/) : [];
    const toString = (rels2) => global$4.trim(rels2.sort().join(" "));
    const addTargetRules = (rels2) => {
      rels2 = removeTargetRules(rels2);
      return rels2.length > 0 ? rels2.concat(rules) : rules;
    };
    const removeTargetRules = (rels2) => rels2.filter((val) => global$4.inArray(rules, val) === -1);
    const newRels = isUnsafe ? addTargetRules(rels) : removeTargetRules(rels);
    return newRels.length > 0 ? toString(newRels) : "";
  };
  const trimCaretContainers = (text) => text.replace(/\uFEFF/g, "");
  const getAnchorElement = (editor, selectedElm) => {
    selectedElm = selectedElm || editor.selection.getNode();
    if (isImageFigure(selectedElm)) {
      return Optional.from(editor.dom.select("a[href]", selectedElm)[0]);
    } else {
      return Optional.from(editor.dom.getParent(selectedElm, "a[href]"));
    }
  };
  const isInAnchor = (editor, selectedElm) => getAnchorElement(editor, selectedElm).isSome();
  const getAnchorText = (selection, anchorElm) => {
    const text = anchorElm.fold(() => selection.getContent({ format: "text" }), (anchorElm2) => anchorElm2.innerText || anchorElm2.textContent || "");
    return trimCaretContainers(text);
  };
  const hasLinks = (elements) => global$4.grep(elements, isLink).length > 0;
  const hasLinksInSelection = (rng) => collectNodesInRange(rng, isLink).length > 0;
  const isOnlyTextSelected = (editor) => {
    const inlineTextElements = editor.schema.getTextInlineElements();
    const isElement = (elm) => elm.nodeType === 1 && !isAnchor(elm) && !has(inlineTextElements, elm.nodeName.toLowerCase());
    const elements = collectNodesInRange(editor.selection.getRng(), isElement);
    return elements.length === 0;
  };
  const isImageFigure = (elm) => isNonNullable(elm) && elm.nodeName === "FIGURE" && /\bimage\b/i.test(elm.className);
  const getLinkAttrs = (data) => {
    const attrs = [
      "title",
      "rel",
      "class",
      "target"
    ];
    return foldl(attrs, (acc, key) => {
      data[key].each((value) => {
        acc[key] = value.length > 0 ? value : null;
      });
      return acc;
    }, { href: data.href });
  };
  const handleExternalTargets = (href, assumeExternalTargets2) => {
    if ((assumeExternalTargets2 === "http" || assumeExternalTargets2 === "https") && !hasProtocol(href)) {
      return assumeExternalTargets2 + "://" + href;
    }
    return href;
  };
  const applyLinkOverrides = (editor, linkAttrs) => {
    const newLinkAttrs = { ...linkAttrs };
    if (getRelList(editor).length === 0 && !allowUnsafeLinkTarget(editor)) {
      const newRel = applyRelTargetRules(newLinkAttrs.rel, newLinkAttrs.target === "_blank");
      newLinkAttrs.rel = newRel ? newRel : null;
    }
    if (Optional.from(newLinkAttrs.target).isNone() && getTargetList(editor) === false) {
      newLinkAttrs.target = getDefaultLinkTarget(editor);
    }
    newLinkAttrs.href = handleExternalTargets(newLinkAttrs.href, assumeExternalTargets(editor));
    return newLinkAttrs;
  };
  const updateLink = (editor, anchorElm, text, linkAttrs) => {
    text.each((text2) => {
      if (has(anchorElm, "innerText")) {
        anchorElm.innerText = text2;
      } else {
        anchorElm.textContent = text2;
      }
    });
    editor.dom.setAttribs(anchorElm, linkAttrs);
    editor.selection.select(anchorElm);
  };
  const createLink = (editor, selectedElm, text, linkAttrs) => {
    const dom = editor.dom;
    if (isImageFigure(selectedElm)) {
      linkImageFigure(dom, selectedElm, linkAttrs);
    } else {
      text.fold(() => {
        editor.execCommand("mceInsertLink", false, linkAttrs);
      }, (text2) => {
        editor.insertContent(dom.createHTML("a", linkAttrs, dom.encode(text2)));
      });
    }
  };
  const linkDomMutation = (editor, attachState, data) => {
    const selectedElm = editor.selection.getNode();
    const anchorElm = getAnchorElement(editor, selectedElm);
    const linkAttrs = applyLinkOverrides(editor, getLinkAttrs(data));
    editor.undoManager.transact(() => {
      if (data.href === attachState.href) {
        attachState.attach();
      }
      anchorElm.fold(() => {
        createLink(editor, selectedElm, data.text, linkAttrs);
      }, (elm) => {
        editor.focus();
        updateLink(editor, elm, data.text, linkAttrs);
      });
    });
  };
  const unlinkSelection = (editor) => {
    const dom = editor.dom, selection = editor.selection;
    const bookmark = selection.getBookmark();
    const rng = selection.getRng().cloneRange();
    const startAnchorElm = dom.getParent(rng.startContainer, "a[href]", editor.getBody());
    const endAnchorElm = dom.getParent(rng.endContainer, "a[href]", editor.getBody());
    if (startAnchorElm) {
      rng.setStartBefore(startAnchorElm);
    }
    if (endAnchorElm) {
      rng.setEndAfter(endAnchorElm);
    }
    selection.setRng(rng);
    editor.execCommand("unlink");
    selection.moveToBookmark(bookmark);
  };
  const unlinkDomMutation = (editor) => {
    editor.undoManager.transact(() => {
      const node = editor.selection.getNode();
      if (isImageFigure(node)) {
        unlinkImageFigure(editor, node);
      } else {
        unlinkSelection(editor);
      }
      editor.focus();
    });
  };
  const unwrapOptions = (data) => {
    const {
      class: cls,
      href,
      rel,
      target,
      text,
      title
    } = data;
    return filter({
      class: cls.getOrNull(),
      href,
      rel: rel.getOrNull(),
      target: target.getOrNull(),
      text: text.getOrNull(),
      title: title.getOrNull()
    }, (v, _k) => isNull(v) === false);
  };
  const sanitizeData = (editor, data) => {
    const getOption = editor.options.get;
    const uriOptions = {
      allow_html_data_urls: getOption("allow_html_data_urls"),
      allow_script_urls: getOption("allow_script_urls"),
      allow_svg_data_urls: getOption("allow_svg_data_urls")
    };
    const href = data.href;
    return {
      ...data,
      href: global$2.isDomSafe(href, "a", uriOptions) ? href : ""
    };
  };
  const link = (editor, attachState, data) => {
    const sanitizedData = sanitizeData(editor, data);
    editor.hasPlugin("rtc", true) ? editor.execCommand("createlink", false, unwrapOptions(sanitizedData)) : linkDomMutation(editor, attachState, sanitizedData);
  };
  const unlink = (editor) => {
    editor.hasPlugin("rtc", true) ? editor.execCommand("unlink") : unlinkDomMutation(editor);
  };
  const unlinkImageFigure = (editor, fig) => {
    var _a;
    const img = editor.dom.select("img", fig)[0];
    if (img) {
      const a = editor.dom.getParents(img, "a[href]", fig)[0];
      if (a) {
        (_a = a.parentNode) === null || _a === void 0 ? void 0 : _a.insertBefore(img, a);
        editor.dom.remove(a);
      }
    }
  };
  const linkImageFigure = (dom, fig, attrs) => {
    var _a;
    const img = dom.select("img", fig)[0];
    if (img) {
      const a = dom.create("a", attrs);
      (_a = img.parentNode) === null || _a === void 0 ? void 0 : _a.insertBefore(a, img);
      a.appendChild(img);
    }
  };
  const isListGroup = (item) => hasNonNullableKey(item, "items");
  const findTextByValue = (value, catalog) => findMap(catalog, (item) => {
    if (isListGroup(item)) {
      return findTextByValue(value, item.items);
    } else {
      return someIf(item.value === value, item);
    }
  });
  const getDelta = (persistentText, fieldName, catalog, data) => {
    const value = data[fieldName];
    const hasPersistentText = persistentText.length > 0;
    return value !== void 0 ? findTextByValue(value, catalog).map((i2) => ({
      url: {
        value: i2.value,
        meta: {
          text: hasPersistentText ? persistentText : i2.text,
          attach: noop
        }
      },
      text: hasPersistentText ? persistentText : i2.text
    })) : Optional.none();
  };
  const findCatalog = (catalogs, fieldName) => {
    if (fieldName === "link") {
      return catalogs.link;
    } else if (fieldName === "anchor") {
      return catalogs.anchor;
    } else {
      return Optional.none();
    }
  };
  const init = (initialData, linkCatalog) => {
    const persistentData = {
      text: initialData.text,
      title: initialData.title
    };
    const getTitleFromUrlChange = (url) => {
      var _a;
      return someIf(persistentData.title.length <= 0, Optional.from((_a = url.meta) === null || _a === void 0 ? void 0 : _a.title).getOr(""));
    };
    const getTextFromUrlChange = (url) => {
      var _a;
      return someIf(persistentData.text.length <= 0, Optional.from((_a = url.meta) === null || _a === void 0 ? void 0 : _a.text).getOr(url.value));
    };
    const onUrlChange = (data) => {
      const text = getTextFromUrlChange(data.url);
      const title = getTitleFromUrlChange(data.url);
      if (text.isSome() || title.isSome()) {
        return Optional.some({
          ...text.map((text2) => ({ text: text2 })).getOr({}),
          ...title.map((title2) => ({ title: title2 })).getOr({})
        });
      } else {
        return Optional.none();
      }
    };
    const onCatalogChange = (data, change) => {
      const catalog = findCatalog(linkCatalog, change).getOr([]);
      return getDelta(persistentData.text, change, catalog, data);
    };
    const onChange = (getData, change) => {
      const name = change.name;
      if (name === "url") {
        return onUrlChange(getData());
      } else if (contains([
        "anchor",
        "link"
      ], name)) {
        return onCatalogChange(getData(), name);
      } else if (name === "text" || name === "title") {
        persistentData[name] = getData()[name];
        return Optional.none();
      } else {
        return Optional.none();
      }
    };
    return { onChange };
  };
  const DialogChanges = {
    init,
    getDelta
  };
  var global$1 = tinymce.util.Tools.resolve("tinymce.util.Delay");
  const delayedConfirm = (editor, message, callback) => {
    const rng = editor.selection.getRng();
    global$1.setEditorTimeout(editor, () => {
      editor.windowManager.confirm(message, (state) => {
        editor.selection.setRng(rng);
        callback(state);
      });
    });
  };
  const tryEmailTransform = (data) => {
    const url = data.href;
    const suggestMailTo = url.indexOf("@") > 0 && url.indexOf("/") === -1 && url.indexOf("mailto:") === -1;
    return suggestMailTo ? Optional.some({
      message: "The URL you entered seems to be an email address. Do you want to add the required mailto: prefix?",
      preprocess: (oldData) => ({
        ...oldData,
        href: "mailto:" + url
      })
    }) : Optional.none();
  };
  const tryProtocolTransform = (assumeExternalTargets2, defaultLinkProtocol) => (data) => {
    const url = data.href;
    const suggestProtocol = assumeExternalTargets2 === 1 && !hasProtocol(url) || assumeExternalTargets2 === 0 && /^\s*www(\.|\d\.)/i.test(url);
    return suggestProtocol ? Optional.some({
      message: `The URL you entered seems to be an external link. Do you want to add the required ${defaultLinkProtocol}:// prefix?`,
      preprocess: (oldData) => ({
        ...oldData,
        href: defaultLinkProtocol + "://" + url
      })
    }) : Optional.none();
  };
  const preprocess = (editor, data) => findMap([
    tryEmailTransform,
    tryProtocolTransform(assumeExternalTargets(editor), getDefaultLinkProtocol(editor))
  ], (f) => f(data)).fold(() => Promise.resolve(data), (transform) => new Promise((callback) => {
    delayedConfirm(editor, transform.message, (state) => {
      callback(state ? transform.preprocess(data) : data);
    });
  }));
  const DialogConfirms = { preprocess };
  const getAnchors = (editor) => {
    const anchorNodes = editor.dom.select("a:not([href])");
    const anchors = bind(anchorNodes, (anchor) => {
      const id = anchor.name || anchor.id;
      return id ? [{
        text: id,
        value: "#" + id
      }] : [];
    });
    return anchors.length > 0 ? Optional.some([{
      text: "None",
      value: ""
    }].concat(anchors)) : Optional.none();
  };
  const AnchorListOptions = { getAnchors };
  const getClasses = (editor) => {
    const list = getLinkClassList(editor);
    if (list.length > 0) {
      return ListOptions.sanitize(list);
    }
    return Optional.none();
  };
  const ClassListOptions = { getClasses };
  const parseJson = (text) => {
    try {
      return Optional.some(JSON.parse(text));
    } catch (err) {
      return Optional.none();
    }
  };
  const getLinks = (editor) => {
    const extractor = (item) => editor.convertURL(item.value || item.url || "", "href");
    const linkList = getLinkList(editor);
    return new Promise((resolve) => {
      if (isString(linkList)) {
        fetch(linkList).then((res) => res.ok ? res.text().then(parseJson) : Promise.reject()).then(resolve, () => resolve(Optional.none()));
      } else if (isFunction(linkList)) {
        linkList((output) => resolve(Optional.some(output)));
      } else {
        resolve(Optional.from(linkList));
      }
    }).then((optItems) => optItems.bind(ListOptions.sanitizeWith(extractor)).map((items) => {
      if (items.length > 0) {
        const noneItem = [{
          text: "None",
          value: ""
        }];
        return noneItem.concat(items);
      } else {
        return items;
      }
    }));
  };
  const LinkListOptions = { getLinks };
  const getRels = (editor, initialTarget) => {
    const list = getRelList(editor);
    if (list.length > 0) {
      const isTargetBlank = is(initialTarget, "_blank");
      const enforceSafe = allowUnsafeLinkTarget(editor) === false;
      const safeRelExtractor = (item) => applyRelTargetRules(ListOptions.getValue(item), isTargetBlank);
      const sanitizer = enforceSafe ? ListOptions.sanitizeWith(safeRelExtractor) : ListOptions.sanitize;
      return sanitizer(list);
    }
    return Optional.none();
  };
  const RelOptions = { getRels };
  const fallbacks = [
    {
      text: "Current window",
      value: ""
    },
    {
      text: "New window",
      value: "_blank"
    }
  ];
  const getTargets = (editor) => {
    const list = getTargetList(editor);
    if (isArray(list)) {
      return ListOptions.sanitize(list).orThunk(() => Optional.some(fallbacks));
    } else if (list === false) {
      return Optional.none();
    }
    return Optional.some(fallbacks);
  };
  const TargetOptions = { getTargets };
  const nonEmptyAttr = (dom, elem, name) => {
    const val = dom.getAttrib(elem, name);
    return val !== null && val.length > 0 ? Optional.some(val) : Optional.none();
  };
  const extractFromAnchor = (editor, anchor) => {
    const dom = editor.dom;
    const onlyText = isOnlyTextSelected(editor);
    const text = onlyText ? Optional.some(getAnchorText(editor.selection, anchor)) : Optional.none();
    const url = anchor.bind((anchorElm) => Optional.from(dom.getAttrib(anchorElm, "href")));
    const target = anchor.bind((anchorElm) => Optional.from(dom.getAttrib(anchorElm, "target")));
    const rel = anchor.bind((anchorElm) => nonEmptyAttr(dom, anchorElm, "rel"));
    const linkClass = anchor.bind((anchorElm) => nonEmptyAttr(dom, anchorElm, "class"));
    const title = anchor.bind((anchorElm) => nonEmptyAttr(dom, anchorElm, "title"));
    return {
      url,
      text,
      title,
      target,
      rel,
      linkClass
    };
  };
  const collect = (editor, linkNode) => LinkListOptions.getLinks(editor).then((links) => {
    const anchor = extractFromAnchor(editor, linkNode);
    return {
      anchor,
      catalogs: {
        targets: TargetOptions.getTargets(editor),
        rels: RelOptions.getRels(editor, anchor.target),
        classes: ClassListOptions.getClasses(editor),
        anchor: AnchorListOptions.getAnchors(editor),
        link: links
      },
      optNode: linkNode,
      flags: { titleEnabled: shouldShowLinkTitle(editor) }
    };
  });
  const DialogInfo = { collect };
  const handleSubmit = (editor, info) => (api) => {
    const data = api.getData();
    if (!data.url.value) {
      unlink(editor);
      api.close();
      return;
    }
    const getChangedValue = (key) => Optional.from(data[key]).filter((value) => !is(info.anchor[key], value));
    const changedData = {
      href: data.url.value,
      text: getChangedValue("text"),
      target: getChangedValue("target"),
      rel: getChangedValue("rel"),
      class: getChangedValue("linkClass"),
      title: getChangedValue("title")
    };
    const attachState = {
      href: data.url.value,
      attach: data.url.meta !== void 0 && data.url.meta.attach ? data.url.meta.attach : noop
    };
    DialogConfirms.preprocess(editor, changedData).then((pData) => {
      link(editor, attachState, pData);
    });
    api.close();
  };
  const collectData = (editor) => {
    const anchorNode = getAnchorElement(editor);
    return DialogInfo.collect(editor, anchorNode);
  };
  const getInitialData = (info, defaultTarget) => {
    const anchor = info.anchor;
    const url = anchor.url.getOr("");
    return {
      url: {
        value: url,
        meta: { original: { value: url } }
      },
      text: anchor.text.getOr(""),
      title: anchor.title.getOr(""),
      anchor: url,
      link: url,
      rel: anchor.rel.getOr(""),
      target: anchor.target.or(defaultTarget).getOr(""),
      linkClass: anchor.linkClass.getOr("")
    };
  };
  const makeDialog = (settings, onSubmit, editor) => {
    const urlInput = [{
      name: "url",
      type: "urlinput",
      filetype: "file",
      label: "URL"
    }];
    const displayText = settings.anchor.text.map(() => ({
      name: "text",
      type: "input",
      label: "Text to display"
    })).toArray();
    const titleText = settings.flags.titleEnabled ? [{
      name: "title",
      type: "input",
      label: "Title"
    }] : [];
    const defaultTarget = Optional.from(getDefaultLinkTarget(editor));
    const initialData = getInitialData(settings, defaultTarget);
    const catalogs = settings.catalogs;
    const dialogDelta = DialogChanges.init(initialData, catalogs);
    const body = {
      type: "panel",
      items: flatten([
        urlInput,
        displayText,
        titleText,
        cat([
          catalogs.anchor.map(ListOptions.createUi("anchor", "Anchors")),
          catalogs.rels.map(ListOptions.createUi("rel", "Rel")),
          catalogs.targets.map(ListOptions.createUi("target", "Open link in...")),
          catalogs.link.map(ListOptions.createUi("link", "Link list")),
          catalogs.classes.map(ListOptions.createUi("linkClass", "Class"))
        ])
      ])
    };
    return {
      title: "Insert/Edit Link",
      size: "normal",
      body,
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData,
      onChange: (api, { name }) => {
        dialogDelta.onChange(api.getData, { name }).each((newData) => {
          api.setData(newData);
        });
      },
      onSubmit
    };
  };
  const open$1 = (editor) => {
    const data = collectData(editor);
    data.then((info) => {
      const onSubmit = handleSubmit(editor, info);
      return makeDialog(info, onSubmit, editor);
    }).then((spec) => {
      editor.windowManager.open(spec);
    });
  };
  const register = (editor) => {
    editor.addCommand("mceLink", (_ui, value) => {
      if ((value === null || value === void 0 ? void 0 : value.dialog) === true || !useQuickLink(editor)) {
        open$1(editor);
      } else {
        editor.dispatch("contexttoolbar-show", { toolbarKey: "quicklink" });
      }
    });
  };
  var global2 = tinymce.util.Tools.resolve("tinymce.util.VK");
  const appendClickRemove = (link2, evt) => {
    document.body.appendChild(link2);
    link2.dispatchEvent(evt);
    document.body.removeChild(link2);
  };
  const open = (url) => {
    const link2 = document.createElement("a");
    link2.target = "_blank";
    link2.href = url;
    link2.rel = "noreferrer noopener";
    const evt = document.createEvent("MouseEvents");
    evt.initMouseEvent("click", true, true, window, 0, 0, 0, 0, 0, false, false, false, false, 0, null);
    appendClickRemove(link2, evt);
  };
  const getLink = (editor, elm) => editor.dom.getParent(elm, "a[href]");
  const getSelectedLink = (editor) => getLink(editor, editor.selection.getStart());
  const hasOnlyAltModifier = (e) => {
    return e.altKey === true && e.shiftKey === false && e.ctrlKey === false && e.metaKey === false;
  };
  const gotoLink = (editor, a) => {
    if (a) {
      const href = getHref(a);
      if (/^#/.test(href)) {
        const targetEl = editor.dom.select(href);
        if (targetEl.length) {
          editor.selection.scrollIntoView(targetEl[0], true);
        }
      } else {
        open(a.href);
      }
    }
  };
  const openDialog = (editor) => () => {
    editor.execCommand("mceLink", false, { dialog: true });
  };
  const gotoSelectedLink = (editor) => () => {
    gotoLink(editor, getSelectedLink(editor));
  };
  const setupGotoLinks = (editor) => {
    editor.on("click", (e) => {
      const link2 = getLink(editor, e.target);
      if (link2 && global2.metaKeyPressed(e)) {
        e.preventDefault();
        gotoLink(editor, link2);
      }
    });
    editor.on("keydown", (e) => {
      if (!e.isDefaultPrevented() && e.keyCode === 13 && hasOnlyAltModifier(e)) {
        const link2 = getSelectedLink(editor);
        if (link2) {
          e.preventDefault();
          gotoLink(editor, link2);
        }
      }
    });
  };
  const toggleState = (editor, toggler) => {
    editor.on("NodeChange", toggler);
    return () => editor.off("NodeChange", toggler);
  };
  const toggleActiveState = (editor) => (api) => {
    const updateState = () => api.setActive(!editor.mode.isReadOnly() && isInAnchor(editor, editor.selection.getNode()));
    updateState();
    return toggleState(editor, updateState);
  };
  const toggleEnabledState = (editor) => (api) => {
    const updateState = () => api.setEnabled(isInAnchor(editor, editor.selection.getNode()));
    updateState();
    return toggleState(editor, updateState);
  };
  const toggleUnlinkState = (editor) => (api) => {
    const hasLinks$1 = (parents2) => hasLinks(parents2) || hasLinksInSelection(editor.selection.getRng());
    const parents = editor.dom.getParents(editor.selection.getStart());
    api.setEnabled(hasLinks$1(parents));
    return toggleState(editor, (e) => api.setEnabled(hasLinks$1(e.parents)));
  };
  const setup = (editor) => {
    editor.addShortcut("Meta+K", "", () => {
      editor.execCommand("mceLink");
    });
  };
  const setupButtons = (editor) => {
    editor.ui.registry.addToggleButton("link", {
      icon: "link",
      tooltip: "Insert/edit link",
      onAction: openDialog(editor),
      onSetup: toggleActiveState(editor)
    });
    editor.ui.registry.addButton("openlink", {
      icon: "new-tab",
      tooltip: "Open link",
      onAction: gotoSelectedLink(editor),
      onSetup: toggleEnabledState(editor)
    });
    editor.ui.registry.addButton("unlink", {
      icon: "unlink",
      tooltip: "Remove link",
      onAction: () => unlink(editor),
      onSetup: toggleUnlinkState(editor)
    });
  };
  const setupMenuItems = (editor) => {
    editor.ui.registry.addMenuItem("openlink", {
      text: "Open link",
      icon: "new-tab",
      onAction: gotoSelectedLink(editor),
      onSetup: toggleEnabledState(editor)
    });
    editor.ui.registry.addMenuItem("link", {
      icon: "link",
      text: "Link...",
      shortcut: "Meta+K",
      onAction: openDialog(editor)
    });
    editor.ui.registry.addMenuItem("unlink", {
      icon: "unlink",
      text: "Remove link",
      onAction: () => unlink(editor),
      onSetup: toggleUnlinkState(editor)
    });
  };
  const setupContextMenu = (editor) => {
    const inLink = "link unlink openlink";
    const noLink = "link";
    editor.ui.registry.addContextMenu("link", { update: (element) => hasLinks(editor.dom.getParents(element, "a")) ? inLink : noLink });
  };
  const setupContextToolbars = (editor) => {
    const collapseSelectionToEnd = (editor2) => {
      editor2.selection.collapse(false);
    };
    const onSetupLink = (buttonApi) => {
      const node = editor.selection.getNode();
      buttonApi.setEnabled(isInAnchor(editor, node));
      return noop;
    };
    const getLinkText = (value) => {
      const anchor = getAnchorElement(editor);
      const onlyText = isOnlyTextSelected(editor);
      if (anchor.isNone() && onlyText) {
        const text = getAnchorText(editor.selection, anchor);
        return Optional.some(text.length > 0 ? text : value);
      } else {
        return Optional.none();
      }
    };
    editor.ui.registry.addContextForm("quicklink", {
      launch: {
        type: "contextformtogglebutton",
        icon: "link",
        tooltip: "Link",
        onSetup: toggleActiveState(editor)
      },
      label: "Link",
      predicate: (node) => hasContextToolbar(editor) && isInAnchor(editor, node),
      initValue: () => {
        const elm = getAnchorElement(editor);
        return elm.fold(constant(""), getHref);
      },
      commands: [
        {
          type: "contextformtogglebutton",
          icon: "link",
          tooltip: "Link",
          primary: true,
          onSetup: (buttonApi) => {
            const node = editor.selection.getNode();
            buttonApi.setActive(isInAnchor(editor, node));
            return toggleActiveState(editor)(buttonApi);
          },
          onAction: (formApi) => {
            const value = formApi.getValue();
            const text = getLinkText(value);
            const attachState = {
              href: value,
              attach: noop
            };
            link(editor, attachState, {
              href: value,
              text,
              title: Optional.none(),
              rel: Optional.none(),
              target: Optional.none(),
              class: Optional.none()
            });
            collapseSelectionToEnd(editor);
            formApi.hide();
          }
        },
        {
          type: "contextformbutton",
          icon: "unlink",
          tooltip: "Remove link",
          onSetup: onSetupLink,
          onAction: (formApi) => {
            unlink(editor);
            formApi.hide();
          }
        },
        {
          type: "contextformbutton",
          icon: "new-tab",
          tooltip: "Open link",
          onSetup: onSetupLink,
          onAction: (formApi) => {
            gotoSelectedLink(editor)();
            formApi.hide();
          }
        }
      ]
    });
  };
  var Plugin = () => {
    global$5.add("link", (editor) => {
      register$1(editor);
      setupButtons(editor);
      setupMenuItems(editor);
      setupContextMenu(editor);
      setupContextToolbars(editor);
      setupGotoLinks(editor);
      register(editor);
      setup(editor);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$6 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType$1 = (type2) => (value) => typeOf(value) === type2;
  const isSimpleType = (type2) => (value) => typeof value === type2;
  const isString = isType$1("string");
  const isObject = isType$1("object");
  const isArray = isType$1("array");
  const isBoolean = isSimpleType("boolean");
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const isNumber = isSimpleType("number");
  const noop = () => {
  };
  const constant = (value) => {
    return () => {
      return value;
    };
  };
  const tripleEquals = (a, b) => {
    return a === b;
  };
  const not = (f) => (t) => !f(t);
  const never = constant(false);
  class Optional {
    constructor(tag, value) {
      this.tag = tag;
      this.value = value;
    }
    static some(value) {
      return new Optional(true, value);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder) {
      if (this.tag) {
        return binder(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value) {
      return isNonNullable(value) ? Optional.some(value) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const nativeSlice = Array.prototype.slice;
  const nativeIndexOf = Array.prototype.indexOf;
  const nativePush = Array.prototype.push;
  const rawIndexOf = (ts, t) => nativeIndexOf.call(ts, t);
  const contains$1 = (xs, x) => rawIndexOf(xs, x) > -1;
  const exists = (xs, pred) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return true;
      }
    }
    return false;
  };
  const map = (xs, f) => {
    const len = xs.length;
    const r = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r[i2] = f(x, i2);
    }
    return r;
  };
  const each$1 = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const filter$1 = (xs, pred) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        r.push(x);
      }
    }
    return r;
  };
  const groupBy = (xs, f) => {
    if (xs.length === 0) {
      return [];
    } else {
      let wasType = f(xs[0]);
      const r = [];
      let group = [];
      for (let i2 = 0, len = xs.length; i2 < len; i2++) {
        const x = xs[i2];
        const type2 = f(x);
        if (type2 !== wasType) {
          r.push(group);
          group = [];
        }
        wasType = type2;
        group.push(x);
      }
      if (group.length !== 0) {
        r.push(group);
      }
      return r;
    }
  };
  const foldl = (xs, f, acc) => {
    each$1(xs, (x, i2) => {
      acc = f(acc, x, i2);
    });
    return acc;
  };
  const findUntil = (xs, pred, until) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return Optional.some(x);
      } else if (until(x, i2)) {
        break;
      }
    }
    return Optional.none();
  };
  const find = (xs, pred) => {
    return findUntil(xs, pred, never);
  };
  const flatten = (xs) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      if (!isArray(xs[i2])) {
        throw new Error("Arr.flatten item " + i2 + " was not an array, input: " + xs);
      }
      nativePush.apply(r, xs[i2]);
    }
    return r;
  };
  const bind = (xs, f) => flatten(map(xs, f));
  const reverse = (xs) => {
    const r = nativeSlice.call(xs, 0);
    r.reverse();
    return r;
  };
  const get$1 = (xs, i2) => i2 >= 0 && i2 < xs.length ? Optional.some(xs[i2]) : Optional.none();
  const head = (xs) => get$1(xs, 0);
  const last = (xs) => get$1(xs, xs.length - 1);
  const unique = (xs, comparator) => {
    const r = [];
    const isDuplicated = isFunction(comparator) ? (x) => exists(r, (i2) => comparator(i2, x)) : (x) => contains$1(r, x);
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (!isDuplicated(x)) {
        r.push(x);
      }
    }
    return r;
  };
  const is$2 = (lhs, rhs, comparator = tripleEquals) => lhs.exists((left) => comparator(left, rhs));
  const equals = (lhs, rhs, comparator = tripleEquals) => lift2(lhs, rhs, comparator).getOr(lhs.isNone() && rhs.isNone());
  const lift2 = (oa, ob, f) => oa.isSome() && ob.isSome() ? Optional.some(f(oa.getOrDie(), ob.getOrDie())) : Optional.none();
  const ELEMENT = 1;
  const fromHtml = (html, scope) => {
    const doc = scope || document;
    const div = doc.createElement("div");
    div.innerHTML = html;
    if (!div.hasChildNodes() || div.childNodes.length > 1) {
      const message = "HTML does not have a single root node";
      console.error(message, html);
      throw new Error(message);
    }
    return fromDom$1(div.childNodes[0]);
  };
  const fromTag = (tag, scope) => {
    const doc = scope || document;
    const node = doc.createElement(tag);
    return fromDom$1(node);
  };
  const fromText = (text, scope) => {
    const doc = scope || document;
    const node = doc.createTextNode(text);
    return fromDom$1(node);
  };
  const fromDom$1 = (node) => {
    if (node === null || node === void 0) {
      throw new Error("Node cannot be null or undefined");
    }
    return { dom: node };
  };
  const fromPoint = (docElm, x, y) => Optional.from(docElm.dom.elementFromPoint(x, y)).map(fromDom$1);
  const SugarElement = {
    fromHtml,
    fromTag,
    fromText,
    fromDom: fromDom$1,
    fromPoint
  };
  const is$1 = (element, selector) => {
    const dom = element.dom;
    if (dom.nodeType !== ELEMENT) {
      return false;
    } else {
      const elem = dom;
      if (elem.matches !== void 0) {
        return elem.matches(selector);
      } else if (elem.msMatchesSelector !== void 0) {
        return elem.msMatchesSelector(selector);
      } else if (elem.webkitMatchesSelector !== void 0) {
        return elem.webkitMatchesSelector(selector);
      } else if (elem.mozMatchesSelector !== void 0) {
        return elem.mozMatchesSelector(selector);
      } else {
        throw new Error("Browser lacks native selectors");
      }
    }
  };
  const eq = (e1, e2) => e1.dom === e2.dom;
  const contains = (e1, e2) => {
    const d1 = e1.dom;
    const d2 = e2.dom;
    return d1 === d2 ? false : d1.contains(d2);
  };
  const is = is$1;
  var ClosestOrAncestor = (is2, ancestor2, scope, a, isRoot) => {
    if (is2(scope, a)) {
      return Optional.some(scope);
    } else if (isFunction(isRoot) && isRoot(scope)) {
      return Optional.none();
    } else {
      return ancestor2(scope, a, isRoot);
    }
  };
  typeof window !== "undefined" ? window : Function("return this;")();
  const name = (element) => {
    const r = element.dom.nodeName;
    return r.toLowerCase();
  };
  const type = (element) => element.dom.nodeType;
  const isType = (t) => (element) => type(element) === t;
  const isElement$1 = isType(ELEMENT);
  const isTag = (tag) => (e) => isElement$1(e) && name(e) === tag;
  const parent = (element) => Optional.from(element.dom.parentNode).map(SugarElement.fromDom);
  const parentElement = (element) => Optional.from(element.dom.parentElement).map(SugarElement.fromDom);
  const nextSibling = (element) => Optional.from(element.dom.nextSibling).map(SugarElement.fromDom);
  const children = (element) => map(element.dom.childNodes, SugarElement.fromDom);
  const child = (element, index) => {
    const cs = element.dom.childNodes;
    return Optional.from(cs[index]).map(SugarElement.fromDom);
  };
  const firstChild = (element) => child(element, 0);
  const lastChild = (element) => child(element, element.dom.childNodes.length - 1);
  const ancestor = (scope, predicate, isRoot) => {
    let element = scope.dom;
    const stop = isFunction(isRoot) ? isRoot : never;
    while (element.parentNode) {
      element = element.parentNode;
      const el = SugarElement.fromDom(element);
      if (predicate(el)) {
        return Optional.some(el);
      } else if (stop(el)) {
        break;
      }
    }
    return Optional.none();
  };
  const closest = (scope, predicate, isRoot) => {
    const is2 = (s, test) => test(s);
    return ClosestOrAncestor(is2, ancestor, scope, predicate, isRoot);
  };
  const before$1 = (marker, element) => {
    const parent$1 = parent(marker);
    parent$1.each((v) => {
      v.dom.insertBefore(element.dom, marker.dom);
    });
  };
  const after = (marker, element) => {
    const sibling = nextSibling(marker);
    sibling.fold(() => {
      const parent$1 = parent(marker);
      parent$1.each((v) => {
        append$1(v, element);
      });
    }, (v) => {
      before$1(v, element);
    });
  };
  const append$1 = (parent2, element) => {
    parent2.dom.appendChild(element.dom);
  };
  const before = (marker, elements) => {
    each$1(elements, (x) => {
      before$1(marker, x);
    });
  };
  const append = (parent2, elements) => {
    each$1(elements, (x) => {
      append$1(parent2, x);
    });
  };
  const empty = (element) => {
    element.dom.textContent = "";
    each$1(children(element), (rogue) => {
      remove(rogue);
    });
  };
  const remove = (element) => {
    const dom = element.dom;
    if (dom.parentNode !== null) {
      dom.parentNode.removeChild(dom);
    }
  };
  var global$5 = tinymce.util.Tools.resolve("tinymce.dom.RangeUtils");
  var global$4 = tinymce.util.Tools.resolve("tinymce.dom.TreeWalker");
  var global$3 = tinymce.util.Tools.resolve("tinymce.util.VK");
  const fromDom = (nodes) => map(nodes, SugarElement.fromDom);
  const keys = Object.keys;
  const each = (obj, f) => {
    const props = keys(obj);
    for (let k = 0, len = props.length; k < len; k++) {
      const i2 = props[k];
      const x = obj[i2];
      f(x, i2);
    }
  };
  const objAcc = (r) => (x, i2) => {
    r[i2] = x;
  };
  const internalFilter = (obj, pred, onTrue, onFalse) => {
    each(obj, (x, i2) => {
      (pred(x, i2) ? onTrue : onFalse)(x, i2);
    });
  };
  const filter = (obj, pred) => {
    const t = {};
    internalFilter(obj, pred, objAcc(t), noop);
    return t;
  };
  const rawSet = (dom, key, value) => {
    if (isString(value) || isBoolean(value) || isNumber(value)) {
      dom.setAttribute(key, value + "");
    } else {
      console.error("Invalid call to Attribute.set. Key ", key, ":: Value ", value, ":: Element ", dom);
      throw new Error("Attribute value was not simple");
    }
  };
  const setAll = (element, attrs) => {
    const dom = element.dom;
    each(attrs, (v, k) => {
      rawSet(dom, k, v);
    });
  };
  const clone$1 = (element) => foldl(element.dom.attributes, (acc, attr) => {
    acc[attr.name] = attr.value;
    return acc;
  }, {});
  const clone = (original, isDeep) => SugarElement.fromDom(original.dom.cloneNode(isDeep));
  const deep = (original) => clone(original, true);
  const shallowAs = (original, tag) => {
    const nu = SugarElement.fromTag(tag);
    const attributes = clone$1(original);
    setAll(nu, attributes);
    return nu;
  };
  const mutate = (original, tag) => {
    const nu = shallowAs(original, tag);
    after(original, nu);
    const children$1 = children(original);
    append(nu, children$1);
    remove(original);
    return nu;
  };
  var global$2 = tinymce.util.Tools.resolve("tinymce.dom.DOMUtils");
  var global$1 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const matchNodeName = (name2) => (node) => isNonNullable(node) && node.nodeName.toLowerCase() === name2;
  const matchNodeNames = (regex) => (node) => isNonNullable(node) && regex.test(node.nodeName);
  const isTextNode = (node) => isNonNullable(node) && node.nodeType === 3;
  const isElement = (node) => isNonNullable(node) && node.nodeType === 1;
  const isListNode = matchNodeNames(/^(OL|UL|DL)$/);
  const isOlUlNode = matchNodeNames(/^(OL|UL)$/);
  const isOlNode = matchNodeName("ol");
  const isListItemNode = matchNodeNames(/^(LI|DT|DD)$/);
  const isDlItemNode = matchNodeNames(/^(DT|DD)$/);
  const isTableCellNode = matchNodeNames(/^(TH|TD)$/);
  const isBr = matchNodeName("br");
  const isFirstChild = (node) => {
    var _a;
    return ((_a = node.parentNode) === null || _a === void 0 ? void 0 : _a.firstChild) === node;
  };
  const isTextBlock = (editor, node) => isNonNullable(node) && node.nodeName in editor.schema.getTextBlockElements();
  const isBlock = (node, blockElements) => isNonNullable(node) && node.nodeName in blockElements;
  const isBogusBr = (dom, node) => {
    if (!isBr(node)) {
      return false;
    }
    return dom.isBlock(node.nextSibling) && !isBr(node.previousSibling);
  };
  const isEmpty$1 = (dom, elm, keepBookmarks) => {
    const empty2 = dom.isEmpty(elm);
    if (keepBookmarks && dom.select("span[data-mce-type=bookmark]", elm).length > 0) {
      return false;
    }
    return empty2;
  };
  const isChildOfBody = (dom, elm) => dom.isChildOf(elm, dom.getRoot());
  const option = (name2) => (editor) => editor.options.get(name2);
  const register$3 = (editor) => {
    const registerOption = editor.options.register;
    registerOption("lists_indent_on_tab", {
      processor: "boolean",
      default: true
    });
  };
  const shouldIndentOnTab = option("lists_indent_on_tab");
  const getForcedRootBlock = option("forced_root_block");
  const getForcedRootBlockAttrs = option("forced_root_block_attrs");
  const createTextBlock = (editor, contentNode) => {
    const dom = editor.dom;
    const blockElements = editor.schema.getBlockElements();
    const fragment = dom.createFragment();
    const blockName = getForcedRootBlock(editor);
    const blockAttrs = getForcedRootBlockAttrs(editor);
    let node;
    let textBlock;
    let hasContentNode = false;
    textBlock = dom.create(blockName, blockAttrs);
    if (!isBlock(contentNode.firstChild, blockElements)) {
      fragment.appendChild(textBlock);
    }
    while (node = contentNode.firstChild) {
      const nodeName = node.nodeName;
      if (!hasContentNode && (nodeName !== "SPAN" || node.getAttribute("data-mce-type") !== "bookmark")) {
        hasContentNode = true;
      }
      if (isBlock(node, blockElements)) {
        fragment.appendChild(node);
        textBlock = null;
      } else {
        if (!textBlock) {
          textBlock = dom.create(blockName, blockAttrs);
          fragment.appendChild(textBlock);
        }
        textBlock.appendChild(node);
      }
    }
    if (!hasContentNode && textBlock) {
      textBlock.appendChild(dom.create("br", { "data-mce-bogus": "1" }));
    }
    return fragment;
  };
  const DOM$2 = global$2.DOM;
  const splitList = (editor, list, li) => {
    const removeAndKeepBookmarks = (targetNode) => {
      const parent3 = targetNode.parentNode;
      if (parent3) {
        global$1.each(bookmarks, (node) => {
          parent3.insertBefore(node, li.parentNode);
        });
      }
      DOM$2.remove(targetNode);
    };
    const bookmarks = DOM$2.select('span[data-mce-type="bookmark"]', list);
    const newBlock = createTextBlock(editor, li);
    const tmpRng = DOM$2.createRng();
    tmpRng.setStartAfter(li);
    tmpRng.setEndAfter(list);
    const fragment = tmpRng.extractContents();
    for (let node = fragment.firstChild; node; node = node.firstChild) {
      if (node.nodeName === "LI" && editor.dom.isEmpty(node)) {
        DOM$2.remove(node);
        break;
      }
    }
    if (!editor.dom.isEmpty(fragment)) {
      DOM$2.insertAfter(fragment, list);
    }
    DOM$2.insertAfter(newBlock, list);
    const parent2 = li.parentElement;
    if (parent2 && isEmpty$1(editor.dom, parent2)) {
      removeAndKeepBookmarks(parent2);
    }
    DOM$2.remove(li);
    if (isEmpty$1(editor.dom, list)) {
      DOM$2.remove(list);
    }
  };
  const isDescriptionDetail = isTag("dd");
  const isDescriptionTerm = isTag("dt");
  const outdentDlItem = (editor, item) => {
    if (isDescriptionDetail(item)) {
      mutate(item, "dt");
    } else if (isDescriptionTerm(item)) {
      parentElement(item).each((dl) => splitList(editor, dl.dom, item.dom));
    }
  };
  const indentDlItem = (item) => {
    if (isDescriptionTerm(item)) {
      mutate(item, "dd");
    }
  };
  const dlIndentation = (editor, indentation, dlItems) => {
    if (indentation === "Indent") {
      each$1(dlItems, indentDlItem);
    } else {
      each$1(dlItems, (item) => outdentDlItem(editor, item));
    }
  };
  const getNormalizedPoint = (container, offset) => {
    if (isTextNode(container)) {
      return {
        container,
        offset
      };
    }
    const node = global$5.getNode(container, offset);
    if (isTextNode(node)) {
      return {
        container: node,
        offset: offset >= container.childNodes.length ? node.data.length : 0
      };
    } else if (node.previousSibling && isTextNode(node.previousSibling)) {
      return {
        container: node.previousSibling,
        offset: node.previousSibling.data.length
      };
    } else if (node.nextSibling && isTextNode(node.nextSibling)) {
      return {
        container: node.nextSibling,
        offset: 0
      };
    }
    return {
      container,
      offset
    };
  };
  const normalizeRange = (rng) => {
    const outRng = rng.cloneRange();
    const rangeStart = getNormalizedPoint(rng.startContainer, rng.startOffset);
    outRng.setStart(rangeStart.container, rangeStart.offset);
    const rangeEnd = getNormalizedPoint(rng.endContainer, rng.endOffset);
    outRng.setEnd(rangeEnd.container, rangeEnd.offset);
    return outRng;
  };
  const listNames = [
    "OL",
    "UL",
    "DL"
  ];
  const listSelector = listNames.join(",");
  const getParentList = (editor, node) => {
    const selectionStart = node || editor.selection.getStart(true);
    return editor.dom.getParent(selectionStart, listSelector, getClosestListHost(editor, selectionStart));
  };
  const isParentListSelected = (parentList, selectedBlocks) => isNonNullable(parentList) && selectedBlocks.length === 1 && selectedBlocks[0] === parentList;
  const findSubLists = (parentList) => filter$1(parentList.querySelectorAll(listSelector), isListNode);
  const getSelectedSubLists = (editor) => {
    const parentList = getParentList(editor);
    const selectedBlocks = editor.selection.getSelectedBlocks();
    if (isParentListSelected(parentList, selectedBlocks)) {
      return findSubLists(parentList);
    } else {
      return filter$1(selectedBlocks, (elm) => {
        return isListNode(elm) && parentList !== elm;
      });
    }
  };
  const findParentListItemsNodes = (editor, elms) => {
    const listItemsElms = global$1.map(elms, (elm) => {
      const parentLi = editor.dom.getParent(elm, "li,dd,dt", getClosestListHost(editor, elm));
      return parentLi ? parentLi : elm;
    });
    return unique(listItemsElms);
  };
  const getSelectedListItems = (editor) => {
    const selectedBlocks = editor.selection.getSelectedBlocks();
    return filter$1(findParentListItemsNodes(editor, selectedBlocks), isListItemNode);
  };
  const getSelectedDlItems = (editor) => filter$1(getSelectedListItems(editor), isDlItemNode);
  const getClosestEditingHost = (editor, elm) => {
    const parentTableCell = editor.dom.getParents(elm, "TD,TH");
    return parentTableCell.length > 0 ? parentTableCell[0] : editor.getBody();
  };
  const isListHost = (schema, node) => !isListNode(node) && !isListItemNode(node) && exists(listNames, (listName) => schema.isValidChild(node.nodeName, listName));
  const getClosestListHost = (editor, elm) => {
    const parentBlocks = editor.dom.getParents(elm, editor.dom.isBlock);
    const parentBlock = find(parentBlocks, (elm2) => isListHost(editor.schema, elm2));
    return parentBlock.getOr(editor.getBody());
  };
  const findLastParentListNode = (editor, elm) => {
    const parentLists = editor.dom.getParents(elm, "ol,ul", getClosestListHost(editor, elm));
    return last(parentLists);
  };
  const getSelectedLists = (editor) => {
    const firstList = findLastParentListNode(editor, editor.selection.getStart());
    const subsequentLists = filter$1(editor.selection.getSelectedBlocks(), isOlUlNode);
    return firstList.toArray().concat(subsequentLists);
  };
  const getSelectedListRoots = (editor) => {
    const selectedLists = getSelectedLists(editor);
    return getUniqueListRoots(editor, selectedLists);
  };
  const getUniqueListRoots = (editor, lists) => {
    const listRoots = map(lists, (list) => findLastParentListNode(editor, list).getOr(list));
    return unique(listRoots);
  };
  const isCustomList = (list) => /\btox\-/.test(list.className);
  const inList = (parents, listName) => findUntil(parents, isListNode, isTableCellNode).exists((list) => list.nodeName === listName && !isCustomList(list));
  const isWithinNonEditable = (editor, element) => element !== null && editor.dom.getContentEditableParent(element) === "false";
  const selectionIsWithinNonEditableList = (editor) => {
    const parentList = getParentList(editor);
    return isWithinNonEditable(editor, parentList);
  };
  const isWithinNonEditableList = (editor, element) => {
    const parentList = editor.dom.getParent(element, "ol,ul,dl");
    return isWithinNonEditable(editor, parentList);
  };
  const setNodeChangeHandler = (editor, nodeChangeHandler) => {
    const initialNode = editor.selection.getNode();
    nodeChangeHandler({
      parents: editor.dom.getParents(initialNode),
      element: initialNode
    });
    editor.on("NodeChange", nodeChangeHandler);
    return () => editor.off("NodeChange", nodeChangeHandler);
  };
  const fromElements = (elements, scope) => {
    const doc = scope || document;
    const fragment = doc.createDocumentFragment();
    each$1(elements, (element) => {
      fragment.appendChild(element.dom);
    });
    return SugarElement.fromDom(fragment);
  };
  const fireListEvent = (editor, action, element) => editor.dispatch("ListMutation", {
    action,
    element
  });
  const blank = (r) => (s) => s.replace(r, "");
  const trim = blank(/^\s+|\s+$/g);
  const isNotEmpty = (s) => s.length > 0;
  const isEmpty = (s) => !isNotEmpty(s);
  const isSupported = (dom) => dom.style !== void 0 && isFunction(dom.style.getPropertyValue);
  const internalSet = (dom, property, value) => {
    if (!isString(value)) {
      console.error("Invalid call to CSS.set. Property ", property, ":: Value ", value, ":: Element ", dom);
      throw new Error("CSS value must be a string: " + value);
    }
    if (isSupported(dom)) {
      dom.style.setProperty(property, value);
    }
  };
  const set = (element, property, value) => {
    const dom = element.dom;
    internalSet(dom, property, value);
  };
  const joinSegment = (parent2, child2) => {
    append$1(parent2.item, child2.list);
  };
  const joinSegments = (segments) => {
    for (let i2 = 1; i2 < segments.length; i2++) {
      joinSegment(segments[i2 - 1], segments[i2]);
    }
  };
  const appendSegments = (head$1, tail) => {
    lift2(last(head$1), head(tail), joinSegment);
  };
  const createSegment = (scope, listType) => {
    const segment = {
      list: SugarElement.fromTag(listType, scope),
      item: SugarElement.fromTag("li", scope)
    };
    append$1(segment.list, segment.item);
    return segment;
  };
  const createSegments = (scope, entry, size) => {
    const segments = [];
    for (let i2 = 0; i2 < size; i2++) {
      segments.push(createSegment(scope, entry.listType));
    }
    return segments;
  };
  const populateSegments = (segments, entry) => {
    for (let i2 = 0; i2 < segments.length - 1; i2++) {
      set(segments[i2].item, "list-style-type", "none");
    }
    last(segments).each((segment) => {
      setAll(segment.list, entry.listAttributes);
      setAll(segment.item, entry.itemAttributes);
      append(segment.item, entry.content);
    });
  };
  const normalizeSegment = (segment, entry) => {
    if (name(segment.list) !== entry.listType) {
      segment.list = mutate(segment.list, entry.listType);
    }
    setAll(segment.list, entry.listAttributes);
  };
  const createItem = (scope, attr, content) => {
    const item = SugarElement.fromTag("li", scope);
    setAll(item, attr);
    append(item, content);
    return item;
  };
  const appendItem = (segment, item) => {
    append$1(segment.list, item);
    segment.item = item;
  };
  const writeShallow = (scope, cast, entry) => {
    const newCast = cast.slice(0, entry.depth);
    last(newCast).each((segment) => {
      const item = createItem(scope, entry.itemAttributes, entry.content);
      appendItem(segment, item);
      normalizeSegment(segment, entry);
    });
    return newCast;
  };
  const writeDeep = (scope, cast, entry) => {
    const segments = createSegments(scope, entry, entry.depth - cast.length);
    joinSegments(segments);
    populateSegments(segments, entry);
    appendSegments(cast, segments);
    return cast.concat(segments);
  };
  const composeList = (scope, entries) => {
    const cast = foldl(entries, (cast2, entry) => {
      return entry.depth > cast2.length ? writeDeep(scope, cast2, entry) : writeShallow(scope, cast2, entry);
    }, []);
    return head(cast).map((segment) => segment.list);
  };
  const isList = (el) => is(el, "OL,UL");
  const hasFirstChildList = (el) => firstChild(el).exists(isList);
  const hasLastChildList = (el) => lastChild(el).exists(isList);
  const isIndented = (entry) => entry.depth > 0;
  const isSelected = (entry) => entry.isSelected;
  const cloneItemContent = (li) => {
    const children$1 = children(li);
    const content = hasLastChildList(li) ? children$1.slice(0, -1) : children$1;
    return map(content, deep);
  };
  const createEntry = (li, depth, isSelected2) => parent(li).filter(isElement$1).map((list) => ({
    depth,
    dirty: false,
    isSelected: isSelected2,
    content: cloneItemContent(li),
    itemAttributes: clone$1(li),
    listAttributes: clone$1(list),
    listType: name(list)
  }));
  const indentEntry = (indentation, entry) => {
    switch (indentation) {
      case "Indent":
        entry.depth++;
        break;
      case "Outdent":
        entry.depth--;
        break;
      case "Flatten":
        entry.depth = 0;
    }
    entry.dirty = true;
  };
  const cloneListProperties = (target, source) => {
    target.listType = source.listType;
    target.listAttributes = { ...source.listAttributes };
  };
  const cleanListProperties = (entry) => {
    entry.listAttributes = filter(entry.listAttributes, (_value, key) => key !== "start");
  };
  const closestSiblingEntry = (entries, start) => {
    const depth = entries[start].depth;
    const matches = (entry) => entry.depth === depth && !entry.dirty;
    const until = (entry) => entry.depth < depth;
    return findUntil(reverse(entries.slice(0, start)), matches, until).orThunk(() => findUntil(entries.slice(start + 1), matches, until));
  };
  const normalizeEntries = (entries) => {
    each$1(entries, (entry, i2) => {
      closestSiblingEntry(entries, i2).fold(() => {
        if (entry.dirty) {
          cleanListProperties(entry);
        }
      }, (matchingEntry) => cloneListProperties(entry, matchingEntry));
    });
    return entries;
  };
  const Cell = (initial) => {
    let value = initial;
    const get2 = () => {
      return value;
    };
    const set2 = (v) => {
      value = v;
    };
    return {
      get: get2,
      set: set2
    };
  };
  const parseItem = (depth, itemSelection, selectionState, item) => firstChild(item).filter(isList).fold(() => {
    itemSelection.each((selection) => {
      if (eq(selection.start, item)) {
        selectionState.set(true);
      }
    });
    const currentItemEntry = createEntry(item, depth, selectionState.get());
    itemSelection.each((selection) => {
      if (eq(selection.end, item)) {
        selectionState.set(false);
      }
    });
    const childListEntries = lastChild(item).filter(isList).map((list) => parseList(depth, itemSelection, selectionState, list)).getOr([]);
    return currentItemEntry.toArray().concat(childListEntries);
  }, (list) => parseList(depth, itemSelection, selectionState, list));
  const parseList = (depth, itemSelection, selectionState, list) => bind(children(list), (element) => {
    const parser = isList(element) ? parseList : parseItem;
    const newDepth = depth + 1;
    return parser(newDepth, itemSelection, selectionState, element);
  });
  const parseLists = (lists, itemSelection) => {
    const selectionState = Cell(false);
    const initialDepth = 0;
    return map(lists, (list) => ({
      sourceList: list,
      entries: parseList(initialDepth, itemSelection, selectionState, list)
    }));
  };
  const outdentedComposer = (editor, entries) => {
    const normalizedEntries = normalizeEntries(entries);
    return map(normalizedEntries, (entry) => {
      const content = fromElements(entry.content);
      return SugarElement.fromDom(createTextBlock(editor, content.dom));
    });
  };
  const indentedComposer = (editor, entries) => {
    const normalizedEntries = normalizeEntries(entries);
    return composeList(editor.contentDocument, normalizedEntries).toArray();
  };
  const composeEntries = (editor, entries) => bind(groupBy(entries, isIndented), (entries2) => {
    const groupIsIndented = head(entries2).exists(isIndented);
    return groupIsIndented ? indentedComposer(editor, entries2) : outdentedComposer(editor, entries2);
  });
  const indentSelectedEntries = (entries, indentation) => {
    each$1(filter$1(entries, isSelected), (entry) => indentEntry(indentation, entry));
  };
  const getItemSelection = (editor) => {
    const selectedListItems = map(getSelectedListItems(editor), SugarElement.fromDom);
    return lift2(find(selectedListItems, not(hasFirstChildList)), find(reverse(selectedListItems), not(hasFirstChildList)), (start, end) => ({
      start,
      end
    }));
  };
  const listIndentation = (editor, lists, indentation) => {
    const entrySets = parseLists(lists, getItemSelection(editor));
    each$1(entrySets, (entrySet) => {
      indentSelectedEntries(entrySet.entries, indentation);
      const composedLists = composeEntries(editor, entrySet.entries);
      each$1(composedLists, (composedList) => {
        fireListEvent(editor, indentation === "Indent" ? "IndentList" : "OutdentList", composedList.dom);
      });
      before(entrySet.sourceList, composedLists);
      remove(entrySet.sourceList);
    });
  };
  const selectionIndentation = (editor, indentation) => {
    const lists = fromDom(getSelectedListRoots(editor));
    const dlItems = fromDom(getSelectedDlItems(editor));
    let isHandled = false;
    if (lists.length || dlItems.length) {
      const bookmark = editor.selection.getBookmark();
      listIndentation(editor, lists, indentation);
      dlIndentation(editor, indentation, dlItems);
      editor.selection.moveToBookmark(bookmark);
      editor.selection.setRng(normalizeRange(editor.selection.getRng()));
      editor.nodeChanged();
      isHandled = true;
    }
    return isHandled;
  };
  const handleIndentation = (editor, indentation) => !selectionIsWithinNonEditableList(editor) && selectionIndentation(editor, indentation);
  const indentListSelection = (editor) => handleIndentation(editor, "Indent");
  const outdentListSelection = (editor) => handleIndentation(editor, "Outdent");
  const flattenListSelection = (editor) => handleIndentation(editor, "Flatten");
  var global2 = tinymce.util.Tools.resolve("tinymce.dom.BookmarkManager");
  const DOM$1 = global$2.DOM;
  const createBookmark = (rng) => {
    const bookmark = {};
    const setupEndPoint = (start) => {
      let container = rng[start ? "startContainer" : "endContainer"];
      let offset = rng[start ? "startOffset" : "endOffset"];
      if (isElement(container)) {
        const offsetNode = DOM$1.create("span", { "data-mce-type": "bookmark" });
        if (container.hasChildNodes()) {
          offset = Math.min(offset, container.childNodes.length - 1);
          if (start) {
            container.insertBefore(offsetNode, container.childNodes[offset]);
          } else {
            DOM$1.insertAfter(offsetNode, container.childNodes[offset]);
          }
        } else {
          container.appendChild(offsetNode);
        }
        container = offsetNode;
        offset = 0;
      }
      bookmark[start ? "startContainer" : "endContainer"] = container;
      bookmark[start ? "startOffset" : "endOffset"] = offset;
    };
    setupEndPoint(true);
    if (!rng.collapsed) {
      setupEndPoint();
    }
    return bookmark;
  };
  const resolveBookmark = (bookmark) => {
    const restoreEndPoint = (start) => {
      const nodeIndex = (container2) => {
        var _a;
        let node = (_a = container2.parentNode) === null || _a === void 0 ? void 0 : _a.firstChild;
        let idx = 0;
        while (node) {
          if (node === container2) {
            return idx;
          }
          if (!isElement(node) || node.getAttribute("data-mce-type") !== "bookmark") {
            idx++;
          }
          node = node.nextSibling;
        }
        return -1;
      };
      let container = bookmark[start ? "startContainer" : "endContainer"];
      let offset = bookmark[start ? "startOffset" : "endOffset"];
      if (!container) {
        return;
      }
      if (isElement(container) && container.parentNode) {
        const node = container;
        offset = nodeIndex(container);
        container = container.parentNode;
        DOM$1.remove(node);
        if (!container.hasChildNodes() && DOM$1.isBlock(container)) {
          container.appendChild(DOM$1.create("br"));
        }
      }
      bookmark[start ? "startContainer" : "endContainer"] = container;
      bookmark[start ? "startOffset" : "endOffset"] = offset;
    };
    restoreEndPoint(true);
    restoreEndPoint();
    const rng = DOM$1.createRng();
    rng.setStart(bookmark.startContainer, bookmark.startOffset);
    if (bookmark.endContainer) {
      rng.setEnd(bookmark.endContainer, bookmark.endOffset);
    }
    return normalizeRange(rng);
  };
  const listToggleActionFromListName = (listName) => {
    switch (listName) {
      case "UL":
        return "ToggleUlList";
      case "OL":
        return "ToggleOlList";
      case "DL":
        return "ToggleDLList";
    }
  };
  const updateListStyle = (dom, el, detail) => {
    const type2 = detail["list-style-type"] ? detail["list-style-type"] : null;
    dom.setStyle(el, "list-style-type", type2);
  };
  const setAttribs = (elm, attrs) => {
    global$1.each(attrs, (value, key) => {
      elm.setAttribute(key, value);
    });
  };
  const updateListAttrs = (dom, el, detail) => {
    setAttribs(el, detail["list-attributes"]);
    global$1.each(dom.select("li", el), (li) => {
      setAttribs(li, detail["list-item-attributes"]);
    });
  };
  const updateListWithDetails = (dom, el, detail) => {
    updateListStyle(dom, el, detail);
    updateListAttrs(dom, el, detail);
  };
  const removeStyles = (dom, element, styles) => {
    global$1.each(styles, (style) => dom.setStyle(element, style, ""));
  };
  const getEndPointNode = (editor, rng, start, root) => {
    let container = rng[start ? "startContainer" : "endContainer"];
    const offset = rng[start ? "startOffset" : "endOffset"];
    if (isElement(container)) {
      container = container.childNodes[Math.min(offset, container.childNodes.length - 1)] || container;
    }
    if (!start && isBr(container.nextSibling)) {
      container = container.nextSibling;
    }
    while (container.parentNode !== root) {
      const parent2 = container.parentNode;
      if (isTextBlock(editor, container)) {
        return container;
      }
      if (/^(TD|TH)$/.test(parent2.nodeName)) {
        return container;
      }
      container = parent2;
    }
    return container;
  };
  const getSelectedTextBlocks = (editor, rng, root) => {
    const textBlocks = [];
    const dom = editor.dom;
    const startNode = getEndPointNode(editor, rng, true, root);
    const endNode = getEndPointNode(editor, rng, false, root);
    let block;
    const siblings = [];
    for (let node = startNode; node; node = node.nextSibling) {
      siblings.push(node);
      if (node === endNode) {
        break;
      }
    }
    global$1.each(siblings, (node) => {
      var _a;
      if (isTextBlock(editor, node)) {
        textBlocks.push(node);
        block = null;
        return;
      }
      if (dom.isBlock(node) || isBr(node)) {
        if (isBr(node)) {
          dom.remove(node);
        }
        block = null;
        return;
      }
      const nextSibling2 = node.nextSibling;
      if (global2.isBookmarkNode(node)) {
        if (isListNode(nextSibling2) || isTextBlock(editor, nextSibling2) || !nextSibling2 && node.parentNode === root) {
          block = null;
          return;
        }
      }
      if (!block) {
        block = dom.create("p");
        (_a = node.parentNode) === null || _a === void 0 ? void 0 : _a.insertBefore(block, node);
        textBlocks.push(block);
      }
      block.appendChild(node);
    });
    return textBlocks;
  };
  const hasCompatibleStyle = (dom, sib, detail) => {
    const sibStyle = dom.getStyle(sib, "list-style-type");
    let detailStyle = detail ? detail["list-style-type"] : "";
    detailStyle = detailStyle === null ? "" : detailStyle;
    return sibStyle === detailStyle;
  };
  const applyList = (editor, listName, detail) => {
    const rng = editor.selection.getRng();
    let listItemName = "LI";
    const root = getClosestListHost(editor, editor.selection.getStart(true));
    const dom = editor.dom;
    if (dom.getContentEditable(editor.selection.getNode()) === "false") {
      return;
    }
    listName = listName.toUpperCase();
    if (listName === "DL") {
      listItemName = "DT";
    }
    const bookmark = createBookmark(rng);
    const selectedTextBlocks = getSelectedTextBlocks(editor, rng, root);
    global$1.each(selectedTextBlocks, (block) => {
      let listBlock;
      const sibling = block.previousSibling;
      const parent2 = block.parentNode;
      if (!isListItemNode(parent2)) {
        if (sibling && isListNode(sibling) && sibling.nodeName === listName && hasCompatibleStyle(dom, sibling, detail)) {
          listBlock = sibling;
          block = dom.rename(block, listItemName);
          sibling.appendChild(block);
        } else {
          listBlock = dom.create(listName);
          parent2.insertBefore(listBlock, block);
          listBlock.appendChild(block);
          block = dom.rename(block, listItemName);
        }
        removeStyles(dom, block, [
          "margin",
          "margin-right",
          "margin-bottom",
          "margin-left",
          "margin-top",
          "padding",
          "padding-right",
          "padding-bottom",
          "padding-left",
          "padding-top"
        ]);
        updateListWithDetails(dom, listBlock, detail);
        mergeWithAdjacentLists(editor.dom, listBlock);
      }
    });
    editor.selection.setRng(resolveBookmark(bookmark));
  };
  const isValidLists = (list1, list2) => {
    return isListNode(list1) && list1.nodeName === (list2 === null || list2 === void 0 ? void 0 : list2.nodeName);
  };
  const hasSameListStyle = (dom, list1, list2) => {
    const targetStyle = dom.getStyle(list1, "list-style-type", true);
    const style = dom.getStyle(list2, "list-style-type", true);
    return targetStyle === style;
  };
  const hasSameClasses = (elm1, elm2) => {
    return elm1.className === elm2.className;
  };
  const shouldMerge = (dom, list1, list2) => {
    return isValidLists(list1, list2) && hasSameListStyle(dom, list1, list2) && hasSameClasses(list1, list2);
  };
  const mergeWithAdjacentLists = (dom, listBlock) => {
    let node;
    let sibling = listBlock.nextSibling;
    if (shouldMerge(dom, listBlock, sibling)) {
      const liSibling = sibling;
      while (node = liSibling.firstChild) {
        listBlock.appendChild(node);
      }
      dom.remove(liSibling);
    }
    sibling = listBlock.previousSibling;
    if (shouldMerge(dom, listBlock, sibling)) {
      const liSibling = sibling;
      while (node = liSibling.lastChild) {
        listBlock.insertBefore(node, listBlock.firstChild);
      }
      dom.remove(liSibling);
    }
  };
  const updateList$1 = (editor, list, listName, detail) => {
    if (list.nodeName !== listName) {
      const newList = editor.dom.rename(list, listName);
      updateListWithDetails(editor.dom, newList, detail);
      fireListEvent(editor, listToggleActionFromListName(listName), newList);
    } else {
      updateListWithDetails(editor.dom, list, detail);
      fireListEvent(editor, listToggleActionFromListName(listName), list);
    }
  };
  const toggleMultipleLists = (editor, parentList, lists, listName, detail) => {
    const parentIsList = isListNode(parentList);
    if (parentIsList && parentList.nodeName === listName && !hasListStyleDetail(detail)) {
      flattenListSelection(editor);
    } else {
      applyList(editor, listName, detail);
      const bookmark = createBookmark(editor.selection.getRng());
      const allLists = parentIsList ? [
        parentList,
        ...lists
      ] : lists;
      global$1.each(allLists, (elm) => {
        updateList$1(editor, elm, listName, detail);
      });
      editor.selection.setRng(resolveBookmark(bookmark));
    }
  };
  const hasListStyleDetail = (detail) => {
    return "list-style-type" in detail;
  };
  const toggleSingleList = (editor, parentList, listName, detail) => {
    if (parentList === editor.getBody()) {
      return;
    }
    if (parentList) {
      if (parentList.nodeName === listName && !hasListStyleDetail(detail) && !isCustomList(parentList)) {
        flattenListSelection(editor);
      } else {
        const bookmark = createBookmark(editor.selection.getRng());
        updateListWithDetails(editor.dom, parentList, detail);
        const newList = editor.dom.rename(parentList, listName);
        mergeWithAdjacentLists(editor.dom, newList);
        editor.selection.setRng(resolveBookmark(bookmark));
        applyList(editor, listName, detail);
        fireListEvent(editor, listToggleActionFromListName(listName), newList);
      }
    } else {
      applyList(editor, listName, detail);
      fireListEvent(editor, listToggleActionFromListName(listName), parentList);
    }
  };
  const toggleList = (editor, listName, _detail) => {
    const parentList = getParentList(editor);
    if (isWithinNonEditableList(editor, parentList)) {
      return;
    }
    const selectedSubLists = getSelectedSubLists(editor);
    const detail = isObject(_detail) ? _detail : {};
    if (selectedSubLists.length > 0) {
      toggleMultipleLists(editor, parentList, selectedSubLists, listName, detail);
    } else {
      toggleSingleList(editor, parentList, listName, detail);
    }
  };
  const DOM = global$2.DOM;
  const normalizeList = (dom, list) => {
    const parentNode = list.parentElement;
    if (parentNode && parentNode.nodeName === "LI" && parentNode.firstChild === list) {
      const sibling = parentNode.previousSibling;
      if (sibling && sibling.nodeName === "LI") {
        sibling.appendChild(list);
        if (isEmpty$1(dom, parentNode)) {
          DOM.remove(parentNode);
        }
      } else {
        DOM.setStyle(parentNode, "listStyleType", "none");
      }
    }
    if (isListNode(parentNode)) {
      const sibling = parentNode.previousSibling;
      if (sibling && sibling.nodeName === "LI") {
        sibling.appendChild(list);
      }
    }
  };
  const normalizeLists = (dom, element) => {
    const lists = global$1.grep(dom.select("ol,ul", element));
    global$1.each(lists, (list) => {
      normalizeList(dom, list);
    });
  };
  const findNextCaretContainer = (editor, rng, isForward, root) => {
    let node = rng.startContainer;
    const offset = rng.startOffset;
    if (isTextNode(node) && (isForward ? offset < node.data.length : offset > 0)) {
      return node;
    }
    const nonEmptyBlocks = editor.schema.getNonEmptyElements();
    if (isElement(node)) {
      node = global$5.getNode(node, offset);
    }
    const walker = new global$4(node, root);
    if (isForward) {
      if (isBogusBr(editor.dom, node)) {
        walker.next();
      }
    }
    const walkFn = isForward ? walker.next.bind(walker) : walker.prev2.bind(walker);
    while (node = walkFn()) {
      if (node.nodeName === "LI" && !node.hasChildNodes()) {
        return node;
      }
      if (nonEmptyBlocks[node.nodeName]) {
        return node;
      }
      if (isTextNode(node) && node.data.length > 0) {
        return node;
      }
    }
    return null;
  };
  const hasOnlyOneBlockChild = (dom, elm) => {
    const childNodes = elm.childNodes;
    return childNodes.length === 1 && !isListNode(childNodes[0]) && dom.isBlock(childNodes[0]);
  };
  const unwrapSingleBlockChild = (dom, elm) => {
    if (hasOnlyOneBlockChild(dom, elm)) {
      dom.remove(elm.firstChild, true);
    }
  };
  const moveChildren = (dom, fromElm, toElm) => {
    let node;
    const targetElm = hasOnlyOneBlockChild(dom, toElm) ? toElm.firstChild : toElm;
    unwrapSingleBlockChild(dom, fromElm);
    if (!isEmpty$1(dom, fromElm, true)) {
      while (node = fromElm.firstChild) {
        targetElm.appendChild(node);
      }
    }
  };
  const mergeLiElements = (dom, fromElm, toElm) => {
    let listNode;
    const ul = fromElm.parentNode;
    if (!isChildOfBody(dom, fromElm) || !isChildOfBody(dom, toElm)) {
      return;
    }
    if (isListNode(toElm.lastChild)) {
      listNode = toElm.lastChild;
    }
    if (ul === toElm.lastChild) {
      if (isBr(ul.previousSibling)) {
        dom.remove(ul.previousSibling);
      }
    }
    const node = toElm.lastChild;
    if (node && isBr(node) && fromElm.hasChildNodes()) {
      dom.remove(node);
    }
    if (isEmpty$1(dom, toElm, true)) {
      empty(SugarElement.fromDom(toElm));
    }
    moveChildren(dom, fromElm, toElm);
    if (listNode) {
      toElm.appendChild(listNode);
    }
    const contains$12 = contains(SugarElement.fromDom(toElm), SugarElement.fromDom(fromElm));
    const nestedLists = contains$12 ? dom.getParents(fromElm, isListNode, toElm) : [];
    dom.remove(fromElm);
    each$1(nestedLists, (list) => {
      if (isEmpty$1(dom, list) && list !== dom.getRoot()) {
        dom.remove(list);
      }
    });
  };
  const mergeIntoEmptyLi = (editor, fromLi, toLi) => {
    empty(SugarElement.fromDom(toLi));
    mergeLiElements(editor.dom, fromLi, toLi);
    editor.selection.setCursorLocation(toLi, 0);
  };
  const mergeForward = (editor, rng, fromLi, toLi) => {
    const dom = editor.dom;
    if (dom.isEmpty(toLi)) {
      mergeIntoEmptyLi(editor, fromLi, toLi);
    } else {
      const bookmark = createBookmark(rng);
      mergeLiElements(dom, fromLi, toLi);
      editor.selection.setRng(resolveBookmark(bookmark));
    }
  };
  const mergeBackward = (editor, rng, fromLi, toLi) => {
    const bookmark = createBookmark(rng);
    mergeLiElements(editor.dom, fromLi, toLi);
    const resolvedBookmark = resolveBookmark(bookmark);
    editor.selection.setRng(resolvedBookmark);
  };
  const backspaceDeleteFromListToListCaret = (editor, isForward) => {
    const dom = editor.dom, selection = editor.selection;
    const selectionStartElm = selection.getStart();
    const root = getClosestEditingHost(editor, selectionStartElm);
    const li = dom.getParent(selection.getStart(), "LI", root);
    if (li) {
      const ul = li.parentElement;
      if (ul === editor.getBody() && isEmpty$1(dom, ul)) {
        return true;
      }
      const rng = normalizeRange(selection.getRng());
      const otherLi = dom.getParent(findNextCaretContainer(editor, rng, isForward, root), "LI", root);
      if (otherLi && otherLi !== li) {
        editor.undoManager.transact(() => {
          if (isForward) {
            mergeForward(editor, rng, otherLi, li);
          } else {
            if (isFirstChild(li)) {
              outdentListSelection(editor);
            } else {
              mergeBackward(editor, rng, li, otherLi);
            }
          }
        });
        return true;
      } else if (!otherLi) {
        if (!isForward && rng.startOffset === 0 && rng.endOffset === 0) {
          editor.undoManager.transact(() => {
            flattenListSelection(editor);
          });
          return true;
        }
      }
    }
    return false;
  };
  const removeBlock = (dom, block, root) => {
    const parentBlock = dom.getParent(block.parentNode, dom.isBlock, root);
    dom.remove(block);
    if (parentBlock && dom.isEmpty(parentBlock)) {
      dom.remove(parentBlock);
    }
  };
  const backspaceDeleteIntoListCaret = (editor, isForward) => {
    const dom = editor.dom;
    const selectionStartElm = editor.selection.getStart();
    const root = getClosestEditingHost(editor, selectionStartElm);
    const block = dom.getParent(selectionStartElm, dom.isBlock, root);
    if (block && dom.isEmpty(block)) {
      const rng = normalizeRange(editor.selection.getRng());
      const otherLi = dom.getParent(findNextCaretContainer(editor, rng, isForward, root), "LI", root);
      if (otherLi) {
        const findValidElement = (element) => contains$1([
          "td",
          "th",
          "caption"
        ], name(element));
        const findRoot = (node) => node.dom === root;
        const otherLiCell = closest(SugarElement.fromDom(otherLi), findValidElement, findRoot);
        const caretCell = closest(SugarElement.fromDom(rng.startContainer), findValidElement, findRoot);
        if (!equals(otherLiCell, caretCell, eq)) {
          return false;
        }
        editor.undoManager.transact(() => {
          removeBlock(dom, block, root);
          mergeWithAdjacentLists(dom, otherLi.parentNode);
          editor.selection.select(otherLi, true);
          editor.selection.collapse(isForward);
        });
        return true;
      }
    }
    return false;
  };
  const backspaceDeleteCaret = (editor, isForward) => {
    return backspaceDeleteFromListToListCaret(editor, isForward) || backspaceDeleteIntoListCaret(editor, isForward);
  };
  const hasListSelection = (editor) => {
    const selectionStartElm = editor.selection.getStart();
    const root = getClosestEditingHost(editor, selectionStartElm);
    const startListParent = editor.dom.getParent(selectionStartElm, "LI,DT,DD", root);
    return startListParent || getSelectedListItems(editor).length > 0;
  };
  const backspaceDeleteRange = (editor) => {
    if (hasListSelection(editor)) {
      editor.undoManager.transact(() => {
        editor.execCommand("Delete");
        normalizeLists(editor.dom, editor.getBody());
      });
      return true;
    }
    return false;
  };
  const backspaceDelete = (editor, isForward) => {
    const selection = editor.selection;
    return !isWithinNonEditableList(editor, selection.getNode()) && (selection.isCollapsed() ? backspaceDeleteCaret(editor, isForward) : backspaceDeleteRange(editor));
  };
  const setup$1 = (editor) => {
    editor.on("ExecCommand", (e) => {
      const cmd = e.command.toLowerCase();
      if ((cmd === "delete" || cmd === "forwarddelete") && hasListSelection(editor)) {
        normalizeLists(editor.dom, editor.getBody());
      }
    });
    editor.on("keydown", (e) => {
      if (e.keyCode === global$3.BACKSPACE) {
        if (backspaceDelete(editor, false)) {
          e.preventDefault();
        }
      } else if (e.keyCode === global$3.DELETE) {
        if (backspaceDelete(editor, true)) {
          e.preventDefault();
        }
      }
    });
  };
  const get = (editor) => ({
    backspaceDelete: (isForward) => {
      backspaceDelete(editor, isForward);
    }
  });
  const updateList = (editor, update) => {
    const parentList = getParentList(editor);
    if (parentList === null || isWithinNonEditableList(editor, parentList)) {
      return;
    }
    editor.undoManager.transact(() => {
      if (isObject(update.styles)) {
        editor.dom.setStyles(parentList, update.styles);
      }
      if (isObject(update.attrs)) {
        each(update.attrs, (v, k) => editor.dom.setAttrib(parentList, k, v));
      }
    });
  };
  const parseAlphabeticBase26 = (str) => {
    const chars = reverse(trim(str).split(""));
    const values = map(chars, (char, i2) => {
      const charValue = char.toUpperCase().charCodeAt(0) - "A".charCodeAt(0) + 1;
      return Math.pow(26, i2) * charValue;
    });
    return foldl(values, (sum, v) => sum + v, 0);
  };
  const composeAlphabeticBase26 = (value) => {
    value--;
    if (value < 0) {
      return "";
    } else {
      const remainder = value % 26;
      const quotient = Math.floor(value / 26);
      const rest = composeAlphabeticBase26(quotient);
      const char = String.fromCharCode("A".charCodeAt(0) + remainder);
      return rest + char;
    }
  };
  const isUppercase = (str) => /^[A-Z]+$/.test(str);
  const isLowercase = (str) => /^[a-z]+$/.test(str);
  const isNumeric = (str) => /^[0-9]+$/.test(str);
  const deduceListType = (start) => {
    if (isNumeric(start)) {
      return 2;
    } else if (isUppercase(start)) {
      return 0;
    } else if (isLowercase(start)) {
      return 1;
    } else if (isEmpty(start)) {
      return 3;
    } else {
      return 4;
    }
  };
  const parseStartValue = (start) => {
    switch (deduceListType(start)) {
      case 2:
        return Optional.some({
          listStyleType: Optional.none(),
          start
        });
      case 0:
        return Optional.some({
          listStyleType: Optional.some("upper-alpha"),
          start: parseAlphabeticBase26(start).toString()
        });
      case 1:
        return Optional.some({
          listStyleType: Optional.some("lower-alpha"),
          start: parseAlphabeticBase26(start).toString()
        });
      case 3:
        return Optional.some({
          listStyleType: Optional.none(),
          start: ""
        });
      case 4:
        return Optional.none();
    }
  };
  const parseDetail = (detail) => {
    const start = parseInt(detail.start, 10);
    if (is$2(detail.listStyleType, "upper-alpha")) {
      return composeAlphabeticBase26(start);
    } else if (is$2(detail.listStyleType, "lower-alpha")) {
      return composeAlphabeticBase26(start).toLowerCase();
    } else {
      return detail.start;
    }
  };
  const open = (editor) => {
    const currentList = getParentList(editor);
    if (!isOlNode(currentList) || isWithinNonEditableList(editor, currentList)) {
      return;
    }
    editor.windowManager.open({
      title: "List Properties",
      body: {
        type: "panel",
        items: [{
          type: "input",
          name: "start",
          label: "Start list at number",
          inputMode: "numeric"
        }]
      },
      initialData: {
        start: parseDetail({
          start: editor.dom.getAttrib(currentList, "start", "1"),
          listStyleType: Optional.from(editor.dom.getStyle(currentList, "list-style-type"))
        })
      },
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      onSubmit: (api) => {
        const data = api.getData();
        parseStartValue(data.start).each((detail) => {
          editor.execCommand("mceListUpdate", false, {
            attrs: { start: detail.start === "1" ? "" : detail.start },
            styles: { "list-style-type": detail.listStyleType.getOr("") }
          });
        });
        api.close();
      }
    });
  };
  const queryListCommandState = (editor, listName) => () => {
    const parentList = getParentList(editor);
    return isNonNullable(parentList) && parentList.nodeName === listName;
  };
  const registerDialog = (editor) => {
    editor.addCommand("mceListProps", () => {
      open(editor);
    });
  };
  const register$2 = (editor) => {
    editor.on("BeforeExecCommand", (e) => {
      const cmd = e.command.toLowerCase();
      if (cmd === "indent") {
        indentListSelection(editor);
      } else if (cmd === "outdent") {
        outdentListSelection(editor);
      }
    });
    editor.addCommand("InsertUnorderedList", (ui, detail) => {
      toggleList(editor, "UL", detail);
    });
    editor.addCommand("InsertOrderedList", (ui, detail) => {
      toggleList(editor, "OL", detail);
    });
    editor.addCommand("InsertDefinitionList", (ui, detail) => {
      toggleList(editor, "DL", detail);
    });
    editor.addCommand("RemoveList", () => {
      flattenListSelection(editor);
    });
    registerDialog(editor);
    editor.addCommand("mceListUpdate", (ui, detail) => {
      if (isObject(detail)) {
        updateList(editor, detail);
      }
    });
    editor.addQueryStateHandler("InsertUnorderedList", queryListCommandState(editor, "UL"));
    editor.addQueryStateHandler("InsertOrderedList", queryListCommandState(editor, "OL"));
    editor.addQueryStateHandler("InsertDefinitionList", queryListCommandState(editor, "DL"));
  };
  const setupTabKey = (editor) => {
    editor.on("keydown", (e) => {
      if (e.keyCode !== global$3.TAB || global$3.metaKeyPressed(e)) {
        return;
      }
      editor.undoManager.transact(() => {
        if (e.shiftKey ? outdentListSelection(editor) : indentListSelection(editor)) {
          e.preventDefault();
        }
      });
    });
  };
  const setup = (editor) => {
    if (shouldIndentOnTab(editor)) {
      setupTabKey(editor);
    }
    setup$1(editor);
  };
  const setupToggleButtonHandler = (editor, listName) => (api) => {
    const toggleButtonHandler = (e) => {
      api.setActive(inList(e.parents, listName));
      api.setEnabled(!isWithinNonEditableList(editor, e.element));
    };
    return setNodeChangeHandler(editor, toggleButtonHandler);
  };
  const register$1 = (editor) => {
    const exec = (command) => () => editor.execCommand(command);
    if (!editor.hasPlugin("advlist")) {
      editor.ui.registry.addToggleButton("numlist", {
        icon: "ordered-list",
        active: false,
        tooltip: "Numbered list",
        onAction: exec("InsertOrderedList"),
        onSetup: setupToggleButtonHandler(editor, "OL")
      });
      editor.ui.registry.addToggleButton("bullist", {
        icon: "unordered-list",
        active: false,
        tooltip: "Bullet list",
        onAction: exec("InsertUnorderedList"),
        onSetup: setupToggleButtonHandler(editor, "UL")
      });
    }
  };
  const setupMenuButtonHandler = (editor, listName) => (api) => {
    const menuButtonHandler = (e) => api.setEnabled(inList(e.parents, listName) && !isWithinNonEditableList(editor, e.element));
    return setNodeChangeHandler(editor, menuButtonHandler);
  };
  const register = (editor) => {
    const listProperties = {
      text: "List properties...",
      icon: "ordered-list",
      onAction: () => editor.execCommand("mceListProps"),
      onSetup: setupMenuButtonHandler(editor, "OL")
    };
    editor.ui.registry.addMenuItem("listprops", listProperties);
    editor.ui.registry.addContextMenu("lists", {
      update: (node) => {
        const parentList = getParentList(editor, node);
        return isOlNode(parentList) ? ["listprops"] : [];
      }
    });
  };
  var Plugin = () => {
    global$6.add("lists", (editor) => {
      register$3(editor);
      if (!editor.hasPlugin("rtc", true)) {
        setup(editor);
        register$2(editor);
      } else {
        registerDialog(editor);
      }
      register$1(editor);
      register(editor);
      return get(editor);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  var global$1 = tinymce.util.Tools.resolve("tinymce.Env");
  var global2 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const option = (name) => (editor) => editor.options.get(name);
  const getContentStyle = option("content_style");
  const shouldUseContentCssCors = option("content_css_cors");
  const getBodyClass = option("body_class");
  const getBodyId = option("body_id");
  const getPreviewHtml = (editor) => {
    var _a;
    let headHtml = "";
    const encode = editor.dom.encode;
    const contentStyle = (_a = getContentStyle(editor)) !== null && _a !== void 0 ? _a : "";
    headHtml += '<base href="' + encode(editor.documentBaseURI.getURI()) + '">';
    const cors = shouldUseContentCssCors(editor) ? ' crossorigin="anonymous"' : "";
    global2.each(editor.contentCSS, (url) => {
      headHtml += '<link type="text/css" rel="stylesheet" href="' + encode(editor.documentBaseURI.toAbsolute(url)) + '"' + cors + ">";
    });
    if (contentStyle) {
      headHtml += '<style type="text/css">' + contentStyle + "</style>";
    }
    const bodyId = getBodyId(editor);
    const bodyClass = getBodyClass(editor);
    const isMetaKeyPressed = global$1.os.isMacOS() || global$1.os.isiOS() ? "e.metaKey" : "e.ctrlKey && !e.altKey";
    const preventClicksOnLinksScript = '<script>document.addEventListener && document.addEventListener("click", function(e) {for (var elm = e.target; elm; elm = elm.parentNode) {if (elm.nodeName === "A" && !(' + isMetaKeyPressed + ")) {e.preventDefault();}}}, false);</script> ";
    const directionality = editor.getBody().dir;
    const dirAttr = directionality ? ' dir="' + encode(directionality) + '"' : "";
    const previewHtml = "<!DOCTYPE html><html><head>" + headHtml + '</head><body id="' + encode(bodyId) + '" class="mce-content-body ' + encode(bodyClass) + '"' + dirAttr + ">" + editor.getContent() + preventClicksOnLinksScript + "</body></html>";
    return previewHtml;
  };
  const open = (editor) => {
    const content = getPreviewHtml(editor);
    const dataApi = editor.windowManager.open({
      title: "Preview",
      size: "large",
      body: {
        type: "panel",
        items: [{
          name: "preview",
          type: "iframe",
          sandboxed: true,
          transparent: false
        }]
      },
      buttons: [{
        type: "cancel",
        name: "close",
        text: "Close",
        primary: true
      }],
      initialData: { preview: content }
    });
    dataApi.focus("close");
  };
  const register$1 = (editor) => {
    editor.addCommand("mcePreview", () => {
      open(editor);
    });
  };
  const register = (editor) => {
    const onAction = () => editor.execCommand("mcePreview");
    editor.ui.registry.addButton("preview", {
      icon: "preview",
      tooltip: "Preview",
      onAction
    });
    editor.ui.registry.addMenuItem("preview", {
      icon: "preview",
      text: "Preview",
      onAction
    });
  };
  var Plugin = () => {
    global$2.add("preview", (editor) => {
      register$1(editor);
      register(editor);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$1 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const link = () => /(?:[A-Za-z][A-Za-z\d.+-]{0,14}:\/\/(?:[-.~*+=!&;:'%@?^${}(),\w]+@)?|www\.|[-;:&=+$,.\w]+@)[A-Za-z\d-]+(?:\.[A-Za-z\d-]+)*(?::\d+)?(?:\/(?:[-.~*+=!;:'%@$(),\/\w]*[-~*+=%@$()\/\w])?)?(?:\?(?:[-.~*+=!&;:'%@?^${}(),\/\w]+))?(?:#(?:[-.~*+=!&;:'%@?^${}(),\/\w]+))?/g;
  const option = (name) => (editor) => editor.options.get(name);
  const register = (editor) => {
    const registerOption = editor.options.register;
    registerOption("autolink_pattern", {
      processor: "regexp",
      default: new RegExp("^" + link().source + "$", "i")
    });
    registerOption("link_default_target", { processor: "string" });
    registerOption("link_default_protocol", {
      processor: "string",
      default: "https"
    });
  };
  const getAutoLinkPattern = option("autolink_pattern");
  const getDefaultLinkTarget = option("link_default_target");
  const getDefaultLinkProtocol = option("link_default_protocol");
  const allowUnsafeLinkTarget = option("allow_unsafe_link_target");
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType = (type) => (value) => typeOf(value) === type;
  const eq = (t) => (a) => t === a;
  const isString = isType("string");
  const isUndefined = eq(void 0);
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const not = (f) => (t) => !f(t);
  const hasOwnProperty = Object.hasOwnProperty;
  const has = (obj, key) => hasOwnProperty.call(obj, key);
  const checkRange = (str, substr, start) => substr === "" || str.length >= substr.length && str.substr(start, start + substr.length) === substr;
  const contains = (str, substr, start = 0, end) => {
    const idx = str.indexOf(substr, start);
    if (idx !== -1) {
      return isUndefined(end) ? true : idx + substr.length <= end;
    } else {
      return false;
    }
  };
  const startsWith = (str, prefix) => {
    return checkRange(str, prefix, 0);
  };
  const zeroWidth = "\uFEFF";
  const isZwsp = (char) => char === zeroWidth;
  const removeZwsp = (s) => s.replace(/\uFEFF/g, "");
  var global2 = tinymce.util.Tools.resolve("tinymce.dom.TextSeeker");
  const isTextNode = (node) => node.nodeType === 3;
  const isElement = (node) => node.nodeType === 1;
  const isBracketOrSpace = (char) => /^[(\[{ \u00a0]$/.test(char);
  const hasProtocol = (url) => /^([A-Za-z][A-Za-z\d.+-]*:\/\/)|mailto:/.test(url);
  const isPunctuation = (char) => /[?!,.;:]/.test(char);
  const findChar = (text, index, predicate) => {
    for (let i2 = index - 1; i2 >= 0; i2--) {
      const char = text.charAt(i2);
      if (!isZwsp(char) && predicate(char)) {
        return i2;
      }
    }
    return -1;
  };
  const freefallRtl = (container, offset) => {
    let tempNode = container;
    let tempOffset = offset;
    while (isElement(tempNode) && tempNode.childNodes[tempOffset]) {
      tempNode = tempNode.childNodes[tempOffset];
      tempOffset = isTextNode(tempNode) ? tempNode.data.length : tempNode.childNodes.length;
    }
    return {
      container: tempNode,
      offset: tempOffset
    };
  };
  const parseCurrentLine = (editor, offset) => {
    var _a;
    const voidElements = editor.schema.getVoidElements();
    const autoLinkPattern = getAutoLinkPattern(editor);
    const { dom, selection } = editor;
    if (dom.getParent(selection.getNode(), "a[href]") !== null) {
      return null;
    }
    const rng = selection.getRng();
    const textSeeker = global2(dom, (node) => {
      return dom.isBlock(node) || has(voidElements, node.nodeName.toLowerCase()) || dom.getContentEditable(node) === "false";
    });
    const {
      container: endContainer,
      offset: endOffset
    } = freefallRtl(rng.endContainer, rng.endOffset);
    const root = (_a = dom.getParent(endContainer, dom.isBlock)) !== null && _a !== void 0 ? _a : dom.getRoot();
    const endSpot = textSeeker.backwards(endContainer, endOffset + offset, (node, offset2) => {
      const text = node.data;
      const idx = findChar(text, offset2, not(isBracketOrSpace));
      return idx === -1 || isPunctuation(text[idx]) ? idx : idx + 1;
    }, root);
    if (!endSpot) {
      return null;
    }
    let lastTextNode = endSpot.container;
    const startSpot = textSeeker.backwards(endSpot.container, endSpot.offset, (node, offset2) => {
      lastTextNode = node;
      const idx = findChar(node.data, offset2, isBracketOrSpace);
      return idx === -1 ? idx : idx + 1;
    }, root);
    const newRng = dom.createRng();
    if (!startSpot) {
      newRng.setStart(lastTextNode, 0);
    } else {
      newRng.setStart(startSpot.container, startSpot.offset);
    }
    newRng.setEnd(endSpot.container, endSpot.offset);
    const rngText = removeZwsp(newRng.toString());
    const matches = rngText.match(autoLinkPattern);
    if (matches) {
      let url = matches[0];
      if (startsWith(url, "www.")) {
        const protocol = getDefaultLinkProtocol(editor);
        url = protocol + "://" + url;
      } else if (contains(url, "@") && !hasProtocol(url)) {
        url = "mailto:" + url;
      }
      return {
        rng: newRng,
        url
      };
    } else {
      return null;
    }
  };
  const convertToLink = (editor, result) => {
    const { dom, selection } = editor;
    const { rng, url } = result;
    const bookmark = selection.getBookmark();
    selection.setRng(rng);
    const command = "createlink";
    const args = {
      command,
      ui: false,
      value: url
    };
    const beforeExecEvent = editor.dispatch("BeforeExecCommand", args);
    if (!beforeExecEvent.isDefaultPrevented()) {
      editor.getDoc().execCommand(command, false, url);
      editor.dispatch("ExecCommand", args);
      const defaultLinkTarget = getDefaultLinkTarget(editor);
      if (isString(defaultLinkTarget)) {
        const anchor = selection.getNode();
        dom.setAttrib(anchor, "target", defaultLinkTarget);
        if (defaultLinkTarget === "_blank" && !allowUnsafeLinkTarget(editor)) {
          dom.setAttrib(anchor, "rel", "noopener");
        }
      }
    }
    selection.moveToBookmark(bookmark);
    editor.nodeChanged();
  };
  const handleSpacebar = (editor) => {
    const result = parseCurrentLine(editor, -1);
    if (isNonNullable(result)) {
      convertToLink(editor, result);
    }
  };
  const handleBracket = handleSpacebar;
  const handleEnter = (editor) => {
    const result = parseCurrentLine(editor, 0);
    if (isNonNullable(result)) {
      convertToLink(editor, result);
    }
  };
  const setup = (editor) => {
    editor.on("keydown", (e) => {
      if (e.keyCode === 13 && !e.isDefaultPrevented()) {
        handleEnter(editor);
      }
    });
    editor.on("keyup", (e) => {
      if (e.keyCode === 32) {
        handleSpacebar(editor);
      } else if (e.keyCode === 48 && e.shiftKey || e.keyCode === 221) {
        handleBracket(editor);
      }
    });
  };
  var Plugin = () => {
    global$1.add("autolink", (editor) => {
      register(editor);
      setup(editor);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$1 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const applyListFormat = (editor, listName, styleValue) => {
    const cmd = listName === "UL" ? "InsertUnorderedList" : "InsertOrderedList";
    editor.execCommand(cmd, false, styleValue === false ? null : { "list-style-type": styleValue });
  };
  const register$2 = (editor) => {
    editor.addCommand("ApplyUnorderedListStyle", (ui, value) => {
      applyListFormat(editor, "UL", value["list-style-type"]);
    });
    editor.addCommand("ApplyOrderedListStyle", (ui, value) => {
      applyListFormat(editor, "OL", value["list-style-type"]);
    });
  };
  const option = (name) => (editor) => editor.options.get(name);
  const register$1 = (editor) => {
    const registerOption = editor.options.register;
    registerOption("advlist_number_styles", {
      processor: "string[]",
      default: "default,lower-alpha,lower-greek,lower-roman,upper-alpha,upper-roman".split(",")
    });
    registerOption("advlist_bullet_styles", {
      processor: "string[]",
      default: "default,circle,square".split(",")
    });
  };
  const getNumberStyles = option("advlist_number_styles");
  const getBulletStyles = option("advlist_bullet_styles");
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  var global2 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  class Optional {
    constructor(tag, value) {
      this.tag = tag;
      this.value = value;
    }
    static some(value) {
      return new Optional(true, value);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder) {
      if (this.tag) {
        return binder(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value) {
      return isNonNullable(value) ? Optional.some(value) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const isChildOfBody = (editor, elm) => {
    return editor.dom.isChildOf(elm, editor.getBody());
  };
  const isTableCellNode = (node) => {
    return isNonNullable(node) && /^(TH|TD)$/.test(node.nodeName);
  };
  const isListNode = (editor) => (node) => {
    return isNonNullable(node) && /^(OL|UL|DL)$/.test(node.nodeName) && isChildOfBody(editor, node);
  };
  const getSelectedStyleType = (editor) => {
    const listElm = editor.dom.getParent(editor.selection.getNode(), "ol,ul");
    const style = editor.dom.getStyle(listElm, "listStyleType");
    return Optional.from(style);
  };
  const isWithinNonEditable = (editor, element) => element !== null && editor.dom.getContentEditableParent(element) === "false";
  const isWithinNonEditableList = (editor, element) => {
    const parentList = editor.dom.getParent(element, "ol,ul,dl");
    return isWithinNonEditable(editor, parentList);
  };
  const findIndex = (list, predicate) => {
    for (let index = 0; index < list.length; index++) {
      const element = list[index];
      if (predicate(element)) {
        return index;
      }
    }
    return -1;
  };
  const styleValueToText = (styleValue) => {
    return styleValue.replace(/\-/g, " ").replace(/\b\w/g, (chr) => {
      return chr.toUpperCase();
    });
  };
  const normalizeStyleValue = (styleValue) => isNullable(styleValue) || styleValue === "default" ? "" : styleValue;
  const isWithinList = (editor, e, nodeName) => {
    const tableCellIndex = findIndex(e.parents, isTableCellNode);
    const parents = tableCellIndex !== -1 ? e.parents.slice(0, tableCellIndex) : e.parents;
    const lists = global2.grep(parents, isListNode(editor));
    return lists.length > 0 && lists[0].nodeName === nodeName;
  };
  const makeSetupHandler = (editor, nodeName) => (api) => {
    const nodeChangeHandler = (e) => {
      api.setActive(isWithinList(editor, e, nodeName));
      api.setEnabled(!isWithinNonEditableList(editor, e.element));
    };
    editor.on("NodeChange", nodeChangeHandler);
    return () => editor.off("NodeChange", nodeChangeHandler);
  };
  const addSplitButton = (editor, id, tooltip, cmd, nodeName, styles) => {
    editor.ui.registry.addSplitButton(id, {
      tooltip,
      icon: nodeName === "OL" ? "ordered-list" : "unordered-list",
      presets: "listpreview",
      columns: 3,
      fetch: (callback) => {
        const items = global2.map(styles, (styleValue) => {
          const iconStyle = nodeName === "OL" ? "num" : "bull";
          const iconName = styleValue === "disc" || styleValue === "decimal" ? "default" : styleValue;
          const itemValue = normalizeStyleValue(styleValue);
          const displayText = styleValueToText(styleValue);
          return {
            type: "choiceitem",
            value: itemValue,
            icon: "list-" + iconStyle + "-" + iconName,
            text: displayText
          };
        });
        callback(items);
      },
      onAction: () => editor.execCommand(cmd),
      onItemAction: (_splitButtonApi, value) => {
        applyListFormat(editor, nodeName, value);
      },
      select: (value) => {
        const listStyleType = getSelectedStyleType(editor);
        return listStyleType.map((listStyle) => value === listStyle).getOr(false);
      },
      onSetup: makeSetupHandler(editor, nodeName)
    });
  };
  const addButton = (editor, id, tooltip, cmd, nodeName, styleValue) => {
    editor.ui.registry.addToggleButton(id, {
      active: false,
      tooltip,
      icon: nodeName === "OL" ? "ordered-list" : "unordered-list",
      onSetup: makeSetupHandler(editor, nodeName),
      onAction: () => editor.queryCommandState(cmd) || styleValue === "" ? editor.execCommand(cmd) : applyListFormat(editor, nodeName, styleValue)
    });
  };
  const addControl = (editor, id, tooltip, cmd, nodeName, styles) => {
    if (styles.length > 1) {
      addSplitButton(editor, id, tooltip, cmd, nodeName, styles);
    } else {
      addButton(editor, id, tooltip, cmd, nodeName, normalizeStyleValue(styles[0]));
    }
  };
  const register = (editor) => {
    addControl(editor, "numlist", "Numbered list", "InsertOrderedList", "OL", getNumberStyles(editor));
    addControl(editor, "bullist", "Bullet list", "InsertUnorderedList", "UL", getBulletStyles(editor));
  };
  var Plugin = () => {
    global$1.add("advlist", (editor) => {
      if (editor.hasPlugin("lists")) {
        register$1(editor);
        register(editor);
        register$2(editor);
      } else {
        console.error("Please use the Lists plugin together with the Advanced List plugin.");
      }
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  var global$1 = tinymce.util.Tools.resolve("tinymce.dom.RangeUtils");
  var global2 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const option = (name) => (editor) => editor.options.get(name);
  const register$2 = (editor) => {
    const registerOption = editor.options.register;
    registerOption("allow_html_in_named_anchor", {
      processor: "boolean",
      default: false
    });
  };
  const allowHtmlInNamedAnchor = option("allow_html_in_named_anchor");
  const namedAnchorSelector = "a:not([href])";
  const isEmptyString = (str) => !str;
  const getIdFromAnchor = (elm) => {
    const id = elm.getAttribute("id") || elm.getAttribute("name");
    return id || "";
  };
  const isAnchor = (elm) => elm.nodeName.toLowerCase() === "a";
  const isNamedAnchor = (elm) => isAnchor(elm) && !elm.getAttribute("href") && getIdFromAnchor(elm) !== "";
  const isEmptyNamedAnchor = (elm) => isNamedAnchor(elm) && !elm.firstChild;
  const removeEmptyNamedAnchorsInSelection = (editor) => {
    const dom = editor.dom;
    global$1(dom).walk(editor.selection.getRng(), (nodes) => {
      global2.each(nodes, (node) => {
        if (isEmptyNamedAnchor(node)) {
          dom.remove(node, false);
        }
      });
    });
  };
  const isValidId = (id) => /^[A-Za-z][A-Za-z0-9\-:._]*$/.test(id);
  const getNamedAnchor = (editor) => editor.dom.getParent(editor.selection.getStart(), namedAnchorSelector);
  const getId = (editor) => {
    const anchor = getNamedAnchor(editor);
    if (anchor) {
      return getIdFromAnchor(anchor);
    } else {
      return "";
    }
  };
  const createAnchor = (editor, id) => {
    editor.undoManager.transact(() => {
      if (!allowHtmlInNamedAnchor(editor)) {
        editor.selection.collapse(true);
      }
      if (editor.selection.isCollapsed()) {
        editor.insertContent(editor.dom.createHTML("a", { id }));
      } else {
        removeEmptyNamedAnchorsInSelection(editor);
        editor.formatter.remove("namedAnchor", void 0, void 0, true);
        editor.formatter.apply("namedAnchor", { value: id });
        editor.addVisual();
      }
    });
  };
  const updateAnchor = (editor, id, anchorElement) => {
    anchorElement.removeAttribute("name");
    anchorElement.id = id;
    editor.addVisual();
    editor.undoManager.add();
  };
  const insert = (editor, id) => {
    const anchor = getNamedAnchor(editor);
    if (anchor) {
      updateAnchor(editor, id, anchor);
    } else {
      createAnchor(editor, id);
    }
    editor.focus();
  };
  const insertAnchor = (editor, newId) => {
    if (!isValidId(newId)) {
      editor.windowManager.alert("ID should start with a letter, followed only by letters, numbers, dashes, dots, colons or underscores.");
      return false;
    } else {
      insert(editor, newId);
      return true;
    }
  };
  const open = (editor) => {
    const currentId = getId(editor);
    editor.windowManager.open({
      title: "Anchor",
      size: "normal",
      body: {
        type: "panel",
        items: [{
          name: "id",
          type: "input",
          label: "ID",
          placeholder: "example"
        }]
      },
      buttons: [
        {
          type: "cancel",
          name: "cancel",
          text: "Cancel"
        },
        {
          type: "submit",
          name: "save",
          text: "Save",
          primary: true
        }
      ],
      initialData: { id: currentId },
      onSubmit: (api) => {
        if (insertAnchor(editor, api.getData().id)) {
          api.close();
        }
      }
    });
  };
  const register$1 = (editor) => {
    editor.addCommand("mceAnchor", () => {
      open(editor);
    });
  };
  const isNamedAnchorNode = (node) => isEmptyString(node.attr("href")) && !isEmptyString(node.attr("id") || node.attr("name"));
  const isEmptyNamedAnchorNode = (node) => isNamedAnchorNode(node) && !node.firstChild;
  const setContentEditable = (state) => (nodes) => {
    for (let i2 = 0; i2 < nodes.length; i2++) {
      const node = nodes[i2];
      if (isEmptyNamedAnchorNode(node)) {
        node.attr("contenteditable", state);
      }
    }
  };
  const setup = (editor) => {
    editor.on("PreInit", () => {
      editor.parser.addNodeFilter("a", setContentEditable("false"));
      editor.serializer.addNodeFilter("a", setContentEditable(null));
    });
  };
  const registerFormats = (editor) => {
    editor.formatter.register("namedAnchor", {
      inline: "a",
      selector: namedAnchorSelector,
      remove: "all",
      split: true,
      deep: true,
      attributes: { id: "%value" },
      onmatch: (node, _fmt, _itemName) => {
        return isNamedAnchor(node);
      }
    });
  };
  const register = (editor) => {
    const onAction = () => editor.execCommand("mceAnchor");
    editor.ui.registry.addToggleButton("anchor", {
      icon: "bookmark",
      tooltip: "Anchor",
      onAction,
      onSetup: (buttonApi) => editor.selection.selectorChangedWithUnbind("a:not([href])", buttonApi.setActive).unbind
    });
    editor.ui.registry.addMenuItem("anchor", {
      icon: "bookmark",
      text: "Anchor...",
      onAction
    });
  };
  var Plugin = () => {
    global$2.add("anchor", (editor) => {
      register$2(editor);
      setup(editor);
      register$1(editor);
      register(editor);
      editor.on("PreInit", () => {
        registerFormats(editor);
      });
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global$1 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const fireInsertCustomChar = (editor, chr) => {
    return editor.dispatch("insertCustomChar", { chr });
  };
  const insertChar = (editor, chr) => {
    const evtChr = fireInsertCustomChar(editor, chr).chr;
    editor.execCommand("mceInsertContent", false, evtChr);
  };
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType = (type) => (value) => typeOf(value) === type;
  const isSimpleType = (type) => (value) => typeof value === type;
  const eq = (t) => (a) => t === a;
  const isArray$1 = isType("array");
  const isNull = eq(null);
  const isUndefined = eq(void 0);
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const constant = (value) => {
    return () => {
      return value;
    };
  };
  const never = constant(false);
  class Optional {
    constructor(tag, value) {
      this.tag = tag;
      this.value = value;
    }
    static some(value) {
      return new Optional(true, value);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder) {
      if (this.tag) {
        return binder(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value) {
      return isNonNullable(value) ? Optional.some(value) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const nativePush = Array.prototype.push;
  const map = (xs, f) => {
    const len = xs.length;
    const r = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r[i2] = f(x, i2);
    }
    return r;
  };
  const each = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const findUntil = (xs, pred, until) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        return Optional.some(x);
      } else if (until(x, i2)) {
        break;
      }
    }
    return Optional.none();
  };
  const find = (xs, pred) => {
    return findUntil(xs, pred, never);
  };
  const flatten = (xs) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; ++i2) {
      if (!isArray$1(xs[i2])) {
        throw new Error("Arr.flatten item " + i2 + " was not an array, input: " + xs);
      }
      nativePush.apply(r, xs[i2]);
    }
    return r;
  };
  const bind = (xs, f) => flatten(map(xs, f));
  var global2 = tinymce.util.Tools.resolve("tinymce.util.Tools");
  const option = (name) => (editor) => editor.options.get(name);
  const register$2 = (editor) => {
    const registerOption = editor.options.register;
    const charMapProcessor = (value) => isFunction(value) || isArray$1(value);
    registerOption("charmap", { processor: charMapProcessor });
    registerOption("charmap_append", { processor: charMapProcessor });
  };
  const getCharMap$1 = option("charmap");
  const getCharMapAppend = option("charmap_append");
  const isArray = global2.isArray;
  const UserDefined = "User Defined";
  const getDefaultCharMap = () => {
    return [
      {
        name: "Currency",
        characters: [
          [
            36,
            "dollar sign"
          ],
          [
            162,
            "cent sign"
          ],
          [
            8364,
            "euro sign"
          ],
          [
            163,
            "pound sign"
          ],
          [
            165,
            "yen sign"
          ],
          [
            164,
            "currency sign"
          ],
          [
            8352,
            "euro-currency sign"
          ],
          [
            8353,
            "colon sign"
          ],
          [
            8354,
            "cruzeiro sign"
          ],
          [
            8355,
            "french franc sign"
          ],
          [
            8356,
            "lira sign"
          ],
          [
            8357,
            "mill sign"
          ],
          [
            8358,
            "naira sign"
          ],
          [
            8359,
            "peseta sign"
          ],
          [
            8360,
            "rupee sign"
          ],
          [
            8361,
            "won sign"
          ],
          [
            8362,
            "new sheqel sign"
          ],
          [
            8363,
            "dong sign"
          ],
          [
            8365,
            "kip sign"
          ],
          [
            8366,
            "tugrik sign"
          ],
          [
            8367,
            "drachma sign"
          ],
          [
            8368,
            "german penny symbol"
          ],
          [
            8369,
            "peso sign"
          ],
          [
            8370,
            "guarani sign"
          ],
          [
            8371,
            "austral sign"
          ],
          [
            8372,
            "hryvnia sign"
          ],
          [
            8373,
            "cedi sign"
          ],
          [
            8374,
            "livre tournois sign"
          ],
          [
            8375,
            "spesmilo sign"
          ],
          [
            8376,
            "tenge sign"
          ],
          [
            8377,
            "indian rupee sign"
          ],
          [
            8378,
            "turkish lira sign"
          ],
          [
            8379,
            "nordic mark sign"
          ],
          [
            8380,
            "manat sign"
          ],
          [
            8381,
            "ruble sign"
          ],
          [
            20870,
            "yen character"
          ],
          [
            20803,
            "yuan character"
          ],
          [
            22291,
            "yuan character, in hong kong and taiwan"
          ],
          [
            22278,
            "yen/yuan character variant one"
          ]
        ]
      },
      {
        name: "Text",
        characters: [
          [
            169,
            "copyright sign"
          ],
          [
            174,
            "registered sign"
          ],
          [
            8482,
            "trade mark sign"
          ],
          [
            8240,
            "per mille sign"
          ],
          [
            181,
            "micro sign"
          ],
          [
            183,
            "middle dot"
          ],
          [
            8226,
            "bullet"
          ],
          [
            8230,
            "three dot leader"
          ],
          [
            8242,
            "minutes / feet"
          ],
          [
            8243,
            "seconds / inches"
          ],
          [
            167,
            "section sign"
          ],
          [
            182,
            "paragraph sign"
          ],
          [
            223,
            "sharp s / ess-zed"
          ]
        ]
      },
      {
        name: "Quotations",
        characters: [
          [
            8249,
            "single left-pointing angle quotation mark"
          ],
          [
            8250,
            "single right-pointing angle quotation mark"
          ],
          [
            171,
            "left pointing guillemet"
          ],
          [
            187,
            "right pointing guillemet"
          ],
          [
            8216,
            "left single quotation mark"
          ],
          [
            8217,
            "right single quotation mark"
          ],
          [
            8220,
            "left double quotation mark"
          ],
          [
            8221,
            "right double quotation mark"
          ],
          [
            8218,
            "single low-9 quotation mark"
          ],
          [
            8222,
            "double low-9 quotation mark"
          ],
          [
            60,
            "less-than sign"
          ],
          [
            62,
            "greater-than sign"
          ],
          [
            8804,
            "less-than or equal to"
          ],
          [
            8805,
            "greater-than or equal to"
          ],
          [
            8211,
            "en dash"
          ],
          [
            8212,
            "em dash"
          ],
          [
            175,
            "macron"
          ],
          [
            8254,
            "overline"
          ],
          [
            164,
            "currency sign"
          ],
          [
            166,
            "broken bar"
          ],
          [
            168,
            "diaeresis"
          ],
          [
            161,
            "inverted exclamation mark"
          ],
          [
            191,
            "turned question mark"
          ],
          [
            710,
            "circumflex accent"
          ],
          [
            732,
            "small tilde"
          ],
          [
            176,
            "degree sign"
          ],
          [
            8722,
            "minus sign"
          ],
          [
            177,
            "plus-minus sign"
          ],
          [
            247,
            "division sign"
          ],
          [
            8260,
            "fraction slash"
          ],
          [
            215,
            "multiplication sign"
          ],
          [
            185,
            "superscript one"
          ],
          [
            178,
            "superscript two"
          ],
          [
            179,
            "superscript three"
          ],
          [
            188,
            "fraction one quarter"
          ],
          [
            189,
            "fraction one half"
          ],
          [
            190,
            "fraction three quarters"
          ]
        ]
      },
      {
        name: "Mathematical",
        characters: [
          [
            402,
            "function / florin"
          ],
          [
            8747,
            "integral"
          ],
          [
            8721,
            "n-ary sumation"
          ],
          [
            8734,
            "infinity"
          ],
          [
            8730,
            "square root"
          ],
          [
            8764,
            "similar to"
          ],
          [
            8773,
            "approximately equal to"
          ],
          [
            8776,
            "almost equal to"
          ],
          [
            8800,
            "not equal to"
          ],
          [
            8801,
            "identical to"
          ],
          [
            8712,
            "element of"
          ],
          [
            8713,
            "not an element of"
          ],
          [
            8715,
            "contains as member"
          ],
          [
            8719,
            "n-ary product"
          ],
          [
            8743,
            "logical and"
          ],
          [
            8744,
            "logical or"
          ],
          [
            172,
            "not sign"
          ],
          [
            8745,
            "intersection"
          ],
          [
            8746,
            "union"
          ],
          [
            8706,
            "partial differential"
          ],
          [
            8704,
            "for all"
          ],
          [
            8707,
            "there exists"
          ],
          [
            8709,
            "diameter"
          ],
          [
            8711,
            "backward difference"
          ],
          [
            8727,
            "asterisk operator"
          ],
          [
            8733,
            "proportional to"
          ],
          [
            8736,
            "angle"
          ]
        ]
      },
      {
        name: "Extended Latin",
        characters: [
          [
            192,
            "A - grave"
          ],
          [
            193,
            "A - acute"
          ],
          [
            194,
            "A - circumflex"
          ],
          [
            195,
            "A - tilde"
          ],
          [
            196,
            "A - diaeresis"
          ],
          [
            197,
            "A - ring above"
          ],
          [
            256,
            "A - macron"
          ],
          [
            198,
            "ligature AE"
          ],
          [
            199,
            "C - cedilla"
          ],
          [
            200,
            "E - grave"
          ],
          [
            201,
            "E - acute"
          ],
          [
            202,
            "E - circumflex"
          ],
          [
            203,
            "E - diaeresis"
          ],
          [
            274,
            "E - macron"
          ],
          [
            204,
            "I - grave"
          ],
          [
            205,
            "I - acute"
          ],
          [
            206,
            "I - circumflex"
          ],
          [
            207,
            "I - diaeresis"
          ],
          [
            298,
            "I - macron"
          ],
          [
            208,
            "ETH"
          ],
          [
            209,
            "N - tilde"
          ],
          [
            210,
            "O - grave"
          ],
          [
            211,
            "O - acute"
          ],
          [
            212,
            "O - circumflex"
          ],
          [
            213,
            "O - tilde"
          ],
          [
            214,
            "O - diaeresis"
          ],
          [
            216,
            "O - slash"
          ],
          [
            332,
            "O - macron"
          ],
          [
            338,
            "ligature OE"
          ],
          [
            352,
            "S - caron"
          ],
          [
            217,
            "U - grave"
          ],
          [
            218,
            "U - acute"
          ],
          [
            219,
            "U - circumflex"
          ],
          [
            220,
            "U - diaeresis"
          ],
          [
            362,
            "U - macron"
          ],
          [
            221,
            "Y - acute"
          ],
          [
            376,
            "Y - diaeresis"
          ],
          [
            562,
            "Y - macron"
          ],
          [
            222,
            "THORN"
          ],
          [
            224,
            "a - grave"
          ],
          [
            225,
            "a - acute"
          ],
          [
            226,
            "a - circumflex"
          ],
          [
            227,
            "a - tilde"
          ],
          [
            228,
            "a - diaeresis"
          ],
          [
            229,
            "a - ring above"
          ],
          [
            257,
            "a - macron"
          ],
          [
            230,
            "ligature ae"
          ],
          [
            231,
            "c - cedilla"
          ],
          [
            232,
            "e - grave"
          ],
          [
            233,
            "e - acute"
          ],
          [
            234,
            "e - circumflex"
          ],
          [
            235,
            "e - diaeresis"
          ],
          [
            275,
            "e - macron"
          ],
          [
            236,
            "i - grave"
          ],
          [
            237,
            "i - acute"
          ],
          [
            238,
            "i - circumflex"
          ],
          [
            239,
            "i - diaeresis"
          ],
          [
            299,
            "i - macron"
          ],
          [
            240,
            "eth"
          ],
          [
            241,
            "n - tilde"
          ],
          [
            242,
            "o - grave"
          ],
          [
            243,
            "o - acute"
          ],
          [
            244,
            "o - circumflex"
          ],
          [
            245,
            "o - tilde"
          ],
          [
            246,
            "o - diaeresis"
          ],
          [
            248,
            "o slash"
          ],
          [
            333,
            "o macron"
          ],
          [
            339,
            "ligature oe"
          ],
          [
            353,
            "s - caron"
          ],
          [
            249,
            "u - grave"
          ],
          [
            250,
            "u - acute"
          ],
          [
            251,
            "u - circumflex"
          ],
          [
            252,
            "u - diaeresis"
          ],
          [
            363,
            "u - macron"
          ],
          [
            253,
            "y - acute"
          ],
          [
            254,
            "thorn"
          ],
          [
            255,
            "y - diaeresis"
          ],
          [
            563,
            "y - macron"
          ],
          [
            913,
            "Alpha"
          ],
          [
            914,
            "Beta"
          ],
          [
            915,
            "Gamma"
          ],
          [
            916,
            "Delta"
          ],
          [
            917,
            "Epsilon"
          ],
          [
            918,
            "Zeta"
          ],
          [
            919,
            "Eta"
          ],
          [
            920,
            "Theta"
          ],
          [
            921,
            "Iota"
          ],
          [
            922,
            "Kappa"
          ],
          [
            923,
            "Lambda"
          ],
          [
            924,
            "Mu"
          ],
          [
            925,
            "Nu"
          ],
          [
            926,
            "Xi"
          ],
          [
            927,
            "Omicron"
          ],
          [
            928,
            "Pi"
          ],
          [
            929,
            "Rho"
          ],
          [
            931,
            "Sigma"
          ],
          [
            932,
            "Tau"
          ],
          [
            933,
            "Upsilon"
          ],
          [
            934,
            "Phi"
          ],
          [
            935,
            "Chi"
          ],
          [
            936,
            "Psi"
          ],
          [
            937,
            "Omega"
          ],
          [
            945,
            "alpha"
          ],
          [
            946,
            "beta"
          ],
          [
            947,
            "gamma"
          ],
          [
            948,
            "delta"
          ],
          [
            949,
            "epsilon"
          ],
          [
            950,
            "zeta"
          ],
          [
            951,
            "eta"
          ],
          [
            952,
            "theta"
          ],
          [
            953,
            "iota"
          ],
          [
            954,
            "kappa"
          ],
          [
            955,
            "lambda"
          ],
          [
            956,
            "mu"
          ],
          [
            957,
            "nu"
          ],
          [
            958,
            "xi"
          ],
          [
            959,
            "omicron"
          ],
          [
            960,
            "pi"
          ],
          [
            961,
            "rho"
          ],
          [
            962,
            "final sigma"
          ],
          [
            963,
            "sigma"
          ],
          [
            964,
            "tau"
          ],
          [
            965,
            "upsilon"
          ],
          [
            966,
            "phi"
          ],
          [
            967,
            "chi"
          ],
          [
            968,
            "psi"
          ],
          [
            969,
            "omega"
          ]
        ]
      },
      {
        name: "Symbols",
        characters: [
          [
            8501,
            "alef symbol"
          ],
          [
            982,
            "pi symbol"
          ],
          [
            8476,
            "real part symbol"
          ],
          [
            978,
            "upsilon - hook symbol"
          ],
          [
            8472,
            "Weierstrass p"
          ],
          [
            8465,
            "imaginary part"
          ]
        ]
      },
      {
        name: "Arrows",
        characters: [
          [
            8592,
            "leftwards arrow"
          ],
          [
            8593,
            "upwards arrow"
          ],
          [
            8594,
            "rightwards arrow"
          ],
          [
            8595,
            "downwards arrow"
          ],
          [
            8596,
            "left right arrow"
          ],
          [
            8629,
            "carriage return"
          ],
          [
            8656,
            "leftwards double arrow"
          ],
          [
            8657,
            "upwards double arrow"
          ],
          [
            8658,
            "rightwards double arrow"
          ],
          [
            8659,
            "downwards double arrow"
          ],
          [
            8660,
            "left right double arrow"
          ],
          [
            8756,
            "therefore"
          ],
          [
            8834,
            "subset of"
          ],
          [
            8835,
            "superset of"
          ],
          [
            8836,
            "not a subset of"
          ],
          [
            8838,
            "subset of or equal to"
          ],
          [
            8839,
            "superset of or equal to"
          ],
          [
            8853,
            "circled plus"
          ],
          [
            8855,
            "circled times"
          ],
          [
            8869,
            "perpendicular"
          ],
          [
            8901,
            "dot operator"
          ],
          [
            8968,
            "left ceiling"
          ],
          [
            8969,
            "right ceiling"
          ],
          [
            8970,
            "left floor"
          ],
          [
            8971,
            "right floor"
          ],
          [
            9001,
            "left-pointing angle bracket"
          ],
          [
            9002,
            "right-pointing angle bracket"
          ],
          [
            9674,
            "lozenge"
          ],
          [
            9824,
            "black spade suit"
          ],
          [
            9827,
            "black club suit"
          ],
          [
            9829,
            "black heart suit"
          ],
          [
            9830,
            "black diamond suit"
          ],
          [
            8194,
            "en space"
          ],
          [
            8195,
            "em space"
          ],
          [
            8201,
            "thin space"
          ],
          [
            8204,
            "zero width non-joiner"
          ],
          [
            8205,
            "zero width joiner"
          ],
          [
            8206,
            "left-to-right mark"
          ],
          [
            8207,
            "right-to-left mark"
          ]
        ]
      }
    ];
  };
  const charmapFilter = (charmap) => {
    return global2.grep(charmap, (item) => {
      return isArray(item) && item.length === 2;
    });
  };
  const getCharsFromOption = (optionValue) => {
    if (isArray(optionValue)) {
      return charmapFilter(optionValue);
    }
    if (typeof optionValue === "function") {
      return optionValue();
    }
    return [];
  };
  const extendCharMap = (editor, charmap) => {
    const userCharMap = getCharMap$1(editor);
    if (userCharMap) {
      charmap = [{
        name: UserDefined,
        characters: getCharsFromOption(userCharMap)
      }];
    }
    const userCharMapAppend = getCharMapAppend(editor);
    if (userCharMapAppend) {
      const userDefinedGroup = global2.grep(charmap, (cg) => cg.name === UserDefined);
      if (userDefinedGroup.length) {
        userDefinedGroup[0].characters = [
          ...userDefinedGroup[0].characters,
          ...getCharsFromOption(userCharMapAppend)
        ];
        return charmap;
      }
      return charmap.concat({
        name: UserDefined,
        characters: getCharsFromOption(userCharMapAppend)
      });
    }
    return charmap;
  };
  const getCharMap = (editor) => {
    const groups = extendCharMap(editor, getDefaultCharMap());
    return groups.length > 1 ? [{
      name: "All",
      characters: bind(groups, (g) => g.characters)
    }].concat(groups) : groups;
  };
  const get = (editor) => {
    const getCharMap$12 = () => {
      return getCharMap(editor);
    };
    const insertChar$1 = (chr) => {
      insertChar(editor, chr);
    };
    return {
      getCharMap: getCharMap$12,
      insertChar: insertChar$1
    };
  };
  const Cell = (initial) => {
    let value = initial;
    const get2 = () => {
      return value;
    };
    const set = (v) => {
      value = v;
    };
    return {
      get: get2,
      set
    };
  };
  const last = (fn, rate) => {
    let timer = null;
    const cancel = () => {
      if (!isNull(timer)) {
        clearTimeout(timer);
        timer = null;
      }
    };
    const throttle = (...args) => {
      cancel();
      timer = setTimeout(() => {
        timer = null;
        fn.apply(null, args);
      }, rate);
    };
    return {
      cancel,
      throttle
    };
  };
  const contains = (str, substr, start = 0, end) => {
    const idx = str.indexOf(substr, start);
    if (idx !== -1) {
      return isUndefined(end) ? true : idx + substr.length <= end;
    } else {
      return false;
    }
  };
  const fromCodePoint = String.fromCodePoint;
  const charMatches = (charCode, name, lowerCasePattern) => {
    if (contains(fromCodePoint(charCode).toLowerCase(), lowerCasePattern)) {
      return true;
    } else {
      return contains(name.toLowerCase(), lowerCasePattern) || contains(name.toLowerCase().replace(/\s+/g, ""), lowerCasePattern);
    }
  };
  const scan = (group, pattern) => {
    const matches = [];
    const lowerCasePattern = pattern.toLowerCase();
    each(group.characters, (g) => {
      if (charMatches(g[0], g[1], lowerCasePattern)) {
        matches.push(g);
      }
    });
    return map(matches, (m) => ({
      text: m[1],
      value: fromCodePoint(m[0]),
      icon: fromCodePoint(m[0])
    }));
  };
  const patternName = "pattern";
  const open = (editor, charMap) => {
    const makeGroupItems = () => [
      {
        label: "Search",
        type: "input",
        name: patternName
      },
      {
        type: "collection",
        name: "results"
      }
    ];
    const makeTabs = () => map(charMap, (charGroup) => ({
      title: charGroup.name,
      name: charGroup.name,
      items: makeGroupItems()
    }));
    const makePanel = () => ({
      type: "panel",
      items: makeGroupItems()
    });
    const makeTabPanel = () => ({
      type: "tabpanel",
      tabs: makeTabs()
    });
    const currentTab = charMap.length === 1 ? Cell(UserDefined) : Cell("All");
    const scanAndSet = (dialogApi2, pattern) => {
      find(charMap, (group) => group.name === currentTab.get()).each((f) => {
        const items = scan(f, pattern);
        dialogApi2.setData({ results: items });
      });
    };
    const SEARCH_DELAY = 40;
    const updateFilter = last((dialogApi2) => {
      const pattern = dialogApi2.getData().pattern;
      scanAndSet(dialogApi2, pattern);
    }, SEARCH_DELAY);
    const body = charMap.length === 1 ? makePanel() : makeTabPanel();
    const initialData = {
      pattern: "",
      results: scan(charMap[0], "")
    };
    const bridgeSpec = {
      title: "Special Character",
      size: "normal",
      body,
      buttons: [{
        type: "cancel",
        name: "close",
        text: "Close",
        primary: true
      }],
      initialData,
      onAction: (api, details) => {
        if (details.name === "results") {
          insertChar(editor, details.value);
          api.close();
        }
      },
      onTabChange: (dialogApi2, details) => {
        currentTab.set(details.newTabName);
        updateFilter.throttle(dialogApi2);
      },
      onChange: (dialogApi2, changeData) => {
        if (changeData.name === patternName) {
          updateFilter.throttle(dialogApi2);
        }
      }
    };
    const dialogApi = editor.windowManager.open(bridgeSpec);
    dialogApi.focus(patternName);
  };
  const register$1 = (editor, charMap) => {
    editor.addCommand("mceShowCharmap", () => {
      open(editor, charMap);
    });
  };
  const init = (editor, all) => {
    editor.ui.registry.addAutocompleter("charmap", {
      trigger: ":",
      columns: "auto",
      minChars: 2,
      fetch: (pattern, _maxResults) => new Promise((resolve, _reject) => {
        resolve(scan(all, pattern));
      }),
      onAction: (autocompleteApi, rng, value) => {
        editor.selection.setRng(rng);
        editor.insertContent(value);
        autocompleteApi.hide();
      }
    });
  };
  const register = (editor) => {
    const onAction = () => editor.execCommand("mceShowCharmap");
    editor.ui.registry.addButton("charmap", {
      icon: "insert-character",
      tooltip: "Special character",
      onAction
    });
    editor.ui.registry.addMenuItem("charmap", {
      icon: "insert-character",
      text: "Special character...",
      onAction
    });
  };
  var Plugin = () => {
    global$1.add("charmap", (editor) => {
      register$2(editor);
      const charMap = getCharMap(editor);
      register$1(editor, charMap);
      register(editor);
      init(editor, charMap[0]);
      return get(editor);
    });
  };
  Plugin();
})();
(function() {
  "use strict";
  var global2 = tinymce.util.Tools.resolve("tinymce.PluginManager");
  const hasProto = (v, constructor, predicate) => {
    var _a;
    if (predicate(v, constructor.prototype)) {
      return true;
    } else {
      return ((_a = v.constructor) === null || _a === void 0 ? void 0 : _a.name) === constructor.name;
    }
  };
  const typeOf = (x) => {
    const t = typeof x;
    if (x === null) {
      return "null";
    } else if (t === "object" && Array.isArray(x)) {
      return "array";
    } else if (t === "object" && hasProto(x, String, (o, proto) => proto.isPrototypeOf(o))) {
      return "string";
    } else {
      return t;
    }
  };
  const isType$1 = (type2) => (value) => typeOf(value) === type2;
  const isSimpleType = (type2) => (value) => typeof value === type2;
  const isString = isType$1("string");
  const isBoolean = isSimpleType("boolean");
  const isNullable = (a) => a === null || a === void 0;
  const isNonNullable = (a) => !isNullable(a);
  const isFunction = isSimpleType("function");
  const isNumber = isSimpleType("number");
  const compose1 = (fbc, fab) => (a) => fbc(fab(a));
  const constant = (value) => {
    return () => {
      return value;
    };
  };
  const never = constant(false);
  class Optional {
    constructor(tag, value) {
      this.tag = tag;
      this.value = value;
    }
    static some(value) {
      return new Optional(true, value);
    }
    static none() {
      return Optional.singletonNone;
    }
    fold(onNone, onSome) {
      if (this.tag) {
        return onSome(this.value);
      } else {
        return onNone();
      }
    }
    isSome() {
      return this.tag;
    }
    isNone() {
      return !this.tag;
    }
    map(mapper) {
      if (this.tag) {
        return Optional.some(mapper(this.value));
      } else {
        return Optional.none();
      }
    }
    bind(binder) {
      if (this.tag) {
        return binder(this.value);
      } else {
        return Optional.none();
      }
    }
    exists(predicate) {
      return this.tag && predicate(this.value);
    }
    forall(predicate) {
      return !this.tag || predicate(this.value);
    }
    filter(predicate) {
      if (!this.tag || predicate(this.value)) {
        return this;
      } else {
        return Optional.none();
      }
    }
    getOr(replacement) {
      return this.tag ? this.value : replacement;
    }
    or(replacement) {
      return this.tag ? this : replacement;
    }
    getOrThunk(thunk) {
      return this.tag ? this.value : thunk();
    }
    orThunk(thunk) {
      return this.tag ? this : thunk();
    }
    getOrDie(message) {
      if (!this.tag) {
        throw new Error(message !== null && message !== void 0 ? message : "Called getOrDie on None");
      } else {
        return this.value;
      }
    }
    static from(value) {
      return isNonNullable(value) ? Optional.some(value) : Optional.none();
    }
    getOrNull() {
      return this.tag ? this.value : null;
    }
    getOrUndefined() {
      return this.value;
    }
    each(worker) {
      if (this.tag) {
        worker(this.value);
      }
    }
    toArray() {
      return this.tag ? [this.value] : [];
    }
    toString() {
      return this.tag ? `some(${this.value})` : "none()";
    }
  }
  Optional.singletonNone = new Optional(false);
  const map = (xs, f) => {
    const len = xs.length;
    const r = new Array(len);
    for (let i2 = 0; i2 < len; i2++) {
      const x = xs[i2];
      r[i2] = f(x, i2);
    }
    return r;
  };
  const each = (xs, f) => {
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      f(x, i2);
    }
  };
  const filter = (xs, pred) => {
    const r = [];
    for (let i2 = 0, len = xs.length; i2 < len; i2++) {
      const x = xs[i2];
      if (pred(x, i2)) {
        r.push(x);
      }
    }
    return r;
  };
  const DOCUMENT = 9;
  const DOCUMENT_FRAGMENT = 11;
  const ELEMENT = 1;
  const TEXT = 3;
  const fromHtml = (html, scope) => {
    const doc = scope || document;
    const div = doc.createElement("div");
    div.innerHTML = html;
    if (!div.hasChildNodes() || div.childNodes.length > 1) {
      const message = "HTML does not have a single root node";
      console.error(message, html);
      throw new Error(message);
    }
    return fromDom(div.childNodes[0]);
  };
  const fromTag = (tag, scope) => {
    const doc = scope || document;
    const node = doc.createElement(tag);
    return fromDom(node);
  };
  const fromText = (text, scope) => {
    const doc = scope || document;
    const node = doc.createTextNode(text);
    return fromDom(node);
  };
  const fromDom = (node) => {
    if (node === null || node === void 0) {
      throw new Error("Node cannot be null or undefined");
    }
    return { dom: node };
  };
  const fromPoint = (docElm, x, y) => Optional.from(docElm.dom.elementFromPoint(x, y)).map(fromDom);
  const SugarElement = {
    fromHtml,
    fromTag,
    fromText,
    fromDom,
    fromPoint
  };
  const is = (element, selector) => {
    const dom = element.dom;
    if (dom.nodeType !== ELEMENT) {
      return false;
    } else {
      const elem = dom;
      if (elem.matches !== void 0) {
        return elem.matches(selector);
      } else if (elem.msMatchesSelector !== void 0) {
        return elem.msMatchesSelector(selector);
      } else if (elem.webkitMatchesSelector !== void 0) {
        return elem.webkitMatchesSelector(selector);
      } else if (elem.mozMatchesSelector !== void 0) {
        return elem.mozMatchesSelector(selector);
      } else {
        throw new Error("Browser lacks native selectors");
      }
    }
  };
  typeof window !== "undefined" ? window : Function("return this;")();
  const name = (element) => {
    const r = element.dom.nodeName;
    return r.toLowerCase();
  };
  const type = (element) => element.dom.nodeType;
  const isType = (t) => (element) => type(element) === t;
  const isElement = isType(ELEMENT);
  const isText = isType(TEXT);
  const isDocument = isType(DOCUMENT);
  const isDocumentFragment = isType(DOCUMENT_FRAGMENT);
  const isTag = (tag) => (e) => isElement(e) && name(e) === tag;
  const owner = (element) => SugarElement.fromDom(element.dom.ownerDocument);
  const documentOrOwner = (dos) => isDocument(dos) ? dos : owner(dos);
  const parent = (element) => Optional.from(element.dom.parentNode).map(SugarElement.fromDom);
  const children$2 = (element) => map(element.dom.childNodes, SugarElement.fromDom);
  const rawSet = (dom, key, value) => {
    if (isString(value) || isBoolean(value) || isNumber(value)) {
      dom.setAttribute(key, value + "");
    } else {
      console.error("Invalid call to Attribute.set. Key ", key, ":: Value ", value, ":: Element ", dom);
      throw new Error("Attribute value was not simple");
    }
  };
  const set = (element, key, value) => {
    rawSet(element.dom, key, value);
  };
  const remove = (element, key) => {
    element.dom.removeAttribute(key);
  };
  const isShadowRoot = (dos) => isDocumentFragment(dos) && isNonNullable(dos.dom.host);
  const supported = isFunction(Element.prototype.attachShadow) && isFunction(Node.prototype.getRootNode);
  const getRootNode = supported ? (e) => SugarElement.fromDom(e.dom.getRootNode()) : documentOrOwner;
  const getShadowRoot = (e) => {
    const r = getRootNode(e);
    return isShadowRoot(r) ? Optional.some(r) : Optional.none();
  };
  const getShadowHost = (e) => SugarElement.fromDom(e.dom.host);
  const inBody = (element) => {
    const dom = isText(element) ? element.dom.parentNode : element.dom;
    if (dom === void 0 || dom === null || dom.ownerDocument === null) {
      return false;
    }
    const doc = dom.ownerDocument;
    return getShadowRoot(SugarElement.fromDom(dom)).fold(() => doc.body.contains(dom), compose1(inBody, getShadowHost));
  };
  const ancestor$1 = (scope, predicate, isRoot) => {
    let element = scope.dom;
    const stop = isFunction(isRoot) ? isRoot : never;
    while (element.parentNode) {
      element = element.parentNode;
      const el = SugarElement.fromDom(element);
      if (predicate(el)) {
        return Optional.some(el);
      } else if (stop(el)) {
        break;
      }
    }
    return Optional.none();
  };
  const ancestor = (scope, selector, isRoot) => ancestor$1(scope, (e) => is(e, selector), isRoot);
  const isSupported = (dom) => dom.style !== void 0 && isFunction(dom.style.getPropertyValue);
  const get = (element, property) => {
    const dom = element.dom;
    const styles = window.getComputedStyle(dom);
    const r = styles.getPropertyValue(property);
    return r === "" && !inBody(element) ? getUnsafeProperty(dom, property) : r;
  };
  const getUnsafeProperty = (dom, property) => isSupported(dom) ? dom.style.getPropertyValue(property) : "";
  const getDirection = (element) => get(element, "direction") === "rtl" ? "rtl" : "ltr";
  const children$1 = (scope, predicate) => filter(children$2(scope), predicate);
  const children = (scope, selector) => children$1(scope, (e) => is(e, selector));
  const getParentElement = (element) => parent(element).filter(isElement);
  const getNormalizedBlock = (element, isListItem2) => {
    const normalizedElement = isListItem2 ? ancestor(element, "ol,ul") : Optional.some(element);
    return normalizedElement.getOr(element);
  };
  const isListItem = isTag("li");
  const setDir = (editor, dir) => {
    const selectedBlocks = editor.selection.getSelectedBlocks();
    if (selectedBlocks.length > 0) {
      each(selectedBlocks, (block) => {
        const blockElement = SugarElement.fromDom(block);
        const isBlockElementListItem = isListItem(blockElement);
        const normalizedBlock = getNormalizedBlock(blockElement, isBlockElementListItem);
        const normalizedBlockParent = getParentElement(normalizedBlock);
        normalizedBlockParent.each((parent2) => {
          const parentDirection = getDirection(parent2);
          if (parentDirection !== dir) {
            set(normalizedBlock, "dir", dir);
          } else if (getDirection(normalizedBlock) !== dir) {
            remove(normalizedBlock, "dir");
          }
          if (isBlockElementListItem) {
            const listItems = children(normalizedBlock, "li[dir]");
            each(listItems, (listItem) => remove(listItem, "dir"));
          }
        });
      });
      editor.nodeChanged();
    }
  };
  const register$1 = (editor) => {
    editor.addCommand("mceDirectionLTR", () => {
      setDir(editor, "ltr");
    });
    editor.addCommand("mceDirectionRTL", () => {
      setDir(editor, "rtl");
    });
  };
  const getNodeChangeHandler = (editor, dir) => (api) => {
    const nodeChangeHandler = (e) => {
      const element = SugarElement.fromDom(e.element);
      api.setActive(getDirection(element) === dir);
    };
    editor.on("NodeChange", nodeChangeHandler);
    return () => editor.off("NodeChange", nodeChangeHandler);
  };
  const register = (editor) => {
    editor.ui.registry.addToggleButton("ltr", {
      tooltip: "Left to right",
      icon: "ltr",
      onAction: () => editor.execCommand("mceDirectionLTR"),
      onSetup: getNodeChangeHandler(editor, "ltr")
    });
    editor.ui.registry.addToggleButton("rtl", {
      tooltip: "Right to left",
      icon: "rtl",
      onAction: () => editor.execCommand("mceDirectionRTL"),
      onSetup: getNodeChangeHandler(editor, "rtl")
    });
  };
  var Plugin = () => {
    global2.add("directionality", (editor) => {
      register$1(editor);
      register(editor);
    });
  };
  Plugin();
})();
/*! jQuery v3.6.0 | (c) OpenJS Foundation and other contributors | jquery.org/license */
!function(e, t) {
  "use strict";
  "object" == typeof module && "object" == typeof module.exports ? module.exports = e.document ? t(e, true) : function(e2) {
    if (!e2.document) throw new Error("jQuery requires a window with a document");
    return t(e2);
  } : t(e);
}("undefined" != typeof window ? window : this, function(C, e) {
  "use strict";
  var t = [], r = Object.getPrototypeOf, s = t.slice, g = t.flat ? function(e2) {
    return t.flat.call(e2);
  } : function(e2) {
    return t.concat.apply([], e2);
  }, u = t.push, i2 = t.indexOf, n = {}, o = n.toString, v = n.hasOwnProperty, a = v.toString, l = a.call(Object), y = {}, m = function(e2) {
    return "function" == typeof e2 && "number" != typeof e2.nodeType && "function" != typeof e2.item;
  }, x = function(e2) {
    return null != e2 && e2 === e2.window;
  }, E = C.document, c = { type: true, src: true, nonce: true, noModule: true };
  function b(e2, t2, n2) {
    var r2, i3, o2 = (n2 = n2 || E).createElement("script");
    if (o2.text = e2, t2) for (r2 in c) (i3 = t2[r2] || t2.getAttribute && t2.getAttribute(r2)) && o2.setAttribute(r2, i3);
    n2.head.appendChild(o2).parentNode.removeChild(o2);
  }
  function w(e2) {
    return null == e2 ? e2 + "" : "object" == typeof e2 || "function" == typeof e2 ? n[o.call(e2)] || "object" : typeof e2;
  }
  var f = "3.6.0", S = function(e2, t2) {
    return new S.fn.init(e2, t2);
  };
  function p(e2) {
    var t2 = !!e2 && "length" in e2 && e2.length, n2 = w(e2);
    return !m(e2) && !x(e2) && ("array" === n2 || 0 === t2 || "number" == typeof t2 && 0 < t2 && t2 - 1 in e2);
  }
  S.fn = S.prototype = { jquery: f, constructor: S, length: 0, toArray: function() {
    return s.call(this);
  }, get: function(e2) {
    return null == e2 ? s.call(this) : e2 < 0 ? this[e2 + this.length] : this[e2];
  }, pushStack: function(e2) {
    var t2 = S.merge(this.constructor(), e2);
    return t2.prevObject = this, t2;
  }, each: function(e2) {
    return S.each(this, e2);
  }, map: function(n2) {
    return this.pushStack(S.map(this, function(e2, t2) {
      return n2.call(e2, t2, e2);
    }));
  }, slice: function() {
    return this.pushStack(s.apply(this, arguments));
  }, first: function() {
    return this.eq(0);
  }, last: function() {
    return this.eq(-1);
  }, even: function() {
    return this.pushStack(S.grep(this, function(e2, t2) {
      return (t2 + 1) % 2;
    }));
  }, odd: function() {
    return this.pushStack(S.grep(this, function(e2, t2) {
      return t2 % 2;
    }));
  }, eq: function(e2) {
    var t2 = this.length, n2 = +e2 + (e2 < 0 ? t2 : 0);
    return this.pushStack(0 <= n2 && n2 < t2 ? [this[n2]] : []);
  }, end: function() {
    return this.prevObject || this.constructor();
  }, push: u, sort: t.sort, splice: t.splice }, S.extend = S.fn.extend = function() {
    var e2, t2, n2, r2, i3, o2, a2 = arguments[0] || {}, s2 = 1, u2 = arguments.length, l2 = false;
    for ("boolean" == typeof a2 && (l2 = a2, a2 = arguments[s2] || {}, s2++), "object" == typeof a2 || m(a2) || (a2 = {}), s2 === u2 && (a2 = this, s2--); s2 < u2; s2++) if (null != (e2 = arguments[s2])) for (t2 in e2) r2 = e2[t2], "__proto__" !== t2 && a2 !== r2 && (l2 && r2 && (S.isPlainObject(r2) || (i3 = Array.isArray(r2))) ? (n2 = a2[t2], o2 = i3 && !Array.isArray(n2) ? [] : i3 || S.isPlainObject(n2) ? n2 : {}, i3 = false, a2[t2] = S.extend(l2, o2, r2)) : void 0 !== r2 && (a2[t2] = r2));
    return a2;
  }, S.extend({ expando: "jQuery" + (f + Math.random()).replace(/\D/g, ""), isReady: true, error: function(e2) {
    throw new Error(e2);
  }, noop: function() {
  }, isPlainObject: function(e2) {
    var t2, n2;
    return !(!e2 || "[object Object]" !== o.call(e2)) && (!(t2 = r(e2)) || "function" == typeof (n2 = v.call(t2, "constructor") && t2.constructor) && a.call(n2) === l);
  }, isEmptyObject: function(e2) {
    var t2;
    for (t2 in e2) return false;
    return true;
  }, globalEval: function(e2, t2, n2) {
    b(e2, { nonce: t2 && t2.nonce }, n2);
  }, each: function(e2, t2) {
    var n2, r2 = 0;
    if (p(e2)) {
      for (n2 = e2.length; r2 < n2; r2++) if (false === t2.call(e2[r2], r2, e2[r2])) break;
    } else for (r2 in e2) if (false === t2.call(e2[r2], r2, e2[r2])) break;
    return e2;
  }, makeArray: function(e2, t2) {
    var n2 = t2 || [];
    return null != e2 && (p(Object(e2)) ? S.merge(n2, "string" == typeof e2 ? [e2] : e2) : u.call(n2, e2)), n2;
  }, inArray: function(e2, t2, n2) {
    return null == t2 ? -1 : i2.call(t2, e2, n2);
  }, merge: function(e2, t2) {
    for (var n2 = +t2.length, r2 = 0, i3 = e2.length; r2 < n2; r2++) e2[i3++] = t2[r2];
    return e2.length = i3, e2;
  }, grep: function(e2, t2, n2) {
    for (var r2 = [], i3 = 0, o2 = e2.length, a2 = !n2; i3 < o2; i3++) !t2(e2[i3], i3) !== a2 && r2.push(e2[i3]);
    return r2;
  }, map: function(e2, t2, n2) {
    var r2, i3, o2 = 0, a2 = [];
    if (p(e2)) for (r2 = e2.length; o2 < r2; o2++) null != (i3 = t2(e2[o2], o2, n2)) && a2.push(i3);
    else for (o2 in e2) null != (i3 = t2(e2[o2], o2, n2)) && a2.push(i3);
    return g(a2);
  }, guid: 1, support: y }), "function" == typeof Symbol && (S.fn[Symbol.iterator] = t[Symbol.iterator]), S.each("Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "), function(e2, t2) {
    n["[object " + t2 + "]"] = t2.toLowerCase();
  });
  var d = function(n2) {
    var e2, d2, b2, o2, i3, h2, f2, g2, w2, u2, l2, T2, C2, a2, E2, v2, s2, c2, y2, S2 = "sizzle" + 1 * /* @__PURE__ */ new Date(), p2 = n2.document, k2 = 0, r2 = 0, m2 = ue2(), x2 = ue2(), A2 = ue2(), N2 = ue2(), j2 = function(e3, t3) {
      return e3 === t3 && (l2 = true), 0;
    }, D2 = {}.hasOwnProperty, t2 = [], q2 = t2.pop, L2 = t2.push, H2 = t2.push, O2 = t2.slice, P2 = function(e3, t3) {
      for (var n3 = 0, r3 = e3.length; n3 < r3; n3++) if (e3[n3] === t3) return n3;
      return -1;
    }, R2 = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", M2 = "[\\x20\\t\\r\\n\\f]", I2 = "(?:\\\\[\\da-fA-F]{1,6}" + M2 + "?|\\\\[^\\r\\n\\f]|[\\w-]|[^\0-\\x7f])+", W2 = "\\[" + M2 + "*(" + I2 + ")(?:" + M2 + "*([*^$|!~]?=)" + M2 + `*(?:'((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)"|(` + I2 + "))|)" + M2 + "*\\]", F2 = ":(" + I2 + `)(?:\\((('((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)")|((?:\\\\.|[^\\\\()[\\]]|` + W2 + ")*)|.*)\\)|)", B2 = new RegExp(M2 + "+", "g"), $3 = new RegExp("^" + M2 + "+|((?:^|[^\\\\])(?:\\\\.)*)" + M2 + "+$", "g"), _2 = new RegExp("^" + M2 + "*," + M2 + "*"), z2 = new RegExp("^" + M2 + "*([>+~]|" + M2 + ")" + M2 + "*"), U2 = new RegExp(M2 + "|>"), X2 = new RegExp(F2), V2 = new RegExp("^" + I2 + "$"), G2 = { ID: new RegExp("^#(" + I2 + ")"), CLASS: new RegExp("^\\.(" + I2 + ")"), TAG: new RegExp("^(" + I2 + "|[*])"), ATTR: new RegExp("^" + W2), PSEUDO: new RegExp("^" + F2), CHILD: new RegExp("^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + M2 + "*(even|odd|(([+-]|)(\\d*)n|)" + M2 + "*(?:([+-]|)" + M2 + "*(\\d+)|))" + M2 + "*\\)|)", "i"), bool: new RegExp("^(?:" + R2 + ")$", "i"), needsContext: new RegExp("^" + M2 + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + M2 + "*((?:-\\d)?\\d*)" + M2 + "*\\)|)(?=[^-]|$)", "i") }, Y2 = /HTML$/i, Q2 = /^(?:input|select|textarea|button)$/i, J2 = /^h\d$/i, K2 = /^[^{]+\{\s*\[native \w/, Z2 = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, ee2 = /[+~]/, te2 = new RegExp("\\\\[\\da-fA-F]{1,6}" + M2 + "?|\\\\([^\\r\\n\\f])", "g"), ne2 = function(e3, t3) {
      var n3 = "0x" + e3.slice(1) - 65536;
      return t3 || (n3 < 0 ? String.fromCharCode(n3 + 65536) : String.fromCharCode(n3 >> 10 | 55296, 1023 & n3 | 56320));
    }, re2 = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\0-\x1f\x7f-\uFFFF\w-]/g, ie2 = function(e3, t3) {
      return t3 ? "\0" === e3 ? "\uFFFD" : e3.slice(0, -1) + "\\" + e3.charCodeAt(e3.length - 1).toString(16) + " " : "\\" + e3;
    }, oe2 = function() {
      T2();
    }, ae2 = be2(function(e3) {
      return true === e3.disabled && "fieldset" === e3.nodeName.toLowerCase();
    }, { dir: "parentNode", next: "legend" });
    try {
      H2.apply(t2 = O2.call(p2.childNodes), p2.childNodes), t2[p2.childNodes.length].nodeType;
    } catch (e3) {
      H2 = { apply: t2.length ? function(e4, t3) {
        L2.apply(e4, O2.call(t3));
      } : function(e4, t3) {
        var n3 = e4.length, r3 = 0;
        while (e4[n3++] = t3[r3++]) ;
        e4.length = n3 - 1;
      } };
    }
    function se2(t3, e3, n3, r3) {
      var i4, o3, a3, s3, u3, l3, c3, f3 = e3 && e3.ownerDocument, p3 = e3 ? e3.nodeType : 9;
      if (n3 = n3 || [], "string" != typeof t3 || !t3 || 1 !== p3 && 9 !== p3 && 11 !== p3) return n3;
      if (!r3 && (T2(e3), e3 = e3 || C2, E2)) {
        if (11 !== p3 && (u3 = Z2.exec(t3))) if (i4 = u3[1]) {
          if (9 === p3) {
            if (!(a3 = e3.getElementById(i4))) return n3;
            if (a3.id === i4) return n3.push(a3), n3;
          } else if (f3 && (a3 = f3.getElementById(i4)) && y2(e3, a3) && a3.id === i4) return n3.push(a3), n3;
        } else {
          if (u3[2]) return H2.apply(n3, e3.getElementsByTagName(t3)), n3;
          if ((i4 = u3[3]) && d2.getElementsByClassName && e3.getElementsByClassName) return H2.apply(n3, e3.getElementsByClassName(i4)), n3;
        }
        if (d2.qsa && !N2[t3 + " "] && (!v2 || !v2.test(t3)) && (1 !== p3 || "object" !== e3.nodeName.toLowerCase())) {
          if (c3 = t3, f3 = e3, 1 === p3 && (U2.test(t3) || z2.test(t3))) {
            (f3 = ee2.test(t3) && ye2(e3.parentNode) || e3) === e3 && d2.scope || ((s3 = e3.getAttribute("id")) ? s3 = s3.replace(re2, ie2) : e3.setAttribute("id", s3 = S2)), o3 = (l3 = h2(t3)).length;
            while (o3--) l3[o3] = (s3 ? "#" + s3 : ":scope") + " " + xe2(l3[o3]);
            c3 = l3.join(",");
          }
          try {
            return H2.apply(n3, f3.querySelectorAll(c3)), n3;
          } catch (e4) {
            N2(t3, true);
          } finally {
            s3 === S2 && e3.removeAttribute("id");
          }
        }
      }
      return g2(t3.replace($3, "$1"), e3, n3, r3);
    }
    function ue2() {
      var r3 = [];
      return function e3(t3, n3) {
        return r3.push(t3 + " ") > b2.cacheLength && delete e3[r3.shift()], e3[t3 + " "] = n3;
      };
    }
    function le2(e3) {
      return e3[S2] = true, e3;
    }
    function ce2(e3) {
      var t3 = C2.createElement("fieldset");
      try {
        return !!e3(t3);
      } catch (e4) {
        return false;
      } finally {
        t3.parentNode && t3.parentNode.removeChild(t3), t3 = null;
      }
    }
    function fe2(e3, t3) {
      var n3 = e3.split("|"), r3 = n3.length;
      while (r3--) b2.attrHandle[n3[r3]] = t3;
    }
    function pe2(e3, t3) {
      var n3 = t3 && e3, r3 = n3 && 1 === e3.nodeType && 1 === t3.nodeType && e3.sourceIndex - t3.sourceIndex;
      if (r3) return r3;
      if (n3) {
        while (n3 = n3.nextSibling) if (n3 === t3) return -1;
      }
      return e3 ? 1 : -1;
    }
    function de2(t3) {
      return function(e3) {
        return "input" === e3.nodeName.toLowerCase() && e3.type === t3;
      };
    }
    function he2(n3) {
      return function(e3) {
        var t3 = e3.nodeName.toLowerCase();
        return ("input" === t3 || "button" === t3) && e3.type === n3;
      };
    }
    function ge2(t3) {
      return function(e3) {
        return "form" in e3 ? e3.parentNode && false === e3.disabled ? "label" in e3 ? "label" in e3.parentNode ? e3.parentNode.disabled === t3 : e3.disabled === t3 : e3.isDisabled === t3 || e3.isDisabled !== !t3 && ae2(e3) === t3 : e3.disabled === t3 : "label" in e3 && e3.disabled === t3;
      };
    }
    function ve2(a3) {
      return le2(function(o3) {
        return o3 = +o3, le2(function(e3, t3) {
          var n3, r3 = a3([], e3.length, o3), i4 = r3.length;
          while (i4--) e3[n3 = r3[i4]] && (e3[n3] = !(t3[n3] = e3[n3]));
        });
      });
    }
    function ye2(e3) {
      return e3 && "undefined" != typeof e3.getElementsByTagName && e3;
    }
    for (e2 in d2 = se2.support = {}, i3 = se2.isXML = function(e3) {
      var t3 = e3 && e3.namespaceURI, n3 = e3 && (e3.ownerDocument || e3).documentElement;
      return !Y2.test(t3 || n3 && n3.nodeName || "HTML");
    }, T2 = se2.setDocument = function(e3) {
      var t3, n3, r3 = e3 ? e3.ownerDocument || e3 : p2;
      return r3 != C2 && 9 === r3.nodeType && r3.documentElement && (a2 = (C2 = r3).documentElement, E2 = !i3(C2), p2 != C2 && (n3 = C2.defaultView) && n3.top !== n3 && (n3.addEventListener ? n3.addEventListener("unload", oe2, false) : n3.attachEvent && n3.attachEvent("onunload", oe2)), d2.scope = ce2(function(e4) {
        return a2.appendChild(e4).appendChild(C2.createElement("div")), "undefined" != typeof e4.querySelectorAll && !e4.querySelectorAll(":scope fieldset div").length;
      }), d2.attributes = ce2(function(e4) {
        return e4.className = "i", !e4.getAttribute("className");
      }), d2.getElementsByTagName = ce2(function(e4) {
        return e4.appendChild(C2.createComment("")), !e4.getElementsByTagName("*").length;
      }), d2.getElementsByClassName = K2.test(C2.getElementsByClassName), d2.getById = ce2(function(e4) {
        return a2.appendChild(e4).id = S2, !C2.getElementsByName || !C2.getElementsByName(S2).length;
      }), d2.getById ? (b2.filter.ID = function(e4) {
        var t4 = e4.replace(te2, ne2);
        return function(e5) {
          return e5.getAttribute("id") === t4;
        };
      }, b2.find.ID = function(e4, t4) {
        if ("undefined" != typeof t4.getElementById && E2) {
          var n4 = t4.getElementById(e4);
          return n4 ? [n4] : [];
        }
      }) : (b2.filter.ID = function(e4) {
        var n4 = e4.replace(te2, ne2);
        return function(e5) {
          var t4 = "undefined" != typeof e5.getAttributeNode && e5.getAttributeNode("id");
          return t4 && t4.value === n4;
        };
      }, b2.find.ID = function(e4, t4) {
        if ("undefined" != typeof t4.getElementById && E2) {
          var n4, r4, i4, o3 = t4.getElementById(e4);
          if (o3) {
            if ((n4 = o3.getAttributeNode("id")) && n4.value === e4) return [o3];
            i4 = t4.getElementsByName(e4), r4 = 0;
            while (o3 = i4[r4++]) if ((n4 = o3.getAttributeNode("id")) && n4.value === e4) return [o3];
          }
          return [];
        }
      }), b2.find.TAG = d2.getElementsByTagName ? function(e4, t4) {
        return "undefined" != typeof t4.getElementsByTagName ? t4.getElementsByTagName(e4) : d2.qsa ? t4.querySelectorAll(e4) : void 0;
      } : function(e4, t4) {
        var n4, r4 = [], i4 = 0, o3 = t4.getElementsByTagName(e4);
        if ("*" === e4) {
          while (n4 = o3[i4++]) 1 === n4.nodeType && r4.push(n4);
          return r4;
        }
        return o3;
      }, b2.find.CLASS = d2.getElementsByClassName && function(e4, t4) {
        if ("undefined" != typeof t4.getElementsByClassName && E2) return t4.getElementsByClassName(e4);
      }, s2 = [], v2 = [], (d2.qsa = K2.test(C2.querySelectorAll)) && (ce2(function(e4) {
        var t4;
        a2.appendChild(e4).innerHTML = "<a id='" + S2 + "'></a><select id='" + S2 + "-\r\\' msallowcapture=''><option selected=''></option></select>", e4.querySelectorAll("[msallowcapture^='']").length && v2.push("[*^$]=" + M2 + `*(?:''|"")`), e4.querySelectorAll("[selected]").length || v2.push("\\[" + M2 + "*(?:value|" + R2 + ")"), e4.querySelectorAll("[id~=" + S2 + "-]").length || v2.push("~="), (t4 = C2.createElement("input")).setAttribute("name", ""), e4.appendChild(t4), e4.querySelectorAll("[name='']").length || v2.push("\\[" + M2 + "*name" + M2 + "*=" + M2 + `*(?:''|"")`), e4.querySelectorAll(":checked").length || v2.push(":checked"), e4.querySelectorAll("a#" + S2 + "+*").length || v2.push(".#.+[+~]"), e4.querySelectorAll("\\\f"), v2.push("[\\r\\n\\f]");
      }), ce2(function(e4) {
        e4.innerHTML = "<a href='' disabled='disabled'></a><select disabled='disabled'><option/></select>";
        var t4 = C2.createElement("input");
        t4.setAttribute("type", "hidden"), e4.appendChild(t4).setAttribute("name", "D"), e4.querySelectorAll("[name=d]").length && v2.push("name" + M2 + "*[*^$|!~]?="), 2 !== e4.querySelectorAll(":enabled").length && v2.push(":enabled", ":disabled"), a2.appendChild(e4).disabled = true, 2 !== e4.querySelectorAll(":disabled").length && v2.push(":enabled", ":disabled"), e4.querySelectorAll("*,:x"), v2.push(",.*:");
      })), (d2.matchesSelector = K2.test(c2 = a2.matches || a2.webkitMatchesSelector || a2.mozMatchesSelector || a2.oMatchesSelector || a2.msMatchesSelector)) && ce2(function(e4) {
        d2.disconnectedMatch = c2.call(e4, "*"), c2.call(e4, "[s!='']:x"), s2.push("!=", F2);
      }), v2 = v2.length && new RegExp(v2.join("|")), s2 = s2.length && new RegExp(s2.join("|")), t3 = K2.test(a2.compareDocumentPosition), y2 = t3 || K2.test(a2.contains) ? function(e4, t4) {
        var n4 = 9 === e4.nodeType ? e4.documentElement : e4, r4 = t4 && t4.parentNode;
        return e4 === r4 || !(!r4 || 1 !== r4.nodeType || !(n4.contains ? n4.contains(r4) : e4.compareDocumentPosition && 16 & e4.compareDocumentPosition(r4)));
      } : function(e4, t4) {
        if (t4) {
          while (t4 = t4.parentNode) if (t4 === e4) return true;
        }
        return false;
      }, j2 = t3 ? function(e4, t4) {
        if (e4 === t4) return l2 = true, 0;
        var n4 = !e4.compareDocumentPosition - !t4.compareDocumentPosition;
        return n4 || (1 & (n4 = (e4.ownerDocument || e4) == (t4.ownerDocument || t4) ? e4.compareDocumentPosition(t4) : 1) || !d2.sortDetached && t4.compareDocumentPosition(e4) === n4 ? e4 == C2 || e4.ownerDocument == p2 && y2(p2, e4) ? -1 : t4 == C2 || t4.ownerDocument == p2 && y2(p2, t4) ? 1 : u2 ? P2(u2, e4) - P2(u2, t4) : 0 : 4 & n4 ? -1 : 1);
      } : function(e4, t4) {
        if (e4 === t4) return l2 = true, 0;
        var n4, r4 = 0, i4 = e4.parentNode, o3 = t4.parentNode, a3 = [e4], s3 = [t4];
        if (!i4 || !o3) return e4 == C2 ? -1 : t4 == C2 ? 1 : i4 ? -1 : o3 ? 1 : u2 ? P2(u2, e4) - P2(u2, t4) : 0;
        if (i4 === o3) return pe2(e4, t4);
        n4 = e4;
        while (n4 = n4.parentNode) a3.unshift(n4);
        n4 = t4;
        while (n4 = n4.parentNode) s3.unshift(n4);
        while (a3[r4] === s3[r4]) r4++;
        return r4 ? pe2(a3[r4], s3[r4]) : a3[r4] == p2 ? -1 : s3[r4] == p2 ? 1 : 0;
      }), C2;
    }, se2.matches = function(e3, t3) {
      return se2(e3, null, null, t3);
    }, se2.matchesSelector = function(e3, t3) {
      if (T2(e3), d2.matchesSelector && E2 && !N2[t3 + " "] && (!s2 || !s2.test(t3)) && (!v2 || !v2.test(t3))) try {
        var n3 = c2.call(e3, t3);
        if (n3 || d2.disconnectedMatch || e3.document && 11 !== e3.document.nodeType) return n3;
      } catch (e4) {
        N2(t3, true);
      }
      return 0 < se2(t3, C2, null, [e3]).length;
    }, se2.contains = function(e3, t3) {
      return (e3.ownerDocument || e3) != C2 && T2(e3), y2(e3, t3);
    }, se2.attr = function(e3, t3) {
      (e3.ownerDocument || e3) != C2 && T2(e3);
      var n3 = b2.attrHandle[t3.toLowerCase()], r3 = n3 && D2.call(b2.attrHandle, t3.toLowerCase()) ? n3(e3, t3, !E2) : void 0;
      return void 0 !== r3 ? r3 : d2.attributes || !E2 ? e3.getAttribute(t3) : (r3 = e3.getAttributeNode(t3)) && r3.specified ? r3.value : null;
    }, se2.escape = function(e3) {
      return (e3 + "").replace(re2, ie2);
    }, se2.error = function(e3) {
      throw new Error("Syntax error, unrecognized expression: " + e3);
    }, se2.uniqueSort = function(e3) {
      var t3, n3 = [], r3 = 0, i4 = 0;
      if (l2 = !d2.detectDuplicates, u2 = !d2.sortStable && e3.slice(0), e3.sort(j2), l2) {
        while (t3 = e3[i4++]) t3 === e3[i4] && (r3 = n3.push(i4));
        while (r3--) e3.splice(n3[r3], 1);
      }
      return u2 = null, e3;
    }, o2 = se2.getText = function(e3) {
      var t3, n3 = "", r3 = 0, i4 = e3.nodeType;
      if (i4) {
        if (1 === i4 || 9 === i4 || 11 === i4) {
          if ("string" == typeof e3.textContent) return e3.textContent;
          for (e3 = e3.firstChild; e3; e3 = e3.nextSibling) n3 += o2(e3);
        } else if (3 === i4 || 4 === i4) return e3.nodeValue;
      } else while (t3 = e3[r3++]) n3 += o2(t3);
      return n3;
    }, (b2 = se2.selectors = { cacheLength: 50, createPseudo: le2, match: G2, attrHandle: {}, find: {}, relative: { ">": { dir: "parentNode", first: true }, " ": { dir: "parentNode" }, "+": { dir: "previousSibling", first: true }, "~": { dir: "previousSibling" } }, preFilter: { ATTR: function(e3) {
      return e3[1] = e3[1].replace(te2, ne2), e3[3] = (e3[3] || e3[4] || e3[5] || "").replace(te2, ne2), "~=" === e3[2] && (e3[3] = " " + e3[3] + " "), e3.slice(0, 4);
    }, CHILD: function(e3) {
      return e3[1] = e3[1].toLowerCase(), "nth" === e3[1].slice(0, 3) ? (e3[3] || se2.error(e3[0]), e3[4] = +(e3[4] ? e3[5] + (e3[6] || 1) : 2 * ("even" === e3[3] || "odd" === e3[3])), e3[5] = +(e3[7] + e3[8] || "odd" === e3[3])) : e3[3] && se2.error(e3[0]), e3;
    }, PSEUDO: function(e3) {
      var t3, n3 = !e3[6] && e3[2];
      return G2.CHILD.test(e3[0]) ? null : (e3[3] ? e3[2] = e3[4] || e3[5] || "" : n3 && X2.test(n3) && (t3 = h2(n3, true)) && (t3 = n3.indexOf(")", n3.length - t3) - n3.length) && (e3[0] = e3[0].slice(0, t3), e3[2] = n3.slice(0, t3)), e3.slice(0, 3));
    } }, filter: { TAG: function(e3) {
      var t3 = e3.replace(te2, ne2).toLowerCase();
      return "*" === e3 ? function() {
        return true;
      } : function(e4) {
        return e4.nodeName && e4.nodeName.toLowerCase() === t3;
      };
    }, CLASS: function(e3) {
      var t3 = m2[e3 + " "];
      return t3 || (t3 = new RegExp("(^|" + M2 + ")" + e3 + "(" + M2 + "|$)")) && m2(e3, function(e4) {
        return t3.test("string" == typeof e4.className && e4.className || "undefined" != typeof e4.getAttribute && e4.getAttribute("class") || "");
      });
    }, ATTR: function(n3, r3, i4) {
      return function(e3) {
        var t3 = se2.attr(e3, n3);
        return null == t3 ? "!=" === r3 : !r3 || (t3 += "", "=" === r3 ? t3 === i4 : "!=" === r3 ? t3 !== i4 : "^=" === r3 ? i4 && 0 === t3.indexOf(i4) : "*=" === r3 ? i4 && -1 < t3.indexOf(i4) : "$=" === r3 ? i4 && t3.slice(-i4.length) === i4 : "~=" === r3 ? -1 < (" " + t3.replace(B2, " ") + " ").indexOf(i4) : "|=" === r3 && (t3 === i4 || t3.slice(0, i4.length + 1) === i4 + "-"));
      };
    }, CHILD: function(h3, e3, t3, g3, v3) {
      var y3 = "nth" !== h3.slice(0, 3), m3 = "last" !== h3.slice(-4), x3 = "of-type" === e3;
      return 1 === g3 && 0 === v3 ? function(e4) {
        return !!e4.parentNode;
      } : function(e4, t4, n3) {
        var r3, i4, o3, a3, s3, u3, l3 = y3 !== m3 ? "nextSibling" : "previousSibling", c3 = e4.parentNode, f3 = x3 && e4.nodeName.toLowerCase(), p3 = !n3 && !x3, d3 = false;
        if (c3) {
          if (y3) {
            while (l3) {
              a3 = e4;
              while (a3 = a3[l3]) if (x3 ? a3.nodeName.toLowerCase() === f3 : 1 === a3.nodeType) return false;
              u3 = l3 = "only" === h3 && !u3 && "nextSibling";
            }
            return true;
          }
          if (u3 = [m3 ? c3.firstChild : c3.lastChild], m3 && p3) {
            d3 = (s3 = (r3 = (i4 = (o3 = (a3 = c3)[S2] || (a3[S2] = {}))[a3.uniqueID] || (o3[a3.uniqueID] = {}))[h3] || [])[0] === k2 && r3[1]) && r3[2], a3 = s3 && c3.childNodes[s3];
            while (a3 = ++s3 && a3 && a3[l3] || (d3 = s3 = 0) || u3.pop()) if (1 === a3.nodeType && ++d3 && a3 === e4) {
              i4[h3] = [k2, s3, d3];
              break;
            }
          } else if (p3 && (d3 = s3 = (r3 = (i4 = (o3 = (a3 = e4)[S2] || (a3[S2] = {}))[a3.uniqueID] || (o3[a3.uniqueID] = {}))[h3] || [])[0] === k2 && r3[1]), false === d3) {
            while (a3 = ++s3 && a3 && a3[l3] || (d3 = s3 = 0) || u3.pop()) if ((x3 ? a3.nodeName.toLowerCase() === f3 : 1 === a3.nodeType) && ++d3 && (p3 && ((i4 = (o3 = a3[S2] || (a3[S2] = {}))[a3.uniqueID] || (o3[a3.uniqueID] = {}))[h3] = [k2, d3]), a3 === e4)) break;
          }
          return (d3 -= v3) === g3 || d3 % g3 == 0 && 0 <= d3 / g3;
        }
      };
    }, PSEUDO: function(e3, o3) {
      var t3, a3 = b2.pseudos[e3] || b2.setFilters[e3.toLowerCase()] || se2.error("unsupported pseudo: " + e3);
      return a3[S2] ? a3(o3) : 1 < a3.length ? (t3 = [e3, e3, "", o3], b2.setFilters.hasOwnProperty(e3.toLowerCase()) ? le2(function(e4, t4) {
        var n3, r3 = a3(e4, o3), i4 = r3.length;
        while (i4--) e4[n3 = P2(e4, r3[i4])] = !(t4[n3] = r3[i4]);
      }) : function(e4) {
        return a3(e4, 0, t3);
      }) : a3;
    } }, pseudos: { not: le2(function(e3) {
      var r3 = [], i4 = [], s3 = f2(e3.replace($3, "$1"));
      return s3[S2] ? le2(function(e4, t3, n3, r4) {
        var i5, o3 = s3(e4, null, r4, []), a3 = e4.length;
        while (a3--) (i5 = o3[a3]) && (e4[a3] = !(t3[a3] = i5));
      }) : function(e4, t3, n3) {
        return r3[0] = e4, s3(r3, null, n3, i4), r3[0] = null, !i4.pop();
      };
    }), has: le2(function(t3) {
      return function(e3) {
        return 0 < se2(t3, e3).length;
      };
    }), contains: le2(function(t3) {
      return t3 = t3.replace(te2, ne2), function(e3) {
        return -1 < (e3.textContent || o2(e3)).indexOf(t3);
      };
    }), lang: le2(function(n3) {
      return V2.test(n3 || "") || se2.error("unsupported lang: " + n3), n3 = n3.replace(te2, ne2).toLowerCase(), function(e3) {
        var t3;
        do {
          if (t3 = E2 ? e3.lang : e3.getAttribute("xml:lang") || e3.getAttribute("lang")) return (t3 = t3.toLowerCase()) === n3 || 0 === t3.indexOf(n3 + "-");
        } while ((e3 = e3.parentNode) && 1 === e3.nodeType);
        return false;
      };
    }), target: function(e3) {
      var t3 = n2.location && n2.location.hash;
      return t3 && t3.slice(1) === e3.id;
    }, root: function(e3) {
      return e3 === a2;
    }, focus: function(e3) {
      return e3 === C2.activeElement && (!C2.hasFocus || C2.hasFocus()) && !!(e3.type || e3.href || ~e3.tabIndex);
    }, enabled: ge2(false), disabled: ge2(true), checked: function(e3) {
      var t3 = e3.nodeName.toLowerCase();
      return "input" === t3 && !!e3.checked || "option" === t3 && !!e3.selected;
    }, selected: function(e3) {
      return e3.parentNode && e3.parentNode.selectedIndex, true === e3.selected;
    }, empty: function(e3) {
      for (e3 = e3.firstChild; e3; e3 = e3.nextSibling) if (e3.nodeType < 6) return false;
      return true;
    }, parent: function(e3) {
      return !b2.pseudos.empty(e3);
    }, header: function(e3) {
      return J2.test(e3.nodeName);
    }, input: function(e3) {
      return Q2.test(e3.nodeName);
    }, button: function(e3) {
      var t3 = e3.nodeName.toLowerCase();
      return "input" === t3 && "button" === e3.type || "button" === t3;
    }, text: function(e3) {
      var t3;
      return "input" === e3.nodeName.toLowerCase() && "text" === e3.type && (null == (t3 = e3.getAttribute("type")) || "text" === t3.toLowerCase());
    }, first: ve2(function() {
      return [0];
    }), last: ve2(function(e3, t3) {
      return [t3 - 1];
    }), eq: ve2(function(e3, t3, n3) {
      return [n3 < 0 ? n3 + t3 : n3];
    }), even: ve2(function(e3, t3) {
      for (var n3 = 0; n3 < t3; n3 += 2) e3.push(n3);
      return e3;
    }), odd: ve2(function(e3, t3) {
      for (var n3 = 1; n3 < t3; n3 += 2) e3.push(n3);
      return e3;
    }), lt: ve2(function(e3, t3, n3) {
      for (var r3 = n3 < 0 ? n3 + t3 : t3 < n3 ? t3 : n3; 0 <= --r3; ) e3.push(r3);
      return e3;
    }), gt: ve2(function(e3, t3, n3) {
      for (var r3 = n3 < 0 ? n3 + t3 : n3; ++r3 < t3; ) e3.push(r3);
      return e3;
    }) } }).pseudos.nth = b2.pseudos.eq, { radio: true, checkbox: true, file: true, password: true, image: true }) b2.pseudos[e2] = de2(e2);
    for (e2 in { submit: true, reset: true }) b2.pseudos[e2] = he2(e2);
    function me2() {
    }
    function xe2(e3) {
      for (var t3 = 0, n3 = e3.length, r3 = ""; t3 < n3; t3++) r3 += e3[t3].value;
      return r3;
    }
    function be2(s3, e3, t3) {
      var u3 = e3.dir, l3 = e3.next, c3 = l3 || u3, f3 = t3 && "parentNode" === c3, p3 = r2++;
      return e3.first ? function(e4, t4, n3) {
        while (e4 = e4[u3]) if (1 === e4.nodeType || f3) return s3(e4, t4, n3);
        return false;
      } : function(e4, t4, n3) {
        var r3, i4, o3, a3 = [k2, p3];
        if (n3) {
          while (e4 = e4[u3]) if ((1 === e4.nodeType || f3) && s3(e4, t4, n3)) return true;
        } else while (e4 = e4[u3]) if (1 === e4.nodeType || f3) if (i4 = (o3 = e4[S2] || (e4[S2] = {}))[e4.uniqueID] || (o3[e4.uniqueID] = {}), l3 && l3 === e4.nodeName.toLowerCase()) e4 = e4[u3] || e4;
        else {
          if ((r3 = i4[c3]) && r3[0] === k2 && r3[1] === p3) return a3[2] = r3[2];
          if ((i4[c3] = a3)[2] = s3(e4, t4, n3)) return true;
        }
        return false;
      };
    }
    function we2(i4) {
      return 1 < i4.length ? function(e3, t3, n3) {
        var r3 = i4.length;
        while (r3--) if (!i4[r3](e3, t3, n3)) return false;
        return true;
      } : i4[0];
    }
    function Te2(e3, t3, n3, r3, i4) {
      for (var o3, a3 = [], s3 = 0, u3 = e3.length, l3 = null != t3; s3 < u3; s3++) (o3 = e3[s3]) && (n3 && !n3(o3, r3, i4) || (a3.push(o3), l3 && t3.push(s3)));
      return a3;
    }
    function Ce2(d3, h3, g3, v3, y3, e3) {
      return v3 && !v3[S2] && (v3 = Ce2(v3)), y3 && !y3[S2] && (y3 = Ce2(y3, e3)), le2(function(e4, t3, n3, r3) {
        var i4, o3, a3, s3 = [], u3 = [], l3 = t3.length, c3 = e4 || function(e5, t4, n4) {
          for (var r4 = 0, i5 = t4.length; r4 < i5; r4++) se2(e5, t4[r4], n4);
          return n4;
        }(h3 || "*", n3.nodeType ? [n3] : n3, []), f3 = !d3 || !e4 && h3 ? c3 : Te2(c3, s3, d3, n3, r3), p3 = g3 ? y3 || (e4 ? d3 : l3 || v3) ? [] : t3 : f3;
        if (g3 && g3(f3, p3, n3, r3), v3) {
          i4 = Te2(p3, u3), v3(i4, [], n3, r3), o3 = i4.length;
          while (o3--) (a3 = i4[o3]) && (p3[u3[o3]] = !(f3[u3[o3]] = a3));
        }
        if (e4) {
          if (y3 || d3) {
            if (y3) {
              i4 = [], o3 = p3.length;
              while (o3--) (a3 = p3[o3]) && i4.push(f3[o3] = a3);
              y3(null, p3 = [], i4, r3);
            }
            o3 = p3.length;
            while (o3--) (a3 = p3[o3]) && -1 < (i4 = y3 ? P2(e4, a3) : s3[o3]) && (e4[i4] = !(t3[i4] = a3));
          }
        } else p3 = Te2(p3 === t3 ? p3.splice(l3, p3.length) : p3), y3 ? y3(null, t3, p3, r3) : H2.apply(t3, p3);
      });
    }
    function Ee2(e3) {
      for (var i4, t3, n3, r3 = e3.length, o3 = b2.relative[e3[0].type], a3 = o3 || b2.relative[" "], s3 = o3 ? 1 : 0, u3 = be2(function(e4) {
        return e4 === i4;
      }, a3, true), l3 = be2(function(e4) {
        return -1 < P2(i4, e4);
      }, a3, true), c3 = [function(e4, t4, n4) {
        var r4 = !o3 && (n4 || t4 !== w2) || ((i4 = t4).nodeType ? u3(e4, t4, n4) : l3(e4, t4, n4));
        return i4 = null, r4;
      }]; s3 < r3; s3++) if (t3 = b2.relative[e3[s3].type]) c3 = [be2(we2(c3), t3)];
      else {
        if ((t3 = b2.filter[e3[s3].type].apply(null, e3[s3].matches))[S2]) {
          for (n3 = ++s3; n3 < r3; n3++) if (b2.relative[e3[n3].type]) break;
          return Ce2(1 < s3 && we2(c3), 1 < s3 && xe2(e3.slice(0, s3 - 1).concat({ value: " " === e3[s3 - 2].type ? "*" : "" })).replace($3, "$1"), t3, s3 < n3 && Ee2(e3.slice(s3, n3)), n3 < r3 && Ee2(e3 = e3.slice(n3)), n3 < r3 && xe2(e3));
        }
        c3.push(t3);
      }
      return we2(c3);
    }
    return me2.prototype = b2.filters = b2.pseudos, b2.setFilters = new me2(), h2 = se2.tokenize = function(e3, t3) {
      var n3, r3, i4, o3, a3, s3, u3, l3 = x2[e3 + " "];
      if (l3) return t3 ? 0 : l3.slice(0);
      a3 = e3, s3 = [], u3 = b2.preFilter;
      while (a3) {
        for (o3 in n3 && !(r3 = _2.exec(a3)) || (r3 && (a3 = a3.slice(r3[0].length) || a3), s3.push(i4 = [])), n3 = false, (r3 = z2.exec(a3)) && (n3 = r3.shift(), i4.push({ value: n3, type: r3[0].replace($3, " ") }), a3 = a3.slice(n3.length)), b2.filter) !(r3 = G2[o3].exec(a3)) || u3[o3] && !(r3 = u3[o3](r3)) || (n3 = r3.shift(), i4.push({ value: n3, type: o3, matches: r3 }), a3 = a3.slice(n3.length));
        if (!n3) break;
      }
      return t3 ? a3.length : a3 ? se2.error(e3) : x2(e3, s3).slice(0);
    }, f2 = se2.compile = function(e3, t3) {
      var n3, v3, y3, m3, x3, r3, i4 = [], o3 = [], a3 = A2[e3 + " "];
      if (!a3) {
        t3 || (t3 = h2(e3)), n3 = t3.length;
        while (n3--) (a3 = Ee2(t3[n3]))[S2] ? i4.push(a3) : o3.push(a3);
        (a3 = A2(e3, (v3 = o3, m3 = 0 < (y3 = i4).length, x3 = 0 < v3.length, r3 = function(e4, t4, n4, r4, i5) {
          var o4, a4, s3, u3 = 0, l3 = "0", c3 = e4 && [], f3 = [], p3 = w2, d3 = e4 || x3 && b2.find.TAG("*", i5), h3 = k2 += null == p3 ? 1 : Math.random() || 0.1, g3 = d3.length;
          for (i5 && (w2 = t4 == C2 || t4 || i5); l3 !== g3 && null != (o4 = d3[l3]); l3++) {
            if (x3 && o4) {
              a4 = 0, t4 || o4.ownerDocument == C2 || (T2(o4), n4 = !E2);
              while (s3 = v3[a4++]) if (s3(o4, t4 || C2, n4)) {
                r4.push(o4);
                break;
              }
              i5 && (k2 = h3);
            }
            m3 && ((o4 = !s3 && o4) && u3--, e4 && c3.push(o4));
          }
          if (u3 += l3, m3 && l3 !== u3) {
            a4 = 0;
            while (s3 = y3[a4++]) s3(c3, f3, t4, n4);
            if (e4) {
              if (0 < u3) while (l3--) c3[l3] || f3[l3] || (f3[l3] = q2.call(r4));
              f3 = Te2(f3);
            }
            H2.apply(r4, f3), i5 && !e4 && 0 < f3.length && 1 < u3 + y3.length && se2.uniqueSort(r4);
          }
          return i5 && (k2 = h3, w2 = p3), c3;
        }, m3 ? le2(r3) : r3))).selector = e3;
      }
      return a3;
    }, g2 = se2.select = function(e3, t3, n3, r3) {
      var i4, o3, a3, s3, u3, l3 = "function" == typeof e3 && e3, c3 = !r3 && h2(e3 = l3.selector || e3);
      if (n3 = n3 || [], 1 === c3.length) {
        if (2 < (o3 = c3[0] = c3[0].slice(0)).length && "ID" === (a3 = o3[0]).type && 9 === t3.nodeType && E2 && b2.relative[o3[1].type]) {
          if (!(t3 = (b2.find.ID(a3.matches[0].replace(te2, ne2), t3) || [])[0])) return n3;
          l3 && (t3 = t3.parentNode), e3 = e3.slice(o3.shift().value.length);
        }
        i4 = G2.needsContext.test(e3) ? 0 : o3.length;
        while (i4--) {
          if (a3 = o3[i4], b2.relative[s3 = a3.type]) break;
          if ((u3 = b2.find[s3]) && (r3 = u3(a3.matches[0].replace(te2, ne2), ee2.test(o3[0].type) && ye2(t3.parentNode) || t3))) {
            if (o3.splice(i4, 1), !(e3 = r3.length && xe2(o3))) return H2.apply(n3, r3), n3;
            break;
          }
        }
      }
      return (l3 || f2(e3, c3))(r3, t3, !E2, n3, !t3 || ee2.test(e3) && ye2(t3.parentNode) || t3), n3;
    }, d2.sortStable = S2.split("").sort(j2).join("") === S2, d2.detectDuplicates = !!l2, T2(), d2.sortDetached = ce2(function(e3) {
      return 1 & e3.compareDocumentPosition(C2.createElement("fieldset"));
    }), ce2(function(e3) {
      return e3.innerHTML = "<a href='#'></a>", "#" === e3.firstChild.getAttribute("href");
    }) || fe2("type|href|height|width", function(e3, t3, n3) {
      if (!n3) return e3.getAttribute(t3, "type" === t3.toLowerCase() ? 1 : 2);
    }), d2.attributes && ce2(function(e3) {
      return e3.innerHTML = "<input/>", e3.firstChild.setAttribute("value", ""), "" === e3.firstChild.getAttribute("value");
    }) || fe2("value", function(e3, t3, n3) {
      if (!n3 && "input" === e3.nodeName.toLowerCase()) return e3.defaultValue;
    }), ce2(function(e3) {
      return null == e3.getAttribute("disabled");
    }) || fe2(R2, function(e3, t3, n3) {
      var r3;
      if (!n3) return true === e3[t3] ? t3.toLowerCase() : (r3 = e3.getAttributeNode(t3)) && r3.specified ? r3.value : null;
    }), se2;
  }(C);
  S.find = d, S.expr = d.selectors, S.expr[":"] = S.expr.pseudos, S.uniqueSort = S.unique = d.uniqueSort, S.text = d.getText, S.isXMLDoc = d.isXML, S.contains = d.contains, S.escapeSelector = d.escape;
  var h = function(e2, t2, n2) {
    var r2 = [], i3 = void 0 !== n2;
    while ((e2 = e2[t2]) && 9 !== e2.nodeType) if (1 === e2.nodeType) {
      if (i3 && S(e2).is(n2)) break;
      r2.push(e2);
    }
    return r2;
  }, T = function(e2, t2) {
    for (var n2 = []; e2; e2 = e2.nextSibling) 1 === e2.nodeType && e2 !== t2 && n2.push(e2);
    return n2;
  }, k = S.expr.match.needsContext;
  function A(e2, t2) {
    return e2.nodeName && e2.nodeName.toLowerCase() === t2.toLowerCase();
  }
  var N = /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i;
  function j(e2, n2, r2) {
    return m(n2) ? S.grep(e2, function(e3, t2) {
      return !!n2.call(e3, t2, e3) !== r2;
    }) : n2.nodeType ? S.grep(e2, function(e3) {
      return e3 === n2 !== r2;
    }) : "string" != typeof n2 ? S.grep(e2, function(e3) {
      return -1 < i2.call(n2, e3) !== r2;
    }) : S.filter(n2, e2, r2);
  }
  S.filter = function(e2, t2, n2) {
    var r2 = t2[0];
    return n2 && (e2 = ":not(" + e2 + ")"), 1 === t2.length && 1 === r2.nodeType ? S.find.matchesSelector(r2, e2) ? [r2] : [] : S.find.matches(e2, S.grep(t2, function(e3) {
      return 1 === e3.nodeType;
    }));
  }, S.fn.extend({ find: function(e2) {
    var t2, n2, r2 = this.length, i3 = this;
    if ("string" != typeof e2) return this.pushStack(S(e2).filter(function() {
      for (t2 = 0; t2 < r2; t2++) if (S.contains(i3[t2], this)) return true;
    }));
    for (n2 = this.pushStack([]), t2 = 0; t2 < r2; t2++) S.find(e2, i3[t2], n2);
    return 1 < r2 ? S.uniqueSort(n2) : n2;
  }, filter: function(e2) {
    return this.pushStack(j(this, e2 || [], false));
  }, not: function(e2) {
    return this.pushStack(j(this, e2 || [], true));
  }, is: function(e2) {
    return !!j(this, "string" == typeof e2 && k.test(e2) ? S(e2) : e2 || [], false).length;
  } });
  var D, q = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/;
  (S.fn.init = function(e2, t2, n2) {
    var r2, i3;
    if (!e2) return this;
    if (n2 = n2 || D, "string" == typeof e2) {
      if (!(r2 = "<" === e2[0] && ">" === e2[e2.length - 1] && 3 <= e2.length ? [null, e2, null] : q.exec(e2)) || !r2[1] && t2) return !t2 || t2.jquery ? (t2 || n2).find(e2) : this.constructor(t2).find(e2);
      if (r2[1]) {
        if (t2 = t2 instanceof S ? t2[0] : t2, S.merge(this, S.parseHTML(r2[1], t2 && t2.nodeType ? t2.ownerDocument || t2 : E, true)), N.test(r2[1]) && S.isPlainObject(t2)) for (r2 in t2) m(this[r2]) ? this[r2](t2[r2]) : this.attr(r2, t2[r2]);
        return this;
      }
      return (i3 = E.getElementById(r2[2])) && (this[0] = i3, this.length = 1), this;
    }
    return e2.nodeType ? (this[0] = e2, this.length = 1, this) : m(e2) ? void 0 !== n2.ready ? n2.ready(e2) : e2(S) : S.makeArray(e2, this);
  }).prototype = S.fn, D = S(E);
  var L = /^(?:parents|prev(?:Until|All))/, H = { children: true, contents: true, next: true, prev: true };
  function O(e2, t2) {
    while ((e2 = e2[t2]) && 1 !== e2.nodeType) ;
    return e2;
  }
  S.fn.extend({ has: function(e2) {
    var t2 = S(e2, this), n2 = t2.length;
    return this.filter(function() {
      for (var e3 = 0; e3 < n2; e3++) if (S.contains(this, t2[e3])) return true;
    });
  }, closest: function(e2, t2) {
    var n2, r2 = 0, i3 = this.length, o2 = [], a2 = "string" != typeof e2 && S(e2);
    if (!k.test(e2)) {
      for (; r2 < i3; r2++) for (n2 = this[r2]; n2 && n2 !== t2; n2 = n2.parentNode) if (n2.nodeType < 11 && (a2 ? -1 < a2.index(n2) : 1 === n2.nodeType && S.find.matchesSelector(n2, e2))) {
        o2.push(n2);
        break;
      }
    }
    return this.pushStack(1 < o2.length ? S.uniqueSort(o2) : o2);
  }, index: function(e2) {
    return e2 ? "string" == typeof e2 ? i2.call(S(e2), this[0]) : i2.call(this, e2.jquery ? e2[0] : e2) : this[0] && this[0].parentNode ? this.first().prevAll().length : -1;
  }, add: function(e2, t2) {
    return this.pushStack(S.uniqueSort(S.merge(this.get(), S(e2, t2))));
  }, addBack: function(e2) {
    return this.add(null == e2 ? this.prevObject : this.prevObject.filter(e2));
  } }), S.each({ parent: function(e2) {
    var t2 = e2.parentNode;
    return t2 && 11 !== t2.nodeType ? t2 : null;
  }, parents: function(e2) {
    return h(e2, "parentNode");
  }, parentsUntil: function(e2, t2, n2) {
    return h(e2, "parentNode", n2);
  }, next: function(e2) {
    return O(e2, "nextSibling");
  }, prev: function(e2) {
    return O(e2, "previousSibling");
  }, nextAll: function(e2) {
    return h(e2, "nextSibling");
  }, prevAll: function(e2) {
    return h(e2, "previousSibling");
  }, nextUntil: function(e2, t2, n2) {
    return h(e2, "nextSibling", n2);
  }, prevUntil: function(e2, t2, n2) {
    return h(e2, "previousSibling", n2);
  }, siblings: function(e2) {
    return T((e2.parentNode || {}).firstChild, e2);
  }, children: function(e2) {
    return T(e2.firstChild);
  }, contents: function(e2) {
    return null != e2.contentDocument && r(e2.contentDocument) ? e2.contentDocument : (A(e2, "template") && (e2 = e2.content || e2), S.merge([], e2.childNodes));
  } }, function(r2, i3) {
    S.fn[r2] = function(e2, t2) {
      var n2 = S.map(this, i3, e2);
      return "Until" !== r2.slice(-5) && (t2 = e2), t2 && "string" == typeof t2 && (n2 = S.filter(t2, n2)), 1 < this.length && (H[r2] || S.uniqueSort(n2), L.test(r2) && n2.reverse()), this.pushStack(n2);
    };
  });
  var P = /[^\x20\t\r\n\f]+/g;
  function R(e2) {
    return e2;
  }
  function M(e2) {
    throw e2;
  }
  function I(e2, t2, n2, r2) {
    var i3;
    try {
      e2 && m(i3 = e2.promise) ? i3.call(e2).done(t2).fail(n2) : e2 && m(i3 = e2.then) ? i3.call(e2, t2, n2) : t2.apply(void 0, [e2].slice(r2));
    } catch (e3) {
      n2.apply(void 0, [e3]);
    }
  }
  S.Callbacks = function(r2) {
    var e2, n2;
    r2 = "string" == typeof r2 ? (e2 = r2, n2 = {}, S.each(e2.match(P) || [], function(e3, t3) {
      n2[t3] = true;
    }), n2) : S.extend({}, r2);
    var i3, t2, o2, a2, s2 = [], u2 = [], l2 = -1, c2 = function() {
      for (a2 = a2 || r2.once, o2 = i3 = true; u2.length; l2 = -1) {
        t2 = u2.shift();
        while (++l2 < s2.length) false === s2[l2].apply(t2[0], t2[1]) && r2.stopOnFalse && (l2 = s2.length, t2 = false);
      }
      r2.memory || (t2 = false), i3 = false, a2 && (s2 = t2 ? [] : "");
    }, f2 = { add: function() {
      return s2 && (t2 && !i3 && (l2 = s2.length - 1, u2.push(t2)), function n3(e3) {
        S.each(e3, function(e4, t3) {
          m(t3) ? r2.unique && f2.has(t3) || s2.push(t3) : t3 && t3.length && "string" !== w(t3) && n3(t3);
        });
      }(arguments), t2 && !i3 && c2()), this;
    }, remove: function() {
      return S.each(arguments, function(e3, t3) {
        var n3;
        while (-1 < (n3 = S.inArray(t3, s2, n3))) s2.splice(n3, 1), n3 <= l2 && l2--;
      }), this;
    }, has: function(e3) {
      return e3 ? -1 < S.inArray(e3, s2) : 0 < s2.length;
    }, empty: function() {
      return s2 && (s2 = []), this;
    }, disable: function() {
      return a2 = u2 = [], s2 = t2 = "", this;
    }, disabled: function() {
      return !s2;
    }, lock: function() {
      return a2 = u2 = [], t2 || i3 || (s2 = t2 = ""), this;
    }, locked: function() {
      return !!a2;
    }, fireWith: function(e3, t3) {
      return a2 || (t3 = [e3, (t3 = t3 || []).slice ? t3.slice() : t3], u2.push(t3), i3 || c2()), this;
    }, fire: function() {
      return f2.fireWith(this, arguments), this;
    }, fired: function() {
      return !!o2;
    } };
    return f2;
  }, S.extend({ Deferred: function(e2) {
    var o2 = [["notify", "progress", S.Callbacks("memory"), S.Callbacks("memory"), 2], ["resolve", "done", S.Callbacks("once memory"), S.Callbacks("once memory"), 0, "resolved"], ["reject", "fail", S.Callbacks("once memory"), S.Callbacks("once memory"), 1, "rejected"]], i3 = "pending", a2 = { state: function() {
      return i3;
    }, always: function() {
      return s2.done(arguments).fail(arguments), this;
    }, "catch": function(e3) {
      return a2.then(null, e3);
    }, pipe: function() {
      var i4 = arguments;
      return S.Deferred(function(r2) {
        S.each(o2, function(e3, t2) {
          var n2 = m(i4[t2[4]]) && i4[t2[4]];
          s2[t2[1]](function() {
            var e4 = n2 && n2.apply(this, arguments);
            e4 && m(e4.promise) ? e4.promise().progress(r2.notify).done(r2.resolve).fail(r2.reject) : r2[t2[0] + "With"](this, n2 ? [e4] : arguments);
          });
        }), i4 = null;
      }).promise();
    }, then: function(t2, n2, r2) {
      var u2 = 0;
      function l2(i4, o3, a3, s3) {
        return function() {
          var n3 = this, r3 = arguments, e3 = function() {
            var e4, t4;
            if (!(i4 < u2)) {
              if ((e4 = a3.apply(n3, r3)) === o3.promise()) throw new TypeError("Thenable self-resolution");
              t4 = e4 && ("object" == typeof e4 || "function" == typeof e4) && e4.then, m(t4) ? s3 ? t4.call(e4, l2(u2, o3, R, s3), l2(u2, o3, M, s3)) : (u2++, t4.call(e4, l2(u2, o3, R, s3), l2(u2, o3, M, s3), l2(u2, o3, R, o3.notifyWith))) : (a3 !== R && (n3 = void 0, r3 = [e4]), (s3 || o3.resolveWith)(n3, r3));
            }
          }, t3 = s3 ? e3 : function() {
            try {
              e3();
            } catch (e4) {
              S.Deferred.exceptionHook && S.Deferred.exceptionHook(e4, t3.stackTrace), u2 <= i4 + 1 && (a3 !== M && (n3 = void 0, r3 = [e4]), o3.rejectWith(n3, r3));
            }
          };
          i4 ? t3() : (S.Deferred.getStackHook && (t3.stackTrace = S.Deferred.getStackHook()), C.setTimeout(t3));
        };
      }
      return S.Deferred(function(e3) {
        o2[0][3].add(l2(0, e3, m(r2) ? r2 : R, e3.notifyWith)), o2[1][3].add(l2(0, e3, m(t2) ? t2 : R)), o2[2][3].add(l2(0, e3, m(n2) ? n2 : M));
      }).promise();
    }, promise: function(e3) {
      return null != e3 ? S.extend(e3, a2) : a2;
    } }, s2 = {};
    return S.each(o2, function(e3, t2) {
      var n2 = t2[2], r2 = t2[5];
      a2[t2[1]] = n2.add, r2 && n2.add(function() {
        i3 = r2;
      }, o2[3 - e3][2].disable, o2[3 - e3][3].disable, o2[0][2].lock, o2[0][3].lock), n2.add(t2[3].fire), s2[t2[0]] = function() {
        return s2[t2[0] + "With"](this === s2 ? void 0 : this, arguments), this;
      }, s2[t2[0] + "With"] = n2.fireWith;
    }), a2.promise(s2), e2 && e2.call(s2, s2), s2;
  }, when: function(e2) {
    var n2 = arguments.length, t2 = n2, r2 = Array(t2), i3 = s.call(arguments), o2 = S.Deferred(), a2 = function(t3) {
      return function(e3) {
        r2[t3] = this, i3[t3] = 1 < arguments.length ? s.call(arguments) : e3, --n2 || o2.resolveWith(r2, i3);
      };
    };
    if (n2 <= 1 && (I(e2, o2.done(a2(t2)).resolve, o2.reject, !n2), "pending" === o2.state() || m(i3[t2] && i3[t2].then))) return o2.then();
    while (t2--) I(i3[t2], a2(t2), o2.reject);
    return o2.promise();
  } });
  var W = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
  S.Deferred.exceptionHook = function(e2, t2) {
    C.console && C.console.warn && e2 && W.test(e2.name) && C.console.warn("jQuery.Deferred exception: " + e2.message, e2.stack, t2);
  }, S.readyException = function(e2) {
    C.setTimeout(function() {
      throw e2;
    });
  };
  var F = S.Deferred();
  function B() {
    E.removeEventListener("DOMContentLoaded", B), C.removeEventListener("load", B), S.ready();
  }
  S.fn.ready = function(e2) {
    return F.then(e2)["catch"](function(e3) {
      S.readyException(e3);
    }), this;
  }, S.extend({ isReady: false, readyWait: 1, ready: function(e2) {
    (true === e2 ? --S.readyWait : S.isReady) || (S.isReady = true) !== e2 && 0 < --S.readyWait || F.resolveWith(E, [S]);
  } }), S.ready.then = F.then, "complete" === E.readyState || "loading" !== E.readyState && !E.documentElement.doScroll ? C.setTimeout(S.ready) : (E.addEventListener("DOMContentLoaded", B), C.addEventListener("load", B));
  var $2 = function(e2, t2, n2, r2, i3, o2, a2) {
    var s2 = 0, u2 = e2.length, l2 = null == n2;
    if ("object" === w(n2)) for (s2 in i3 = true, n2) $2(e2, t2, s2, n2[s2], true, o2, a2);
    else if (void 0 !== r2 && (i3 = true, m(r2) || (a2 = true), l2 && (a2 ? (t2.call(e2, r2), t2 = null) : (l2 = t2, t2 = function(e3, t3, n3) {
      return l2.call(S(e3), n3);
    })), t2)) for (; s2 < u2; s2++) t2(e2[s2], n2, a2 ? r2 : r2.call(e2[s2], s2, t2(e2[s2], n2)));
    return i3 ? e2 : l2 ? t2.call(e2) : u2 ? t2(e2[0], n2) : o2;
  }, _ = /^-ms-/, z = /-([a-z])/g;
  function U(e2, t2) {
    return t2.toUpperCase();
  }
  function X(e2) {
    return e2.replace(_, "ms-").replace(z, U);
  }
  var V = function(e2) {
    return 1 === e2.nodeType || 9 === e2.nodeType || !+e2.nodeType;
  };
  function G() {
    this.expando = S.expando + G.uid++;
  }
  G.uid = 1, G.prototype = { cache: function(e2) {
    var t2 = e2[this.expando];
    return t2 || (t2 = {}, V(e2) && (e2.nodeType ? e2[this.expando] = t2 : Object.defineProperty(e2, this.expando, { value: t2, configurable: true }))), t2;
  }, set: function(e2, t2, n2) {
    var r2, i3 = this.cache(e2);
    if ("string" == typeof t2) i3[X(t2)] = n2;
    else for (r2 in t2) i3[X(r2)] = t2[r2];
    return i3;
  }, get: function(e2, t2) {
    return void 0 === t2 ? this.cache(e2) : e2[this.expando] && e2[this.expando][X(t2)];
  }, access: function(e2, t2, n2) {
    return void 0 === t2 || t2 && "string" == typeof t2 && void 0 === n2 ? this.get(e2, t2) : (this.set(e2, t2, n2), void 0 !== n2 ? n2 : t2);
  }, remove: function(e2, t2) {
    var n2, r2 = e2[this.expando];
    if (void 0 !== r2) {
      if (void 0 !== t2) {
        n2 = (t2 = Array.isArray(t2) ? t2.map(X) : (t2 = X(t2)) in r2 ? [t2] : t2.match(P) || []).length;
        while (n2--) delete r2[t2[n2]];
      }
      (void 0 === t2 || S.isEmptyObject(r2)) && (e2.nodeType ? e2[this.expando] = void 0 : delete e2[this.expando]);
    }
  }, hasData: function(e2) {
    var t2 = e2[this.expando];
    return void 0 !== t2 && !S.isEmptyObject(t2);
  } };
  var Y = new G(), Q = new G(), J = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, K = /[A-Z]/g;
  function Z(e2, t2, n2) {
    var r2, i3;
    if (void 0 === n2 && 1 === e2.nodeType) if (r2 = "data-" + t2.replace(K, "-$&").toLowerCase(), "string" == typeof (n2 = e2.getAttribute(r2))) {
      try {
        n2 = "true" === (i3 = n2) || "false" !== i3 && ("null" === i3 ? null : i3 === +i3 + "" ? +i3 : J.test(i3) ? JSON.parse(i3) : i3);
      } catch (e3) {
      }
      Q.set(e2, t2, n2);
    } else n2 = void 0;
    return n2;
  }
  S.extend({ hasData: function(e2) {
    return Q.hasData(e2) || Y.hasData(e2);
  }, data: function(e2, t2, n2) {
    return Q.access(e2, t2, n2);
  }, removeData: function(e2, t2) {
    Q.remove(e2, t2);
  }, _data: function(e2, t2, n2) {
    return Y.access(e2, t2, n2);
  }, _removeData: function(e2, t2) {
    Y.remove(e2, t2);
  } }), S.fn.extend({ data: function(n2, e2) {
    var t2, r2, i3, o2 = this[0], a2 = o2 && o2.attributes;
    if (void 0 === n2) {
      if (this.length && (i3 = Q.get(o2), 1 === o2.nodeType && !Y.get(o2, "hasDataAttrs"))) {
        t2 = a2.length;
        while (t2--) a2[t2] && 0 === (r2 = a2[t2].name).indexOf("data-") && (r2 = X(r2.slice(5)), Z(o2, r2, i3[r2]));
        Y.set(o2, "hasDataAttrs", true);
      }
      return i3;
    }
    return "object" == typeof n2 ? this.each(function() {
      Q.set(this, n2);
    }) : $2(this, function(e3) {
      var t3;
      if (o2 && void 0 === e3) return void 0 !== (t3 = Q.get(o2, n2)) ? t3 : void 0 !== (t3 = Z(o2, n2)) ? t3 : void 0;
      this.each(function() {
        Q.set(this, n2, e3);
      });
    }, null, e2, 1 < arguments.length, null, true);
  }, removeData: function(e2) {
    return this.each(function() {
      Q.remove(this, e2);
    });
  } }), S.extend({ queue: function(e2, t2, n2) {
    var r2;
    if (e2) return t2 = (t2 || "fx") + "queue", r2 = Y.get(e2, t2), n2 && (!r2 || Array.isArray(n2) ? r2 = Y.access(e2, t2, S.makeArray(n2)) : r2.push(n2)), r2 || [];
  }, dequeue: function(e2, t2) {
    t2 = t2 || "fx";
    var n2 = S.queue(e2, t2), r2 = n2.length, i3 = n2.shift(), o2 = S._queueHooks(e2, t2);
    "inprogress" === i3 && (i3 = n2.shift(), r2--), i3 && ("fx" === t2 && n2.unshift("inprogress"), delete o2.stop, i3.call(e2, function() {
      S.dequeue(e2, t2);
    }, o2)), !r2 && o2 && o2.empty.fire();
  }, _queueHooks: function(e2, t2) {
    var n2 = t2 + "queueHooks";
    return Y.get(e2, n2) || Y.access(e2, n2, { empty: S.Callbacks("once memory").add(function() {
      Y.remove(e2, [t2 + "queue", n2]);
    }) });
  } }), S.fn.extend({ queue: function(t2, n2) {
    var e2 = 2;
    return "string" != typeof t2 && (n2 = t2, t2 = "fx", e2--), arguments.length < e2 ? S.queue(this[0], t2) : void 0 === n2 ? this : this.each(function() {
      var e3 = S.queue(this, t2, n2);
      S._queueHooks(this, t2), "fx" === t2 && "inprogress" !== e3[0] && S.dequeue(this, t2);
    });
  }, dequeue: function(e2) {
    return this.each(function() {
      S.dequeue(this, e2);
    });
  }, clearQueue: function(e2) {
    return this.queue(e2 || "fx", []);
  }, promise: function(e2, t2) {
    var n2, r2 = 1, i3 = S.Deferred(), o2 = this, a2 = this.length, s2 = function() {
      --r2 || i3.resolveWith(o2, [o2]);
    };
    "string" != typeof e2 && (t2 = e2, e2 = void 0), e2 = e2 || "fx";
    while (a2--) (n2 = Y.get(o2[a2], e2 + "queueHooks")) && n2.empty && (r2++, n2.empty.add(s2));
    return s2(), i3.promise(t2);
  } });
  var ee = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source, te = new RegExp("^(?:([+-])=|)(" + ee + ")([a-z%]*)$", "i"), ne = ["Top", "Right", "Bottom", "Left"], re = E.documentElement, ie = function(e2) {
    return S.contains(e2.ownerDocument, e2);
  }, oe = { composed: true };
  re.getRootNode && (ie = function(e2) {
    return S.contains(e2.ownerDocument, e2) || e2.getRootNode(oe) === e2.ownerDocument;
  });
  var ae = function(e2, t2) {
    return "none" === (e2 = t2 || e2).style.display || "" === e2.style.display && ie(e2) && "none" === S.css(e2, "display");
  };
  function se(e2, t2, n2, r2) {
    var i3, o2, a2 = 20, s2 = r2 ? function() {
      return r2.cur();
    } : function() {
      return S.css(e2, t2, "");
    }, u2 = s2(), l2 = n2 && n2[3] || (S.cssNumber[t2] ? "" : "px"), c2 = e2.nodeType && (S.cssNumber[t2] || "px" !== l2 && +u2) && te.exec(S.css(e2, t2));
    if (c2 && c2[3] !== l2) {
      u2 /= 2, l2 = l2 || c2[3], c2 = +u2 || 1;
      while (a2--) S.style(e2, t2, c2 + l2), (1 - o2) * (1 - (o2 = s2() / u2 || 0.5)) <= 0 && (a2 = 0), c2 /= o2;
      c2 *= 2, S.style(e2, t2, c2 + l2), n2 = n2 || [];
    }
    return n2 && (c2 = +c2 || +u2 || 0, i3 = n2[1] ? c2 + (n2[1] + 1) * n2[2] : +n2[2], r2 && (r2.unit = l2, r2.start = c2, r2.end = i3)), i3;
  }
  var ue = {};
  function le(e2, t2) {
    for (var n2, r2, i3, o2, a2, s2, u2, l2 = [], c2 = 0, f2 = e2.length; c2 < f2; c2++) (r2 = e2[c2]).style && (n2 = r2.style.display, t2 ? ("none" === n2 && (l2[c2] = Y.get(r2, "display") || null, l2[c2] || (r2.style.display = "")), "" === r2.style.display && ae(r2) && (l2[c2] = (u2 = a2 = o2 = void 0, a2 = (i3 = r2).ownerDocument, s2 = i3.nodeName, (u2 = ue[s2]) || (o2 = a2.body.appendChild(a2.createElement(s2)), u2 = S.css(o2, "display"), o2.parentNode.removeChild(o2), "none" === u2 && (u2 = "block"), ue[s2] = u2)))) : "none" !== n2 && (l2[c2] = "none", Y.set(r2, "display", n2)));
    for (c2 = 0; c2 < f2; c2++) null != l2[c2] && (e2[c2].style.display = l2[c2]);
    return e2;
  }
  S.fn.extend({ show: function() {
    return le(this, true);
  }, hide: function() {
    return le(this);
  }, toggle: function(e2) {
    return "boolean" == typeof e2 ? e2 ? this.show() : this.hide() : this.each(function() {
      ae(this) ? S(this).show() : S(this).hide();
    });
  } });
  var ce, fe, pe = /^(?:checkbox|radio)$/i, de = /<([a-z][^\/\0>\x20\t\r\n\f]*)/i, he = /^$|^module$|\/(?:java|ecma)script/i;
  ce = E.createDocumentFragment().appendChild(E.createElement("div")), (fe = E.createElement("input")).setAttribute("type", "radio"), fe.setAttribute("checked", "checked"), fe.setAttribute("name", "t"), ce.appendChild(fe), y.checkClone = ce.cloneNode(true).cloneNode(true).lastChild.checked, ce.innerHTML = "<textarea>x</textarea>", y.noCloneChecked = !!ce.cloneNode(true).lastChild.defaultValue, ce.innerHTML = "<option></option>", y.option = !!ce.lastChild;
  var ge = { thead: [1, "<table>", "</table>"], col: [2, "<table><colgroup>", "</colgroup></table>"], tr: [2, "<table><tbody>", "</tbody></table>"], td: [3, "<table><tbody><tr>", "</tr></tbody></table>"], _default: [0, "", ""] };
  function ve(e2, t2) {
    var n2;
    return n2 = "undefined" != typeof e2.getElementsByTagName ? e2.getElementsByTagName(t2 || "*") : "undefined" != typeof e2.querySelectorAll ? e2.querySelectorAll(t2 || "*") : [], void 0 === t2 || t2 && A(e2, t2) ? S.merge([e2], n2) : n2;
  }
  function ye(e2, t2) {
    for (var n2 = 0, r2 = e2.length; n2 < r2; n2++) Y.set(e2[n2], "globalEval", !t2 || Y.get(t2[n2], "globalEval"));
  }
  ge.tbody = ge.tfoot = ge.colgroup = ge.caption = ge.thead, ge.th = ge.td, y.option || (ge.optgroup = ge.option = [1, "<select multiple='multiple'>", "</select>"]);
  var me = /<|&#?\w+;/;
  function xe(e2, t2, n2, r2, i3) {
    for (var o2, a2, s2, u2, l2, c2, f2 = t2.createDocumentFragment(), p2 = [], d2 = 0, h2 = e2.length; d2 < h2; d2++) if ((o2 = e2[d2]) || 0 === o2) if ("object" === w(o2)) S.merge(p2, o2.nodeType ? [o2] : o2);
    else if (me.test(o2)) {
      a2 = a2 || f2.appendChild(t2.createElement("div")), s2 = (de.exec(o2) || ["", ""])[1].toLowerCase(), u2 = ge[s2] || ge._default, a2.innerHTML = u2[1] + S.htmlPrefilter(o2) + u2[2], c2 = u2[0];
      while (c2--) a2 = a2.lastChild;
      S.merge(p2, a2.childNodes), (a2 = f2.firstChild).textContent = "";
    } else p2.push(t2.createTextNode(o2));
    f2.textContent = "", d2 = 0;
    while (o2 = p2[d2++]) if (r2 && -1 < S.inArray(o2, r2)) i3 && i3.push(o2);
    else if (l2 = ie(o2), a2 = ve(f2.appendChild(o2), "script"), l2 && ye(a2), n2) {
      c2 = 0;
      while (o2 = a2[c2++]) he.test(o2.type || "") && n2.push(o2);
    }
    return f2;
  }
  var be = /^([^.]*)(?:\.(.+)|)/;
  function we() {
    return true;
  }
  function Te() {
    return false;
  }
  function Ce(e2, t2) {
    return e2 === function() {
      try {
        return E.activeElement;
      } catch (e3) {
      }
    }() == ("focus" === t2);
  }
  function Ee(e2, t2, n2, r2, i3, o2) {
    var a2, s2;
    if ("object" == typeof t2) {
      for (s2 in "string" != typeof n2 && (r2 = r2 || n2, n2 = void 0), t2) Ee(e2, s2, n2, r2, t2[s2], o2);
      return e2;
    }
    if (null == r2 && null == i3 ? (i3 = n2, r2 = n2 = void 0) : null == i3 && ("string" == typeof n2 ? (i3 = r2, r2 = void 0) : (i3 = r2, r2 = n2, n2 = void 0)), false === i3) i3 = Te;
    else if (!i3) return e2;
    return 1 === o2 && (a2 = i3, (i3 = function(e3) {
      return S().off(e3), a2.apply(this, arguments);
    }).guid = a2.guid || (a2.guid = S.guid++)), e2.each(function() {
      S.event.add(this, t2, i3, r2, n2);
    });
  }
  function Se(e2, i3, o2) {
    o2 ? (Y.set(e2, i3, false), S.event.add(e2, i3, { namespace: false, handler: function(e3) {
      var t2, n2, r2 = Y.get(this, i3);
      if (1 & e3.isTrigger && this[i3]) {
        if (r2.length) (S.event.special[i3] || {}).delegateType && e3.stopPropagation();
        else if (r2 = s.call(arguments), Y.set(this, i3, r2), t2 = o2(this, i3), this[i3](), r2 !== (n2 = Y.get(this, i3)) || t2 ? Y.set(this, i3, false) : n2 = {}, r2 !== n2) return e3.stopImmediatePropagation(), e3.preventDefault(), n2 && n2.value;
      } else r2.length && (Y.set(this, i3, { value: S.event.trigger(S.extend(r2[0], S.Event.prototype), r2.slice(1), this) }), e3.stopImmediatePropagation());
    } })) : void 0 === Y.get(e2, i3) && S.event.add(e2, i3, we);
  }
  S.event = { global: {}, add: function(t2, e2, n2, r2, i3) {
    var o2, a2, s2, u2, l2, c2, f2, p2, d2, h2, g2, v2 = Y.get(t2);
    if (V(t2)) {
      n2.handler && (n2 = (o2 = n2).handler, i3 = o2.selector), i3 && S.find.matchesSelector(re, i3), n2.guid || (n2.guid = S.guid++), (u2 = v2.events) || (u2 = v2.events = /* @__PURE__ */ Object.create(null)), (a2 = v2.handle) || (a2 = v2.handle = function(e3) {
        return "undefined" != typeof S && S.event.triggered !== e3.type ? S.event.dispatch.apply(t2, arguments) : void 0;
      }), l2 = (e2 = (e2 || "").match(P) || [""]).length;
      while (l2--) d2 = g2 = (s2 = be.exec(e2[l2]) || [])[1], h2 = (s2[2] || "").split(".").sort(), d2 && (f2 = S.event.special[d2] || {}, d2 = (i3 ? f2.delegateType : f2.bindType) || d2, f2 = S.event.special[d2] || {}, c2 = S.extend({ type: d2, origType: g2, data: r2, handler: n2, guid: n2.guid, selector: i3, needsContext: i3 && S.expr.match.needsContext.test(i3), namespace: h2.join(".") }, o2), (p2 = u2[d2]) || ((p2 = u2[d2] = []).delegateCount = 0, f2.setup && false !== f2.setup.call(t2, r2, h2, a2) || t2.addEventListener && t2.addEventListener(d2, a2)), f2.add && (f2.add.call(t2, c2), c2.handler.guid || (c2.handler.guid = n2.guid)), i3 ? p2.splice(p2.delegateCount++, 0, c2) : p2.push(c2), S.event.global[d2] = true);
    }
  }, remove: function(e2, t2, n2, r2, i3) {
    var o2, a2, s2, u2, l2, c2, f2, p2, d2, h2, g2, v2 = Y.hasData(e2) && Y.get(e2);
    if (v2 && (u2 = v2.events)) {
      l2 = (t2 = (t2 || "").match(P) || [""]).length;
      while (l2--) if (d2 = g2 = (s2 = be.exec(t2[l2]) || [])[1], h2 = (s2[2] || "").split(".").sort(), d2) {
        f2 = S.event.special[d2] || {}, p2 = u2[d2 = (r2 ? f2.delegateType : f2.bindType) || d2] || [], s2 = s2[2] && new RegExp("(^|\\.)" + h2.join("\\.(?:.*\\.|)") + "(\\.|$)"), a2 = o2 = p2.length;
        while (o2--) c2 = p2[o2], !i3 && g2 !== c2.origType || n2 && n2.guid !== c2.guid || s2 && !s2.test(c2.namespace) || r2 && r2 !== c2.selector && ("**" !== r2 || !c2.selector) || (p2.splice(o2, 1), c2.selector && p2.delegateCount--, f2.remove && f2.remove.call(e2, c2));
        a2 && !p2.length && (f2.teardown && false !== f2.teardown.call(e2, h2, v2.handle) || S.removeEvent(e2, d2, v2.handle), delete u2[d2]);
      } else for (d2 in u2) S.event.remove(e2, d2 + t2[l2], n2, r2, true);
      S.isEmptyObject(u2) && Y.remove(e2, "handle events");
    }
  }, dispatch: function(e2) {
    var t2, n2, r2, i3, o2, a2, s2 = new Array(arguments.length), u2 = S.event.fix(e2), l2 = (Y.get(this, "events") || /* @__PURE__ */ Object.create(null))[u2.type] || [], c2 = S.event.special[u2.type] || {};
    for (s2[0] = u2, t2 = 1; t2 < arguments.length; t2++) s2[t2] = arguments[t2];
    if (u2.delegateTarget = this, !c2.preDispatch || false !== c2.preDispatch.call(this, u2)) {
      a2 = S.event.handlers.call(this, u2, l2), t2 = 0;
      while ((i3 = a2[t2++]) && !u2.isPropagationStopped()) {
        u2.currentTarget = i3.elem, n2 = 0;
        while ((o2 = i3.handlers[n2++]) && !u2.isImmediatePropagationStopped()) u2.rnamespace && false !== o2.namespace && !u2.rnamespace.test(o2.namespace) || (u2.handleObj = o2, u2.data = o2.data, void 0 !== (r2 = ((S.event.special[o2.origType] || {}).handle || o2.handler).apply(i3.elem, s2)) && false === (u2.result = r2) && (u2.preventDefault(), u2.stopPropagation()));
      }
      return c2.postDispatch && c2.postDispatch.call(this, u2), u2.result;
    }
  }, handlers: function(e2, t2) {
    var n2, r2, i3, o2, a2, s2 = [], u2 = t2.delegateCount, l2 = e2.target;
    if (u2 && l2.nodeType && !("click" === e2.type && 1 <= e2.button)) {
      for (; l2 !== this; l2 = l2.parentNode || this) if (1 === l2.nodeType && ("click" !== e2.type || true !== l2.disabled)) {
        for (o2 = [], a2 = {}, n2 = 0; n2 < u2; n2++) void 0 === a2[i3 = (r2 = t2[n2]).selector + " "] && (a2[i3] = r2.needsContext ? -1 < S(i3, this).index(l2) : S.find(i3, this, null, [l2]).length), a2[i3] && o2.push(r2);
        o2.length && s2.push({ elem: l2, handlers: o2 });
      }
    }
    return l2 = this, u2 < t2.length && s2.push({ elem: l2, handlers: t2.slice(u2) }), s2;
  }, addProp: function(t2, e2) {
    Object.defineProperty(S.Event.prototype, t2, { enumerable: true, configurable: true, get: m(e2) ? function() {
      if (this.originalEvent) return e2(this.originalEvent);
    } : function() {
      if (this.originalEvent) return this.originalEvent[t2];
    }, set: function(e3) {
      Object.defineProperty(this, t2, { enumerable: true, configurable: true, writable: true, value: e3 });
    } });
  }, fix: function(e2) {
    return e2[S.expando] ? e2 : new S.Event(e2);
  }, special: { load: { noBubble: true }, click: { setup: function(e2) {
    var t2 = this || e2;
    return pe.test(t2.type) && t2.click && A(t2, "input") && Se(t2, "click", we), false;
  }, trigger: function(e2) {
    var t2 = this || e2;
    return pe.test(t2.type) && t2.click && A(t2, "input") && Se(t2, "click"), true;
  }, _default: function(e2) {
    var t2 = e2.target;
    return pe.test(t2.type) && t2.click && A(t2, "input") && Y.get(t2, "click") || A(t2, "a");
  } }, beforeunload: { postDispatch: function(e2) {
    void 0 !== e2.result && e2.originalEvent && (e2.originalEvent.returnValue = e2.result);
  } } } }, S.removeEvent = function(e2, t2, n2) {
    e2.removeEventListener && e2.removeEventListener(t2, n2);
  }, S.Event = function(e2, t2) {
    if (!(this instanceof S.Event)) return new S.Event(e2, t2);
    e2 && e2.type ? (this.originalEvent = e2, this.type = e2.type, this.isDefaultPrevented = e2.defaultPrevented || void 0 === e2.defaultPrevented && false === e2.returnValue ? we : Te, this.target = e2.target && 3 === e2.target.nodeType ? e2.target.parentNode : e2.target, this.currentTarget = e2.currentTarget, this.relatedTarget = e2.relatedTarget) : this.type = e2, t2 && S.extend(this, t2), this.timeStamp = e2 && e2.timeStamp || Date.now(), this[S.expando] = true;
  }, S.Event.prototype = { constructor: S.Event, isDefaultPrevented: Te, isPropagationStopped: Te, isImmediatePropagationStopped: Te, isSimulated: false, preventDefault: function() {
    var e2 = this.originalEvent;
    this.isDefaultPrevented = we, e2 && !this.isSimulated && e2.preventDefault();
  }, stopPropagation: function() {
    var e2 = this.originalEvent;
    this.isPropagationStopped = we, e2 && !this.isSimulated && e2.stopPropagation();
  }, stopImmediatePropagation: function() {
    var e2 = this.originalEvent;
    this.isImmediatePropagationStopped = we, e2 && !this.isSimulated && e2.stopImmediatePropagation(), this.stopPropagation();
  } }, S.each({ altKey: true, bubbles: true, cancelable: true, changedTouches: true, ctrlKey: true, detail: true, eventPhase: true, metaKey: true, pageX: true, pageY: true, shiftKey: true, view: true, "char": true, code: true, charCode: true, key: true, keyCode: true, button: true, buttons: true, clientX: true, clientY: true, offsetX: true, offsetY: true, pointerId: true, pointerType: true, screenX: true, screenY: true, targetTouches: true, toElement: true, touches: true, which: true }, S.event.addProp), S.each({ focus: "focusin", blur: "focusout" }, function(e2, t2) {
    S.event.special[e2] = { setup: function() {
      return Se(this, e2, Ce), false;
    }, trigger: function() {
      return Se(this, e2), true;
    }, _default: function() {
      return true;
    }, delegateType: t2 };
  }), S.each({ mouseenter: "mouseover", mouseleave: "mouseout", pointerenter: "pointerover", pointerleave: "pointerout" }, function(e2, i3) {
    S.event.special[e2] = { delegateType: i3, bindType: i3, handle: function(e3) {
      var t2, n2 = e3.relatedTarget, r2 = e3.handleObj;
      return n2 && (n2 === this || S.contains(this, n2)) || (e3.type = r2.origType, t2 = r2.handler.apply(this, arguments), e3.type = i3), t2;
    } };
  }), S.fn.extend({ on: function(e2, t2, n2, r2) {
    return Ee(this, e2, t2, n2, r2);
  }, one: function(e2, t2, n2, r2) {
    return Ee(this, e2, t2, n2, r2, 1);
  }, off: function(e2, t2, n2) {
    var r2, i3;
    if (e2 && e2.preventDefault && e2.handleObj) return r2 = e2.handleObj, S(e2.delegateTarget).off(r2.namespace ? r2.origType + "." + r2.namespace : r2.origType, r2.selector, r2.handler), this;
    if ("object" == typeof e2) {
      for (i3 in e2) this.off(i3, t2, e2[i3]);
      return this;
    }
    return false !== t2 && "function" != typeof t2 || (n2 = t2, t2 = void 0), false === n2 && (n2 = Te), this.each(function() {
      S.event.remove(this, e2, n2, t2);
    });
  } });
  var ke = /<script|<style|<link/i, Ae = /checked\s*(?:[^=]|=\s*.checked.)/i, Ne = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g;
  function je(e2, t2) {
    return A(e2, "table") && A(11 !== t2.nodeType ? t2 : t2.firstChild, "tr") && S(e2).children("tbody")[0] || e2;
  }
  function De(e2) {
    return e2.type = (null !== e2.getAttribute("type")) + "/" + e2.type, e2;
  }
  function qe(e2) {
    return "true/" === (e2.type || "").slice(0, 5) ? e2.type = e2.type.slice(5) : e2.removeAttribute("type"), e2;
  }
  function Le(e2, t2) {
    var n2, r2, i3, o2, a2, s2;
    if (1 === t2.nodeType) {
      if (Y.hasData(e2) && (s2 = Y.get(e2).events)) for (i3 in Y.remove(t2, "handle events"), s2) for (n2 = 0, r2 = s2[i3].length; n2 < r2; n2++) S.event.add(t2, i3, s2[i3][n2]);
      Q.hasData(e2) && (o2 = Q.access(e2), a2 = S.extend({}, o2), Q.set(t2, a2));
    }
  }
  function He(n2, r2, i3, o2) {
    r2 = g(r2);
    var e2, t2, a2, s2, u2, l2, c2 = 0, f2 = n2.length, p2 = f2 - 1, d2 = r2[0], h2 = m(d2);
    if (h2 || 1 < f2 && "string" == typeof d2 && !y.checkClone && Ae.test(d2)) return n2.each(function(e3) {
      var t3 = n2.eq(e3);
      h2 && (r2[0] = d2.call(this, e3, t3.html())), He(t3, r2, i3, o2);
    });
    if (f2 && (t2 = (e2 = xe(r2, n2[0].ownerDocument, false, n2, o2)).firstChild, 1 === e2.childNodes.length && (e2 = t2), t2 || o2)) {
      for (s2 = (a2 = S.map(ve(e2, "script"), De)).length; c2 < f2; c2++) u2 = e2, c2 !== p2 && (u2 = S.clone(u2, true, true), s2 && S.merge(a2, ve(u2, "script"))), i3.call(n2[c2], u2, c2);
      if (s2) for (l2 = a2[a2.length - 1].ownerDocument, S.map(a2, qe), c2 = 0; c2 < s2; c2++) u2 = a2[c2], he.test(u2.type || "") && !Y.access(u2, "globalEval") && S.contains(l2, u2) && (u2.src && "module" !== (u2.type || "").toLowerCase() ? S._evalUrl && !u2.noModule && S._evalUrl(u2.src, { nonce: u2.nonce || u2.getAttribute("nonce") }, l2) : b(u2.textContent.replace(Ne, ""), u2, l2));
    }
    return n2;
  }
  function Oe(e2, t2, n2) {
    for (var r2, i3 = t2 ? S.filter(t2, e2) : e2, o2 = 0; null != (r2 = i3[o2]); o2++) n2 || 1 !== r2.nodeType || S.cleanData(ve(r2)), r2.parentNode && (n2 && ie(r2) && ye(ve(r2, "script")), r2.parentNode.removeChild(r2));
    return e2;
  }
  S.extend({ htmlPrefilter: function(e2) {
    return e2;
  }, clone: function(e2, t2, n2) {
    var r2, i3, o2, a2, s2, u2, l2, c2 = e2.cloneNode(true), f2 = ie(e2);
    if (!(y.noCloneChecked || 1 !== e2.nodeType && 11 !== e2.nodeType || S.isXMLDoc(e2))) for (a2 = ve(c2), r2 = 0, i3 = (o2 = ve(e2)).length; r2 < i3; r2++) s2 = o2[r2], u2 = a2[r2], void 0, "input" === (l2 = u2.nodeName.toLowerCase()) && pe.test(s2.type) ? u2.checked = s2.checked : "input" !== l2 && "textarea" !== l2 || (u2.defaultValue = s2.defaultValue);
    if (t2) if (n2) for (o2 = o2 || ve(e2), a2 = a2 || ve(c2), r2 = 0, i3 = o2.length; r2 < i3; r2++) Le(o2[r2], a2[r2]);
    else Le(e2, c2);
    return 0 < (a2 = ve(c2, "script")).length && ye(a2, !f2 && ve(e2, "script")), c2;
  }, cleanData: function(e2) {
    for (var t2, n2, r2, i3 = S.event.special, o2 = 0; void 0 !== (n2 = e2[o2]); o2++) if (V(n2)) {
      if (t2 = n2[Y.expando]) {
        if (t2.events) for (r2 in t2.events) i3[r2] ? S.event.remove(n2, r2) : S.removeEvent(n2, r2, t2.handle);
        n2[Y.expando] = void 0;
      }
      n2[Q.expando] && (n2[Q.expando] = void 0);
    }
  } }), S.fn.extend({ detach: function(e2) {
    return Oe(this, e2, true);
  }, remove: function(e2) {
    return Oe(this, e2);
  }, text: function(e2) {
    return $2(this, function(e3) {
      return void 0 === e3 ? S.text(this) : this.empty().each(function() {
        1 !== this.nodeType && 11 !== this.nodeType && 9 !== this.nodeType || (this.textContent = e3);
      });
    }, null, e2, arguments.length);
  }, append: function() {
    return He(this, arguments, function(e2) {
      1 !== this.nodeType && 11 !== this.nodeType && 9 !== this.nodeType || je(this, e2).appendChild(e2);
    });
  }, prepend: function() {
    return He(this, arguments, function(e2) {
      if (1 === this.nodeType || 11 === this.nodeType || 9 === this.nodeType) {
        var t2 = je(this, e2);
        t2.insertBefore(e2, t2.firstChild);
      }
    });
  }, before: function() {
    return He(this, arguments, function(e2) {
      this.parentNode && this.parentNode.insertBefore(e2, this);
    });
  }, after: function() {
    return He(this, arguments, function(e2) {
      this.parentNode && this.parentNode.insertBefore(e2, this.nextSibling);
    });
  }, empty: function() {
    for (var e2, t2 = 0; null != (e2 = this[t2]); t2++) 1 === e2.nodeType && (S.cleanData(ve(e2, false)), e2.textContent = "");
    return this;
  }, clone: function(e2, t2) {
    return e2 = null != e2 && e2, t2 = null == t2 ? e2 : t2, this.map(function() {
      return S.clone(this, e2, t2);
    });
  }, html: function(e2) {
    return $2(this, function(e3) {
      var t2 = this[0] || {}, n2 = 0, r2 = this.length;
      if (void 0 === e3 && 1 === t2.nodeType) return t2.innerHTML;
      if ("string" == typeof e3 && !ke.test(e3) && !ge[(de.exec(e3) || ["", ""])[1].toLowerCase()]) {
        e3 = S.htmlPrefilter(e3);
        try {
          for (; n2 < r2; n2++) 1 === (t2 = this[n2] || {}).nodeType && (S.cleanData(ve(t2, false)), t2.innerHTML = e3);
          t2 = 0;
        } catch (e4) {
        }
      }
      t2 && this.empty().append(e3);
    }, null, e2, arguments.length);
  }, replaceWith: function() {
    var n2 = [];
    return He(this, arguments, function(e2) {
      var t2 = this.parentNode;
      S.inArray(this, n2) < 0 && (S.cleanData(ve(this)), t2 && t2.replaceChild(e2, this));
    }, n2);
  } }), S.each({ appendTo: "append", prependTo: "prepend", insertBefore: "before", insertAfter: "after", replaceAll: "replaceWith" }, function(e2, a2) {
    S.fn[e2] = function(e3) {
      for (var t2, n2 = [], r2 = S(e3), i3 = r2.length - 1, o2 = 0; o2 <= i3; o2++) t2 = o2 === i3 ? this : this.clone(true), S(r2[o2])[a2](t2), u.apply(n2, t2.get());
      return this.pushStack(n2);
    };
  });
  var Pe = new RegExp("^(" + ee + ")(?!px)[a-z%]+$", "i"), Re = function(e2) {
    var t2 = e2.ownerDocument.defaultView;
    return t2 && t2.opener || (t2 = C), t2.getComputedStyle(e2);
  }, Me = function(e2, t2, n2) {
    var r2, i3, o2 = {};
    for (i3 in t2) o2[i3] = e2.style[i3], e2.style[i3] = t2[i3];
    for (i3 in r2 = n2.call(e2), t2) e2.style[i3] = o2[i3];
    return r2;
  }, Ie = new RegExp(ne.join("|"), "i");
  function We(e2, t2, n2) {
    var r2, i3, o2, a2, s2 = e2.style;
    return (n2 = n2 || Re(e2)) && ("" !== (a2 = n2.getPropertyValue(t2) || n2[t2]) || ie(e2) || (a2 = S.style(e2, t2)), !y.pixelBoxStyles() && Pe.test(a2) && Ie.test(t2) && (r2 = s2.width, i3 = s2.minWidth, o2 = s2.maxWidth, s2.minWidth = s2.maxWidth = s2.width = a2, a2 = n2.width, s2.width = r2, s2.minWidth = i3, s2.maxWidth = o2)), void 0 !== a2 ? a2 + "" : a2;
  }
  function Fe(e2, t2) {
    return { get: function() {
      if (!e2()) return (this.get = t2).apply(this, arguments);
      delete this.get;
    } };
  }
  !function() {
    function e2() {
      if (l2) {
        u2.style.cssText = "position:absolute;left:-11111px;width:60px;margin-top:1px;padding:0;border:0", l2.style.cssText = "position:relative;display:block;box-sizing:border-box;overflow:scroll;margin:auto;border:1px;padding:1px;width:60%;top:1%", re.appendChild(u2).appendChild(l2);
        var e3 = C.getComputedStyle(l2);
        n2 = "1%" !== e3.top, s2 = 12 === t2(e3.marginLeft), l2.style.right = "60%", o2 = 36 === t2(e3.right), r2 = 36 === t2(e3.width), l2.style.position = "absolute", i3 = 12 === t2(l2.offsetWidth / 3), re.removeChild(u2), l2 = null;
      }
    }
    function t2(e3) {
      return Math.round(parseFloat(e3));
    }
    var n2, r2, i3, o2, a2, s2, u2 = E.createElement("div"), l2 = E.createElement("div");
    l2.style && (l2.style.backgroundClip = "content-box", l2.cloneNode(true).style.backgroundClip = "", y.clearCloneStyle = "content-box" === l2.style.backgroundClip, S.extend(y, { boxSizingReliable: function() {
      return e2(), r2;
    }, pixelBoxStyles: function() {
      return e2(), o2;
    }, pixelPosition: function() {
      return e2(), n2;
    }, reliableMarginLeft: function() {
      return e2(), s2;
    }, scrollboxSize: function() {
      return e2(), i3;
    }, reliableTrDimensions: function() {
      var e3, t3, n3, r3;
      return null == a2 && (e3 = E.createElement("table"), t3 = E.createElement("tr"), n3 = E.createElement("div"), e3.style.cssText = "position:absolute;left:-11111px;border-collapse:separate", t3.style.cssText = "border:1px solid", t3.style.height = "1px", n3.style.height = "9px", n3.style.display = "block", re.appendChild(e3).appendChild(t3).appendChild(n3), r3 = C.getComputedStyle(t3), a2 = parseInt(r3.height, 10) + parseInt(r3.borderTopWidth, 10) + parseInt(r3.borderBottomWidth, 10) === t3.offsetHeight, re.removeChild(e3)), a2;
    } }));
  }();
  var Be = ["Webkit", "Moz", "ms"], $e = E.createElement("div").style, _e = {};
  function ze(e2) {
    var t2 = S.cssProps[e2] || _e[e2];
    return t2 || (e2 in $e ? e2 : _e[e2] = function(e3) {
      var t3 = e3[0].toUpperCase() + e3.slice(1), n2 = Be.length;
      while (n2--) if ((e3 = Be[n2] + t3) in $e) return e3;
    }(e2) || e2);
  }
  var Ue = /^(none|table(?!-c[ea]).+)/, Xe = /^--/, Ve = { position: "absolute", visibility: "hidden", display: "block" }, Ge = { letterSpacing: "0", fontWeight: "400" };
  function Ye(e2, t2, n2) {
    var r2 = te.exec(t2);
    return r2 ? Math.max(0, r2[2] - (n2 || 0)) + (r2[3] || "px") : t2;
  }
  function Qe(e2, t2, n2, r2, i3, o2) {
    var a2 = "width" === t2 ? 1 : 0, s2 = 0, u2 = 0;
    if (n2 === (r2 ? "border" : "content")) return 0;
    for (; a2 < 4; a2 += 2) "margin" === n2 && (u2 += S.css(e2, n2 + ne[a2], true, i3)), r2 ? ("content" === n2 && (u2 -= S.css(e2, "padding" + ne[a2], true, i3)), "margin" !== n2 && (u2 -= S.css(e2, "border" + ne[a2] + "Width", true, i3))) : (u2 += S.css(e2, "padding" + ne[a2], true, i3), "padding" !== n2 ? u2 += S.css(e2, "border" + ne[a2] + "Width", true, i3) : s2 += S.css(e2, "border" + ne[a2] + "Width", true, i3));
    return !r2 && 0 <= o2 && (u2 += Math.max(0, Math.ceil(e2["offset" + t2[0].toUpperCase() + t2.slice(1)] - o2 - u2 - s2 - 0.5)) || 0), u2;
  }
  function Je(e2, t2, n2) {
    var r2 = Re(e2), i3 = (!y.boxSizingReliable() || n2) && "border-box" === S.css(e2, "boxSizing", false, r2), o2 = i3, a2 = We(e2, t2, r2), s2 = "offset" + t2[0].toUpperCase() + t2.slice(1);
    if (Pe.test(a2)) {
      if (!n2) return a2;
      a2 = "auto";
    }
    return (!y.boxSizingReliable() && i3 || !y.reliableTrDimensions() && A(e2, "tr") || "auto" === a2 || !parseFloat(a2) && "inline" === S.css(e2, "display", false, r2)) && e2.getClientRects().length && (i3 = "border-box" === S.css(e2, "boxSizing", false, r2), (o2 = s2 in e2) && (a2 = e2[s2])), (a2 = parseFloat(a2) || 0) + Qe(e2, t2, n2 || (i3 ? "border" : "content"), o2, r2, a2) + "px";
  }
  function Ke(e2, t2, n2, r2, i3) {
    return new Ke.prototype.init(e2, t2, n2, r2, i3);
  }
  S.extend({ cssHooks: { opacity: { get: function(e2, t2) {
    if (t2) {
      var n2 = We(e2, "opacity");
      return "" === n2 ? "1" : n2;
    }
  } } }, cssNumber: { animationIterationCount: true, columnCount: true, fillOpacity: true, flexGrow: true, flexShrink: true, fontWeight: true, gridArea: true, gridColumn: true, gridColumnEnd: true, gridColumnStart: true, gridRow: true, gridRowEnd: true, gridRowStart: true, lineHeight: true, opacity: true, order: true, orphans: true, widows: true, zIndex: true, zoom: true }, cssProps: {}, style: function(e2, t2, n2, r2) {
    if (e2 && 3 !== e2.nodeType && 8 !== e2.nodeType && e2.style) {
      var i3, o2, a2, s2 = X(t2), u2 = Xe.test(t2), l2 = e2.style;
      if (u2 || (t2 = ze(s2)), a2 = S.cssHooks[t2] || S.cssHooks[s2], void 0 === n2) return a2 && "get" in a2 && void 0 !== (i3 = a2.get(e2, false, r2)) ? i3 : l2[t2];
      "string" === (o2 = typeof n2) && (i3 = te.exec(n2)) && i3[1] && (n2 = se(e2, t2, i3), o2 = "number"), null != n2 && n2 == n2 && ("number" !== o2 || u2 || (n2 += i3 && i3[3] || (S.cssNumber[s2] ? "" : "px")), y.clearCloneStyle || "" !== n2 || 0 !== t2.indexOf("background") || (l2[t2] = "inherit"), a2 && "set" in a2 && void 0 === (n2 = a2.set(e2, n2, r2)) || (u2 ? l2.setProperty(t2, n2) : l2[t2] = n2));
    }
  }, css: function(e2, t2, n2, r2) {
    var i3, o2, a2, s2 = X(t2);
    return Xe.test(t2) || (t2 = ze(s2)), (a2 = S.cssHooks[t2] || S.cssHooks[s2]) && "get" in a2 && (i3 = a2.get(e2, true, n2)), void 0 === i3 && (i3 = We(e2, t2, r2)), "normal" === i3 && t2 in Ge && (i3 = Ge[t2]), "" === n2 || n2 ? (o2 = parseFloat(i3), true === n2 || isFinite(o2) ? o2 || 0 : i3) : i3;
  } }), S.each(["height", "width"], function(e2, u2) {
    S.cssHooks[u2] = { get: function(e3, t2, n2) {
      if (t2) return !Ue.test(S.css(e3, "display")) || e3.getClientRects().length && e3.getBoundingClientRect().width ? Je(e3, u2, n2) : Me(e3, Ve, function() {
        return Je(e3, u2, n2);
      });
    }, set: function(e3, t2, n2) {
      var r2, i3 = Re(e3), o2 = !y.scrollboxSize() && "absolute" === i3.position, a2 = (o2 || n2) && "border-box" === S.css(e3, "boxSizing", false, i3), s2 = n2 ? Qe(e3, u2, n2, a2, i3) : 0;
      return a2 && o2 && (s2 -= Math.ceil(e3["offset" + u2[0].toUpperCase() + u2.slice(1)] - parseFloat(i3[u2]) - Qe(e3, u2, "border", false, i3) - 0.5)), s2 && (r2 = te.exec(t2)) && "px" !== (r2[3] || "px") && (e3.style[u2] = t2, t2 = S.css(e3, u2)), Ye(0, t2, s2);
    } };
  }), S.cssHooks.marginLeft = Fe(y.reliableMarginLeft, function(e2, t2) {
    if (t2) return (parseFloat(We(e2, "marginLeft")) || e2.getBoundingClientRect().left - Me(e2, { marginLeft: 0 }, function() {
      return e2.getBoundingClientRect().left;
    })) + "px";
  }), S.each({ margin: "", padding: "", border: "Width" }, function(i3, o2) {
    S.cssHooks[i3 + o2] = { expand: function(e2) {
      for (var t2 = 0, n2 = {}, r2 = "string" == typeof e2 ? e2.split(" ") : [e2]; t2 < 4; t2++) n2[i3 + ne[t2] + o2] = r2[t2] || r2[t2 - 2] || r2[0];
      return n2;
    } }, "margin" !== i3 && (S.cssHooks[i3 + o2].set = Ye);
  }), S.fn.extend({ css: function(e2, t2) {
    return $2(this, function(e3, t3, n2) {
      var r2, i3, o2 = {}, a2 = 0;
      if (Array.isArray(t3)) {
        for (r2 = Re(e3), i3 = t3.length; a2 < i3; a2++) o2[t3[a2]] = S.css(e3, t3[a2], false, r2);
        return o2;
      }
      return void 0 !== n2 ? S.style(e3, t3, n2) : S.css(e3, t3);
    }, e2, t2, 1 < arguments.length);
  } }), ((S.Tween = Ke).prototype = { constructor: Ke, init: function(e2, t2, n2, r2, i3, o2) {
    this.elem = e2, this.prop = n2, this.easing = i3 || S.easing._default, this.options = t2, this.start = this.now = this.cur(), this.end = r2, this.unit = o2 || (S.cssNumber[n2] ? "" : "px");
  }, cur: function() {
    var e2 = Ke.propHooks[this.prop];
    return e2 && e2.get ? e2.get(this) : Ke.propHooks._default.get(this);
  }, run: function(e2) {
    var t2, n2 = Ke.propHooks[this.prop];
    return this.options.duration ? this.pos = t2 = S.easing[this.easing](e2, this.options.duration * e2, 0, 1, this.options.duration) : this.pos = t2 = e2, this.now = (this.end - this.start) * t2 + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), n2 && n2.set ? n2.set(this) : Ke.propHooks._default.set(this), this;
  } }).init.prototype = Ke.prototype, (Ke.propHooks = { _default: { get: function(e2) {
    var t2;
    return 1 !== e2.elem.nodeType || null != e2.elem[e2.prop] && null == e2.elem.style[e2.prop] ? e2.elem[e2.prop] : (t2 = S.css(e2.elem, e2.prop, "")) && "auto" !== t2 ? t2 : 0;
  }, set: function(e2) {
    S.fx.step[e2.prop] ? S.fx.step[e2.prop](e2) : 1 !== e2.elem.nodeType || !S.cssHooks[e2.prop] && null == e2.elem.style[ze(e2.prop)] ? e2.elem[e2.prop] = e2.now : S.style(e2.elem, e2.prop, e2.now + e2.unit);
  } } }).scrollTop = Ke.propHooks.scrollLeft = { set: function(e2) {
    e2.elem.nodeType && e2.elem.parentNode && (e2.elem[e2.prop] = e2.now);
  } }, S.easing = { linear: function(e2) {
    return e2;
  }, swing: function(e2) {
    return 0.5 - Math.cos(e2 * Math.PI) / 2;
  }, _default: "swing" }, S.fx = Ke.prototype.init, S.fx.step = {};
  var Ze, et, tt, nt, rt = /^(?:toggle|show|hide)$/, it = /queueHooks$/;
  function ot() {
    et && (false === E.hidden && C.requestAnimationFrame ? C.requestAnimationFrame(ot) : C.setTimeout(ot, S.fx.interval), S.fx.tick());
  }
  function at() {
    return C.setTimeout(function() {
      Ze = void 0;
    }), Ze = Date.now();
  }
  function st(e2, t2) {
    var n2, r2 = 0, i3 = { height: e2 };
    for (t2 = t2 ? 1 : 0; r2 < 4; r2 += 2 - t2) i3["margin" + (n2 = ne[r2])] = i3["padding" + n2] = e2;
    return t2 && (i3.opacity = i3.width = e2), i3;
  }
  function ut(e2, t2, n2) {
    for (var r2, i3 = (lt.tweeners[t2] || []).concat(lt.tweeners["*"]), o2 = 0, a2 = i3.length; o2 < a2; o2++) if (r2 = i3[o2].call(n2, t2, e2)) return r2;
  }
  function lt(o2, e2, t2) {
    var n2, a2, r2 = 0, i3 = lt.prefilters.length, s2 = S.Deferred().always(function() {
      delete u2.elem;
    }), u2 = function() {
      if (a2) return false;
      for (var e3 = Ze || at(), t3 = Math.max(0, l2.startTime + l2.duration - e3), n3 = 1 - (t3 / l2.duration || 0), r3 = 0, i4 = l2.tweens.length; r3 < i4; r3++) l2.tweens[r3].run(n3);
      return s2.notifyWith(o2, [l2, n3, t3]), n3 < 1 && i4 ? t3 : (i4 || s2.notifyWith(o2, [l2, 1, 0]), s2.resolveWith(o2, [l2]), false);
    }, l2 = s2.promise({ elem: o2, props: S.extend({}, e2), opts: S.extend(true, { specialEasing: {}, easing: S.easing._default }, t2), originalProperties: e2, originalOptions: t2, startTime: Ze || at(), duration: t2.duration, tweens: [], createTween: function(e3, t3) {
      var n3 = S.Tween(o2, l2.opts, e3, t3, l2.opts.specialEasing[e3] || l2.opts.easing);
      return l2.tweens.push(n3), n3;
    }, stop: function(e3) {
      var t3 = 0, n3 = e3 ? l2.tweens.length : 0;
      if (a2) return this;
      for (a2 = true; t3 < n3; t3++) l2.tweens[t3].run(1);
      return e3 ? (s2.notifyWith(o2, [l2, 1, 0]), s2.resolveWith(o2, [l2, e3])) : s2.rejectWith(o2, [l2, e3]), this;
    } }), c2 = l2.props;
    for (!function(e3, t3) {
      var n3, r3, i4, o3, a3;
      for (n3 in e3) if (i4 = t3[r3 = X(n3)], o3 = e3[n3], Array.isArray(o3) && (i4 = o3[1], o3 = e3[n3] = o3[0]), n3 !== r3 && (e3[r3] = o3, delete e3[n3]), (a3 = S.cssHooks[r3]) && "expand" in a3) for (n3 in o3 = a3.expand(o3), delete e3[r3], o3) n3 in e3 || (e3[n3] = o3[n3], t3[n3] = i4);
      else t3[r3] = i4;
    }(c2, l2.opts.specialEasing); r2 < i3; r2++) if (n2 = lt.prefilters[r2].call(l2, o2, c2, l2.opts)) return m(n2.stop) && (S._queueHooks(l2.elem, l2.opts.queue).stop = n2.stop.bind(n2)), n2;
    return S.map(c2, ut, l2), m(l2.opts.start) && l2.opts.start.call(o2, l2), l2.progress(l2.opts.progress).done(l2.opts.done, l2.opts.complete).fail(l2.opts.fail).always(l2.opts.always), S.fx.timer(S.extend(u2, { elem: o2, anim: l2, queue: l2.opts.queue })), l2;
  }
  S.Animation = S.extend(lt, { tweeners: { "*": [function(e2, t2) {
    var n2 = this.createTween(e2, t2);
    return se(n2.elem, e2, te.exec(t2), n2), n2;
  }] }, tweener: function(e2, t2) {
    m(e2) ? (t2 = e2, e2 = ["*"]) : e2 = e2.match(P);
    for (var n2, r2 = 0, i3 = e2.length; r2 < i3; r2++) n2 = e2[r2], lt.tweeners[n2] = lt.tweeners[n2] || [], lt.tweeners[n2].unshift(t2);
  }, prefilters: [function(e2, t2, n2) {
    var r2, i3, o2, a2, s2, u2, l2, c2, f2 = "width" in t2 || "height" in t2, p2 = this, d2 = {}, h2 = e2.style, g2 = e2.nodeType && ae(e2), v2 = Y.get(e2, "fxshow");
    for (r2 in n2.queue || (null == (a2 = S._queueHooks(e2, "fx")).unqueued && (a2.unqueued = 0, s2 = a2.empty.fire, a2.empty.fire = function() {
      a2.unqueued || s2();
    }), a2.unqueued++, p2.always(function() {
      p2.always(function() {
        a2.unqueued--, S.queue(e2, "fx").length || a2.empty.fire();
      });
    })), t2) if (i3 = t2[r2], rt.test(i3)) {
      if (delete t2[r2], o2 = o2 || "toggle" === i3, i3 === (g2 ? "hide" : "show")) {
        if ("show" !== i3 || !v2 || void 0 === v2[r2]) continue;
        g2 = true;
      }
      d2[r2] = v2 && v2[r2] || S.style(e2, r2);
    }
    if ((u2 = !S.isEmptyObject(t2)) || !S.isEmptyObject(d2)) for (r2 in f2 && 1 === e2.nodeType && (n2.overflow = [h2.overflow, h2.overflowX, h2.overflowY], null == (l2 = v2 && v2.display) && (l2 = Y.get(e2, "display")), "none" === (c2 = S.css(e2, "display")) && (l2 ? c2 = l2 : (le([e2], true), l2 = e2.style.display || l2, c2 = S.css(e2, "display"), le([e2]))), ("inline" === c2 || "inline-block" === c2 && null != l2) && "none" === S.css(e2, "float") && (u2 || (p2.done(function() {
      h2.display = l2;
    }), null == l2 && (c2 = h2.display, l2 = "none" === c2 ? "" : c2)), h2.display = "inline-block")), n2.overflow && (h2.overflow = "hidden", p2.always(function() {
      h2.overflow = n2.overflow[0], h2.overflowX = n2.overflow[1], h2.overflowY = n2.overflow[2];
    })), u2 = false, d2) u2 || (v2 ? "hidden" in v2 && (g2 = v2.hidden) : v2 = Y.access(e2, "fxshow", { display: l2 }), o2 && (v2.hidden = !g2), g2 && le([e2], true), p2.done(function() {
      for (r2 in g2 || le([e2]), Y.remove(e2, "fxshow"), d2) S.style(e2, r2, d2[r2]);
    })), u2 = ut(g2 ? v2[r2] : 0, r2, p2), r2 in v2 || (v2[r2] = u2.start, g2 && (u2.end = u2.start, u2.start = 0));
  }], prefilter: function(e2, t2) {
    t2 ? lt.prefilters.unshift(e2) : lt.prefilters.push(e2);
  } }), S.speed = function(e2, t2, n2) {
    var r2 = e2 && "object" == typeof e2 ? S.extend({}, e2) : { complete: n2 || !n2 && t2 || m(e2) && e2, duration: e2, easing: n2 && t2 || t2 && !m(t2) && t2 };
    return S.fx.off ? r2.duration = 0 : "number" != typeof r2.duration && (r2.duration in S.fx.speeds ? r2.duration = S.fx.speeds[r2.duration] : r2.duration = S.fx.speeds._default), null != r2.queue && true !== r2.queue || (r2.queue = "fx"), r2.old = r2.complete, r2.complete = function() {
      m(r2.old) && r2.old.call(this), r2.queue && S.dequeue(this, r2.queue);
    }, r2;
  }, S.fn.extend({ fadeTo: function(e2, t2, n2, r2) {
    return this.filter(ae).css("opacity", 0).show().end().animate({ opacity: t2 }, e2, n2, r2);
  }, animate: function(t2, e2, n2, r2) {
    var i3 = S.isEmptyObject(t2), o2 = S.speed(e2, n2, r2), a2 = function() {
      var e3 = lt(this, S.extend({}, t2), o2);
      (i3 || Y.get(this, "finish")) && e3.stop(true);
    };
    return a2.finish = a2, i3 || false === o2.queue ? this.each(a2) : this.queue(o2.queue, a2);
  }, stop: function(i3, e2, o2) {
    var a2 = function(e3) {
      var t2 = e3.stop;
      delete e3.stop, t2(o2);
    };
    return "string" != typeof i3 && (o2 = e2, e2 = i3, i3 = void 0), e2 && this.queue(i3 || "fx", []), this.each(function() {
      var e3 = true, t2 = null != i3 && i3 + "queueHooks", n2 = S.timers, r2 = Y.get(this);
      if (t2) r2[t2] && r2[t2].stop && a2(r2[t2]);
      else for (t2 in r2) r2[t2] && r2[t2].stop && it.test(t2) && a2(r2[t2]);
      for (t2 = n2.length; t2--; ) n2[t2].elem !== this || null != i3 && n2[t2].queue !== i3 || (n2[t2].anim.stop(o2), e3 = false, n2.splice(t2, 1));
      !e3 && o2 || S.dequeue(this, i3);
    });
  }, finish: function(a2) {
    return false !== a2 && (a2 = a2 || "fx"), this.each(function() {
      var e2, t2 = Y.get(this), n2 = t2[a2 + "queue"], r2 = t2[a2 + "queueHooks"], i3 = S.timers, o2 = n2 ? n2.length : 0;
      for (t2.finish = true, S.queue(this, a2, []), r2 && r2.stop && r2.stop.call(this, true), e2 = i3.length; e2--; ) i3[e2].elem === this && i3[e2].queue === a2 && (i3[e2].anim.stop(true), i3.splice(e2, 1));
      for (e2 = 0; e2 < o2; e2++) n2[e2] && n2[e2].finish && n2[e2].finish.call(this);
      delete t2.finish;
    });
  } }), S.each(["toggle", "show", "hide"], function(e2, r2) {
    var i3 = S.fn[r2];
    S.fn[r2] = function(e3, t2, n2) {
      return null == e3 || "boolean" == typeof e3 ? i3.apply(this, arguments) : this.animate(st(r2, true), e3, t2, n2);
    };
  }), S.each({ slideDown: st("show"), slideUp: st("hide"), slideToggle: st("toggle"), fadeIn: { opacity: "show" }, fadeOut: { opacity: "hide" }, fadeToggle: { opacity: "toggle" } }, function(e2, r2) {
    S.fn[e2] = function(e3, t2, n2) {
      return this.animate(r2, e3, t2, n2);
    };
  }), S.timers = [], S.fx.tick = function() {
    var e2, t2 = 0, n2 = S.timers;
    for (Ze = Date.now(); t2 < n2.length; t2++) (e2 = n2[t2])() || n2[t2] !== e2 || n2.splice(t2--, 1);
    n2.length || S.fx.stop(), Ze = void 0;
  }, S.fx.timer = function(e2) {
    S.timers.push(e2), S.fx.start();
  }, S.fx.interval = 13, S.fx.start = function() {
    et || (et = true, ot());
  }, S.fx.stop = function() {
    et = null;
  }, S.fx.speeds = { slow: 600, fast: 200, _default: 400 }, S.fn.delay = function(r2, e2) {
    return r2 = S.fx && S.fx.speeds[r2] || r2, e2 = e2 || "fx", this.queue(e2, function(e3, t2) {
      var n2 = C.setTimeout(e3, r2);
      t2.stop = function() {
        C.clearTimeout(n2);
      };
    });
  }, tt = E.createElement("input"), nt = E.createElement("select").appendChild(E.createElement("option")), tt.type = "checkbox", y.checkOn = "" !== tt.value, y.optSelected = nt.selected, (tt = E.createElement("input")).value = "t", tt.type = "radio", y.radioValue = "t" === tt.value;
  var ct, ft = S.expr.attrHandle;
  S.fn.extend({ attr: function(e2, t2) {
    return $2(this, S.attr, e2, t2, 1 < arguments.length);
  }, removeAttr: function(e2) {
    return this.each(function() {
      S.removeAttr(this, e2);
    });
  } }), S.extend({ attr: function(e2, t2, n2) {
    var r2, i3, o2 = e2.nodeType;
    if (3 !== o2 && 8 !== o2 && 2 !== o2) return "undefined" == typeof e2.getAttribute ? S.prop(e2, t2, n2) : (1 === o2 && S.isXMLDoc(e2) || (i3 = S.attrHooks[t2.toLowerCase()] || (S.expr.match.bool.test(t2) ? ct : void 0)), void 0 !== n2 ? null === n2 ? void S.removeAttr(e2, t2) : i3 && "set" in i3 && void 0 !== (r2 = i3.set(e2, n2, t2)) ? r2 : (e2.setAttribute(t2, n2 + ""), n2) : i3 && "get" in i3 && null !== (r2 = i3.get(e2, t2)) ? r2 : null == (r2 = S.find.attr(e2, t2)) ? void 0 : r2);
  }, attrHooks: { type: { set: function(e2, t2) {
    if (!y.radioValue && "radio" === t2 && A(e2, "input")) {
      var n2 = e2.value;
      return e2.setAttribute("type", t2), n2 && (e2.value = n2), t2;
    }
  } } }, removeAttr: function(e2, t2) {
    var n2, r2 = 0, i3 = t2 && t2.match(P);
    if (i3 && 1 === e2.nodeType) while (n2 = i3[r2++]) e2.removeAttribute(n2);
  } }), ct = { set: function(e2, t2, n2) {
    return false === t2 ? S.removeAttr(e2, n2) : e2.setAttribute(n2, n2), n2;
  } }, S.each(S.expr.match.bool.source.match(/\w+/g), function(e2, t2) {
    var a2 = ft[t2] || S.find.attr;
    ft[t2] = function(e3, t3, n2) {
      var r2, i3, o2 = t3.toLowerCase();
      return n2 || (i3 = ft[o2], ft[o2] = r2, r2 = null != a2(e3, t3, n2) ? o2 : null, ft[o2] = i3), r2;
    };
  });
  var pt = /^(?:input|select|textarea|button)$/i, dt = /^(?:a|area)$/i;
  function ht(e2) {
    return (e2.match(P) || []).join(" ");
  }
  function gt(e2) {
    return e2.getAttribute && e2.getAttribute("class") || "";
  }
  function vt(e2) {
    return Array.isArray(e2) ? e2 : "string" == typeof e2 && e2.match(P) || [];
  }
  S.fn.extend({ prop: function(e2, t2) {
    return $2(this, S.prop, e2, t2, 1 < arguments.length);
  }, removeProp: function(e2) {
    return this.each(function() {
      delete this[S.propFix[e2] || e2];
    });
  } }), S.extend({ prop: function(e2, t2, n2) {
    var r2, i3, o2 = e2.nodeType;
    if (3 !== o2 && 8 !== o2 && 2 !== o2) return 1 === o2 && S.isXMLDoc(e2) || (t2 = S.propFix[t2] || t2, i3 = S.propHooks[t2]), void 0 !== n2 ? i3 && "set" in i3 && void 0 !== (r2 = i3.set(e2, n2, t2)) ? r2 : e2[t2] = n2 : i3 && "get" in i3 && null !== (r2 = i3.get(e2, t2)) ? r2 : e2[t2];
  }, propHooks: { tabIndex: { get: function(e2) {
    var t2 = S.find.attr(e2, "tabindex");
    return t2 ? parseInt(t2, 10) : pt.test(e2.nodeName) || dt.test(e2.nodeName) && e2.href ? 0 : -1;
  } } }, propFix: { "for": "htmlFor", "class": "className" } }), y.optSelected || (S.propHooks.selected = { get: function(e2) {
    var t2 = e2.parentNode;
    return t2 && t2.parentNode && t2.parentNode.selectedIndex, null;
  }, set: function(e2) {
    var t2 = e2.parentNode;
    t2 && (t2.selectedIndex, t2.parentNode && t2.parentNode.selectedIndex);
  } }), S.each(["tabIndex", "readOnly", "maxLength", "cellSpacing", "cellPadding", "rowSpan", "colSpan", "useMap", "frameBorder", "contentEditable"], function() {
    S.propFix[this.toLowerCase()] = this;
  }), S.fn.extend({ addClass: function(t2) {
    var e2, n2, r2, i3, o2, a2, s2, u2 = 0;
    if (m(t2)) return this.each(function(e3) {
      S(this).addClass(t2.call(this, e3, gt(this)));
    });
    if ((e2 = vt(t2)).length) {
      while (n2 = this[u2++]) if (i3 = gt(n2), r2 = 1 === n2.nodeType && " " + ht(i3) + " ") {
        a2 = 0;
        while (o2 = e2[a2++]) r2.indexOf(" " + o2 + " ") < 0 && (r2 += o2 + " ");
        i3 !== (s2 = ht(r2)) && n2.setAttribute("class", s2);
      }
    }
    return this;
  }, removeClass: function(t2) {
    var e2, n2, r2, i3, o2, a2, s2, u2 = 0;
    if (m(t2)) return this.each(function(e3) {
      S(this).removeClass(t2.call(this, e3, gt(this)));
    });
    if (!arguments.length) return this.attr("class", "");
    if ((e2 = vt(t2)).length) {
      while (n2 = this[u2++]) if (i3 = gt(n2), r2 = 1 === n2.nodeType && " " + ht(i3) + " ") {
        a2 = 0;
        while (o2 = e2[a2++]) while (-1 < r2.indexOf(" " + o2 + " ")) r2 = r2.replace(" " + o2 + " ", " ");
        i3 !== (s2 = ht(r2)) && n2.setAttribute("class", s2);
      }
    }
    return this;
  }, toggleClass: function(i3, t2) {
    var o2 = typeof i3, a2 = "string" === o2 || Array.isArray(i3);
    return "boolean" == typeof t2 && a2 ? t2 ? this.addClass(i3) : this.removeClass(i3) : m(i3) ? this.each(function(e2) {
      S(this).toggleClass(i3.call(this, e2, gt(this), t2), t2);
    }) : this.each(function() {
      var e2, t3, n2, r2;
      if (a2) {
        t3 = 0, n2 = S(this), r2 = vt(i3);
        while (e2 = r2[t3++]) n2.hasClass(e2) ? n2.removeClass(e2) : n2.addClass(e2);
      } else void 0 !== i3 && "boolean" !== o2 || ((e2 = gt(this)) && Y.set(this, "__className__", e2), this.setAttribute && this.setAttribute("class", e2 || false === i3 ? "" : Y.get(this, "__className__") || ""));
    });
  }, hasClass: function(e2) {
    var t2, n2, r2 = 0;
    t2 = " " + e2 + " ";
    while (n2 = this[r2++]) if (1 === n2.nodeType && -1 < (" " + ht(gt(n2)) + " ").indexOf(t2)) return true;
    return false;
  } });
  var yt = /\r/g;
  S.fn.extend({ val: function(n2) {
    var r2, e2, i3, t2 = this[0];
    return arguments.length ? (i3 = m(n2), this.each(function(e3) {
      var t3;
      1 === this.nodeType && (null == (t3 = i3 ? n2.call(this, e3, S(this).val()) : n2) ? t3 = "" : "number" == typeof t3 ? t3 += "" : Array.isArray(t3) && (t3 = S.map(t3, function(e4) {
        return null == e4 ? "" : e4 + "";
      })), (r2 = S.valHooks[this.type] || S.valHooks[this.nodeName.toLowerCase()]) && "set" in r2 && void 0 !== r2.set(this, t3, "value") || (this.value = t3));
    })) : t2 ? (r2 = S.valHooks[t2.type] || S.valHooks[t2.nodeName.toLowerCase()]) && "get" in r2 && void 0 !== (e2 = r2.get(t2, "value")) ? e2 : "string" == typeof (e2 = t2.value) ? e2.replace(yt, "") : null == e2 ? "" : e2 : void 0;
  } }), S.extend({ valHooks: { option: { get: function(e2) {
    var t2 = S.find.attr(e2, "value");
    return null != t2 ? t2 : ht(S.text(e2));
  } }, select: { get: function(e2) {
    var t2, n2, r2, i3 = e2.options, o2 = e2.selectedIndex, a2 = "select-one" === e2.type, s2 = a2 ? null : [], u2 = a2 ? o2 + 1 : i3.length;
    for (r2 = o2 < 0 ? u2 : a2 ? o2 : 0; r2 < u2; r2++) if (((n2 = i3[r2]).selected || r2 === o2) && !n2.disabled && (!n2.parentNode.disabled || !A(n2.parentNode, "optgroup"))) {
      if (t2 = S(n2).val(), a2) return t2;
      s2.push(t2);
    }
    return s2;
  }, set: function(e2, t2) {
    var n2, r2, i3 = e2.options, o2 = S.makeArray(t2), a2 = i3.length;
    while (a2--) ((r2 = i3[a2]).selected = -1 < S.inArray(S.valHooks.option.get(r2), o2)) && (n2 = true);
    return n2 || (e2.selectedIndex = -1), o2;
  } } } }), S.each(["radio", "checkbox"], function() {
    S.valHooks[this] = { set: function(e2, t2) {
      if (Array.isArray(t2)) return e2.checked = -1 < S.inArray(S(e2).val(), t2);
    } }, y.checkOn || (S.valHooks[this].get = function(e2) {
      return null === e2.getAttribute("value") ? "on" : e2.value;
    });
  }), y.focusin = "onfocusin" in C;
  var mt = /^(?:focusinfocus|focusoutblur)$/, xt = function(e2) {
    e2.stopPropagation();
  };
  S.extend(S.event, { trigger: function(e2, t2, n2, r2) {
    var i3, o2, a2, s2, u2, l2, c2, f2, p2 = [n2 || E], d2 = v.call(e2, "type") ? e2.type : e2, h2 = v.call(e2, "namespace") ? e2.namespace.split(".") : [];
    if (o2 = f2 = a2 = n2 = n2 || E, 3 !== n2.nodeType && 8 !== n2.nodeType && !mt.test(d2 + S.event.triggered) && (-1 < d2.indexOf(".") && (d2 = (h2 = d2.split(".")).shift(), h2.sort()), u2 = d2.indexOf(":") < 0 && "on" + d2, (e2 = e2[S.expando] ? e2 : new S.Event(d2, "object" == typeof e2 && e2)).isTrigger = r2 ? 2 : 3, e2.namespace = h2.join("."), e2.rnamespace = e2.namespace ? new RegExp("(^|\\.)" + h2.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, e2.result = void 0, e2.target || (e2.target = n2), t2 = null == t2 ? [e2] : S.makeArray(t2, [e2]), c2 = S.event.special[d2] || {}, r2 || !c2.trigger || false !== c2.trigger.apply(n2, t2))) {
      if (!r2 && !c2.noBubble && !x(n2)) {
        for (s2 = c2.delegateType || d2, mt.test(s2 + d2) || (o2 = o2.parentNode); o2; o2 = o2.parentNode) p2.push(o2), a2 = o2;
        a2 === (n2.ownerDocument || E) && p2.push(a2.defaultView || a2.parentWindow || C);
      }
      i3 = 0;
      while ((o2 = p2[i3++]) && !e2.isPropagationStopped()) f2 = o2, e2.type = 1 < i3 ? s2 : c2.bindType || d2, (l2 = (Y.get(o2, "events") || /* @__PURE__ */ Object.create(null))[e2.type] && Y.get(o2, "handle")) && l2.apply(o2, t2), (l2 = u2 && o2[u2]) && l2.apply && V(o2) && (e2.result = l2.apply(o2, t2), false === e2.result && e2.preventDefault());
      return e2.type = d2, r2 || e2.isDefaultPrevented() || c2._default && false !== c2._default.apply(p2.pop(), t2) || !V(n2) || u2 && m(n2[d2]) && !x(n2) && ((a2 = n2[u2]) && (n2[u2] = null), S.event.triggered = d2, e2.isPropagationStopped() && f2.addEventListener(d2, xt), n2[d2](), e2.isPropagationStopped() && f2.removeEventListener(d2, xt), S.event.triggered = void 0, a2 && (n2[u2] = a2)), e2.result;
    }
  }, simulate: function(e2, t2, n2) {
    var r2 = S.extend(new S.Event(), n2, { type: e2, isSimulated: true });
    S.event.trigger(r2, null, t2);
  } }), S.fn.extend({ trigger: function(e2, t2) {
    return this.each(function() {
      S.event.trigger(e2, t2, this);
    });
  }, triggerHandler: function(e2, t2) {
    var n2 = this[0];
    if (n2) return S.event.trigger(e2, t2, n2, true);
  } }), y.focusin || S.each({ focus: "focusin", blur: "focusout" }, function(n2, r2) {
    var i3 = function(e2) {
      S.event.simulate(r2, e2.target, S.event.fix(e2));
    };
    S.event.special[r2] = { setup: function() {
      var e2 = this.ownerDocument || this.document || this, t2 = Y.access(e2, r2);
      t2 || e2.addEventListener(n2, i3, true), Y.access(e2, r2, (t2 || 0) + 1);
    }, teardown: function() {
      var e2 = this.ownerDocument || this.document || this, t2 = Y.access(e2, r2) - 1;
      t2 ? Y.access(e2, r2, t2) : (e2.removeEventListener(n2, i3, true), Y.remove(e2, r2));
    } };
  });
  var bt = C.location, wt = { guid: Date.now() }, Tt = /\?/;
  S.parseXML = function(e2) {
    var t2, n2;
    if (!e2 || "string" != typeof e2) return null;
    try {
      t2 = new C.DOMParser().parseFromString(e2, "text/xml");
    } catch (e3) {
    }
    return n2 = t2 && t2.getElementsByTagName("parsererror")[0], t2 && !n2 || S.error("Invalid XML: " + (n2 ? S.map(n2.childNodes, function(e3) {
      return e3.textContent;
    }).join("\n") : e2)), t2;
  };
  var Ct = /\[\]$/, Et = /\r?\n/g, St = /^(?:submit|button|image|reset|file)$/i, kt = /^(?:input|select|textarea|keygen)/i;
  function At(n2, e2, r2, i3) {
    var t2;
    if (Array.isArray(e2)) S.each(e2, function(e3, t3) {
      r2 || Ct.test(n2) ? i3(n2, t3) : At(n2 + "[" + ("object" == typeof t3 && null != t3 ? e3 : "") + "]", t3, r2, i3);
    });
    else if (r2 || "object" !== w(e2)) i3(n2, e2);
    else for (t2 in e2) At(n2 + "[" + t2 + "]", e2[t2], r2, i3);
  }
  S.param = function(e2, t2) {
    var n2, r2 = [], i3 = function(e3, t3) {
      var n3 = m(t3) ? t3() : t3;
      r2[r2.length] = encodeURIComponent(e3) + "=" + encodeURIComponent(null == n3 ? "" : n3);
    };
    if (null == e2) return "";
    if (Array.isArray(e2) || e2.jquery && !S.isPlainObject(e2)) S.each(e2, function() {
      i3(this.name, this.value);
    });
    else for (n2 in e2) At(n2, e2[n2], t2, i3);
    return r2.join("&");
  }, S.fn.extend({ serialize: function() {
    return S.param(this.serializeArray());
  }, serializeArray: function() {
    return this.map(function() {
      var e2 = S.prop(this, "elements");
      return e2 ? S.makeArray(e2) : this;
    }).filter(function() {
      var e2 = this.type;
      return this.name && !S(this).is(":disabled") && kt.test(this.nodeName) && !St.test(e2) && (this.checked || !pe.test(e2));
    }).map(function(e2, t2) {
      var n2 = S(this).val();
      return null == n2 ? null : Array.isArray(n2) ? S.map(n2, function(e3) {
        return { name: t2.name, value: e3.replace(Et, "\r\n") };
      }) : { name: t2.name, value: n2.replace(Et, "\r\n") };
    }).get();
  } });
  var Nt = /%20/g, jt = /#.*$/, Dt = /([?&])_=[^&]*/, qt = /^(.*?):[ \t]*([^\r\n]*)$/gm, Lt = /^(?:GET|HEAD)$/, Ht = /^\/\//, Ot = {}, Pt = {}, Rt = "*/".concat("*"), Mt = E.createElement("a");
  function It(o2) {
    return function(e2, t2) {
      "string" != typeof e2 && (t2 = e2, e2 = "*");
      var n2, r2 = 0, i3 = e2.toLowerCase().match(P) || [];
      if (m(t2)) while (n2 = i3[r2++]) "+" === n2[0] ? (n2 = n2.slice(1) || "*", (o2[n2] = o2[n2] || []).unshift(t2)) : (o2[n2] = o2[n2] || []).push(t2);
    };
  }
  function Wt(t2, i3, o2, a2) {
    var s2 = {}, u2 = t2 === Pt;
    function l2(e2) {
      var r2;
      return s2[e2] = true, S.each(t2[e2] || [], function(e3, t3) {
        var n2 = t3(i3, o2, a2);
        return "string" != typeof n2 || u2 || s2[n2] ? u2 ? !(r2 = n2) : void 0 : (i3.dataTypes.unshift(n2), l2(n2), false);
      }), r2;
    }
    return l2(i3.dataTypes[0]) || !s2["*"] && l2("*");
  }
  function Ft(e2, t2) {
    var n2, r2, i3 = S.ajaxSettings.flatOptions || {};
    for (n2 in t2) void 0 !== t2[n2] && ((i3[n2] ? e2 : r2 || (r2 = {}))[n2] = t2[n2]);
    return r2 && S.extend(true, e2, r2), e2;
  }
  Mt.href = bt.href, S.extend({ active: 0, lastModified: {}, etag: {}, ajaxSettings: { url: bt.href, type: "GET", isLocal: /^(?:about|app|app-storage|.+-extension|file|res|widget):$/.test(bt.protocol), global: true, processData: true, async: true, contentType: "application/x-www-form-urlencoded; charset=UTF-8", accepts: { "*": Rt, text: "text/plain", html: "text/html", xml: "application/xml, text/xml", json: "application/json, text/javascript" }, contents: { xml: /\bxml\b/, html: /\bhtml/, json: /\bjson\b/ }, responseFields: { xml: "responseXML", text: "responseText", json: "responseJSON" }, converters: { "* text": String, "text html": true, "text json": JSON.parse, "text xml": S.parseXML }, flatOptions: { url: true, context: true } }, ajaxSetup: function(e2, t2) {
    return t2 ? Ft(Ft(e2, S.ajaxSettings), t2) : Ft(S.ajaxSettings, e2);
  }, ajaxPrefilter: It(Ot), ajaxTransport: It(Pt), ajax: function(e2, t2) {
    "object" == typeof e2 && (t2 = e2, e2 = void 0), t2 = t2 || {};
    var c2, f2, p2, n2, d2, r2, h2, g2, i3, o2, v2 = S.ajaxSetup({}, t2), y2 = v2.context || v2, m2 = v2.context && (y2.nodeType || y2.jquery) ? S(y2) : S.event, x2 = S.Deferred(), b2 = S.Callbacks("once memory"), w2 = v2.statusCode || {}, a2 = {}, s2 = {}, u2 = "canceled", T2 = { readyState: 0, getResponseHeader: function(e3) {
      var t3;
      if (h2) {
        if (!n2) {
          n2 = {};
          while (t3 = qt.exec(p2)) n2[t3[1].toLowerCase() + " "] = (n2[t3[1].toLowerCase() + " "] || []).concat(t3[2]);
        }
        t3 = n2[e3.toLowerCase() + " "];
      }
      return null == t3 ? null : t3.join(", ");
    }, getAllResponseHeaders: function() {
      return h2 ? p2 : null;
    }, setRequestHeader: function(e3, t3) {
      return null == h2 && (e3 = s2[e3.toLowerCase()] = s2[e3.toLowerCase()] || e3, a2[e3] = t3), this;
    }, overrideMimeType: function(e3) {
      return null == h2 && (v2.mimeType = e3), this;
    }, statusCode: function(e3) {
      var t3;
      if (e3) if (h2) T2.always(e3[T2.status]);
      else for (t3 in e3) w2[t3] = [w2[t3], e3[t3]];
      return this;
    }, abort: function(e3) {
      var t3 = e3 || u2;
      return c2 && c2.abort(t3), l2(0, t3), this;
    } };
    if (x2.promise(T2), v2.url = ((e2 || v2.url || bt.href) + "").replace(Ht, bt.protocol + "//"), v2.type = t2.method || t2.type || v2.method || v2.type, v2.dataTypes = (v2.dataType || "*").toLowerCase().match(P) || [""], null == v2.crossDomain) {
      r2 = E.createElement("a");
      try {
        r2.href = v2.url, r2.href = r2.href, v2.crossDomain = Mt.protocol + "//" + Mt.host != r2.protocol + "//" + r2.host;
      } catch (e3) {
        v2.crossDomain = true;
      }
    }
    if (v2.data && v2.processData && "string" != typeof v2.data && (v2.data = S.param(v2.data, v2.traditional)), Wt(Ot, v2, t2, T2), h2) return T2;
    for (i3 in (g2 = S.event && v2.global) && 0 == S.active++ && S.event.trigger("ajaxStart"), v2.type = v2.type.toUpperCase(), v2.hasContent = !Lt.test(v2.type), f2 = v2.url.replace(jt, ""), v2.hasContent ? v2.data && v2.processData && 0 === (v2.contentType || "").indexOf("application/x-www-form-urlencoded") && (v2.data = v2.data.replace(Nt, "+")) : (o2 = v2.url.slice(f2.length), v2.data && (v2.processData || "string" == typeof v2.data) && (f2 += (Tt.test(f2) ? "&" : "?") + v2.data, delete v2.data), false === v2.cache && (f2 = f2.replace(Dt, "$1"), o2 = (Tt.test(f2) ? "&" : "?") + "_=" + wt.guid++ + o2), v2.url = f2 + o2), v2.ifModified && (S.lastModified[f2] && T2.setRequestHeader("If-Modified-Since", S.lastModified[f2]), S.etag[f2] && T2.setRequestHeader("If-None-Match", S.etag[f2])), (v2.data && v2.hasContent && false !== v2.contentType || t2.contentType) && T2.setRequestHeader("Content-Type", v2.contentType), T2.setRequestHeader("Accept", v2.dataTypes[0] && v2.accepts[v2.dataTypes[0]] ? v2.accepts[v2.dataTypes[0]] + ("*" !== v2.dataTypes[0] ? ", " + Rt + "; q=0.01" : "") : v2.accepts["*"]), v2.headers) T2.setRequestHeader(i3, v2.headers[i3]);
    if (v2.beforeSend && (false === v2.beforeSend.call(y2, T2, v2) || h2)) return T2.abort();
    if (u2 = "abort", b2.add(v2.complete), T2.done(v2.success), T2.fail(v2.error), c2 = Wt(Pt, v2, t2, T2)) {
      if (T2.readyState = 1, g2 && m2.trigger("ajaxSend", [T2, v2]), h2) return T2;
      v2.async && 0 < v2.timeout && (d2 = C.setTimeout(function() {
        T2.abort("timeout");
      }, v2.timeout));
      try {
        h2 = false, c2.send(a2, l2);
      } catch (e3) {
        if (h2) throw e3;
        l2(-1, e3);
      }
    } else l2(-1, "No Transport");
    function l2(e3, t3, n3, r3) {
      var i4, o3, a3, s3, u3, l3 = t3;
      h2 || (h2 = true, d2 && C.clearTimeout(d2), c2 = void 0, p2 = r3 || "", T2.readyState = 0 < e3 ? 4 : 0, i4 = 200 <= e3 && e3 < 300 || 304 === e3, n3 && (s3 = function(e4, t4, n4) {
        var r4, i5, o4, a4, s4 = e4.contents, u4 = e4.dataTypes;
        while ("*" === u4[0]) u4.shift(), void 0 === r4 && (r4 = e4.mimeType || t4.getResponseHeader("Content-Type"));
        if (r4) {
          for (i5 in s4) if (s4[i5] && s4[i5].test(r4)) {
            u4.unshift(i5);
            break;
          }
        }
        if (u4[0] in n4) o4 = u4[0];
        else {
          for (i5 in n4) {
            if (!u4[0] || e4.converters[i5 + " " + u4[0]]) {
              o4 = i5;
              break;
            }
            a4 || (a4 = i5);
          }
          o4 = o4 || a4;
        }
        if (o4) return o4 !== u4[0] && u4.unshift(o4), n4[o4];
      }(v2, T2, n3)), !i4 && -1 < S.inArray("script", v2.dataTypes) && S.inArray("json", v2.dataTypes) < 0 && (v2.converters["text script"] = function() {
      }), s3 = function(e4, t4, n4, r4) {
        var i5, o4, a4, s4, u4, l4 = {}, c3 = e4.dataTypes.slice();
        if (c3[1]) for (a4 in e4.converters) l4[a4.toLowerCase()] = e4.converters[a4];
        o4 = c3.shift();
        while (o4) if (e4.responseFields[o4] && (n4[e4.responseFields[o4]] = t4), !u4 && r4 && e4.dataFilter && (t4 = e4.dataFilter(t4, e4.dataType)), u4 = o4, o4 = c3.shift()) {
          if ("*" === o4) o4 = u4;
          else if ("*" !== u4 && u4 !== o4) {
            if (!(a4 = l4[u4 + " " + o4] || l4["* " + o4])) {
              for (i5 in l4) if ((s4 = i5.split(" "))[1] === o4 && (a4 = l4[u4 + " " + s4[0]] || l4["* " + s4[0]])) {
                true === a4 ? a4 = l4[i5] : true !== l4[i5] && (o4 = s4[0], c3.unshift(s4[1]));
                break;
              }
            }
            if (true !== a4) if (a4 && e4["throws"]) t4 = a4(t4);
            else try {
              t4 = a4(t4);
            } catch (e5) {
              return { state: "parsererror", error: a4 ? e5 : "No conversion from " + u4 + " to " + o4 };
            }
          }
        }
        return { state: "success", data: t4 };
      }(v2, s3, T2, i4), i4 ? (v2.ifModified && ((u3 = T2.getResponseHeader("Last-Modified")) && (S.lastModified[f2] = u3), (u3 = T2.getResponseHeader("etag")) && (S.etag[f2] = u3)), 204 === e3 || "HEAD" === v2.type ? l3 = "nocontent" : 304 === e3 ? l3 = "notmodified" : (l3 = s3.state, o3 = s3.data, i4 = !(a3 = s3.error))) : (a3 = l3, !e3 && l3 || (l3 = "error", e3 < 0 && (e3 = 0))), T2.status = e3, T2.statusText = (t3 || l3) + "", i4 ? x2.resolveWith(y2, [o3, l3, T2]) : x2.rejectWith(y2, [T2, l3, a3]), T2.statusCode(w2), w2 = void 0, g2 && m2.trigger(i4 ? "ajaxSuccess" : "ajaxError", [T2, v2, i4 ? o3 : a3]), b2.fireWith(y2, [T2, l3]), g2 && (m2.trigger("ajaxComplete", [T2, v2]), --S.active || S.event.trigger("ajaxStop")));
    }
    return T2;
  }, getJSON: function(e2, t2, n2) {
    return S.get(e2, t2, n2, "json");
  }, getScript: function(e2, t2) {
    return S.get(e2, void 0, t2, "script");
  } }), S.each(["get", "post"], function(e2, i3) {
    S[i3] = function(e3, t2, n2, r2) {
      return m(t2) && (r2 = r2 || n2, n2 = t2, t2 = void 0), S.ajax(S.extend({ url: e3, type: i3, dataType: r2, data: t2, success: n2 }, S.isPlainObject(e3) && e3));
    };
  }), S.ajaxPrefilter(function(e2) {
    var t2;
    for (t2 in e2.headers) "content-type" === t2.toLowerCase() && (e2.contentType = e2.headers[t2] || "");
  }), S._evalUrl = function(e2, t2, n2) {
    return S.ajax({ url: e2, type: "GET", dataType: "script", cache: true, async: false, global: false, converters: { "text script": function() {
    } }, dataFilter: function(e3) {
      S.globalEval(e3, t2, n2);
    } });
  }, S.fn.extend({ wrapAll: function(e2) {
    var t2;
    return this[0] && (m(e2) && (e2 = e2.call(this[0])), t2 = S(e2, this[0].ownerDocument).eq(0).clone(true), this[0].parentNode && t2.insertBefore(this[0]), t2.map(function() {
      var e3 = this;
      while (e3.firstElementChild) e3 = e3.firstElementChild;
      return e3;
    }).append(this)), this;
  }, wrapInner: function(n2) {
    return m(n2) ? this.each(function(e2) {
      S(this).wrapInner(n2.call(this, e2));
    }) : this.each(function() {
      var e2 = S(this), t2 = e2.contents();
      t2.length ? t2.wrapAll(n2) : e2.append(n2);
    });
  }, wrap: function(t2) {
    var n2 = m(t2);
    return this.each(function(e2) {
      S(this).wrapAll(n2 ? t2.call(this, e2) : t2);
    });
  }, unwrap: function(e2) {
    return this.parent(e2).not("body").each(function() {
      S(this).replaceWith(this.childNodes);
    }), this;
  } }), S.expr.pseudos.hidden = function(e2) {
    return !S.expr.pseudos.visible(e2);
  }, S.expr.pseudos.visible = function(e2) {
    return !!(e2.offsetWidth || e2.offsetHeight || e2.getClientRects().length);
  }, S.ajaxSettings.xhr = function() {
    try {
      return new C.XMLHttpRequest();
    } catch (e2) {
    }
  };
  var Bt = { 0: 200, 1223: 204 }, $t = S.ajaxSettings.xhr();
  y.cors = !!$t && "withCredentials" in $t, y.ajax = $t = !!$t, S.ajaxTransport(function(i3) {
    var o2, a2;
    if (y.cors || $t && !i3.crossDomain) return { send: function(e2, t2) {
      var n2, r2 = i3.xhr();
      if (r2.open(i3.type, i3.url, i3.async, i3.username, i3.password), i3.xhrFields) for (n2 in i3.xhrFields) r2[n2] = i3.xhrFields[n2];
      for (n2 in i3.mimeType && r2.overrideMimeType && r2.overrideMimeType(i3.mimeType), i3.crossDomain || e2["X-Requested-With"] || (e2["X-Requested-With"] = "XMLHttpRequest"), e2) r2.setRequestHeader(n2, e2[n2]);
      o2 = function(e3) {
        return function() {
          o2 && (o2 = a2 = r2.onload = r2.onerror = r2.onabort = r2.ontimeout = r2.onreadystatechange = null, "abort" === e3 ? r2.abort() : "error" === e3 ? "number" != typeof r2.status ? t2(0, "error") : t2(r2.status, r2.statusText) : t2(Bt[r2.status] || r2.status, r2.statusText, "text" !== (r2.responseType || "text") || "string" != typeof r2.responseText ? { binary: r2.response } : { text: r2.responseText }, r2.getAllResponseHeaders()));
        };
      }, r2.onload = o2(), a2 = r2.onerror = r2.ontimeout = o2("error"), void 0 !== r2.onabort ? r2.onabort = a2 : r2.onreadystatechange = function() {
        4 === r2.readyState && C.setTimeout(function() {
          o2 && a2();
        });
      }, o2 = o2("abort");
      try {
        r2.send(i3.hasContent && i3.data || null);
      } catch (e3) {
        if (o2) throw e3;
      }
    }, abort: function() {
      o2 && o2();
    } };
  }), S.ajaxPrefilter(function(e2) {
    e2.crossDomain && (e2.contents.script = false);
  }), S.ajaxSetup({ accepts: { script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript" }, contents: { script: /\b(?:java|ecma)script\b/ }, converters: { "text script": function(e2) {
    return S.globalEval(e2), e2;
  } } }), S.ajaxPrefilter("script", function(e2) {
    void 0 === e2.cache && (e2.cache = false), e2.crossDomain && (e2.type = "GET");
  }), S.ajaxTransport("script", function(n2) {
    var r2, i3;
    if (n2.crossDomain || n2.scriptAttrs) return { send: function(e2, t2) {
      r2 = S("<script>").attr(n2.scriptAttrs || {}).prop({ charset: n2.scriptCharset, src: n2.url }).on("load error", i3 = function(e3) {
        r2.remove(), i3 = null, e3 && t2("error" === e3.type ? 404 : 200, e3.type);
      }), E.head.appendChild(r2[0]);
    }, abort: function() {
      i3 && i3();
    } };
  });
  var _t, zt = [], Ut = /(=)\?(?=&|$)|\?\?/;
  S.ajaxSetup({ jsonp: "callback", jsonpCallback: function() {
    var e2 = zt.pop() || S.expando + "_" + wt.guid++;
    return this[e2] = true, e2;
  } }), S.ajaxPrefilter("json jsonp", function(e2, t2, n2) {
    var r2, i3, o2, a2 = false !== e2.jsonp && (Ut.test(e2.url) ? "url" : "string" == typeof e2.data && 0 === (e2.contentType || "").indexOf("application/x-www-form-urlencoded") && Ut.test(e2.data) && "data");
    if (a2 || "jsonp" === e2.dataTypes[0]) return r2 = e2.jsonpCallback = m(e2.jsonpCallback) ? e2.jsonpCallback() : e2.jsonpCallback, a2 ? e2[a2] = e2[a2].replace(Ut, "$1" + r2) : false !== e2.jsonp && (e2.url += (Tt.test(e2.url) ? "&" : "?") + e2.jsonp + "=" + r2), e2.converters["script json"] = function() {
      return o2 || S.error(r2 + " was not called"), o2[0];
    }, e2.dataTypes[0] = "json", i3 = C[r2], C[r2] = function() {
      o2 = arguments;
    }, n2.always(function() {
      void 0 === i3 ? S(C).removeProp(r2) : C[r2] = i3, e2[r2] && (e2.jsonpCallback = t2.jsonpCallback, zt.push(r2)), o2 && m(i3) && i3(o2[0]), o2 = i3 = void 0;
    }), "script";
  }), y.createHTMLDocument = ((_t = E.implementation.createHTMLDocument("").body).innerHTML = "<form></form><form></form>", 2 === _t.childNodes.length), S.parseHTML = function(e2, t2, n2) {
    return "string" != typeof e2 ? [] : ("boolean" == typeof t2 && (n2 = t2, t2 = false), t2 || (y.createHTMLDocument ? ((r2 = (t2 = E.implementation.createHTMLDocument("")).createElement("base")).href = E.location.href, t2.head.appendChild(r2)) : t2 = E), o2 = !n2 && [], (i3 = N.exec(e2)) ? [t2.createElement(i3[1])] : (i3 = xe([e2], t2, o2), o2 && o2.length && S(o2).remove(), S.merge([], i3.childNodes)));
    var r2, i3, o2;
  }, S.fn.load = function(e2, t2, n2) {
    var r2, i3, o2, a2 = this, s2 = e2.indexOf(" ");
    return -1 < s2 && (r2 = ht(e2.slice(s2)), e2 = e2.slice(0, s2)), m(t2) ? (n2 = t2, t2 = void 0) : t2 && "object" == typeof t2 && (i3 = "POST"), 0 < a2.length && S.ajax({ url: e2, type: i3 || "GET", dataType: "html", data: t2 }).done(function(e3) {
      o2 = arguments, a2.html(r2 ? S("<div>").append(S.parseHTML(e3)).find(r2) : e3);
    }).always(n2 && function(e3, t3) {
      a2.each(function() {
        n2.apply(this, o2 || [e3.responseText, t3, e3]);
      });
    }), this;
  }, S.expr.pseudos.animated = function(t2) {
    return S.grep(S.timers, function(e2) {
      return t2 === e2.elem;
    }).length;
  }, S.offset = { setOffset: function(e2, t2, n2) {
    var r2, i3, o2, a2, s2, u2, l2 = S.css(e2, "position"), c2 = S(e2), f2 = {};
    "static" === l2 && (e2.style.position = "relative"), s2 = c2.offset(), o2 = S.css(e2, "top"), u2 = S.css(e2, "left"), ("absolute" === l2 || "fixed" === l2) && -1 < (o2 + u2).indexOf("auto") ? (a2 = (r2 = c2.position()).top, i3 = r2.left) : (a2 = parseFloat(o2) || 0, i3 = parseFloat(u2) || 0), m(t2) && (t2 = t2.call(e2, n2, S.extend({}, s2))), null != t2.top && (f2.top = t2.top - s2.top + a2), null != t2.left && (f2.left = t2.left - s2.left + i3), "using" in t2 ? t2.using.call(e2, f2) : c2.css(f2);
  } }, S.fn.extend({ offset: function(t2) {
    if (arguments.length) return void 0 === t2 ? this : this.each(function(e3) {
      S.offset.setOffset(this, t2, e3);
    });
    var e2, n2, r2 = this[0];
    return r2 ? r2.getClientRects().length ? (e2 = r2.getBoundingClientRect(), n2 = r2.ownerDocument.defaultView, { top: e2.top + n2.pageYOffset, left: e2.left + n2.pageXOffset }) : { top: 0, left: 0 } : void 0;
  }, position: function() {
    if (this[0]) {
      var e2, t2, n2, r2 = this[0], i3 = { top: 0, left: 0 };
      if ("fixed" === S.css(r2, "position")) t2 = r2.getBoundingClientRect();
      else {
        t2 = this.offset(), n2 = r2.ownerDocument, e2 = r2.offsetParent || n2.documentElement;
        while (e2 && (e2 === n2.body || e2 === n2.documentElement) && "static" === S.css(e2, "position")) e2 = e2.parentNode;
        e2 && e2 !== r2 && 1 === e2.nodeType && ((i3 = S(e2).offset()).top += S.css(e2, "borderTopWidth", true), i3.left += S.css(e2, "borderLeftWidth", true));
      }
      return { top: t2.top - i3.top - S.css(r2, "marginTop", true), left: t2.left - i3.left - S.css(r2, "marginLeft", true) };
    }
  }, offsetParent: function() {
    return this.map(function() {
      var e2 = this.offsetParent;
      while (e2 && "static" === S.css(e2, "position")) e2 = e2.offsetParent;
      return e2 || re;
    });
  } }), S.each({ scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function(t2, i3) {
    var o2 = "pageYOffset" === i3;
    S.fn[t2] = function(e2) {
      return $2(this, function(e3, t3, n2) {
        var r2;
        if (x(e3) ? r2 = e3 : 9 === e3.nodeType && (r2 = e3.defaultView), void 0 === n2) return r2 ? r2[i3] : e3[t3];
        r2 ? r2.scrollTo(o2 ? r2.pageXOffset : n2, o2 ? n2 : r2.pageYOffset) : e3[t3] = n2;
      }, t2, e2, arguments.length);
    };
  }), S.each(["top", "left"], function(e2, n2) {
    S.cssHooks[n2] = Fe(y.pixelPosition, function(e3, t2) {
      if (t2) return t2 = We(e3, n2), Pe.test(t2) ? S(e3).position()[n2] + "px" : t2;
    });
  }), S.each({ Height: "height", Width: "width" }, function(a2, s2) {
    S.each({ padding: "inner" + a2, content: s2, "": "outer" + a2 }, function(r2, o2) {
      S.fn[o2] = function(e2, t2) {
        var n2 = arguments.length && (r2 || "boolean" != typeof e2), i3 = r2 || (true === e2 || true === t2 ? "margin" : "border");
        return $2(this, function(e3, t3, n3) {
          var r3;
          return x(e3) ? 0 === o2.indexOf("outer") ? e3["inner" + a2] : e3.document.documentElement["client" + a2] : 9 === e3.nodeType ? (r3 = e3.documentElement, Math.max(e3.body["scroll" + a2], r3["scroll" + a2], e3.body["offset" + a2], r3["offset" + a2], r3["client" + a2])) : void 0 === n3 ? S.css(e3, t3, i3) : S.style(e3, t3, n3, i3);
        }, s2, n2 ? e2 : void 0, n2);
      };
    });
  }), S.each(["ajaxStart", "ajaxStop", "ajaxComplete", "ajaxError", "ajaxSuccess", "ajaxSend"], function(e2, t2) {
    S.fn[t2] = function(e3) {
      return this.on(t2, e3);
    };
  }), S.fn.extend({ bind: function(e2, t2, n2) {
    return this.on(e2, null, t2, n2);
  }, unbind: function(e2, t2) {
    return this.off(e2, null, t2);
  }, delegate: function(e2, t2, n2, r2) {
    return this.on(t2, e2, n2, r2);
  }, undelegate: function(e2, t2, n2) {
    return 1 === arguments.length ? this.off(e2, "**") : this.off(t2, e2 || "**", n2);
  }, hover: function(e2, t2) {
    return this.mouseenter(e2).mouseleave(t2 || e2);
  } }), S.each("blur focus focusin focusout resize scroll click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup contextmenu".split(" "), function(e2, n2) {
    S.fn[n2] = function(e3, t2) {
      return 0 < arguments.length ? this.on(n2, null, e3, t2) : this.trigger(n2);
    };
  });
  var Xt = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g;
  S.proxy = function(e2, t2) {
    var n2, r2, i3;
    if ("string" == typeof t2 && (n2 = e2[t2], t2 = e2, e2 = n2), m(e2)) return r2 = s.call(arguments, 2), (i3 = function() {
      return e2.apply(t2 || this, r2.concat(s.call(arguments)));
    }).guid = e2.guid = e2.guid || S.guid++, i3;
  }, S.holdReady = function(e2) {
    e2 ? S.readyWait++ : S.ready(true);
  }, S.isArray = Array.isArray, S.parseJSON = JSON.parse, S.nodeName = A, S.isFunction = m, S.isWindow = x, S.camelCase = X, S.type = w, S.now = Date.now, S.isNumeric = function(e2) {
    var t2 = S.type(e2);
    return ("number" === t2 || "string" === t2) && !isNaN(e2 - parseFloat(e2));
  }, S.trim = function(e2) {
    return null == e2 ? "" : (e2 + "").replace(Xt, "");
  }, "function" == typeof define && define.amd && define("jquery", [], function() {
    return S;
  });
  var Vt = C.jQuery, Gt = C.$;
  return S.noConflict = function(e2) {
    return C.$ === S && (C.$ = Gt), e2 && C.jQuery === S && (C.jQuery = Vt), S;
  }, "undefined" == typeof e && (C.jQuery = C.$ = S), S;
});
(function(e, t) {
  "object" == typeof exports && "undefined" != typeof module ? module.exports = t() : "function" == typeof define && define.amd ? define(t) : e.Popper = t();
})(this, function() {
  "use strict";
  function e(e2) {
    return e2 && "[object Function]" === {}.toString.call(e2);
  }
  function t(e2, t2) {
    if (1 !== e2.nodeType) return [];
    var o2 = getComputedStyle(e2, null);
    return t2 ? o2[t2] : o2;
  }
  function o(e2) {
    return "HTML" === e2.nodeName ? e2 : e2.parentNode || e2.host;
  }
  function n(e2) {
    if (!e2) return document.body;
    switch (e2.nodeName) {
      case "HTML":
      case "BODY":
        return e2.ownerDocument.body;
      case "#document":
        return e2.body;
    }
    var i3 = t(e2), r2 = i3.overflow, p2 = i3.overflowX, s2 = i3.overflowY;
    return /(auto|scroll)/.test(r2 + s2 + p2) ? e2 : n(o(e2));
  }
  function r(e2) {
    var o2 = e2 && e2.offsetParent, i3 = o2 && o2.nodeName;
    return i3 && "BODY" !== i3 && "HTML" !== i3 ? -1 !== ["TD", "TABLE"].indexOf(o2.nodeName) && "static" === t(o2, "position") ? r(o2) : o2 : e2 ? e2.ownerDocument.documentElement : document.documentElement;
  }
  function p(e2) {
    var t2 = e2.nodeName;
    return "BODY" !== t2 && ("HTML" === t2 || r(e2.firstElementChild) === e2);
  }
  function s(e2) {
    return null === e2.parentNode ? e2 : s(e2.parentNode);
  }
  function d(e2, t2) {
    if (!e2 || !e2.nodeType || !t2 || !t2.nodeType) return document.documentElement;
    var o2 = e2.compareDocumentPosition(t2) & Node.DOCUMENT_POSITION_FOLLOWING, i3 = o2 ? e2 : t2, n2 = o2 ? t2 : e2, a2 = document.createRange();
    a2.setStart(i3, 0), a2.setEnd(n2, 0);
    var l2 = a2.commonAncestorContainer;
    if (e2 !== l2 && t2 !== l2 || i3.contains(n2)) return p(l2) ? l2 : r(l2);
    var f2 = s(e2);
    return f2.host ? d(f2.host, t2) : d(e2, s(t2).host);
  }
  function a(e2) {
    var t2 = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : "top", o2 = "top" === t2 ? "scrollTop" : "scrollLeft", i3 = e2.nodeName;
    if ("BODY" === i3 || "HTML" === i3) {
      var n2 = e2.ownerDocument.documentElement, r2 = e2.ownerDocument.scrollingElement || n2;
      return r2[o2];
    }
    return e2[o2];
  }
  function l(e2, t2) {
    var o2 = 2 < arguments.length && void 0 !== arguments[2] && arguments[2], i3 = a(t2, "top"), n2 = a(t2, "left"), r2 = o2 ? -1 : 1;
    return e2.top += i3 * r2, e2.bottom += i3 * r2, e2.left += n2 * r2, e2.right += n2 * r2, e2;
  }
  function f(e2, t2) {
    var o2 = "x" === t2 ? "Left" : "Top", i3 = "Left" == o2 ? "Right" : "Bottom";
    return parseFloat(e2["border" + o2 + "Width"], 10) + parseFloat(e2["border" + i3 + "Width"], 10);
  }
  function m(e2, t2, o2, i3) {
    return J(t2["offset" + e2], t2["scroll" + e2], o2["client" + e2], o2["offset" + e2], o2["scroll" + e2], ie() ? o2["offset" + e2] + i3["margin" + ("Height" === e2 ? "Top" : "Left")] + i3["margin" + ("Height" === e2 ? "Bottom" : "Right")] : 0);
  }
  function h() {
    var e2 = document.body, t2 = document.documentElement, o2 = ie() && getComputedStyle(t2);
    return { height: m("Height", e2, t2, o2), width: m("Width", e2, t2, o2) };
  }
  function c(e2) {
    return se({}, e2, { right: e2.left + e2.width, bottom: e2.top + e2.height });
  }
  function g(e2) {
    var o2 = {};
    if (ie()) try {
      o2 = e2.getBoundingClientRect();
      var i3 = a(e2, "top"), n2 = a(e2, "left");
      o2.top += i3, o2.left += n2, o2.bottom += i3, o2.right += n2;
    } catch (e3) {
    }
    else o2 = e2.getBoundingClientRect();
    var r2 = { left: o2.left, top: o2.top, width: o2.right - o2.left, height: o2.bottom - o2.top }, p2 = "HTML" === e2.nodeName ? h() : {}, s2 = p2.width || e2.clientWidth || r2.right - r2.left, d2 = p2.height || e2.clientHeight || r2.bottom - r2.top, l2 = e2.offsetWidth - s2, m2 = e2.offsetHeight - d2;
    if (l2 || m2) {
      var g2 = t(e2);
      l2 -= f(g2, "x"), m2 -= f(g2, "y"), r2.width -= l2, r2.height -= m2;
    }
    return c(r2);
  }
  function u(e2, o2) {
    var i3 = ie(), r2 = "HTML" === o2.nodeName, p2 = g(e2), s2 = g(o2), d2 = n(e2), a2 = t(o2), f2 = parseFloat(a2.borderTopWidth, 10), m2 = parseFloat(a2.borderLeftWidth, 10), h2 = c({ top: p2.top - s2.top - f2, left: p2.left - s2.left - m2, width: p2.width, height: p2.height });
    if (h2.marginTop = 0, h2.marginLeft = 0, !i3 && r2) {
      var u2 = parseFloat(a2.marginTop, 10), b2 = parseFloat(a2.marginLeft, 10);
      h2.top -= f2 - u2, h2.bottom -= f2 - u2, h2.left -= m2 - b2, h2.right -= m2 - b2, h2.marginTop = u2, h2.marginLeft = b2;
    }
    return (i3 ? o2.contains(d2) : o2 === d2 && "BODY" !== d2.nodeName) && (h2 = l(h2, o2)), h2;
  }
  function b(e2) {
    var t2 = e2.ownerDocument.documentElement, o2 = u(e2, t2), i3 = J(t2.clientWidth, window.innerWidth || 0), n2 = J(t2.clientHeight, window.innerHeight || 0), r2 = a(t2), p2 = a(t2, "left"), s2 = { top: r2 - o2.top + o2.marginTop, left: p2 - o2.left + o2.marginLeft, width: i3, height: n2 };
    return c(s2);
  }
  function w(e2) {
    var i3 = e2.nodeName;
    return "BODY" === i3 || "HTML" === i3 ? false : "fixed" === t(e2, "position") || w(o(e2));
  }
  function y(e2, t2, i3, r2) {
    var p2 = { top: 0, left: 0 }, s2 = d(e2, t2);
    if ("viewport" === r2) p2 = b(s2);
    else {
      var a2;
      "scrollParent" === r2 ? (a2 = n(o(t2)), "BODY" === a2.nodeName && (a2 = e2.ownerDocument.documentElement)) : "window" === r2 ? a2 = e2.ownerDocument.documentElement : a2 = r2;
      var l2 = u(a2, s2);
      if ("HTML" === a2.nodeName && !w(s2)) {
        var f2 = h(), m2 = f2.height, c2 = f2.width;
        p2.top += l2.top - l2.marginTop, p2.bottom = m2 + l2.top, p2.left += l2.left - l2.marginLeft, p2.right = c2 + l2.left;
      } else p2 = l2;
    }
    return p2.left += i3, p2.top += i3, p2.right -= i3, p2.bottom -= i3, p2;
  }
  function E(e2) {
    var t2 = e2.width, o2 = e2.height;
    return t2 * o2;
  }
  function v(e2, t2, o2, i3, n2) {
    var r2 = 5 < arguments.length && void 0 !== arguments[5] ? arguments[5] : 0;
    if (-1 === e2.indexOf("auto")) return e2;
    var p2 = y(o2, i3, r2, n2), s2 = { top: { width: p2.width, height: t2.top - p2.top }, right: { width: p2.right - t2.right, height: p2.height }, bottom: { width: p2.width, height: p2.bottom - t2.bottom }, left: { width: t2.left - p2.left, height: p2.height } }, d2 = Object.keys(s2).map(function(e3) {
      return se({ key: e3 }, s2[e3], { area: E(s2[e3]) });
    }).sort(function(e3, t3) {
      return t3.area - e3.area;
    }), a2 = d2.filter(function(e3) {
      var t3 = e3.width, i4 = e3.height;
      return t3 >= o2.clientWidth && i4 >= o2.clientHeight;
    }), l2 = 0 < a2.length ? a2[0].key : d2[0].key, f2 = e2.split("-")[1];
    return l2 + (f2 ? "-" + f2 : "");
  }
  function O(e2, t2, o2) {
    var i3 = d(t2, o2);
    return u(o2, i3);
  }
  function L(e2) {
    var t2 = getComputedStyle(e2), o2 = parseFloat(t2.marginTop) + parseFloat(t2.marginBottom), i3 = parseFloat(t2.marginLeft) + parseFloat(t2.marginRight), n2 = { width: e2.offsetWidth + i3, height: e2.offsetHeight + o2 };
    return n2;
  }
  function x(e2) {
    var t2 = { left: "right", right: "left", bottom: "top", top: "bottom" };
    return e2.replace(/left|right|bottom|top/g, function(e3) {
      return t2[e3];
    });
  }
  function S(e2, t2, o2) {
    o2 = o2.split("-")[0];
    var i3 = L(e2), n2 = { width: i3.width, height: i3.height }, r2 = -1 !== ["right", "left"].indexOf(o2), p2 = r2 ? "top" : "left", s2 = r2 ? "left" : "top", d2 = r2 ? "height" : "width", a2 = r2 ? "width" : "height";
    return n2[p2] = t2[p2] + t2[d2] / 2 - i3[d2] / 2, n2[s2] = o2 === s2 ? t2[s2] - i3[a2] : t2[x(s2)], n2;
  }
  function T(e2, t2) {
    return Array.prototype.find ? e2.find(t2) : e2.filter(t2)[0];
  }
  function D(e2, t2, o2) {
    if (Array.prototype.findIndex) return e2.findIndex(function(e3) {
      return e3[t2] === o2;
    });
    var i3 = T(e2, function(e3) {
      return e3[t2] === o2;
    });
    return e2.indexOf(i3);
  }
  function C(t2, o2, i3) {
    var n2 = void 0 === i3 ? t2 : t2.slice(0, D(t2, "name", i3));
    return n2.forEach(function(t3) {
      t3["function"] && console.warn("`modifier.function` is deprecated, use `modifier.fn`!");
      var i4 = t3["function"] || t3.fn;
      t3.enabled && e(i4) && (o2.offsets.popper = c(o2.offsets.popper), o2.offsets.reference = c(o2.offsets.reference), o2 = i4(o2, t3));
    }), o2;
  }
  function N() {
    if (!this.state.isDestroyed) {
      var e2 = { instance: this, styles: {}, arrowStyles: {}, attributes: {}, flipped: false, offsets: {} };
      e2.offsets.reference = O(this.state, this.popper, this.reference), e2.placement = v(this.options.placement, e2.offsets.reference, this.popper, this.reference, this.options.modifiers.flip.boundariesElement, this.options.modifiers.flip.padding), e2.originalPlacement = e2.placement, e2.offsets.popper = S(this.popper, e2.offsets.reference, e2.placement), e2.offsets.popper.position = "absolute", e2 = C(this.modifiers, e2), this.state.isCreated ? this.options.onUpdate(e2) : (this.state.isCreated = true, this.options.onCreate(e2));
    }
  }
  function k(e2, t2) {
    return e2.some(function(e3) {
      var o2 = e3.name, i3 = e3.enabled;
      return i3 && o2 === t2;
    });
  }
  function W(e2) {
    for (var t2 = [false, "ms", "Webkit", "Moz", "O"], o2 = e2.charAt(0).toUpperCase() + e2.slice(1), n2 = 0; n2 < t2.length - 1; n2++) {
      var i3 = t2[n2], r2 = i3 ? "" + i3 + o2 : e2;
      if ("undefined" != typeof document.body.style[r2]) return r2;
    }
    return null;
  }
  function P() {
    return this.state.isDestroyed = true, k(this.modifiers, "applyStyle") && (this.popper.removeAttribute("x-placement"), this.popper.style.left = "", this.popper.style.position = "", this.popper.style.top = "", this.popper.style[W("transform")] = ""), this.disableEventListeners(), this.options.removeOnDestroy && this.popper.parentNode.removeChild(this.popper), this;
  }
  function B(e2) {
    var t2 = e2.ownerDocument;
    return t2 ? t2.defaultView : window;
  }
  function H(e2, t2, o2, i3) {
    var r2 = "BODY" === e2.nodeName, p2 = r2 ? e2.ownerDocument.defaultView : e2;
    p2.addEventListener(t2, o2, { passive: true }), r2 || H(n(p2.parentNode), t2, o2, i3), i3.push(p2);
  }
  function A(e2, t2, o2, i3) {
    o2.updateBound = i3, B(e2).addEventListener("resize", o2.updateBound, { passive: true });
    var r2 = n(e2);
    return H(r2, "scroll", o2.updateBound, o2.scrollParents), o2.scrollElement = r2, o2.eventsEnabled = true, o2;
  }
  function I() {
    this.state.eventsEnabled || (this.state = A(this.reference, this.options, this.state, this.scheduleUpdate));
  }
  function M(e2, t2) {
    return B(e2).removeEventListener("resize", t2.updateBound), t2.scrollParents.forEach(function(e3) {
      e3.removeEventListener("scroll", t2.updateBound);
    }), t2.updateBound = null, t2.scrollParents = [], t2.scrollElement = null, t2.eventsEnabled = false, t2;
  }
  function R() {
    this.state.eventsEnabled && (cancelAnimationFrame(this.scheduleUpdate), this.state = M(this.reference, this.state));
  }
  function U(e2) {
    return "" !== e2 && !isNaN(parseFloat(e2)) && isFinite(e2);
  }
  function Y(e2, t2) {
    Object.keys(t2).forEach(function(o2) {
      var i3 = "";
      -1 !== ["width", "height", "top", "right", "bottom", "left"].indexOf(o2) && U(t2[o2]) && (i3 = "px"), e2.style[o2] = t2[o2] + i3;
    });
  }
  function j(e2, t2) {
    Object.keys(t2).forEach(function(o2) {
      var i3 = t2[o2];
      false === i3 ? e2.removeAttribute(o2) : e2.setAttribute(o2, t2[o2]);
    });
  }
  function F(e2, t2, o2) {
    var i3 = T(e2, function(e3) {
      var o3 = e3.name;
      return o3 === t2;
    }), n2 = !!i3 && e2.some(function(e3) {
      return e3.name === o2 && e3.enabled && e3.order < i3.order;
    });
    if (!n2) {
      var r2 = "`" + t2 + "`";
      console.warn("`" + o2 + "` modifier is required by " + r2 + " modifier in order to work, be sure to include it before " + r2 + "!");
    }
    return n2;
  }
  function K(e2) {
    return "end" === e2 ? "start" : "start" === e2 ? "end" : e2;
  }
  function q(e2) {
    var t2 = 1 < arguments.length && void 0 !== arguments[1] && arguments[1], o2 = ae.indexOf(e2), i3 = ae.slice(o2 + 1).concat(ae.slice(0, o2));
    return t2 ? i3.reverse() : i3;
  }
  function V(e2, t2, o2, i3) {
    var n2 = e2.match(/((?:\-|\+)?\d*\.?\d*)(.*)/), r2 = +n2[1], p2 = n2[2];
    if (!r2) return e2;
    if (0 === p2.indexOf("%")) {
      var s2;
      switch (p2) {
        case "%p":
          s2 = o2;
          break;
        case "%":
        case "%r":
        default:
          s2 = i3;
      }
      var d2 = c(s2);
      return d2[t2] / 100 * r2;
    }
    if ("vh" === p2 || "vw" === p2) {
      var a2;
      return a2 = "vh" === p2 ? J(document.documentElement.clientHeight, window.innerHeight || 0) : J(document.documentElement.clientWidth, window.innerWidth || 0), a2 / 100 * r2;
    }
    return r2;
  }
  function z(e2, t2, o2, i3) {
    var n2 = [0, 0], r2 = -1 !== ["right", "left"].indexOf(i3), p2 = e2.split(/(\+|\-)/).map(function(e3) {
      return e3.trim();
    }), s2 = p2.indexOf(T(p2, function(e3) {
      return -1 !== e3.search(/,|\s/);
    }));
    p2[s2] && -1 === p2[s2].indexOf(",") && console.warn("Offsets separated by white space(s) are deprecated, use a comma (,) instead.");
    var d2 = /\s*,\s*|\s+/, a2 = -1 === s2 ? [p2] : [p2.slice(0, s2).concat([p2[s2].split(d2)[0]]), [p2[s2].split(d2)[1]].concat(p2.slice(s2 + 1))];
    return a2 = a2.map(function(e3, i4) {
      var n3 = (1 === i4 ? !r2 : r2) ? "height" : "width", p3 = false;
      return e3.reduce(function(e4, t3) {
        return "" === e4[e4.length - 1] && -1 !== ["+", "-"].indexOf(t3) ? (e4[e4.length - 1] = t3, p3 = true, e4) : p3 ? (e4[e4.length - 1] += t3, p3 = false, e4) : e4.concat(t3);
      }, []).map(function(e4) {
        return V(e4, n3, t2, o2);
      });
    }), a2.forEach(function(e3, t3) {
      e3.forEach(function(o3, i4) {
        U(o3) && (n2[t3] += o3 * ("-" === e3[i4 - 1] ? -1 : 1));
      });
    }), n2;
  }
  function G(e2, t2) {
    var o2, i3 = t2.offset, n2 = e2.placement, r2 = e2.offsets, p2 = r2.popper, s2 = r2.reference, d2 = n2.split("-")[0];
    return o2 = U(+i3) ? [+i3, 0] : z(i3, p2, s2, d2), "left" === d2 ? (p2.top += o2[0], p2.left -= o2[1]) : "right" === d2 ? (p2.top += o2[0], p2.left += o2[1]) : "top" === d2 ? (p2.left += o2[0], p2.top -= o2[1]) : "bottom" === d2 && (p2.left += o2[0], p2.top += o2[1]), e2.popper = p2, e2;
  }
  for (var _ = Math.min, X = Math.floor, J = Math.max, Q = "undefined" != typeof window && "undefined" != typeof document, Z = ["Edge", "Trident", "Firefox"], $2 = 0, ee = 0; ee < Z.length; ee += 1) if (Q && 0 <= navigator.userAgent.indexOf(Z[ee])) {
    $2 = 1;
    break;
  }
  var i2, te = Q && window.Promise, oe = te ? function(e2) {
    var t2 = false;
    return function() {
      t2 || (t2 = true, window.Promise.resolve().then(function() {
        t2 = false, e2();
      }));
    };
  } : function(e2) {
    var t2 = false;
    return function() {
      t2 || (t2 = true, setTimeout(function() {
        t2 = false, e2();
      }, $2));
    };
  }, ie = function() {
    return void 0 == i2 && (i2 = -1 !== navigator.appVersion.indexOf("MSIE 10")), i2;
  }, ne = function(e2, t2) {
    if (!(e2 instanceof t2)) throw new TypeError("Cannot call a class as a function");
  }, re = /* @__PURE__ */ function() {
    function e2(e3, t2) {
      for (var o2, n2 = 0; n2 < t2.length; n2++) o2 = t2[n2], o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(e3, o2.key, o2);
    }
    return function(t2, o2, i3) {
      return o2 && e2(t2.prototype, o2), i3 && e2(t2, i3), t2;
    };
  }(), pe = function(e2, t2, o2) {
    return t2 in e2 ? Object.defineProperty(e2, t2, { value: o2, enumerable: true, configurable: true, writable: true }) : e2[t2] = o2, e2;
  }, se = Object.assign || function(e2) {
    for (var t2, o2 = 1; o2 < arguments.length; o2++) for (var i3 in t2 = arguments[o2], t2) Object.prototype.hasOwnProperty.call(t2, i3) && (e2[i3] = t2[i3]);
    return e2;
  }, de = ["auto-start", "auto", "auto-end", "top-start", "top", "top-end", "right-start", "right", "right-end", "bottom-end", "bottom", "bottom-start", "left-end", "left", "left-start"], ae = de.slice(3), le = { FLIP: "flip", CLOCKWISE: "clockwise", COUNTERCLOCKWISE: "counterclockwise" }, fe = function() {
    function t2(o2, i3) {
      var n2 = this, r2 = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : {};
      ne(this, t2), this.scheduleUpdate = function() {
        return requestAnimationFrame(n2.update);
      }, this.update = oe(this.update.bind(this)), this.options = se({}, t2.Defaults, r2), this.state = { isDestroyed: false, isCreated: false, scrollParents: [] }, this.reference = o2 && o2.jquery ? o2[0] : o2, this.popper = i3 && i3.jquery ? i3[0] : i3, this.options.modifiers = {}, Object.keys(se({}, t2.Defaults.modifiers, r2.modifiers)).forEach(function(e2) {
        n2.options.modifiers[e2] = se({}, t2.Defaults.modifiers[e2] || {}, r2.modifiers ? r2.modifiers[e2] : {});
      }), this.modifiers = Object.keys(this.options.modifiers).map(function(e2) {
        return se({ name: e2 }, n2.options.modifiers[e2]);
      }).sort(function(e2, t3) {
        return e2.order - t3.order;
      }), this.modifiers.forEach(function(t3) {
        t3.enabled && e(t3.onLoad) && t3.onLoad(n2.reference, n2.popper, n2.options, t3, n2.state);
      }), this.update();
      var p2 = this.options.eventsEnabled;
      p2 && this.enableEventListeners(), this.state.eventsEnabled = p2;
    }
    return re(t2, [{ key: "update", value: function() {
      return N.call(this);
    } }, { key: "destroy", value: function() {
      return P.call(this);
    } }, { key: "enableEventListeners", value: function() {
      return I.call(this);
    } }, { key: "disableEventListeners", value: function() {
      return R.call(this);
    } }]), t2;
  }();
  return fe.Utils = ("undefined" == typeof window ? global : window).PopperUtils, fe.placements = de, fe.Defaults = { placement: "bottom", eventsEnabled: true, removeOnDestroy: false, onCreate: function() {
  }, onUpdate: function() {
  }, modifiers: { shift: { order: 100, enabled: true, fn: function(e2) {
    var t2 = e2.placement, o2 = t2.split("-")[0], i3 = t2.split("-")[1];
    if (i3) {
      var n2 = e2.offsets, r2 = n2.reference, p2 = n2.popper, s2 = -1 !== ["bottom", "top"].indexOf(o2), d2 = s2 ? "left" : "top", a2 = s2 ? "width" : "height", l2 = { start: pe({}, d2, r2[d2]), end: pe({}, d2, r2[d2] + r2[a2] - p2[a2]) };
      e2.offsets.popper = se({}, p2, l2[i3]);
    }
    return e2;
  } }, offset: { order: 200, enabled: true, fn: G, offset: 0 }, preventOverflow: { order: 300, enabled: true, fn: function(e2, t2) {
    var o2 = t2.boundariesElement || r(e2.instance.popper);
    e2.instance.reference === o2 && (o2 = r(o2));
    var i3 = y(e2.instance.popper, e2.instance.reference, t2.padding, o2);
    t2.boundaries = i3;
    var n2 = t2.priority, p2 = e2.offsets.popper, s2 = { primary: function(e3) {
      var o3 = p2[e3];
      return p2[e3] < i3[e3] && !t2.escapeWithReference && (o3 = J(p2[e3], i3[e3])), pe({}, e3, o3);
    }, secondary: function(e3) {
      var o3 = "right" === e3 ? "left" : "top", n3 = p2[o3];
      return p2[e3] > i3[e3] && !t2.escapeWithReference && (n3 = _(p2[o3], i3[e3] - ("right" === e3 ? p2.width : p2.height))), pe({}, o3, n3);
    } };
    return n2.forEach(function(e3) {
      var t3 = -1 === ["left", "top"].indexOf(e3) ? "secondary" : "primary";
      p2 = se({}, p2, s2[t3](e3));
    }), e2.offsets.popper = p2, e2;
  }, priority: ["left", "right", "top", "bottom"], padding: 5, boundariesElement: "scrollParent" }, keepTogether: { order: 400, enabled: true, fn: function(e2) {
    var t2 = e2.offsets, o2 = t2.popper, i3 = t2.reference, n2 = e2.placement.split("-")[0], r2 = X, p2 = -1 !== ["top", "bottom"].indexOf(n2), s2 = p2 ? "right" : "bottom", d2 = p2 ? "left" : "top", a2 = p2 ? "width" : "height";
    return o2[s2] < r2(i3[d2]) && (e2.offsets.popper[d2] = r2(i3[d2]) - o2[a2]), o2[d2] > r2(i3[s2]) && (e2.offsets.popper[d2] = r2(i3[s2])), e2;
  } }, arrow: { order: 500, enabled: true, fn: function(e2, o2) {
    var i3;
    if (!F(e2.instance.modifiers, "arrow", "keepTogether")) return e2;
    var n2 = o2.element;
    if ("string" == typeof n2) {
      if (n2 = e2.instance.popper.querySelector(n2), !n2) return e2;
    } else if (!e2.instance.popper.contains(n2)) return console.warn("WARNING: `arrow.element` must be child of its popper element!"), e2;
    var r2 = e2.placement.split("-")[0], p2 = e2.offsets, s2 = p2.popper, d2 = p2.reference, a2 = -1 !== ["left", "right"].indexOf(r2), l2 = a2 ? "height" : "width", f2 = a2 ? "Top" : "Left", m2 = f2.toLowerCase(), h2 = a2 ? "left" : "top", g2 = a2 ? "bottom" : "right", u2 = L(n2)[l2];
    d2[g2] - u2 < s2[m2] && (e2.offsets.popper[m2] -= s2[m2] - (d2[g2] - u2)), d2[m2] + u2 > s2[g2] && (e2.offsets.popper[m2] += d2[m2] + u2 - s2[g2]), e2.offsets.popper = c(e2.offsets.popper);
    var b2 = d2[m2] + d2[l2] / 2 - u2 / 2, w2 = t(e2.instance.popper), y2 = parseFloat(w2["margin" + f2], 10), E2 = parseFloat(w2["border" + f2 + "Width"], 10), v2 = b2 - e2.offsets.popper[m2] - y2 - E2;
    return v2 = J(_(s2[l2] - u2, v2), 0), e2.arrowElement = n2, e2.offsets.arrow = (i3 = {}, pe(i3, m2, Math.round(v2)), pe(i3, h2, ""), i3), e2;
  }, element: "[x-arrow]" }, flip: { order: 600, enabled: true, fn: function(e2, t2) {
    if (k(e2.instance.modifiers, "inner")) return e2;
    if (e2.flipped && e2.placement === e2.originalPlacement) return e2;
    var o2 = y(e2.instance.popper, e2.instance.reference, t2.padding, t2.boundariesElement), i3 = e2.placement.split("-")[0], n2 = x(i3), r2 = e2.placement.split("-")[1] || "", p2 = [];
    switch (t2.behavior) {
      case le.FLIP:
        p2 = [i3, n2];
        break;
      case le.CLOCKWISE:
        p2 = q(i3);
        break;
      case le.COUNTERCLOCKWISE:
        p2 = q(i3, true);
        break;
      default:
        p2 = t2.behavior;
    }
    return p2.forEach(function(s2, d2) {
      if (i3 !== s2 || p2.length === d2 + 1) return e2;
      i3 = e2.placement.split("-")[0], n2 = x(i3);
      var a2 = e2.offsets.popper, l2 = e2.offsets.reference, f2 = X, m2 = "left" === i3 && f2(a2.right) > f2(l2.left) || "right" === i3 && f2(a2.left) < f2(l2.right) || "top" === i3 && f2(a2.bottom) > f2(l2.top) || "bottom" === i3 && f2(a2.top) < f2(l2.bottom), h2 = f2(a2.left) < f2(o2.left), c2 = f2(a2.right) > f2(o2.right), g2 = f2(a2.top) < f2(o2.top), u2 = f2(a2.bottom) > f2(o2.bottom), b2 = "left" === i3 && h2 || "right" === i3 && c2 || "top" === i3 && g2 || "bottom" === i3 && u2, w2 = -1 !== ["top", "bottom"].indexOf(i3), y2 = !!t2.flipVariations && (w2 && "start" === r2 && h2 || w2 && "end" === r2 && c2 || !w2 && "start" === r2 && g2 || !w2 && "end" === r2 && u2);
      (m2 || b2 || y2) && (e2.flipped = true, (m2 || b2) && (i3 = p2[d2 + 1]), y2 && (r2 = K(r2)), e2.placement = i3 + (r2 ? "-" + r2 : ""), e2.offsets.popper = se({}, e2.offsets.popper, S(e2.instance.popper, e2.offsets.reference, e2.placement)), e2 = C(e2.instance.modifiers, e2, "flip"));
    }), e2;
  }, behavior: "flip", padding: 5, boundariesElement: "viewport" }, inner: { order: 700, enabled: false, fn: function(e2) {
    var t2 = e2.placement, o2 = t2.split("-")[0], i3 = e2.offsets, n2 = i3.popper, r2 = i3.reference, p2 = -1 !== ["left", "right"].indexOf(o2), s2 = -1 === ["top", "left"].indexOf(o2);
    return n2[p2 ? "left" : "top"] = r2[o2] - (s2 ? n2[p2 ? "width" : "height"] : 0), e2.placement = x(t2), e2.offsets.popper = c(n2), e2;
  } }, hide: { order: 800, enabled: true, fn: function(e2) {
    if (!F(e2.instance.modifiers, "hide", "preventOverflow")) return e2;
    var t2 = e2.offsets.reference, o2 = T(e2.instance.modifiers, function(e3) {
      return "preventOverflow" === e3.name;
    }).boundaries;
    if (t2.bottom < o2.top || t2.left > o2.right || t2.top > o2.bottom || t2.right < o2.left) {
      if (true === e2.hide) return e2;
      e2.hide = true, e2.attributes["x-out-of-boundaries"] = "";
    } else {
      if (false === e2.hide) return e2;
      e2.hide = false, e2.attributes["x-out-of-boundaries"] = false;
    }
    return e2;
  } }, computeStyle: { order: 850, enabled: true, fn: function(e2, t2) {
    var o2 = t2.x, i3 = t2.y, n2 = e2.offsets.popper, p2 = T(e2.instance.modifiers, function(e3) {
      return "applyStyle" === e3.name;
    }).gpuAcceleration;
    void 0 !== p2 && console.warn("WARNING: `gpuAcceleration` option moved to `computeStyle` modifier and will not be supported in future versions of Popper.js!");
    var s2, d2, a2 = void 0 === p2 ? t2.gpuAcceleration : p2, l2 = r(e2.instance.popper), f2 = g(l2), m2 = { position: n2.position }, h2 = { left: X(n2.left), top: X(n2.top), bottom: X(n2.bottom), right: X(n2.right) }, c2 = "bottom" === o2 ? "top" : "bottom", u2 = "right" === i3 ? "left" : "right", b2 = W("transform");
    if (d2 = "bottom" == c2 ? -f2.height + h2.bottom : h2.top, s2 = "right" == u2 ? -f2.width + h2.right : h2.left, a2 && b2) m2[b2] = "translate3d(" + s2 + "px, " + d2 + "px, 0)", m2[c2] = 0, m2[u2] = 0, m2.willChange = "transform";
    else {
      var w2 = "bottom" == c2 ? -1 : 1, y2 = "right" == u2 ? -1 : 1;
      m2[c2] = d2 * w2, m2[u2] = s2 * y2, m2.willChange = c2 + ", " + u2;
    }
    var E2 = { "x-placement": e2.placement };
    return e2.attributes = se({}, E2, e2.attributes), e2.styles = se({}, m2, e2.styles), e2.arrowStyles = se({}, e2.offsets.arrow, e2.arrowStyles), e2;
  }, gpuAcceleration: true, x: "bottom", y: "right" }, applyStyle: { order: 900, enabled: true, fn: function(e2) {
    return Y(e2.instance.popper, e2.styles), j(e2.instance.popper, e2.attributes), e2.arrowElement && Object.keys(e2.arrowStyles).length && Y(e2.arrowElement, e2.arrowStyles), e2;
  }, onLoad: function(e2, t2, o2, i3, n2) {
    var r2 = O(n2, t2, e2), p2 = v(o2.placement, r2, t2, e2, o2.modifiers.flip.boundariesElement, o2.modifiers.flip.padding);
    return t2.setAttribute("x-placement", p2), Y(t2, { position: "absolute" }), o2;
  }, gpuAcceleration: void 0 } } }, fe;
});
/*!
  * Bootstrap v4.5.0 (https://getbootstrap.com/)
  * Copyright 2011-2020 The Bootstrap Authors (https://github.com/twbs/bootstrap/graphs/contributors)
  * Licensed under MIT (https://github.com/twbs/bootstrap/blob/master/LICENSE)
  */
!function(t, e) {
  "object" == typeof exports && "undefined" != typeof module ? e(exports, require("jquery"), require("popper.js")) : "function" == typeof define && define.amd ? define(["exports", "jquery", "popper.js"], e) : e((t = t || self).bootstrap = {}, t.jQuery, t.Popper);
}(this, function(t, e, n) {
  "use strict";
  function i2(t2, e2) {
    for (var n2 = 0; n2 < e2.length; n2++) {
      var i3 = e2[n2];
      i3.enumerable = i3.enumerable || false, i3.configurable = true, "value" in i3 && (i3.writable = true), Object.defineProperty(t2, i3.key, i3);
    }
  }
  function o(t2, e2, n2) {
    return e2 && i2(t2.prototype, e2), n2 && i2(t2, n2), t2;
  }
  function s(t2, e2, n2) {
    return e2 in t2 ? Object.defineProperty(t2, e2, { value: n2, enumerable: true, configurable: true, writable: true }) : t2[e2] = n2, t2;
  }
  function r(t2, e2) {
    var n2 = Object.keys(t2);
    if (Object.getOwnPropertySymbols) {
      var i3 = Object.getOwnPropertySymbols(t2);
      e2 && (i3 = i3.filter(function(e3) {
        return Object.getOwnPropertyDescriptor(t2, e3).enumerable;
      })), n2.push.apply(n2, i3);
    }
    return n2;
  }
  function a(t2) {
    for (var e2 = 1; e2 < arguments.length; e2++) {
      var n2 = null != arguments[e2] ? arguments[e2] : {};
      e2 % 2 ? r(Object(n2), true).forEach(function(e3) {
        s(t2, e3, n2[e3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(t2, Object.getOwnPropertyDescriptors(n2)) : r(Object(n2)).forEach(function(e3) {
        Object.defineProperty(t2, e3, Object.getOwnPropertyDescriptor(n2, e3));
      });
    }
    return t2;
  }
  e = e && Object.prototype.hasOwnProperty.call(e, "default") ? e.default : e, n = n && Object.prototype.hasOwnProperty.call(n, "default") ? n.default : n;
  function l(t2) {
    var n2 = this, i3 = false;
    return e(this).one(c.TRANSITION_END, function() {
      i3 = true;
    }), setTimeout(function() {
      i3 || c.triggerTransitionEnd(n2);
    }, t2), this;
  }
  var c = { TRANSITION_END: "bsTransitionEnd", getUID: function(t2) {
    do {
      t2 += ~~(1e6 * Math.random());
    } while (document.getElementById(t2));
    return t2;
  }, getSelectorFromElement: function(t2) {
    var e2 = t2.getAttribute("data-target");
    if (!e2 || "#" === e2) {
      var n2 = t2.getAttribute("href");
      e2 = n2 && "#" !== n2 ? n2.trim() : "";
    }
    try {
      return document.querySelector(e2) ? e2 : null;
    } catch (t3) {
      return null;
    }
  }, getTransitionDurationFromElement: function(t2) {
    if (!t2) return 0;
    var n2 = e(t2).css("transition-duration"), i3 = e(t2).css("transition-delay"), o2 = parseFloat(n2), s2 = parseFloat(i3);
    return o2 || s2 ? (n2 = n2.split(",")[0], i3 = i3.split(",")[0], 1e3 * (parseFloat(n2) + parseFloat(i3))) : 0;
  }, reflow: function(t2) {
    return t2.offsetHeight;
  }, triggerTransitionEnd: function(t2) {
    e(t2).trigger("transitionend");
  }, supportsTransitionEnd: function() {
    return Boolean("transitionend");
  }, isElement: function(t2) {
    return (t2[0] || t2).nodeType;
  }, typeCheckConfig: function(t2, e2, n2) {
    for (var i3 in n2) if (Object.prototype.hasOwnProperty.call(n2, i3)) {
      var o2 = n2[i3], s2 = e2[i3], r2 = s2 && c.isElement(s2) ? "element" : null === (a2 = s2) || "undefined" == typeof a2 ? "" + a2 : {}.toString.call(a2).match(/\s([a-z]+)/i)[1].toLowerCase();
      if (!new RegExp(o2).test(r2)) throw new Error(t2.toUpperCase() + ': Option "' + i3 + '" provided type "' + r2 + '" but expected type "' + o2 + '".');
    }
    var a2;
  }, findShadowRoot: function(t2) {
    if (!document.documentElement.attachShadow) return null;
    if ("function" == typeof t2.getRootNode) {
      var e2 = t2.getRootNode();
      return e2 instanceof ShadowRoot ? e2 : null;
    }
    return t2 instanceof ShadowRoot ? t2 : t2.parentNode ? c.findShadowRoot(t2.parentNode) : null;
  }, jQueryDetection: function() {
    if ("undefined" == typeof e) throw new TypeError("Bootstrap's JavaScript requires jQuery. jQuery must be included before Bootstrap's JavaScript.");
    var t2 = e.fn.jquery.split(" ")[0].split(".");
    if (t2[0] < 2 && t2[1] < 9 || 1 === t2[0] && 9 === t2[1] && t2[2] < 1 || t2[0] >= 4) throw new Error("Bootstrap's JavaScript requires at least jQuery v1.9.1 but less than v4.0.0");
  } };
  c.jQueryDetection(), e.fn.emulateTransitionEnd = l, e.event.special[c.TRANSITION_END] = { bindType: "transitionend", delegateType: "transitionend", handle: function(t2) {
    if (e(t2.target).is(this)) return t2.handleObj.handler.apply(this, arguments);
  } };
  var h = "alert", u = e.fn[h], d = function() {
    function t2(t3) {
      this._element = t3;
    }
    var n2 = t2.prototype;
    return n2.close = function(t3) {
      var e2 = this._element;
      t3 && (e2 = this._getRootElement(t3)), this._triggerCloseEvent(e2).isDefaultPrevented() || this._removeElement(e2);
    }, n2.dispose = function() {
      e.removeData(this._element, "bs.alert"), this._element = null;
    }, n2._getRootElement = function(t3) {
      var n3 = c.getSelectorFromElement(t3), i3 = false;
      return n3 && (i3 = document.querySelector(n3)), i3 || (i3 = e(t3).closest(".alert")[0]), i3;
    }, n2._triggerCloseEvent = function(t3) {
      var n3 = e.Event("close.bs.alert");
      return e(t3).trigger(n3), n3;
    }, n2._removeElement = function(t3) {
      var n3 = this;
      if (e(t3).removeClass("show"), e(t3).hasClass("fade")) {
        var i3 = c.getTransitionDurationFromElement(t3);
        e(t3).one(c.TRANSITION_END, function(e2) {
          return n3._destroyElement(t3, e2);
        }).emulateTransitionEnd(i3);
      } else this._destroyElement(t3);
    }, n2._destroyElement = function(t3) {
      e(t3).detach().trigger("closed.bs.alert").remove();
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this), o2 = i3.data("bs.alert");
        o2 || (o2 = new t2(this), i3.data("bs.alert", o2)), "close" === n3 && o2[n3](this);
      });
    }, t2._handleDismiss = function(t3) {
      return function(e2) {
        e2 && e2.preventDefault(), t3.close(this);
      };
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }]), t2;
  }();
  e(document).on("click.bs.alert.data-api", '[data-dismiss="alert"]', d._handleDismiss(new d())), e.fn[h] = d._jQueryInterface, e.fn[h].Constructor = d, e.fn[h].noConflict = function() {
    return e.fn[h] = u, d._jQueryInterface;
  };
  var f = e.fn.button, g = function() {
    function t2(t3) {
      this._element = t3;
    }
    var n2 = t2.prototype;
    return n2.toggle = function() {
      var t3 = true, n3 = true, i3 = e(this._element).closest('[data-toggle="buttons"]')[0];
      if (i3) {
        var o2 = this._element.querySelector('input:not([type="hidden"])');
        if (o2) {
          if ("radio" === o2.type) if (o2.checked && this._element.classList.contains("active")) t3 = false;
          else {
            var s2 = i3.querySelector(".active");
            s2 && e(s2).removeClass("active");
          }
          t3 && ("checkbox" !== o2.type && "radio" !== o2.type || (o2.checked = !this._element.classList.contains("active")), e(o2).trigger("change")), o2.focus(), n3 = false;
        }
      }
      this._element.hasAttribute("disabled") || this._element.classList.contains("disabled") || (n3 && this._element.setAttribute("aria-pressed", !this._element.classList.contains("active")), t3 && e(this._element).toggleClass("active"));
    }, n2.dispose = function() {
      e.removeData(this._element, "bs.button"), this._element = null;
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this).data("bs.button");
        i3 || (i3 = new t2(this), e(this).data("bs.button", i3)), "toggle" === n3 && i3[n3]();
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }]), t2;
  }();
  e(document).on("click.bs.button.data-api", '[data-toggle^="button"]', function(t2) {
    var n2 = t2.target, i3 = n2;
    if (e(n2).hasClass("btn") || (n2 = e(n2).closest(".btn")[0]), !n2 || n2.hasAttribute("disabled") || n2.classList.contains("disabled")) t2.preventDefault();
    else {
      var o2 = n2.querySelector('input:not([type="hidden"])');
      if (o2 && (o2.hasAttribute("disabled") || o2.classList.contains("disabled"))) return void t2.preventDefault();
      "LABEL" === i3.tagName && o2 && "checkbox" === o2.type && t2.preventDefault(), g._jQueryInterface.call(e(n2), "toggle");
    }
  }).on("focus.bs.button.data-api blur.bs.button.data-api", '[data-toggle^="button"]', function(t2) {
    var n2 = e(t2.target).closest(".btn")[0];
    e(n2).toggleClass("focus", /^focus(in)?$/.test(t2.type));
  }), e(window).on("load.bs.button.data-api", function() {
    for (var t2 = [].slice.call(document.querySelectorAll('[data-toggle="buttons"] .btn')), e2 = 0, n2 = t2.length; e2 < n2; e2++) {
      var i3 = t2[e2], o2 = i3.querySelector('input:not([type="hidden"])');
      o2.checked || o2.hasAttribute("checked") ? i3.classList.add("active") : i3.classList.remove("active");
    }
    for (var s2 = 0, r2 = (t2 = [].slice.call(document.querySelectorAll('[data-toggle="button"]'))).length; s2 < r2; s2++) {
      var a2 = t2[s2];
      "true" === a2.getAttribute("aria-pressed") ? a2.classList.add("active") : a2.classList.remove("active");
    }
  }), e.fn.button = g._jQueryInterface, e.fn.button.Constructor = g, e.fn.button.noConflict = function() {
    return e.fn.button = f, g._jQueryInterface;
  };
  var m = "carousel", p = ".bs.carousel", _ = e.fn[m], v = { interval: 5e3, keyboard: true, slide: false, pause: "hover", wrap: true, touch: true }, b = { interval: "(number|boolean)", keyboard: "boolean", slide: "(boolean|string)", pause: "(string|boolean)", wrap: "boolean", touch: "boolean" }, y = { TOUCH: "touch", PEN: "pen" }, E = function() {
    function t2(t3, e2) {
      this._items = null, this._interval = null, this._activeElement = null, this._isPaused = false, this._isSliding = false, this.touchTimeout = null, this.touchStartX = 0, this.touchDeltaX = 0, this._config = this._getConfig(e2), this._element = t3, this._indicatorsElement = this._element.querySelector(".carousel-indicators"), this._touchSupported = "ontouchstart" in document.documentElement || navigator.maxTouchPoints > 0, this._pointerEvent = Boolean(window.PointerEvent || window.MSPointerEvent), this._addEventListeners();
    }
    var n2 = t2.prototype;
    return n2.next = function() {
      this._isSliding || this._slide("next");
    }, n2.nextWhenVisible = function() {
      !document.hidden && e(this._element).is(":visible") && "hidden" !== e(this._element).css("visibility") && this.next();
    }, n2.prev = function() {
      this._isSliding || this._slide("prev");
    }, n2.pause = function(t3) {
      t3 || (this._isPaused = true), this._element.querySelector(".carousel-item-next, .carousel-item-prev") && (c.triggerTransitionEnd(this._element), this.cycle(true)), clearInterval(this._interval), this._interval = null;
    }, n2.cycle = function(t3) {
      t3 || (this._isPaused = false), this._interval && (clearInterval(this._interval), this._interval = null), this._config.interval && !this._isPaused && (this._interval = setInterval((document.visibilityState ? this.nextWhenVisible : this.next).bind(this), this._config.interval));
    }, n2.to = function(t3) {
      var n3 = this;
      this._activeElement = this._element.querySelector(".active.carousel-item");
      var i3 = this._getItemIndex(this._activeElement);
      if (!(t3 > this._items.length - 1 || t3 < 0)) if (this._isSliding) e(this._element).one("slid.bs.carousel", function() {
        return n3.to(t3);
      });
      else {
        if (i3 === t3) return this.pause(), void this.cycle();
        var o2 = t3 > i3 ? "next" : "prev";
        this._slide(o2, this._items[t3]);
      }
    }, n2.dispose = function() {
      e(this._element).off(p), e.removeData(this._element, "bs.carousel"), this._items = null, this._config = null, this._element = null, this._interval = null, this._isPaused = null, this._isSliding = null, this._activeElement = null, this._indicatorsElement = null;
    }, n2._getConfig = function(t3) {
      return t3 = a(a({}, v), t3), c.typeCheckConfig(m, t3, b), t3;
    }, n2._handleSwipe = function() {
      var t3 = Math.abs(this.touchDeltaX);
      if (!(t3 <= 40)) {
        var e2 = t3 / this.touchDeltaX;
        this.touchDeltaX = 0, e2 > 0 && this.prev(), e2 < 0 && this.next();
      }
    }, n2._addEventListeners = function() {
      var t3 = this;
      this._config.keyboard && e(this._element).on("keydown.bs.carousel", function(e2) {
        return t3._keydown(e2);
      }), "hover" === this._config.pause && e(this._element).on("mouseenter.bs.carousel", function(e2) {
        return t3.pause(e2);
      }).on("mouseleave.bs.carousel", function(e2) {
        return t3.cycle(e2);
      }), this._config.touch && this._addTouchEventListeners();
    }, n2._addTouchEventListeners = function() {
      var t3 = this;
      if (this._touchSupported) {
        var n3 = function(e2) {
          t3._pointerEvent && y[e2.originalEvent.pointerType.toUpperCase()] ? t3.touchStartX = e2.originalEvent.clientX : t3._pointerEvent || (t3.touchStartX = e2.originalEvent.touches[0].clientX);
        }, i3 = function(e2) {
          t3._pointerEvent && y[e2.originalEvent.pointerType.toUpperCase()] && (t3.touchDeltaX = e2.originalEvent.clientX - t3.touchStartX), t3._handleSwipe(), "hover" === t3._config.pause && (t3.pause(), t3.touchTimeout && clearTimeout(t3.touchTimeout), t3.touchTimeout = setTimeout(function(e3) {
            return t3.cycle(e3);
          }, 500 + t3._config.interval));
        };
        e(this._element.querySelectorAll(".carousel-item img")).on("dragstart.bs.carousel", function(t4) {
          return t4.preventDefault();
        }), this._pointerEvent ? (e(this._element).on("pointerdown.bs.carousel", function(t4) {
          return n3(t4);
        }), e(this._element).on("pointerup.bs.carousel", function(t4) {
          return i3(t4);
        }), this._element.classList.add("pointer-event")) : (e(this._element).on("touchstart.bs.carousel", function(t4) {
          return n3(t4);
        }), e(this._element).on("touchmove.bs.carousel", function(e2) {
          return function(e3) {
            e3.originalEvent.touches && e3.originalEvent.touches.length > 1 ? t3.touchDeltaX = 0 : t3.touchDeltaX = e3.originalEvent.touches[0].clientX - t3.touchStartX;
          }(e2);
        }), e(this._element).on("touchend.bs.carousel", function(t4) {
          return i3(t4);
        }));
      }
    }, n2._keydown = function(t3) {
      if (!/input|textarea/i.test(t3.target.tagName)) switch (t3.which) {
        case 37:
          t3.preventDefault(), this.prev();
          break;
        case 39:
          t3.preventDefault(), this.next();
      }
    }, n2._getItemIndex = function(t3) {
      return this._items = t3 && t3.parentNode ? [].slice.call(t3.parentNode.querySelectorAll(".carousel-item")) : [], this._items.indexOf(t3);
    }, n2._getItemByDirection = function(t3, e2) {
      var n3 = "next" === t3, i3 = "prev" === t3, o2 = this._getItemIndex(e2), s2 = this._items.length - 1;
      if ((i3 && 0 === o2 || n3 && o2 === s2) && !this._config.wrap) return e2;
      var r2 = (o2 + ("prev" === t3 ? -1 : 1)) % this._items.length;
      return -1 === r2 ? this._items[this._items.length - 1] : this._items[r2];
    }, n2._triggerSlideEvent = function(t3, n3) {
      var i3 = this._getItemIndex(t3), o2 = this._getItemIndex(this._element.querySelector(".active.carousel-item")), s2 = e.Event("slide.bs.carousel", { relatedTarget: t3, direction: n3, from: o2, to: i3 });
      return e(this._element).trigger(s2), s2;
    }, n2._setActiveIndicatorElement = function(t3) {
      if (this._indicatorsElement) {
        var n3 = [].slice.call(this._indicatorsElement.querySelectorAll(".active"));
        e(n3).removeClass("active");
        var i3 = this._indicatorsElement.children[this._getItemIndex(t3)];
        i3 && e(i3).addClass("active");
      }
    }, n2._slide = function(t3, n3) {
      var i3, o2, s2, r2 = this, a2 = this._element.querySelector(".active.carousel-item"), l2 = this._getItemIndex(a2), h2 = n3 || a2 && this._getItemByDirection(t3, a2), u2 = this._getItemIndex(h2), d2 = Boolean(this._interval);
      if ("next" === t3 ? (i3 = "carousel-item-left", o2 = "carousel-item-next", s2 = "left") : (i3 = "carousel-item-right", o2 = "carousel-item-prev", s2 = "right"), h2 && e(h2).hasClass("active")) this._isSliding = false;
      else if (!this._triggerSlideEvent(h2, s2).isDefaultPrevented() && a2 && h2) {
        this._isSliding = true, d2 && this.pause(), this._setActiveIndicatorElement(h2);
        var f2 = e.Event("slid.bs.carousel", { relatedTarget: h2, direction: s2, from: l2, to: u2 });
        if (e(this._element).hasClass("slide")) {
          e(h2).addClass(o2), c.reflow(h2), e(a2).addClass(i3), e(h2).addClass(i3);
          var g2 = parseInt(h2.getAttribute("data-interval"), 10);
          g2 ? (this._config.defaultInterval = this._config.defaultInterval || this._config.interval, this._config.interval = g2) : this._config.interval = this._config.defaultInterval || this._config.interval;
          var m2 = c.getTransitionDurationFromElement(a2);
          e(a2).one(c.TRANSITION_END, function() {
            e(h2).removeClass(i3 + " " + o2).addClass("active"), e(a2).removeClass("active " + o2 + " " + i3), r2._isSliding = false, setTimeout(function() {
              return e(r2._element).trigger(f2);
            }, 0);
          }).emulateTransitionEnd(m2);
        } else e(a2).removeClass("active"), e(h2).addClass("active"), this._isSliding = false, e(this._element).trigger(f2);
        d2 && this.cycle();
      }
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this).data("bs.carousel"), o2 = a(a({}, v), e(this).data());
        "object" == typeof n3 && (o2 = a(a({}, o2), n3));
        var s2 = "string" == typeof n3 ? n3 : o2.slide;
        if (i3 || (i3 = new t2(this, o2), e(this).data("bs.carousel", i3)), "number" == typeof n3) i3.to(n3);
        else if ("string" == typeof s2) {
          if ("undefined" == typeof i3[s2]) throw new TypeError('No method named "' + s2 + '"');
          i3[s2]();
        } else o2.interval && o2.ride && (i3.pause(), i3.cycle());
      });
    }, t2._dataApiClickHandler = function(n3) {
      var i3 = c.getSelectorFromElement(this);
      if (i3) {
        var o2 = e(i3)[0];
        if (o2 && e(o2).hasClass("carousel")) {
          var s2 = a(a({}, e(o2).data()), e(this).data()), r2 = this.getAttribute("data-slide-to");
          r2 && (s2.interval = false), t2._jQueryInterface.call(e(o2), s2), r2 && e(o2).data("bs.carousel").to(r2), n3.preventDefault();
        }
      }
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return v;
    } }]), t2;
  }();
  e(document).on("click.bs.carousel.data-api", "[data-slide], [data-slide-to]", E._dataApiClickHandler), e(window).on("load.bs.carousel.data-api", function() {
    for (var t2 = [].slice.call(document.querySelectorAll('[data-ride="carousel"]')), n2 = 0, i3 = t2.length; n2 < i3; n2++) {
      var o2 = e(t2[n2]);
      E._jQueryInterface.call(o2, o2.data());
    }
  }), e.fn[m] = E._jQueryInterface, e.fn[m].Constructor = E, e.fn[m].noConflict = function() {
    return e.fn[m] = _, E._jQueryInterface;
  };
  var w = "collapse", T = e.fn[w], C = { toggle: true, parent: "" }, S = { toggle: "boolean", parent: "(string|element)" }, D = function() {
    function t2(t3, e2) {
      this._isTransitioning = false, this._element = t3, this._config = this._getConfig(e2), this._triggerArray = [].slice.call(document.querySelectorAll('[data-toggle="collapse"][href="#' + t3.id + '"],[data-toggle="collapse"][data-target="#' + t3.id + '"]'));
      for (var n3 = [].slice.call(document.querySelectorAll('[data-toggle="collapse"]')), i3 = 0, o2 = n3.length; i3 < o2; i3++) {
        var s2 = n3[i3], r2 = c.getSelectorFromElement(s2), a2 = [].slice.call(document.querySelectorAll(r2)).filter(function(e3) {
          return e3 === t3;
        });
        null !== r2 && a2.length > 0 && (this._selector = r2, this._triggerArray.push(s2));
      }
      this._parent = this._config.parent ? this._getParent() : null, this._config.parent || this._addAriaAndCollapsedClass(this._element, this._triggerArray), this._config.toggle && this.toggle();
    }
    var n2 = t2.prototype;
    return n2.toggle = function() {
      e(this._element).hasClass("show") ? this.hide() : this.show();
    }, n2.show = function() {
      var n3, i3, o2 = this;
      if (!this._isTransitioning && !e(this._element).hasClass("show") && (this._parent && 0 === (n3 = [].slice.call(this._parent.querySelectorAll(".show, .collapsing")).filter(function(t3) {
        return "string" == typeof o2._config.parent ? t3.getAttribute("data-parent") === o2._config.parent : t3.classList.contains("collapse");
      })).length && (n3 = null), !(n3 && (i3 = e(n3).not(this._selector).data("bs.collapse")) && i3._isTransitioning))) {
        var s2 = e.Event("show.bs.collapse");
        if (e(this._element).trigger(s2), !s2.isDefaultPrevented()) {
          n3 && (t2._jQueryInterface.call(e(n3).not(this._selector), "hide"), i3 || e(n3).data("bs.collapse", null));
          var r2 = this._getDimension();
          e(this._element).removeClass("collapse").addClass("collapsing"), this._element.style[r2] = 0, this._triggerArray.length && e(this._triggerArray).removeClass("collapsed").attr("aria-expanded", true), this.setTransitioning(true);
          var a2 = "scroll" + (r2[0].toUpperCase() + r2.slice(1)), l2 = c.getTransitionDurationFromElement(this._element);
          e(this._element).one(c.TRANSITION_END, function() {
            e(o2._element).removeClass("collapsing").addClass("collapse show"), o2._element.style[r2] = "", o2.setTransitioning(false), e(o2._element).trigger("shown.bs.collapse");
          }).emulateTransitionEnd(l2), this._element.style[r2] = this._element[a2] + "px";
        }
      }
    }, n2.hide = function() {
      var t3 = this;
      if (!this._isTransitioning && e(this._element).hasClass("show")) {
        var n3 = e.Event("hide.bs.collapse");
        if (e(this._element).trigger(n3), !n3.isDefaultPrevented()) {
          var i3 = this._getDimension();
          this._element.style[i3] = this._element.getBoundingClientRect()[i3] + "px", c.reflow(this._element), e(this._element).addClass("collapsing").removeClass("collapse show");
          var o2 = this._triggerArray.length;
          if (o2 > 0) for (var s2 = 0; s2 < o2; s2++) {
            var r2 = this._triggerArray[s2], a2 = c.getSelectorFromElement(r2);
            if (null !== a2) e([].slice.call(document.querySelectorAll(a2))).hasClass("show") || e(r2).addClass("collapsed").attr("aria-expanded", false);
          }
          this.setTransitioning(true);
          this._element.style[i3] = "";
          var l2 = c.getTransitionDurationFromElement(this._element);
          e(this._element).one(c.TRANSITION_END, function() {
            t3.setTransitioning(false), e(t3._element).removeClass("collapsing").addClass("collapse").trigger("hidden.bs.collapse");
          }).emulateTransitionEnd(l2);
        }
      }
    }, n2.setTransitioning = function(t3) {
      this._isTransitioning = t3;
    }, n2.dispose = function() {
      e.removeData(this._element, "bs.collapse"), this._config = null, this._parent = null, this._element = null, this._triggerArray = null, this._isTransitioning = null;
    }, n2._getConfig = function(t3) {
      return (t3 = a(a({}, C), t3)).toggle = Boolean(t3.toggle), c.typeCheckConfig(w, t3, S), t3;
    }, n2._getDimension = function() {
      return e(this._element).hasClass("width") ? "width" : "height";
    }, n2._getParent = function() {
      var n3, i3 = this;
      c.isElement(this._config.parent) ? (n3 = this._config.parent, "undefined" != typeof this._config.parent.jquery && (n3 = this._config.parent[0])) : n3 = document.querySelector(this._config.parent);
      var o2 = '[data-toggle="collapse"][data-parent="' + this._config.parent + '"]', s2 = [].slice.call(n3.querySelectorAll(o2));
      return e(s2).each(function(e2, n4) {
        i3._addAriaAndCollapsedClass(t2._getTargetFromElement(n4), [n4]);
      }), n3;
    }, n2._addAriaAndCollapsedClass = function(t3, n3) {
      var i3 = e(t3).hasClass("show");
      n3.length && e(n3).toggleClass("collapsed", !i3).attr("aria-expanded", i3);
    }, t2._getTargetFromElement = function(t3) {
      var e2 = c.getSelectorFromElement(t3);
      return e2 ? document.querySelector(e2) : null;
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this), o2 = i3.data("bs.collapse"), s2 = a(a(a({}, C), i3.data()), "object" == typeof n3 && n3 ? n3 : {});
        if (!o2 && s2.toggle && "string" == typeof n3 && /show|hide/.test(n3) && (s2.toggle = false), o2 || (o2 = new t2(this, s2), i3.data("bs.collapse", o2)), "string" == typeof n3) {
          if ("undefined" == typeof o2[n3]) throw new TypeError('No method named "' + n3 + '"');
          o2[n3]();
        }
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return C;
    } }]), t2;
  }();
  e(document).on("click.bs.collapse.data-api", '[data-toggle="collapse"]', function(t2) {
    "A" === t2.currentTarget.tagName && t2.preventDefault();
    var n2 = e(this), i3 = c.getSelectorFromElement(this), o2 = [].slice.call(document.querySelectorAll(i3));
    e(o2).each(function() {
      var t3 = e(this), i4 = t3.data("bs.collapse") ? "toggle" : n2.data();
      D._jQueryInterface.call(t3, i4);
    });
  }), e.fn[w] = D._jQueryInterface, e.fn[w].Constructor = D, e.fn[w].noConflict = function() {
    return e.fn[w] = T, D._jQueryInterface;
  };
  var k = "dropdown", N = e.fn[k], A = new RegExp("38|40|27"), I = { offset: 0, flip: true, boundary: "scrollParent", reference: "toggle", display: "dynamic", popperConfig: null }, O = { offset: "(number|string|function)", flip: "boolean", boundary: "(string|element)", reference: "(string|element)", display: "string", popperConfig: "(null|object)" }, j = function() {
    function t2(t3, e2) {
      this._element = t3, this._popper = null, this._config = this._getConfig(e2), this._menu = this._getMenuElement(), this._inNavbar = this._detectNavbar(), this._addEventListeners();
    }
    var i3 = t2.prototype;
    return i3.toggle = function() {
      if (!this._element.disabled && !e(this._element).hasClass("disabled")) {
        var n2 = e(this._menu).hasClass("show");
        t2._clearMenus(), n2 || this.show(true);
      }
    }, i3.show = function(i4) {
      if (void 0 === i4 && (i4 = false), !(this._element.disabled || e(this._element).hasClass("disabled") || e(this._menu).hasClass("show"))) {
        var o2 = { relatedTarget: this._element }, s2 = e.Event("show.bs.dropdown", o2), r2 = t2._getParentFromElement(this._element);
        if (e(r2).trigger(s2), !s2.isDefaultPrevented()) {
          if (!this._inNavbar && i4) {
            if ("undefined" == typeof n) throw new TypeError("Bootstrap's dropdowns require Popper.js (https://popper.js.org/)");
            var a2 = this._element;
            "parent" === this._config.reference ? a2 = r2 : c.isElement(this._config.reference) && (a2 = this._config.reference, "undefined" != typeof this._config.reference.jquery && (a2 = this._config.reference[0])), "scrollParent" !== this._config.boundary && e(r2).addClass("position-static"), this._popper = new n(a2, this._menu, this._getPopperConfig());
          }
          "ontouchstart" in document.documentElement && 0 === e(r2).closest(".navbar-nav").length && e(document.body).children().on("mouseover", null, e.noop), this._element.focus(), this._element.setAttribute("aria-expanded", true), e(this._menu).toggleClass("show"), e(r2).toggleClass("show").trigger(e.Event("shown.bs.dropdown", o2));
        }
      }
    }, i3.hide = function() {
      if (!this._element.disabled && !e(this._element).hasClass("disabled") && e(this._menu).hasClass("show")) {
        var n2 = { relatedTarget: this._element }, i4 = e.Event("hide.bs.dropdown", n2), o2 = t2._getParentFromElement(this._element);
        e(o2).trigger(i4), i4.isDefaultPrevented() || (this._popper && this._popper.destroy(), e(this._menu).toggleClass("show"), e(o2).toggleClass("show").trigger(e.Event("hidden.bs.dropdown", n2)));
      }
    }, i3.dispose = function() {
      e.removeData(this._element, "bs.dropdown"), e(this._element).off(".bs.dropdown"), this._element = null, this._menu = null, null !== this._popper && (this._popper.destroy(), this._popper = null);
    }, i3.update = function() {
      this._inNavbar = this._detectNavbar(), null !== this._popper && this._popper.scheduleUpdate();
    }, i3._addEventListeners = function() {
      var t3 = this;
      e(this._element).on("click.bs.dropdown", function(e2) {
        e2.preventDefault(), e2.stopPropagation(), t3.toggle();
      });
    }, i3._getConfig = function(t3) {
      return t3 = a(a(a({}, this.constructor.Default), e(this._element).data()), t3), c.typeCheckConfig(k, t3, this.constructor.DefaultType), t3;
    }, i3._getMenuElement = function() {
      if (!this._menu) {
        var e2 = t2._getParentFromElement(this._element);
        e2 && (this._menu = e2.querySelector(".dropdown-menu"));
      }
      return this._menu;
    }, i3._getPlacement = function() {
      var t3 = e(this._element.parentNode), n2 = "bottom-start";
      return t3.hasClass("dropup") ? n2 = e(this._menu).hasClass("dropdown-menu-right") ? "top-end" : "top-start" : t3.hasClass("dropright") ? n2 = "right-start" : t3.hasClass("dropleft") ? n2 = "left-start" : e(this._menu).hasClass("dropdown-menu-right") && (n2 = "bottom-end"), n2;
    }, i3._detectNavbar = function() {
      return e(this._element).closest(".navbar").length > 0;
    }, i3._getOffset = function() {
      var t3 = this, e2 = {};
      return "function" == typeof this._config.offset ? e2.fn = function(e3) {
        return e3.offsets = a(a({}, e3.offsets), t3._config.offset(e3.offsets, t3._element) || {}), e3;
      } : e2.offset = this._config.offset, e2;
    }, i3._getPopperConfig = function() {
      var t3 = { placement: this._getPlacement(), modifiers: { offset: this._getOffset(), flip: { enabled: this._config.flip }, preventOverflow: { boundariesElement: this._config.boundary } } };
      return "static" === this._config.display && (t3.modifiers.applyStyle = { enabled: false }), a(a({}, t3), this._config.popperConfig);
    }, t2._jQueryInterface = function(n2) {
      return this.each(function() {
        var i4 = e(this).data("bs.dropdown");
        if (i4 || (i4 = new t2(this, "object" == typeof n2 ? n2 : null), e(this).data("bs.dropdown", i4)), "string" == typeof n2) {
          if ("undefined" == typeof i4[n2]) throw new TypeError('No method named "' + n2 + '"');
          i4[n2]();
        }
      });
    }, t2._clearMenus = function(n2) {
      if (!n2 || 3 !== n2.which && ("keyup" !== n2.type || 9 === n2.which)) for (var i4 = [].slice.call(document.querySelectorAll('[data-toggle="dropdown"]')), o2 = 0, s2 = i4.length; o2 < s2; o2++) {
        var r2 = t2._getParentFromElement(i4[o2]), a2 = e(i4[o2]).data("bs.dropdown"), l2 = { relatedTarget: i4[o2] };
        if (n2 && "click" === n2.type && (l2.clickEvent = n2), a2) {
          var c2 = a2._menu;
          if (e(r2).hasClass("show") && !(n2 && ("click" === n2.type && /input|textarea/i.test(n2.target.tagName) || "keyup" === n2.type && 9 === n2.which) && e.contains(r2, n2.target))) {
            var h2 = e.Event("hide.bs.dropdown", l2);
            e(r2).trigger(h2), h2.isDefaultPrevented() || ("ontouchstart" in document.documentElement && e(document.body).children().off("mouseover", null, e.noop), i4[o2].setAttribute("aria-expanded", "false"), a2._popper && a2._popper.destroy(), e(c2).removeClass("show"), e(r2).removeClass("show").trigger(e.Event("hidden.bs.dropdown", l2)));
          }
        }
      }
    }, t2._getParentFromElement = function(t3) {
      var e2, n2 = c.getSelectorFromElement(t3);
      return n2 && (e2 = document.querySelector(n2)), e2 || t3.parentNode;
    }, t2._dataApiKeydownHandler = function(n2) {
      if (!(/input|textarea/i.test(n2.target.tagName) ? 32 === n2.which || 27 !== n2.which && (40 !== n2.which && 38 !== n2.which || e(n2.target).closest(".dropdown-menu").length) : !A.test(n2.which)) && !this.disabled && !e(this).hasClass("disabled")) {
        var i4 = t2._getParentFromElement(this), o2 = e(i4).hasClass("show");
        if (o2 || 27 !== n2.which) {
          if (n2.preventDefault(), n2.stopPropagation(), !o2 || o2 && (27 === n2.which || 32 === n2.which)) return 27 === n2.which && e(i4.querySelector('[data-toggle="dropdown"]')).trigger("focus"), void e(this).trigger("click");
          var s2 = [].slice.call(i4.querySelectorAll(".dropdown-menu .dropdown-item:not(.disabled):not(:disabled)")).filter(function(t3) {
            return e(t3).is(":visible");
          });
          if (0 !== s2.length) {
            var r2 = s2.indexOf(n2.target);
            38 === n2.which && r2 > 0 && r2--, 40 === n2.which && r2 < s2.length - 1 && r2++, r2 < 0 && (r2 = 0), s2[r2].focus();
          }
        }
      }
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return I;
    } }, { key: "DefaultType", get: function() {
      return O;
    } }]), t2;
  }();
  e(document).on("keydown.bs.dropdown.data-api", '[data-toggle="dropdown"]', j._dataApiKeydownHandler).on("keydown.bs.dropdown.data-api", ".dropdown-menu", j._dataApiKeydownHandler).on("click.bs.dropdown.data-api keyup.bs.dropdown.data-api", j._clearMenus).on("click.bs.dropdown.data-api", '[data-toggle="dropdown"]', function(t2) {
    t2.preventDefault(), t2.stopPropagation(), j._jQueryInterface.call(e(this), "toggle");
  }).on("click.bs.dropdown.data-api", ".dropdown form", function(t2) {
    t2.stopPropagation();
  }), e.fn[k] = j._jQueryInterface, e.fn[k].Constructor = j, e.fn[k].noConflict = function() {
    return e.fn[k] = N, j._jQueryInterface;
  };
  var P = e.fn.modal, x = { backdrop: true, keyboard: true, focus: true, show: true }, L = { backdrop: "(boolean|string)", keyboard: "boolean", focus: "boolean", show: "boolean" }, R = function() {
    function t2(t3, e2) {
      this._config = this._getConfig(e2), this._element = t3, this._dialog = t3.querySelector(".modal-dialog"), this._backdrop = null, this._isShown = false, this._isBodyOverflowing = false, this._ignoreBackdropClick = false, this._isTransitioning = false, this._scrollbarWidth = 0;
    }
    var n2 = t2.prototype;
    return n2.toggle = function(t3) {
      return this._isShown ? this.hide() : this.show(t3);
    }, n2.show = function(t3) {
      var n3 = this;
      if (!this._isShown && !this._isTransitioning) {
        e(this._element).hasClass("fade") && (this._isTransitioning = true);
        var i3 = e.Event("show.bs.modal", { relatedTarget: t3 });
        e(this._element).trigger(i3), this._isShown || i3.isDefaultPrevented() || (this._isShown = true, this._checkScrollbar(), this._setScrollbar(), this._adjustDialog(), this._setEscapeEvent(), this._setResizeEvent(), e(this._element).on("click.dismiss.bs.modal", '[data-dismiss="modal"]', function(t4) {
          return n3.hide(t4);
        }), e(this._dialog).on("mousedown.dismiss.bs.modal", function() {
          e(n3._element).one("mouseup.dismiss.bs.modal", function(t4) {
            e(t4.target).is(n3._element) && (n3._ignoreBackdropClick = true);
          });
        }), this._showBackdrop(function() {
          return n3._showElement(t3);
        }));
      }
    }, n2.hide = function(t3) {
      var n3 = this;
      if (t3 && t3.preventDefault(), this._isShown && !this._isTransitioning) {
        var i3 = e.Event("hide.bs.modal");
        if (e(this._element).trigger(i3), this._isShown && !i3.isDefaultPrevented()) {
          this._isShown = false;
          var o2 = e(this._element).hasClass("fade");
          if (o2 && (this._isTransitioning = true), this._setEscapeEvent(), this._setResizeEvent(), e(document).off("focusin.bs.modal"), e(this._element).removeClass("show"), e(this._element).off("click.dismiss.bs.modal"), e(this._dialog).off("mousedown.dismiss.bs.modal"), o2) {
            var s2 = c.getTransitionDurationFromElement(this._element);
            e(this._element).one(c.TRANSITION_END, function(t4) {
              return n3._hideModal(t4);
            }).emulateTransitionEnd(s2);
          } else this._hideModal();
        }
      }
    }, n2.dispose = function() {
      [window, this._element, this._dialog].forEach(function(t3) {
        return e(t3).off(".bs.modal");
      }), e(document).off("focusin.bs.modal"), e.removeData(this._element, "bs.modal"), this._config = null, this._element = null, this._dialog = null, this._backdrop = null, this._isShown = null, this._isBodyOverflowing = null, this._ignoreBackdropClick = null, this._isTransitioning = null, this._scrollbarWidth = null;
    }, n2.handleUpdate = function() {
      this._adjustDialog();
    }, n2._getConfig = function(t3) {
      return t3 = a(a({}, x), t3), c.typeCheckConfig("modal", t3, L), t3;
    }, n2._triggerBackdropTransition = function() {
      var t3 = this;
      if ("static" === this._config.backdrop) {
        var n3 = e.Event("hidePrevented.bs.modal");
        if (e(this._element).trigger(n3), n3.defaultPrevented) return;
        this._element.classList.add("modal-static");
        var i3 = c.getTransitionDurationFromElement(this._element);
        e(this._element).one(c.TRANSITION_END, function() {
          t3._element.classList.remove("modal-static");
        }).emulateTransitionEnd(i3), this._element.focus();
      } else this.hide();
    }, n2._showElement = function(t3) {
      var n3 = this, i3 = e(this._element).hasClass("fade"), o2 = this._dialog ? this._dialog.querySelector(".modal-body") : null;
      this._element.parentNode && this._element.parentNode.nodeType === Node.ELEMENT_NODE || document.body.appendChild(this._element), this._element.style.display = "block", this._element.removeAttribute("aria-hidden"), this._element.setAttribute("aria-modal", true), e(this._dialog).hasClass("modal-dialog-scrollable") && o2 ? o2.scrollTop = 0 : this._element.scrollTop = 0, i3 && c.reflow(this._element), e(this._element).addClass("show"), this._config.focus && this._enforceFocus();
      var s2 = e.Event("shown.bs.modal", { relatedTarget: t3 }), r2 = function() {
        n3._config.focus && n3._element.focus(), n3._isTransitioning = false, e(n3._element).trigger(s2);
      };
      if (i3) {
        var a2 = c.getTransitionDurationFromElement(this._dialog);
        e(this._dialog).one(c.TRANSITION_END, r2).emulateTransitionEnd(a2);
      } else r2();
    }, n2._enforceFocus = function() {
      var t3 = this;
      e(document).off("focusin.bs.modal").on("focusin.bs.modal", function(n3) {
        document !== n3.target && t3._element !== n3.target && 0 === e(t3._element).has(n3.target).length && t3._element.focus();
      });
    }, n2._setEscapeEvent = function() {
      var t3 = this;
      this._isShown ? e(this._element).on("keydown.dismiss.bs.modal", function(e2) {
        t3._config.keyboard && 27 === e2.which ? (e2.preventDefault(), t3.hide()) : t3._config.keyboard || 27 !== e2.which || t3._triggerBackdropTransition();
      }) : this._isShown || e(this._element).off("keydown.dismiss.bs.modal");
    }, n2._setResizeEvent = function() {
      var t3 = this;
      this._isShown ? e(window).on("resize.bs.modal", function(e2) {
        return t3.handleUpdate(e2);
      }) : e(window).off("resize.bs.modal");
    }, n2._hideModal = function() {
      var t3 = this;
      this._element.style.display = "none", this._element.setAttribute("aria-hidden", true), this._element.removeAttribute("aria-modal"), this._isTransitioning = false, this._showBackdrop(function() {
        e(document.body).removeClass("modal-open"), t3._resetAdjustments(), t3._resetScrollbar(), e(t3._element).trigger("hidden.bs.modal");
      });
    }, n2._removeBackdrop = function() {
      this._backdrop && (e(this._backdrop).remove(), this._backdrop = null);
    }, n2._showBackdrop = function(t3) {
      var n3 = this, i3 = e(this._element).hasClass("fade") ? "fade" : "";
      if (this._isShown && this._config.backdrop) {
        if (this._backdrop = document.createElement("div"), this._backdrop.className = "modal-backdrop", i3 && this._backdrop.classList.add(i3), e(this._backdrop).appendTo(document.body), e(this._element).on("click.dismiss.bs.modal", function(t4) {
          n3._ignoreBackdropClick ? n3._ignoreBackdropClick = false : t4.target === t4.currentTarget && n3._triggerBackdropTransition();
        }), i3 && c.reflow(this._backdrop), e(this._backdrop).addClass("show"), !t3) return;
        if (!i3) return void t3();
        var o2 = c.getTransitionDurationFromElement(this._backdrop);
        e(this._backdrop).one(c.TRANSITION_END, t3).emulateTransitionEnd(o2);
      } else if (!this._isShown && this._backdrop) {
        e(this._backdrop).removeClass("show");
        var s2 = function() {
          n3._removeBackdrop(), t3 && t3();
        };
        if (e(this._element).hasClass("fade")) {
          var r2 = c.getTransitionDurationFromElement(this._backdrop);
          e(this._backdrop).one(c.TRANSITION_END, s2).emulateTransitionEnd(r2);
        } else s2();
      } else t3 && t3();
    }, n2._adjustDialog = function() {
      var t3 = this._element.scrollHeight > document.documentElement.clientHeight;
      !this._isBodyOverflowing && t3 && (this._element.style.paddingLeft = this._scrollbarWidth + "px"), this._isBodyOverflowing && !t3 && (this._element.style.paddingRight = this._scrollbarWidth + "px");
    }, n2._resetAdjustments = function() {
      this._element.style.paddingLeft = "", this._element.style.paddingRight = "";
    }, n2._checkScrollbar = function() {
      var t3 = document.body.getBoundingClientRect();
      this._isBodyOverflowing = Math.round(t3.left + t3.right) < window.innerWidth, this._scrollbarWidth = this._getScrollbarWidth();
    }, n2._setScrollbar = function() {
      var t3 = this;
      if (this._isBodyOverflowing) {
        var n3 = [].slice.call(document.querySelectorAll(".fixed-top, .fixed-bottom, .is-fixed, .sticky-top")), i3 = [].slice.call(document.querySelectorAll(".sticky-top"));
        e(n3).each(function(n4, i4) {
          var o3 = i4.style.paddingRight, s3 = e(i4).css("padding-right");
          e(i4).data("padding-right", o3).css("padding-right", parseFloat(s3) + t3._scrollbarWidth + "px");
        }), e(i3).each(function(n4, i4) {
          var o3 = i4.style.marginRight, s3 = e(i4).css("margin-right");
          e(i4).data("margin-right", o3).css("margin-right", parseFloat(s3) - t3._scrollbarWidth + "px");
        });
        var o2 = document.body.style.paddingRight, s2 = e(document.body).css("padding-right");
        e(document.body).data("padding-right", o2).css("padding-right", parseFloat(s2) + this._scrollbarWidth + "px");
      }
      e(document.body).addClass("modal-open");
    }, n2._resetScrollbar = function() {
      var t3 = [].slice.call(document.querySelectorAll(".fixed-top, .fixed-bottom, .is-fixed, .sticky-top"));
      e(t3).each(function(t4, n4) {
        var i4 = e(n4).data("padding-right");
        e(n4).removeData("padding-right"), n4.style.paddingRight = i4 || "";
      });
      var n3 = [].slice.call(document.querySelectorAll(".sticky-top"));
      e(n3).each(function(t4, n4) {
        var i4 = e(n4).data("margin-right");
        "undefined" != typeof i4 && e(n4).css("margin-right", i4).removeData("margin-right");
      });
      var i3 = e(document.body).data("padding-right");
      e(document.body).removeData("padding-right"), document.body.style.paddingRight = i3 || "";
    }, n2._getScrollbarWidth = function() {
      var t3 = document.createElement("div");
      t3.className = "modal-scrollbar-measure", document.body.appendChild(t3);
      var e2 = t3.getBoundingClientRect().width - t3.clientWidth;
      return document.body.removeChild(t3), e2;
    }, t2._jQueryInterface = function(n3, i3) {
      return this.each(function() {
        var o2 = e(this).data("bs.modal"), s2 = a(a(a({}, x), e(this).data()), "object" == typeof n3 && n3 ? n3 : {});
        if (o2 || (o2 = new t2(this, s2), e(this).data("bs.modal", o2)), "string" == typeof n3) {
          if ("undefined" == typeof o2[n3]) throw new TypeError('No method named "' + n3 + '"');
          o2[n3](i3);
        } else s2.show && o2.show(i3);
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return x;
    } }]), t2;
  }();
  e(document).on("click.bs.modal.data-api", '[data-toggle="modal"]', function(t2) {
    var n2, i3 = this, o2 = c.getSelectorFromElement(this);
    o2 && (n2 = document.querySelector(o2));
    var s2 = e(n2).data("bs.modal") ? "toggle" : a(a({}, e(n2).data()), e(this).data());
    "A" !== this.tagName && "AREA" !== this.tagName || t2.preventDefault();
    var r2 = e(n2).one("show.bs.modal", function(t3) {
      t3.isDefaultPrevented() || r2.one("hidden.bs.modal", function() {
        e(i3).is(":visible") && i3.focus();
      });
    });
    R._jQueryInterface.call(e(n2), s2, this);
  }), e.fn.modal = R._jQueryInterface, e.fn.modal.Constructor = R, e.fn.modal.noConflict = function() {
    return e.fn.modal = P, R._jQueryInterface;
  };
  var q = ["background", "cite", "href", "itemtype", "longdesc", "poster", "src", "xlink:href"], F = { "*": ["class", "dir", "id", "lang", "role", /^aria-[\w-]*$/i], a: ["target", "href", "title", "rel"], area: [], b: [], br: [], col: [], code: [], div: [], em: [], hr: [], h1: [], h2: [], h3: [], h4: [], h5: [], h6: [], i: [], img: ["src", "srcset", "alt", "title", "width", "height"], li: [], ol: [], p: [], pre: [], s: [], small: [], span: [], sub: [], sup: [], strong: [], u: [], ul: [] }, Q = /^(?:(?:https?|mailto|ftp|tel|file):|[^#&/:?]*(?:[#/?]|$))/gi, B = /^data:(?:image\/(?:bmp|gif|jpeg|jpg|png|tiff|webp)|video\/(?:mpeg|mp4|ogg|webm)|audio\/(?:mp3|oga|ogg|opus));base64,[\d+/a-z]+=*$/i;
  function H(t2, e2, n2) {
    if (0 === t2.length) return t2;
    if (n2 && "function" == typeof n2) return n2(t2);
    for (var i3 = new window.DOMParser().parseFromString(t2, "text/html"), o2 = Object.keys(e2), s2 = [].slice.call(i3.body.querySelectorAll("*")), r2 = function(t3, n3) {
      var i4 = s2[t3], r3 = i4.nodeName.toLowerCase();
      if (-1 === o2.indexOf(i4.nodeName.toLowerCase())) return i4.parentNode.removeChild(i4), "continue";
      var a3 = [].slice.call(i4.attributes), l3 = [].concat(e2["*"] || [], e2[r3] || []);
      a3.forEach(function(t4) {
        (function(t5, e3) {
          var n4 = t5.nodeName.toLowerCase();
          if (-1 !== e3.indexOf(n4)) return -1 === q.indexOf(n4) || Boolean(t5.nodeValue.match(Q) || t5.nodeValue.match(B));
          for (var i5 = e3.filter(function(t6) {
            return t6 instanceof RegExp;
          }), o3 = 0, s3 = i5.length; o3 < s3; o3++) if (n4.match(i5[o3])) return true;
          return false;
        })(t4, l3) || i4.removeAttribute(t4.nodeName);
      });
    }, a2 = 0, l2 = s2.length; a2 < l2; a2++) r2(a2);
    return i3.body.innerHTML;
  }
  var U = "tooltip", M = e.fn[U], W = new RegExp("(^|\\s)bs-tooltip\\S+", "g"), V = ["sanitize", "whiteList", "sanitizeFn"], z = { animation: "boolean", template: "string", title: "(string|element|function)", trigger: "string", delay: "(number|object)", html: "boolean", selector: "(string|boolean)", placement: "(string|function)", offset: "(number|string|function)", container: "(string|element|boolean)", fallbackPlacement: "(string|array)", boundary: "(string|element)", sanitize: "boolean", sanitizeFn: "(null|function)", whiteList: "object", popperConfig: "(null|object)" }, K = { AUTO: "auto", TOP: "top", RIGHT: "right", BOTTOM: "bottom", LEFT: "left" }, X = { animation: true, template: '<div class="tooltip" role="tooltip"><div class="arrow"></div><div class="tooltip-inner"></div></div>', trigger: "hover focus", title: "", delay: 0, html: false, selector: false, placement: "top", offset: 0, container: false, fallbackPlacement: "flip", boundary: "scrollParent", sanitize: true, sanitizeFn: null, whiteList: F, popperConfig: null }, Y = { HIDE: "hide.bs.tooltip", HIDDEN: "hidden.bs.tooltip", SHOW: "show.bs.tooltip", SHOWN: "shown.bs.tooltip", INSERTED: "inserted.bs.tooltip", CLICK: "click.bs.tooltip", FOCUSIN: "focusin.bs.tooltip", FOCUSOUT: "focusout.bs.tooltip", MOUSEENTER: "mouseenter.bs.tooltip", MOUSELEAVE: "mouseleave.bs.tooltip" }, $2 = function() {
    function t2(t3, e2) {
      if ("undefined" == typeof n) throw new TypeError("Bootstrap's tooltips require Popper.js (https://popper.js.org/)");
      this._isEnabled = true, this._timeout = 0, this._hoverState = "", this._activeTrigger = {}, this._popper = null, this.element = t3, this.config = this._getConfig(e2), this.tip = null, this._setListeners();
    }
    var i3 = t2.prototype;
    return i3.enable = function() {
      this._isEnabled = true;
    }, i3.disable = function() {
      this._isEnabled = false;
    }, i3.toggleEnabled = function() {
      this._isEnabled = !this._isEnabled;
    }, i3.toggle = function(t3) {
      if (this._isEnabled) if (t3) {
        var n2 = this.constructor.DATA_KEY, i4 = e(t3.currentTarget).data(n2);
        i4 || (i4 = new this.constructor(t3.currentTarget, this._getDelegateConfig()), e(t3.currentTarget).data(n2, i4)), i4._activeTrigger.click = !i4._activeTrigger.click, i4._isWithActiveTrigger() ? i4._enter(null, i4) : i4._leave(null, i4);
      } else {
        if (e(this.getTipElement()).hasClass("show")) return void this._leave(null, this);
        this._enter(null, this);
      }
    }, i3.dispose = function() {
      clearTimeout(this._timeout), e.removeData(this.element, this.constructor.DATA_KEY), e(this.element).off(this.constructor.EVENT_KEY), e(this.element).closest(".modal").off("hide.bs.modal", this._hideModalHandler), this.tip && e(this.tip).remove(), this._isEnabled = null, this._timeout = null, this._hoverState = null, this._activeTrigger = null, this._popper && this._popper.destroy(), this._popper = null, this.element = null, this.config = null, this.tip = null;
    }, i3.show = function() {
      var t3 = this;
      if ("none" === e(this.element).css("display")) throw new Error("Please use show on visible elements");
      var i4 = e.Event(this.constructor.Event.SHOW);
      if (this.isWithContent() && this._isEnabled) {
        e(this.element).trigger(i4);
        var o2 = c.findShadowRoot(this.element), s2 = e.contains(null !== o2 ? o2 : this.element.ownerDocument.documentElement, this.element);
        if (i4.isDefaultPrevented() || !s2) return;
        var r2 = this.getTipElement(), a2 = c.getUID(this.constructor.NAME);
        r2.setAttribute("id", a2), this.element.setAttribute("aria-describedby", a2), this.setContent(), this.config.animation && e(r2).addClass("fade");
        var l2 = "function" == typeof this.config.placement ? this.config.placement.call(this, r2, this.element) : this.config.placement, h2 = this._getAttachment(l2);
        this.addAttachmentClass(h2);
        var u2 = this._getContainer();
        e(r2).data(this.constructor.DATA_KEY, this), e.contains(this.element.ownerDocument.documentElement, this.tip) || e(r2).appendTo(u2), e(this.element).trigger(this.constructor.Event.INSERTED), this._popper = new n(this.element, r2, this._getPopperConfig(h2)), e(r2).addClass("show"), "ontouchstart" in document.documentElement && e(document.body).children().on("mouseover", null, e.noop);
        var d2 = function() {
          t3.config.animation && t3._fixTransition();
          var n2 = t3._hoverState;
          t3._hoverState = null, e(t3.element).trigger(t3.constructor.Event.SHOWN), "out" === n2 && t3._leave(null, t3);
        };
        if (e(this.tip).hasClass("fade")) {
          var f2 = c.getTransitionDurationFromElement(this.tip);
          e(this.tip).one(c.TRANSITION_END, d2).emulateTransitionEnd(f2);
        } else d2();
      }
    }, i3.hide = function(t3) {
      var n2 = this, i4 = this.getTipElement(), o2 = e.Event(this.constructor.Event.HIDE), s2 = function() {
        "show" !== n2._hoverState && i4.parentNode && i4.parentNode.removeChild(i4), n2._cleanTipClass(), n2.element.removeAttribute("aria-describedby"), e(n2.element).trigger(n2.constructor.Event.HIDDEN), null !== n2._popper && n2._popper.destroy(), t3 && t3();
      };
      if (e(this.element).trigger(o2), !o2.isDefaultPrevented()) {
        if (e(i4).removeClass("show"), "ontouchstart" in document.documentElement && e(document.body).children().off("mouseover", null, e.noop), this._activeTrigger.click = false, this._activeTrigger.focus = false, this._activeTrigger.hover = false, e(this.tip).hasClass("fade")) {
          var r2 = c.getTransitionDurationFromElement(i4);
          e(i4).one(c.TRANSITION_END, s2).emulateTransitionEnd(r2);
        } else s2();
        this._hoverState = "";
      }
    }, i3.update = function() {
      null !== this._popper && this._popper.scheduleUpdate();
    }, i3.isWithContent = function() {
      return Boolean(this.getTitle());
    }, i3.addAttachmentClass = function(t3) {
      e(this.getTipElement()).addClass("bs-tooltip-" + t3);
    }, i3.getTipElement = function() {
      return this.tip = this.tip || e(this.config.template)[0], this.tip;
    }, i3.setContent = function() {
      var t3 = this.getTipElement();
      this.setElementContent(e(t3.querySelectorAll(".tooltip-inner")), this.getTitle()), e(t3).removeClass("fade show");
    }, i3.setElementContent = function(t3, n2) {
      "object" != typeof n2 || !n2.nodeType && !n2.jquery ? this.config.html ? (this.config.sanitize && (n2 = H(n2, this.config.whiteList, this.config.sanitizeFn)), t3.html(n2)) : t3.text(n2) : this.config.html ? e(n2).parent().is(t3) || t3.empty().append(n2) : t3.text(e(n2).text());
    }, i3.getTitle = function() {
      var t3 = this.element.getAttribute("data-original-title");
      return t3 || (t3 = "function" == typeof this.config.title ? this.config.title.call(this.element) : this.config.title), t3;
    }, i3._getPopperConfig = function(t3) {
      var e2 = this;
      return a(a({}, { placement: t3, modifiers: { offset: this._getOffset(), flip: { behavior: this.config.fallbackPlacement }, arrow: { element: ".arrow" }, preventOverflow: { boundariesElement: this.config.boundary } }, onCreate: function(t4) {
        t4.originalPlacement !== t4.placement && e2._handlePopperPlacementChange(t4);
      }, onUpdate: function(t4) {
        return e2._handlePopperPlacementChange(t4);
      } }), this.config.popperConfig);
    }, i3._getOffset = function() {
      var t3 = this, e2 = {};
      return "function" == typeof this.config.offset ? e2.fn = function(e3) {
        return e3.offsets = a(a({}, e3.offsets), t3.config.offset(e3.offsets, t3.element) || {}), e3;
      } : e2.offset = this.config.offset, e2;
    }, i3._getContainer = function() {
      return false === this.config.container ? document.body : c.isElement(this.config.container) ? e(this.config.container) : e(document).find(this.config.container);
    }, i3._getAttachment = function(t3) {
      return K[t3.toUpperCase()];
    }, i3._setListeners = function() {
      var t3 = this;
      this.config.trigger.split(" ").forEach(function(n2) {
        if ("click" === n2) e(t3.element).on(t3.constructor.Event.CLICK, t3.config.selector, function(e2) {
          return t3.toggle(e2);
        });
        else if ("manual" !== n2) {
          var i4 = "hover" === n2 ? t3.constructor.Event.MOUSEENTER : t3.constructor.Event.FOCUSIN, o2 = "hover" === n2 ? t3.constructor.Event.MOUSELEAVE : t3.constructor.Event.FOCUSOUT;
          e(t3.element).on(i4, t3.config.selector, function(e2) {
            return t3._enter(e2);
          }).on(o2, t3.config.selector, function(e2) {
            return t3._leave(e2);
          });
        }
      }), this._hideModalHandler = function() {
        t3.element && t3.hide();
      }, e(this.element).closest(".modal").on("hide.bs.modal", this._hideModalHandler), this.config.selector ? this.config = a(a({}, this.config), {}, { trigger: "manual", selector: "" }) : this._fixTitle();
    }, i3._fixTitle = function() {
      var t3 = typeof this.element.getAttribute("data-original-title");
      (this.element.getAttribute("title") || "string" !== t3) && (this.element.setAttribute("data-original-title", this.element.getAttribute("title") || ""), this.element.setAttribute("title", ""));
    }, i3._enter = function(t3, n2) {
      var i4 = this.constructor.DATA_KEY;
      (n2 = n2 || e(t3.currentTarget).data(i4)) || (n2 = new this.constructor(t3.currentTarget, this._getDelegateConfig()), e(t3.currentTarget).data(i4, n2)), t3 && (n2._activeTrigger["focusin" === t3.type ? "focus" : "hover"] = true), e(n2.getTipElement()).hasClass("show") || "show" === n2._hoverState ? n2._hoverState = "show" : (clearTimeout(n2._timeout), n2._hoverState = "show", n2.config.delay && n2.config.delay.show ? n2._timeout = setTimeout(function() {
        "show" === n2._hoverState && n2.show();
      }, n2.config.delay.show) : n2.show());
    }, i3._leave = function(t3, n2) {
      var i4 = this.constructor.DATA_KEY;
      (n2 = n2 || e(t3.currentTarget).data(i4)) || (n2 = new this.constructor(t3.currentTarget, this._getDelegateConfig()), e(t3.currentTarget).data(i4, n2)), t3 && (n2._activeTrigger["focusout" === t3.type ? "focus" : "hover"] = false), n2._isWithActiveTrigger() || (clearTimeout(n2._timeout), n2._hoverState = "out", n2.config.delay && n2.config.delay.hide ? n2._timeout = setTimeout(function() {
        "out" === n2._hoverState && n2.hide();
      }, n2.config.delay.hide) : n2.hide());
    }, i3._isWithActiveTrigger = function() {
      for (var t3 in this._activeTrigger) if (this._activeTrigger[t3]) return true;
      return false;
    }, i3._getConfig = function(t3) {
      var n2 = e(this.element).data();
      return Object.keys(n2).forEach(function(t4) {
        -1 !== V.indexOf(t4) && delete n2[t4];
      }), "number" == typeof (t3 = a(a(a({}, this.constructor.Default), n2), "object" == typeof t3 && t3 ? t3 : {})).delay && (t3.delay = { show: t3.delay, hide: t3.delay }), "number" == typeof t3.title && (t3.title = t3.title.toString()), "number" == typeof t3.content && (t3.content = t3.content.toString()), c.typeCheckConfig(U, t3, this.constructor.DefaultType), t3.sanitize && (t3.template = H(t3.template, t3.whiteList, t3.sanitizeFn)), t3;
    }, i3._getDelegateConfig = function() {
      var t3 = {};
      if (this.config) for (var e2 in this.config) this.constructor.Default[e2] !== this.config[e2] && (t3[e2] = this.config[e2]);
      return t3;
    }, i3._cleanTipClass = function() {
      var t3 = e(this.getTipElement()), n2 = t3.attr("class").match(W);
      null !== n2 && n2.length && t3.removeClass(n2.join(""));
    }, i3._handlePopperPlacementChange = function(t3) {
      this.tip = t3.instance.popper, this._cleanTipClass(), this.addAttachmentClass(this._getAttachment(t3.placement));
    }, i3._fixTransition = function() {
      var t3 = this.getTipElement(), n2 = this.config.animation;
      null === t3.getAttribute("x-placement") && (e(t3).removeClass("fade"), this.config.animation = false, this.hide(), this.show(), this.config.animation = n2);
    }, t2._jQueryInterface = function(n2) {
      return this.each(function() {
        var i4 = e(this).data("bs.tooltip"), o2 = "object" == typeof n2 && n2;
        if ((i4 || !/dispose|hide/.test(n2)) && (i4 || (i4 = new t2(this, o2), e(this).data("bs.tooltip", i4)), "string" == typeof n2)) {
          if ("undefined" == typeof i4[n2]) throw new TypeError('No method named "' + n2 + '"');
          i4[n2]();
        }
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return X;
    } }, { key: "NAME", get: function() {
      return U;
    } }, { key: "DATA_KEY", get: function() {
      return "bs.tooltip";
    } }, { key: "Event", get: function() {
      return Y;
    } }, { key: "EVENT_KEY", get: function() {
      return ".bs.tooltip";
    } }, { key: "DefaultType", get: function() {
      return z;
    } }]), t2;
  }();
  e.fn[U] = $2._jQueryInterface, e.fn[U].Constructor = $2, e.fn[U].noConflict = function() {
    return e.fn[U] = M, $2._jQueryInterface;
  };
  var J = "popover", G = e.fn[J], Z = new RegExp("(^|\\s)bs-popover\\S+", "g"), tt = a(a({}, $2.Default), {}, { placement: "right", trigger: "click", content: "", template: '<div class="popover" role="tooltip"><div class="arrow"></div><h3 class="popover-header"></h3><div class="popover-body"></div></div>' }), et = a(a({}, $2.DefaultType), {}, { content: "(string|element|function)" }), nt = { HIDE: "hide.bs.popover", HIDDEN: "hidden.bs.popover", SHOW: "show.bs.popover", SHOWN: "shown.bs.popover", INSERTED: "inserted.bs.popover", CLICK: "click.bs.popover", FOCUSIN: "focusin.bs.popover", FOCUSOUT: "focusout.bs.popover", MOUSEENTER: "mouseenter.bs.popover", MOUSELEAVE: "mouseleave.bs.popover" }, it = function(t2) {
    var n2, i3;
    function s2() {
      return t2.apply(this, arguments) || this;
    }
    i3 = t2, (n2 = s2).prototype = Object.create(i3.prototype), n2.prototype.constructor = n2, n2.__proto__ = i3;
    var r2 = s2.prototype;
    return r2.isWithContent = function() {
      return this.getTitle() || this._getContent();
    }, r2.addAttachmentClass = function(t3) {
      e(this.getTipElement()).addClass("bs-popover-" + t3);
    }, r2.getTipElement = function() {
      return this.tip = this.tip || e(this.config.template)[0], this.tip;
    }, r2.setContent = function() {
      var t3 = e(this.getTipElement());
      this.setElementContent(t3.find(".popover-header"), this.getTitle());
      var n3 = this._getContent();
      "function" == typeof n3 && (n3 = n3.call(this.element)), this.setElementContent(t3.find(".popover-body"), n3), t3.removeClass("fade show");
    }, r2._getContent = function() {
      return this.element.getAttribute("data-content") || this.config.content;
    }, r2._cleanTipClass = function() {
      var t3 = e(this.getTipElement()), n3 = t3.attr("class").match(Z);
      null !== n3 && n3.length > 0 && t3.removeClass(n3.join(""));
    }, s2._jQueryInterface = function(t3) {
      return this.each(function() {
        var n3 = e(this).data("bs.popover"), i4 = "object" == typeof t3 ? t3 : null;
        if ((n3 || !/dispose|hide/.test(t3)) && (n3 || (n3 = new s2(this, i4), e(this).data("bs.popover", n3)), "string" == typeof t3)) {
          if ("undefined" == typeof n3[t3]) throw new TypeError('No method named "' + t3 + '"');
          n3[t3]();
        }
      });
    }, o(s2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return tt;
    } }, { key: "NAME", get: function() {
      return J;
    } }, { key: "DATA_KEY", get: function() {
      return "bs.popover";
    } }, { key: "Event", get: function() {
      return nt;
    } }, { key: "EVENT_KEY", get: function() {
      return ".bs.popover";
    } }, { key: "DefaultType", get: function() {
      return et;
    } }]), s2;
  }($2);
  e.fn[J] = it._jQueryInterface, e.fn[J].Constructor = it, e.fn[J].noConflict = function() {
    return e.fn[J] = G, it._jQueryInterface;
  };
  var ot = "scrollspy", st = e.fn[ot], rt = { offset: 10, method: "auto", target: "" }, at = { offset: "number", method: "string", target: "(string|element)" }, lt = function() {
    function t2(t3, n3) {
      var i3 = this;
      this._element = t3, this._scrollElement = "BODY" === t3.tagName ? window : t3, this._config = this._getConfig(n3), this._selector = this._config.target + " .nav-link," + this._config.target + " .list-group-item," + this._config.target + " .dropdown-item", this._offsets = [], this._targets = [], this._activeTarget = null, this._scrollHeight = 0, e(this._scrollElement).on("scroll.bs.scrollspy", function(t4) {
        return i3._process(t4);
      }), this.refresh(), this._process();
    }
    var n2 = t2.prototype;
    return n2.refresh = function() {
      var t3 = this, n3 = this._scrollElement === this._scrollElement.window ? "offset" : "position", i3 = "auto" === this._config.method ? n3 : this._config.method, o2 = "position" === i3 ? this._getScrollTop() : 0;
      this._offsets = [], this._targets = [], this._scrollHeight = this._getScrollHeight(), [].slice.call(document.querySelectorAll(this._selector)).map(function(t4) {
        var n4, s2 = c.getSelectorFromElement(t4);
        if (s2 && (n4 = document.querySelector(s2)), n4) {
          var r2 = n4.getBoundingClientRect();
          if (r2.width || r2.height) return [e(n4)[i3]().top + o2, s2];
        }
        return null;
      }).filter(function(t4) {
        return t4;
      }).sort(function(t4, e2) {
        return t4[0] - e2[0];
      }).forEach(function(e2) {
        t3._offsets.push(e2[0]), t3._targets.push(e2[1]);
      });
    }, n2.dispose = function() {
      e.removeData(this._element, "bs.scrollspy"), e(this._scrollElement).off(".bs.scrollspy"), this._element = null, this._scrollElement = null, this._config = null, this._selector = null, this._offsets = null, this._targets = null, this._activeTarget = null, this._scrollHeight = null;
    }, n2._getConfig = function(t3) {
      if ("string" != typeof (t3 = a(a({}, rt), "object" == typeof t3 && t3 ? t3 : {})).target && c.isElement(t3.target)) {
        var n3 = e(t3.target).attr("id");
        n3 || (n3 = c.getUID(ot), e(t3.target).attr("id", n3)), t3.target = "#" + n3;
      }
      return c.typeCheckConfig(ot, t3, at), t3;
    }, n2._getScrollTop = function() {
      return this._scrollElement === window ? this._scrollElement.pageYOffset : this._scrollElement.scrollTop;
    }, n2._getScrollHeight = function() {
      return this._scrollElement.scrollHeight || Math.max(document.body.scrollHeight, document.documentElement.scrollHeight);
    }, n2._getOffsetHeight = function() {
      return this._scrollElement === window ? window.innerHeight : this._scrollElement.getBoundingClientRect().height;
    }, n2._process = function() {
      var t3 = this._getScrollTop() + this._config.offset, e2 = this._getScrollHeight(), n3 = this._config.offset + e2 - this._getOffsetHeight();
      if (this._scrollHeight !== e2 && this.refresh(), t3 >= n3) {
        var i3 = this._targets[this._targets.length - 1];
        this._activeTarget !== i3 && this._activate(i3);
      } else {
        if (this._activeTarget && t3 < this._offsets[0] && this._offsets[0] > 0) return this._activeTarget = null, void this._clear();
        for (var o2 = this._offsets.length; o2--; ) {
          this._activeTarget !== this._targets[o2] && t3 >= this._offsets[o2] && ("undefined" == typeof this._offsets[o2 + 1] || t3 < this._offsets[o2 + 1]) && this._activate(this._targets[o2]);
        }
      }
    }, n2._activate = function(t3) {
      this._activeTarget = t3, this._clear();
      var n3 = this._selector.split(",").map(function(e2) {
        return e2 + '[data-target="' + t3 + '"],' + e2 + '[href="' + t3 + '"]';
      }), i3 = e([].slice.call(document.querySelectorAll(n3.join(","))));
      i3.hasClass("dropdown-item") ? (i3.closest(".dropdown").find(".dropdown-toggle").addClass("active"), i3.addClass("active")) : (i3.addClass("active"), i3.parents(".nav, .list-group").prev(".nav-link, .list-group-item").addClass("active"), i3.parents(".nav, .list-group").prev(".nav-item").children(".nav-link").addClass("active")), e(this._scrollElement).trigger("activate.bs.scrollspy", { relatedTarget: t3 });
    }, n2._clear = function() {
      [].slice.call(document.querySelectorAll(this._selector)).filter(function(t3) {
        return t3.classList.contains("active");
      }).forEach(function(t3) {
        return t3.classList.remove("active");
      });
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this).data("bs.scrollspy");
        if (i3 || (i3 = new t2(this, "object" == typeof n3 && n3), e(this).data("bs.scrollspy", i3)), "string" == typeof n3) {
          if ("undefined" == typeof i3[n3]) throw new TypeError('No method named "' + n3 + '"');
          i3[n3]();
        }
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "Default", get: function() {
      return rt;
    } }]), t2;
  }();
  e(window).on("load.bs.scrollspy.data-api", function() {
    for (var t2 = [].slice.call(document.querySelectorAll('[data-spy="scroll"]')), n2 = t2.length; n2--; ) {
      var i3 = e(t2[n2]);
      lt._jQueryInterface.call(i3, i3.data());
    }
  }), e.fn[ot] = lt._jQueryInterface, e.fn[ot].Constructor = lt, e.fn[ot].noConflict = function() {
    return e.fn[ot] = st, lt._jQueryInterface;
  };
  var ct = e.fn.tab, ht = function() {
    function t2(t3) {
      this._element = t3;
    }
    var n2 = t2.prototype;
    return n2.show = function() {
      var t3 = this;
      if (!(this._element.parentNode && this._element.parentNode.nodeType === Node.ELEMENT_NODE && e(this._element).hasClass("active") || e(this._element).hasClass("disabled"))) {
        var n3, i3, o2 = e(this._element).closest(".nav, .list-group")[0], s2 = c.getSelectorFromElement(this._element);
        if (o2) {
          var r2 = "UL" === o2.nodeName || "OL" === o2.nodeName ? "> li > .active" : ".active";
          i3 = (i3 = e.makeArray(e(o2).find(r2)))[i3.length - 1];
        }
        var a2 = e.Event("hide.bs.tab", { relatedTarget: this._element }), l2 = e.Event("show.bs.tab", { relatedTarget: i3 });
        if (i3 && e(i3).trigger(a2), e(this._element).trigger(l2), !l2.isDefaultPrevented() && !a2.isDefaultPrevented()) {
          s2 && (n3 = document.querySelector(s2)), this._activate(this._element, o2);
          var h2 = function() {
            var n4 = e.Event("hidden.bs.tab", { relatedTarget: t3._element }), o3 = e.Event("shown.bs.tab", { relatedTarget: i3 });
            e(i3).trigger(n4), e(t3._element).trigger(o3);
          };
          n3 ? this._activate(n3, n3.parentNode, h2) : h2();
        }
      }
    }, n2.dispose = function() {
      e.removeData(this._element, "bs.tab"), this._element = null;
    }, n2._activate = function(t3, n3, i3) {
      var o2 = this, s2 = (!n3 || "UL" !== n3.nodeName && "OL" !== n3.nodeName ? e(n3).children(".active") : e(n3).find("> li > .active"))[0], r2 = i3 && s2 && e(s2).hasClass("fade"), a2 = function() {
        return o2._transitionComplete(t3, s2, i3);
      };
      if (s2 && r2) {
        var l2 = c.getTransitionDurationFromElement(s2);
        e(s2).removeClass("show").one(c.TRANSITION_END, a2).emulateTransitionEnd(l2);
      } else a2();
    }, n2._transitionComplete = function(t3, n3, i3) {
      if (n3) {
        e(n3).removeClass("active");
        var o2 = e(n3.parentNode).find("> .dropdown-menu .active")[0];
        o2 && e(o2).removeClass("active"), "tab" === n3.getAttribute("role") && n3.setAttribute("aria-selected", false);
      }
      if (e(t3).addClass("active"), "tab" === t3.getAttribute("role") && t3.setAttribute("aria-selected", true), c.reflow(t3), t3.classList.contains("fade") && t3.classList.add("show"), t3.parentNode && e(t3.parentNode).hasClass("dropdown-menu")) {
        var s2 = e(t3).closest(".dropdown")[0];
        if (s2) {
          var r2 = [].slice.call(s2.querySelectorAll(".dropdown-toggle"));
          e(r2).addClass("active");
        }
        t3.setAttribute("aria-expanded", true);
      }
      i3 && i3();
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this), o2 = i3.data("bs.tab");
        if (o2 || (o2 = new t2(this), i3.data("bs.tab", o2)), "string" == typeof n3) {
          if ("undefined" == typeof o2[n3]) throw new TypeError('No method named "' + n3 + '"');
          o2[n3]();
        }
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }]), t2;
  }();
  e(document).on("click.bs.tab.data-api", '[data-toggle="tab"], [data-toggle="pill"], [data-toggle="list"]', function(t2) {
    t2.preventDefault(), ht._jQueryInterface.call(e(this), "show");
  }), e.fn.tab = ht._jQueryInterface, e.fn.tab.Constructor = ht, e.fn.tab.noConflict = function() {
    return e.fn.tab = ct, ht._jQueryInterface;
  };
  var ut = e.fn.toast, dt = { animation: "boolean", autohide: "boolean", delay: "number" }, ft = { animation: true, autohide: true, delay: 500 }, gt = function() {
    function t2(t3, e2) {
      this._element = t3, this._config = this._getConfig(e2), this._timeout = null, this._setListeners();
    }
    var n2 = t2.prototype;
    return n2.show = function() {
      var t3 = this, n3 = e.Event("show.bs.toast");
      if (e(this._element).trigger(n3), !n3.isDefaultPrevented()) {
        this._config.animation && this._element.classList.add("fade");
        var i3 = function() {
          t3._element.classList.remove("showing"), t3._element.classList.add("show"), e(t3._element).trigger("shown.bs.toast"), t3._config.autohide && (t3._timeout = setTimeout(function() {
            t3.hide();
          }, t3._config.delay));
        };
        if (this._element.classList.remove("hide"), c.reflow(this._element), this._element.classList.add("showing"), this._config.animation) {
          var o2 = c.getTransitionDurationFromElement(this._element);
          e(this._element).one(c.TRANSITION_END, i3).emulateTransitionEnd(o2);
        } else i3();
      }
    }, n2.hide = function() {
      if (this._element.classList.contains("show")) {
        var t3 = e.Event("hide.bs.toast");
        e(this._element).trigger(t3), t3.isDefaultPrevented() || this._close();
      }
    }, n2.dispose = function() {
      clearTimeout(this._timeout), this._timeout = null, this._element.classList.contains("show") && this._element.classList.remove("show"), e(this._element).off("click.dismiss.bs.toast"), e.removeData(this._element, "bs.toast"), this._element = null, this._config = null;
    }, n2._getConfig = function(t3) {
      return t3 = a(a(a({}, ft), e(this._element).data()), "object" == typeof t3 && t3 ? t3 : {}), c.typeCheckConfig("toast", t3, this.constructor.DefaultType), t3;
    }, n2._setListeners = function() {
      var t3 = this;
      e(this._element).on("click.dismiss.bs.toast", '[data-dismiss="toast"]', function() {
        return t3.hide();
      });
    }, n2._close = function() {
      var t3 = this, n3 = function() {
        t3._element.classList.add("hide"), e(t3._element).trigger("hidden.bs.toast");
      };
      if (this._element.classList.remove("show"), this._config.animation) {
        var i3 = c.getTransitionDurationFromElement(this._element);
        e(this._element).one(c.TRANSITION_END, n3).emulateTransitionEnd(i3);
      } else n3();
    }, t2._jQueryInterface = function(n3) {
      return this.each(function() {
        var i3 = e(this), o2 = i3.data("bs.toast");
        if (o2 || (o2 = new t2(this, "object" == typeof n3 && n3), i3.data("bs.toast", o2)), "string" == typeof n3) {
          if ("undefined" == typeof o2[n3]) throw new TypeError('No method named "' + n3 + '"');
          o2[n3](this);
        }
      });
    }, o(t2, null, [{ key: "VERSION", get: function() {
      return "4.5.0";
    } }, { key: "DefaultType", get: function() {
      return dt;
    } }, { key: "Default", get: function() {
      return ft;
    } }]), t2;
  }();
  e.fn.toast = gt._jQueryInterface, e.fn.toast.Constructor = gt, e.fn.toast.noConflict = function() {
    return e.fn.toast = ut, gt._jQueryInterface;
  }, t.Alert = d, t.Button = g, t.Carousel = E, t.Collapse = D, t.Dropdown = j, t.Modal = R, t.Popover = it, t.Scrollspy = lt, t.Tab = ht, t.Toast = gt, t.Tooltip = $2, t.Util = c, Object.defineProperty(t, "__esModule", { value: true });
});
var suppressDropdownFocus = null;
function getDropdownKey(x) {
  return x.replace(/^#/, "");
}
function getDropdownElement(x) {
  var key = getDropdownKey(x);
  return document.getElementById(x) || document.getElementById(key) || document.getElementById("#" + key);
}
function showDropdown(x) {
  var dropdown = getDropdownElement(x);
  if (!dropdown) return;
  if (dropdown.style.display !== "block") {
    if (getDropdownKey(x) === "helpMenu") {
      matomoTrackEvent(MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER, `${MatomoTrackingPhrases.OPENED_MEGAMENU} - Help - ${MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER}`);
    } else if (getDropdownKey(x) === "qualityProgramsMenu") {
      matomoTrackEvent(MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER, `${MatomoTrackingPhrases.OPENED_MEGAMENU} - QP - ${MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER}`);
    }
  }
  dropdown.style.display = "block";
}
function hideDropdown(x) {
  var dropdown = getDropdownElement(x);
  if (!dropdown) return;
  dropdown.style.display = "none";
}
function closeDropdown(x) {
  suppressDropdownFocus = getDropdownKey(x);
  hideDropdown(x);
}
function clearDropdownFocusSuppress(x) {
  if (suppressDropdownFocus === getDropdownKey(x)) {
    suppressDropdownFocus = null;
  }
}
function handleDropdownBlur(x, event2) {
  clearDropdownFocusSuppress(x);
  const dropdown = getDropdownElement(x);
  const movingIntoMenu = dropdown && event2 && event2.relatedTarget && dropdown.contains(event2.relatedTarget);
  if (!movingIntoMenu) {
    hideDropdown(x);
  }
}
function handleDropdownTrigger(x, otherMenu, event2) {
  if (suppressDropdownFocus === getDropdownKey(x)) {
    hideDropdown(x);
    if (!event2 || event2.type !== "click") {
      return;
    }
  }
  suppressDropdownFocus = null;
  showDropdown(x);
  hideDropdown(otherMenu);
}
var showObject = false;
function toggleMobileNav(x) {
  showObject = !showObject;
  var objects = document.getElementsByClassName(x);
  if (showObject) {
    for (i = 0; i < objects.length; i++) {
      objects[i].style.display = "block";
    }
  } else {
    for (i = 0; i < objects.length; i++) {
      objects[i].style.display = "none";
    }
  }
}
function showMore() {
  var moreText = document.getElementById("moreText");
  var moreButton = document.getElementById("moreButton");
  if (moreText.classList.contains("d-none")) {
    moreText.classList.remove("d-none");
    moreButton.innerHTML = "Read less";
  } else {
    moreText.classList.add("d-none");
    moreButton.innerHTML = "Read more";
  }
}
function onClickSearch() {
  if (event.keyCode == 13) {
    if (event.currentTarget.value && event.currentTarget.value.trim() !== "") {
      matomoTrackEvent(MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER, `Search - ${MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER}`, window.location.pathname);
      document.getElementById("qnpNavSearchBtn").click();
    }
  }
}
;
$(document).on("input", "#qnpNavSearch", function() {
  const hasText = $(this).val().trim() !== "";
  $("#search-clear").toggle(hasText);
});
$(document).on("click", "#search-clear", function() {
  $("input.search-bar").val("").trigger("input");
  $("input.search-bar").focus();
});
function onClickFooterLink(linkName) {
  matomoTrackEvent(
    MatomoTrackingPhrases.PUBLIC_GLOBAL_FOOTER,
    MatomoTrackingPhrases.OPENED_LINK + " - " + linkName + " - " + MatomoTrackingPhrases.PUBLIC_GLOBAL_FOOTER
  );
}
function onClickMenuLink(linkName, menuType) {
  matomoTrackEvent(
    MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER,
    MatomoTrackingPhrases.OPENED_LINK + " - " + linkName + " - " + menuType + " " + MatomoTrackingPhrases.MEGAMENU
  );
}
function onClickRegisterLink() {
  matomoTrackEvent(
    MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER,
    MatomoTrackingPhrases.OPENED_LINK + " - Register - " + MatomoTrackingPhrases.PUBLIC_GLOBAL_HEADER
  );
}
function onClickLandingPageCard(linkName) {
  matomoTrackEvent(
    MatomoTrackingPhrases.PUBLIC_LANDING_PAGE_CARDS,
    MatomoTrackingPhrases.OPENED_LINK + " - " + linkName + " - " + MatomoTrackingPhrases.LANDING_PAGE_CARDS
  );
}
function rfc() {
  var f = document.hdcreg;
  if (f.name.value == "") {
    alert("Please enter your name");
    return false;
  }
  if (f.email.value == "" || f.ver_email.value == "") {
    alert("Pleae enter your email and verify email address");
    return false;
  }
  if (f.email.value != f.ver_email.value) {
    alert("Verify email address does not match email address.");
    return false;
  }
  if (f.pw.value == "" || f.pwv.value == "") {
    alert("Please check that both your Password and verify Password address are entered");
    f.pw.focus();
    return false;
  }
  if (f.pw.value.toLowerCase() != f.pwv.value.toLowerCase()) {
    alert("The Password fields do not match");
    f.ver_pw.focus();
    return false;
  }
  if (!listCheck()) {
    alert("Please choose a list");
    return false;
  }
  return true;
}
function listCheck() {
  var ck = false;
  var f = document.hdcreg;
  for (var i2 = 0; i2 < f.length; i2++) {
    if (f.elements[i2].type == "checkbox") {
      if (document.hdcreg.elements[i2].checked == true) {
        ck = true;
        break;
      }
    }
  }
  return ck;
}
function listservepop() {
  window.open(
    "/dcs/ContentServer?pagename=QnetPublic/ListServe/ListServeF",
    "Window1",
    "menubar=no,width=460,height=360,toolbar=no"
  );
}
//# sourceMappingURL=scripts.js.map
