# Pulse 01

## Objective

Remove hardcoded response-intake class lists from the CLI.

## Done

- Added ordered wire values and intake meanings to `PerformanceDemandResponseClass`.
- Rendered intake template response classes from `PerformanceDemandResponseClass::all_classes()`.
- Rendered intake schema allowed classes from the same core metadata.
- Added review and VTRACE rows for `WP-TAX-046`.
