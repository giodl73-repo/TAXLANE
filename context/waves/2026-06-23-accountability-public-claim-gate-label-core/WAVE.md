# Accountability Public Claim Gate Label Core Wave

## Goal

Move public-claim gate labels into reusable Rust core constants.

## Scope

- Add core labels for public-claim allowed and blocked states.
- Use core labels in checklist row generation and validation.
- Use core labels in response-log validation.
- Use core labels in CLI checklist rendering.
- Close VTRACE work package `WP-TAX-048`.

## Status

Complete.
