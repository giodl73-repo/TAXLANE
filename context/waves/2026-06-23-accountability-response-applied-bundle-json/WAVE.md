# Accountability Response Applied Bundle JSON

## Goal

Generate a machine-readable manifest for the applied response importer fixture bundle.

## Scope

- Add a reusable core manifest contract for applied response bundle metadata.
- Serialize the applied bundle manifest as JSON for UI/API consumers.
- Validate manifest membership, counts, boundaries, and README/artifact-map discovery.

## Status

Complete.
