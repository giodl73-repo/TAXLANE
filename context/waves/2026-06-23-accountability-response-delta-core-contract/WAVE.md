# Accountability Response Delta Core Contract

## Goal

Move response delta comparison into the reusable Rust core crate.

## Scope

- Add `PerformanceDemandResponseDeltaRow` to `taxlane-core`.
- Validate before/after response-log row matching and blocked claim gates.
- Render the applied response delta Markdown from the core type.

## Status

Complete.
