# Solver input inventory

Machine record:
`data/derived/breadth_benchmark_matrix/solver_input_inventory.v1.draft.json`

Pulse 101 turns the solver gate into a checklist. It names each required solver
input, the artifact that currently gives partial context, and the missing
evidence that still blocks execution.

Current status: the solver is not ready.

Largest blockers:

- OASDI annual fund path;
- Medicare HI annual fund path;
- transportation trust-fund annual values;
- general-fund annual path;
- reserves rule;
- explicit interfund transfers;
- credited offsetting collections;
- endogenous net-interest formula;
- assigned receipt bases;
- distributional placeholder.

This is a solver input inventory, not a solver run, not target-cost selection, not rate calculation, not a public rate card, not a tax proposal, not a savings estimate, not a waste finding, not a fraud finding, not a department-cut instruction, not a technology-savings claim, and not a balanced-budget claim.
